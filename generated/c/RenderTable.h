/* Generated by the schema compiler from Render.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0x14cc156665b9f146
   The TABLE wire (evolution-tolerant). */

#ifndef SCHEMA_EXAMPLE_RENDERTABLE_H
#define SCHEMA_EXAMPLE_RENDERTABLE_H

#include "Render.h"
#include "TableRuntime.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Appends RenderSprite's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_render_sprite_into( table_writer_t * w, const RenderSprite * value )
{
    if ( value->sort_key != 0 )
    {
        table_w_u16( w, 0x4fe9 ); /* sort_key */
        table_w_u8( w, 9 );
        table_w_u64( w, (serialize_uint64_t) value->sort_key );
    }
    if ( value->mesh_id != 0 )
    {
        table_w_u16( w, 0xd021 ); /* mesh_id */
        table_w_u8( w, 8 );
        table_w_u32( w, (serialize_uint32_t) value->mesh_id );
    }
    if ( value->material_id != 0 )
    {
        table_w_u16( w, 0x78c0 ); /* material_id */
        table_w_u8( w, 8 );
        table_w_u32( w, (serialize_uint32_t) value->material_id );
    }
    if ( value->layer != 0 )
    {
        table_w_u16( w, 0x4750 ); /* layer */
        table_w_u8( w, 6 );
        table_w_u8( w, (serialize_uint8_t) value->layer );
    }
    if ( value->team != TEAM_NONE )
    {
        table_w_u16( w, 0xdff1 ); /* team */
        table_w_u8( w, 6 );
        table_w_u8( w, (serialize_uint8_t) value->team );
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes RenderSprite's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask RENDER_SPRITE_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_render_sprite( void * buffer, int bytes, const RenderSprite * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_render_sprite_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_render_sprite_from( table_reader_t * r, RenderSprite * value, table_report_t * report )
{
    memset( value, 0, sizeof( *value ) ); /* prefill (all-zero), then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0x4fe9: /* sort_key */
            {
                if ( kind != 9 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t raw = table_r_u64( r );
                        value->sort_key = (uint64_t) raw;
                    }
                }
                break;
            }
            case 0xd021: /* mesh_id */
            {
                if ( kind != 8 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint32_t raw = table_r_u32( r );
                        value->mesh_id = (uint32_t) raw;
                    }
                }
                break;
            }
            case 0x78c0: /* material_id */
            {
                if ( kind != 8 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint32_t raw = table_r_u32( r );
                        value->material_id = (uint32_t) raw;
                    }
                }
                break;
            }
            case 0x4750: /* layer */
            {
                if ( kind != 6 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint8_t raw = table_r_u8( r );
                        value->layer = (uint8_t) raw;
                    }
                }
                break;
            }
            case 0xdff1: /* team */
            {
                if ( kind != 6 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint8_t raw = table_r_u8( r );
                        Team v = raw;
                        if ( (serialize_uint64_t) v > 2 ) { v = 0; report->clamped++; }
                        value->team = v;
                    }
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_render_sprite( const void * buffer, int bytes, RenderSprite * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_render_sprite_from( &r, value, report );
}

/* Appends RenderBlock's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_render_block_into( table_writer_t * w, const RenderBlock * value )
{
    if ( value->worker_index != 0 )
    {
        table_w_u16( w, 0xc58e ); /* worker_index */
        table_w_u8( w, 8 );
        table_w_u32( w, (serialize_uint32_t) value->worker_index );
    }
    if ( value->sprite_count_hint != 0 )
    {
        table_w_u16( w, 0xe26a ); /* sprite_count_hint */
        table_w_u8( w, 8 );
        table_w_u32( w, (serialize_uint32_t) value->sprite_count_hint );
    }
    if ( value->sprites_count > 0 )
    {
        int len_at;
        int32_t i;
        table_w_u16( w, 0xd0ec ); /* sprites */
        table_w_u8( w, 14 );
        len_at = w->length;
        table_w_u32( w, 0 );
        table_w_u8( w, 13 );
        table_w_u16( w, (serialize_uint16_t) value->sprites_count );
        for ( i = 0; i < value->sprites_count; i++ )
        {
            {
                int elem_len_at = w->length;
                table_w_u32( w, 0 );
                table_write_render_sprite_into( w, &value->sprites[i] );
                table_w_patch32( w, elem_len_at, (serialize_uint32_t) ( w->length - elem_len_at - 4 ) );
            }
        }
        table_w_patch32( w, len_at, (serialize_uint32_t) ( w->length - len_at - 4 ) );
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes RenderBlock's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask RENDER_BLOCK_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_render_block( void * buffer, int bytes, const RenderBlock * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_render_block_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_render_block_from( table_reader_t * r, RenderBlock * value, table_report_t * report )
{
    memset( value, 0, sizeof( *value ) ); /* prefill (all-zero), then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0xc58e: /* worker_index */
            {
                if ( kind != 8 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint32_t raw = table_r_u32( r );
                        value->worker_index = (uint32_t) raw;
                    }
                }
                break;
            }
            case 0xe26a: /* sprite_count_hint */
            {
                if ( kind != 8 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint32_t raw = table_r_u32( r );
                        value->sprite_count_hint = (uint32_t) raw;
                    }
                }
                break;
            }
            case 0xd0ec: /* sprites */
            {
                if ( kind != 14 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    int body_len;
                    int body_end;
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    body_len = (int) table_r_u32( r );
                    if ( !table_r_has( r, body_len ) ) { report->malformed = 1; return 0; }
                    body_end = r->off + body_len;
                    if ( body_len >= 3 )
                    {
                        serialize_uint8_t elem_kind = table_r_u8( r );
                        int count = (int) table_r_u16( r );
                        if ( elem_kind != 13 )
                        {
                            report->kind_mismatch++;
                        }
                        else
                        {
                            int keep = count;
                            int i;
                            if ( keep > 64 ) { keep = 64; report->clamped++; }
                            for ( i = 0; i < keep; i++ )
                            {
                                if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                                {
                                    int elem_len = (int) table_r_u32( r );
                                    table_reader_t sub;
                                    if ( !table_r_has( r, elem_len ) ) { report->malformed = 1; return 0; }
                                    sub.data = r->data + r->off;
                                    sub.length = elem_len;
                                    sub.off = 0;
                                    table_read_render_sprite_from( &sub, &value->sprites[i], report );
                                    r->off += elem_len;
                                }
                            }
                            value->sprites_count = (int32_t) keep;
                        }
                    }
                    r->off = body_end; /* excess elements and slack skip via the length */
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_render_block( const void * buffer, int bytes, RenderBlock * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_render_block_from( &r, value, report );
}

/* ---- reflection descriptors (tables only) ---- */

static const table_field_info_t TABLE_TYPE_RENDER_SPRITE_FIELDS[] = {
    { "sort_key", "uint64", 0x4fe9, 9, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "mesh_id", "uint32", 0xd021, 8, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "material_id", "uint32", 0x78c0, 8, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "layer", "uint8", 0x4750, 6, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "team", "Team", 0xdff1, 6, 0, 0, 0, NULL, 0, 0.0, 0.0, 2, enum_name_team_dyn, "" },
};

static const table_type_info_t TABLE_TYPE_RENDER_SPRITE = {
    "RenderSprite",
    TABLE_TYPE_RENDER_SPRITE_FIELDS,
    5
};

/* Returns RenderSprite's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_render_sprite( void ) { return &TABLE_TYPE_RENDER_SPRITE; }

static const table_field_info_t TABLE_TYPE_RENDER_BLOCK_FIELDS[] = {
    { "worker_index", "uint32", 0xc58e, 8, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "sprite_count_hint", "uint32", 0xe26a, 8, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "sprites", "RenderSprite", 0xd0ec, 13, 1, 1, 64, &TABLE_TYPE_RENDER_SPRITE, 0, 0.0, 0.0, -1, NULL, "" },
};

static const table_type_info_t TABLE_TYPE_RENDER_BLOCK = {
    "RenderBlock",
    TABLE_TYPE_RENDER_BLOCK_FIELDS,
    3
};

/* Returns RenderBlock's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_render_block( void ) { return &TABLE_TYPE_RENDER_BLOCK; }

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_RENDERTABLE_H */
