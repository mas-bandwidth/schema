/* Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0xcaa884d7062cccc8 */

#ifndef SCHEMA_EXAMPLE_VOID_ARMSWIRE_H
#define SCHEMA_EXAMPLE_VOID_ARMSWIRE_H

#include "VoidArms.h"
#include <string.h>   /* memset, strlen */
#include "serialize.h"

#ifndef SCHEMA_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define SCHEMA_UNUSED __attribute__((unused))
#else
#define SCHEMA_UNUSED
#endif
#endif

#ifdef __cplusplus
extern "C" {
#endif

/* Every write_x/read_x returns 1 on success, 0 on failure — the stream
   latches the error, so a caller may check once at the end of a message.
   Reads REFUSE out-of-range values, never clamp, in every build. A tag on
   WRITE is asserted before it rides — caller error, gone under NDEBUG — and
   every read reconstructs the selected arm with its declared initial values
   before decoding it (SPEC §4.8, §5). */

#ifndef SCHEMA_C_SPINE_INLINE_DEFINED
#define SCHEMA_C_SPINE_INLINE_DEFINED
/* SCHEMA_C_READ_INLINE / SCHEMA_C_WRITE_INLINE — how every generated wire
   function is spelled, beside `static SCHEMA_UNUSED`: an inlining DEMAND
   (always_inline / __forceinline), the serialize family's remedy for LLVM
   refusing the header-static per-message entries into small-message call
   sites at marginal cost over the hot-callsite inline threshold. Measured
   and shipped; compilers with neither spelling get plain `static`.
   Branch-weight hints are NOT the fix here — measured in this family to
   invite the machine outliner into the hot bodies. Do not add them. */
#if defined( _MSC_VER )
#define SCHEMA_C_READ_INLINE __forceinline
#define SCHEMA_C_WRITE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_C_READ_INLINE inline __attribute__(( always_inline ))
#define SCHEMA_C_WRITE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_C_READ_INLINE
#define SCHEMA_C_WRITE_INLINE
#endif
#endif /* SCHEMA_C_SPINE_INLINE_DEFINED */

/* Writes VoidPulse. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_void_pulse( serialize_write_stream_t * stream, const VoidPulse * value )
{
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->strength, 16 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads VoidPulse. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_void_pulse( serialize_read_stream_t * stream, VoidPulse * value )
{
    /* fixed 16-bit wire: one guard, and every per-field past-end test below folds into it */
    if ( serialize_read_bits_remaining( stream ) < 16 )
    {
        return serialize_read_fail( stream );
    }

    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 16 ) )
        {
            return 0;
        }
        value->strength = (uint16_t) raw;
    }
    return 1;
}

/* Writes VoidLevel. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_void_level( serialize_write_stream_t * stream, const VoidLevel * value )
{
    serialize_assert( (serialize_int64_t) value->level <= 7 );
    if ( !serialize_write_bits( stream, (serialize_uint32_t) ( value->level ), 3 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads VoidLevel. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_void_level( serialize_read_stream_t * stream, VoidLevel * value )
{
    /* fixed 3-bit wire: one guard, and every per-field past-end test below folds into it */
    if ( serialize_read_bits_remaining( stream ) < 3 )
    {
        return serialize_read_fail( stream );
    }

    {
        serialize_uint64_t offset_value = 0;
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 3 ) )
        {
            return 0;
        }
        offset_value = raw;
        value->level = (uint8_t) offset_value;
    }
    return 1;
}

/* Writes VoidSignal. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_void_signal( serialize_write_stream_t * stream, const VoidSignal * value )
{
    serialize_assert( value->type <= VOID_SIGNAL_TYPE_MAX ); /* an out-of-set tag is caller error (SPEC §4.8, §5) */
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->type, 3 ) )
    {
        return 0;
    }
    switch ( value->type )
    {
        case 1:
            return 1; /* payload-free arm — the tag is the whole wire (SPEC §4.8) */
        case 2:
            return write_void_pulse( stream, &value->as.pulse );
        case 3:
            return write_void_level( stream, &value->as.level );
        case 4:
            return 1; /* payload-free arm — the tag is the whole wire (SPEC §4.8) */
        default:
            return 1; /* None — the tag is the whole wire (SPEC §4.8) */
    }
}

/* Reads VoidSignal. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_void_signal( serialize_read_stream_t * stream, VoidSignal * value )
{
    {
        serialize_uint32_t tag_value = 0;
        if ( !serialize_read_bits( stream, &tag_value, 3 ) )
        {
            return 0;
        }
        if ( tag_value > VOID_SIGNAL_TYPE_MAX )
        {
            return 0; /* not a wire-legal tag */
        }
        value->type = (VoidSignalType) tag_value;
    }
    switch ( value->type )
    {
        case 1:
            return 1; /* payload-free arm — the tag is the whole wire (SPEC §4.8) */
        case 2:
            memset( &value->as.pulse, 0, sizeof( value->as.pulse ) );
            return read_void_pulse( stream, &value->as.pulse );
        case 3:
            memset( &value->as.level, 0, sizeof( value->as.level ) );
            return read_void_level( stream, &value->as.level );
        case 4:
            return 1; /* payload-free arm — the tag is the whole wire (SPEC §4.8) */
        default:
            return 1; /* None */
    }
}

/* Writes VoidSignalPacket. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_void_signal_packet( serialize_write_stream_t * stream, const VoidSignalPacket * value )
{
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->lead, 3 ) )
    {
        return 0;
    }
    if ( !write_void_signal( stream, &value->signal ) )
    {
        return 0;
    }
    if ( !write_void_signal( stream, &value->echo ) )
    {
        return 0;
    }
    serialize_assert( value->history_count >= 0 && value->history_count <= 2 );
    if ( !serialize_write_int( stream, value->history_count, 0, 2 ) )
    {
        return 0;
    }
    {
        int32_t i;
        for ( i = 0; i < value->history_count; i++ )
        {
            if ( !write_void_signal( stream, &value->history[i] ) )
            {
                return 0;
            }
        }
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->tail, 3 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads VoidSignalPacket. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_void_signal_packet( serialize_read_stream_t * stream, VoidSignalPacket * value )
{
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 3 ) )
        {
            return 0;
        }
        value->lead = (uint32_t) raw;
    }
    if ( !read_void_signal( stream, &value->signal ) )
    {
        return 0;
    }
    if ( !read_void_signal( stream, &value->echo ) )
    {
        return 0;
    }
    if ( !serialize_read_int( stream, &value->history_count, 0, 2 ) )
    {
        return 0;
    }
    {
        int32_t i;
        for ( i = 0; i < value->history_count; i++ )
        {
            if ( !read_void_signal( stream, &value->history[i] ) )
            {
                return 0;
            }
        }
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 3 ) )
        {
            return 0;
        }
        value->tail = (uint32_t) raw;
    }
    return 1;
}

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_VOID_ARMSWIRE_H */
