// Code generated by the schema compiler from Clauses.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x3d5823781128b414
//
// THE FLAT TIER — the shipped JavaScript wire path: a single-word 32-bit
// bitpacker inlined at every field (byte-identical wire to serialize.js),
// constant widths and masks, zero function calls. Same generated classes,
// same bytes as the runtime tier (a standing CI gate); the runtime tier
// remains the diagnostic and reference surface — re-read a failing buffer
// through it to learn WHICH operation failed and why.
//
// Write<Name>Flat(value, view) -> bytes written, or -1 when the checked
// writer refuses an out-of-contract value (the production writer trusts
// the caller — serialize.js src/mode.js's own NODE_ENV fork, frozen at
// module load). Read<Name>Flat(value, view, numBits) -> bool, the family
// read verdict; reader obligations ride in every mode.
//
// Buffers are caller-owned DataViews. Write buffers: at least <Name>MaxBytes
// (the constants live in the runtime-tier module). Read buffers: at least
// FLAT_READ_SLACK = 8 bytes past the payload — 64-bit windows load
// unconditionally; copy exactly-sized receive buffers into a persistent
// MaxBytes + 8 scratch first.

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type W13: the flat codec ----

function writeW13FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    v = (((value.Items[i0]) & 0x1fff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW13FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 12) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 8191) {
      return -1;
    }
    v = (((value.Items[i0]) & 0x1fff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW13Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W13MaxBytes.
export const WriteW13Flat = PRODUCTION ? writeW13FlatProduction : writeW13FlatChecked;

// ReadW13Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW13Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  if (v > 12) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 4;
  if (br + value.ItemsCount * 13 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x1fff) >>> 0;
    value.Items[i0] = v | 0;
    br += 13;
  }
  return true;
}

// ---- type W17: the flat codec ----

function writeW17FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    v = (((value.Items[i0]) & 0x1ffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 17;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (17 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW17FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 9) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 131071) {
      return -1;
    }
    v = (((value.Items[i0]) & 0x1ffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 17;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (17 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW17Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W17MaxBytes.
export const WriteW17Flat = PRODUCTION ? writeW17FlatProduction : writeW17FlatChecked;

// ReadW17Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW17Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  if (v > 9) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 4;
  if (br + value.ItemsCount * 17 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x1ffff) >>> 0;
    value.Items[i0] = v | 0;
    br += 17;
  }
  return true;
}

// ---- type W26: the flat codec ----

function writeW26FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    v = (((value.Items[i0]) & 0x3ffffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 26;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (26 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW26FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 6) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 67108863) {
      return -1;
    }
    v = (((value.Items[i0]) & 0x3ffffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 26;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (26 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW26Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W26MaxBytes.
export const WriteW26Flat = PRODUCTION ? writeW26FlatProduction : writeW26FlatChecked;

// ReadW26Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW26Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 6) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 3;
  if (br + value.ItemsCount * 26 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x3ffffff) >>> 0;
    value.Items[i0] = v | 0;
    br += 26;
  }
  return true;
}

// ---- type W1: the flat codec ----

function writeW1FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0x1f)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    v = (((value.Items[i0]) & 0x1)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 1;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (1 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW1FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 20) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0x1f)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 1) {
      return -1;
    }
    v = (((value.Items[i0]) & 0x1)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 1;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (1 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW1Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W1MaxBytes.
export const WriteW1Flat = PRODUCTION ? writeW1FlatProduction : writeW1FlatChecked;

// ReadW1Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW1Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1f) >>> 0;
  if (v > 20) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 5;
  if (br + value.ItemsCount * 1 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x1) >>> 0;
    value.Items[i0] = v | 0;
    br += 1;
  }
  return true;
}

// ---- type W52: the flat codec ----

function writeW52FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    SC.setBigUint64(0, value.Items[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true) & 0xfffff;
    lo = (lo | (v << sb)) >>> 0;
    sb += 20;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (20 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW52FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (value.Items[i0] > 4503599627370495n) {
      return -1;
    }
    SC.setBigUint64(0, value.Items[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true) & 0xfffff;
    lo = (lo | (v << sb)) >>> 0;
    sb += 20;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (20 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW52Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W52MaxBytes.
export const WriteW52Flat = PRODUCTION ? writeW52FlatProduction : writeW52FlatChecked;

// ReadW52Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW52Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  value.ItemsCount = v;
  br += 2;
  if (br + value.ItemsCount * 52 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0xfffff) >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    value.Items[i0] = bg;
    br += 20;
  }
  return true;
}

// ---- type W50: the flat codec ----

function writeW50FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    SC.setBigUint64(0, value.Items[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true) & 0x3ffff;
    lo = (lo | (v << sb)) >>> 0;
    sb += 18;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (18 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeW50FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (value.Items[i0] > 1125899906842623n) {
      return -1;
    }
    SC.setBigUint64(0, value.Items[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true) & 0x3ffff;
    lo = (lo | (v << sb)) >>> 0;
    sb += 18;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (18 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteW50Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold W50MaxBytes.
export const WriteW50Flat = PRODUCTION ? writeW50FlatProduction : writeW50FlatChecked;

// ReadW50Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadW50Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  value.ItemsCount = v;
  br += 2;
  if (br + value.ItemsCount * 50 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x3ffff) >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    value.Items[i0] = bg;
    br += 18;
  }
  return true;
}

// ---- type F13: the flat codec ----

function writeF13FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 7; i0++) {
    v = (((value.Items[i0]) & 0x1fff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeF13FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 7; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 8191) {
      return -1;
    }
    v = (((value.Items[i0]) & 0x1fff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteF13Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold F13MaxBytes.
export const WriteF13Flat = PRODUCTION ? writeF13FlatProduction : writeF13FlatChecked;

// ReadF13Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadF13Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 91 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 7; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x1fff) >>> 0;
    value.Items[i0] = v | 0;
    br += 13;
  }
  return true;
}

// ---- type Tri3: the flat codec ----

function writeTri3FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0x1) | ((value.B & 0x3) << 1)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTri3FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0x1) | ((value.B & 0x3) << 1)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTri3Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold Tri3MaxBytes.
export const WriteTri3Flat = PRODUCTION ? writeTri3FlatProduction : writeTri3FlatChecked;

// ReadTri3Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTri3Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1) >>> 0;
  value.A = v;
  v = (out >>> 1) & 0x3;
  value.B = v;
  return true;
}

// ---- type ArrTri3: the flat codec ----

function writeArrTri3FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x1) | ((e0.B & 0x3) << 1)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArrTri3FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 10) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = (((value.ItemsCount) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x1) | ((e0.B & 0x3) << 1)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArrTri3Flat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArrTri3MaxBytes.
export const WriteArrTri3Flat = PRODUCTION ? writeArrTri3FlatProduction : writeArrTri3FlatChecked;

// ReadArrTri3Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArrTri3Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  if (v > 10) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 4;
  if (br + value.ItemsCount * 3 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x1) >>> 0;
    e0.A = v;
    v = (out >>> 1) & 0x3;
    e0.B = v;
    br += 3;
  }
  return true;
}

// ---- type Eleven: the flat codec ----

function writeElevenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0x7) | ((value.B & 0xff) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 11;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (11 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeElevenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0x7) | ((value.B & 0xff) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 11;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (11 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteElevenFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ElevenMaxBytes.
export const WriteElevenFlat = PRODUCTION ? writeElevenFlatProduction : writeElevenFlatChecked;

// ReadElevenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadElevenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 11 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.A = v;
  v = (out >>> 3) & 0xff;
  value.B = v;
  return true;
}

// ---- type ArrEleven: the flat codec ----

function writeArrElevenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 9; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x7) | ((e0.B & 0xff) << 3)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArrElevenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 9; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x7) | ((e0.B & 0xff) << 3)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArrElevenFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArrElevenMaxBytes.
export const WriteArrElevenFlat = PRODUCTION ? writeArrElevenFlatProduction : writeArrElevenFlatChecked;

// ReadArrElevenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArrElevenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 99 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 9; i0++) {
    const e0 = value.Items[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x7) >>> 0;
    e0.A = v;
    v = (out >>> 3) & 0xff;
    e0.B = v;
    br += 11;
  }
  return true;
}

// ---- type EmptyA: the flat codec ----

function writeEmptyAFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeEmptyAFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteEmptyAFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold EmptyAMaxBytes.
export const WriteEmptyAFlat = PRODUCTION ? writeEmptyAFlatProduction : writeEmptyAFlatChecked;

// ReadEmptyAFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadEmptyAFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  return true;
}

// ---- type EmptyB: the flat codec ----

function writeEmptyBFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeEmptyBFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteEmptyBFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold EmptyBMaxBytes.
export const WriteEmptyBFlat = PRODUCTION ? writeEmptyBFlatProduction : writeEmptyBFlatChecked;

// ReadEmptyBFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadEmptyBFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  return true;
}

// ---- type HoldsEmptyUnion: the flat codec ----

function writeHoldsEmptyUnionFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Lead & 0x1f) | ((value.U.Type & 0x3) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  switch (value.U.Type) {
    case 1: {
      break;
    }
    case 2: {
      break;
    }
  }
  v = ((value.Tail & 0x7f)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeHoldsEmptyUnionFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.U.Type) || value.U.Type < 0 || value.U.Type > 2) { // the tag validates BEFORE it rides (SPEC §4.8)
    return -1;
  }
  v = ((value.Lead & 0x1f) | ((value.U.Type & 0x3) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  switch (value.U.Type) {
    case 1: {
      break;
    }
    case 2: {
      break;
    }
  }
  v = ((value.Tail & 0x7f)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteHoldsEmptyUnionFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold HoldsEmptyUnionMaxBytes.
export const WriteHoldsEmptyUnionFlat = PRODUCTION ? writeHoldsEmptyUnionFlatProduction : writeHoldsEmptyUnionFlatChecked;

// ReadHoldsEmptyUnionFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadHoldsEmptyUnionFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  br += 5;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // not a wire-legal tag (SPEC §4.8)
    return false;
  }
  value.U.Type = v;
  br += 2;
  switch (value.U.Type) {
    case 1: {
      break;
    }
    case 2: {
      break;
    }
  }
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type Strs: the flat codec ----

function writeStrsFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Lead & 0x1f) | (((value.SLength) & 0xf) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 9;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (9 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.SLength; i0 += 4) {
      v = (value.S[i0] | (value.S[i0 + 1] << 8) | (value.S[i0 + 2] << 16) | (value.S[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.SLength; i0++) {
      v = value.S[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  v = (((value.BLength) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.BLength; i0 += 4) {
      v = (value.B[i0] | (value.B[i0 + 1] << 8) | (value.B[i0 + 2] << 16) | (value.B[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.BLength; i0++) {
      v = value.B[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeStrsFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 8) { // the length guards the slice; out-of-contract writes are refused
    return -1;
  }
  v = ((value.Lead & 0x1f) | (((value.SLength) & 0xf) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 9;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (9 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.SLength; i0 += 4) {
      v = (value.S[i0] | (value.S[i0 + 1] << 8) | (value.S[i0 + 2] << 16) | (value.S[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.SLength; i0++) {
      v = value.S[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  if (!Number.isInteger(value.BLength) || value.BLength < 0 || value.BLength > 8) { // the length guards the slice; out-of-contract writes are refused
    return -1;
  }
  v = (((value.BLength) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.BLength; i0 += 4) {
      v = (value.B[i0] | (value.B[i0 + 1] << 8) | (value.B[i0 + 2] << 16) | (value.B[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.BLength; i0++) {
      v = value.B[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteStrsFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold StrsMaxBytes.
export const WriteStrsFlat = PRODUCTION ? writeStrsFlatProduction : writeStrsFlatChecked;

// ReadStrsFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadStrsFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  br += 5;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  if (v > 8) { // the length guards the slice — reject, never clamp
    return false;
  }
  value.SLength = v;
  br += 4;
  s2 = br & 7;
  if (s2 !== 0) {
    if (br + (8 - s2) > numBits) {
      return false;
    }
    if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
      return false;
    }
    br += 8 - s2;
  }
  if (br + value.SLength * 8 > numBits) {
    return false;
  }
  bi = br >>> 3;
  for (let i0 = 0; i0 < value.SLength; i0++) {
    value.S[i0] = view.getUint8(bi + i0);
  }
  br += value.SLength * 8;
  for (let i0 = 0; i0 < value.SLength; i0++) {
    if (value.S[i0] === 0) { // an interior null is content the read refuses
      return false;
    }
  }
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  if (v > 8) { // the length guards the slice — reject, never clamp
    return false;
  }
  value.BLength = v;
  br += 4;
  s2 = br & 7;
  if (s2 !== 0) {
    if (br + (8 - s2) > numBits) {
      return false;
    }
    if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
      return false;
    }
    br += 8 - s2;
  }
  if (br + value.BLength * 8 > numBits) {
    return false;
  }
  bi = br >>> 3;
  for (let i0 = 0; i0 < value.BLength; i0++) {
    value.B[i0] = view.getUint8(bi + i0);
  }
  br += value.BLength * 8;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArrNested: the flat codec ----

function writeArrNestedFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Lead & 0x1f) | (((value.ItemsCount) & 0x7) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 8;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (8 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x7) | ((e0.B & 0xff) << 3)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArrNestedFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 4) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = ((value.Lead & 0x1f) | (((value.ItemsCount) & 0x7) << 5)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 8;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (8 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    v = ((e0.A & 0x7) | ((e0.B & 0xff) << 3)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArrNestedFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArrNestedMaxBytes.
export const WriteArrNestedFlat = PRODUCTION ? writeArrNestedFlatProduction : writeArrNestedFlatChecked;

// ReadArrNestedFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArrNestedFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  br += 5;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 4) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.ItemsCount = v;
  br += 3;
  if (br + value.ItemsCount * 11 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x7) >>> 0;
    e0.A = v;
    v = (out >>> 3) & 0xff;
    e0.B = v;
    br += 11;
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type Sole: the flat codec ----

function writeSoleFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Only & 0x1fff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 13;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (13 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSoleFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Only & 0x1fff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 13;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (13 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSoleFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold SoleMaxBytes.
export const WriteSoleFlat = PRODUCTION ? writeSoleFlatProduction : writeSoleFlatChecked;

// ReadSoleFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSoleFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 13 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x1fff) >>> 0;
  value.Only = v;
  return true;
}

