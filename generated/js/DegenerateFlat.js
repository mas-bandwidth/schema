// Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x682e2a15a56b78bf

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type Vec2: the flat codec ----

function writeVec2FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setFloat64(0, value.X, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setFloat64(0, value.Y, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeVec2FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setFloat64(0, value.X, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setFloat64(0, value.Y, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteVec2Flat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold Vec2MaxBytes.
export const WriteVec2Flat = PRODUCTION ? writeVec2FlatProduction : writeVec2FlatChecked;

// ReadVec2Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadVec2Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 128 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  value.X = SC.getFloat64(0, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  value.Y = SC.getFloat64(0, true);
  return true;
}

// ---- type SpanF64: the flat codec ----

function writeSpanF64FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanF64FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanF64Flat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanF64MaxBytes.
export const WriteSpanF64Flat = PRODUCTION ? writeSpanF64FlatProduction : writeSpanF64FlatChecked;

// ReadSpanF64Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanF64Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 128 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    value.Values[i0] = SC.getFloat64(0, true);
    br += 32;
  }
  return true;
}

// ---- type SpanU64: the flat codec ----

function writeSpanU64FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanU64FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanU64Flat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanU64MaxBytes.
export const WriteSpanU64Flat = PRODUCTION ? writeSpanU64FlatProduction : writeSpanU64FlatChecked;

// ReadSpanU64Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanU64Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 128 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    value.Values[i0] = bg;
    br += 32;
  }
  return true;
}

// ---- type SpanI64: the flat codec ----

function writeSpanI64FlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanI64FlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanI64Flat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanI64MaxBytes.
export const WriteSpanI64Flat = PRODUCTION ? writeSpanI64FlatProduction : writeSpanI64FlatChecked;

// ReadSpanI64Flat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanI64Flat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 128 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    value.Values[i0] = BigInt.asIntN(64, bg);
    br += 32;
  }
  return true;
}

// ---- type SpanOne: the flat codec ----

function writeSpanOneFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 1; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanOneFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 1; i0++) {
    SC.setBigUint64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanOneFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanOneMaxBytes.
export const WriteSpanOneFlat = PRODUCTION ? writeSpanOneFlatProduction : writeSpanOneFlatChecked;

// ReadSpanOneFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanOneFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 64 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 1; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    value.Values[i0] = bg;
    br += 32;
  }
  return true;
}

// ---- type SpanChunk: the flat codec ----

function writeSpanChunkFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 4; i0++) {
    v = (((value.Values[i0]) & 0xffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 16;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (16 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanChunkFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 4; i0++) {
    v = (((value.Values[i0]) & 0xffff)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 16;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (16 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanChunkFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanChunkMaxBytes.
export const WriteSpanChunkFlat = PRODUCTION ? writeSpanChunkFlatProduction : writeSpanChunkFlatChecked;

// ReadSpanChunkFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanChunkFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 64 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 4; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0xffff) >>> 0;
    value.Values[i0] = v;
    br += 16;
  }
  return true;
}

// ---- type SpanTail: the flat codec ----

function writeSpanTailFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  v = (((value.Tail) >>> 0)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanTailFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.Values[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  v = (((value.Tail) >>> 0)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanTailFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanTailMaxBytes.
export const WriteSpanTailFlat = PRODUCTION ? writeSpanTailFlatProduction : writeSpanTailFlatChecked;

// ReadSpanTailFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanTailFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 160 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    value.Values[i0] = SC.getFloat64(0, true);
    br += 32;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  value.Tail = v;
  return true;
}

// ---- type SpanTwice: the flat codec ----

function writeSpanTwiceFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.A[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.B[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeSpanTwiceFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.A[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  for (let i0 = 0; i0 < 2; i0++) {
    SC.setFloat64(0, value.B[i0], true);
    v = SC.getUint32(0, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = SC.getUint32(4, true);
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteSpanTwiceFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold SpanTwiceMaxBytes.
export const WriteSpanTwiceFlat = PRODUCTION ? writeSpanTwiceFlatProduction : writeSpanTwiceFlatChecked;

// ReadSpanTwiceFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadSpanTwiceFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 256 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    value.A[i0] = SC.getFloat64(0, true);
    br += 32;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(0, v, true);
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    SC.setUint32(4, v, true);
    value.B[i0] = SC.getFloat64(0, true);
    br += 32;
  }
  return true;
}

// ---- type Trio: the flat codec ----

function writeTrioFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTrioFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTrioFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold TrioMaxBytes.
export const WriteTrioFlat = PRODUCTION ? writeTrioFlatProduction : writeTrioFlatChecked;

// ReadTrioFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTrioFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 64 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.A = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.B = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffffff) >>> 0;
  value.C = v;
  return true;
}

// ---- type TrioSole: the flat codec ----

function writeTrioSoleFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTrioSoleFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTrioSoleFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold TrioSoleMaxBytes.
export const WriteTrioSoleFlat = PRODUCTION ? writeTrioSoleFlatProduction : writeTrioSoleFlatChecked;

// ReadTrioSoleFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTrioSoleFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 64 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.A = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.B = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffffff) >>> 0;
  value.Inner.C = v;
  return true;
}

// ---- type TrioFirst: the flat codec ----

function writeTrioFirstFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  v = ((value.Trailer & 0xffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 16;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (16 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTrioFirstFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  v = ((value.Trailer & 0xffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 16;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (16 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTrioFirstFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold TrioFirstMaxBytes.
export const WriteTrioFirstFlat = PRODUCTION ? writeTrioFirstFlatProduction : writeTrioFirstFlatChecked;

// ReadTrioFirstFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTrioFirstFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 80 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.A = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.B = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffffff) >>> 0;
  value.Inner.C = v;
  br += 24;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffff) >>> 0;
  value.Trailer = v;
  return true;
}

// ---- type TrioStraddle: the flat codec ----

function writeTrioStraddleFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, value.Pad0, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad1, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad2, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad3, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad4, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = ((value.Pad5 & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTrioStraddleFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, value.Pad0, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad1, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad2, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad3, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  SC.setBigUint64(0, value.Pad4, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = ((value.Pad5 & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  v = ((value.Inner.A & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.B & 0xfffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 20;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (20 - sb);
  }
  v = ((value.Inner.C & 0xffffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 24;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (24 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTrioStraddleFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold TrioStraddleMaxBytes.
export const WriteTrioStraddleFlat = PRODUCTION ? writeTrioStraddleFlatProduction : writeTrioStraddleFlatChecked;

// ReadTrioStraddleFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTrioStraddleFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 408 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.Pad0 = bg;
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.Pad1 = bg;
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.Pad2 = bg;
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.Pad3 = bg;
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(0, v, true);
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.Pad4 = bg;
  br += 32;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffffff) >>> 0;
  value.Pad5 = v;
  br += 24;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.A = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfffff) >>> 0;
  value.Inner.B = v;
  br += 20;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffffff) >>> 0;
  value.Inner.C = v;
  return true;
}

