// Code generated by the schema compiler from Joins.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xdd71ce8b50d78939

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type ArmsAgree: the flat codec ----

function writeArmsAgreeFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  } else {
    v = (value.B & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmsAgreeFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  } else {
    v = (value.B & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmsAgreeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmsAgreeMaxBytes.
export const WriteArmsAgreeFlat = PRODUCTION ? writeArmsAgreeFlatProduction : writeArmsAgreeFlatChecked;

// ReadArmsAgreeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmsAgreeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 24 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Flag = v !== 0;
  if (value.Flag) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 11;
    v = (out & 0x7ff) >>> 0;
    value.A = v;
    value.B = 0;
  } else {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 11;
    v = (out & 0x7ff) >>> 0;
    value.B = v;
    value.A = 0;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArmsDisagree: the flat codec ----

function writeArmsDisagreeFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  } else {
    v = (value.B & 0x7) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmsDisagreeFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 11;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (11 - sb);
    }
  } else {
    v = (value.B & 0x7) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmsDisagreeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmsDisagreeMaxBytes.
export const WriteArmsDisagreeFlat = PRODUCTION ? writeArmsDisagreeFlatProduction : writeArmsDisagreeFlatChecked;

// ReadArmsDisagreeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmsDisagreeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 6 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Flag = v !== 0;
  if (value.Flag) {
    if (br + 11 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 11;
    v = (out & 0x7ff) >>> 0;
    value.A = v;
    value.B = 0;
  } else {
    if (br + 3 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 3;
    v = (out & 0x7) >>> 0;
    value.B = v;
    value.A = 0;
  }
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArmEmpty: the flat codec ----

function writeArmEmptyFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ffff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 19;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (19 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmEmptyFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = (value.A & 0x7ffff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 19;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (19 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmEmptyFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmEmptyMaxBytes.
export const WriteArmEmptyFlat = PRODUCTION ? writeArmEmptyFlatProduction : writeArmEmptyFlatChecked;

// ReadArmEmptyFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmEmptyFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 6 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Flag = v !== 0;
  if (value.Flag) {
    if (br + 19 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 19;
    v = (out & 0x7ffff) >>> 0;
    value.A = v;
  } else {
    value.A = 0;
  }
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArmsNested: the flat codec ----

function writeArmsNestedFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  v = value.Outer ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Outer) {
    v = value.Inner ? 1 : 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 1;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (1 - sb);
    }
    if (value.Inner) {
      v = (value.X & 0x1fffffff) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 29;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (29 - sb);
      }
    } else {
      v = (value.Y & 0x1f) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 5;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (5 - sb);
      }
    }
  } else {
    v = (value.Z & 0x1fff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  v = (value.Tail & 0x3f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 6;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (6 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmsNestedFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  v = value.Outer ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Outer) {
    v = value.Inner ? 1 : 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 1;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (1 - sb);
    }
    if (value.Inner) {
      v = (value.X & 0x1fffffff) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 29;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (29 - sb);
      }
    } else {
      v = (value.Y & 0x1f) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 5;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (5 - sb);
      }
    }
  } else {
    v = (value.Z & 0x1fff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  v = (value.Tail & 0x3f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 6;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (6 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmsNestedFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmsNestedMaxBytes.
export const WriteArmsNestedFlat = PRODUCTION ? writeArmsNestedFlatProduction : writeArmsNestedFlatChecked;

// ReadArmsNestedFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmsNestedFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 3;
  v = (out & 0x7) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Outer = v !== 0;
  if (value.Outer) {
    if (br + 1 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 1;
    v = (out & 0x1) >>> 0;
    value.Inner = v !== 0;
    if (value.Inner) {
      if (br + 29 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 29;
      v = (out & 0x1fffffff) >>> 0;
      value.X = v;
      value.Y = 0;
    } else {
      if (br + 5 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 5;
      v = (out & 0x1f) >>> 0;
      value.Y = v;
      value.X = 0;
    }
    value.Z = 0;
  } else {
    if (br + 13 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 13;
    v = (out & 0x1fff) >>> 0;
    value.Z = v;
    value.Inner = false;
    value.X = 0;
    value.Y = 0;
  }
  if (br + 6 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 6;
  v = (out & 0x3f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArmAlign: the flat codec ----

function writeArmAlignFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = ((value.SLength) & 0x7) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
    sb = (sb + 7) & -8;
    if (sb === 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      lo = 0;
      sb = 0;
    }
    for (let i0 = 0; i0 < value.SLength; i0++) {
      v = value.S[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  } else {
    v = (value.B & 0x3ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 10;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (10 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmAlignFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 4) { // the length guards the slice; out-of-contract writes are refused
      return -1;
    }
    v = ((value.SLength) & 0x7) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
    sb = (sb + 7) & -8;
    if (sb === 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      lo = 0;
      sb = 0;
    }
    for (let i0 = 0; i0 < value.SLength; i0++) {
      v = value.S[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  } else {
    v = (value.B & 0x3ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 10;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (10 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmAlignFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmAlignMaxBytes.
export const WriteArmAlignFlat = PRODUCTION ? writeArmAlignFlatProduction : writeArmAlignFlatChecked;

// ReadArmAlignFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmAlignFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 6 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Flag = v !== 0;
  if (value.Flag) {
    if (br + 3 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 3;
    v = (out & 0x7) >>> 0;
    if (v > 4) { // the length guards the slice — reject, never clamp
      return false;
    }
    value.SLength = v;
    s2 = br & 7;
    if (s2 !== 0) {
      if (br + (8 - s2) > numBits) {
        return false;
      }
      if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
        return false;
      }
      br += 8 - s2;
    }
    if (br + value.SLength * 8 > numBits) {
      return false;
    }
    bi = br >>> 3;
    for (let i0 = 0; i0 < value.SLength; i0++) {
      value.S[i0] = view.getUint8(bi + i0);
    }
    br += value.SLength * 8;
    for (let i0 = 0; i0 < value.SLength; i0++) {
      if (value.S[i0] === 0) { // an interior null is content the read refuses
        return false;
      }
    }
    value.B = 0;
  } else {
    if (br + 10 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 10;
    v = (out & 0x3ff) >>> 0;
    value.B = v;
    value.S.fill(0);
    value.SLength = 0;
  }
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArmArray: the flat codec ----

function writeArmArrayFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    v = ((value.ItemsCount) & 0x3) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 2;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (2 - sb);
    }
    for (let i0 = 0; i0 < value.ItemsCount; i0++) {
      v = ((value.Items[i0]) & 0x1fff) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 13;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (13 - sb);
      }
    }
  } else {
    v = (value.B & 0x1ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 9;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (9 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArmArrayFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = value.Flag ? 1 : 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 1;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (1 - sb);
  }
  if (value.Flag) {
    if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop; out-of-contract writes are refused
      return -1;
    }
    v = ((value.ItemsCount) & 0x3) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 2;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (2 - sb);
    }
    for (let i0 = 0; i0 < value.ItemsCount; i0++) {
      if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 8191) {
        return -1;
      }
      v = ((value.Items[i0]) & 0x1fff) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 13;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (13 - sb);
      }
    }
  } else {
    v = (value.B & 0x1ff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 9;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (9 - sb);
    }
  }
  v = (value.Tail & 0x7f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 7;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (7 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArmArrayFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArmArrayMaxBytes.
export const WriteArmArrayFlat = PRODUCTION ? writeArmArrayFlatProduction : writeArmArrayFlatChecked;

// ReadArmArrayFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArmArrayFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 6 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.Flag = v !== 0;
  if (value.Flag) {
    if (br + 2 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 2;
    v = (out & 0x3) >>> 0;
    value.ItemsCount = v;
    if (br + value.ItemsCount * 13 > numBits) {
      return false;
    }
    for (let i0 = 0; i0 < value.ItemsCount; i0++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 13;
      v = (out & 0x1fff) >>> 0;
      value.Items[i0] = v | 0;
    }
    value.B = 0;
  } else {
    if (br + 9 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 9;
    v = (out & 0x1ff) >>> 0;
    value.B = v;
    value.Items.fill(0);
    value.ItemsCount = 0;
  }
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type Narrow: the flat codec ----

function writeNarrowFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.N & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeNarrowFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.N & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteNarrowFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold NarrowMaxBytes.
export const WriteNarrowFlat = PRODUCTION ? writeNarrowFlatProduction : writeNarrowFlatChecked;

// ReadNarrowFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadNarrowFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 3;
  v = (out & 0x7) >>> 0;
  value.N = v;
  return true;
}

// ---- type Wide: the flat codec ----

function writeWideFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, value.W, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true) & 0x1f;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeWideFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, value.W, true);
  v = SC.getUint32(0, true);
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = SC.getUint32(4, true) & 0x1f;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteWideFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold WideMaxBytes.
export const WriteWideFlat = PRODUCTION ? writeWideFlatProduction : writeWideFlatChecked;

// ReadWideFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadWideFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 37 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.W = bg;
  return true;
}

// ---- type HoldsUneven: the flat codec ----

function writeHoldsUnevenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = (value.U.Type & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  switch (value.U.Type) {
    case 1: {
      v = (value.U.Narrow.N & 0x7) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 2: {
      SC.setBigUint64(0, value.U.Wide.W, true);
      v = SC.getUint32(0, true);
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
      v = SC.getUint32(4, true) & 0x1f;
      lo = (lo | (v << sb)) >>> 0;
      sb += 5;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (5 - sb);
      }
      break;
    }
  }
  v = (value.Tail & 0x7ff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 11;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (11 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeHoldsUnevenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  if (!Number.isInteger(value.U.Type) || value.U.Type < 0 || value.U.Type > 2) { // the tag validates BEFORE it rides (SPEC §4.8)
    return -1;
  }
  v = (value.U.Type & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  switch (value.U.Type) {
    case 1: {
      v = (value.U.Narrow.N & 0x7) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 2: {
      SC.setBigUint64(0, value.U.Wide.W, true);
      v = SC.getUint32(0, true);
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
      v = SC.getUint32(4, true) & 0x1f;
      lo = (lo | (v << sb)) >>> 0;
      sb += 5;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (5 - sb);
      }
      break;
    }
  }
  v = (value.Tail & 0x7ff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 11;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (11 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteHoldsUnevenFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold HoldsUnevenMaxBytes.
export const WriteHoldsUnevenFlat = PRODUCTION ? writeHoldsUnevenFlatProduction : writeHoldsUnevenFlatChecked;

// ReadHoldsUnevenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadHoldsUnevenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  if (v > 2) { // not a wire-legal tag (SPEC §4.8)
    return false;
  }
  value.U.Type = v;
  switch (value.U.Type) {
    case 1: {
      value.U.Narrow.N = 0;
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 3;
      v = (out & 0x7) >>> 0;
      value.U.Narrow.N = v;
      break;
    }
    case 2: {
      value.U.Wide.W = 0n;
      if (br + 37 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      SC.setUint32(0, v, true);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 5;
      v = (out & 0x1f) >>> 0;
      SC.setUint32(4, v, true);
      bg = SC.getBigUint64(0, true);
      value.U.Wide.W = bg;
      break;
    }
  }
  if (br + 11 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 11;
  v = (out & 0x7ff) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type ArrUneven: the flat codec ----

function writeArrUnevenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = ((value.ItemsCount) & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    v = (e0.Type & 0x3) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 2;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (2 - sb);
    }
    switch (e0.Type) {
      case 1: {
        v = (e0.Narrow.N & 0x7) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 3;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (3 - sb);
        }
        break;
      }
      case 2: {
        SC.setBigUint64(0, e0.Wide.W, true);
        v = SC.getUint32(0, true);
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = SC.getUint32(4, true) & 0x1f;
        lo = (lo | (v << sb)) >>> 0;
        sb += 5;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (5 - sb);
        }
        break;
      }
    }
  }
  v = (value.Tail & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeArrUnevenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = ((value.ItemsCount) & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    if (!Number.isInteger(e0.Type) || e0.Type < 0 || e0.Type > 2) { // the tag validates BEFORE it rides (SPEC §4.8)
      return -1;
    }
    v = (e0.Type & 0x3) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 2;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (2 - sb);
    }
    switch (e0.Type) {
      case 1: {
        v = (e0.Narrow.N & 0x7) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 3;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (3 - sb);
        }
        break;
      }
      case 2: {
        SC.setBigUint64(0, e0.Wide.W, true);
        v = SC.getUint32(0, true);
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = SC.getUint32(4, true) & 0x1f;
        lo = (lo | (v << sb)) >>> 0;
        sb += 5;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (5 - sb);
        }
        break;
      }
    }
  }
  v = (value.Tail & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteArrUnevenFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold ArrUnevenMaxBytes.
export const WriteArrUnevenFlat = PRODUCTION ? writeArrUnevenFlatProduction : writeArrUnevenFlatChecked;

// ReadArrUnevenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadArrUnevenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  value.ItemsCount = v;
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    const e0 = value.Items[i0];
    if (br + 2 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 2;
    v = (out & 0x3) >>> 0;
    if (v > 2) { // not a wire-legal tag (SPEC §4.8)
      return false;
    }
    e0.Type = v;
    switch (e0.Type) {
      case 1: {
        e0.Narrow.N = 0;
        if (br + 3 > numBits) {
          return false;
        }
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 3;
        v = (out & 0x7) >>> 0;
        e0.Narrow.N = v;
        break;
      }
      case 2: {
        e0.Wide.W = 0n;
        if (br + 37 > numBits) {
          return false;
        }
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        SC.setUint32(0, v, true);
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 5;
        v = (out & 0x1f) >>> 0;
        SC.setUint32(4, v, true);
        bg = SC.getBigUint64(0, true);
        e0.Wide.W = bg;
        break;
      }
    }
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 3;
  v = (out & 0x7) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type RegainAfterAlign: the flat codec ----

function writeRegainAfterAlignFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  v = ((value.ItemsCount) & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    v = ((value.Items[i0]) & 0x1fff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  v = ((value.SLength) & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  for (let i0 = 0; i0 < value.SLength; i0++) {
    v = value.S[i0];
    lo = (lo | (v << sb)) >>> 0;
    sb += 8;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (8 - sb);
    }
  }
  v = value.P >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = (value.Q & 0x1fffffff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 29;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (29 - sb);
  }
  v = (value.R & 0x7ffff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 19;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (19 - sb);
  }
  v = (value.Tail & 0xf) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeRegainAfterAlignFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (value.Lead & 0x1f) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = ((value.ItemsCount) & 0x3) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    if (!Number.isInteger(value.Items[i0]) || value.Items[i0] < 0 || value.Items[i0] > 8191) {
      return -1;
    }
    v = ((value.Items[i0]) & 0x1fff) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 13;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (13 - sb);
    }
  }
  if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 4) { // the length guards the slice; out-of-contract writes are refused
    return -1;
  }
  v = ((value.SLength) & 0x7) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  for (let i0 = 0; i0 < value.SLength; i0++) {
    v = value.S[i0];
    lo = (lo | (v << sb)) >>> 0;
    sb += 8;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (8 - sb);
    }
  }
  v = value.P >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 32;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (32 - sb);
  }
  v = (value.Q & 0x1fffffff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 29;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (29 - sb);
  }
  v = (value.R & 0x7ffff) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 19;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (19 - sb);
  }
  v = (value.Tail & 0xf) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteRegainAfterAlignFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold RegainAfterAlignMaxBytes.
export const WriteRegainAfterAlignFlat = PRODUCTION ? writeRegainAfterAlignFlatProduction : writeRegainAfterAlignFlatChecked;

// ReadRegainAfterAlignFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadRegainAfterAlignFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 5 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  value.Lead = v;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  value.ItemsCount = v;
  if (br + value.ItemsCount * 13 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.ItemsCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 13;
    v = (out & 0x1fff) >>> 0;
    value.Items[i0] = v | 0;
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 3;
  v = (out & 0x7) >>> 0;
  if (v > 4) { // the length guards the slice — reject, never clamp
    return false;
  }
  value.SLength = v;
  s2 = br & 7;
  if (s2 !== 0) {
    if (br + (8 - s2) > numBits) {
      return false;
    }
    if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
      return false;
    }
    br += 8 - s2;
  }
  if (br + value.SLength * 8 > numBits) {
    return false;
  }
  bi = br >>> 3;
  for (let i0 = 0; i0 < value.SLength; i0++) {
    value.S[i0] = view.getUint8(bi + i0);
  }
  br += value.SLength * 8;
  for (let i0 = 0; i0 < value.SLength; i0++) {
    if (value.S[i0] === 0) { // an interior null is content the read refuses
      return false;
    }
  }
  if (br + 84 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  value.P = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 29;
  v = (out & 0x1fffffff) >>> 0;
  value.Q = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 19;
  v = (out & 0x7ffff) >>> 0;
  value.R = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 4;
  v = (out & 0xf) >>> 0;
  value.Tail = v;
  return true;
}

