// Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x91a8e85156dfe2b1

// Scratch holders for the runtime's {value} refs — single threaded per
// realm, always consumed in the same call that fills them.
const NUMBER_SCRATCH = { value: 0 };
const BIGINT_SCRATCH = { value: 0n };

// type Vec2
export class Vec2 {
  constructor() {
    this.X = 0;
    this.Y = 0;
  }
}

// Vec2MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// Vec2MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const Vec2MaxBits = 128;
export const Vec2MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroVec2(value) {
  value.X = 0;
  value.Y = 0;
}

export function WriteVec2(stream, value) {
  NUMBER_SCRATCH.value = value.X;
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Y;
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  return true;
}

export function ReadVec2(stream, value) {
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  value.X = NUMBER_SCRATCH.value;
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  value.Y = NUMBER_SCRATCH.value;
  return true;
}

// type SpanF64
export class SpanF64 {
  constructor() {
    this.Values = new Array(2).fill(0);
  }
}

// SpanF64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanF64MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanF64MaxBits = 128;
export const SpanF64MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanF64(value) {
  value.Values.fill(0);
}

export function WriteSpanF64(stream, value) {
  for (let i = 0; i < 2; i++) {
    NUMBER_SCRATCH.value = value.Values[i];
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanF64(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
    value.Values[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type SpanU64
export class SpanU64 {
  constructor() {
    this.Values = new Array(2).fill(0n);
  }
}

// SpanU64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanU64MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanU64MaxBits = 128;
export const SpanU64MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanU64(value) {
  value.Values.fill(0n);
}

export function WriteSpanU64(stream, value) {
  for (let i = 0; i < 2; i++) {
    BIGINT_SCRATCH.value = value.Values[i];
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanU64(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
    value.Values[i] = BIGINT_SCRATCH.value;
  }
  return true;
}

// type SpanI64
export class SpanI64 {
  constructor() {
    this.Values = new Array(2).fill(0n);
  }
}

// SpanI64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanI64MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanI64MaxBits = 128;
export const SpanI64MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanI64(value) {
  value.Values.fill(0n);
}

export function WriteSpanI64(stream, value) {
  for (let i = 0; i < 2; i++) {
    BIGINT_SCRATCH.value = value.Values[i];
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanI64(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
    value.Values[i] = BigInt.asIntN(64, BIGINT_SCRATCH.value);
  }
  return true;
}

// type SpanOne
export class SpanOne {
  constructor() {
    this.Values = new Array(1).fill(0n);
  }
}

// SpanOneMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanOneMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanOneMaxBits = 64;
export const SpanOneMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanOne(value) {
  value.Values.fill(0n);
}

export function WriteSpanOne(stream, value) {
  for (let i = 0; i < 1; i++) {
    BIGINT_SCRATCH.value = value.Values[i];
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanOne(stream, value) {
  for (let i = 0; i < 1; i++) {
    if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
      return false;
    }
    value.Values[i] = BIGINT_SCRATCH.value;
  }
  return true;
}

// type SpanChunk
export class SpanChunk {
  constructor() {
    this.Values = new Array(4).fill(0);
  }
}

// SpanChunkMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanChunkMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanChunkMaxBits = 64;
export const SpanChunkMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanChunk(value) {
  value.Values.fill(0);
}

export function WriteSpanChunk(stream, value) {
  for (let i = 0; i < 4; i++) {
    NUMBER_SCRATCH.value = value.Values[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanChunk(stream, value) {
  for (let i = 0; i < 4; i++) {
    if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
      return false;
    }
    value.Values[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type SpanTail
export class SpanTail {
  constructor() {
    this.Values = new Array(2).fill(0);
    this.Tail = 0;
  }
}

// SpanTailMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanTailMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanTailMaxBits = 160;
export const SpanTailMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanTail(value) {
  value.Values.fill(0);
  value.Tail = 0;
}

export function WriteSpanTail(stream, value) {
  for (let i = 0; i < 2; i++) {
    NUMBER_SCRATCH.value = value.Values[i];
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  return true;
}

export function ReadSpanTail(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
    value.Values[i] = NUMBER_SCRATCH.value;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type SpanTwice
export class SpanTwice {
  constructor() {
    this.A = new Array(2).fill(0);
    this.B = new Array(2).fill(0);
  }
}

// SpanTwiceMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SpanTwiceMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SpanTwiceMaxBits = 256;
export const SpanTwiceMaxBytes = 32;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSpanTwice(value) {
  value.A.fill(0);
  value.B.fill(0);
}

export function WriteSpanTwice(stream, value) {
  for (let i = 0; i < 2; i++) {
    NUMBER_SCRATCH.value = value.A[i];
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
  }
  for (let i = 0; i < 2; i++) {
    NUMBER_SCRATCH.value = value.B[i];
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
  }
  return true;
}

export function ReadSpanTwice(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
    value.A[i] = NUMBER_SCRATCH.value;
  }
  for (let i = 0; i < 2; i++) {
    if (!stream.serializeDouble(NUMBER_SCRATCH)) {
      return false;
    }
    value.B[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type Trio
export class Trio {
  constructor() {
    this.A = 0;
    this.B = 0;
    this.C = 0;
  }
}

// TrioMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TrioMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TrioMaxBits = 64;
export const TrioMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTrio(value) {
  value.A = 0;
  value.B = 0;
  value.C = 0;
}

export function WriteTrio(stream, value) {
  NUMBER_SCRATCH.value = value.A;
  if (!stream.serializeBits(NUMBER_SCRATCH, 20)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.B;
  if (!stream.serializeBits(NUMBER_SCRATCH, 20)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.C;
  if (!stream.serializeBits(NUMBER_SCRATCH, 24)) {
    return false;
  }
  return true;
}

export function ReadTrio(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 20)) {
    return false;
  }
  value.A = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 20)) {
    return false;
  }
  value.B = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 24)) {
    return false;
  }
  value.C = NUMBER_SCRATCH.value;
  return true;
}

// type TrioSole
export class TrioSole {
  constructor() {
    this.Inner = new Trio();
  }
}

// TrioSoleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TrioSoleMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TrioSoleMaxBits = 64;
export const TrioSoleMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTrioSole(value) {
  ZeroTrio(value.Inner);
}

export function WriteTrioSole(stream, value) {
  if (!WriteTrio(stream, value.Inner)) {
    return false;
  }
  return true;
}

export function ReadTrioSole(stream, value) {
  if (!ReadTrio(stream, value.Inner)) {
    return false;
  }
  return true;
}

// type TrioFirst
export class TrioFirst {
  constructor() {
    this.Inner = new Trio();
    this.Trailer = 0;
  }
}

// TrioFirstMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TrioFirstMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TrioFirstMaxBits = 80;
export const TrioFirstMaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTrioFirst(value) {
  ZeroTrio(value.Inner);
  value.Trailer = 0;
}

export function WriteTrioFirst(stream, value) {
  if (!WriteTrio(stream, value.Inner)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Trailer;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  return true;
}

export function ReadTrioFirst(stream, value) {
  if (!ReadTrio(stream, value.Inner)) {
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  value.Trailer = NUMBER_SCRATCH.value;
  return true;
}

// type TrioStraddle
export class TrioStraddle {
  constructor() {
    this.Pad0 = 0n;
    this.Pad1 = 0n;
    this.Pad2 = 0n;
    this.Pad3 = 0n;
    this.Pad4 = 0n;
    this.Pad5 = 0;
    this.Inner = new Trio();
  }
}

// TrioStraddleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TrioStraddleMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TrioStraddleMaxBits = 408;
export const TrioStraddleMaxBytes = 56;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTrioStraddle(value) {
  value.Pad0 = 0n;
  value.Pad1 = 0n;
  value.Pad2 = 0n;
  value.Pad3 = 0n;
  value.Pad4 = 0n;
  value.Pad5 = 0;
  ZeroTrio(value.Inner);
}

export function WriteTrioStraddle(stream, value) {
  BIGINT_SCRATCH.value = value.Pad0;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Pad1;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Pad2;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Pad3;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Pad4;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Pad5;
  if (!stream.serializeBits(NUMBER_SCRATCH, 24)) {
    return false;
  }
  if (!WriteTrio(stream, value.Inner)) {
    return false;
  }
  return true;
}

export function ReadTrioStraddle(stream, value) {
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Pad0 = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Pad1 = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Pad2 = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Pad3 = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Pad4 = BIGINT_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 24)) {
    return false;
  }
  value.Pad5 = NUMBER_SCRATCH.value;
  if (!ReadTrio(stream, value.Inner)) {
    return false;
  }
  return true;
}

