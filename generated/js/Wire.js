// Code generated by the schema compiler from Wire.schema. DO NOT EDIT.
// package example — protocol id 0x01125e6515d05ec4
//
// Storage members are PascalCase via the same mapping as the Go target, so
// the checker's collision registry covers JS for free. Wire functions return
// bool — the C++-style early-out. A schema validation failure (a wrong wire
// constant, nonzero reserved bits, an out-of-contract write) returns false
// WITHOUT latching; stream failures latch on stream.error — the runtime's own
// sticky latch. Callers get bool always; error tells the two apart.
//
// Number storage for widths of 32 bits or fewer, BigInt for 64 and 128 —
// the serialize.js value-domain seam. Checked/production comes from the
// stream object; generated code never reads NODE_ENV.

import { ReadTest, Test, WriteTest, ZeroTest } from "./Messages.js";

// Scratch holders for the runtime's {value} refs (streams.js has no ref
// parameters). Module scope is safe — JavaScript is single threaded per
// realm and a holder is consumed in the same call that fills it, the
// runtime's own FLOAT_SCRATCH argument; generated code never holds one
// across a nested Write/Read call.
const NUMBER_SCRATCH = { value: 0 };
const BIGINT_SCRATCH = { value: 0n };
const BOOL_SCRATCH = { value: false };

export const ProbeId = 244837814094590n;

export const HalfTurn = 180.0;

export const TickRate = 60.0;

// Weapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
// a frozen object of Number values — the JS translation of the family's
// integer-backed enums; [max = ...] headroom values are plain Numbers
export const Weapon = Object.freeze({
  None: 0,
  Laser: 1,
  Missile: 2,
  Railgun: 3,
});

// EnumNameWeapon: debug/log/tooling name for any Weapon wire value —
// out-of-set values (wire-legal up to the declared max) name as "???"
export function EnumNameWeapon(value) {
  switch (value) {
    case Weapon.None:
      return "None";
    case Weapon.Laser:
      return "Laser";
    case Weapon.Missile:
      return "Missile";
    case Weapon.Railgun:
      return "Railgun";
    default:
      return "???";
  }
}

// ProbeFlags — one bit per variant, consumed as masks; flags-typed fields store
// uint64 in every target, so BigInt here, wire 8 bits (SPEC §4.2). Masks
// are flat PascalCase — the Go target's spelling exactly, so the checker's
// existing claims cover them.
export const ProbeFlagsArmed = 1n << 0n;
export const ProbeFlagsCloaked = 1n << 1n;
export const ProbeFlagsDamaged = 1n << 2n;

// type ProbeHeader
export class ProbeHeader {
  constructor() {
    this.Version = 0;
    this.ProbeId = 0n;
  }
}

// ProbeHeaderMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeHeaderMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeHeaderMaxBits = 87;
export const ProbeHeaderMaxBytes = 16;

// ZeroProbeHeader resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeHeader(value) {
  value.Version = 0;
  value.ProbeId = 0n;
}

export function WriteProbeHeader(stream, value) {
  NUMBER_SCRATCH.value = 171;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) { // const(171, 8) — SPEC §4.3
    return false;
  }
  NUMBER_SCRATCH.value = value.Version;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  NUMBER_SCRATCH.value = 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) { // reserved(5) — zeros on the wire
    return false;
  }
  if (!stream.serializeAlign()) {
    return false;
  }
  BIGINT_SCRATCH.value = value.ProbeId;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  return true;
}

export function ReadProbeHeader(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  if (NUMBER_SCRATCH.value !== 171) { // const(171, 8): a read rejects any other value (SPEC §4.3)
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Version = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (NUMBER_SCRATCH.value !== 0) { // reserved(5): a read rejects nonzero (SPEC §4.3)
    return false;
  }
  if (!stream.serializeAlign()) { // rejects nonzero padding (SPEC §4.3)
    return false;
  }
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.ProbeId = BIGINT_SCRATCH.value;
  return true;
}

// type ProbeBits
export class ProbeBits {
  constructor() {
    this.Small = 0;
    this.Boundary = 0n;
    this.Wide = 0n;
    this.Sensor = 0; // wire [0, 4294967295]
    this.Nonce = 0n; // wire [0, 18446744073709551615]
  }
}

// ProbeBitsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeBitsMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeBitsMaxBits = 202;
export const ProbeBitsMaxBytes = 32;

// ZeroProbeBits resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeBits(value) {
  value.Small = 0;
  value.Boundary = 0n;
  value.Wide = 0n;
  value.Sensor = 0;
  value.Nonce = 0n;
}

export function WriteProbeBits(stream, value) {
  NUMBER_SCRATCH.value = value.Small;
  if (!stream.serializeBits(NUMBER_SCRATCH, 9)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Boundary;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 33)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Wide;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  if (!Number.isInteger(value.Sensor) || value.Sensor < 0 || value.Sensor > 4294967295) { // out-of-contract writes are refused, not wrapped
    return false;
  }
  NUMBER_SCRATCH.value = value.Sensor;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  BIGINT_SCRATCH.value = BigInt.asUintN(64, value.Nonce);
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  return true;
}

export function ReadProbeBits(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 9)) {
    return false;
  }
  value.Small = NUMBER_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 33)) {
    return false;
  }
  value.Boundary = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Wide = BIGINT_SCRATCH.value;
  if (!stream.serializeInt64(BIGINT_SCRATCH, 0n, 4294967295n)) {
    return false;
  }
  value.Sensor = Number(BIGINT_SCRATCH.value);
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Nonce = BIGINT_SCRATCH.value;
  return true;
}

// type ProbeSample
export class ProbeSample {
  constructor() {
    this.Active = true; // = true at construction; Zero* gives the §5 zero form
    this.Orientation = 0; // compressed float [-180.0, 180.0] @ 0.01
    this.RawDelta = 0;
    this.BigDelta = 0n;

    // if active — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.Weapon = Weapon.None;
    this.HasTarget = false;

    // if active / if has_target — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.TargetId = 0;

    // if active else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.IdleTicks = 0;

    this.Samples = new Array(8).fill(0);
    this.SamplesCount = 0;
  }
}

// ProbeSampleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeSampleMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeSampleMaxBits = 276;
export const ProbeSampleMaxBytes = 40;

// ZeroProbeSample resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeSample(value) {
  value.Active = false;
  value.Orientation = 0;
  value.RawDelta = 0;
  value.BigDelta = 0n;
  value.Weapon = Weapon.None;
  value.HasTarget = false;
  value.TargetId = 0;
  value.IdleTicks = 0;
  value.Samples.fill(0);
  value.SamplesCount = 0;
}

export function WriteProbeSample(stream, value) {
  BOOL_SCRATCH.value = value.Active;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Orientation;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, -180.0, 180.0, 0.01)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.RawDelta >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.BigDelta;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  if (value.Active) {
    if (!Number.isInteger(value.Weapon) || value.Weapon < 0 || value.Weapon > 15) { // headroom above the wire range cannot ride
      return false;
    }
    NUMBER_SCRATCH.value = value.Weapon;
    if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
      return false;
    }
    BOOL_SCRATCH.value = value.HasTarget;
    if (!stream.serializeBool(BOOL_SCRATCH)) {
      return false;
    }
    if (value.HasTarget) {
      NUMBER_SCRATCH.value = value.TargetId;
      if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
        return false;
      }
    }
  } else {
    NUMBER_SCRATCH.value = value.IdleTicks;
    if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
      return false;
    }
  }
  if (!Number.isInteger(value.SamplesCount) || value.SamplesCount < 1 || value.SamplesCount > 8) { // the count guards the loop (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = ((value.SamplesCount >>> 0) - (1 >>> 0)) >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  for (let i = 0; i < value.SamplesCount; i++) {
    NUMBER_SCRATCH.value = value.Samples[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
      return false;
    }
  }
  return true;
}

export function ReadProbeSample(stream, value) {
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Active = BOOL_SCRATCH.value;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, -180.0, 180.0, 0.01)) {
    return false;
  }
  value.Orientation = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.RawDelta = NUMBER_SCRATCH.value | 0;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.BigDelta = BigInt.asIntN(64, BIGINT_SCRATCH.value);
  if (value.Active) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 15)) {
      return false;
    }
    value.Weapon = NUMBER_SCRATCH.value;
    if (!stream.serializeBool(BOOL_SCRATCH)) {
      return false;
    }
    value.HasTarget = BOOL_SCRATCH.value;
    if (value.HasTarget) {
      if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
        return false;
      }
      value.TargetId = NUMBER_SCRATCH.value;
    } else {
      value.TargetId = 0;
    }
    value.IdleTicks = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
      return false;
    }
    value.IdleTicks = NUMBER_SCRATCH.value;
    value.Weapon = Weapon.None;
    value.HasTarget = false;
    value.TargetId = 0;
  }
  if (!stream.serializeInt(NUMBER_SCRATCH, 1, 8)) { // the count guards the loop (§6.3)
    return false;
  }
  value.SamplesCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.SamplesCount; i++) {
    if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
      return false;
    }
    value.Samples[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type ProbeConfig
export class ProbeConfig {
  constructor() {
    this.Retries = -1; // = -1 at construction; Zero* gives the §5 zero form
    this.Preferred = Weapon.Railgun; // = Weapon.Railgun at construction; Zero* gives the §5 zero form
  }
}

// ProbeConfigMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeConfigMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeConfigMaxBits = 36;
export const ProbeConfigMaxBytes = 8;

// ZeroProbeConfig resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeConfig(value) {
  value.Retries = 0;
  value.Preferred = Weapon.None;
}

export function WriteProbeConfig(stream, value) {
  NUMBER_SCRATCH.value = value.Retries >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  if (!Number.isInteger(value.Preferred) || value.Preferred < 0 || value.Preferred > 15) { // headroom above the wire range cannot ride
    return false;
  }
  NUMBER_SCRATCH.value = value.Preferred;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  return true;
}

export function ReadProbeConfig(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.Retries = NUMBER_SCRATCH.value | 0;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 15)) {
    return false;
  }
  value.Preferred = NUMBER_SCRATCH.value;
  return true;
}

// table ProbeArray
export class ProbeArray {
  constructor() {
    this.Samples = Array.from({ length: 2 }, () => new ProbeSample());
    this.Config = new ProbeConfig();
  }
}

// ProbeArrayMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeArrayMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeArrayMaxBits = 588;
export const ProbeArrayMaxBytes = 80;

// ZeroProbeArray resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeArray(value) {
  for (let i = 0; i < 2; i++) {
    ZeroProbeSample(value.Samples[i]);
  }
  ZeroProbeConfig(value.Config);
}

export function WriteProbeArray(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!WriteProbeSample(stream, value.Samples[i])) {
      return false;
    }
  }
  if (!WriteProbeConfig(stream, value.Config)) {
    return false;
  }
  return true;
}

export function ReadProbeArray(stream, value) {
  for (let i = 0; i < 2; i++) {
    if (!ReadProbeSample(stream, value.Samples[i])) {
      return false;
    }
  }
  if (!ReadProbeConfig(stream, value.Config)) {
    return false;
  }
  return true;
}

// type ProbeReport
export class ProbeReport {
  constructor() {
    this.Header = new ProbeHeader();
    this.Flags = 0n; // ProbeFlags — consumed as masks, BigInt (uint64) storage (SPEC §4.2)
    this.Echo = new Test();
  }
}

// ProbeReportMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ProbeReportMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ProbeReportMaxBits = 141;
export const ProbeReportMaxBytes = 24;

// ZeroProbeReport resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroProbeReport(value) {
  ZeroProbeHeader(value.Header);
  value.Flags = 0n;
  ZeroTest(value.Echo);
}

export function WriteProbeReport(stream, value) {
  if (!WriteProbeHeader(stream, value.Header)) {
    return false;
  }
  {
    const flagsValue = BigInt.asUintN(64, value.Flags);
    if (flagsValue >= 1n << 8n) { // a mask bit above the wire width cannot ride
      return false;
    }
    NUMBER_SCRATCH.value = Number(flagsValue);
    if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
      return false;
    }
  }
  if (!WriteTest(stream, value.Echo)) {
    return false;
  }
  return true;
}

export function ReadProbeReport(stream, value) {
  if (!ReadProbeHeader(stream, value.Header)) {
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.Flags = BigInt(NUMBER_SCRATCH.value);
  if (!ReadTest(stream, value.Echo)) {
    return false;
  }
  return true;
}

// table TestData
export class TestData {
  constructor() {
    this.A = 0; // wire [-100, 100]
    this.B = 0; // wire [-100, 100]
    this.C = 0; // wire [-100, 150]
    this.D = 0;
    this.E = 0;
    this.F = 0;
    this.G = false;
    this.Items = new Array(16).fill(0); // wire [0, 255]
    this.ItemsCount = 0;
    this.FloatValue = 0;
    this.CompressedFloatValue = 0; // compressed float [0.0, 10.0] @ 0.01
    this.DoubleValue = 0;
    this.Int8Value = 0;
    this.Int16Value = 0;
    this.Uint8Value = 0;
    this.Uint16Value = 0;
    this.Uint32Value = 0;
    this.Uint64Value = 0n;
    this.Int64Full = 0n;
    this.Int64Range = 0n; // wire [-1000000000000, 1000000000000]
    this.FixedBytes = new Uint8Array(17);
    this.Text = new Uint8Array(255); // string(255): max length, used length beside it (SPEC §4.7)
    this.TextLength = 0;
  }
}

// TestDataMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TestDataMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TestDataMaxBits = 2735;
export const TestDataMaxBytes = 344;

// ZeroTestData resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroTestData(value) {
  value.A = 0;
  value.B = 0;
  value.C = 0;
  value.D = 0;
  value.E = 0;
  value.F = 0;
  value.G = false;
  value.Items.fill(0);
  value.ItemsCount = 0;
  value.FloatValue = 0;
  value.CompressedFloatValue = 0;
  value.DoubleValue = 0;
  value.Int8Value = 0;
  value.Int16Value = 0;
  value.Uint8Value = 0;
  value.Uint16Value = 0;
  value.Uint32Value = 0;
  value.Uint64Value = 0n;
  value.Int64Full = 0n;
  value.Int64Range = 0n;
  value.FixedBytes.fill(0);
  value.Text.fill(0);
  value.TextLength = 0;
}

export function WriteTestData(stream, value) {
  if (!Number.isInteger(value.A) || value.A < -100 || value.A > 100) { // out-of-contract writes are refused, not wrapped
    return false;
  }
  NUMBER_SCRATCH.value = ((value.A >>> 0) - (-100 >>> 0)) >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  if (!Number.isInteger(value.B) || value.B < -100 || value.B > 100) { // out-of-contract writes are refused, not wrapped
    return false;
  }
  NUMBER_SCRATCH.value = ((value.B >>> 0) - (-100 >>> 0)) >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  if (!Number.isInteger(value.C) || value.C < -100 || value.C > 150) { // out-of-contract writes are refused, not wrapped
    return false;
  }
  NUMBER_SCRATCH.value = ((value.C >>> 0) - (-100 >>> 0)) >>> 0;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.D;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.E;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.F;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  BOOL_SCRATCH.value = value.G;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 16) { // the count guards the loop (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 255) { // out-of-contract writes are refused, not wrapped
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.FloatValue;
  if (!stream.serializeFloat(NUMBER_SCRATCH)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.CompressedFloatValue;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, 0.0, 10.0, 0.01)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.DoubleValue;
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Int8Value & 0xff;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Int16Value & 0xffff;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Uint8Value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Uint16Value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Uint32Value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Uint64Value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  BIGINT_SCRATCH.value = value.Int64Full;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  if (value.Int64Range < -1000000000000n || value.Int64Range > 1000000000000n) { // out-of-contract writes are refused, not wrapped
    return false;
  }
  BIGINT_SCRATCH.value = BigInt.asUintN(64, value.Int64Range - -1000000000000n);
  if (!stream.serializeBits64(BIGINT_SCRATCH, 41)) {
    return false;
  }
  if (!stream.serializeAlign()) {
    return false;
  }
  if (!stream.serializeBytes(value.FixedBytes.subarray(0, 17))) { // byte-aligned [N]uint8 — bulk copy, wire-identical to the per-byte loop
    return false;
  }
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 255) { // the length guards the slice (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.TextLength;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  if (!stream.serializeBytes(value.Text.subarray(0, value.TextLength))) {
    return false;
  }
  return true;
}

export function ReadTestData(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, -100, 100)) {
    return false;
  }
  value.A = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, -100, 100)) {
    return false;
  }
  value.B = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, -100, 150)) {
    return false;
  }
  value.C = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.D = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.E = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.F = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.G = BOOL_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 16)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 255)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  if (!stream.serializeFloat(NUMBER_SCRATCH)) {
    return false;
  }
  value.FloatValue = NUMBER_SCRATCH.value;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, 0.0, 10.0, 0.01)) {
    return false;
  }
  value.CompressedFloatValue = NUMBER_SCRATCH.value;
  if (!stream.serializeDouble(NUMBER_SCRATCH)) {
    return false;
  }
  value.DoubleValue = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.Int8Value = (NUMBER_SCRATCH.value << 24) >> 24;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  value.Int16Value = (NUMBER_SCRATCH.value << 16) >> 16;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.Uint8Value = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  value.Uint16Value = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.Uint32Value = NUMBER_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Uint64Value = BIGINT_SCRATCH.value;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.Int64Full = BigInt.asIntN(64, BIGINT_SCRATCH.value);
  if (!stream.serializeInt64(BIGINT_SCRATCH, -1000000000000n, 1000000000000n)) {
    return false;
  }
  value.Int64Range = BIGINT_SCRATCH.value;
  if (!stream.serializeAlign()) { // rejects nonzero padding (SPEC §4.3)
    return false;
  }
  if (!stream.serializeBytes(value.FixedBytes.subarray(0, 17))) { // byte-aligned [N]uint8 — bulk copy, wire-identical to the per-byte loop
    return false;
  }
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 255)) { // the length guards the slice (§6.3)
    return false;
  }
  value.TextLength = NUMBER_SCRATCH.value;
  if (!stream.serializeBytes(value.Text.subarray(0, value.TextLength))) {
    return false;
  }
  for (let i = 0; i < value.TextLength; i++) {
    if (value.Text[i] === 0) { // an interior null is content the read refuses (SPEC §4.7)
      return false;
    }
  }
  return true;
}

// type CompressedProbe
export class CompressedProbe {
  constructor() {
    this.Boundary = 0; // compressed float [0.0, 10.0] @ 0.01
    this.Offset = 0; // compressed float [-5.0, 5.0] @ 0.001
  }
}

// CompressedProbeMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// CompressedProbeMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const CompressedProbeMaxBits = 24;
export const CompressedProbeMaxBytes = 8;

// ZeroCompressedProbe resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroCompressedProbe(value) {
  value.Boundary = 0;
  value.Offset = 0;
}

export function WriteCompressedProbe(stream, value) {
  NUMBER_SCRATCH.value = value.Boundary;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, 0.0, 10.0, 0.01)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Offset;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, -5.0, 5.0, 0.001)) {
    return false;
  }
  return true;
}

export function ReadCompressedProbe(stream, value) {
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, 0.0, 10.0, 0.01)) {
    return false;
  }
  value.Boundary = NUMBER_SCRATCH.value;
  if (!stream.serializeCompressedFloat(NUMBER_SCRATCH, -5.0, 5.0, 0.001)) {
    return false;
  }
  value.Offset = NUMBER_SCRATCH.value;
  return true;
}

