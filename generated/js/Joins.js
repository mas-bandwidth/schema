// Code generated by the schema compiler from Joins.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x682e2a15a56b78bf

// Scratch holders for the runtime's {value} refs — single threaded per
// realm, always consumed in the same call that fills them.
const NUMBER_SCRATCH = { value: 0 };
const BIGINT_SCRATCH = { value: 0n };
const BOOL_SCRATCH = { value: false };

// type ArmsAgree
export class ArmsAgree {
  constructor() {
    this.Lead = 0;
    this.Flag = false;

    // if flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.A = 0;

    // if flag else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.B = 0;

    this.Tail = 0;
  }
}

// ArmsAgreeMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmsAgreeMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmsAgreeMaxBits = 24;
export const ArmsAgreeMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmsAgree(value) {
  value.Lead = 0;
  value.Flag = false;
  value.A = 0;
  value.B = 0;
  value.Tail = 0;
}

export function WriteArmsAgree(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Flag;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Flag) {
    NUMBER_SCRATCH.value = value.A;
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
  } else {
    NUMBER_SCRATCH.value = value.B;
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadArmsAgree(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Flag = BOOL_SCRATCH.value;
  if (value.Flag) {
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
    value.A = NUMBER_SCRATCH.value;
    value.B = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
    value.B = NUMBER_SCRATCH.value;
    value.A = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArmsDisagree
export class ArmsDisagree {
  constructor() {
    this.Lead = 0;
    this.Flag = false;

    // if flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.A = 0;

    // if flag else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.B = 0;

    this.Tail = 0;
  }
}

// ArmsDisagreeMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmsDisagreeMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmsDisagreeMaxBits = 24;
export const ArmsDisagreeMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmsDisagree(value) {
  value.Lead = 0;
  value.Flag = false;
  value.A = 0;
  value.B = 0;
  value.Tail = 0;
}

export function WriteArmsDisagree(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Flag;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Flag) {
    NUMBER_SCRATCH.value = value.A;
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
  } else {
    NUMBER_SCRATCH.value = value.B;
    if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadArmsDisagree(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Flag = BOOL_SCRATCH.value;
  if (value.Flag) {
    if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
      return false;
    }
    value.A = NUMBER_SCRATCH.value;
    value.B = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
      return false;
    }
    value.B = NUMBER_SCRATCH.value;
    value.A = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArmEmpty
export class ArmEmpty {
  constructor() {
    this.Lead = 0;
    this.Flag = false;

    // if flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.A = 0;

    this.Tail = 0;
  }
}

// ArmEmptyMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmEmptyMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmEmptyMaxBits = 32;
export const ArmEmptyMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmEmpty(value) {
  value.Lead = 0;
  value.Flag = false;
  value.A = 0;
  value.Tail = 0;
}

export function WriteArmEmpty(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Flag;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Flag) {
    NUMBER_SCRATCH.value = value.A;
    if (!stream.serializeBits(NUMBER_SCRATCH, 19)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadArmEmpty(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Flag = BOOL_SCRATCH.value;
  if (value.Flag) {
    if (!stream.serializeBits(NUMBER_SCRATCH, 19)) {
      return false;
    }
    value.A = NUMBER_SCRATCH.value;
  } else {
    value.A = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArmsNested
export class ArmsNested {
  constructor() {
    this.Lead = 0;
    this.Outer = false;

    // if outer — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.Inner = false;

    // if outer / if inner — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.X = 0;

    // if outer / if inner else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.Y = 0;

    // if outer else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.Z = 0;

    this.Tail = 0;
  }
}

// ArmsNestedMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmsNestedMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmsNestedMaxBits = 40;
export const ArmsNestedMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmsNested(value) {
  value.Lead = 0;
  value.Outer = false;
  value.Inner = false;
  value.X = 0;
  value.Y = 0;
  value.Z = 0;
  value.Tail = 0;
}

export function WriteArmsNested(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Outer;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Outer) {
    BOOL_SCRATCH.value = value.Inner;
    if (!stream.serializeBool(BOOL_SCRATCH)) {
      return false;
    }
    if (value.Inner) {
      NUMBER_SCRATCH.value = value.X;
      if (!stream.serializeBits(NUMBER_SCRATCH, 29)) {
        return false;
      }
    } else {
      NUMBER_SCRATCH.value = value.Y;
      if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
        return false;
      }
    }
  } else {
    NUMBER_SCRATCH.value = value.Z;
    if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 6)) {
    return false;
  }
  return true;
}

export function ReadArmsNested(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Outer = BOOL_SCRATCH.value;
  if (value.Outer) {
    if (!stream.serializeBool(BOOL_SCRATCH)) {
      return false;
    }
    value.Inner = BOOL_SCRATCH.value;
    if (value.Inner) {
      if (!stream.serializeBits(NUMBER_SCRATCH, 29)) {
        return false;
      }
      value.X = NUMBER_SCRATCH.value;
      value.Y = 0;
    } else {
      if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
        return false;
      }
      value.Y = NUMBER_SCRATCH.value;
      value.X = 0;
    }
    value.Z = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
      return false;
    }
    value.Z = NUMBER_SCRATCH.value;
    value.Inner = false;
    value.X = 0;
    value.Y = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 6)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArmAlign
export class ArmAlign {
  constructor() {
    this.Lead = 0;
    this.Flag = false;

    // if flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.S = new Uint8Array(4); // string(4): max length, used length beside it (SPEC §4.7)
    this.SLength = 0;

    // if flag else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.B = 0;

    this.Tail = 0;
  }
}

// ArmAlignMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmAlignMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmAlignMaxBits = 55;
export const ArmAlignMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmAlign(value) {
  value.Lead = 0;
  value.Flag = false;
  value.S.fill(0);
  value.SLength = 0;
  value.B = 0;
  value.Tail = 0;
}

export function WriteArmAlign(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Flag;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Flag) {
    if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 4) { // the length guards the slice (§6.3); out-of-contract writes are refused
      return false;
    }
    NUMBER_SCRATCH.value = value.SLength;
    if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
      return false;
    }
    if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
      return false;
    }
  } else {
    NUMBER_SCRATCH.value = value.B;
    if (!stream.serializeBits(NUMBER_SCRATCH, 10)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadArmAlign(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Flag = BOOL_SCRATCH.value;
  if (value.Flag) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 4)) { // the length guards the slice (§6.3)
      return false;
    }
    value.SLength = NUMBER_SCRATCH.value;
    if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
      return false;
    }
    for (let i = 0; i < value.SLength; i++) {
      if (value.S[i] === 0) { // an interior null is content the read refuses (SPEC §4.7)
        return false;
      }
    }
    value.B = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 10)) {
      return false;
    }
    value.B = NUMBER_SCRATCH.value;
    value.S.fill(0);
    value.SLength = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArmArray
export class ArmArray {
  constructor() {
    this.Lead = 0;
    this.Flag = false;

    // if flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.Items = new Array(3).fill(0); // wire [0, 8191]
    this.ItemsCount = 0;

    // if flag else — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    this.B = 0;

    this.Tail = 0;
  }
}

// ArmArrayMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArmArrayMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArmArrayMaxBits = 54;
export const ArmArrayMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArmArray(value) {
  value.Lead = 0;
  value.Flag = false;
  value.Items.fill(0);
  value.ItemsCount = 0;
  value.B = 0;
  value.Tail = 0;
}

export function WriteArmArray(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Flag;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  if (value.Flag) {
    if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop (§6.3)
      return false;
    }
    NUMBER_SCRATCH.value = value.ItemsCount;
    if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
      return false;
    }
    for (let i = 0; i < value.ItemsCount; i++) {
      if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 8191) {
        return false;
      }
      NUMBER_SCRATCH.value = value.Items[i];
      if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
        return false;
      }
    }
  } else {
    NUMBER_SCRATCH.value = value.B;
    if (!stream.serializeBits(NUMBER_SCRATCH, 9)) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadArmArray(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Flag = BOOL_SCRATCH.value;
  if (value.Flag) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // the count guards the loop (§6.3)
      return false;
    }
    value.ItemsCount = NUMBER_SCRATCH.value;
    for (let i = 0; i < value.ItemsCount; i++) {
      if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8191)) {
        return false;
      }
      value.Items[i] = NUMBER_SCRATCH.value;
    }
    value.B = 0;
  } else {
    if (!stream.serializeBits(NUMBER_SCRATCH, 9)) {
      return false;
    }
    value.B = NUMBER_SCRATCH.value;
    value.Items.fill(0);
    value.ItemsCount = 0;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type Narrow
export class Narrow {
  constructor() {
    this.N = 0;
  }
}

// NarrowMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// NarrowMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const NarrowMaxBits = 3;
export const NarrowMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroNarrow(value) {
  value.N = 0;
}

export function WriteNarrow(stream, value) {
  NUMBER_SCRATCH.value = value.N;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadNarrow(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.N = NUMBER_SCRATCH.value;
  return true;
}

// type Wide
export class Wide {
  constructor() {
    this.W = 0n;
  }
}

// WideMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// WideMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const WideMaxBits = 37;
export const WideMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroWide(value) {
  value.W = 0n;
}

export function WriteWide(stream, value) {
  BIGINT_SCRATCH.value = value.W;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 37)) {
    return false;
  }
  return true;
}

export function ReadWide(stream, value) {
  if (!stream.serializeBits64(BIGINT_SCRATCH, 37)) {
    return false;
  }
  value.W = BIGINT_SCRATCH.value;
  return true;
}

// UnevenType: union Uneven's tag — None = 0, then each variant in declared order (SPEC §4.8)
export const UnevenType = Object.freeze({
  None: 0,
  Narrow: 1,
  Wide: 2,
  Max: 2, // the exported extent (SPEC §4.2)
});

// Uneven — at most one of the arms; Type says which. Construction is the empty
// union (None). A read zero-establishes exactly the selected arm before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
export class Uneven {
  constructor() {
    this.Type = UnevenType.None;
    this.Narrow = new Narrow();
    this.Wide = new Wide();
  }
}

// UnevenMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// UnevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const UnevenMaxBits = 39;
export const UnevenMaxBytes = 8;

// ZeroUneven resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm re-zeroes at its next selection.
export function ZeroUneven(value) {
  value.Type = UnevenType.None;
}

export function WriteUneven(stream, value) {
  if (!Number.isInteger(value.Type) || value.Type < 0 || value.Type > 2) {
    return false; // the tag validates BEFORE it rides (SPEC §4.8)
  }
  NUMBER_SCRATCH.value = value.Type;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  switch (value.Type) {
    case UnevenType.Narrow:
      return WriteNarrow(stream, value.Narrow);
    case UnevenType.Wide:
      return WriteWide(stream, value.Wide);
  }
  return true; // None — the tag is the whole wire (SPEC §4.8)
}

export function ReadUneven(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 2)) { // rejects a tag above the count (SPEC §4.8)
    return false;
  }
  value.Type = NUMBER_SCRATCH.value;
  switch (value.Type) {
    case UnevenType.Narrow:
      ZeroNarrow(value.Narrow); // the selected arm starts from the zero form (SPEC §5)
      return ReadNarrow(stream, value.Narrow);
    case UnevenType.Wide:
      ZeroWide(value.Wide); // the selected arm starts from the zero form (SPEC §5)
      return ReadWide(stream, value.Wide);
  }
  return true; // None
}

// type HoldsUneven
export class HoldsUneven {
  constructor() {
    this.Lead = 0;
    this.U = new Uneven();
    this.Tail = 0;
  }
}

// HoldsUnevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// HoldsUnevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const HoldsUnevenMaxBits = 55;
export const HoldsUnevenMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroHoldsUneven(value) {
  value.Lead = 0;
  ZeroUneven(value.U);
  value.Tail = 0;
}

export function WriteHoldsUneven(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!WriteUneven(stream, value.U)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
    return false;
  }
  return true;
}

export function ReadHoldsUneven(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!ReadUneven(stream, value.U)) {
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 11)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArrUneven
export class ArrUneven {
  constructor() {
    this.Lead = 0;
    this.Items = Array.from({ length: 3 }, () => new Uneven());
    this.ItemsCount = 0;
    this.Tail = 0;
  }
}

// ArrUnevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArrUnevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArrUnevenMaxBits = 127;
export const ArrUnevenMaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArrUneven(value) {
  value.Lead = 0;
  for (let i = 0; i < 3; i++) {
    ZeroUneven(value.Items[i]);
  }
  value.ItemsCount = 0;
  value.Tail = 0;
}

export function WriteArrUneven(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!WriteUneven(stream, value.Items[i])) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadArrUneven(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!ReadUneven(stream, value.Items[i])) {
      return false;
    }
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type RegainAfterAlign
export class RegainAfterAlign {
  constructor() {
    this.Lead = 0;
    this.Items = new Array(3).fill(0); // wire [0, 8191]
    this.ItemsCount = 0;
    this.S = new Uint8Array(4); // string(4): max length, used length beside it (SPEC §4.7)
    this.SLength = 0;
    this.P = 0;
    this.Q = 0;
    this.R = 0;
    this.Tail = 0;
  }
}

// RegainAfterAlignMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// RegainAfterAlignMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const RegainAfterAlignMaxBits = 172;
export const RegainAfterAlignMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroRegainAfterAlign(value) {
  value.Lead = 0;
  value.Items.fill(0);
  value.ItemsCount = 0;
  value.S.fill(0);
  value.SLength = 0;
  value.P = 0;
  value.Q = 0;
  value.R = 0;
  value.Tail = 0;
}

export function WriteRegainAfterAlign(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 8191) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
      return false;
    }
  }
  if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 4) { // the length guards the slice (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.SLength;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
    return false;
  }
  NUMBER_SCRATCH.value = value.P;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Q;
  if (!stream.serializeBits(NUMBER_SCRATCH, 29)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.R;
  if (!stream.serializeBits(NUMBER_SCRATCH, 19)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  return true;
}

export function ReadRegainAfterAlign(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8191)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 4)) { // the length guards the slice (§6.3)
    return false;
  }
  value.SLength = NUMBER_SCRATCH.value;
  if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
    return false;
  }
  for (let i = 0; i < value.SLength; i++) {
    if (value.S[i] === 0) { // an interior null is content the read refuses (SPEC §4.7)
      return false;
    }
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.P = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 29)) {
    return false;
  }
  value.Q = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 19)) {
    return false;
  }
  value.R = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

