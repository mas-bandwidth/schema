// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// package example — protocol id 0xbc05d83a8135cdb9
//
// Storage members are PascalCase via the same mapping as the Go target, so
// the checker's collision registry covers JS for free. Wire functions return
// bool — the C++-style early-out. A schema validation failure (a wrong wire
// constant, nonzero reserved bits, an out-of-contract write) returns false
// WITHOUT latching; stream failures latch on stream.error — the runtime's own
// sticky latch. Callers get bool always; error tells the two apart.
//
// Number storage for widths of 32 bits or fewer, BigInt for 64 and 128 —
// the serialize.js value-domain seam. Checked/production comes from the
// stream object; generated code never reads NODE_ENV.

import { Team } from "./Enums.js";

// Scratch holders for the runtime's {value} refs (streams.js has no ref
// parameters). Module scope is safe — JavaScript is single threaded per
// realm and a holder is consumed in the same call that fills it, the
// runtime's own FLOAT_SCRATCH argument; generated code never holds one
// across a nested Write/Read call.
const NUMBER_SCRATCH = { value: 0 };
const BIGINT_SCRATCH = { value: 0n };

export const RenderBlockMaxSprites = 64;

// type RenderSprite
export class RenderSprite {
  constructor() {
    this.SortKey = 0n;
    this.MeshId = 0;
    this.MaterialId = 0;
    this.Layer = 0;
    this.Team = Team.None;
  }
}

// RenderSpriteMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// RenderSpriteMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const RenderSpriteMaxBits = 138;
export const RenderSpriteMaxBytes = 24;

// ZeroRenderSprite resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroRenderSprite(value) {
  value.SortKey = 0n;
  value.MeshId = 0;
  value.MaterialId = 0;
  value.Layer = 0;
  value.Team = Team.None;
}

export function WriteRenderSprite(stream, value) {
  BIGINT_SCRATCH.value = value.SortKey;
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.MeshId;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.MaterialId;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Layer;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  if (!Number.isInteger(value.Team) || value.Team < 0 || value.Team > 2) { // headroom above the wire range cannot ride
    return false;
  }
  NUMBER_SCRATCH.value = value.Team;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  return true;
}

export function ReadRenderSprite(stream, value) {
  if (!stream.serializeBits64(BIGINT_SCRATCH, 64)) {
    return false;
  }
  value.SortKey = BIGINT_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.MeshId = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.MaterialId = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.Layer = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 2)) {
    return false;
  }
  value.Team = NUMBER_SCRATCH.value;
  return true;
}

// type RenderBlock
export class RenderBlock {
  constructor() {
    this.WorkerIndex = 0;
    this.SpriteCountHint = 0;
    this.Sprites = Array.from({ length: RenderBlockMaxSprites }, () => new RenderSprite());
    this.SpritesCount = 0;
  }
}

// RenderBlockMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// RenderBlockMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const RenderBlockMaxBits = 8903;
export const RenderBlockMaxBytes = 1120;

// ZeroRenderBlock resets value to the §5 ZERO form — all-zero storage; specified
// defaults live in construction only and are NOT reapplied here.
export function ZeroRenderBlock(value) {
  value.WorkerIndex = 0;
  value.SpriteCountHint = 0;
  for (let i = 0; i < RenderBlockMaxSprites; i++) {
    ZeroRenderSprite(value.Sprites[i]);
  }
  value.SpritesCount = 0;
}

export function WriteRenderBlock(stream, value) {
  NUMBER_SCRATCH.value = value.WorkerIndex;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.SpriteCountHint;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  if (!Number.isInteger(value.SpritesCount) || value.SpritesCount < 0 || value.SpritesCount > RenderBlockMaxSprites) { // the count guards the loop (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.SpritesCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  for (let i = 0; i < value.SpritesCount; i++) {
    if (!WriteRenderSprite(stream, value.Sprites[i])) {
      return false;
    }
  }
  return true;
}

export function ReadRenderBlock(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.WorkerIndex = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 32)) {
    return false;
  }
  value.SpriteCountHint = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, RenderBlockMaxSprites)) { // the count guards the loop (§6.3)
    return false;
  }
  value.SpritesCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.SpritesCount; i++) {
    if (!ReadRenderSprite(stream, value.Sprites[i])) {
      return false;
    }
  }
  return true;
}

