// Code generated by the schema compiler from Bench.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package bench — the BLITTABLE RECORDS both accelerators are built from (docs/SPEC-TABLES.md §7.2, §19.3)
//
// Every size and offset below is the C ABI model #[repr(C)] commits to, and
// every one of them is asserted AT COMPILE TIME against the layout the
// compiler derived. A bool in one of these records is ONE byte.
//
// The module is not behind either accelerator's cargo feature: a cooked record
// IS the blittable row, so a build with only the block form and a build with
// only the cooked form want the same structs.

#![allow(unused_imports)]

use crate::*;

// THE UNION TWINS. A union's blittable form is a TAG beside a #[repr(C)]
// UNION OF THE ARMS (docs/SPEC-TABLES.md §7.2, §15): the tag at offset zero
// at the width its variant count derives, the arms overlaid at the greatest
// arm alignment, the whole rounded up — which is ir.UnionLayout, the same
// model the C++ reference's storage struct meets and the same one the const
// asserts below hold this build to.
//
// THE TAG NAMES THE LIVE ARM, and that is the type's whole contract — the
// same one the C reference's storage carries, where reading `as.hit` with
// `type != HIT` is equally wrong and equally unchecked. Rust says it out
// loud: a READ of an arm is `unsafe` and every one of them in this crate is
// guarded by the tag. A WRITE of an arm is safe and needs no guard.
// `Row` and `RowArms` are CLAIMED suffixes (docs/SPEC-TABLES.md §11).

// MixedEvent — the union twin: 20 bytes, aligned 4; a 1-byte tag, the arms at 4.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedEventRow {
    pub tag: u8, // 0 is None (the empty union); then each arm in declared order
    pub arms: MixedEventRowArms,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union MixedEventRowArms {
    pub hit: MixedHitEventRow, // MixedHitEvent
    pub chat: MixedChatEventRow, // MixedChatEvent
    pub pickup: MixedPickupEventRow, // MixedPickupEvent
}

impl Default for MixedEventRow {
    fn default() -> Self {
        // the empty union: tag zero and an overlay of zeros, which is a value
        // of every arm — each is plain data with no niche in it
        unsafe { core::mem::zeroed() }
    }
}

impl Default for MixedEventRowArms {
    fn default() -> Self {
        // the empty union: tag zero and an overlay of zeros, which is a value
        // of every arm — each is plain data with no niche in it
        unsafe { core::mem::zeroed() }
    }
}

// The BLITTABLE records a cooked region is laid out from: the C ABI
// layout §20.3 commits the compiler to. #[repr(C)] IS that layout, so
// there are no generated padding fields — what C# has to spell out, Rust
// gets from the representation, and the const asserts below hold it.
// `Row` is a CLAIMED suffix (docs/SPEC-TABLES.md §11), so no declaration
// in the unit can take it.

// MixedEntity — a cooked record: 64 bytes, aligned 8.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedEntityRow {
    pub entity_id: u32,
    pub pos_x: i32,
    pub pos_y: i32,
    pub pos_z: i32,
    pub yaw: u32,
    pub pitch: u32,
    pub vel_x: i32,
    pub vel_y: i32,
    pub vel_z: i32,
    pub health: i32,
    pub weapon: u8,
    pub damage: u64,
    pub moving: bool,
    pub firing: bool,
}

impl Default for MixedEntityRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

// MixedStat — a cooked record: 8 bytes, aligned 4.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedStatRow {
    pub stat_id: u32,
    pub delta: i32,
}

impl Default for MixedStatRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

// MixedHitEvent — a cooked record: 16 bytes, aligned 4.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedHitEventRow {
    pub target_id: u32,
    pub damage: i32,
    pub hit_kind: i32,
    pub crit: bool,
}

impl Default for MixedHitEventRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

// MixedChatEvent — a cooked record: 8 bytes, aligned 4.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedChatEventRow {
    pub channel: i32,
    pub speaker: u32,
}

impl Default for MixedChatEventRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

// MixedPickupEvent — a cooked record: 8 bytes, aligned 4.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct MixedPickupEventRow {
    pub item_id: u32,
    pub amount: i32,
}

impl Default for MixedPickupEventRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

// BenchMixed — a cooked record: 1376 bytes, aligned 16.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct BenchMixedRow {
    pub sequence: u32,
    pub ack_sequence: i32,
    pub ack_bits: u32,
    pub session_id: u64,
    pub client_id: u32,
    pub nonce: u64,
    pub world_time: i64,
    pub frame_tick: u64,
    pub server_time: i32,
    pub entities: [MixedEntityRow; 8],
    pub entities_count: i32,
    pub stats: [MixedStatRow; 80],
    pub stats_count: i32,
    pub game_event: MixedEventRow,
    pub loadout: [u8; 4],
    pub player_name: [u8; 16], // string(15): buffer, used length beside it
    pub player_name_length: i32,
    pub payload: [u8; 16], // bytes(16): buffer, used length beside it
    pub payload_length: i32,
    pub aim_x: f32,
    pub aim_y: f32,
    pub aim_z: f32,
    pub recoil: f32,
    pub drift: f64,
    pub wide_key: u128,
    pub flux: i128,
    pub ping: u16,
    pub crc_hint: u32,
    pub has_extra: bool,
    pub extra: i32,
    pub idle_ticks: i32,
}

impl Default for BenchMixedRow {
    fn default() -> Self {
        // a cooked record is plain bytes with no niche in it, so the zero
        // form is a value of the type by construction
        unsafe { core::mem::zeroed() }
    }
}

const _: () = assert!(core::mem::size_of::<MixedEventRow>() == 20, "MixedEventRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedEventRow>() == 4, "MixedEventRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEventRow, tag) == 0, "MixedEventRow: the tag leads the union storage");
const _: () = assert!(core::mem::offset_of!(MixedEventRow, arms) == 4, "MixedEventRow: the arms are overlaid here");
const _: () = assert!(core::mem::offset_of!(MixedEventRowArms, hit) == 0, "MixedEventRowArms.hit: every arm starts the overlay");
const _: () = assert!(core::mem::offset_of!(MixedEventRowArms, chat) == 0, "MixedEventRowArms.chat: every arm starts the overlay");
const _: () = assert!(core::mem::offset_of!(MixedEventRowArms, pickup) == 0, "MixedEventRowArms.pickup: every arm starts the overlay");

// THE LAYOUT CONTRACT (docs/SPEC-TABLES.md §20.3), checked at COMPILE
// TIME: every size and every offset below is the number the compiler
// derived from the declaration, and this build agreeing with it is what
// makes a cook's bytes readable here. Neither side's layout is inferred
// from the other's.
const _: () = assert!(core::mem::size_of::<MixedEntityRow>() == 64, "MixedEntityRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedEntityRow>() == 8, "MixedEntityRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, entity_id) == 0, "MixedEntityRow.entity_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, pos_x) == 4, "MixedEntityRow.pos_x moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, pos_y) == 8, "MixedEntityRow.pos_y moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, pos_z) == 12, "MixedEntityRow.pos_z moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, yaw) == 16, "MixedEntityRow.yaw moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, pitch) == 20, "MixedEntityRow.pitch moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, vel_x) == 24, "MixedEntityRow.vel_x moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, vel_y) == 28, "MixedEntityRow.vel_y moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, vel_z) == 32, "MixedEntityRow.vel_z moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, health) == 36, "MixedEntityRow.health moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, weapon) == 40, "MixedEntityRow.weapon moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, damage) == 48, "MixedEntityRow.damage moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, moving) == 56, "MixedEntityRow.moving moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedEntityRow, firing) == 57, "MixedEntityRow.firing moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::size_of::<MixedStatRow>() == 8, "MixedStatRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedStatRow>() == 4, "MixedStatRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedStatRow, stat_id) == 0, "MixedStatRow.stat_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedStatRow, delta) == 4, "MixedStatRow.delta moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::size_of::<MixedHitEventRow>() == 16, "MixedHitEventRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedHitEventRow>() == 4, "MixedHitEventRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedHitEventRow, target_id) == 0, "MixedHitEventRow.target_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedHitEventRow, damage) == 4, "MixedHitEventRow.damage moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedHitEventRow, hit_kind) == 8, "MixedHitEventRow.hit_kind moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedHitEventRow, crit) == 12, "MixedHitEventRow.crit moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::size_of::<MixedChatEventRow>() == 8, "MixedChatEventRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedChatEventRow>() == 4, "MixedChatEventRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedChatEventRow, channel) == 0, "MixedChatEventRow.channel moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedChatEventRow, speaker) == 4, "MixedChatEventRow.speaker moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::size_of::<MixedPickupEventRow>() == 8, "MixedPickupEventRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<MixedPickupEventRow>() == 4, "MixedPickupEventRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedPickupEventRow, item_id) == 0, "MixedPickupEventRow.item_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(MixedPickupEventRow, amount) == 4, "MixedPickupEventRow.amount moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::size_of::<BenchMixedRow>() == 1376, "BenchMixedRow's size moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::align_of::<BenchMixedRow>() == 16, "BenchMixedRow's alignment moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, sequence) == 0, "BenchMixedRow.sequence moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, ack_sequence) == 4, "BenchMixedRow.ack_sequence moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, ack_bits) == 8, "BenchMixedRow.ack_bits moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, session_id) == 16, "BenchMixedRow.session_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, client_id) == 24, "BenchMixedRow.client_id moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, nonce) == 32, "BenchMixedRow.nonce moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, world_time) == 40, "BenchMixedRow.world_time moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, frame_tick) == 48, "BenchMixedRow.frame_tick moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, server_time) == 56, "BenchMixedRow.server_time moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, entities) == 64, "BenchMixedRow.entities moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, entities_count) == 576, "BenchMixedRow.entities_count moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, stats) == 580, "BenchMixedRow.stats moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, stats_count) == 1220, "BenchMixedRow.stats_count moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, game_event) == 1224, "BenchMixedRow.game_event moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, loadout) == 1244, "BenchMixedRow.loadout moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, player_name) == 1248, "BenchMixedRow.player_name moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, player_name_length) == 1264, "BenchMixedRow.player_name_length moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, payload) == 1268, "BenchMixedRow.payload moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, payload_length) == 1284, "BenchMixedRow.payload_length moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, aim_x) == 1288, "BenchMixedRow.aim_x moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, aim_y) == 1292, "BenchMixedRow.aim_y moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, aim_z) == 1296, "BenchMixedRow.aim_z moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, recoil) == 1300, "BenchMixedRow.recoil moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, drift) == 1304, "BenchMixedRow.drift moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, wide_key) == 1312, "BenchMixedRow.wide_key moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, flux) == 1328, "BenchMixedRow.flux moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, ping) == 1344, "BenchMixedRow.ping moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, crc_hint) == 1348, "BenchMixedRow.crc_hint moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, has_extra) == 1352, "BenchMixedRow.has_extra moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, extra) == 1356, "BenchMixedRow.extra moved (docs/SPEC-TABLES.md §20.3)");
const _: () = assert!(core::mem::offset_of!(BenchMixedRow, idle_ticks) == 1360, "BenchMixedRow.idle_ticks moved (docs/SPEC-TABLES.md §20.3)");

