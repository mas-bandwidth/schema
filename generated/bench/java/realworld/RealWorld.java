// Code generated by the schema compiler from RealWorld.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package realworld — protocol id 0x11a7ba9416ba6b58
//
// The shipped Java wire path (issue #156): the serialize.java bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, data) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts live in one assert-predicate call — active
// under -ea, dormant bytecode otherwise — and width masks stay, as wire
// arithmetic. The buffer must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, data, numBits) -> boolean, the family read verdict.
// The wire is a trust boundary: reads validate everything — bounds fused
// per static run, ranges, wire constants, reserved and align padding,
// union tags — and NEVER throw on hostile bytes. No slack past the
// payload is required: the reader prices its 64-bit windows inside the
// buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

package realworld;

// RealWorld carries every generated declaration of RealWorld.schema — Java has no
// top-level functions or constants, so the file's one public class is their
// home; types nest inside it (SPEC §6.1 naming).
public final class RealWorld {
    private RealWorld() {}

    // 64-bit little-endian word access into byte[] — the proven-fast JVM
    // form (VarHandle byteArrayViewVarHandle, serialize.java's own).
    private static final java.lang.invoke.VarHandle LONG_LE =
            java.lang.invoke.MethodHandles.byteArrayViewVarHandle(
                    long[].class, java.nio.ByteOrder.LITTLE_ENDIAN);

    // The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
    // sides at the same id speak identical bits; there is no other versioning.
    public static final long protocolId = 0x11a7ba9416ba6b58L;

    // PacketMode — None = 0 implicit, variants dense from 1, wire range [0, 5] (SPEC §4.2);
    // an int-constant namespace — the Java translation of the family's integer-
    // backed enums: storage must hold every wire-legal value, and | max = ...
    // headroom values have no Java enum member to be
    public static final class PacketMode {
        private PacketMode() {}

        public static final byte none = 0;
        public static final byte idle = 1;
        public static final byte active = 2;
        public static final byte combat = 3;
        public static final byte docked = 4;
        public static final byte warping = 5;
        // the declared variant count (SPEC §4.2)
        public static final byte count = 5;
        // the exported extent (SPEC §4.2)
        public static final byte max = 5;
    }

    // enumNamePacketMode: debug/log/tooling name for any PacketMode storage value —
    // out-of-set values (wire-legal up to the declared max) name as "???"
    public static String enumNamePacketMode(long value) {
        if (value == PacketMode.none) {
            return "None";
        }
        if (value == PacketMode.idle) {
            return "Idle";
        }
        if (value == PacketMode.active) {
            return "Active";
        }
        if (value == PacketMode.combat) {
            return "Combat";
        }
        if (value == PacketMode.docked) {
            return "Docked";
        }
        if (value == PacketMode.warping) {
            return "Warping";
        }
        return "???";
    }

    // PacketFlags — one bit per variant, consumed as masks; flags-typed fields store
    // uint64 in every target — a bit-transparent long here — wire 5 bits
    // (SPEC §4.2). Mask names are the family's flat spelling, lowerCamel.
    public static final long packetFlagsShielded = 1L << 0;
    public static final long packetFlagsCloaked = 1L << 1;
    public static final long packetFlagsOverheated = 1L << 2;
    public static final long packetFlagsLowPower = 1L << 3;
    public static final long packetFlagsJamming = 1L << 4;
    // the declared variant count (SPEC §4.2)
    public static final int packetFlagsCount = 5;

    // flagNamePacketFlags: debug/log/tooling name for bit i of PacketFlags —
    // out-of-range bits name as "???"
    public static String flagNamePacketFlags(int bit) {
        switch (bit) {
            case 0:
                return "Shielded";
            case 1:
                return "Cloaked";
            case 2:
                return "Overheated";
            case 3:
                return "LowPower";
            case 4:
                return "Jamming";
            default:
                return "???";
        }
    }

    // flagNamesPacketFlags renders the set bits of value as "A|B" — "0" for the
    // empty set, bits past the declared variants as hex
    public static String flagNamesPacketFlags(long value) {
        StringBuilder names = new StringBuilder();
        if ((value & (1L << 0)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Shielded");
        }
        if ((value & (1L << 1)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Cloaked");
        }
        if ((value & (1L << 2)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Overheated");
        }
        if ((value & (1L << 3)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("LowPower");
        }
        if ((value & (1L << 4)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Jamming");
        }
        if (value >>> 5 != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            // Long.toHexString renders the bit pattern, not a sign
            names.append("0x").append(Long.toHexString((value >>> 5) << 5));
        }
        return names.length() == 0 ? "0" : names.toString();
    }

    // type RealPacket
    public static final class RealPacket {
        // wire [-805495, 805495]
        public int f001Int;
        public double f002F64;
        // wire [-835897, 835897]
        public int f003Int;
        // compressed float [0.0, 2000.0] @ 0.1
        public float f004Cf32;
        // wire [0, 7316]
        public short f005Uint;
        // wire [-1513, 1513]
        public short f006Int;
        public float f007F32;
        public long f008U64;
        // wire [-22, 22]
        public byte f009Int;
        public float f010F32;
        public int f011Bits;
        // specified default at construction; zero* gives the §5 zero form
        public boolean f012Bool = true;

        // f012_bool — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        public float f013F32;
        // wire [0, 775]
        public short f014Uint;
        // wire [-21, 21]
        public byte f015Int;
        // wire [-36, 36]
        public int f016Fixed;
        // wire [0, 4606]
        public short f017Uint;

        // wire [-834, 834]
        public short f018Int;
        public double f019F64;
        public float f020F32;
        // wire [0, 25141]
        public int f021Ufixed;
        public float f022F32;
        public int f023Bits;
        public float f024F32;
        // wire [-119, 119]
        public short f025Fixed;
        public int f026Bits;
        // compressed float [-2.0, 2.0] @ 0.25
        public float f027Cf32;
        public int f028Bits;
        public long f029I64;
        public float f030F32;
        public int f031Bits;
        // wire [-3, 3]
        public byte f032Int;
        // wire [0, 142780]
        public int f033Uint;
        // wire [0, 14149]
        public short f034Uint;
        public int f035Bits;
        public byte f036Enum;
        public boolean f037Bool;
        public boolean f038Bool;
        public int f039Bits;
        // wire [-5, 5]
        public short f040Fixed;
        // wire [-55, 55]
        public byte f041Int;
        public int f042Bits;
        // specified default at construction; zero* gives the §5 zero form
        public boolean f043Bool;

        // f043_bool — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        public float f044F32;
        public int f045Bits;
        // wire [0, 76063]
        public int f046Uint;
        // wire [-430976, 430976]
        public int f047Int;

        public double f048F64;
        // wire [0, 3]
        public short f049Ufixed;
        // specified default at construction; zero* gives the §5 zero form
        public boolean f050Bool = true;

        // f050_bool — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        public boolean f051Bool;
        // wire [-57, 57]
        public byte f052Int;
        public float f053F32;
        // wire [-35, 35]
        public byte f054Int;

        public boolean f055Bool;
        // wire [-13, 13]
        public byte f056Int;
        // wire [-15, 15]
        public byte f057Int;
        public float f058F32;
        public double f059F64;
        public int f060Bits;
        // compressed float [-90.0, 90.0] @ 0.5
        public float f061Cf32;
        // wire [0, 503]
        public short f062Uint;
        public long f063I64;
        // wire [0, 299]
        public short f064Uint;
        // compressed float [0.0, 30.0] @ 0.5
        public float f065Cf32;
        // wire [0, 2]
        public short f066Ufixed;
        // compressed float [-100.0, 100.0] @ 0.25
        public float f067Cf32;
        // compressed float [0.0, 2000.0] @ 1.0
        public float f068Cf32;
        public int f069Bits;
        // wire [0, 2]
        public byte f070Uint;
        // compressed float [0.0, 10.0] @ 0.02
        public float f071Cf32;
        // compressed float [0.0, 100.0] @ 0.01
        public float f072Cf32;
        // wire [-4, 4]
        public byte f073Int;
        // specified default at construction; zero* gives the §5 zero form
        public boolean f074Bool;

        // f074_bool — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        public long f075U64;
        // wire [-26218, 26218]
        public short f076Int;
        // wire [-17, 17]
        public byte f077Int;
        public int f078Bits;
        // wire [0, 17]
        public byte f079Uint;

        public boolean f080Bool;
        public int f081Bits;
        public int f082Bits;
        public byte f083Enum;
        // wire [0, 1]
        public byte f084Ufixed;
        public int f085Bits;
        // wire [0, 399]
        public short f086Uint;
        public double f087F64;
        // wire [-694, 694]
        public short f088Int;
        public long f089Bits;
        // wire [0, 214]
        public byte f090Uint;
        // PacketFlags — consumed as masks, uint64 storage (SPEC §4.2)
        public long f091Flags;
        public boolean f092Bool;
        public long f093Bits;
        public boolean f094Bool;
        // wire [-1577, 1577]
        public int f095Fixed;
        public int f096Bits;
        public int f097Bits;
    }

    // realPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // realPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int realPacketMaxBits = 1810;
    public static final int realPacketMaxBytes = 232;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroRealPacket(RealPacket value) {
        value.f001Int = 0;
        value.f002F64 = 0.0;
        value.f003Int = 0;
        value.f004Cf32 = 0.0f;
        value.f005Uint = 0;
        value.f006Int = 0;
        value.f007F32 = 0.0f;
        value.f008U64 = 0;
        value.f009Int = 0;
        value.f010F32 = 0.0f;
        value.f011Bits = 0;
        value.f012Bool = false;
        value.f013F32 = 0.0f;
        value.f014Uint = 0;
        value.f015Int = 0;
        value.f016Fixed = 0;
        value.f017Uint = 0;
        value.f018Int = 0;
        value.f019F64 = 0.0;
        value.f020F32 = 0.0f;
        value.f021Ufixed = 0;
        value.f022F32 = 0.0f;
        value.f023Bits = 0;
        value.f024F32 = 0.0f;
        value.f025Fixed = 0;
        value.f026Bits = 0;
        value.f027Cf32 = 0.0f;
        value.f028Bits = 0;
        value.f029I64 = 0;
        value.f030F32 = 0.0f;
        value.f031Bits = 0;
        value.f032Int = 0;
        value.f033Uint = 0;
        value.f034Uint = 0;
        value.f035Bits = 0;
        value.f036Enum = 0;
        value.f037Bool = false;
        value.f038Bool = false;
        value.f039Bits = 0;
        value.f040Fixed = 0;
        value.f041Int = 0;
        value.f042Bits = 0;
        value.f043Bool = false;
        value.f044F32 = 0.0f;
        value.f045Bits = 0;
        value.f046Uint = 0;
        value.f047Int = 0;
        value.f048F64 = 0.0;
        value.f049Ufixed = 0;
        value.f050Bool = false;
        value.f051Bool = false;
        value.f052Int = 0;
        value.f053F32 = 0.0f;
        value.f054Int = 0;
        value.f055Bool = false;
        value.f056Int = 0;
        value.f057Int = 0;
        value.f058F32 = 0.0f;
        value.f059F64 = 0.0;
        value.f060Bits = 0;
        value.f061Cf32 = 0.0f;
        value.f062Uint = 0;
        value.f063I64 = 0;
        value.f064Uint = 0;
        value.f065Cf32 = 0.0f;
        value.f066Ufixed = 0;
        value.f067Cf32 = 0.0f;
        value.f068Cf32 = 0.0f;
        value.f069Bits = 0;
        value.f070Uint = 0;
        value.f071Cf32 = 0.0f;
        value.f072Cf32 = 0.0f;
        value.f073Int = 0;
        value.f074Bool = false;
        value.f075U64 = 0;
        value.f076Int = 0;
        value.f077Int = 0;
        value.f078Bits = 0;
        value.f079Uint = 0;
        value.f080Bool = false;
        value.f081Bits = 0;
        value.f082Bits = 0;
        value.f083Enum = 0;
        value.f084Ufixed = 0;
        value.f085Bits = 0;
        value.f086Uint = 0;
        value.f087F64 = 0.0;
        value.f088Int = 0;
        value.f089Bits = 0;
        value.f090Uint = 0;
        value.f091Flags = 0;
        value.f092Bool = false;
        value.f093Bits = 0;
        value.f094Bool = false;
        value.f095Fixed = 0;
        value.f096Bits = 0;
        value.f097Bits = 0;
    }

    // checkWriteRealPacket is writeRealPacket's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteRealPacket(RealPacket value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= realPacketMaxBytes;
        assert value.f001Int >= -805495;
        assert value.f001Int <= 805495;
        assert value.f003Int >= -835897;
        assert value.f003Int <= 835897;
        assert value.f004Cf32 - value.f004Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert (value.f005Uint & 0xffffL) >= 0;
        assert (value.f005Uint & 0xffffL) <= 7316;
        assert value.f006Int >= -1513;
        assert value.f006Int <= 1513;
        assert value.f009Int >= -22;
        assert value.f009Int <= 22;
        if (value.f012Bool) {
            assert (value.f014Uint & 0xffffL) >= 0;
            assert (value.f014Uint & 0xffffL) <= 775;
            assert value.f015Int >= -21;
            assert value.f015Int <= 21;
            assert value.f016Fixed >= -37748736;
            assert value.f016Fixed <= 37748736;
            assert (value.f017Uint & 0xffffL) >= 0;
            assert (value.f017Uint & 0xffffL) <= 4606;
        }
        assert value.f018Int >= -834;
        assert value.f018Int <= 834;
        assert (value.f021Ufixed & 0xffffffffL) >= 0;
        assert (value.f021Ufixed & 0xffffffffL) <= 102977536;
        assert value.f025Fixed >= -30464;
        assert value.f025Fixed <= 30464;
        assert value.f027Cf32 - value.f027Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert value.f032Int >= -3;
        assert value.f032Int <= 3;
        assert (value.f033Uint & 0xffffffffL) >= 0;
        assert (value.f033Uint & 0xffffffffL) <= 142780;
        assert (value.f034Uint & 0xffffL) >= 0;
        assert (value.f034Uint & 0xffffL) <= 14149;
        assert (value.f036Enum & 0xffL) >= 0;
        assert (value.f036Enum & 0xffL) <= 5;
        assert value.f040Fixed >= -20480;
        assert value.f040Fixed <= 20480;
        assert value.f041Int >= -55;
        assert value.f041Int <= 55;
        if (value.f043Bool) {
            assert (value.f046Uint & 0xffffffffL) >= 0;
            assert (value.f046Uint & 0xffffffffL) <= 76063;
            assert value.f047Int >= -430976;
            assert value.f047Int <= 430976;
        }
        assert (value.f049Ufixed & 0xffffL) >= 0;
        assert (value.f049Ufixed & 0xffffL) <= 49152;
        if (value.f050Bool) {
            assert value.f052Int >= -57;
            assert value.f052Int <= 57;
            assert value.f054Int >= -35;
            assert value.f054Int <= 35;
        }
        assert value.f056Int >= -13;
        assert value.f056Int <= 13;
        assert value.f057Int >= -15;
        assert value.f057Int <= 15;
        assert value.f061Cf32 - value.f061Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert (value.f062Uint & 0xffffL) >= 0;
        assert (value.f062Uint & 0xffffL) <= 503;
        assert (value.f064Uint & 0xffffL) >= 0;
        assert (value.f064Uint & 0xffffL) <= 299;
        assert value.f065Cf32 - value.f065Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert (value.f066Ufixed & 0xffffL) >= 0;
        assert (value.f066Ufixed & 0xffffL) <= 32768;
        assert value.f067Cf32 - value.f067Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert value.f068Cf32 - value.f068Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert (value.f070Uint & 0xffL) >= 0;
        assert (value.f070Uint & 0xffL) <= 2;
        assert value.f071Cf32 - value.f071Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert value.f072Cf32 - value.f072Cf32 == 0.0f; // finite: NaN and both infinities fail this
        assert value.f073Int >= -4;
        assert value.f073Int <= 4;
        if (value.f074Bool) {
            assert value.f076Int >= -26218;
            assert value.f076Int <= 26218;
            assert value.f077Int >= -17;
            assert value.f077Int <= 17;
            assert (value.f079Uint & 0xffL) >= 0;
            assert (value.f079Uint & 0xffL) <= 17;
        }
        assert (value.f083Enum & 0xffL) >= 0;
        assert (value.f083Enum & 0xffL) <= 5;
        assert (value.f084Ufixed & 0xffL) >= 0;
        assert (value.f084Ufixed & 0xffL) <= 128;
        assert (value.f086Uint & 0xffffL) >= 0;
        assert (value.f086Uint & 0xffffL) <= 399;
        assert value.f088Int >= -694;
        assert value.f088Int <= 694;
        assert (value.f090Uint & 0xffL) >= 0;
        assert (value.f090Uint & 0xffL) <= 214;
        assert value.f091Flags >>> 5 == 0;
        assert value.f095Fixed >= -103350272;
        assert value.f095Fixed <= 103350272;
        return true;
    }

    // writeRealPacket packs value into data — the trusted writer (contracts in the
    // checkWriteRealPacket predicate, one dormant assert call without -ea). The buffer
    // must hold realPacketMaxBytes. Returns the bytes written.
    public static int writeRealPacket(RealPacket value, byte[] data) {
        assert checkWriteRealPacket(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.f001Int + 805495) & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        {
            final long b = Double.doubleToRawLongBits(value.f002F64);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = (value.f003Int + 835897) & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        {
            float n = (value.f004Cf32 - 0.0f) / 2000.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 20000.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 15;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (15 - scratchBits);
        }
        v = ((value.f005Uint & 0xffffL)) & 0x1fffL;
        scratch |= v << scratchBits;
        scratchBits += 13;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (13 - scratchBits);
        }
        v = (value.f006Int + 1513) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f007F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.f008U64 & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f008U64 >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f009Int + 22) & 0x3fL;
        scratch |= v << scratchBits;
        scratchBits += 6;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (6 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f010F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.f011Bits & 0x3ffL;
        scratch |= v << scratchBits;
        scratchBits += 10;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (10 - scratchBits);
        }
        v = value.f012Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (value.f012Bool) {
            v = Float.floatToRawIntBits(value.f013F32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = ((value.f014Uint & 0xffffL)) & 0x3ffL;
            scratch |= v << scratchBits;
            scratchBits += 10;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (10 - scratchBits);
            }
            v = (value.f015Int + 21) & 0x3fL;
            scratch |= v << scratchBits;
            scratchBits += 6;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (6 - scratchBits);
            }
            v = (value.f016Fixed + 37748736) & 0x7ffffffL;
            scratch |= v << scratchBits;
            scratchBits += 27;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (27 - scratchBits);
            }
            v = ((value.f017Uint & 0xffffL)) & 0x1fffL;
            scratch |= v << scratchBits;
            scratchBits += 13;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (13 - scratchBits);
            }
        }
        v = (value.f018Int + 834) & 0x7ffL;
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        {
            final long b = Double.doubleToRawLongBits(value.f019F64);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = Float.floatToRawIntBits(value.f020F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = ((value.f021Ufixed & 0xffffffffL)) & 0x7ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 27;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (27 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f022F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.f023Bits & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f024F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f025Fixed + 30464) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = value.f026Bits & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        {
            float n = (value.f027Cf32 - -2.0f) / 4.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 16.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = value.f028Bits & 0xfL;
        scratch |= v << scratchBits;
        scratchBits += 4;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (4 - scratchBits);
        }
        v = value.f029I64 & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f029I64 >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f030F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.f031Bits & 0x1L;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = (value.f032Int + 3) & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = ((value.f033Uint & 0xffffffffL)) & 0x3ffffL;
        scratch |= v << scratchBits;
        scratchBits += 18;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (18 - scratchBits);
        }
        v = ((value.f034Uint & 0xffffL)) & 0x3fffL;
        scratch |= v << scratchBits;
        scratchBits += 14;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (14 - scratchBits);
        }
        v = value.f035Bits & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        v = value.f036Enum & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = value.f037Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.f038Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.f039Bits & 0x7ffffL;
        scratch |= v << scratchBits;
        scratchBits += 19;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (19 - scratchBits);
        }
        v = (value.f040Fixed + 20480) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = (value.f041Int + 55) & 0x7fL;
        scratch |= v << scratchBits;
        scratchBits += 7;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (7 - scratchBits);
        }
        v = value.f042Bits & 0x3fffffffL;
        scratch |= v << scratchBits;
        scratchBits += 30;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (30 - scratchBits);
        }
        v = value.f043Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (value.f043Bool) {
            v = Float.floatToRawIntBits(value.f044F32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = value.f045Bits & 0xfffL;
            scratch |= v << scratchBits;
            scratchBits += 12;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (12 - scratchBits);
            }
            v = ((value.f046Uint & 0xffffffffL)) & 0x1ffffL;
            scratch |= v << scratchBits;
            scratchBits += 17;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (17 - scratchBits);
            }
            v = (value.f047Int + 430976) & 0xfffffL;
            scratch |= v << scratchBits;
            scratchBits += 20;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (20 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.f048F64);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = ((value.f049Ufixed & 0xffffL)) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = value.f050Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (value.f050Bool) {
            v = value.f051Bool ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = (value.f052Int + 57) & 0x7fL;
            scratch |= v << scratchBits;
            scratchBits += 7;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (7 - scratchBits);
            }
            v = Float.floatToRawIntBits(value.f053F32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (value.f054Int + 35) & 0x7fL;
            scratch |= v << scratchBits;
            scratchBits += 7;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (7 - scratchBits);
            }
        }
        v = value.f055Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = (value.f056Int + 13) & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = (value.f057Int + 15) & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.f058F32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        {
            final long b = Double.doubleToRawLongBits(value.f059F64);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = value.f060Bits & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        {
            float n = (value.f061Cf32 - -90.0f) / 180.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 360.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        v = ((value.f062Uint & 0xffffL)) & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        v = value.f063I64 & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f063I64 >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = ((value.f064Uint & 0xffffL)) & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        {
            float n = (value.f065Cf32 - 0.0f) / 30.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 60.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 6;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (6 - scratchBits);
        }
        v = ((value.f066Ufixed & 0xffffL)) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        {
            float n = (value.f067Cf32 - -100.0f) / 200.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 800.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 10;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (10 - scratchBits);
        }
        {
            float n = (value.f068Cf32 - 0.0f) / 2000.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 2000.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        v = value.f069Bits & 0x7ffL;
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        v = ((value.f070Uint & 0xffL)) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        {
            float n = (value.f071Cf32 - 0.0f) / 10.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 500.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        {
            float n = (value.f072Cf32 - 0.0f) / 100.0f;
            if (!(n >= 0.0f)) {
                n = 0.0f;
            } else if (!(n <= 1.0f)) {
                n = 1.0f;
            }
            // two roundings, not one: the product rounds to float32 BEFORE 0.5
            // is added, and the sum rounds before the floor (SPEC §4.3)
            v = (long) Math.floor(n * 10000.0f + 0.5f);
        }
        scratch |= v << scratchBits;
        scratchBits += 14;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (14 - scratchBits);
        }
        v = (value.f073Int + 4) & 0xfL;
        scratch |= v << scratchBits;
        scratchBits += 4;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (4 - scratchBits);
        }
        v = value.f074Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (value.f074Bool) {
            v = value.f075U64 & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (value.f075U64 >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (value.f076Int + 26218) & 0xffffL;
            scratch |= v << scratchBits;
            scratchBits += 16;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (16 - scratchBits);
            }
            v = (value.f077Int + 17) & 0x3fL;
            scratch |= v << scratchBits;
            scratchBits += 6;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (6 - scratchBits);
            }
            v = value.f078Bits & 0x1ffL;
            scratch |= v << scratchBits;
            scratchBits += 9;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (9 - scratchBits);
            }
            v = ((value.f079Uint & 0xffL)) & 0x1fL;
            scratch |= v << scratchBits;
            scratchBits += 5;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (5 - scratchBits);
            }
        }
        v = value.f080Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.f081Bits & 0x1fffffffL;
        scratch |= v << scratchBits;
        scratchBits += 29;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (29 - scratchBits);
        }
        v = value.f082Bits & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = value.f083Enum & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = ((value.f084Ufixed & 0xffL)) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        v = value.f085Bits & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        v = ((value.f086Uint & 0xffffL)) & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        {
            final long b = Double.doubleToRawLongBits(value.f087F64);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = (value.f088Int + 694) & 0x7ffL;
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        v = value.f089Bits & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f089Bits >>> 32) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = ((value.f090Uint & 0xffL)) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        v = value.f091Flags & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = value.f092Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.f093Bits & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.f093Bits >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.f094Bool ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = (value.f095Fixed + 103350272) & 0xfffffffL;
        scratch |= v << scratchBits;
        scratchBits += 28;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (28 - scratchBits);
        }
        v = value.f096Bits & 0x3ffffL;
        scratch |= v << scratchBits;
        scratchBits += 18;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (18 - scratchBits);
        }
        v = value.f097Bits & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readRealPacket decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readRealPacket(RealPacket value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 291 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        if (v > 1610990) {
            return false; // a smuggled offset is refused
        }
        value.f001Int = (int) (v + -805495);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f002F64 = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        if (v > 1671794) {
            return false; // a smuggled offset is refused
        }
        value.f003Int = (int) (v + -835897);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffL;
        bitsRead += 15;
        if (v > 20000L) {
            return false; // headroom above the quantum count is refused
        }
        value.f004Cf32 = (float) v / 20000.0f * 2000.0f + 0.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffL;
        bitsRead += 13;
        if (v > 7316) {
            return false; // a smuggled offset is refused
        }
        value.f005Uint = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 3026) {
            return false; // a smuggled offset is refused
        }
        value.f006Int = (short) (v + -1513);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f007F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f008U64 = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fL;
        bitsRead += 6;
        if (v > 44) {
            return false; // a smuggled offset is refused
        }
        value.f009Int = (byte) (v + -22);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f010F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffL;
        bitsRead += 10;
        value.f011Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f012Bool = v != 0;
        if (value.f012Bool) {
            if (bitsRead + 88 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            value.f013F32 = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x3ffL;
            bitsRead += 10;
            if (v > 775) {
                return false; // a smuggled offset is refused
            }
            value.f014Uint = (short) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x3fL;
            bitsRead += 6;
            if (v > 42) {
                return false; // a smuggled offset is refused
            }
            value.f015Int = (byte) (v + -21);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x7ffffffL;
            bitsRead += 27;
            if (v > 75497472) {
                return false; // a smuggled offset is refused
            }
            value.f016Fixed = (int) (v + -37748736);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1fffL;
            bitsRead += 13;
            if (v > 4606) {
                return false; // a smuggled offset is refused
            }
            value.f017Uint = (short) v;
        } else {
            value.f013F32 = 0.0f;
            value.f014Uint = 0;
            value.f015Int = 0;
            value.f016Fixed = 0;
            value.f017Uint = 0;
        }
        if (bitsRead + 476 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        if (v > 1668) {
            return false; // a smuggled offset is refused
        }
        value.f018Int = (short) (v + -834);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f019F64 = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f020F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffffffL;
        bitsRead += 27;
        if (v > 102977536) {
            return false; // a smuggled offset is refused
        }
        value.f021Ufixed = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f022F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        value.f023Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f024F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        if (v > 60928) {
            return false; // a smuggled offset is refused
        }
        value.f025Fixed = (short) (v + -30464);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        value.f026Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        if (v > 16L) {
            return false; // headroom above the quantum count is refused
        }
        value.f027Cf32 = (float) v / 16.0f * 4.0f + -2.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfL;
        bitsRead += 4;
        value.f028Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f029I64 = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f030F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f031Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        if (v > 6) {
            return false; // a smuggled offset is refused
        }
        value.f032Int = (byte) (v + -3);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffffL;
        bitsRead += 18;
        if (v > 142780) {
            return false; // a smuggled offset is refused
        }
        value.f033Uint = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fffL;
        bitsRead += 14;
        if (v > 14149) {
            return false; // a smuggled offset is refused
        }
        value.f034Uint = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        value.f035Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        if (v > 5) {
            return false; // headroom above the wire range is refused
        }
        value.f036Enum = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f037Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f038Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffffL;
        bitsRead += 19;
        value.f039Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        if (v > 40960) {
            return false; // a smuggled offset is refused
        }
        value.f040Fixed = (short) (v + -20480);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fL;
        bitsRead += 7;
        if (v > 110) {
            return false; // a smuggled offset is refused
        }
        value.f041Int = (byte) (v + -55);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fffffffL;
        bitsRead += 30;
        value.f042Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f043Bool = v != 0;
        if (value.f043Bool) {
            if (bitsRead + 81 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            value.f044F32 = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xfffL;
            bitsRead += 12;
            value.f045Bits = (int) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1ffffL;
            bitsRead += 17;
            if (v > 76063) {
                return false; // a smuggled offset is refused
            }
            value.f046Uint = (int) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xfffffL;
            bitsRead += 20;
            if (v > 861952) {
                return false; // a smuggled offset is refused
            }
            value.f047Int = (int) (v + -430976);
        } else {
            value.f044F32 = 0.0f;
            value.f045Bits = 0;
            value.f046Uint = 0;
            value.f047Int = 0;
        }
        if (bitsRead + 81 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f048F64 = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        if (v > 49152) {
            return false; // a smuggled offset is refused
        }
        value.f049Ufixed = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f050Bool = v != 0;
        if (value.f050Bool) {
            if (bitsRead + 47 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            value.f051Bool = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x7fL;
            bitsRead += 7;
            if (v > 114) {
                return false; // a smuggled offset is refused
            }
            value.f052Int = (byte) (v + -57);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            value.f053F32 = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x7fL;
            bitsRead += 7;
            if (v > 70) {
                return false; // a smuggled offset is refused
            }
            value.f054Int = (byte) (v + -35);
        } else {
            value.f051Bool = false;
            value.f052Int = 0;
            value.f053F32 = 0.0f;
            value.f054Int = 0;
        }
        if (bitsRead + 290 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f055Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        if (v > 26) {
            return false; // a smuggled offset is refused
        }
        value.f056Int = (byte) (v + -13);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        if (v > 30) {
            return false; // a smuggled offset is refused
        }
        value.f057Int = (byte) (v + -15);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.f058F32 = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f059F64 = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        value.f060Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        if (v > 360L) {
            return false; // headroom above the quantum count is refused
        }
        value.f061Cf32 = (float) v / 360.0f * 180.0f + -90.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        if (v > 503) {
            return false; // a smuggled offset is refused
        }
        value.f062Uint = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f063I64 = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        if (v > 299) {
            return false; // a smuggled offset is refused
        }
        value.f064Uint = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fL;
        bitsRead += 6;
        if (v > 60L) {
            return false; // headroom above the quantum count is refused
        }
        value.f065Cf32 = (float) v / 60.0f * 30.0f + 0.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        if (v > 32768) {
            return false; // a smuggled offset is refused
        }
        value.f066Ufixed = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffL;
        bitsRead += 10;
        if (v > 800L) {
            return false; // headroom above the quantum count is refused
        }
        value.f067Cf32 = (float) v / 800.0f * 200.0f + -100.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        if (v > 2000L) {
            return false; // headroom above the quantum count is refused
        }
        value.f068Cf32 = (float) v / 2000.0f * 2000.0f + 0.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        value.f069Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // a smuggled offset is refused
        }
        value.f070Uint = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        if (v > 500L) {
            return false; // headroom above the quantum count is refused
        }
        value.f071Cf32 = (float) v / 500.0f * 10.0f + 0.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fffL;
        bitsRead += 14;
        if (v > 10000L) {
            return false; // headroom above the quantum count is refused
        }
        value.f072Cf32 = (float) v / 10000.0f * 100.0f + 0.0f;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfL;
        bitsRead += 4;
        if (v > 8) {
            return false; // a smuggled offset is refused
        }
        value.f073Int = (byte) (v + -4);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f074Bool = v != 0;
        if (value.f074Bool) {
            if (bitsRead + 100 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.f075U64 = lo;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffL;
            bitsRead += 16;
            if (v > 52436) {
                return false; // a smuggled offset is refused
            }
            value.f076Int = (short) (v + -26218);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x3fL;
            bitsRead += 6;
            if (v > 34) {
                return false; // a smuggled offset is refused
            }
            value.f077Int = (byte) (v + -17);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1ffL;
            bitsRead += 9;
            value.f078Bits = (int) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1fL;
            bitsRead += 5;
            if (v > 17) {
                return false; // a smuggled offset is refused
            }
            value.f079Uint = (byte) v;
        } else {
            value.f075U64 = 0;
            value.f076Int = 0;
            value.f077Int = 0;
            value.f078Bits = 0;
            value.f079Uint = 0;
        }
        if (bitsRead + 356 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f080Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffffL;
        bitsRead += 29;
        value.f081Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        value.f082Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        if (v > 5) {
            return false; // headroom above the wire range is refused
        }
        value.f083Enum = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        if (v > 128) {
            return false; // a smuggled offset is refused
        }
        value.f084Ufixed = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        value.f085Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        if (v > 399) {
            return false; // a smuggled offset is refused
        }
        value.f086Uint = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f087F64 = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        if (v > 1388) {
            return false; // a smuggled offset is refused
        }
        value.f088Int = (short) (v + -694);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        lo |= v << 32;
        value.f089Bits = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        if (v > 214) {
            return false; // a smuggled offset is refused
        }
        value.f090Uint = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        value.f091Flags = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f092Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.f093Bits = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.f094Bool = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffffffL;
        bitsRead += 28;
        if (v > 206700544) {
            return false; // a smuggled offset is refused
        }
        value.f095Fixed = (int) (v + -103350272);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffffL;
        bitsRead += 18;
        value.f096Bits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.f097Bits = (int) v;
        return true;
    }

    // measureRealPacket is the exact wire bits writeRealPacket would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureRealPacket(RealPacket value) {
        int bits = 0;
        bits += 291;
        if (value.f012Bool) {
            bits += 88;
        }
        bits += 476;
        if (value.f043Bool) {
            bits += 81;
        }
        bits += 81;
        if (value.f050Bool) {
            bits += 47;
        }
        bits += 290;
        if (value.f074Bool) {
            bits += 100;
        }
        bits += 356;
        return bits;
    }
}
