// Code generated by the schema compiler from Bench.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package bench — protocol id 0x694f261d887abaa5
//
// The shipped Java wire path (issue #156): the serialize.java bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, data) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts live in one assert-predicate call — active
// under -ea, dormant bytecode otherwise — and width masks stay, as wire
// arithmetic. The buffer must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, data, numBits) -> boolean, the family read verdict.
// The wire is a trust boundary: reads validate everything — bounds fused
// per static run, ranges, wire constants, reserved and align padding,
// union tags — and NEVER throw on hostile bytes. No slack past the
// payload is required: the reader prices its 64-bit windows inside the
// buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

package bench;

// Bench carries every generated declaration of Bench.schema — Java has no
// top-level functions or constants, so the file's one public class is their
// home; types nest inside it (SPEC §6.1 naming).
public final class Bench {
    private Bench() {}

    // 64-bit little-endian word access into byte[] — the proven-fast JVM
    // form (VarHandle byteArrayViewVarHandle, serialize.java's own).
    private static final java.lang.invoke.VarHandle LONG_LE =
            java.lang.invoke.MethodHandles.byteArrayViewVarHandle(
                    long[].class, java.nio.ByteOrder.LITTLE_ENDIAN);

    // The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
    // sides at the same id speak identical bits; there is no other versioning.
    public static final long protocolId = 0x694f261d887abaa5L;

    // type BenchPacket
    public static final class BenchPacket {
        // wire [-100, 100]
        public int a;
        // wire [0, 65535]
        public int b;
        // wire [-1000000, 1000000]
        public int c;
        public int bits7;
        public int bits13;
        public int bits23;
        public boolean flag;
        public float x;
        public float y;
        public float z;
        public long big;
        public final byte[] blob = new byte[17];
    }

    // benchPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // benchPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int benchPacketMaxBits = 392;
    public static final int benchPacketMaxBytes = 56;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroBenchPacket(BenchPacket value) {
        value.a = 0;
        value.b = 0;
        value.c = 0;
        value.bits7 = 0;
        value.bits13 = 0;
        value.bits23 = 0;
        value.flag = false;
        value.x = 0.0f;
        value.y = 0.0f;
        value.z = 0.0f;
        value.big = 0;
        java.util.Arrays.fill(value.blob, (byte) 0);
    }

    // checkWriteBenchPacket is writeBenchPacket's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteBenchPacket(BenchPacket value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= benchPacketMaxBytes;
        assert value.a >= -100;
        assert value.a <= 100;
        assert value.b >= 0;
        assert value.b <= 65535;
        assert value.c >= -1000000;
        assert value.c <= 1000000;
        return true;
    }

    // writeBenchPacket packs value into data — the trusted writer (contracts in the
    // checkWriteBenchPacket predicate, one dormant assert call without -ea). The buffer
    // must hold benchPacketMaxBytes. Returns the bytes written.
    public static int writeBenchPacket(BenchPacket value, byte[] data) {
        assert checkWriteBenchPacket(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.a + 100) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        v = (value.b) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = (value.c + 1000000) & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        v = value.bits7 & 0x7fL;
        scratch |= v << scratchBits;
        scratchBits += 7;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (7 - scratchBits);
        }
        v = value.bits13 & 0x1fffL;
        scratch |= v << scratchBits;
        scratchBits += 13;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (13 - scratchBits);
        }
        v = value.bits23 & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = value.flag ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.x) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.y) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.z) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.big & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.big >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        {
            final int pad = scratchBits & 7;
            if (pad != 0) {
                scratchBits += 8 - pad;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = 0;
                }
            }
        }
        // byte-aligned payload — the fused bulk copy (flush, copy, reload),
        // wire-identical to the per-byte merge
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        System.arraycopy(value.blob, 0, data, wordIndex * 8 + (scratchBits >>> 3), 17);
        scratchBits += 136;
        wordIndex += scratchBits >>> 6;
        scratchBits &= 63;
        if (scratchBits != 0) {
            scratch = (long) LONG_LE.get(data, wordIndex * 8) & ((1L << scratchBits) - 1);
        } else {
            scratch = 0;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readBenchPacket decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readBenchPacket(BenchPacket value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 249 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        if (v > 200) {
            return false; // a smuggled offset is refused
        }
        value.a = (int) (v + -100);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        value.b = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        if (v > 2000000) {
            return false; // a smuggled offset is refused
        }
        value.c = (int) (v + -1000000);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fL;
        bitsRead += 7;
        value.bits7 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffL;
        bitsRead += 13;
        value.bits13 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        value.bits23 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.flag = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.x = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.y = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.z = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.big = lo;
        {
            final int pad = bitsRead & 7;
            if (pad != 0) {
                if (bitsRead + (8 - pad) > numBits) {
                    return false;
                }
                if ((data[bitsRead >>> 3] & 0xff) >>> pad != 0) {
                    return false; // nonzero padding is refused (SPEC §4.3)
                }
                bitsRead += 8 - pad;
            }
        }
        if (bitsRead + 136 > numBits) {
            return false;
        }
        System.arraycopy(data, bitsRead >>> 3, value.blob, 0, 17);
        bitsRead += 136;
        return true;
    }

    // measureBenchPacket is the exact wire bits writeBenchPacket would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureBenchPacket(BenchPacket value) {
        int bits = 0;
        bits += 249;
        bits += (8 - (bits & 7)) & 7;
        bits += 136;
        return bits;
    }

    // type BenchInts
    public static final class BenchInts {
        // wire [-100, 100]
        public int f0;
        // wire [0, 65535]
        public int f1;
        // wire [-1000000, 1000000]
        public int f2;
        // wire [0, 3]
        public int f3;
        // wire [-15, 15]
        public int f4;
        // wire [0, 1000]
        public int f5;
        // wire [-2048, 2047]
        public int f6;
        // wire [0, 255]
        public int f7;
        // wire [-600000, 600000]
        public int f8;
        // wire [0, 100]
        public int f9;
    }

    // benchIntsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // benchIntsMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int benchIntsMaxBits = 110;
    public static final int benchIntsMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroBenchInts(BenchInts value) {
        value.f0 = 0;
        value.f1 = 0;
        value.f2 = 0;
        value.f3 = 0;
        value.f4 = 0;
        value.f5 = 0;
        value.f6 = 0;
        value.f7 = 0;
        value.f8 = 0;
        value.f9 = 0;
    }

    // checkWriteBenchInts is writeBenchInts's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteBenchInts(BenchInts value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= benchIntsMaxBytes;
        assert value.f0 >= -100;
        assert value.f0 <= 100;
        assert value.f1 >= 0;
        assert value.f1 <= 65535;
        assert value.f2 >= -1000000;
        assert value.f2 <= 1000000;
        assert value.f3 >= 0;
        assert value.f3 <= 3;
        assert value.f4 >= -15;
        assert value.f4 <= 15;
        assert value.f5 >= 0;
        assert value.f5 <= 1000;
        assert value.f6 >= -2048;
        assert value.f6 <= 2047;
        assert value.f7 >= 0;
        assert value.f7 <= 255;
        assert value.f8 >= -600000;
        assert value.f8 <= 600000;
        assert value.f9 >= 0;
        assert value.f9 <= 100;
        return true;
    }

    // writeBenchInts packs value into data — the trusted writer (contracts in the
    // checkWriteBenchInts predicate, one dormant assert call without -ea). The buffer
    // must hold benchIntsMaxBytes. Returns the bytes written.
    public static int writeBenchInts(BenchInts value, byte[] data) {
        assert checkWriteBenchInts(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.f0 + 100) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        v = (value.f1) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = (value.f2 + 1000000) & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        v = (value.f3) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        v = (value.f4 + 15) & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = (value.f5) & 0x3ffL;
        scratch |= v << scratchBits;
        scratchBits += 10;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (10 - scratchBits);
        }
        v = (value.f6 + 2048) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.f7) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        v = (value.f8 + 600000) & 0x1fffffL;
        scratch |= v << scratchBits;
        scratchBits += 21;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (21 - scratchBits);
        }
        v = (value.f9) & 0x7fL;
        scratch |= v << scratchBits;
        scratchBits += 7;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (7 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readBenchInts decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readBenchInts(BenchInts value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 110 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        if (v > 200) {
            return false; // a smuggled offset is refused
        }
        value.f0 = (int) (v + -100);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        value.f1 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        if (v > 2000000) {
            return false; // a smuggled offset is refused
        }
        value.f2 = (int) (v + -1000000);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        value.f3 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        if (v > 30) {
            return false; // a smuggled offset is refused
        }
        value.f4 = (int) (v + -15);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffL;
        bitsRead += 10;
        if (v > 1000) {
            return false; // a smuggled offset is refused
        }
        value.f5 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.f6 = (int) (v + -2048);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        value.f7 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffffL;
        bitsRead += 21;
        if (v > 1200000) {
            return false; // a smuggled offset is refused
        }
        value.f8 = (int) (v + -600000);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fL;
        bitsRead += 7;
        if (v > 100) {
            return false; // a smuggled offset is refused
        }
        value.f9 = (int) v;
        return true;
    }

    // measureBenchInts is the exact wire bits writeBenchInts would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureBenchInts(BenchInts value) {
        return 110;
    }

    // type BenchBits
    public static final class BenchBits {
        public int b7;
        public int b13;
        public int b23;
        public int b3;
        public int b32;
        public int b11;
        public int b19;
        public long b48;
    }

    // benchBitsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // benchBitsMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int benchBitsMaxBits = 156;
    public static final int benchBitsMaxBytes = 24;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroBenchBits(BenchBits value) {
        value.b7 = 0;
        value.b13 = 0;
        value.b23 = 0;
        value.b3 = 0;
        value.b32 = 0;
        value.b11 = 0;
        value.b19 = 0;
        value.b48 = 0;
    }

    // checkWriteBenchBits is writeBenchBits's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteBenchBits(BenchBits value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= benchBitsMaxBytes;
        return true;
    }

    // writeBenchBits packs value into data — the trusted writer (contracts in the
    // checkWriteBenchBits predicate, one dormant assert call without -ea). The buffer
    // must hold benchBitsMaxBytes. Returns the bytes written.
    public static int writeBenchBits(BenchBits value, byte[] data) {
        assert checkWriteBenchBits(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.b7 & 0x7fL;
        scratch |= v << scratchBits;
        scratchBits += 7;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (7 - scratchBits);
        }
        v = value.b13 & 0x1fffL;
        scratch |= v << scratchBits;
        scratchBits += 13;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (13 - scratchBits);
        }
        v = value.b23 & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = value.b3 & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = value.b32 & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.b11 & 0x7ffL;
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        v = value.b19 & 0x7ffffL;
        scratch |= v << scratchBits;
        scratchBits += 19;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (19 - scratchBits);
        }
        v = value.b48 & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.b48 >>> 32) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readBenchBits decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readBenchBits(BenchBits value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 156 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fL;
        bitsRead += 7;
        value.b7 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fffL;
        bitsRead += 13;
        value.b13 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        value.b23 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        value.b3 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.b32 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        value.b11 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffffL;
        bitsRead += 19;
        value.b19 = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        lo |= v << 32;
        value.b48 = lo;
        return true;
    }

    // measureBenchBits is the exact wire bits writeBenchBits would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureBenchBits(BenchBits value) {
        return 156;
    }

    // type BenchMixed
    public static final class BenchMixed {
        // wire [0, 65535]
        public int sequence;
        public int ackBits;
        public int entityId;
        // wire [-16384, 16383]
        public int posX;
        // wire [-16384, 16383]
        public int posY;
        // wire [-16384, 16383]
        public int posZ;
        public int yaw;
        public boolean moving;
        public boolean firing;
        public long timestamp;
        // wire [0, 15]
        public int weapon;
    }

    // benchMixedMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // benchMixedMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int benchMixedMaxBits = 168;
    public static final int benchMixedMaxBytes = 24;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroBenchMixed(BenchMixed value) {
        value.sequence = 0;
        value.ackBits = 0;
        value.entityId = 0;
        value.posX = 0;
        value.posY = 0;
        value.posZ = 0;
        value.yaw = 0;
        value.moving = false;
        value.firing = false;
        value.timestamp = 0;
        value.weapon = 0;
    }

    // checkWriteBenchMixed is writeBenchMixed's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteBenchMixed(BenchMixed value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= benchMixedMaxBytes;
        assert value.sequence >= 0;
        assert value.sequence <= 65535;
        assert value.posX >= -16384;
        assert value.posX <= 16383;
        assert value.posY >= -16384;
        assert value.posY <= 16383;
        assert value.posZ >= -16384;
        assert value.posZ <= 16383;
        assert value.weapon >= 0;
        assert value.weapon <= 15;
        return true;
    }

    // writeBenchMixed packs value into data — the trusted writer (contracts in the
    // checkWriteBenchMixed predicate, one dormant assert call without -ea). The buffer
    // must hold benchMixedMaxBytes. Returns the bytes written.
    public static int writeBenchMixed(BenchMixed value, byte[] data) {
        assert checkWriteBenchMixed(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.sequence) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = value.ackBits & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.entityId & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.posX + 16384) & 0x7fffL;
        scratch |= v << scratchBits;
        scratchBits += 15;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (15 - scratchBits);
        }
        v = (value.posY + 16384) & 0x7fffL;
        scratch |= v << scratchBits;
        scratchBits += 15;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (15 - scratchBits);
        }
        v = (value.posZ + 16384) & 0x7fffL;
        scratch |= v << scratchBits;
        scratchBits += 15;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (15 - scratchBits);
        }
        v = value.yaw & 0x1ffL;
        scratch |= v << scratchBits;
        scratchBits += 9;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (9 - scratchBits);
        }
        v = value.moving ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.firing ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.timestamp & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.timestamp >>> 32) & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = (value.weapon) & 0xfL;
        scratch |= v << scratchBits;
        scratchBits += 4;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (4 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readBenchMixed decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readBenchMixed(BenchMixed value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 168 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        value.sequence = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.ackBits = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.entityId = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffL;
        bitsRead += 15;
        value.posX = (int) (v + -16384);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffL;
        bitsRead += 15;
        value.posY = (int) (v + -16384);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffL;
        bitsRead += 15;
        value.posZ = (int) (v + -16384);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffL;
        bitsRead += 9;
        value.yaw = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.moving = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.firing = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        lo |= v << 32;
        value.timestamp = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfL;
        bitsRead += 4;
        value.weapon = (int) v;
        return true;
    }

    // measureBenchMixed is the exact wire bits writeBenchMixed would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureBenchMixed(BenchMixed value) {
        return 168;
    }
}
