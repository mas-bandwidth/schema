// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0xd093e62e6f907c36
//
// Wire functions return bool — the C++-style early-out. A schema validation
// failure (a wrong wire constant, nonzero reserved bits, an interior null)
// returns false WITHOUT latching; stream failures latch on stream.Error —
// the runtime's own sticky latch. Callers get bool always; Error tells the
// two apart.

using System.Runtime.CompilerServices;
using Serialize;

namespace Benchtable
{

    // TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
    // a native enum with unsigned backing — | max = ... headroom values are
    // representable because C# enums are open over their backing type (SPEC §6.1)
    public enum TableWeapon : byte
    {
        None = 0,
        Fists = 1,
        Pistol = 2,
        Shotgun = 3,
        Rifle = 4,
        Sniper = 5,
        Smg = 6,
        Rocket = 7,
        Grenade = 8,
        Plasma = 9,
        Railgun = 10,
        Flamer = 11,
        Mine = 12,
        Turret = 13,
        Drone = 14,
        Repair = 15,
        Count = 15, // the declared variant count (SPEC §4.2)
        Max = 15, // the exported extent (SPEC §4.2)
    }

    // type TableHitEvent
    public sealed class TableHitEvent
    {
        public uint TargetId;
        public int Damage; // wire [0, 4095]
        public int HitKind; // wire [0, 7]
        public bool Crit;
    }

    // type TableChatEvent
    public sealed class TableChatEvent
    {
        public int Channel; // wire [0, 3]
        public uint Speaker;
    }

    // type TablePickupEvent
    public sealed class TablePickupEvent
    {
        public uint ItemId;
        public int Amount; // wire [0, 255]
    }

    // TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
    public enum TableEventType : byte
    {
        None = 0,
        Hit = 1,
        Chat = 2,
        Pickup = 3,
        Max = 3, // the exported extent (SPEC §4.2)
    }

    // TableEvent — at most one of the arms; Type says which. Construction is the empty
    // union (None). A read zero-establishes exactly the selected arm before
    // decoding it (SPEC §5); unselected arms keep what they last held — the
    // MessageStorage reuse discipline. Consumers read the selected arm only.
    public sealed class TableEvent
    {
        public TableEventType Type;
        public TableHitEvent Hit = new TableHitEvent();
        public TableChatEvent Chat = new TableChatEvent();
        public TablePickupEvent Pickup = new TablePickupEvent();
    }

    // Schema carries every generated function and constant of the unit — C# has
    // no namespace-level functions or constants, so the static class is their
    // home (SPEC §6.1 naming); partial, one slice per generated file.
    public static partial class Schema
    {
        // The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
        // sides at the same id speak identical bits; there is no other versioning.
        public const ulong ProtocolId = 0xd093e62e6f907c36;

        // EnumNameTableWeapon: debug/log/tooling name for any TableWeapon wire value —
        // out-of-set values (wire-legal up to the declared max) name as "???"
        public static string EnumNameTableWeapon(ulong value)
        {
            switch (value)
            {
                case (ulong)TableWeapon.None:
                    return "None";
                case (ulong)TableWeapon.Fists:
                    return "Fists";
                case (ulong)TableWeapon.Pistol:
                    return "Pistol";
                case (ulong)TableWeapon.Shotgun:
                    return "Shotgun";
                case (ulong)TableWeapon.Rifle:
                    return "Rifle";
                case (ulong)TableWeapon.Sniper:
                    return "Sniper";
                case (ulong)TableWeapon.Smg:
                    return "Smg";
                case (ulong)TableWeapon.Rocket:
                    return "Rocket";
                case (ulong)TableWeapon.Grenade:
                    return "Grenade";
                case (ulong)TableWeapon.Plasma:
                    return "Plasma";
                case (ulong)TableWeapon.Railgun:
                    return "Railgun";
                case (ulong)TableWeapon.Flamer:
                    return "Flamer";
                case (ulong)TableWeapon.Mine:
                    return "Mine";
                case (ulong)TableWeapon.Turret:
                    return "Turret";
                case (ulong)TableWeapon.Drone:
                    return "Drone";
                case (ulong)TableWeapon.Repair:
                    return "Repair";
                default:
                    return "???";
            }
        }

        // TableDamage — one bit per variant, consumed as masks; flags-typed fields store a
        // plain ulong, wire 8 bits (SPEC §4.2). Masks are flat PascalCase — the Go
        // target's spelling exactly, so the checker's existing claims cover them.
        public const ulong TableDamageBleeding = 1ul << 0;
        public const ulong TableDamageBurning = 1ul << 1;
        public const ulong TableDamageStunned = 1ul << 2;
        public const ulong TableDamageSlowed = 1ul << 3;
        public const ulong TableDamagePoisoned = 1ul << 4;
        public const ulong TableDamageShielded = 1ul << 5;
        public const ulong TableDamageAirborne = 1ul << 6;
        public const ulong TableDamageDowned = 1ul << 7;
        public const long TableDamageCount = 8; // the declared variant count (SPEC §4.2)

        // FlagNameTableDamage: debug/log/tooling name for bit i of TableDamage —
        // out-of-range bits name as "???"
        public static string FlagNameTableDamage(int bit)
        {
            switch (bit)
            {
                case 0: return "Bleeding";
                case 1: return "Burning";
                case 2: return "Stunned";
                case 3: return "Slowed";
                case 4: return "Poisoned";
                case 5: return "Shielded";
                case 6: return "Airborne";
                case 7: return "Downed";
                default: return "???";
            }
        }

        // FlagNamesTableDamage renders the set bits of value as "A|B" — "0" for the
        // empty set, bits past the declared variants as hex
        public static string FlagNamesTableDamage(ulong value)
        {
            string names = "";
            if ((value & (1ul << 0)) != 0)
            {
                names += "|Bleeding";
            }
            if ((value & (1ul << 1)) != 0)
            {
                names += "|Burning";
            }
            if ((value & (1ul << 2)) != 0)
            {
                names += "|Stunned";
            }
            if ((value & (1ul << 3)) != 0)
            {
                names += "|Slowed";
            }
            if ((value & (1ul << 4)) != 0)
            {
                names += "|Poisoned";
            }
            if ((value & (1ul << 5)) != 0)
            {
                names += "|Shielded";
            }
            if ((value & (1ul << 6)) != 0)
            {
                names += "|Airborne";
            }
            if ((value & (1ul << 7)) != 0)
            {
                names += "|Downed";
            }
            if ((value >> 8) != 0)
            {
                names += "|0x" + ((value >> 8) << 8).ToString("x");
            }
            return names.Length == 0 ? "0" : names.Substring(1);
        }

        // TableHitEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TableHitEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TableHitEventMaxBits = 28;
        public const long TableHitEventMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTableHitEvent(TableHitEvent value)
        {
            value.TargetId = 0;
            value.Damage = 0;
            value.HitKind = 0;
            value.Crit = false;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTableHitEvent(WriteStream stream, TableHitEvent value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTableHitEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTableHitEventBatch(ref WriteBatch batch, TableHitEvent value)
        {
            {
                // flat run: 28 bits in 1 chunk(s) — the field placement is folded
                if (value.Damage < 0 || value.Damage > 4095)
                {
                    return false;
                }
                if (value.HitKind < 0 || value.HitKind > 7)
                {
                    return false;
                }
                ulong f0 = ((ulong)value.TargetId) & 0xfffUL;
                ulong f1 = ((ulong)((uint)(value.Damage))) & 0xfffUL;
                ulong f2 = ((ulong)((uint)(value.HitKind))) & 0x7UL;
                ulong f3 = value.Crit ? 1UL : 0UL;
                uint w0 = (uint)(f0 | (f1 << 12) | (f2 << 24) | (f3 << 27));
                if (!batch.SerializeBits(ref w0, 28))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadTableHitEvent(ReadStream stream, TableHitEvent value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTableHitEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTableHitEventBatch(ref ReadBatch batch, TableHitEvent value)
        {
            {
                // flat run: 28 bits in 1 chunk(s) — one bounds check per chunk,
                // one sticky-error test for the whole run
                ulong c0 = 0;
                batch.SerializeBits64(ref c0, 28);
                if (!batch.Ok)
                {
                    return false;
                }
                ulong v0 = c0 & 0xfffUL;
                value.TargetId = (uint)v0;
                ulong v1 = (c0 >> 12) & 0xfffUL;
                value.Damage = (int)((int)(uint)v1);
                ulong v2 = (c0 >> 24) & 0x7UL;
                value.HitKind = (int)((int)(uint)v2);
                ulong v3 = (c0 >> 27) & 0x1UL;
                value.Crit = v3 != 0UL;
            }
            return true;
        }

        // TableChatEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TableChatEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TableChatEventMaxBits = 14;
        public const long TableChatEventMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTableChatEvent(TableChatEvent value)
        {
            value.Channel = 0;
            value.Speaker = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTableChatEvent(WriteStream stream, TableChatEvent value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTableChatEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTableChatEventBatch(ref WriteBatch batch, TableChatEvent value)
        {
            {
                // flat run: 14 bits in 1 chunk(s) — the field placement is folded
                if (value.Channel < 0 || value.Channel > 3)
                {
                    return false;
                }
                ulong f0 = ((ulong)((uint)(value.Channel))) & 0x3UL;
                ulong f1 = ((ulong)value.Speaker) & 0xfffUL;
                uint w0 = (uint)(f0 | (f1 << 2));
                if (!batch.SerializeBits(ref w0, 14))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadTableChatEvent(ReadStream stream, TableChatEvent value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTableChatEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTableChatEventBatch(ref ReadBatch batch, TableChatEvent value)
        {
            {
                // flat run: 14 bits in 1 chunk(s) — one bounds check per chunk,
                // one sticky-error test for the whole run
                ulong c0 = 0;
                batch.SerializeBits64(ref c0, 14);
                if (!batch.Ok)
                {
                    return false;
                }
                ulong v0 = c0 & 0x3UL;
                value.Channel = (int)((int)(uint)v0);
                ulong v1 = (c0 >> 2) & 0xfffUL;
                value.Speaker = (uint)v1;
            }
            return true;
        }

        // TablePickupEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TablePickupEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TablePickupEventMaxBits = 18;
        public const long TablePickupEventMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTablePickupEvent(TablePickupEvent value)
        {
            value.ItemId = 0;
            value.Amount = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTablePickupEvent(WriteStream stream, TablePickupEvent value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTablePickupEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTablePickupEventBatch(ref WriteBatch batch, TablePickupEvent value)
        {
            {
                // flat run: 18 bits in 1 chunk(s) — the field placement is folded
                if (value.Amount < 0 || value.Amount > 255)
                {
                    return false;
                }
                ulong f0 = ((ulong)value.ItemId) & 0x3ffUL;
                ulong f1 = ((ulong)((uint)(value.Amount))) & 0xffUL;
                uint w0 = (uint)(f0 | (f1 << 10));
                if (!batch.SerializeBits(ref w0, 18))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadTablePickupEvent(ReadStream stream, TablePickupEvent value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTablePickupEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTablePickupEventBatch(ref ReadBatch batch, TablePickupEvent value)
        {
            {
                // flat run: 18 bits in 1 chunk(s) — one bounds check per chunk,
                // one sticky-error test for the whole run
                ulong c0 = 0;
                batch.SerializeBits64(ref c0, 18);
                if (!batch.Ok)
                {
                    return false;
                }
                ulong v0 = c0 & 0x3ffUL;
                value.ItemId = (uint)v0;
                ulong v1 = (c0 >> 10) & 0xffUL;
                value.Amount = (int)((int)(uint)v1);
            }
            return true;
        }

        // TableEventMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
        // TableEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TableEventMaxBits = 30;
        public const long TableEventMaxBytes = 8;

        // ZeroTableEvent resets value to the §5 zero form — the empty union. The tag alone
        // resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
        // is unselected at None; an arm re-zeroes at its next selection.
        public static void ZeroTableEvent(TableEvent value)
        {
            value.Type = TableEventType.None;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTableEvent(WriteStream stream, TableEvent value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTableEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTableEventBatch(ref WriteBatch batch, TableEvent value)
        {
            uint tagValue = (uint)value.Type;
            if (tagValue > 3) // the tag validates BEFORE it rides (SPEC §4.8)
            {
                return false;
            }
            if (!batch.SerializeBits(ref tagValue, 2))
            {
                return false;
            }
            switch (value.Type)
            {
                case TableEventType.Hit:
                    return WriteTableHitEventBatch(ref batch, value.Hit);
                case TableEventType.Chat:
                    return WriteTableChatEventBatch(ref batch, value.Chat);
                case TableEventType.Pickup:
                    return WriteTablePickupEventBatch(ref batch, value.Pickup);
            }
            return true; // None — the tag is the whole wire (SPEC §4.8)
        }

        public static bool ReadTableEvent(ReadStream stream, TableEvent value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTableEventBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTableEventBatch(ref ReadBatch batch, TableEvent value)
        {
            int tagValue = 0;
            if (!batch.SerializeInt(ref tagValue, 0, 3)) // rejects a tag above the count (SPEC §4.8)
            {
                return false;
            }
            value.Type = (TableEventType)tagValue;
            switch (value.Type)
            {
                case TableEventType.Hit:
                    ZeroTableHitEvent(value.Hit); // the selected arm starts from the zero form (SPEC §5)
                    return ReadTableHitEventBatch(ref batch, value.Hit);
                case TableEventType.Chat:
                    ZeroTableChatEvent(value.Chat); // the selected arm starts from the zero form (SPEC §5)
                    return ReadTableChatEventBatch(ref batch, value.Chat);
                case TableEventType.Pickup:
                    ZeroTablePickupEvent(value.Pickup); // the selected arm starts from the zero form (SPEC §5)
                    return ReadTablePickupEventBatch(ref batch, value.Pickup);
            }
            return true; // None
        }
    }

}
