// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — the BLOCK FORM (docs/SPEC-TABLES.md §19): the READ half.
//
// NOTHING DECLARES THIS FORM. Every fixed table has one, and it is emitted on
// the side: import this library only if you read a block. The unit's
// <Base>Table.dart carries not one symbol of it.
//
// A block is bytes another language wrote, and reading them without a copy is
// the whole point (§12.1). Dart has no struct and no pointer, so every field
// below is read at a GENERATED OFFSET through a ByteData over the caller's
// bytes — the offsets are the one layout model, and the prologue's BUILD
// VERSION is what refuses a producer that disagrees with them.

import 'dart:typed_data';

// THE BUILD VERSION (docs/SPEC-TABLES.md §20): one digest over every fact the bytes
// this build produces depend on — the type wire's protocol id, every table's
// layout keyed by wire id, every table's meaning (defaults, ranges, enum and
// union vocabularies, keyed the same way), and the build's byte order. It is
// the number a block carries and the number open compares.
//
// There are TWO ids in the design and they are not interchangeable: the
// PROTOCOL ID is the type wire's and nothing else, and the BUILD VERSION is
// what everything cooked or blocked is keyed by. A table edit moves this and
// never the protocol id; a type edit moves both.
const int tableBuildVersion = 0xbee10aff8f660e35;

// The block's magic (docs/SPEC-TABLES.md §19.1), read BYTEWISE: it is the one field
// read without assuming the order the rest of the block is in. A consumer that
// reads back the byte-swapped value has found a foreign byte order and refuses;
// one that reads back anything else has not found a block at all.
const int tableBlockMagic = 0x4b4c42414d484353;

// This build's byte order, as the prologue carries it: 1 little, 2 big. Dart's
// typed data is read at an EXPLICIT endianness everywhere in this file, so the
// reader has no native order of its own — the constant says what the producer's
// prologue must say, and a block of the other order is refused rather than
// fixed up.
const int tableBlockByteOrder = 1;

// The prologue's first three words, read bytewise so no assumption about order
// rides ahead of the check that establishes it.
int tableBlockRead64(ByteData view, int at) {
  var value = 0;
  for (var i = 7; i >= 0; i--) {
    value = (value << 8) | view.getUint8(at + i);
  }
  return value;
}

// ---- the block form's reflection (docs/SPEC-TABLES.md §8, §19.2) ----
//
// One record's layout as DATA — the whole mechanism behind the block form's
// read side, and what retires a hand-kept mirror. A block-form table's own
// descriptor describes its PROJECTION; the element descriptor of each
// out-of-line array describes that array's ROW, and so on down.

final class TableBlockFieldInfo {
  final String name;
  // the field's offset in the record this descriptor describes, and its size
  final int offset;
  final int size;
  final int kind; // the table-wire kind, as TableFieldInfo carries it
  // an out-of-line array: the triple's three members are live
  final bool outOfLine;
  final int offsetOfOffset; // the triple's offsetOf member, or -1

  // The COUNT COMPANION, one column doing one job in both spellings: the
  // triple's count member for an out-of-line array, the int32 used length of a
  // string or a bytes inline, -1 when the field has none.
  final int countOffset;
  final int strideOffset; // the triple's stride member, or -1
  // THIS BUILD's pitch, to assert against — never to index with (§19.2)
  final int stride;

  // ---- what a GENERIC ROW WALK needs, in the vocabulary TableFieldInfo
  // already uses (docs/SPEC-TABLES.md §8.1), so ONE walker reads a cooked node and
  // a block row without learning a second one. Where the field starts is the
  // pair above; this is everything after it.
  // inline storage of arrayBound slots at elemSize (bytes included)
  final bool isArray;
  final bool counted; // countOffset names a used-length companion
  final bool optional; // presentOffset names a bool presence companion
  // inline slots, or a string's declared maximum; 0 for a plain scalar
  final int arrayBound;
  // ONE slot's size; the field's own when it holds one value
  final int elemSize;
  final int presentOffset; // the presence companion, or -1

  // the ELEMENT's or the nested record's own layout. null when the field is a
  // scalar. Following it is how a walker DESCENDS: an out-of-line array's rows,
  // and a nested record's fields, are both reached through this one column.
  final TableBlockInfo? element;

  const TableBlockFieldInfo({
    required this.name,
    required this.offset,
    required this.size,
    required this.kind,
    required this.outOfLine,
    required this.offsetOfOffset,
    required this.countOffset,
    required this.strideOffset,
    required this.stride,
    required this.isArray,
    required this.counted,
    required this.optional,
    required this.arrayBound,
    required this.elemSize,
    required this.presentOffset,
    required this.element,
  });
}

final class TableBlockInfo {
  final String name;
  final int buildVersion; // the unit's (docs/SPEC-TABLES.md §20)
  final int size; // the record's own size: a projection's, or a row's
  final int align;
  final List<TableBlockFieldInfo> fields;

  const TableBlockInfo({
    required this.name,
    required this.buildVersion,
    required this.size,
    required this.align,
    required this.fields,
  });

  int get numFields => fields.length;
}
// TableEntity's block: bytes, a base and a length, and then rows in place. Opening
// one is ONE check and no copy; reading a row is one add (docs/SPEC-TABLES.md
// §19.2).
//
// The bytes belong to the CONSUMER — a buffer it owns, or memory a producer
// handed across. Nothing here copies them.
final class TableEntityBlock {
  final Uint8List bytes;
  final ByteData view;
  // the block's first byte within bytes, and the used extent from it
  final int base;
  final int length;

  const TableEntityBlock(this.bytes, this.view, this.base, this.length);

  // The storage a PRODUCER of this block allocates, sized from the declared
  // maxima (docs/SPEC-TABLES.md §19.1). A Dart consumer does not allocate a block
  // — the bytes are handed to it — but it caps by this.
  static const int blockMaxBytes = 128;

  static const int projectionSize = 88;

  int get entityId => view.getUint16(base + 24, Endian.little);
  int get posX => view.getInt32(base + 28, Endian.little);
  int get posY => view.getInt32(base + 32, Endian.little);
  int get posZ => view.getInt32(base + 36, Endian.little);
  int get yaw => view.getUint16(base + 40, Endian.little);
  int get pitch => view.getUint16(base + 44, Endian.little);
  int get velX => view.getInt32(base + 48, Endian.little);
  int get velY => view.getInt32(base + 52, Endian.little);
  int get velZ => view.getInt32(base + 56, Endian.little);
  int get health => view.getInt32(base + 60, Endian.little);
  int get weapon => view.getUint8(base + 64);
  int get damage => view.getUint8(base + 72);
  bool get moving => view.getUint8(base + 80) != 0;
  bool get firing => view.getUint8(base + 81) != 0;

  // Open checks once and points, and this is the WHOLE check
  // (docs/SPEC-TABLES.md §19.2): the magic read bytewise, the BYTE ORDER the
  // prologue carries against this build's own, the BUILD VERSION against this
  // build's own, each array's pitch, its offset_of, its COUNT against the
  // declared maximum and its extent inside the block, the used extent against
  // the bytes the caller passed, and the base's alignment. On a match the
  // bytes are what a build with this layout wrote, so there is nothing to
  // validate and nothing to fix up. On any failure it returns null and points
  // at nothing.
  //
  // There is ONE entry point and no tolerant twin: the block form is
  // same-build by construction — both sides generated from one declaration at
  // one build — so a consumer older than its producer is not a case. A
  // mismatch is a refusal; regenerate both sides. Data that must outlive the
  // build that wrote it takes the wire, which this same table still has.
  //
  // `base` is the block's first byte within `bytes`, and it is the only base a
  // Dart consumer holds: §19.1's 64-byte alignment is checked on it.
  static TableEntityBlock? open(Uint8List? bytes, int base, int extent) {
    if (bytes == null || extent < 88 || base < 0) {
      return null;
    }
    if (base % 64 != 0) {
      return null; // the base's alignment
    }
    if (extent > bytes.length - base) {
      return null; // the claim runs past the bytes the caller owns
    }
    final view = ByteData.view(
      bytes.buffer,
      bytes.offsetInBytes,
      bytes.lengthInBytes,
    );
    if (tableBlockRead64(view, base) != tableBlockMagic) {
      return null;
    }
    if (tableBlockRead64(view, base + 8) != tableBuildVersion) {
      return null;
    }
    if (tableBlockRead64(view, base + 16) != tableBlockByteOrder) {
      return null;
    }
    var used = 88;
    // the used extent, rounded to 64 WITHOUT the rounding itself wrapping:
    // used is already inside extent, and the padding is paid out of the slack
    // that is left rather than added and compared after.
    final padding = (64 - (used % 64)) % 64;
    if (padding > extent - used) {
      return null;
    }
    used += padding;
    return TableEntityBlock(bytes, view, base, used);
  }

  // this table's block descriptors: CONSTANT data, so a reflective read
  // costs a lookup and not a parse. The row layouts hang off the element
  // column rather than taking names of their own, so a walker reaches every
  // record through the graph — and they are MEMBERS, so the whole graph
  // claims one name and that name is the block's (docs/SPEC-TABLES.md §11).
  static const TableBlockInfo type = TableBlockInfo(
    name: 'TableEntity',
    buildVersion: tableBuildVersion,
    size: 88,
    align: 8,
    fields: <TableBlockFieldInfo>[
      TableBlockFieldInfo(
        name: 'entity_id',
        offset: 24,
        size: 4,
        kind: 7,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'pos_x',
        offset: 28,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'pos_y',
        offset: 32,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'pos_z',
        offset: 36,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'yaw',
        offset: 40,
        size: 4,
        kind: 7,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'pitch',
        offset: 44,
        size: 4,
        kind: 7,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'vel_x',
        offset: 48,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'vel_y',
        offset: 52,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'vel_z',
        offset: 56,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'health',
        offset: 60,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'weapon',
        offset: 64,
        size: 1,
        kind: 7,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 1,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'damage',
        offset: 72,
        size: 8,
        kind: 9,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 8,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'moving',
        offset: 80,
        size: 1,
        kind: 1,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 1,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'firing',
        offset: 81,
        size: 1,
        kind: 1,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 1,
        presentOffset: -1,
        element: null,
      ),
    ],
  );
}

// TableStat's block: bytes, a base and a length, and then rows in place. Opening
// one is ONE check and no copy; reading a row is one add (docs/SPEC-TABLES.md
// §19.2).
//
// The bytes belong to the CONSUMER — a buffer it owns, or memory a producer
// handed across. Nothing here copies them.
final class TableStatBlock {
  final Uint8List bytes;
  final ByteData view;
  // the block's first byte within bytes, and the used extent from it
  final int base;
  final int length;

  const TableStatBlock(this.bytes, this.view, this.base, this.length);

  // The storage a PRODUCER of this block allocates, sized from the declared
  // maxima (docs/SPEC-TABLES.md §19.1). A Dart consumer does not allocate a block
  // — the bytes are handed to it — but it caps by this.
  static const int blockMaxBytes = 64;

  static const int projectionSize = 32;

  int get statId => view.getUint8(base + 24);
  int get delta => view.getInt32(base + 28, Endian.little);

  // Open checks once and points, and this is the WHOLE check
  // (docs/SPEC-TABLES.md §19.2): the magic read bytewise, the BYTE ORDER the
  // prologue carries against this build's own, the BUILD VERSION against this
  // build's own, each array's pitch, its offset_of, its COUNT against the
  // declared maximum and its extent inside the block, the used extent against
  // the bytes the caller passed, and the base's alignment. On a match the
  // bytes are what a build with this layout wrote, so there is nothing to
  // validate and nothing to fix up. On any failure it returns null and points
  // at nothing.
  //
  // There is ONE entry point and no tolerant twin: the block form is
  // same-build by construction — both sides generated from one declaration at
  // one build — so a consumer older than its producer is not a case. A
  // mismatch is a refusal; regenerate both sides. Data that must outlive the
  // build that wrote it takes the wire, which this same table still has.
  //
  // `base` is the block's first byte within `bytes`, and it is the only base a
  // Dart consumer holds: §19.1's 64-byte alignment is checked on it.
  static TableStatBlock? open(Uint8List? bytes, int base, int extent) {
    if (bytes == null || extent < 32 || base < 0) {
      return null;
    }
    if (base % 64 != 0) {
      return null; // the base's alignment
    }
    if (extent > bytes.length - base) {
      return null; // the claim runs past the bytes the caller owns
    }
    final view = ByteData.view(
      bytes.buffer,
      bytes.offsetInBytes,
      bytes.lengthInBytes,
    );
    if (tableBlockRead64(view, base) != tableBlockMagic) {
      return null;
    }
    if (tableBlockRead64(view, base + 8) != tableBuildVersion) {
      return null;
    }
    if (tableBlockRead64(view, base + 16) != tableBlockByteOrder) {
      return null;
    }
    var used = 32;
    // the used extent, rounded to 64 WITHOUT the rounding itself wrapping:
    // used is already inside extent, and the padding is paid out of the slack
    // that is left rather than added and compared after.
    final padding = (64 - (used % 64)) % 64;
    if (padding > extent - used) {
      return null;
    }
    used += padding;
    return TableStatBlock(bytes, view, base, used);
  }

  // this table's block descriptors: CONSTANT data, so a reflective read
  // costs a lookup and not a parse. The row layouts hang off the element
  // column rather than taking names of their own, so a walker reaches every
  // record through the graph — and they are MEMBERS, so the whole graph
  // claims one name and that name is the block's (docs/SPEC-TABLES.md §11).
  static const TableBlockInfo type = TableBlockInfo(
    name: 'TableStat',
    buildVersion: tableBuildVersion,
    size: 32,
    align: 8,
    fields: <TableBlockFieldInfo>[
      TableBlockFieldInfo(
        name: 'stat_id',
        offset: 24,
        size: 4,
        kind: 6,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
      TableBlockFieldInfo(
        name: 'delta',
        offset: 28,
        size: 4,
        kind: 4,
        outOfLine: false,
        offsetOfOffset: -1,
        countOffset: -1,
        strideOffset: -1,
        stride: 0,
        isArray: false,
        counted: false,
        optional: false,
        arrayBound: 0,
        elemSize: 4,
        presentOffset: -1,
        element: null,
      ),
    ],
  );
}

// table TableMixed has NO block form: TableMixed.game_event is a union, and a block's blittable C# form is Sequential with generated padding, which cannot overlay arms.
// Its wire (§3) is unaffected — only this projection is absent, and it is
// absent by construction rather than by refusal.
