// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0xd093e62e6f907c36
//
// The shipped Dart wire path (issue #155): the serialize.dart bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, view) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts are asserted — active under --enable-asserts,
// compiled out in release — and width masks stay, as wire arithmetic. The
// buffer behind view must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, view, numBits) -> bool, the family read verdict. The
// wire is a trust boundary: reads validate everything — bounds fused per
// static run, ranges, wire constants, reserved and align padding, union
// tags — and NEVER throw on hostile bytes. No slack past the payload is
// required: the reader prices its 64-bit windows inside the buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

import 'dart:typed_data';

// Hex of a uint64 bit pattern held in a signed int (toRadixString would
// render the sign, not the pattern).
String _hex64(int value) => value < 0
    ? (value >>> 32).toRadixString(16) +
          (value & 0xffffffff).toRadixString(16).padLeft(8, '0')
    : value.toRadixString(16);

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
const int protocolId = 0xd093e62e6f907c36;

// TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class TableWeapon {
  static const int none = 0;
  static const int fists = 1;
  static const int pistol = 2;
  static const int shotgun = 3;
  static const int rifle = 4;
  static const int sniper = 5;
  static const int smg = 6;
  static const int rocket = 7;
  static const int grenade = 8;
  static const int plasma = 9;
  static const int railgun = 10;
  static const int flamer = 11;
  static const int mine = 12;
  static const int turret = 13;
  static const int drone = 14;
  static const int repair = 15;
  static const int count = 15; // the declared variant count (SPEC §4.2)
  static const int max = 15; // the exported extent (SPEC §4.2)
}

// enumNameTableWeapon: debug/log/tooling name for any TableWeapon wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNameTableWeapon(int value) {
  switch (value) {
    case TableWeapon.none:
      return 'None';
    case TableWeapon.fists:
      return 'Fists';
    case TableWeapon.pistol:
      return 'Pistol';
    case TableWeapon.shotgun:
      return 'Shotgun';
    case TableWeapon.rifle:
      return 'Rifle';
    case TableWeapon.sniper:
      return 'Sniper';
    case TableWeapon.smg:
      return 'Smg';
    case TableWeapon.rocket:
      return 'Rocket';
    case TableWeapon.grenade:
      return 'Grenade';
    case TableWeapon.plasma:
      return 'Plasma';
    case TableWeapon.railgun:
      return 'Railgun';
    case TableWeapon.flamer:
      return 'Flamer';
    case TableWeapon.mine:
      return 'Mine';
    case TableWeapon.turret:
      return 'Turret';
    case TableWeapon.drone:
      return 'Drone';
    case TableWeapon.repair:
      return 'Repair';
    default:
      return '???';
  }
}

// TableDamage — one bit per variant, consumed as masks; flags-typed fields store
// uint64 in every target — a bit-transparent int here — wire 8 bits
// (SPEC §4.2). Mask names are the family's flat spelling, lowerCamel.
const int tableDamageBleeding = 1 << 0;
const int tableDamageBurning = 1 << 1;
const int tableDamageStunned = 1 << 2;
const int tableDamageSlowed = 1 << 3;
const int tableDamagePoisoned = 1 << 4;
const int tableDamageShielded = 1 << 5;
const int tableDamageAirborne = 1 << 6;
const int tableDamageDowned = 1 << 7;
// the declared variant count (SPEC §4.2)
const int tableDamageCount = 8;

// flagNameTableDamage: debug/log/tooling name for bit i of TableDamage —
// out-of-range bits name as '???'
String flagNameTableDamage(int bit) {
  switch (bit) {
    case 0:
      return 'Bleeding';
    case 1:
      return 'Burning';
    case 2:
      return 'Stunned';
    case 3:
      return 'Slowed';
    case 4:
      return 'Poisoned';
    case 5:
      return 'Shielded';
    case 6:
      return 'Airborne';
    case 7:
      return 'Downed';
    default:
      return '???';
  }
}

// flagNamesTableDamage renders the set bits of value as 'A|B' — '0' for the
// empty set, bits past the declared variants as hex
String flagNamesTableDamage(int value) {
  final names = <String>[];
  if (value & (1 << 0) != 0) {
    names.add('Bleeding');
  }
  if (value & (1 << 1) != 0) {
    names.add('Burning');
  }
  if (value & (1 << 2) != 0) {
    names.add('Stunned');
  }
  if (value & (1 << 3) != 0) {
    names.add('Slowed');
  }
  if (value & (1 << 4) != 0) {
    names.add('Poisoned');
  }
  if (value & (1 << 5) != 0) {
    names.add('Shielded');
  }
  if (value & (1 << 6) != 0) {
    names.add('Airborne');
  }
  if (value & (1 << 7) != 0) {
    names.add('Downed');
  }
  if (value >>> 8 != 0) {
    names.add('0x${_hex64((value >>> 8) << 8)}');
  }
  return names.isEmpty ? '0' : names.join('|');
}

// type TableHitEvent
final class TableHitEvent {
  int targetId = 0;
  // wire [0, 4095]
  int damage = 0;
  // wire [0, 7]
  int hitKind = 0;
  bool crit = false;
}

// tableHitEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// tableHitEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int tableHitEventMaxBits = 28;
const int tableHitEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroTableHitEvent(TableHitEvent value) {
  value.targetId = 0;
  value.damage = 0;
  value.hitKind = 0;
  value.crit = false;
}

// writeTableHitEvent packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// tableHitEventMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeTableHitEvent(TableHitEvent value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= tableHitEventMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.damage >= 0);
  assert(value.damage <= 4095);
  assert(value.hitKind >= 0);
  assert(value.hitKind <= 7);
  v =
      ((value.targetId) & 0xfff) |
      (((value.damage) & 0xfff) << 12) |
      (((value.hitKind) & 0x7) << 24) |
      ((value.crit ? 1 : 0) << 27);
  scratch |= v << scratchBits;
  scratchBits += 28;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (28 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readTableHitEvent decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readTableHitEvent(TableHitEvent value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 28 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xfff;
  bitsRead += 12;
  value.targetId = v;
  v = (window >>> 12) & 0xfff;
  bitsRead += 12;
  value.damage = v;
  v = (window >>> 24) & 0x7;
  bitsRead += 3;
  value.hitKind = v;
  v = (window >>> 27) & 0x1;
  bitsRead += 1;
  value.crit = v != 0;
  return true;
}

// measureTableHitEvent is the exact wire bits writeTableHitEvent would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureTableHitEvent(TableHitEvent value) => 28;

// type TableChatEvent
final class TableChatEvent {
  // wire [0, 3]
  int channel = 0;
  int speaker = 0;
}

// tableChatEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// tableChatEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int tableChatEventMaxBits = 14;
const int tableChatEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroTableChatEvent(TableChatEvent value) {
  value.channel = 0;
  value.speaker = 0;
}

// writeTableChatEvent packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// tableChatEventMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeTableChatEvent(TableChatEvent value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= tableChatEventMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.channel >= 0);
  assert(value.channel <= 3);
  v = ((value.channel) & 0x3) | (((value.speaker) & 0xfff) << 2);
  scratch |= v << scratchBits;
  scratchBits += 14;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (14 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readTableChatEvent decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readTableChatEvent(TableChatEvent value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 14 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x3;
  bitsRead += 2;
  value.channel = v;
  v = (window >>> 2) & 0xfff;
  bitsRead += 12;
  value.speaker = v;
  return true;
}

// measureTableChatEvent is the exact wire bits writeTableChatEvent would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureTableChatEvent(TableChatEvent value) => 14;

// type TablePickupEvent
final class TablePickupEvent {
  int itemId = 0;
  // wire [0, 255]
  int amount = 0;
}

// tablePickupEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// tablePickupEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int tablePickupEventMaxBits = 18;
const int tablePickupEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroTablePickupEvent(TablePickupEvent value) {
  value.itemId = 0;
  value.amount = 0;
}

// writeTablePickupEvent packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// tablePickupEventMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeTablePickupEvent(TablePickupEvent value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= tablePickupEventMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.amount >= 0);
  assert(value.amount <= 255);
  v = ((value.itemId) & 0x3ff) | (((value.amount) & 0xff) << 10);
  scratch |= v << scratchBits;
  scratchBits += 18;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (18 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readTablePickupEvent decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readTablePickupEvent(TablePickupEvent value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 18 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x3ff;
  bitsRead += 10;
  value.itemId = v;
  v = (window >>> 10) & 0xff;
  bitsRead += 8;
  value.amount = v;
  return true;
}

// measureTablePickupEvent is the exact wire bits writeTablePickupEvent would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureTablePickupEvent(TablePickupEvent value) => 18;

// TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
abstract final class TableEventType {
  static const int none = 0;
  static const int hit = 1;
  static const int chat = 2;
  static const int pickup = 3;
  static const int count = 3; // the declared variant count (SPEC §4.2)
  static const int max = 3; // the exported extent (SPEC §4.2)
}

// enumNameTableEventType: debug/log/tooling name for any TableEventType wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNameTableEventType(int value) {
  switch (value) {
    case TableEventType.none:
      return 'None';
    case TableEventType.hit:
      return 'Hit';
    case TableEventType.chat:
      return 'Chat';
    case TableEventType.pickup:
      return 'Pickup';
    default:
      return '???';
  }
}

// TableEvent — at most one of the arms; type says which. Construction is the empty
// union (None). A read zero-establishes exactly the selected arm before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
final class TableEvent {
  int type = TableEventType.none;
  final TableHitEvent hit = TableHitEvent();
  final TableChatEvent chat = TableChatEvent();
  final TablePickupEvent pickup = TablePickupEvent();
}

// tableEventMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// tableEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int tableEventMaxBits = 30;
const int tableEventMaxBytes = 8;

// zeroTableEvent resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm re-zeroes at its next selection.
void zeroTableEvent(TableEvent value) {
  value.type = TableEventType.none;
}

// writeTableEvent packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// tableEventMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeTableEvent(TableEvent value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= tableEventMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.type >= 0);
  assert(value.type <= 3);
  v = ((value.type) & 0x3);
  scratch |= v << scratchBits;
  scratchBits += 2;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (2 - scratchBits);
  }
  switch (value.type) {
    case 1:
      assert(value.hit.damage >= 0);
      assert(value.hit.damage <= 4095);
      assert(value.hit.hitKind >= 0);
      assert(value.hit.hitKind <= 7);
      v =
          ((value.hit.targetId) & 0xfff) |
          (((value.hit.damage) & 0xfff) << 12) |
          (((value.hit.hitKind) & 0x7) << 24) |
          ((value.hit.crit ? 1 : 0) << 27);
      scratch |= v << scratchBits;
      scratchBits += 28;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (28 - scratchBits);
      }
    case 2:
      assert(value.chat.channel >= 0);
      assert(value.chat.channel <= 3);
      v = ((value.chat.channel) & 0x3) | (((value.chat.speaker) & 0xfff) << 2);
      scratch |= v << scratchBits;
      scratchBits += 14;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (14 - scratchBits);
      }
    case 3:
      assert(value.pickup.amount >= 0);
      assert(value.pickup.amount <= 255);
      v =
          ((value.pickup.itemId) & 0x3ff) |
          (((value.pickup.amount) & 0xff) << 10);
      scratch |= v << scratchBits;
      scratchBits += 18;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (18 - scratchBits);
      }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readTableEvent decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readTableEvent(TableEvent value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 2 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x3;
  bitsRead += 2;
  value.type = v;
  switch (value.type) {
    case 1:
      value.hit.targetId = 0;
      value.hit.damage = 0;
      value.hit.hitKind = 0;
      value.hit.crit = false;
      if (bitsRead + 28 > numBits) {
        return false;
      }
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0xfff;
      bitsRead += 12;
      value.hit.targetId = v;
      v = (window >>> 12) & 0xfff;
      bitsRead += 12;
      value.hit.damage = v;
      v = (window >>> 24) & 0x7;
      bitsRead += 3;
      value.hit.hitKind = v;
      v = (window >>> 27) & 0x1;
      bitsRead += 1;
      value.hit.crit = v != 0;
    case 2:
      value.chat.channel = 0;
      value.chat.speaker = 0;
      if (bitsRead + 14 > numBits) {
        return false;
      }
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0x3;
      bitsRead += 2;
      value.chat.channel = v;
      v = (window >>> 2) & 0xfff;
      bitsRead += 12;
      value.chat.speaker = v;
    case 3:
      value.pickup.itemId = 0;
      value.pickup.amount = 0;
      if (bitsRead + 18 > numBits) {
        return false;
      }
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0x3ff;
      bitsRead += 10;
      value.pickup.itemId = v;
      v = (window >>> 10) & 0xff;
      bitsRead += 8;
      value.pickup.amount = v;
  }
  return true;
}

// measureTableEvent is the exact wire bits writeTableEvent would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureTableEvent(TableEvent value) {
  var bits = 0;
  bits += 2;
  switch (value.type) {
    case 1:
      bits += 28;
    case 2:
      bits += 14;
    case 3:
      bits += 18;
  }
  return bits;
}
