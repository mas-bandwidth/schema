# Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
# SPDX-License-Identifier: NONE — this generated output is yours, under terms of
# your choice. See the LICENSE exception in the schema compiler; the compiler is
# AGPL-3.0, its output is not.
# package benchtable — the COOKED FORM (docs/SPEC-TABLES.md §7): the READ half

defmodule Benchtable.BenchTableCook do
  @moduledoc """
  The COOKED FORM's read half (docs/SPEC-TABLES.md §7), which §7 calls a
  PREVIEW. A cook is a load-trusted-data-from-tools format and not a wire:
  tooling writes a region for one BUILD VERSION and this build points at it.

  Open matches the header and returns the region — no walk, no fix-up, no copy.
  The region is a SUB-BINARY over the caller's own bytes.

  READING ONLY. A region is a memory image of a storage type and a BEAM term
  has none, so a cook arrives from a C++, C# or Rust build.
  """

  alias Benchtable.CookRuntime, as: C

  @build_version Benchtable.BuildVersion.build_version()

  # ---- the cooked records as DATA (docs/SPEC-TABLES.md §7.5) ----
  #
  # One record's layout, as a module attribute: where each field sits, how
  # big it is, whether it is a POINTER EDGE, the bound its COUNT COMPANION
  # is checked against, and the record it names. A walker DESCENDS through
  # the record column — a pointer's target, a by-value nesting and an
  # array's element are all reached through that one column.

  @cook_info_table_entity %{
    name: "TableEntity",
    size: 64,
    align: 8,
    fields: [
      %{
        name: "entity_id",
        offset: 0,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "pos_x",
        offset: 4,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "pos_y",
        offset: 8,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "pos_z",
        offset: 12,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "yaw",
        offset: 16,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "pitch",
        offset: 20,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "vel_x",
        offset: 24,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "vel_y",
        offset: 28,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "vel_z",
        offset: 32,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "health",
        offset: 36,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "weapon",
        offset: 40,
        size: 1,
        elem_size: 1,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "damage",
        offset: 48,
        size: 8,
        elem_size: 8,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "moving",
        offset: 56,
        size: 1,
        elem_size: 1,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :bool,
        record: nil
      },
      %{
        name: "firing",
        offset: 57,
        size: 1,
        elem_size: 1,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :bool,
        record: nil
      },
    ]
  }
  def cook_info_table_entity, do: @cook_info_table_entity

  @cook_info_table_stat %{
    name: "TableStat",
    size: 8,
    align: 4,
    fields: [
      %{
        name: "stat_id",
        offset: 0,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "delta",
        offset: 4,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
    ]
  }
  def cook_info_table_stat, do: @cook_info_table_stat

  @cook_info_table_hit_event %{
    name: "TableHitEvent",
    size: 16,
    align: 4,
    fields: [
      %{
        name: "target_id",
        offset: 0,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "damage",
        offset: 4,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "hit_kind",
        offset: 8,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "crit",
        offset: 12,
        size: 1,
        elem_size: 1,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :bool,
        record: nil
      },
    ]
  }
  def cook_info_table_hit_event, do: @cook_info_table_hit_event

  @cook_info_table_chat_event %{
    name: "TableChatEvent",
    size: 8,
    align: 4,
    fields: [
      %{
        name: "channel",
        offset: 0,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
      %{
        name: "speaker",
        offset: 4,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
    ]
  }
  def cook_info_table_chat_event, do: @cook_info_table_chat_event

  @cook_info_table_pickup_event %{
    name: "TablePickupEvent",
    size: 8,
    align: 4,
    fields: [
      %{
        name: "item_id",
        offset: 0,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :unsigned,
        record: nil
      },
      %{
        name: "amount",
        offset: 4,
        size: 4,
        elem_size: 4,
        is_array: false,
        array_bound: 0,
        is_pointer: false,
        count_offset: -1,
        present_offset: -1,
        storage: :signed,
        record: nil
      },
    ]
  }
  def cook_info_table_pickup_event, do: @cook_info_table_pickup_event

  # TableEntity's cook: a region and a length, and then the root where it lies.
  # Opening one is a HEADER MATCH and no copy (docs/SPEC-TABLES.md §7).
  #
  # §7.1's constants, so a consumer reading this file has the facts and not
  # a description of them.
  def table_entity_cook_region_alignment, do: 8
  def table_entity_cook_root_size, do: 64
  def table_entity_cook_root_align, do: 8

  def cook_open_table_entity(data), do: cook_open_table_entity(data, 0)

  # lead is how many bytes past an aligned base the caller's buffer
  # begins — the one pointer fact a BEAM binary cannot carry, so the caller
  # states it (see CookRuntime.open/5).
  def cook_open_table_entity(data, lead) do
    case C.open(data, lead, 64, 8, @build_version) do
      {:ok, region, region_length} ->
        {:ok,
         %{
           name: "TableEntity",
           region: region,
           region_length: region_length,
           info: {__MODULE__, :cook_info_table_entity}
         }}

      :error ->
        :error
    end
  end

  # the TYPED slot accessors: each field of a TableEntity node, read at its own
  # offset from the node's base within the region.
  def table_entity_node_entity_id(region, node), do: C.uint(region, node + 0, 4)
  def table_entity_node_pos_x(region, node), do: C.int(region, node + 4, 4)
  def table_entity_node_pos_y(region, node), do: C.int(region, node + 8, 4)
  def table_entity_node_pos_z(region, node), do: C.int(region, node + 12, 4)
  def table_entity_node_yaw(region, node), do: C.uint(region, node + 16, 4)
  def table_entity_node_pitch(region, node), do: C.uint(region, node + 20, 4)
  def table_entity_node_vel_x(region, node), do: C.int(region, node + 24, 4)
  def table_entity_node_vel_y(region, node), do: C.int(region, node + 28, 4)
  def table_entity_node_vel_z(region, node), do: C.int(region, node + 32, 4)
  def table_entity_node_health(region, node), do: C.int(region, node + 36, 4)
  def table_entity_node_weapon(region, node), do: C.uint(region, node + 40, 1)
  def table_entity_node_damage(region, node), do: C.uint(region, node + 48, 8)
  def table_entity_node_moving(region, node), do: C.bool(region, node + 56)
  def table_entity_node_firing(region, node), do: C.bool(region, node + 57)

  # TableStat's cook: a region and a length, and then the root where it lies.
  # Opening one is a HEADER MATCH and no copy (docs/SPEC-TABLES.md §7).
  #
  # §7.1's constants, so a consumer reading this file has the facts and not
  # a description of them.
  def table_stat_cook_region_alignment, do: 8
  def table_stat_cook_root_size, do: 8
  def table_stat_cook_root_align, do: 4

  def cook_open_table_stat(data), do: cook_open_table_stat(data, 0)

  # lead is how many bytes past an aligned base the caller's buffer
  # begins — the one pointer fact a BEAM binary cannot carry, so the caller
  # states it (see CookRuntime.open/5).
  def cook_open_table_stat(data, lead) do
    case C.open(data, lead, 8, 4, @build_version) do
      {:ok, region, region_length} ->
        {:ok,
         %{
           name: "TableStat",
           region: region,
           region_length: region_length,
           info: {__MODULE__, :cook_info_table_stat}
         }}

      :error ->
        :error
    end
  end

  # the TYPED slot accessors: each field of a TableStat node, read at its own
  # offset from the node's base within the region.
  def table_stat_node_stat_id(region, node), do: C.uint(region, node + 0, 4)
  def table_stat_node_delta(region, node), do: C.int(region, node + 4, 4)

  # TableHitEvent's cook: a region and a length, and then the root where it lies.
  # Opening one is a HEADER MATCH and no copy (docs/SPEC-TABLES.md §7).
  #
  # §7.1's constants, so a consumer reading this file has the facts and not
  # a description of them.
  def table_hit_event_cook_region_alignment, do: 8
  def table_hit_event_cook_root_size, do: 16
  def table_hit_event_cook_root_align, do: 4

  def cook_open_table_hit_event(data), do: cook_open_table_hit_event(data, 0)

  # lead is how many bytes past an aligned base the caller's buffer
  # begins — the one pointer fact a BEAM binary cannot carry, so the caller
  # states it (see CookRuntime.open/5).
  def cook_open_table_hit_event(data, lead) do
    case C.open(data, lead, 16, 4, @build_version) do
      {:ok, region, region_length} ->
        {:ok,
         %{
           name: "TableHitEvent",
           region: region,
           region_length: region_length,
           info: {__MODULE__, :cook_info_table_hit_event}
         }}

      :error ->
        :error
    end
  end

  # the TYPED slot accessors: each field of a TableHitEvent node, read at its own
  # offset from the node's base within the region.
  def table_hit_event_node_target_id(region, node), do: C.uint(region, node + 0, 4)
  def table_hit_event_node_damage(region, node), do: C.int(region, node + 4, 4)
  def table_hit_event_node_hit_kind(region, node), do: C.int(region, node + 8, 4)
  def table_hit_event_node_crit(region, node), do: C.bool(region, node + 12)

  # TableChatEvent's cook: a region and a length, and then the root where it lies.
  # Opening one is a HEADER MATCH and no copy (docs/SPEC-TABLES.md §7).
  #
  # §7.1's constants, so a consumer reading this file has the facts and not
  # a description of them.
  def table_chat_event_cook_region_alignment, do: 8
  def table_chat_event_cook_root_size, do: 8
  def table_chat_event_cook_root_align, do: 4

  def cook_open_table_chat_event(data), do: cook_open_table_chat_event(data, 0)

  # lead is how many bytes past an aligned base the caller's buffer
  # begins — the one pointer fact a BEAM binary cannot carry, so the caller
  # states it (see CookRuntime.open/5).
  def cook_open_table_chat_event(data, lead) do
    case C.open(data, lead, 8, 4, @build_version) do
      {:ok, region, region_length} ->
        {:ok,
         %{
           name: "TableChatEvent",
           region: region,
           region_length: region_length,
           info: {__MODULE__, :cook_info_table_chat_event}
         }}

      :error ->
        :error
    end
  end

  # the TYPED slot accessors: each field of a TableChatEvent node, read at its own
  # offset from the node's base within the region.
  def table_chat_event_node_channel(region, node), do: C.int(region, node + 0, 4)
  def table_chat_event_node_speaker(region, node), do: C.uint(region, node + 4, 4)

  # TablePickupEvent's cook: a region and a length, and then the root where it lies.
  # Opening one is a HEADER MATCH and no copy (docs/SPEC-TABLES.md §7).
  #
  # §7.1's constants, so a consumer reading this file has the facts and not
  # a description of them.
  def table_pickup_event_cook_region_alignment, do: 8
  def table_pickup_event_cook_root_size, do: 8
  def table_pickup_event_cook_root_align, do: 4

  def cook_open_table_pickup_event(data), do: cook_open_table_pickup_event(data, 0)

  # lead is how many bytes past an aligned base the caller's buffer
  # begins — the one pointer fact a BEAM binary cannot carry, so the caller
  # states it (see CookRuntime.open/5).
  def cook_open_table_pickup_event(data, lead) do
    case C.open(data, lead, 8, 4, @build_version) do
      {:ok, region, region_length} ->
        {:ok,
         %{
           name: "TablePickupEvent",
           region: region,
           region_length: region_length,
           info: {__MODULE__, :cook_info_table_pickup_event}
         }}

      :error ->
        :error
    end
  end

  # the TYPED slot accessors: each field of a TablePickupEvent node, read at its own
  # offset from the node's base within the region.
  def table_pickup_event_node_item_id(region, node), do: C.uint(region, node + 0, 4)
  def table_pickup_event_node_amount(region, node), do: C.int(region, node + 4, 4)

end
