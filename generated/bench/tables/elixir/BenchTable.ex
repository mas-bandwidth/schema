# Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
# SPDX-License-Identifier: NONE — this generated output is yours, under terms of
# your choice. See the LICENSE exception in the schema compiler; the compiler is
# AGPL-3.0, its output is not.
# package benchtable — protocol id 0x0926221bcb6f475f
#
# The shipped Elixir wire path (issue #167): the serialize.elixir port's
# measured shapes — byte-granular 32-bit-group packing and 40-bit read
# windows (every intermediate a BEAM small integer), single accumulator-
# threaded read/write functions per type (no stream struct, no per-field
# tuple), function-head loops — with literal widths, zero dependencies.
#
# write_<name>(value) -> the wire binary. The writer TRUSTS the caller
# (the C++ stance) up to the O(1) contract checks, which raise
# ArgumentError — the BEAM has no compile-out assert, so they are always
# on, matching the port.
#
# read_<name>(data, num_bits) -> {:ok, value} | :error, the family read
# verdict. The wire is a trust boundary: reads validate everything —
# bounds fused per static run, ranges, wire constants, reserved and align
# padding, union tags, string content — and NEVER raise on hostile bytes.
# No slack past the payload is required: the windows price themselves
# inside the binary.
#
# measure_<name>(value) -> exact wire bits for that value (trusted, like
# the writer). zero_<name>() -> the §5 zero form; construction
# (%Struct{}) carries the specified defaults instead. Floats travel
# bit-transparently: a non-finite IEEE-754 pattern — which no BEAM float
# term can hold — reads back as {:nonfinite, bits} and writes from the
# same form, the serialize.elixir convention.

# TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
# an integer-constant namespace — the Elixir translation of the family's
# integer-backed enums: storage must hold every wire-legal value, and
# | max = ... headroom values have no richer Elixir value to be
defmodule Benchtable.TableWeapon do
  def none, do: 0
  def fists, do: 1
  def pistol, do: 2
  def shotgun, do: 3
  def rifle, do: 4
  def sniper, do: 5
  def smg, do: 6
  def rocket, do: 7
  def grenade, do: 8
  def plasma, do: 9
  def railgun, do: 10
  def flamer, do: 11
  def mine, do: 12
  def turret, do: 13
  def drone, do: 14
  def repair, do: 15
  # the declared variant count (SPEC §4.2)
  def count, do: 15
  # the exported extent (SPEC §4.2)
  def max, do: 15
end

# type TableHitEvent
defmodule Benchtable.TableHitEvent do
  # damage: wire [0, 4095]
  # hit_kind: wire [0, 7]
  defstruct target_id: 0, damage: 0, hit_kind: 0, crit: false
end

# type TableChatEvent
defmodule Benchtable.TableChatEvent do
  # channel: wire [0, 3]
  defstruct channel: 0, speaker: 0
end

# type TablePickupEvent
defmodule Benchtable.TablePickupEvent do
  # amount: wire [0, 255]
  defstruct item_id: 0, amount: 0
end

# TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
defmodule Benchtable.TableEventType do
  def none, do: 0
  def hit, do: 1
  def chat, do: 2
  def pickup, do: 3
  # the declared variant count (SPEC §4.2)
  def count, do: 3
  # the exported extent (SPEC §4.2)
  def max, do: 3
end

# TableEvent — at most one of the arms; type says which. Construction is the empty
# union (None). A read builds the selected arm from the wire; unselected
# arms hold their construction form (SPEC §4.8 leaves them unspecified).
# Consumers read the selected arm only.
defmodule Benchtable.TableEvent do
  defstruct type: 0,
            hit: %Benchtable.TableHitEvent{},
            chat: %Benchtable.TableChatEvent{},
            pickup: %Benchtable.TablePickupEvent{}
end

# The file-scope surface: constants, flags masks, name helpers and the
# wire functions of every declaration in BenchTable.schema.
defmodule Benchtable.BenchTable do
  import Bitwise

  @compile {:inline, rd: 3}

  @compile {:inline, rdw: 3}

  # The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
  # sides at the same id speak identical bits; there is no other versioning.
  def protocol_id, do: 0x0926221BCB6F475F

  # enum_name_table_weapon: debug/log/tooling name for any TableWeapon wire value —
  # out-of-set values (wire-legal up to the declared max) name as "???"
  def enum_name_table_weapon(value) do
    case value do
      0 -> "None"
      1 -> "Fists"
      2 -> "Pistol"
      3 -> "Shotgun"
      4 -> "Rifle"
      5 -> "Sniper"
      6 -> "Smg"
      7 -> "Rocket"
      8 -> "Grenade"
      9 -> "Plasma"
      10 -> "Railgun"
      11 -> "Flamer"
      12 -> "Mine"
      13 -> "Turret"
      14 -> "Drone"
      15 -> "Repair"
      _ -> "???"
    end
  end

  # TableDamage — one bit per variant, consumed as masks; flags-typed fields store
  # the uint64 mask pattern in every target — a plain integer here — wire
  # 8 bits (SPEC §4.2). Mask names are the family's flat spelling.
  def table_damage_bleeding, do: 1 <<< 0
  def table_damage_burning, do: 1 <<< 1
  def table_damage_stunned, do: 1 <<< 2
  def table_damage_slowed, do: 1 <<< 3
  def table_damage_poisoned, do: 1 <<< 4
  def table_damage_shielded, do: 1 <<< 5
  def table_damage_airborne, do: 1 <<< 6
  def table_damage_downed, do: 1 <<< 7
  # the declared variant count (SPEC §4.2)
  def table_damage_count, do: 8

  # flag_name_table_damage: debug/log/tooling name for bit i of TableDamage —
  # out-of-range bits name as "???"
  def flag_name_table_damage(bit) do
    case bit do
      0 -> "Bleeding"
      1 -> "Burning"
      2 -> "Stunned"
      3 -> "Slowed"
      4 -> "Poisoned"
      5 -> "Shielded"
      6 -> "Airborne"
      7 -> "Downed"
      _ -> "???"
    end
  end

  # flag_names_table_damage renders the set bits of value as "A|B" — "0" for the
  # empty set, bits past the declared variants as hex
  def flag_names_table_damage(value) do
    names =
      Enum.filter(0..7, fn bit -> (value >>> bit &&& 1) != 0 end)
      |> Enum.map(&flag_name_table_damage/1)

    names =
      if value >>> 8 != 0 do
        hex = String.downcase(Integer.to_string((value >>> 8) <<< 8, 16))
        names ++ ["0x" <> hex]
      else
        names
      end

    if names == [], do: "0", else: Enum.join(names, "|")
  end

  # table_hit_event_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # table_hit_event_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def table_hit_event_max_bits, do: 28
  def table_hit_event_max_bytes, do: 8

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Benchtable.TableHitEvent{}).
  def zero_table_hit_event do
    %Benchtable.TableHitEvent{target_id: 0, damage: 0, hit_kind: 0, crit: false}
  end

  # write_table_hit_event packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_table_hit_event(value) do
    data = <<>>

    %{
      target_id: value_target_id,
      damage: value_damage,
      hit_kind: value_hit_kind,
      crit: value_crit
    } = value

    v = value_target_id &&& 0xFFF
    scratch = v

    if value_damage < 0 do
      raise ArgumentError, "value.damage is below the wire minimum"
    end

    if value_damage > 4095 do
      raise ArgumentError, "value.damage is above the wire maximum"
    end

    v = value_damage
    scratch = scratch ||| v <<< 12

    if value_hit_kind < 0 do
      raise ArgumentError, "value.hit_kind is below the wire minimum"
    end

    if value_hit_kind > 7 do
      raise ArgumentError, "value.hit_kind is above the wire maximum"
    end

    v = value_hit_kind
    scratch = scratch ||| v <<< 24
    v = if value_crit, do: 1, else: 0
    scratch = scratch ||| v <<< 27
    data = <<data::binary, scratch::little-size(3)-unit(8)>>
    scratch = scratch >>> 24
    # the residual byte, statically known to be there
    <<data::binary, scratch>>
  end

  # read_table_hit_event decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_table_hit_event(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 28 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 28)
      v = rv &&& 0xFFF
      bits_read = bits_read + 12
      v_target_id = v
      v = rv >>> 12 &&& 0xFFF
      bits_read = bits_read + 12
      v_damage = v
      v = rv >>> 24 &&& 0x7
      bits_read = bits_read + 3
      v_hit_kind = v
      v = rv >>> 27
      bits_read = bits_read + 1
      v_crit = v == 1
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read

      value = %Benchtable.TableHitEvent{
        target_id: v_target_id,
        damage: v_damage,
        hit_kind: v_hit_kind,
        crit: v_crit
      }

      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_table_hit_event is the exact wire bits write_table_hit_event would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_table_hit_event(_value), do: 28

  # table_chat_event_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # table_chat_event_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def table_chat_event_max_bits, do: 14
  def table_chat_event_max_bytes, do: 8

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Benchtable.TableChatEvent{}).
  def zero_table_chat_event, do: %Benchtable.TableChatEvent{channel: 0, speaker: 0}

  # write_table_chat_event packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_table_chat_event(value) do
    data = <<>>
    %{channel: value_channel, speaker: value_speaker} = value

    if value_channel < 0 do
      raise ArgumentError, "value.channel is below the wire minimum"
    end

    if value_channel > 3 do
      raise ArgumentError, "value.channel is above the wire maximum"
    end

    v = value_channel
    scratch = v
    v = value_speaker &&& 0xFFF
    scratch = scratch ||| v <<< 2
    data = <<data::binary, scratch::little-size(1)-unit(8)>>
    scratch = scratch >>> 8
    # the residual byte, statically known to be there
    <<data::binary, scratch>>
  end

  # read_table_chat_event decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_table_chat_event(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 14 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 14)
      v = rv &&& 0x3
      bits_read = bits_read + 2
      v_channel = v
      v = rv >>> 2
      bits_read = bits_read + 12
      v_speaker = v
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      value = %Benchtable.TableChatEvent{channel: v_channel, speaker: v_speaker}
      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_table_chat_event is the exact wire bits write_table_chat_event would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_table_chat_event(_value), do: 14

  # table_pickup_event_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # table_pickup_event_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def table_pickup_event_max_bits, do: 18
  def table_pickup_event_max_bytes, do: 8

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Benchtable.TablePickupEvent{}).
  def zero_table_pickup_event, do: %Benchtable.TablePickupEvent{item_id: 0, amount: 0}

  # write_table_pickup_event packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_table_pickup_event(value) do
    data = <<>>
    %{item_id: value_item_id, amount: value_amount} = value
    v = value_item_id &&& 0x3FF
    scratch = v

    if value_amount < 0 do
      raise ArgumentError, "value.amount is below the wire minimum"
    end

    if value_amount > 255 do
      raise ArgumentError, "value.amount is above the wire maximum"
    end

    v = value_amount
    scratch = scratch ||| v <<< 10
    data = <<data::binary, scratch::little-size(2)-unit(8)>>
    scratch = scratch >>> 16
    # the residual byte, statically known to be there
    <<data::binary, scratch>>
  end

  # read_table_pickup_event decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_table_pickup_event(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 18 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 18)
      v = rv &&& 0x3FF
      bits_read = bits_read + 10
      v_item_id = v
      v = rv >>> 10
      bits_read = bits_read + 8
      v_amount = v
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      value = %Benchtable.TablePickupEvent{item_id: v_item_id, amount: v_amount}
      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_table_pickup_event is the exact wire bits write_table_pickup_event would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_table_pickup_event(_value), do: 18

  # table_event_max_bits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
  # table_event_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def table_event_max_bits, do: 30
  def table_event_max_bytes, do: 8

  # enum_name_table_event_type: debug/log/tooling name for any TableEventType wire value —
  # out-of-set values (wire-legal up to the declared max) name as "???"
  def enum_name_table_event_type(value) do
    case value do
      0 -> "None"
      1 -> "Hit"
      2 -> "Chat"
      3 -> "Pickup"
      _ -> "???"
    end
  end

  # The §5 zero form — the empty union (None). Arms hold their construction
  # form: every arm is unselected at None, and unselected arms are
  # unspecified by rule (SPEC §4.8).
  def zero_table_event, do: %Benchtable.TableEvent{}

  # write_table_event packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_table_event(value) do
    data = <<>>

    if value.type < 0 do
      raise ArgumentError, "value.type is below the wire minimum"
    end

    if value.type > 3 do
      raise ArgumentError, "value.type is above the wire maximum"
    end

    v = value.type
    scratch = v

    {data, scratch, scratch_bits} =
      case value.type do
        1 ->
          %{
            target_id: value_hit_target_id,
            damage: value_hit_damage,
            hit_kind: value_hit_hit_kind,
            crit: value_hit_crit
          } = value.hit

          v = value_hit_target_id &&& 0xFFF
          scratch = scratch ||| v <<< 2

          if value_hit_damage < 0 do
            raise ArgumentError, "value.hit.damage is below the wire minimum"
          end

          if value_hit_damage > 4095 do
            raise ArgumentError, "value.hit.damage is above the wire maximum"
          end

          v = value_hit_damage
          scratch = scratch ||| v <<< 14

          if value_hit_hit_kind < 0 do
            raise ArgumentError, "value.hit.hit_kind is below the wire minimum"
          end

          if value_hit_hit_kind > 7 do
            raise ArgumentError, "value.hit.hit_kind is above the wire maximum"
          end

          v = value_hit_hit_kind
          scratch = scratch ||| v <<< 26
          v = if value_hit_crit, do: 1, else: 0
          scratch = scratch ||| v <<< 29
          data = <<data::binary, scratch::little-size(3)-unit(8)>>
          scratch = scratch >>> 24
          scratch_bits = 6
          {data, scratch, scratch_bits}

        2 ->
          %{channel: value_chat_channel, speaker: value_chat_speaker} = value.chat

          if value_chat_channel < 0 do
            raise ArgumentError, "value.chat.channel is below the wire minimum"
          end

          if value_chat_channel > 3 do
            raise ArgumentError, "value.chat.channel is above the wire maximum"
          end

          v = value_chat_channel
          scratch = scratch ||| v <<< 2
          v = value_chat_speaker &&& 0xFFF
          scratch = scratch ||| v <<< 4
          data = <<data::binary, scratch::little-size(2)-unit(8)>>
          scratch_bits = 0
          scratch = 0
          {data, scratch, scratch_bits}

        3 ->
          %{item_id: value_pickup_item_id, amount: value_pickup_amount} = value.pickup
          v = value_pickup_item_id &&& 0x3FF
          scratch = scratch ||| v <<< 2

          if value_pickup_amount < 0 do
            raise ArgumentError, "value.pickup.amount is below the wire minimum"
          end

          if value_pickup_amount > 255 do
            raise ArgumentError, "value.pickup.amount is above the wire maximum"
          end

          v = value_pickup_amount
          scratch = scratch ||| v <<< 12
          data = <<data::binary, scratch::little-size(2)-unit(8)>>
          scratch = scratch >>> 16
          scratch_bits = 4
          {data, scratch, scratch_bits}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          scratch_bits = 2
          {data, scratch, scratch_bits}
      end

    if scratch_bits != 0, do: <<data::binary, scratch>>, else: data
  end

  # read_table_event decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_table_event(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 2 > num_bits, do: throw(:invalid)
      rv = rdw(data, bits_read, 49)
      v = rv &&& 0x3
      bits_read = bits_read + 2

      {bits_read, v} =
        case v do
          1 ->
            if bits_read + 28 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 28)
            v = rv &&& 0xFFF
            bits_read = bits_read + 12
            v_hit_target_id = v
            v = rv >>> 12 &&& 0xFFF
            bits_read = bits_read + 12
            v_hit_damage = v
            v = rv >>> 24 &&& 0x7
            bits_read = bits_read + 3
            v_hit_hit_kind = v
            v = rv >>> 27
            bits_read = bits_read + 1
            v_hit_crit = v == 1

            v_hit = %Benchtable.TableHitEvent{
              target_id: v_hit_target_id,
              damage: v_hit_damage,
              hit_kind: v_hit_hit_kind,
              crit: v_hit_crit
            }

            u = %Benchtable.TableEvent{type: 1, hit: v_hit}
            {bits_read, u}

          2 ->
            if bits_read + 14 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 14)
            v = rv &&& 0x3
            bits_read = bits_read + 2
            v_chat_channel = v
            v = rv >>> 2
            bits_read = bits_read + 12
            v_chat_speaker = v
            v_chat = %Benchtable.TableChatEvent{channel: v_chat_channel, speaker: v_chat_speaker}
            u = %Benchtable.TableEvent{type: 2, chat: v_chat}
            {bits_read, u}

          3 ->
            if bits_read + 18 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 18)
            v = rv &&& 0x3FF
            bits_read = bits_read + 10
            v_pickup_item_id = v
            v = rv >>> 10
            bits_read = bits_read + 8
            v_pickup_amount = v

            v_pickup = %Benchtable.TablePickupEvent{
              item_id: v_pickup_item_id,
              amount: v_pickup_amount
            }

            u = %Benchtable.TableEvent{type: 3, pickup: v_pickup}
            {bits_read, u}

          _ ->
            # None — the tag is the whole wire (SPEC §4.8)
            {bits_read, %Benchtable.TableEvent{}}
        end

      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      {:ok, v}
    catch
      :invalid -> :error
    end
  end

  # measure_table_event is the exact wire bits write_table_event would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_table_event(value) do
    bits = 0
    bits = bits + 2

    bits =
      case value.type do
        1 ->
          bits + 28

        2 ->
          bits + 14

        3 ->
          bits + 18

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          bits
      end

    bits
  end

  # The port's 40-bit window decode (issue #167): enough for a 7-bit offset
  # plus a 32-bit group, small enough that no intermediate ever boxes. The
  # callers have already priced the read inside num_bits, so the tail
  # fallback decodes exactly the bytes that exist.
  defp rd(data, bits_read, bits) do
    i = bits_read >>> 3

    window =
      case data do
        <<_::binary-size(^i), w::little-40, _::binary>> -> w
        <<_::binary-size(^i), rest::binary>> -> :binary.decode_unsigned(rest, :little)
      end

    window >>> (bits_read &&& 7) &&& (1 <<< bits) - 1
  end

  # The wide window decode: 56 bits, enough for a 7-bit offset plus a
  # 49-bit group, and still under the 2^59 fixnum boundary — one match
  # context serves a whole group of fields instead of one per field. A
  # 64-bit window would box and measures slower than the reads it saves.
  defp rdw(data, bits_read, bits) do
    i = bits_read >>> 3

    window =
      case data do
        <<_::binary-size(^i), w::little-56, _::binary>> -> w
        <<_::binary-size(^i), rest::binary>> -> :binary.decode_unsigned(rest, :little)
      end

    window >>> (bits_read &&& 7) &&& (1 <<< bits) - 1
  end
end
