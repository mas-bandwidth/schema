# Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
# SPDX-License-Identifier: NONE — this generated output is yours, under terms of
# your choice. See the LICENSE exception in the schema compiler; the compiler is
# AGPL-3.0, its output is not.
# package benchtable — the BLOCK FORM (docs/SPEC-TABLES.md §19)

defmodule Benchtable.BenchTableBlock do
  @moduledoc """
  NOTHING DECLARES THIS FORM. Every fixed table has one, and it is emitted on
  the side: call into this module only if you use the block form. The unit's
  table module carries not one symbol of it.

  A block is one flat extent: the table's own instance at the front — the
  PROJECTION, carrying per bounded array of structs where its rows start, how
  many there are and how far apart they sit — and then those rows, each array
  at a fixed pitch. This side reads those three facts and points.

  READING ONLY. A BEAM term has no layout a producer could write, so a block
  arrives from a C++, C# or Rust build and this module opens it. The bytes are
  never copied: a row is a SUB-BINARY over the caller's own block.
  """

  alias Benchtable.BlockRuntime, as: B

  @build_version Benchtable.BuildVersion.build_version()

  # ---- the block form of table TableEntity (docs/SPEC-TABLES.md §19) ----

  # The block's STORAGE is sized from the declared maxima: one extent,
  # allocated once, never grown, never pooled (§19.1).
  def table_entity_block_max_bytes, do: 128
  def table_entity_block_projection_bytes, do: 88

  # Open checks once and POINTS, and this is the WHOLE check (§19.2): the
  # magic, the build version, the byte order, and then, per out-of-line
  # array, the pitch against this build's, the count against the declared
  # maximum, and the rows inside the extent the caller passed.
  #
  # THE BASE'S ALIGNMENT IS NOT CHECKED, and that is a fact about the BEAM
  # rather than a gap: a binary has no address a caller places, so there is
  # no unaligned base for this leg to refuse. Every ARRAY START is still
  # checked against the 64-byte rule, because that one is a fact the bytes
  # carry.
  #
  # EVERY NUMBER BELOW COMES FROM THE INSTANCE. BEAM integers are
  # arbitrary-precision, so no term of this arithmetic can carry past the
  # top of a type — a forged offset_of near 2^63 is simply a large number
  # that fails its bound.
  def block_open_table_entity(data) when is_binary(data) do
    bytes = byte_size(data)

    if bytes < 88 do
      :refuse
    else
      case data do
        <<magic::little-unsigned-64, build::little-unsigned-64,
          order::little-unsigned-64, _::binary>> ->
          # a byte-swapped magic is a FOREIGN BYTE ORDER, and anything else
          # is not a block at all. Both refuse.
          if magic != B.magic() or build != @build_version or order != B.byte_order() do
            :refuse
          else
            block_extent_table_entity(data, bytes)
          end

        _ ->
          :refuse
      end
    end
  end

  def block_open_table_entity(_data), do: :refuse

  defp block_extent_table_entity(data, bytes) do
    # this table declares no out-of-line array, so the prologue and the
    # projection's own extent are the whole check
    block_used_table_entity(data, bytes, 88)
  end

  # the used extent, rounded to 64 out of the slack that is LEFT rather than
  # added and compared after
  defp block_used_table_entity(data, bytes, used) do
    padding = rem(B.align() - rem(used, B.align()), B.align())

    if padding > bytes - used do
      :refuse
    else
      {:ok, %{name: "TableEntity", base: data, bytes: used + padding}}
    end
  end

  # TableEntity's block descriptor (docs/SPEC-TABLES.md §8, §19.2): a module
  # attribute, so a read of it copies no term — describing the PROJECTION.
  @block_info_table_entity %{
    name: "TableEntity",
    build_version: Benchtable.BuildVersion.build_version(),
    size: 88,
    align: 8,
    fields: [
      %{
        name: "entity_id",
        offset: 24,
        size: 4,
        kind: 7,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "pos_x",
        offset: 28,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "pos_y",
        offset: 32,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "pos_z",
        offset: 36,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "yaw",
        offset: 40,
        size: 4,
        kind: 7,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "pitch",
        offset: 44,
        size: 4,
        kind: 7,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "vel_x",
        offset: 48,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "vel_y",
        offset: 52,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "vel_z",
        offset: 56,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "health",
        offset: 60,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "weapon",
        offset: 64,
        size: 1,
        kind: 7,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 1,
        present_offset: nil,
        element: nil
      },
      %{
        name: "damage",
        offset: 72,
        size: 8,
        kind: 9,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 8,
        present_offset: nil,
        element: nil
      },
      %{
        name: "moving",
        offset: 80,
        size: 1,
        kind: 1,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 1,
        present_offset: nil,
        element: nil
      },
      %{
        name: "firing",
        offset: 81,
        size: 1,
        kind: 1,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 1,
        present_offset: nil,
        element: nil
      },
    ]
  }
  def block_info_table_entity, do: @block_info_table_entity

  # ---- the block form of table TableStat (docs/SPEC-TABLES.md §19) ----

  # The block's STORAGE is sized from the declared maxima: one extent,
  # allocated once, never grown, never pooled (§19.1).
  def table_stat_block_max_bytes, do: 64
  def table_stat_block_projection_bytes, do: 32

  # Open checks once and POINTS, and this is the WHOLE check (§19.2): the
  # magic, the build version, the byte order, and then, per out-of-line
  # array, the pitch against this build's, the count against the declared
  # maximum, and the rows inside the extent the caller passed.
  #
  # THE BASE'S ALIGNMENT IS NOT CHECKED, and that is a fact about the BEAM
  # rather than a gap: a binary has no address a caller places, so there is
  # no unaligned base for this leg to refuse. Every ARRAY START is still
  # checked against the 64-byte rule, because that one is a fact the bytes
  # carry.
  #
  # EVERY NUMBER BELOW COMES FROM THE INSTANCE. BEAM integers are
  # arbitrary-precision, so no term of this arithmetic can carry past the
  # top of a type — a forged offset_of near 2^63 is simply a large number
  # that fails its bound.
  def block_open_table_stat(data) when is_binary(data) do
    bytes = byte_size(data)

    if bytes < 32 do
      :refuse
    else
      case data do
        <<magic::little-unsigned-64, build::little-unsigned-64,
          order::little-unsigned-64, _::binary>> ->
          # a byte-swapped magic is a FOREIGN BYTE ORDER, and anything else
          # is not a block at all. Both refuse.
          if magic != B.magic() or build != @build_version or order != B.byte_order() do
            :refuse
          else
            block_extent_table_stat(data, bytes)
          end

        _ ->
          :refuse
      end
    end
  end

  def block_open_table_stat(_data), do: :refuse

  defp block_extent_table_stat(data, bytes) do
    # this table declares no out-of-line array, so the prologue and the
    # projection's own extent are the whole check
    block_used_table_stat(data, bytes, 32)
  end

  # the used extent, rounded to 64 out of the slack that is LEFT rather than
  # added and compared after
  defp block_used_table_stat(data, bytes, used) do
    padding = rem(B.align() - rem(used, B.align()), B.align())

    if padding > bytes - used do
      :refuse
    else
      {:ok, %{name: "TableStat", base: data, bytes: used + padding}}
    end
  end

  # TableStat's block descriptor (docs/SPEC-TABLES.md §8, §19.2): a module
  # attribute, so a read of it copies no term — describing the PROJECTION.
  @block_info_table_stat %{
    name: "TableStat",
    build_version: Benchtable.BuildVersion.build_version(),
    size: 32,
    align: 8,
    fields: [
      %{
        name: "stat_id",
        offset: 24,
        size: 4,
        kind: 6,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
      %{
        name: "delta",
        offset: 28,
        size: 4,
        kind: 4,
        out_of_line: false,
        offset_of_offset: nil,
        count_offset: nil,
        stride_offset: nil,
        stride: 0,
        is_array: false,
        counted: false,
        optional: false,
        array_bound: 0,
        elem_size: 4,
        present_offset: nil,
        element: nil
      },
    ]
  }
  def block_info_table_stat, do: @block_info_table_stat

end
