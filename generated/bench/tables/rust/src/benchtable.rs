// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0x0926221bcb6f475f

use serialize::{ReadStream, Stream, WriteStream};

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
pub const PROTOCOL_ID: u64 = 0x0926221bcb6f475f;

/// The generated crate's error: the runtime's own errors pass through;
/// Validation is a read rejecting the wire (SPEC §4.3, §4.7).
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub enum Error {
    Stream(serialize::Error),
    Validation,
}

impl From<serialize::Error> for Error {
    fn from(e: serialize::Error) -> Error {
        Error::Stream(e)
    }
}

// serialize.rs 2.0.0 writes are infallible: their Result carries an
// uninhabited error, and `?` on one needs this vacuous conversion.
impl From<core::convert::Infallible> for Error {
    fn from(e: core::convert::Infallible) -> Error {
        match e {}
    }
}

pub type Result<T = ()> = core::result::Result<T, Error>;

// TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct TableWeapon(pub u8);

impl TableWeapon {
    pub const NONE: TableWeapon = TableWeapon(0);
    pub const FISTS: TableWeapon = TableWeapon(1);
    pub const PISTOL: TableWeapon = TableWeapon(2);
    pub const SHOTGUN: TableWeapon = TableWeapon(3);
    pub const RIFLE: TableWeapon = TableWeapon(4);
    pub const SNIPER: TableWeapon = TableWeapon(5);
    pub const SMG: TableWeapon = TableWeapon(6);
    pub const ROCKET: TableWeapon = TableWeapon(7);
    pub const GRENADE: TableWeapon = TableWeapon(8);
    pub const PLASMA: TableWeapon = TableWeapon(9);
    pub const RAILGUN: TableWeapon = TableWeapon(10);
    pub const FLAMER: TableWeapon = TableWeapon(11);
    pub const MINE: TableWeapon = TableWeapon(12);
    pub const TURRET: TableWeapon = TableWeapon(13);
    pub const DRONE: TableWeapon = TableWeapon(14);
    pub const REPAIR: TableWeapon = TableWeapon(15);
    pub const COUNT: TableWeapon = TableWeapon(15); // the declared variant count (SPEC §4.2)
    pub const MAX: TableWeapon = TableWeapon(15); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `TableWeapon` value, out-of-set included.
pub fn enum_name_table_weapon(value: TableWeapon) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Fists",
        2 => "Pistol",
        3 => "Shotgun",
        4 => "Rifle",
        5 => "Sniper",
        6 => "Smg",
        7 => "Rocket",
        8 => "Grenade",
        9 => "Plasma",
        10 => "Railgun",
        11 => "Flamer",
        12 => "Mine",
        13 => "Turret",
        14 => "Drone",
        15 => "Repair",
        _ => "???",
    }
}

// TableDamage — one bit per variant, consumed as masks; storage u64 in every
// target, wire 8 bits (SPEC §4.2)
pub type TableDamage = u64;

pub const TABLE_DAMAGE_BLEEDING: TableDamage = 1 << 0;
pub const TABLE_DAMAGE_BURNING: TableDamage = 1 << 1;
pub const TABLE_DAMAGE_STUNNED: TableDamage = 1 << 2;
pub const TABLE_DAMAGE_SLOWED: TableDamage = 1 << 3;
pub const TABLE_DAMAGE_POISONED: TableDamage = 1 << 4;
pub const TABLE_DAMAGE_SHIELDED: TableDamage = 1 << 5;
pub const TABLE_DAMAGE_AIRBORNE: TableDamage = 1 << 6;
pub const TABLE_DAMAGE_DOWNED: TableDamage = 1 << 7;
pub const TABLE_DAMAGE_COUNT: i64 = 8; // the declared variant count (SPEC §4.2)

/// Debug/log name for bit `i` of `TableDamage` — out-of-range bits name as `"???"`.
pub fn flag_name_table_damage(bit: u32) -> &'static str {
    match bit {
        0 => "Bleeding",
        1 => "Burning",
        2 => "Stunned",
        3 => "Slowed",
        4 => "Poisoned",
        5 => "Shielded",
        6 => "Airborne",
        7 => "Downed",
        _ => "???",
    }
}

/// Renders the set bits of `value` as `"A|B"` — `"0"` for the empty set,
/// bits past the declared variants as hex.
pub fn flag_names_table_damage(value: TableDamage) -> String {
    let mut names = String::new();
    if value & (1 << 0) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(0));
    }
    if value & (1 << 1) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(1));
    }
    if value & (1 << 2) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(2));
    }
    if value & (1 << 3) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(3));
    }
    if value & (1 << 4) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(4));
    }
    if value & (1 << 5) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(5));
    }
    if value & (1 << 6) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(6));
    }
    if value & (1 << 7) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_table_damage(7));
    }
    if value >> 8 != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(&format!("{:#x}", (value >> 8) << 8));
    }
    if names.is_empty() {
        names.push('0');
    }
    names
}

// type TableHitEvent
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TableHitEvent {
    pub target_id: u32,
    pub damage: i32, // wire [0, 4095]
    pub hit_kind: i32, // wire [0, 7]
    pub crit: bool,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TableHitEvent {
    fn default() -> Self {
        TableHitEvent {
            target_id: 0,
            damage: 0,
            hit_kind: 0,
            crit: false,
        }
    }
}

// TABLE_HIT_EVENT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TABLE_HIT_EVENT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TABLE_HIT_EVENT_MAX_BITS: u64 = 28;
pub const TABLE_HIT_EVENT_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_table_hit_event(stream: &mut WriteStream<'_>, value: &TableHitEvent) -> Result {
    if value.target_id >= 1 << 12 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.damage < 0 || value.damage > 4095 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.hit_kind < 0 || value.hit_kind > 7 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.target_id);
    let f1: u64 = u64::from(value.damage as u32);
    let f2: u64 = u64::from(value.hit_kind as u32);
    let f3: u64 = u64::from(value.crit);
    let mut w0 = (f0 | (f1 << 12) | (f2 << 24) | (f3 << 27)) as u32;
    stream.serialize_bits(&mut w0, 28)?;
    Ok(())
}

#[inline]
pub fn read_table_hit_event(stream: &mut ReadStream<'_>, value: &mut TableHitEvent) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 28)?;
    let c0 = u64::from(c);
    let v0: u64 = c0 & 0xfff;
    value.target_id = v0 as u32;
    let v1: u64 = (c0 >> 12) & 0xfff;
    value.damage = v1 as i32;
    let v2: u64 = (c0 >> 24) & 0x7;
    value.hit_kind = v2 as i32;
    let v3: u64 = (c0 >> 27) & 0x1;
    value.crit = v3 != 0;
    Ok(())
}

// type TableChatEvent
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TableChatEvent {
    pub channel: i32, // wire [0, 3]
    pub speaker: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TableChatEvent {
    fn default() -> Self {
        TableChatEvent {
            channel: 0,
            speaker: 0,
        }
    }
}

// TABLE_CHAT_EVENT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TABLE_CHAT_EVENT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TABLE_CHAT_EVENT_MAX_BITS: u64 = 14;
pub const TABLE_CHAT_EVENT_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_table_chat_event(stream: &mut WriteStream<'_>, value: &TableChatEvent) -> Result {
    if value.channel < 0 || value.channel > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.speaker >= 1 << 12 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.channel as u32);
    let f1: u64 = u64::from(value.speaker);
    let mut w0 = (f0 | (f1 << 2)) as u32;
    stream.serialize_bits(&mut w0, 14)?;
    Ok(())
}

#[inline]
pub fn read_table_chat_event(stream: &mut ReadStream<'_>, value: &mut TableChatEvent) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 14)?;
    let c0 = u64::from(c);
    let v0: u64 = c0 & 0x3;
    value.channel = v0 as i32;
    let v1: u64 = (c0 >> 2) & 0xfff;
    value.speaker = v1 as u32;
    Ok(())
}

// type TablePickupEvent
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TablePickupEvent {
    pub item_id: u32,
    pub amount: i32, // wire [0, 255]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TablePickupEvent {
    fn default() -> Self {
        TablePickupEvent {
            item_id: 0,
            amount: 0,
        }
    }
}

// TABLE_PICKUP_EVENT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TABLE_PICKUP_EVENT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TABLE_PICKUP_EVENT_MAX_BITS: u64 = 18;
pub const TABLE_PICKUP_EVENT_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_table_pickup_event(stream: &mut WriteStream<'_>, value: &TablePickupEvent) -> Result {
    if value.item_id >= 1 << 10 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.amount < 0 || value.amount > 255 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.item_id);
    let f1: u64 = u64::from(value.amount as u32);
    let mut w0 = (f0 | (f1 << 10)) as u32;
    stream.serialize_bits(&mut w0, 18)?;
    Ok(())
}

#[inline]
pub fn read_table_pickup_event(stream: &mut ReadStream<'_>, value: &mut TablePickupEvent) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 18)?;
    let c0 = u64::from(c);
    let v0: u64 = c0 & 0x3ff;
    value.item_id = v0 as u32;
    let v1: u64 = (c0 >> 10) & 0xff;
    value.amount = v1 as i32;
    Ok(())
}

// TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct TableEventType(pub u8);

impl TableEventType {
    pub const NONE: TableEventType = TableEventType(0);
    pub const HIT: TableEventType = TableEventType(1);
    pub const CHAT: TableEventType = TableEventType(2);
    pub const PICKUP: TableEventType = TableEventType(3);
    pub const COUNT: TableEventType = TableEventType(3); // the declared variant count (SPEC §4.2)
    pub const MAX: TableEventType = TableEventType(3); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `TableEventType` value, out-of-set included.
pub fn enum_name_table_event_type(value: TableEventType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Hit",
        2 => "Chat",
        3 => "Pickup",
        _ => "???",
    }
}

// TableEvent — at most one of the arms. The default is None (the empty union);
// every read reconstructs the selected payload with its declared initial
// values, even at the same tag, and replaces the whole value on success.
#[derive(Clone, Copy, PartialEq, Debug, Default)]
pub enum TableEvent {
    #[default]
    None,
    Hit(TableHitEvent),
    Chat(TableChatEvent),
    Pickup(TablePickupEvent),
}

// TABLE_EVENT_MAX_BITS is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// TABLE_EVENT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TABLE_EVENT_MAX_BITS: u64 = 30;
pub const TABLE_EVENT_MAX_BYTES: usize = 8;

pub fn write_table_event(stream: &mut WriteStream<'_>, value: &TableEvent) -> Result {
    match value {
        TableEvent::None => {
            let mut offset_value: u32 = 0;
            stream.serialize_bits(&mut offset_value, 2)?;
            Ok(()) // no payload — the tag is the whole wire (SPEC §4.8)
        }
        TableEvent::Hit(arm) => {
            let mut offset_value: u32 = 1;
            stream.serialize_bits(&mut offset_value, 2)?;
            write_table_hit_event(stream, arm)
        }
        TableEvent::Chat(arm) => {
            let mut offset_value: u32 = 2;
            stream.serialize_bits(&mut offset_value, 2)?;
            write_table_chat_event(stream, arm)
        }
        TableEvent::Pickup(arm) => {
            let mut offset_value: u32 = 3;
            stream.serialize_bits(&mut offset_value, 2)?;
            write_table_pickup_event(stream, arm)
        }
    }
}

pub fn read_table_event(stream: &mut ReadStream<'_>, value: &mut TableEvent) -> Result {
    let mut tag_value: i32 = 0;
    stream.serialize_int(&mut tag_value, 0, 3)?; // rejects a tag above the count (SPEC §4.8)
    match tag_value {
        1 => {
            let mut arm = TableHitEvent::default();
            read_table_hit_event(stream, &mut arm)?;
            *value = TableEvent::Hit(arm);
        }
        2 => {
            let mut arm = TableChatEvent::default();
            read_table_chat_event(stream, &mut arm)?;
            *value = TableEvent::Chat(arm);
        }
        3 => {
            let mut arm = TablePickupEvent::default();
            read_table_pickup_event(stream, &mut arm)?;
            *value = TableEvent::Pickup(arm);
        }
        _ => *value = TableEvent::None,
    }
    Ok(())
}

