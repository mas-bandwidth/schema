// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0x1a38ac487752b0ae

#pragma once

#include <cstdint>

namespace benchtable {

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
inline constexpr uint64_t ProtocolId = 0x1a38ac487752b0aeull;

// enum TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2)
enum class TableWeapon : uint8_t {
    None = 0,
    Fists = 1,
    Pistol = 2,
    Shotgun = 3,
    Rifle = 4,
    Sniper = 5,
    Smg = 6,
    Rocket = 7,
    Grenade = 8,
    Plasma = 9,
    Railgun = 10,
    Flamer = 11,
    Mine = 12,
    Turret = 13,
    Drone = 14,
    Repair = 15,
    Max = 15, // the exported extent (SPEC §4.2)
};

// EnumName: debug/log name for any TableWeapon value, out-of-set included
inline const char * EnumName( TableWeapon value )
{
    switch ( value )
    {
        case TableWeapon::None: return "None";
        case TableWeapon::Fists: return "Fists";
        case TableWeapon::Pistol: return "Pistol";
        case TableWeapon::Shotgun: return "Shotgun";
        case TableWeapon::Rifle: return "Rifle";
        case TableWeapon::Sniper: return "Sniper";
        case TableWeapon::Smg: return "Smg";
        case TableWeapon::Rocket: return "Rocket";
        case TableWeapon::Grenade: return "Grenade";
        case TableWeapon::Plasma: return "Plasma";
        case TableWeapon::Railgun: return "Railgun";
        case TableWeapon::Flamer: return "Flamer";
        case TableWeapon::Mine: return "Mine";
        case TableWeapon::Turret: return "Turret";
        case TableWeapon::Drone: return "Drone";
        case TableWeapon::Repair: return "Repair";
        default: return "???";
    }
}

// flags TableDamage — one bit per variant, consumed as masks; storage uint64 in every
// target, wire 8 bits (SPEC §4.2)
using TableDamage = uint64_t;
inline constexpr TableDamage TableDamage_Bleeding = 1ull << 0;
inline constexpr TableDamage TableDamage_Burning = 1ull << 1;
inline constexpr TableDamage TableDamage_Stunned = 1ull << 2;
inline constexpr TableDamage TableDamage_Slowed = 1ull << 3;
inline constexpr TableDamage TableDamage_Poisoned = 1ull << 4;
inline constexpr TableDamage TableDamage_Shielded = 1ull << 5;
inline constexpr TableDamage TableDamage_Airborne = 1ull << 6;
inline constexpr TableDamage TableDamage_Downed = 1ull << 7;
inline constexpr int64_t TableDamageCount = 8; // the declared variant count (SPEC §4.2)

// FlagNameTableDamage: debug/log name for bit i of TableDamage — out-of-range bits name as "???"
inline const char * FlagNameTableDamage( int bit )
{
    switch ( bit )
    {
        case 0: return "Bleeding";
        case 1: return "Burning";
        case 2: return "Stunned";
        case 3: return "Slowed";
        case 4: return "Poisoned";
        case 5: return "Shielded";
        case 6: return "Airborne";
        case 7: return "Downed";
        default: return "???";
    }
}

#ifndef SCHEMA_BENCHTABLE_FLAG_APPEND_DEFINED
#define SCHEMA_BENCHTABLE_FLAG_APPEND_DEFINED
inline int SchemaFlagAppend_( char * buffer, int bufferSize, int position, const char * name )
{
    if ( position > 0 && position < bufferSize - 1 )
    {
        buffer[position++] = '|';
    }
    while ( *name && position < bufferSize - 1 )
    {
        buffer[position++] = *name++;
    }
    return position;
}

inline int SchemaFlagAppendHex_( char * buffer, int bufferSize, int position, uint64_t bits )
{
    position = SchemaFlagAppend_( buffer, bufferSize, position, "0x" );
    int shift = 60;
    while ( shift > 0 && ( ( bits >> shift ) & 0xf ) == 0 )
    {
        shift -= 4;
    }
    for ( ; shift >= 0; shift -= 4 )
    {
        if ( position < bufferSize - 1 )
        {
            buffer[position++] = "0123456789abcdef"[( bits >> shift ) & 0xf];
        }
    }
    return position;
}
#endif // SCHEMA_BENCHTABLE_FLAG_APPEND_DEFINED

// FlagNamesTableDamage renders the set bits of value into buffer as "A|B" — "0" for
// the empty set, bits past the declared variants as hex — NUL-terminates and
// returns buffer; TableDamageNamesMax bytes always suffice
inline constexpr int TableDamageNamesMax = 86;
inline const char * FlagNamesTableDamage( uint64_t value, char * buffer, int bufferSize )
{
    int position = 0;
    if ( value & ( 1ull << 0 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 0 ) );
    }
    if ( value & ( 1ull << 1 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 1 ) );
    }
    if ( value & ( 1ull << 2 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 2 ) );
    }
    if ( value & ( 1ull << 3 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 3 ) );
    }
    if ( value & ( 1ull << 4 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 4 ) );
    }
    if ( value & ( 1ull << 5 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 5 ) );
    }
    if ( value & ( 1ull << 6 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 6 ) );
    }
    if ( value & ( 1ull << 7 ) )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, FlagNameTableDamage( 7 ) );
    }
    if ( value >> 8 )
    {
        position = SchemaFlagAppendHex_( buffer, bufferSize, position, ( value >> 8 ) << 8 );
    }
    if ( position == 0 )
    {
        position = SchemaFlagAppend_( buffer, bufferSize, position, "0" );
    }
    if ( position < bufferSize )
    {
        buffer[position] = '\0';
    }
    return buffer;
}

// type TableHitEvent
struct TableHitEvent {
    uint32_t target_id = 0;
    int32_t damage = 0; // wire [0, 4095]
    int32_t hit_kind = 0; // wire [0, 7]
    bool crit = false;
};

inline constexpr int64_t TableHitEventMaxBits = 28; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t TableHitEventMaxBytes = 8; // rounded up to the 8-byte write-buffer granularity; a read buffer's allocation must extend at least 8 bytes past the data — the reader loads 64-bit windows

// type TableChatEvent
struct TableChatEvent {
    int32_t channel = 0; // wire [0, 3]
    uint32_t speaker = 0;
};

inline constexpr int64_t TableChatEventMaxBits = 14; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t TableChatEventMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type TablePickupEvent
struct TablePickupEvent {
    uint32_t item_id = 0;
    int32_t amount = 0; // wire [0, 255]
};

inline constexpr int64_t TablePickupEventMaxBits = 18; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t TablePickupEventMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class TableEventType : uint8_t {
    None = 0,
    Hit = 1,
    Chat = 2,
    Pickup = 3,
    Max = 3, // the exported extent (SPEC §4.2)
};

// union TableEvent — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm's storage is established ZEROED
// when the arm is selected — by ReadTableEvent before it decodes (SPEC §5), or by
// assigning it: value.hit = TableHitEvent{}. Bytes of unselected arms are indeterminate.
struct TableEvent
{
    TableEventType type;

    union
    {
        TableHitEvent hit;
        TableChatEvent chat;
        TablePickupEvent pickup;
    };

    TableEvent() : type( TableEventType::None ) {} // the tag only — arms are zero-established at selection
};

inline constexpr int64_t TableEventMaxBits = 30; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t TableEventMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

} // namespace benchtable
