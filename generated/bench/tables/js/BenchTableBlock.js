// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — the BLOCK FORM (docs/SPEC-TABLES.md §19): the READ half.
//
// the unit's shared runtime lives here — <Package>Block.js, one home per unit, named by
// the package and independent of file order (docs/SPEC-TABLES.md §19.2).
//
// NOTHING DECLARES THIS FORM. Every fixed table has one, and it is emitted on
// the side: import this module only if you read a block. The unit's
// <Base>Table.js carries not one symbol of it.
//
// Open takes the bytes the CONSUMER holds — a Uint8Array — and points at them.
// Nothing here copies, and nothing per row allocates. Bytes that are not a
// block of this build are refused with a null handle, whatever they carry; a
// CALLER's error — no Uint8Array, or a view that starts at an unaligned
// byteOffset — throws, naming the fix.
//
// THE BASE'S ALIGNMENT IS THE VIEW'S byteOffset. JavaScript has no addresses,
// so the one alignment fact a consumer can state is where its view starts
// inside its buffer, and that is what Open measures against §19.1's sixty-four.

// THE BUILD VERSION (docs/SPEC-TABLES.md §20): one digest over every fact the
// bytes this build produces depend on — the type wire's protocol id, every
// table's layout keyed by wire id, every table's meaning (defaults, ranges,
// enum and union vocabularies, keyed the same way), and the build's byte
// order. It is the number a block carries and the number Open compares.
//
// There are TWO ids in the design and they are not interchangeable: the
// PROTOCOL ID is the type wire's and nothing else, and the BUILD VERSION is
// what everything cooked or blocked is keyed by. A table edit moves this and
// never the protocol id; a type edit moves both.
export const BuildVersion = 0x1864d106cc5f2c11n;

// The block's magic (docs/SPEC-TABLES.md §19.1), read as an explicit
// little-endian word: it is what separates a block written by a build of this
// byte order from one written by a build of the other, whose bytes read back
// byte-reversed here and refuse.
export const TableBlockMagic = 0x4b4c42414d484353n;

// THIS SIDE's byte order, as the prologue carries it (§20.3). A JavaScript
// reader reads at explicit little-endian offsets, so its order IS little
// whatever the host is, and a block written by a build of the other order is
// refused twice over: by the magic, and by this word.
export const TableBlockByteOrder = 1n;

// THE LAYOUT CONTRACT (docs/SPEC-TABLES.md §19.3) in a language with no layout.
//
// C++ static_asserts each size and offset against its own compiler's model and
// C# asserts against the managed model. JavaScript has no second model — an
// object has no offsets at all — so there is nothing here to measure the
// compiler's model against, and a check that claimed otherwise could never
// fail. What this checks instead is everything the generated constants can
// still disagree about, and it is checked ONCE, before any Open returns a
// handle:
//
//   - every record's size against the extent of its own last field, its
//     alignment a power of two, and its size a multiple of that alignment;
//   - the PROJECTION's fields all past the generated prologue's twenty-four
//     bytes, which nothing in the descriptors describes;
//   - each array's PITCH CONSTANT against the size of the row object it names
//     — the one pair here that is TWO INDEPENDENT DERIVATIONS, and §19.5's
//     named negative control.
//
// A failure is THIS BUILD's defect — its own constants disagree — and the
// first Open of the block throws, naming it. It is not a null: a null says the
// bytes are not this build's, and these bytes were never looked at.
export const TableBlockLayout = Object.freeze({
  verify(projection, strides) {
    const seen = new Set();
    function record(info, isProjection) {
      if (info === null || seen.has(info)) { return true; }
      seen.add(info);
      if (!(info.Size > 0) || !(info.Align > 0)) { return false; }
      if ((info.Align & (info.Align - 1)) !== 0) { return false; }
      if ((info.Size % info.Align) !== 0) { return false; }
      for (let i = 0; i < info.Fields.length; i++) {
        const f = info.Fields[i];
        if (f.Offset < 0 || f.Size < 0) { return false; }
        if (isProjection && f.Offset < 24) { return false; } // the generated prologue
        if (f.Offset + f.Size > info.Size) { return false; }
        if (f.CountOffset >= 0 && f.CountOffset + 4 > info.Size) { return false; }
        if (f.PresentOffset >= 0 && f.PresentOffset + 1 > info.Size) { return false; }
        if (f.OutOfLine) {
          if (f.OffsetOfOffset < 0 || f.OffsetOfOffset + 16 > info.Size) { return false; }
          if (f.CountOffset !== f.OffsetOfOffset + 8) { return false; }
          if (f.StrideOffset !== f.OffsetOfOffset + 12) { return false; }
        }
        if (f.ElementRef !== null && !record(f.ElementRef(), false)) { return false; }
      }
      return true;
    }
    if (!record(projection, true)) { return false; }
    for (let i = 0; i < strides.length; i++) {
      if (strides[i].Stride !== strides[i].SizeOf()) { return false; }
    }
    return true;
  },
});

// TableEntity's block: a view and a length, and then rows in place. Opening one is
// ONE check and no copy; reading a row is one add (docs/SPEC-TABLES.md §19.2).
//
// The bytes belong to the CONSUMER. Nothing here allocates but the handle.
export const TableEntityBlock = (() => {
  const projection = Object.freeze({
    Name: "TableEntity", BuildVersion: BuildVersion, Size: 88, Align: 8, NumFields: 14,
    Fields: Object.freeze([
      { Name: "entity_id", Offset: 24, Size: 4, Kind: 7, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "pos_x", Offset: 28, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "pos_y", Offset: 32, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "pos_z", Offset: 36, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "yaw", Offset: 40, Size: 4, Kind: 7, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "pitch", Offset: 44, Size: 4, Kind: 7, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "vel_x", Offset: 48, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "vel_y", Offset: 52, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "vel_z", Offset: 56, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "health", Offset: 60, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "weapon", Offset: 64, Size: 1, Kind: 7, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 1, PresentOffset: -1, ElementRef: null },
      { Name: "damage", Offset: 72, Size: 8, Kind: 9, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 8, PresentOffset: -1, ElementRef: null },
      { Name: "moving", Offset: 80, Size: 1, Kind: 1, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 1, PresentOffset: -1, ElementRef: null },
      { Name: "firing", Offset: 81, Size: 1, Kind: 1, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 1, PresentOffset: -1, ElementRef: null },
    ]),
  });

  // the layout contract's one independent pair (§19.3, §19.5): the array's
  // generated pitch, and the row object's own size.
  const strides = [
  ];
  // the check runs ONCE, before the first Open points at anything, and a
  // failure is this BUILD's defect rather than the bytes': the generated
  // constants disagree with each other, so it throws and names the block.
  let layoutChecked = false;
  function Layout() {
    if (!layoutChecked) {
      if (!TableBlockLayout.verify(projection, strides)) {
        throw new Error("TableEntityBlock: this build's generated layout constants disagree with each other (docs/SPEC-TABLES.md §19.3) — regenerate");
      }
      layoutChecked = true;
    }
    return true;
  }

  // Open checks once and points, and this is the WHOLE check
  // (docs/SPEC-TABLES.md §19.2): the magic, the BYTE ORDER the prologue carries
  // against this build's own, the BUILD VERSION against this build's own,
  // each array's pitch, its offset_of, its COUNT against the declared
  // maximum and its extent inside the block, the used extent against the
  // bytes the caller passed, and the base's alignment. On a match the bytes
  // are what a build with this layout wrote, so there is nothing to validate
  // and nothing to fix up. On any failure it returns null and points at
  // nothing.
  //
  // There is ONE entry point and no tolerant twin: the block form is
  // same-build by construction — both sides generated from one declaration
  // at one build — so a consumer older than its producer is not a case. A
  // mismatch is a refusal; regenerate both sides. Data that must outlive the
  // build that wrote it takes the wire, which this same table still has.
  //
  // NULL IS ONE ANSWER AND IT MEANS ONE THING: these bytes are not a block of
  // this build — a foreign magic, the other byte order, another build version,
  // or framing the prologue's own numbers refuse. What the CALLER got wrong is
  // not a null, it is a throw with the fix in it: no Uint8Array at all is a
  // TypeError, and a view whose byteOffset is not a multiple of 64 — a Node
  // Buffer under 4 KiB is carved out of a shared pool at an arbitrary offset —
  // is a RangeError, because that is the caller's placement and not the bytes,
  // and the same bytes copied into a fresh Uint8Array open.
  //
  // EVERY NUMBER OUT OF THE INSTANCE IS BIGINT ARITHMETIC, and each term is
  // BOUNDED BEFORE IT IS ADDED. A forged offset_of near 2^63 must refuse, and
  // a Number would have lost the low bits of it before the comparison ran.
  // The C++ and C# sides hold the same shape for the same reason.
  function Open(bytes) {
    if (!(bytes instanceof Uint8Array)) {
      throw new TypeError("TableEntityBlock.Open takes the block's bytes as a Uint8Array, not " + (bytes === null ? "null" : typeof bytes));
    }
    if ((bytes.byteOffset % 64) !== 0) { // the base's alignment
      throw new RangeError("TableEntityBlock.Open: the view starts " + bytes.byteOffset + " bytes into its ArrayBuffer, and a block's base " +
        "must be a multiple of 64 (docs/SPEC-TABLES.md §19.1) — a pooled Node Buffer starts anywhere; copy the bytes " +
        "into a fresh Uint8Array first: new Uint8Array(bytes)");
    }
    Layout();
    if (bytes.length < 88) { return null; }
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.length);
    if (view.getBigUint64(0, true) !== TableBlockMagic) { return null; }
    if (view.getBigUint64(8, true) !== BuildVersion) { return null; }
    if (view.getBigUint64(16, true) !== TableBlockByteOrder) { return null; }
    const extent = BigInt(bytes.length);
    let used = 88n;
    // the used extent, rounded to 64 WITHOUT the rounding itself wrapping:
    // used is already inside the extent, and the padding is paid out of the
    // slack that is left rather than added and compared after.
    const padding = (64n - (used % 64n)) % 64n;
    if (padding > extent - used) { return null; }
    used += padding;
    return { Bytes: bytes, View: view, Used: Number(used) };
  }

  function EntityId(block, index = 0) { const view = block.View; return view.getUint32(24 + index * 4, true); }
  function PosX(block, index = 0) { const view = block.View; return view.getInt32(28 + index * 4, true); }
  function PosY(block, index = 0) { const view = block.View; return view.getInt32(32 + index * 4, true); }
  function PosZ(block, index = 0) { const view = block.View; return view.getInt32(36 + index * 4, true); }
  function Yaw(block, index = 0) { const view = block.View; return view.getUint32(40 + index * 4, true); }
  function Pitch(block, index = 0) { const view = block.View; return view.getUint32(44 + index * 4, true); }
  function VelX(block, index = 0) { const view = block.View; return view.getInt32(48 + index * 4, true); }
  function VelY(block, index = 0) { const view = block.View; return view.getInt32(52 + index * 4, true); }
  function VelZ(block, index = 0) { const view = block.View; return view.getInt32(56 + index * 4, true); }
  function Health(block, index = 0) { const view = block.View; return view.getInt32(60 + index * 4, true); }
  function Weapon(block, index = 0) { const view = block.View; return view.getUint8(64 + index * 1); }
  function Damage(block, index = 0) { const view = block.View; return view.getBigUint64(72 + index * 8, true); }
  function DamageHas(block, bit, index = 0) { const view = block.View; return bit < 32 ? ((view.getUint32(72 + index * 8, true) >>> bit) & 1) === 1 : ((view.getUint32((72 + index * 8) + 4, true) >>> (bit - 32)) & 1) === 1; }
  function Moving(block, index = 0) { const view = block.View; return view.getUint8(80 + index * 1) !== 0; }
  function Firing(block, index = 0) { const view = block.View; return view.getUint8(81 + index * 1) !== 0; }

  return Object.freeze({
    // The storage a PRODUCER of this block allocates, sized from the declared
    // maxima (docs/SPEC-TABLES.md §19.1). A JavaScript consumer does not allocate
    // a block — the bytes are handed to it — but it caps by this: a playback
    // buffer, a recording, a scratch copy all size from the generated constant
    // rather than from a number a person wrote down beside it.
    MaxBytes: 128,
    Open, Layout, Type: projection, EntityId, PosX, PosY, PosZ, Yaw, Pitch, VelX, VelY, VelZ, Health, Weapon, Damage, DamageHas, Moving, Firing,
  });
})();

// TableStat's block: a view and a length, and then rows in place. Opening one is
// ONE check and no copy; reading a row is one add (docs/SPEC-TABLES.md §19.2).
//
// The bytes belong to the CONSUMER. Nothing here allocates but the handle.
export const TableStatBlock = (() => {
  const projection = Object.freeze({
    Name: "TableStat", BuildVersion: BuildVersion, Size: 32, Align: 8, NumFields: 2,
    Fields: Object.freeze([
      { Name: "stat_id", Offset: 24, Size: 4, Kind: 6, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
      { Name: "delta", Offset: 28, Size: 4, Kind: 4, OutOfLine: false, OffsetOfOffset: -1, CountOffset: -1, StrideOffset: -1, Stride: 0, IsArray: false, Counted: false, Optional: false, ArrayBound: 0, ElemSize: 4, PresentOffset: -1, ElementRef: null },
    ]),
  });

  // the layout contract's one independent pair (§19.3, §19.5): the array's
  // generated pitch, and the row object's own size.
  const strides = [
  ];
  // the check runs ONCE, before the first Open points at anything, and a
  // failure is this BUILD's defect rather than the bytes': the generated
  // constants disagree with each other, so it throws and names the block.
  let layoutChecked = false;
  function Layout() {
    if (!layoutChecked) {
      if (!TableBlockLayout.verify(projection, strides)) {
        throw new Error("TableStatBlock: this build's generated layout constants disagree with each other (docs/SPEC-TABLES.md §19.3) — regenerate");
      }
      layoutChecked = true;
    }
    return true;
  }

  // Open checks once and points, and this is the WHOLE check
  // (docs/SPEC-TABLES.md §19.2): the magic, the BYTE ORDER the prologue carries
  // against this build's own, the BUILD VERSION against this build's own,
  // each array's pitch, its offset_of, its COUNT against the declared
  // maximum and its extent inside the block, the used extent against the
  // bytes the caller passed, and the base's alignment. On a match the bytes
  // are what a build with this layout wrote, so there is nothing to validate
  // and nothing to fix up. On any failure it returns null and points at
  // nothing.
  //
  // There is ONE entry point and no tolerant twin: the block form is
  // same-build by construction — both sides generated from one declaration
  // at one build — so a consumer older than its producer is not a case. A
  // mismatch is a refusal; regenerate both sides. Data that must outlive the
  // build that wrote it takes the wire, which this same table still has.
  //
  // NULL IS ONE ANSWER AND IT MEANS ONE THING: these bytes are not a block of
  // this build — a foreign magic, the other byte order, another build version,
  // or framing the prologue's own numbers refuse. What the CALLER got wrong is
  // not a null, it is a throw with the fix in it: no Uint8Array at all is a
  // TypeError, and a view whose byteOffset is not a multiple of 64 — a Node
  // Buffer under 4 KiB is carved out of a shared pool at an arbitrary offset —
  // is a RangeError, because that is the caller's placement and not the bytes,
  // and the same bytes copied into a fresh Uint8Array open.
  //
  // EVERY NUMBER OUT OF THE INSTANCE IS BIGINT ARITHMETIC, and each term is
  // BOUNDED BEFORE IT IS ADDED. A forged offset_of near 2^63 must refuse, and
  // a Number would have lost the low bits of it before the comparison ran.
  // The C++ and C# sides hold the same shape for the same reason.
  function Open(bytes) {
    if (!(bytes instanceof Uint8Array)) {
      throw new TypeError("TableStatBlock.Open takes the block's bytes as a Uint8Array, not " + (bytes === null ? "null" : typeof bytes));
    }
    if ((bytes.byteOffset % 64) !== 0) { // the base's alignment
      throw new RangeError("TableStatBlock.Open: the view starts " + bytes.byteOffset + " bytes into its ArrayBuffer, and a block's base " +
        "must be a multiple of 64 (docs/SPEC-TABLES.md §19.1) — a pooled Node Buffer starts anywhere; copy the bytes " +
        "into a fresh Uint8Array first: new Uint8Array(bytes)");
    }
    Layout();
    if (bytes.length < 32) { return null; }
    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.length);
    if (view.getBigUint64(0, true) !== TableBlockMagic) { return null; }
    if (view.getBigUint64(8, true) !== BuildVersion) { return null; }
    if (view.getBigUint64(16, true) !== TableBlockByteOrder) { return null; }
    const extent = BigInt(bytes.length);
    let used = 32n;
    // the used extent, rounded to 64 WITHOUT the rounding itself wrapping:
    // used is already inside the extent, and the padding is paid out of the
    // slack that is left rather than added and compared after.
    const padding = (64n - (used % 64n)) % 64n;
    if (padding > extent - used) { return null; }
    used += padding;
    return { Bytes: bytes, View: view, Used: Number(used) };
  }

  function StatId(block, index = 0) { const view = block.View; return view.getUint32(24 + index * 4, true); }
  function Delta(block, index = 0) { const view = block.View; return view.getInt32(28 + index * 4, true); }

  return Object.freeze({
    // The storage a PRODUCER of this block allocates, sized from the declared
    // maxima (docs/SPEC-TABLES.md §19.1). A JavaScript consumer does not allocate
    // a block — the bytes are handed to it — but it caps by this: a playback
    // buffer, a recording, a scratch copy all size from the generated constant
    // rather than from a number a person wrote down beside it.
    MaxBytes: 64,
    Open, Layout, Type: projection, StatId, Delta,
  });
})();

// table TableMixed has NO block form: TableMixed.game_event is a union, and a block's blittable C# form is Sequential with generated padding, which cannot overlay arms (docs/SPEC-TABLES.md §19).
// Its wire (§3) is unaffected — only this projection is absent, and it is
// absent by construction rather than by refusal.

