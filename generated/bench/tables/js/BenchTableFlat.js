// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0x88cf953e975ace60
//
// THE FLAT TIER — the shipped JavaScript wire path: a single-word 32-bit
// bitpacker inlined at every field (byte-identical wire to serialize.js),
// constant widths and masks, zero function calls. Same generated classes,
// same bytes as the runtime tier (a standing CI gate); the runtime tier
// remains the diagnostic and reference surface — re-read a failing buffer
// through it to learn WHICH operation failed and why.
//
// Write<Name>Flat(value, view) -> bytes written, or -1 when the checked
// writer refuses an out-of-contract value (the production writer trusts
// the caller — serialize.js src/mode.js's own NODE_ENV fork, frozen at
// module load). Read<Name>Flat(value, view, numBits) -> bool, the family
// read verdict; reader obligations ride in every mode.
//
// Buffers are caller-owned DataViews. Write buffers: at least <Name>MaxBytes
// (the constants live in the runtime-tier module). Read buffers: at least
// FLAT_READ_SLACK = 8 bytes past the payload — 64-bit windows load
// unconditionally; copy exactly-sized receive buffers into a persistent
// MaxBytes + 8 scratch first.

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type TableHitEvent: the flat codec ----

function writeTableHitEventFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.TargetId & 0xfff) | (((value.Damage) & 0xfff) << 12) | (((value.HitKind) & 0x7) << 24) | ((value.Crit ? 1 : 0) << 27)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 28;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (28 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTableHitEventFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Damage) || value.Damage < 0 || value.Damage > 4095) {
    return -1;
  }
  if (!Number.isInteger(value.HitKind) || value.HitKind < 0 || value.HitKind > 7) {
    return -1;
  }
  v = ((value.TargetId & 0xfff) | (((value.Damage) & 0xfff) << 12) | (((value.HitKind) & 0x7) << 24) | ((value.Crit ? 1 : 0) << 27)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 28;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (28 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTableHitEventFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold TableHitEventMaxBytes.
export const WriteTableHitEventFlat = PRODUCTION ? writeTableHitEventFlatProduction : writeTableHitEventFlatChecked;

// ReadTableHitEventFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTableHitEventFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 28 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xfff) >>> 0;
  value.TargetId = v;
  v = (out >>> 12) & 0xfff;
  value.Damage = v | 0;
  v = (out >>> 24) & 0x7;
  value.HitKind = v | 0;
  v = (out >>> 27) & 0x1;
  value.Crit = v !== 0;
  return true;
}

// ---- type TableChatEvent: the flat codec ----

function writeTableChatEventFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.Channel) & 0x3) | ((value.Speaker & 0xfff) << 2)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 14;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (14 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTableChatEventFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Channel) || value.Channel < 0 || value.Channel > 3) {
    return -1;
  }
  v = (((value.Channel) & 0x3) | ((value.Speaker & 0xfff) << 2)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 14;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (14 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTableChatEventFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold TableChatEventMaxBytes.
export const WriteTableChatEventFlat = PRODUCTION ? writeTableChatEventFlatProduction : writeTableChatEventFlatChecked;

// ReadTableChatEventFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTableChatEventFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 14 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  value.Channel = v | 0;
  v = (out >>> 2) & 0xfff;
  value.Speaker = v;
  return true;
}

// ---- type TablePickupEvent: the flat codec ----

function writeTablePickupEventFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.ItemId & 0x3ff) | (((value.Amount) & 0xff) << 10)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 18;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (18 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeTablePickupEventFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Amount) || value.Amount < 0 || value.Amount > 255) {
    return -1;
  }
  v = ((value.ItemId & 0x3ff) | (((value.Amount) & 0xff) << 10)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 18;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (18 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteTablePickupEventFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold TablePickupEventMaxBytes.
export const WriteTablePickupEventFlat = PRODUCTION ? writeTablePickupEventFlatProduction : writeTablePickupEventFlatChecked;

// ReadTablePickupEventFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadTablePickupEventFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 18 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3ff) >>> 0;
  value.ItemId = v;
  v = (out >>> 10) & 0xff;
  value.Amount = v | 0;
  return true;
}

