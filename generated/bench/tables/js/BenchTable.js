// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0x88cf953e975ace60
//
// Wire functions return bool — the C++-style early-out. A schema validation
// failure (a wrong wire constant, nonzero reserved bits, an out-of-contract
// write) returns false WITHOUT latching; stream failures latch on
// stream.error — the runtime's own sticky latch. Callers get bool always;
// error tells the two apart.
//
// Number storage for widths of 32 bits or fewer, BigInt for 64 and 128 —
// the serialize.js value-domain seam. Checked/production comes from the
// stream object; generated code never reads NODE_ENV.

// Scratch holders for the runtime's {value} refs — single threaded per
// realm, always consumed in the same call that fills them.
const NUMBER_SCRATCH = { value: 0 };
const BOOL_SCRATCH = { value: false };

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
export const ProtocolId = 0x88cf953e975ace60n;

// TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
// a frozen object of Number values — the JS translation of the family's
// integer-backed enums; | max = ... headroom values are plain Numbers
export const TableWeapon = Object.freeze({
  None: 0,
  Fists: 1,
  Pistol: 2,
  Shotgun: 3,
  Rifle: 4,
  Sniper: 5,
  Smg: 6,
  Rocket: 7,
  Grenade: 8,
  Plasma: 9,
  Railgun: 10,
  Flamer: 11,
  Mine: 12,
  Turret: 13,
  Drone: 14,
  Repair: 15,
  Count: 15, // the declared variant count (SPEC §4.2)
  Max: 15, // the exported extent (SPEC §4.2)
});

// EnumNameTableWeapon: debug/log/tooling name for any TableWeapon wire value —
// out-of-set values (wire-legal up to the declared max) name as "???"
export function EnumNameTableWeapon(value) {
  switch (value) {
    case TableWeapon.None:
      return "None";
    case TableWeapon.Fists:
      return "Fists";
    case TableWeapon.Pistol:
      return "Pistol";
    case TableWeapon.Shotgun:
      return "Shotgun";
    case TableWeapon.Rifle:
      return "Rifle";
    case TableWeapon.Sniper:
      return "Sniper";
    case TableWeapon.Smg:
      return "Smg";
    case TableWeapon.Rocket:
      return "Rocket";
    case TableWeapon.Grenade:
      return "Grenade";
    case TableWeapon.Plasma:
      return "Plasma";
    case TableWeapon.Railgun:
      return "Railgun";
    case TableWeapon.Flamer:
      return "Flamer";
    case TableWeapon.Mine:
      return "Mine";
    case TableWeapon.Turret:
      return "Turret";
    case TableWeapon.Drone:
      return "Drone";
    case TableWeapon.Repair:
      return "Repair";
    default:
      return "???";
  }
}

// TableDamage — one bit per variant, consumed as masks; flags-typed fields store
// uint64 in every target, so BigInt here, wire 8 bits (SPEC §4.2). Masks
// are flat PascalCase — the Go target's spelling exactly, so the checker's
// existing claims cover them.
export const TableDamageBleeding = 1n << 0n;
export const TableDamageBurning = 1n << 1n;
export const TableDamageStunned = 1n << 2n;
export const TableDamageSlowed = 1n << 3n;
export const TableDamagePoisoned = 1n << 4n;
export const TableDamageShielded = 1n << 5n;
export const TableDamageAirborne = 1n << 6n;
export const TableDamageDowned = 1n << 7n;
export const TableDamageCount = 8; // the declared variant count (SPEC §4.2)

// FlagNameTableDamage: debug/log/tooling name for bit i of TableDamage —
// out-of-range bits name as "???"
export function FlagNameTableDamage(bit) {
  switch (bit) {
    case 0: return "Bleeding";
    case 1: return "Burning";
    case 2: return "Stunned";
    case 3: return "Slowed";
    case 4: return "Poisoned";
    case 5: return "Shielded";
    case 6: return "Airborne";
    case 7: return "Downed";
    default: return "???";
  }
}

// FlagNamesTableDamage renders the set bits of value (BigInt) as "A|B" — "0" for
// the empty set, bits past the declared variants as hex
export function FlagNamesTableDamage(value) {
  const names = [];
  if (value & (1n << 0n)) {
    names.push("Bleeding");
  }
  if (value & (1n << 1n)) {
    names.push("Burning");
  }
  if (value & (1n << 2n)) {
    names.push("Stunned");
  }
  if (value & (1n << 3n)) {
    names.push("Slowed");
  }
  if (value & (1n << 4n)) {
    names.push("Poisoned");
  }
  if (value & (1n << 5n)) {
    names.push("Shielded");
  }
  if (value & (1n << 6n)) {
    names.push("Airborne");
  }
  if (value & (1n << 7n)) {
    names.push("Downed");
  }
  if (value >> 8n) {
    names.push("0x" + ((value >> 8n) << 8n).toString(16));
  }
  return names.length === 0 ? "0" : names.join("|");
}

// type TableHitEvent
export class TableHitEvent {
  constructor() {
    this.TargetId = 0;
    this.Damage = 0; // wire [0, 4095]
    this.HitKind = 0; // wire [0, 7]
    this.Crit = false;
  }
}

// TableHitEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TableHitEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TableHitEventMaxBits = 28;
export const TableHitEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTableHitEvent(value) {
  value.TargetId = 0;
  value.Damage = 0;
  value.HitKind = 0;
  value.Crit = false;
}

export function WriteTableHitEvent(stream, value) {
  NUMBER_SCRATCH.value = value.TargetId;
  if (!stream.serializeBits(NUMBER_SCRATCH, 12)) {
    return false;
  }
  if (!Number.isInteger(value.Damage) || value.Damage < 0 || value.Damage > 4095) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Damage;
  if (!stream.serializeBits(NUMBER_SCRATCH, 12)) {
    return false;
  }
  if (!Number.isInteger(value.HitKind) || value.HitKind < 0 || value.HitKind > 7) {
    return false;
  }
  NUMBER_SCRATCH.value = value.HitKind;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  BOOL_SCRATCH.value = value.Crit;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  return true;
}

export function ReadTableHitEvent(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 12)) {
    return false;
  }
  value.TargetId = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 4095)) {
    return false;
  }
  value.Damage = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 7)) {
    return false;
  }
  value.HitKind = NUMBER_SCRATCH.value;
  if (!stream.serializeBool(BOOL_SCRATCH)) {
    return false;
  }
  value.Crit = BOOL_SCRATCH.value;
  return true;
}

// type TableChatEvent
export class TableChatEvent {
  constructor() {
    this.Channel = 0; // wire [0, 3]
    this.Speaker = 0;
  }
}

// TableChatEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TableChatEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TableChatEventMaxBits = 14;
export const TableChatEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTableChatEvent(value) {
  value.Channel = 0;
  value.Speaker = 0;
}

export function WriteTableChatEvent(stream, value) {
  if (!Number.isInteger(value.Channel) || value.Channel < 0 || value.Channel > 3) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Channel;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Speaker;
  if (!stream.serializeBits(NUMBER_SCRATCH, 12)) {
    return false;
  }
  return true;
}

export function ReadTableChatEvent(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) {
    return false;
  }
  value.Channel = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 12)) {
    return false;
  }
  value.Speaker = NUMBER_SCRATCH.value;
  return true;
}

// type TablePickupEvent
export class TablePickupEvent {
  constructor() {
    this.ItemId = 0;
    this.Amount = 0; // wire [0, 255]
  }
}

// TablePickupEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// TablePickupEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TablePickupEventMaxBits = 18;
export const TablePickupEventMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTablePickupEvent(value) {
  value.ItemId = 0;
  value.Amount = 0;
}

export function WriteTablePickupEvent(stream, value) {
  NUMBER_SCRATCH.value = value.ItemId;
  if (!stream.serializeBits(NUMBER_SCRATCH, 10)) {
    return false;
  }
  if (!Number.isInteger(value.Amount) || value.Amount < 0 || value.Amount > 255) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Amount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  return true;
}

export function ReadTablePickupEvent(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 10)) {
    return false;
  }
  value.ItemId = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 255)) {
    return false;
  }
  value.Amount = NUMBER_SCRATCH.value;
  return true;
}

// TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
export const TableEventType = Object.freeze({
  None: 0,
  Hit: 1,
  Chat: 2,
  Pickup: 3,
  Count: 3, // the declared variant count (SPEC §4.2)
  Max: 3, // the exported extent (SPEC §4.2)
});

// EnumNameTableEventType: debug/log/tooling name for any TableEventType wire value —
// out-of-set values (wire-legal up to the declared max) name as "???"
export function EnumNameTableEventType(value) {
  switch (value) {
    case TableEventType.None:
      return "None";
    case TableEventType.Hit:
      return "Hit";
    case TableEventType.Chat:
      return "Chat";
    case TableEventType.Pickup:
      return "Pickup";
    default:
      return "???";
  }
}

// TableEvent — at most one of the arms; Type says which. Construction is the empty
// union (None). A read zero-establishes exactly the selected arm before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
export class TableEvent {
  constructor() {
    this.Type = TableEventType.None;
    this.Hit = new TableHitEvent();
    this.Chat = new TableChatEvent();
    this.Pickup = new TablePickupEvent();
  }
}

// TableEventMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// TableEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const TableEventMaxBits = 30;
export const TableEventMaxBytes = 8;

// ZeroTableEvent resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm re-zeroes at its next selection.
export function ZeroTableEvent(value) {
  value.Type = TableEventType.None;
}

export function WriteTableEvent(stream, value) {
  if (!Number.isInteger(value.Type) || value.Type < 0 || value.Type > 3) {
    return false; // the tag validates BEFORE it rides (SPEC §4.8)
  }
  NUMBER_SCRATCH.value = value.Type;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  switch (value.Type) {
    case TableEventType.Hit:
      return WriteTableHitEvent(stream, value.Hit);
    case TableEventType.Chat:
      return WriteTableChatEvent(stream, value.Chat);
    case TableEventType.Pickup:
      return WriteTablePickupEvent(stream, value.Pickup);
  }
  return true; // None — the tag is the whole wire (SPEC §4.8)
}

export function ReadTableEvent(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // rejects a tag above the count (SPEC §4.8)
    return false;
  }
  value.Type = NUMBER_SCRATCH.value;
  switch (value.Type) {
    case TableEventType.Hit:
      ZeroTableHitEvent(value.Hit); // the selected arm starts from the zero form (SPEC §5)
      return ReadTableHitEvent(stream, value.Hit);
    case TableEventType.Chat:
      ZeroTableChatEvent(value.Chat); // the selected arm starts from the zero form (SPEC §5)
      return ReadTableChatEvent(stream, value.Chat);
    case TableEventType.Pickup:
      ZeroTablePickupEvent(value.Pickup); // the selected arm starts from the zero form (SPEC §5)
      return ReadTablePickupEvent(stream, value.Pickup);
  }
  return true; // None
}

