/* Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package benchtable — the BLOCK FORM (docs/SPEC-TABLES.md §19).

   The block form's COLD half, in its own translation unit: the open path, run
   once per handoff, and the reflection descriptors, which are constant data.
   The per-frame FILL path stays inline in the header, because a call per row
   accessor is exactly the cost this form exists to avoid. */

#include "BenchTableBlock.h"

/* ---- the block form of table TableEntity: the open path and the descriptors ---- */

int schema_benchtable_table_entity_block_open_( TableEntityBlock * block, void * base, int64_t bytes )
{
    const uint8_t * raw;
    uint64_t magic;
    TableEntityBlockProjection * projection;
    int64_t used, padding;
    block->base = NULL;
    block->projection = NULL;
    block->bytes = 0;
    if ( base == NULL || bytes < 88 ) { return 0; }
    if ( ( (uintptr_t) base % 64 ) != 0 ) { return 0; } /* the base's alignment */
    raw = (const uint8_t *) base;
    magic = table_block_read64( raw );
    if ( magic != table_block_magic )
    {
        /* a byte-swapped magic is a FOREIGN BYTE ORDER, and anything else is
           not a block at all. Both refuse; the distinction is here so a
           reader of this code knows the check covers the order too. */
        (void) table_block_byteswap64( magic );
        return 0;
    }
    if ( table_block_read64( raw + 8 ) != SCHEMA_BENCHTABLE_BUILD_VERSION_VALUE ) { return 0; }
    if ( table_block_read64( raw + 16 ) != table_block_byte_order )
    {
        return 0; /* a block of the other byte order: the fix-up path is a named obligation */
    }
    projection = (TableEntityBlockProjection *) base;
    used = 88;
    /* the used extent, rounded to 64 WITHOUT the rounding itself carrying past
       the top of the type: used is already inside bytes, and the padding is
       paid out of the slack that is left rather than added and compared after. */
    padding = ( 64 - ( used % 64 ) ) % 64;
    if ( padding > bytes - used ) { return 0; }
    used += padding;
    block->base = (uint8_t *) base;
    block->projection = projection;
    block->bytes = used;
    return 1;
}

/* Every record's field table, concatenated: the projection's, then each
   row's. One name for the whole graph (docs/SPEC-TABLES.md §11). */
static const TableBlockFieldInfo schema_benchtable_table_entity_block_fields_[] = {
    /* TableEntity (the projection) */
    { "entity_id", 24u, 4u, 7, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "pos_x", 28u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "pos_y", 32u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "pos_z", 36u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "yaw", 40u, 4u, 7, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "pitch", 44u, 4u, 7, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "vel_x", 48u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "vel_y", 52u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "vel_z", 56u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "health", 60u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "weapon", 64u, 1u, 7, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 1u, 0xffffffffu, NULL },
    { "damage", 72u, 8u, 9, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 8u, 0xffffffffu, NULL },
    { "moving", 80u, 1u, 1, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 1u, 0xffffffffu, NULL },
    { "firing", 81u, 1u, 1, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 1u, 0xffffffffu, NULL },
};

const TableBlockInfo schema_benchtable_table_entity_block_info_ = { "TableEntity", SCHEMA_BENCHTABLE_BUILD_VERSION_VALUE, 88u, 8u, 14, schema_benchtable_table_entity_block_fields_ };

/* ---- the block form of table TableStat: the open path and the descriptors ---- */

int schema_benchtable_table_stat_block_open_( TableStatBlock * block, void * base, int64_t bytes )
{
    const uint8_t * raw;
    uint64_t magic;
    TableStatBlockProjection * projection;
    int64_t used, padding;
    block->base = NULL;
    block->projection = NULL;
    block->bytes = 0;
    if ( base == NULL || bytes < 32 ) { return 0; }
    if ( ( (uintptr_t) base % 64 ) != 0 ) { return 0; } /* the base's alignment */
    raw = (const uint8_t *) base;
    magic = table_block_read64( raw );
    if ( magic != table_block_magic )
    {
        /* a byte-swapped magic is a FOREIGN BYTE ORDER, and anything else is
           not a block at all. Both refuse; the distinction is here so a
           reader of this code knows the check covers the order too. */
        (void) table_block_byteswap64( magic );
        return 0;
    }
    if ( table_block_read64( raw + 8 ) != SCHEMA_BENCHTABLE_BUILD_VERSION_VALUE ) { return 0; }
    if ( table_block_read64( raw + 16 ) != table_block_byte_order )
    {
        return 0; /* a block of the other byte order: the fix-up path is a named obligation */
    }
    projection = (TableStatBlockProjection *) base;
    used = 32;
    /* the used extent, rounded to 64 WITHOUT the rounding itself carrying past
       the top of the type: used is already inside bytes, and the padding is
       paid out of the slack that is left rather than added and compared after. */
    padding = ( 64 - ( used % 64 ) ) % 64;
    if ( padding > bytes - used ) { return 0; }
    used += padding;
    block->base = (uint8_t *) base;
    block->projection = projection;
    block->bytes = used;
    return 1;
}

/* Every record's field table, concatenated: the projection's, then each
   row's. One name for the whole graph (docs/SPEC-TABLES.md §11). */
static const TableBlockFieldInfo schema_benchtable_table_stat_block_fields_[] = {
    /* TableStat (the projection) */
    { "stat_id", 24u, 4u, 6, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
    { "delta", 28u, 4u, 4, 0, 0xffffffffu, 0xffffffffu, 0xffffffffu, 0u, 0, 0, 0, 0, 4u, 0xffffffffu, NULL },
};

const TableBlockInfo schema_benchtable_table_stat_block_info_ = { "TableStat", SCHEMA_BENCHTABLE_BUILD_VERSION_VALUE, 32u, 8u, 2, schema_benchtable_table_stat_block_fields_ };

