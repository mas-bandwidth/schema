// Code generated by the schema compiler from BenchTable.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — protocol id 0x0926221bcb6f475f
//
// The shipped Java wire path (issue #156): the serialize.java bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, data) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts live in one assert-predicate call — active
// under -ea, dormant bytecode otherwise — and width masks stay, as wire
// arithmetic. The buffer must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, data, numBits) -> boolean, the family read verdict.
// The wire is a trust boundary: reads validate everything — bounds fused
// per static run, ranges, wire constants, reserved and align padding,
// union tags — and NEVER throw on hostile bytes. No slack past the
// payload is required: the reader prices its 64-bit windows inside the
// buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

package benchtable;

// BenchTable carries every generated declaration of BenchTable.schema — Java has no
// top-level functions or constants, so the file's one public class is their
// home; types nest inside it (SPEC §6.1 naming).
public final class BenchTable {
    private BenchTable() {}

    // 64-bit little-endian word access into byte[] — the proven-fast JVM
    // form (VarHandle byteArrayViewVarHandle, serialize.java's own).
    private static final java.lang.invoke.VarHandle LONG_LE =
            java.lang.invoke.MethodHandles.byteArrayViewVarHandle(
                    long[].class, java.nio.ByteOrder.LITTLE_ENDIAN);

    // The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
    // sides at the same id speak identical bits; there is no other versioning.
    public static final long protocolId = 0x0926221bcb6f475fL;

    // TableWeapon — None = 0 implicit, variants dense from 1, wire range [0, 15] (SPEC §4.2);
    // an int-constant namespace — the Java translation of the family's integer-
    // backed enums: storage must hold every wire-legal value, and | max = ...
    // headroom values have no Java enum member to be
    public static final class TableWeapon {
        private TableWeapon() {}

        public static final byte none = 0;
        public static final byte fists = 1;
        public static final byte pistol = 2;
        public static final byte shotgun = 3;
        public static final byte rifle = 4;
        public static final byte sniper = 5;
        public static final byte smg = 6;
        public static final byte rocket = 7;
        public static final byte grenade = 8;
        public static final byte plasma = 9;
        public static final byte railgun = 10;
        public static final byte flamer = 11;
        public static final byte mine = 12;
        public static final byte turret = 13;
        public static final byte drone = 14;
        public static final byte repair = 15;
        // the declared variant count (SPEC §4.2)
        public static final byte count = 15;
        // the exported extent (SPEC §4.2)
        public static final byte max = 15;
    }

    // enumNameTableWeapon: debug/log/tooling name for any TableWeapon storage value —
    // out-of-set values (wire-legal up to the declared max) name as "???"
    public static String enumNameTableWeapon(long value) {
        if (value == TableWeapon.none) {
            return "None";
        }
        if (value == TableWeapon.fists) {
            return "Fists";
        }
        if (value == TableWeapon.pistol) {
            return "Pistol";
        }
        if (value == TableWeapon.shotgun) {
            return "Shotgun";
        }
        if (value == TableWeapon.rifle) {
            return "Rifle";
        }
        if (value == TableWeapon.sniper) {
            return "Sniper";
        }
        if (value == TableWeapon.smg) {
            return "Smg";
        }
        if (value == TableWeapon.rocket) {
            return "Rocket";
        }
        if (value == TableWeapon.grenade) {
            return "Grenade";
        }
        if (value == TableWeapon.plasma) {
            return "Plasma";
        }
        if (value == TableWeapon.railgun) {
            return "Railgun";
        }
        if (value == TableWeapon.flamer) {
            return "Flamer";
        }
        if (value == TableWeapon.mine) {
            return "Mine";
        }
        if (value == TableWeapon.turret) {
            return "Turret";
        }
        if (value == TableWeapon.drone) {
            return "Drone";
        }
        if (value == TableWeapon.repair) {
            return "Repair";
        }
        return "???";
    }

    // TableDamage — one bit per variant, consumed as masks; flags-typed fields store
    // uint64 in every target — a bit-transparent long here — wire 8 bits
    // (SPEC §4.2). Mask names are the family's flat spelling, lowerCamel.
    public static final long tableDamageBleeding = 1L << 0;
    public static final long tableDamageBurning = 1L << 1;
    public static final long tableDamageStunned = 1L << 2;
    public static final long tableDamageSlowed = 1L << 3;
    public static final long tableDamagePoisoned = 1L << 4;
    public static final long tableDamageShielded = 1L << 5;
    public static final long tableDamageAirborne = 1L << 6;
    public static final long tableDamageDowned = 1L << 7;
    // the declared variant count (SPEC §4.2)
    public static final int tableDamageCount = 8;

    // flagNameTableDamage: debug/log/tooling name for bit i of TableDamage —
    // out-of-range bits name as "???"
    public static String flagNameTableDamage(int bit) {
        switch (bit) {
            case 0:
                return "Bleeding";
            case 1:
                return "Burning";
            case 2:
                return "Stunned";
            case 3:
                return "Slowed";
            case 4:
                return "Poisoned";
            case 5:
                return "Shielded";
            case 6:
                return "Airborne";
            case 7:
                return "Downed";
            default:
                return "???";
        }
    }

    // flagNamesTableDamage renders the set bits of value as "A|B" — "0" for the
    // empty set, bits past the declared variants as hex
    public static String flagNamesTableDamage(long value) {
        StringBuilder names = new StringBuilder();
        if ((value & (1L << 0)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Bleeding");
        }
        if ((value & (1L << 1)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Burning");
        }
        if ((value & (1L << 2)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Stunned");
        }
        if ((value & (1L << 3)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Slowed");
        }
        if ((value & (1L << 4)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Poisoned");
        }
        if ((value & (1L << 5)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Shielded");
        }
        if ((value & (1L << 6)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Airborne");
        }
        if ((value & (1L << 7)) != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            names.append("Downed");
        }
        if (value >>> 8 != 0) {
            if (names.length() > 0) {
                names.append('|');
            }
            // Long.toHexString renders the bit pattern, not a sign
            names.append("0x").append(Long.toHexString((value >>> 8) << 8));
        }
        return names.length() == 0 ? "0" : names.toString();
    }

    // type TableHitEvent
    public static final class TableHitEvent {
        public int targetId;
        // wire [0, 4095]
        public int damage;
        // wire [0, 7]
        public int hitKind;
        public boolean crit;
    }

    // tableHitEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // tableHitEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int tableHitEventMaxBits = 28;
    public static final int tableHitEventMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroTableHitEvent(TableHitEvent value) {
        value.targetId = 0;
        value.damage = 0;
        value.hitKind = 0;
        value.crit = false;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initTableHitEvent(TableHitEvent value) {
        value.targetId = 0;
        value.damage = 0;
        value.hitKind = 0;
        value.crit = false;
    }

    // checkWriteTableHitEvent is writeTableHitEvent's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteTableHitEvent(TableHitEvent value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= tableHitEventMaxBytes;
        assert value.damage >= 0;
        assert value.damage <= 4095;
        assert value.hitKind >= 0;
        assert value.hitKind <= 7;
        return true;
    }

    // writeTableHitEvent packs value into data — the trusted writer (contracts in the
    // checkWriteTableHitEvent predicate, one dormant assert call without -ea). The buffer
    // must hold tableHitEventMaxBytes. Returns the bytes written.
    public static int writeTableHitEvent(TableHitEvent value, byte[] data) {
        assert checkWriteTableHitEvent(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.targetId & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.damage) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.hitKind) & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = value.crit ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readTableHitEvent decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readTableHitEvent(TableHitEvent value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 28 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.targetId = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.damage = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        value.hitKind = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.crit = v != 0;
        return true;
    }

    // measureTableHitEvent is the exact wire bits writeTableHitEvent would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureTableHitEvent(TableHitEvent value) {
        return 28;
    }

    // type TableChatEvent
    public static final class TableChatEvent {
        // wire [0, 3]
        public int channel;
        public int speaker;
    }

    // tableChatEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // tableChatEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int tableChatEventMaxBits = 14;
    public static final int tableChatEventMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroTableChatEvent(TableChatEvent value) {
        value.channel = 0;
        value.speaker = 0;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initTableChatEvent(TableChatEvent value) {
        value.channel = 0;
        value.speaker = 0;
    }

    // checkWriteTableChatEvent is writeTableChatEvent's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteTableChatEvent(TableChatEvent value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= tableChatEventMaxBytes;
        assert value.channel >= 0;
        assert value.channel <= 3;
        return true;
    }

    // writeTableChatEvent packs value into data — the trusted writer (contracts in the
    // checkWriteTableChatEvent predicate, one dormant assert call without -ea). The buffer
    // must hold tableChatEventMaxBytes. Returns the bytes written.
    public static int writeTableChatEvent(TableChatEvent value, byte[] data) {
        assert checkWriteTableChatEvent(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.channel) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        v = value.speaker & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readTableChatEvent decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readTableChatEvent(TableChatEvent value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 14 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        value.channel = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        value.speaker = (int) v;
        return true;
    }

    // measureTableChatEvent is the exact wire bits writeTableChatEvent would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureTableChatEvent(TableChatEvent value) {
        return 14;
    }

    // type TablePickupEvent
    public static final class TablePickupEvent {
        public int itemId;
        // wire [0, 255]
        public int amount;
    }

    // tablePickupEventMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // tablePickupEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int tablePickupEventMaxBits = 18;
    public static final int tablePickupEventMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroTablePickupEvent(TablePickupEvent value) {
        value.itemId = 0;
        value.amount = 0;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initTablePickupEvent(TablePickupEvent value) {
        value.itemId = 0;
        value.amount = 0;
    }

    // checkWriteTablePickupEvent is writeTablePickupEvent's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteTablePickupEvent(TablePickupEvent value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= tablePickupEventMaxBytes;
        assert value.amount >= 0;
        assert value.amount <= 255;
        return true;
    }

    // writeTablePickupEvent packs value into data — the trusted writer (contracts in the
    // checkWriteTablePickupEvent predicate, one dormant assert call without -ea). The buffer
    // must hold tablePickupEventMaxBytes. Returns the bytes written.
    public static int writeTablePickupEvent(TablePickupEvent value, byte[] data) {
        assert checkWriteTablePickupEvent(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.itemId & 0x3ffL;
        scratch |= v << scratchBits;
        scratchBits += 10;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (10 - scratchBits);
        }
        v = (value.amount) & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readTablePickupEvent decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readTablePickupEvent(TablePickupEvent value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 18 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffL;
        bitsRead += 10;
        value.itemId = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        value.amount = (int) v;
        return true;
    }

    // measureTablePickupEvent is the exact wire bits writeTablePickupEvent would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureTablePickupEvent(TablePickupEvent value) {
        return 18;
    }

    // TableEventType: union TableEvent's tag — None = 0, then each variant in declared order (SPEC §4.8)
    public static final class TableEventType {
        private TableEventType() {}

        public static final byte none = 0;
        public static final byte hit = 1;
        public static final byte chat = 2;
        public static final byte pickup = 3;
        // the declared variant count (SPEC §4.2)
        public static final byte count = 3;
        // the exported extent (SPEC §4.2)
        public static final byte max = 3;
    }

    // enumNameTableEventType: debug/log/tooling name for any TableEventType storage value —
    // out-of-set values (wire-legal up to the declared max) name as "???"
    public static String enumNameTableEventType(long value) {
        if (value == TableEventType.none) {
            return "None";
        }
        if (value == TableEventType.hit) {
            return "Hit";
        }
        if (value == TableEventType.chat) {
            return "Chat";
        }
        if (value == TableEventType.pickup) {
            return "Pickup";
        }
        return "???";
    }

    // TableEvent — at most one of the arms; type says which. Construction is the empty
    // union (None). Every read selection initializes the chosen payload from
    // construction defaults; unselected arms keep what they last held — the
    // reused-storage discipline. Consumers read the selected arm only.
    public static final class TableEvent {
        public byte type = TableEventType.none;
        public final TableHitEvent hit = new TableHitEvent();
        public final TableChatEvent chat = new TableChatEvent();
        public final TablePickupEvent pickup = new TablePickupEvent();
    }

    // tableEventMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
    // tableEventMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int tableEventMaxBits = 30;
    public static final int tableEventMaxBytes = 8;

    // zeroTableEvent resets value to the §5 zero form — the empty union. The tag alone
    // resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
    // is unselected at None; an arm initializes at its next selection.
    public static void zeroTableEvent(TableEvent value) {
        value.type = TableEventType.none;
    }

    // checkWriteTableEvent is writeTableEvent's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteTableEvent(TableEvent value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= tableEventMaxBytes;
        assert (value.type & 0xffL) >= 0;
        assert (value.type & 0xffL) <= 3;
        switch (value.type) {
            case 1:
                assert value.hit.damage >= 0;
                assert value.hit.damage <= 4095;
                assert value.hit.hitKind >= 0;
                assert value.hit.hitKind <= 7;
                break;
            case 2:
                assert value.chat.channel >= 0;
                assert value.chat.channel <= 3;
                break;
            case 3:
                assert value.pickup.amount >= 0;
                assert value.pickup.amount <= 255;
                break;
        }
        return true;
    }

    // writeTableEvent packs value into data — the trusted writer (contracts in the
    // checkWriteTableEvent predicate, one dormant assert call without -ea). The buffer
    // must hold tableEventMaxBytes. Returns the bytes written.
    public static int writeTableEvent(TableEvent value, byte[] data) {
        assert checkWriteTableEvent(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.type & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        switch (value.type) {
            case 1:
                v = value.hit.targetId & 0xfffL;
                scratch |= v << scratchBits;
                scratchBits += 12;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (12 - scratchBits);
                }
                v = (value.hit.damage) & 0xfffL;
                scratch |= v << scratchBits;
                scratchBits += 12;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (12 - scratchBits);
                }
                v = (value.hit.hitKind) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                v = value.hit.crit ? 1 : 0;
                scratch |= v << scratchBits;
                scratchBits += 1;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (1 - scratchBits);
                }
                break;
            case 2:
                v = (value.chat.channel) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                v = value.chat.speaker & 0xfffL;
                scratch |= v << scratchBits;
                scratchBits += 12;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (12 - scratchBits);
                }
                break;
            case 3:
                v = value.pickup.itemId & 0x3ffL;
                scratch |= v << scratchBits;
                scratchBits += 10;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (10 - scratchBits);
                }
                v = (value.pickup.amount) & 0xffL;
                scratch |= v << scratchBits;
                scratchBits += 8;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (8 - scratchBits);
                }
                break;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readTableEvent decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readTableEvent(TableEvent value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        value.type = (byte) v;
        switch (value.type) {
            case 1:
                value.hit.targetId = 0;
                value.hit.damage = 0;
                value.hit.hitKind = 0;
                value.hit.crit = false;
                if (bitsRead + 28 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0xfffL;
                bitsRead += 12;
                value.hit.targetId = (int) v;
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0xfffL;
                bitsRead += 12;
                value.hit.damage = (int) v;
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.hit.hitKind = (int) v;
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x1L;
                bitsRead += 1;
                value.hit.crit = v != 0;
                break;
            case 2:
                value.chat.channel = 0;
                value.chat.speaker = 0;
                if (bitsRead + 14 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                value.chat.channel = (int) v;
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0xfffL;
                bitsRead += 12;
                value.chat.speaker = (int) v;
                break;
            case 3:
                value.pickup.itemId = 0;
                value.pickup.amount = 0;
                if (bitsRead + 18 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3ffL;
                bitsRead += 10;
                value.pickup.itemId = (int) v;
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0xffL;
                bitsRead += 8;
                value.pickup.amount = (int) v;
                break;
        }
        return true;
    }

    // measureTableEvent is the exact wire bits writeTableEvent would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureTableEvent(TableEvent value) {
        int bits = 0;
        bits += 2;
        switch (value.type) {
            case 1:
                bits += 28;
                break;
            case 2:
                bits += 14;
                break;
            case 3:
                bits += 18;
                break;
        }
        return bits;
    }
}
