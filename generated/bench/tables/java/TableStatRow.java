// Code generated by the schema compiler for package benchtable. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package benchtable — one record of the two accelerators, read where it lies (docs/SPEC-TABLES.md §7, §19).

package benchtable;

// TableStat — a block row, a cooked record, or a record one of them nests by
// value. `Row` is a CLAIMED suffix (docs/SPEC-TABLES.md §11), so no declaration
// in the unit can take it.
//
// The accessors are STATIC over (byte[] data, int at): `at` is where this
// record starts in the caller's array, and every read is one offset from it.
// Nothing here copies and nothing here allocates.
public final class TableStatRow {
    private TableStatRow() {}

    /** the record's own size and alignment, from the ONE layout model (§19.3). */
    public static final int size = 8;
    public static final int align = 4;

    /** where each field starts, in declaration order — the accessors' own
     *  constants, which TableBlockLayout / TableCookLayout check against the
     *  descriptors below (§19.3). */
    static final int[] offsets = { 0, 4 };

    public static int statId(byte[] data, int at) { return TableBytes.i32(data, at + 0); }

    public static int delta(byte[] data, int at) { return TableBytes.i32(data, at + 4); }

    // this record's COOK descriptor (docs/SPEC-TABLES.md §7): the facts a region
    // actually has — where a field sits, how big it is, whether it is a
    // POINTER EDGE, the bound its COUNT COMPANION is checked against, and the
    // record it names.
    // published by CLASS INITIALIZATION, for the reason the block half states.
    private static final class CookInfoHolder {
        static final TableCookInfo INFO = build();

        private static TableCookInfo build() {
        TableCookInfo info = new TableCookInfo();
        info.name = "TableStat"; info.size = 8; info.align = 4; info.numFields = 2;
        TableCookFieldInfo[] fields = new TableCookFieldInfo[2];
        fields[0] = TableCookFieldInfo.of("stat_id", 0, 4, 4, false, 1, false, -1, -1, TableCookStorage.UNSIGNED, null);
        fields[1] = TableCookFieldInfo.of("delta", 4, 4, 4, false, 1, false, -1, -1, TableCookStorage.SIGNED, null);
        info.fields = fields;
        return info;
        }
    }

    public static TableCookInfo cookInfo() { return CookInfoHolder.INFO; }

}
