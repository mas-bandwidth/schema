// Code generated by the schema compiler from Bench.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package bench — protocol id 0x694f261d887abaa5
//
// The shipped Dart wire path (issue #155): the serialize.dart bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, view) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts are asserted — active under --enable-asserts,
// compiled out in release — and width masks stay, as wire arithmetic. The
// buffer behind view must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, view, numBits) -> bool, the family read verdict. The
// wire is a trust boundary: reads validate everything — bounds fused per
// static run, ranges, wire constants, reserved and align padding, union
// tags — and NEVER throw on hostile bytes. No slack past the payload is
// required: the reader prices its 64-bit windows inside the buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

import 'dart:typed_data';

// One 8-byte conversion scratch under overlaid typed-data views — single
// threaded per isolate, always consumed in the same operation that fills it.
final Float32List _f32 = Float32List(2);
final Uint32List _u32 = _f32.buffer.asUint32List();
final Float64List _f64 = _f32.buffer.asFloat64List();
final Uint64List _u64 = _f32.buffer.asUint64List();

// The 32 bits of the IEEE-754 single representation of value. Non-NaN goes
// through the hardware conversion; a NaN narrows in software — sign kept,
// top 23 mantissa bits kept, the quiet bit forced only for the all-low-
// payload case — so every pattern the read half produces round trips.
@pragma('vm:prefer-inline')
int _float32BitsFromDouble(double value) {
  if (!value.isNaN) {
    _f32[0] = value;
    return _u32[0];
  }
  _f64[0] = value;
  final bits64 = _u64[0];
  final sign = (bits64 >>> 63) << 31;
  var mantissa = (bits64 >>> 29) & 0x7fffff;
  if (mantissa == 0) {
    mantissa = 0x400000;
  }
  return sign | 0x7f800000 | mantissa;
}

// The double whose IEEE-754 single representation is bits. NaN patterns
// widen in software (the hardware conversion quiets a signaling NaN); the
// reader reproduces the transmitted pattern exactly.
@pragma('vm:prefer-inline')
double _doubleFromFloat32Bits(int bits) {
  if ((bits & 0x7f800000) == 0x7f800000 && (bits & 0x7fffff) != 0) {
    _u64[0] =
        ((bits >>> 31) << 63) | 0x7ff0000000000000 | ((bits & 0x7fffff) << 29);
    return _f64[0];
  }
  _u32[0] = bits;
  return _f32[0];
}

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
const int protocolId = 0x694f261d887abaa5;

// type BenchPacket
final class BenchPacket {
  // wire [-100, 100]
  int a = 0;
  // wire [0, 65535]
  int b = 0;
  // wire [-1000000, 1000000]
  int c = 0;
  int bits7 = 0;
  int bits13 = 0;
  int bits23 = 0;
  bool flag = false;
  double x = 0.0;
  double y = 0.0;
  double z = 0.0;
  int big = 0;
  final Uint8List blob = Uint8List(17);
}

// benchPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// benchPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int benchPacketMaxBits = 392;
const int benchPacketMaxBytes = 56;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroBenchPacket(BenchPacket value) {
  value.a = 0;
  value.b = 0;
  value.c = 0;
  value.bits7 = 0;
  value.bits13 = 0;
  value.bits23 = 0;
  value.flag = false;
  value.x = 0.0;
  value.y = 0.0;
  value.z = 0.0;
  value.big = 0;
  value.blob.fillRange(0, value.blob.length, 0);
}

// writeBenchPacket packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// benchPacketMaxBytes. Returns the bytes written.
int writeBenchPacket(BenchPacket value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= benchPacketMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.a >= -100);
  assert(value.a <= 100);
  v = (value.a + 100) & 0xff;
  scratch |= v << scratchBits;
  scratchBits += 8;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (8 - scratchBits);
  }
  assert(value.b >= 0);
  assert(value.b <= 65535);
  v = (value.b) & 0xffff;
  scratch |= v << scratchBits;
  scratchBits += 16;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (16 - scratchBits);
  }
  assert(value.c >= -1000000);
  assert(value.c <= 1000000);
  v = (value.c + 1000000) & 0x1fffff;
  scratch |= v << scratchBits;
  scratchBits += 21;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (21 - scratchBits);
  }
  v = value.bits7 & 0x7f;
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  v = value.bits13 & 0x1fff;
  scratch |= v << scratchBits;
  scratchBits += 13;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (13 - scratchBits);
  }
  v = value.bits23 & 0x7fffff;
  scratch |= v << scratchBits;
  scratchBits += 23;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (23 - scratchBits);
  }
  v = value.flag ? 1 : 0;
  scratch |= v << scratchBits;
  scratchBits += 1;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (1 - scratchBits);
  }
  v = _float32BitsFromDouble(value.x);
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = _float32BitsFromDouble(value.y);
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = _float32BitsFromDouble(value.z);
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = value.big & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = (value.big >>> 32) & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  {
    final pad = scratchBits & 7;
    if (pad != 0) {
      scratchBits += 8 - pad;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = 0;
      }
    }
  }
  for (var i0 = 0; i0 < 17; i0++) {
    v = value.blob[i0] & 0xff;
    scratch |= v << scratchBits;
    scratchBits += 8;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (8 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readBenchPacket decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readBenchPacket(BenchPacket value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 249 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xff;
  bitsRead += 8;
  if (v > 200) {
    return false; // a smuggled offset is refused
  }
  value.a = -100 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffff;
  bitsRead += 16;
  value.b = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fffff;
  bitsRead += 21;
  if (v > 2000000) {
    return false; // a smuggled offset is refused
  }
  value.c = -1000000 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7f;
  bitsRead += 7;
  value.bits7 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fff;
  bitsRead += 13;
  value.bits13 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7fffff;
  bitsRead += 23;
  value.bits23 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1;
  bitsRead += 1;
  value.flag = v != 0;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  value.x = _doubleFromFloat32Bits(v);
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  value.y = _doubleFromFloat32Bits(v);
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  value.z = _doubleFromFloat32Bits(v);
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  lo = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  lo |= v << 32;
  value.big = lo;
  {
    final pad = bitsRead & 7;
    if (pad != 0) {
      if (bitsRead + (8 - pad) > numBits) {
        return false;
      }
      if (view.getUint8(bitsRead >>> 3) >>> pad != 0) {
        return false; // nonzero padding is refused (SPEC §4.3)
      }
      bitsRead += 8 - pad;
    }
  }
  if (bitsRead + 136 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < 17; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xff;
    bitsRead += 8;
    value.blob[i0] = v;
  }
  return true;
}

// measureBenchPacket is the exact wire bits writeBenchPacket would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureBenchPacket(BenchPacket value) {
  var bits = 0;
  bits += 249;
  bits += (8 - (bits & 7)) & 7;
  bits += 136;
  return bits;
}

// type BenchInts
final class BenchInts {
  // wire [-100, 100]
  int f0 = 0;
  // wire [0, 65535]
  int f1 = 0;
  // wire [-1000000, 1000000]
  int f2 = 0;
  // wire [0, 3]
  int f3 = 0;
  // wire [-15, 15]
  int f4 = 0;
  // wire [0, 1000]
  int f5 = 0;
  // wire [-2048, 2047]
  int f6 = 0;
  // wire [0, 255]
  int f7 = 0;
  // wire [-600000, 600000]
  int f8 = 0;
  // wire [0, 100]
  int f9 = 0;
}

// benchIntsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// benchIntsMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int benchIntsMaxBits = 110;
const int benchIntsMaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroBenchInts(BenchInts value) {
  value.f0 = 0;
  value.f1 = 0;
  value.f2 = 0;
  value.f3 = 0;
  value.f4 = 0;
  value.f5 = 0;
  value.f6 = 0;
  value.f7 = 0;
  value.f8 = 0;
  value.f9 = 0;
}

// writeBenchInts packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// benchIntsMaxBytes. Returns the bytes written.
int writeBenchInts(BenchInts value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= benchIntsMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.f0 >= -100);
  assert(value.f0 <= 100);
  v = (value.f0 + 100) & 0xff;
  scratch |= v << scratchBits;
  scratchBits += 8;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (8 - scratchBits);
  }
  assert(value.f1 >= 0);
  assert(value.f1 <= 65535);
  v = (value.f1) & 0xffff;
  scratch |= v << scratchBits;
  scratchBits += 16;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (16 - scratchBits);
  }
  assert(value.f2 >= -1000000);
  assert(value.f2 <= 1000000);
  v = (value.f2 + 1000000) & 0x1fffff;
  scratch |= v << scratchBits;
  scratchBits += 21;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (21 - scratchBits);
  }
  assert(value.f3 >= 0);
  assert(value.f3 <= 3);
  v = (value.f3) & 0x3;
  scratch |= v << scratchBits;
  scratchBits += 2;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (2 - scratchBits);
  }
  assert(value.f4 >= -15);
  assert(value.f4 <= 15);
  v = (value.f4 + 15) & 0x1f;
  scratch |= v << scratchBits;
  scratchBits += 5;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (5 - scratchBits);
  }
  assert(value.f5 >= 0);
  assert(value.f5 <= 1000);
  v = (value.f5) & 0x3ff;
  scratch |= v << scratchBits;
  scratchBits += 10;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (10 - scratchBits);
  }
  assert(value.f6 >= -2048);
  assert(value.f6 <= 2047);
  v = (value.f6 + 2048) & 0xfff;
  scratch |= v << scratchBits;
  scratchBits += 12;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (12 - scratchBits);
  }
  assert(value.f7 >= 0);
  assert(value.f7 <= 255);
  v = (value.f7) & 0xff;
  scratch |= v << scratchBits;
  scratchBits += 8;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (8 - scratchBits);
  }
  assert(value.f8 >= -600000);
  assert(value.f8 <= 600000);
  v = (value.f8 + 600000) & 0x1fffff;
  scratch |= v << scratchBits;
  scratchBits += 21;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (21 - scratchBits);
  }
  assert(value.f9 >= 0);
  assert(value.f9 <= 100);
  v = (value.f9) & 0x7f;
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readBenchInts decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readBenchInts(BenchInts value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 110 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xff;
  bitsRead += 8;
  if (v > 200) {
    return false; // a smuggled offset is refused
  }
  value.f0 = -100 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffff;
  bitsRead += 16;
  value.f1 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fffff;
  bitsRead += 21;
  if (v > 2000000) {
    return false; // a smuggled offset is refused
  }
  value.f2 = -1000000 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  value.f3 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1f;
  bitsRead += 5;
  if (v > 30) {
    return false; // a smuggled offset is refused
  }
  value.f4 = -15 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3ff;
  bitsRead += 10;
  if (v > 1000) {
    return false; // a smuggled offset is refused
  }
  value.f5 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xfff;
  bitsRead += 12;
  value.f6 = -2048 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xff;
  bitsRead += 8;
  value.f7 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fffff;
  bitsRead += 21;
  if (v > 1200000) {
    return false; // a smuggled offset is refused
  }
  value.f8 = -600000 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7f;
  bitsRead += 7;
  if (v > 100) {
    return false; // a smuggled offset is refused
  }
  value.f9 = v;
  return true;
}

// measureBenchInts is the exact wire bits writeBenchInts would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureBenchInts(BenchInts value) => 110;

// type BenchBits
final class BenchBits {
  int b7 = 0;
  int b13 = 0;
  int b23 = 0;
  int b3 = 0;
  int b32 = 0;
  int b11 = 0;
  int b19 = 0;
  int b48 = 0;
}

// benchBitsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// benchBitsMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int benchBitsMaxBits = 156;
const int benchBitsMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroBenchBits(BenchBits value) {
  value.b7 = 0;
  value.b13 = 0;
  value.b23 = 0;
  value.b3 = 0;
  value.b32 = 0;
  value.b11 = 0;
  value.b19 = 0;
  value.b48 = 0;
}

// writeBenchBits packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// benchBitsMaxBytes. Returns the bytes written.
int writeBenchBits(BenchBits value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= benchBitsMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  v = value.b7 & 0x7f;
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  v = value.b13 & 0x1fff;
  scratch |= v << scratchBits;
  scratchBits += 13;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (13 - scratchBits);
  }
  v = value.b23 & 0x7fffff;
  scratch |= v << scratchBits;
  scratchBits += 23;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (23 - scratchBits);
  }
  v = value.b3 & 0x7;
  scratch |= v << scratchBits;
  scratchBits += 3;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (3 - scratchBits);
  }
  v = value.b32 & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = value.b11 & 0x7ff;
  scratch |= v << scratchBits;
  scratchBits += 11;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (11 - scratchBits);
  }
  v = value.b19 & 0x7ffff;
  scratch |= v << scratchBits;
  scratchBits += 19;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (19 - scratchBits);
  }
  v = value.b48 & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = (value.b48 >>> 32) & 0xffff;
  scratch |= v << scratchBits;
  scratchBits += 16;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (16 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readBenchBits decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readBenchBits(BenchBits value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 156 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7f;
  bitsRead += 7;
  value.b7 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fff;
  bitsRead += 13;
  value.b13 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7fffff;
  bitsRead += 23;
  value.b23 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  value.b3 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  value.b32 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7ff;
  bitsRead += 11;
  value.b11 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7ffff;
  bitsRead += 19;
  value.b19 = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  lo = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffff;
  bitsRead += 16;
  lo |= v << 32;
  value.b48 = lo;
  return true;
}

// measureBenchBits is the exact wire bits writeBenchBits would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureBenchBits(BenchBits value) => 156;

// type BenchMixed
final class BenchMixed {
  // wire [0, 65535]
  int sequence = 0;
  int ackBits = 0;
  int entityId = 0;
  // wire [-16384, 16383]
  int posX = 0;
  // wire [-16384, 16383]
  int posY = 0;
  // wire [-16384, 16383]
  int posZ = 0;
  int yaw = 0;
  bool moving = false;
  bool firing = false;
  int timestamp = 0;
  // wire [0, 15]
  int weapon = 0;
}

// benchMixedMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// benchMixedMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int benchMixedMaxBits = 168;
const int benchMixedMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroBenchMixed(BenchMixed value) {
  value.sequence = 0;
  value.ackBits = 0;
  value.entityId = 0;
  value.posX = 0;
  value.posY = 0;
  value.posZ = 0;
  value.yaw = 0;
  value.moving = false;
  value.firing = false;
  value.timestamp = 0;
  value.weapon = 0;
}

// writeBenchMixed packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// benchMixedMaxBytes. Returns the bytes written.
int writeBenchMixed(BenchMixed value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= benchMixedMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.sequence >= 0);
  assert(value.sequence <= 65535);
  v = (value.sequence) & 0xffff;
  scratch |= v << scratchBits;
  scratchBits += 16;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (16 - scratchBits);
  }
  v = value.ackBits & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = value.entityId & 0xfff;
  scratch |= v << scratchBits;
  scratchBits += 12;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (12 - scratchBits);
  }
  assert(value.posX >= -16384);
  assert(value.posX <= 16383);
  v = (value.posX + 16384) & 0x7fff;
  scratch |= v << scratchBits;
  scratchBits += 15;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (15 - scratchBits);
  }
  assert(value.posY >= -16384);
  assert(value.posY <= 16383);
  v = (value.posY + 16384) & 0x7fff;
  scratch |= v << scratchBits;
  scratchBits += 15;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (15 - scratchBits);
  }
  assert(value.posZ >= -16384);
  assert(value.posZ <= 16383);
  v = (value.posZ + 16384) & 0x7fff;
  scratch |= v << scratchBits;
  scratchBits += 15;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (15 - scratchBits);
  }
  v = value.yaw & 0x1ff;
  scratch |= v << scratchBits;
  scratchBits += 9;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (9 - scratchBits);
  }
  v = value.moving ? 1 : 0;
  scratch |= v << scratchBits;
  scratchBits += 1;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (1 - scratchBits);
  }
  v = value.firing ? 1 : 0;
  scratch |= v << scratchBits;
  scratchBits += 1;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (1 - scratchBits);
  }
  v = value.timestamp & 0xffffffff;
  scratch |= v << scratchBits;
  scratchBits += 32;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (32 - scratchBits);
  }
  v = (value.timestamp >>> 32) & 0xffff;
  scratch |= v << scratchBits;
  scratchBits += 16;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (16 - scratchBits);
  }
  assert(value.weapon >= 0);
  assert(value.weapon <= 15);
  v = (value.weapon) & 0xf;
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readBenchMixed decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readBenchMixed(BenchMixed value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 168 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffff;
  bitsRead += 16;
  value.sequence = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  value.ackBits = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xfff;
  bitsRead += 12;
  value.entityId = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7fff;
  bitsRead += 15;
  value.posX = -16384 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7fff;
  bitsRead += 15;
  value.posY = -16384 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7fff;
  bitsRead += 15;
  value.posZ = -16384 + v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1ff;
  bitsRead += 9;
  value.yaw = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1;
  bitsRead += 1;
  value.moving = v != 0;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1;
  bitsRead += 1;
  value.firing = v != 0;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffffffff;
  bitsRead += 32;
  lo = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xffff;
  bitsRead += 16;
  lo |= v << 32;
  value.timestamp = lo;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  value.weapon = v;
  return true;
}

// measureBenchMixed is the exact wire bits writeBenchMixed would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureBenchMixed(BenchMixed value) => 168;
