// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x682e2a15a56b78bf

using System.Runtime.CompilerServices;
using Serialize;

namespace Example
{

    // type RenderSprite
    public sealed class RenderSprite
    {
        public ulong SortKey;
        public uint MeshId;
        public uint MaterialId;
        public byte Layer;
        public Team Team;
    }

    // type RenderBlock
    public sealed class RenderBlock
    {
        public uint WorkerIndex;
        public uint SpriteCountHint;
        public RenderSprite[] Sprites = new RenderSprite[Schema.RenderBlockMaxSprites]; // used count beside it; wire count in [0, RenderBlockMaxSprites]
        public int SpritesCount;

        public RenderBlock()
        {
            for (int i = 0; i < Sprites.Length; i++)
            {
                Sprites[i] = new RenderSprite();
            }
        }
    }

    // Schema carries every generated function and constant of the unit — C# has
    // no namespace-level functions or constants, so the static class is their
    // home (SPEC §6.1 naming); partial, one slice per generated file.
    public static partial class Schema
    {
        public const long RenderBlockMaxSprites = 64;

        // RenderSpriteMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // RenderSpriteMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long RenderSpriteMaxBits = 138;
        public const long RenderSpriteMaxBytes = 24;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroRenderSprite(RenderSprite value)
        {
            value.SortKey = 0;
            value.MeshId = 0;
            value.MaterialId = 0;
            value.Layer = 0;
            value.Team = Team.None;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteRenderSprite(WriteStream stream, RenderSprite value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteRenderSpriteBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteRenderSpriteBatch(ref WriteBatch batch, RenderSprite value)
        {
            {
                // flat run: 138 bits in 3 chunk(s) — the field placement is folded
                if ((uint)value.Team > 2) // headroom above the wire range cannot ride
                {
                    return false;
                }
                ulong f0 = (ulong)value.SortKey;
                ulong f1 = ((ulong)(value.MeshId)) & 0xffffffffUL;
                ulong f2 = ((ulong)(value.MaterialId)) & 0xffffffffUL;
                ulong f3 = ((ulong)(value.Layer)) & 0xffUL;
                ulong f4 = ((ulong)(uint)value.Team) & 0x3UL;
                ulong w0 = f0;
                if (!batch.SerializeBits64(ref w0, 64))
                {
                    return false;
                }
                ulong w1 = f1 | (f2 << 32);
                if (!batch.SerializeBits64(ref w1, 64))
                {
                    return false;
                }
                uint w2 = (uint)(f3 | (f4 << 8));
                if (!batch.SerializeBits(ref w2, 10))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadRenderSprite(ReadStream stream, RenderSprite value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadRenderSpriteBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadRenderSpriteBatch(ref ReadBatch batch, RenderSprite value)
        {
            {
                // flat run: 136 bits in 3 chunk(s) — one bounds check per chunk,
                // one sticky-error test for the whole run
                ulong c0 = 0;
                batch.SerializeBits64(ref c0, 64);
                ulong c1 = 0;
                batch.SerializeBits64(ref c1, 64);
                ulong c2 = 0;
                batch.SerializeBits64(ref c2, 8);
                if (!batch.Ok)
                {
                    return false;
                }
                ulong v0 = c0;
                value.SortKey = v0;
                ulong v1 = c1 & 0xffffffffUL;
                value.MeshId = (uint)(uint)v1;
                ulong v2 = (c1 >> 32) & 0xffffffffUL;
                value.MaterialId = (uint)(uint)v2;
                ulong v3 = c2 & 0xffUL;
                value.Layer = (byte)(uint)v3;
            }
            {
                int enumValue = 0;
                if (!batch.SerializeInt(ref enumValue, 0, 2))
                {
                    return false;
                }
                value.Team = (Team)enumValue;
            }
            return true;
        }

        // RenderBlockMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // RenderBlockMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long RenderBlockMaxBits = 8903;
        public const long RenderBlockMaxBytes = 1120;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroRenderBlock(RenderBlock value)
        {
            value.WorkerIndex = 0;
            value.SpriteCountHint = 0;
            for (int i = 0; i < (int)RenderBlockMaxSprites; i++)
            {
                ZeroRenderSprite(value.Sprites[i]);
            }
            value.SpritesCount = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteRenderBlock(WriteStream stream, RenderBlock value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteRenderBlockBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteRenderBlockBatch(ref WriteBatch batch, RenderBlock value)
        {
            {
                // flat run: 64 bits in 1 chunk(s) — the field placement is folded
                ulong f0 = ((ulong)(value.WorkerIndex)) & 0xffffffffUL;
                ulong f1 = ((ulong)(value.SpriteCountHint)) & 0xffffffffUL;
                ulong w0 = f0 | (f1 << 32);
                if (!batch.SerializeBits64(ref w0, 64))
                {
                    return false;
                }
            }
            if (value.SpritesCount < 0 || value.SpritesCount > (int)RenderBlockMaxSprites) // the count guards the loop (§6.3); out-of-contract writes are refused
            {
                return false;
            }
            {
                uint offsetValue = (uint)(value.SpritesCount);
                if (!batch.SerializeBits(ref offsetValue, 7))
                {
                    return false;
                }
            }
            for (int i = 0; i < value.SpritesCount; i++)
            {
                if (!WriteRenderSpriteBatch(ref batch, value.Sprites[i]))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadRenderBlock(ReadStream stream, RenderBlock value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadRenderBlockBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadRenderBlockBatch(ref ReadBatch batch, RenderBlock value)
        {
            {
                // flat run: 64 bits in 1 chunk(s) — one bounds check per chunk,
                // one sticky-error test for the whole run
                ulong c0 = 0;
                batch.SerializeBits64(ref c0, 64);
                if (!batch.Ok)
                {
                    return false;
                }
                ulong v0 = c0 & 0xffffffffUL;
                value.WorkerIndex = (uint)(uint)v0;
                ulong v1 = (c0 >> 32) & 0xffffffffUL;
                value.SpriteCountHint = (uint)(uint)v1;
            }
            if (!batch.SerializeInt(ref value.SpritesCount, 0, (int)RenderBlockMaxSprites)) // the count guards the loop (§6.3)
            {
                return false;
            }
            for (int i = 0; i < value.SpritesCount; i++)
            {
                if (!ReadRenderSpriteBatch(ref batch, value.Sprites[i]))
                {
                    return false;
                }
            }
            return true;
        }
    }

}
