// Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xdd71ce8b50d78939

using System;
using System.Runtime.CompilerServices;
using Serialize;

namespace Example
{

    // type Vec2
    public sealed class Vec2
    {
        public double X;
        public double Y;
    }

    // type SpanF64
    public sealed class SpanF64
    {
        public double[] Values = new double[2];
    }

    // type SpanU64
    public sealed class SpanU64
    {
        public ulong[] Values = new ulong[2];
    }

    // type SpanI64
    public sealed class SpanI64
    {
        public long[] Values = new long[2];
    }

    // type SpanOne
    public sealed class SpanOne
    {
        public ulong[] Values = new ulong[1];
    }

    // type SpanChunk
    public sealed class SpanChunk
    {
        public ushort[] Values = new ushort[4];
    }

    // type SpanTail
    public sealed class SpanTail
    {
        public double[] Values = new double[2];
        public uint Tail;
    }

    // type SpanTwice
    public sealed class SpanTwice
    {
        public double[] A = new double[2];
        public double[] B = new double[2];
    }

    // type Trio
    public sealed class Trio
    {
        public uint A;
        public uint B;
        public uint C;
    }

    // type TrioSole
    public sealed class TrioSole
    {
        public Trio Inner = new Trio();
    }

    // type TrioFirst
    public sealed class TrioFirst
    {
        public Trio Inner = new Trio();
        public uint Trailer;
    }

    // type TrioStraddle
    public sealed class TrioStraddle
    {
        public ulong Pad0;
        public ulong Pad1;
        public ulong Pad2;
        public ulong Pad3;
        public ulong Pad4;
        public uint Pad5;
        public Trio Inner = new Trio();
    }

    // Schema carries every generated function and constant of the unit — C# has
    // no namespace-level functions or constants, so the static class is their
    // home (SPEC §6.1 naming); partial, one slice per generated file.
    public static partial class Schema
    {
        // Vec2MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // Vec2MaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long Vec2MaxBits = 128;
        public const long Vec2MaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroVec2(Vec2 value)
        {
            value.X = 0.0;
            value.Y = 0.0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteVec2(WriteStream stream, Vec2 value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteVec2Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteVec2Batch(ref WriteBatch batch, Vec2 value)
        {
            if (!batch.SerializeDouble(ref value.X))
            {
                return false;
            }
            if (!batch.SerializeDouble(ref value.Y))
            {
                return false;
            }
            return true;
        }

        public static bool ReadVec2(ReadStream stream, Vec2 value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadVec2Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadVec2Batch(ref ReadBatch batch, Vec2 value)
        {
            if (!batch.SerializeDouble(ref value.X))
            {
                return false;
            }
            if (!batch.SerializeDouble(ref value.Y))
            {
                return false;
            }
            return true;
        }

        // SpanF64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanF64MaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanF64MaxBits = 128;
        public const long SpanF64MaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanF64(SpanF64 value)
        {
            Array.Clear(value.Values, 0, 2);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanF64(WriteStream stream, SpanF64 value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanF64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanF64Batch(ref WriteBatch batch, SpanF64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.Values[i]))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadSpanF64(ReadStream stream, SpanF64 value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanF64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanF64Batch(ref ReadBatch batch, SpanF64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.Values[i]))
                {
                    return false;
                }
            }
            return true;
        }

        // SpanU64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanU64MaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanU64MaxBits = 128;
        public const long SpanU64MaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanU64(SpanU64 value)
        {
            Array.Clear(value.Values, 0, 2);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanU64(WriteStream stream, SpanU64 value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanU64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanU64Batch(ref WriteBatch batch, SpanU64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeBits64(ref value.Values[i], 64))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadSpanU64(ReadStream stream, SpanU64 value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanU64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanU64Batch(ref ReadBatch batch, SpanU64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeBits64(ref value.Values[i], 64))
                {
                    return false;
                }
            }
            return true;
        }

        // SpanI64MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanI64MaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanI64MaxBits = 128;
        public const long SpanI64MaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanI64(SpanI64 value)
        {
            Array.Clear(value.Values, 0, 2);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanI64(WriteStream stream, SpanI64 value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanI64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanI64Batch(ref WriteBatch batch, SpanI64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                {
                    ulong rawValue = (ulong)value.Values[i];
                    if (!batch.SerializeBits64(ref rawValue, 64))
                    {
                        return false;
                    }
                }
            }
            return true;
        }

        public static bool ReadSpanI64(ReadStream stream, SpanI64 value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanI64Batch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanI64Batch(ref ReadBatch batch, SpanI64 value)
        {
            for (int i = 0; i < 2; i++)
            {
                {
                    ulong rawValue = 0;
                    if (!batch.SerializeBits64(ref rawValue, 64))
                    {
                        return false;
                    }
                    value.Values[i] = (long)rawValue;
                }
            }
            return true;
        }

        // SpanOneMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanOneMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanOneMaxBits = 64;
        public const long SpanOneMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanOne(SpanOne value)
        {
            Array.Clear(value.Values, 0, 1);
        }

        public static bool WriteSpanOne(WriteStream stream, SpanOne value)
        {
            for (int i = 0; i < 1; i++)
            {
                if (!stream.SerializeBits64(ref value.Values[i], 64))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadSpanOne(ReadStream stream, SpanOne value)
        {
            for (int i = 0; i < 1; i++)
            {
                if (!stream.SerializeBits64(ref value.Values[i], 64))
                {
                    return false;
                }
            }
            return true;
        }

        // SpanChunkMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanChunkMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanChunkMaxBits = 64;
        public const long SpanChunkMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanChunk(SpanChunk value)
        {
            Array.Clear(value.Values, 0, 4);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanChunk(WriteStream stream, SpanChunk value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanChunkBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanChunkBatch(ref WriteBatch batch, SpanChunk value)
        {
            for (int i = 0; i < 4; i++)
            {
                {
                    uint rawValue = value.Values[i];
                    if (!batch.SerializeBits(ref rawValue, 16))
                    {
                        return false;
                    }
                }
            }
            return true;
        }

        public static bool ReadSpanChunk(ReadStream stream, SpanChunk value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanChunkBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanChunkBatch(ref ReadBatch batch, SpanChunk value)
        {
            for (int i = 0; i < 4; i++)
            {
                {
                    uint rawValue = 0;
                    if (!batch.SerializeBits(ref rawValue, 16))
                    {
                        return false;
                    }
                    value.Values[i] = (ushort)rawValue;
                }
            }
            return true;
        }

        // SpanTailMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanTailMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanTailMaxBits = 160;
        public const long SpanTailMaxBytes = 24;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanTail(SpanTail value)
        {
            Array.Clear(value.Values, 0, 2);
            value.Tail = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanTail(WriteStream stream, SpanTail value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanTailBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanTailBatch(ref WriteBatch batch, SpanTail value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.Values[i]))
                {
                    return false;
                }
            }
            if (!batch.SerializeBits(ref value.Tail, 32))
            {
                return false;
            }
            return true;
        }

        public static bool ReadSpanTail(ReadStream stream, SpanTail value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanTailBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanTailBatch(ref ReadBatch batch, SpanTail value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.Values[i]))
                {
                    return false;
                }
            }
            if (!batch.SerializeBits(ref value.Tail, 32))
            {
                return false;
            }
            return true;
        }

        // SpanTwiceMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // SpanTwiceMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long SpanTwiceMaxBits = 256;
        public const long SpanTwiceMaxBytes = 32;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroSpanTwice(SpanTwice value)
        {
            Array.Clear(value.A, 0, 2);
            Array.Clear(value.B, 0, 2);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteSpanTwice(WriteStream stream, SpanTwice value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteSpanTwiceBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteSpanTwiceBatch(ref WriteBatch batch, SpanTwice value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.A[i]))
                {
                    return false;
                }
            }
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.B[i]))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadSpanTwice(ReadStream stream, SpanTwice value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadSpanTwiceBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadSpanTwiceBatch(ref ReadBatch batch, SpanTwice value)
        {
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.A[i]))
                {
                    return false;
                }
            }
            for (int i = 0; i < 2; i++)
            {
                if (!batch.SerializeDouble(ref value.B[i]))
                {
                    return false;
                }
            }
            return true;
        }

        // TrioMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TrioMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TrioMaxBits = 64;
        public const long TrioMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTrio(Trio value)
        {
            value.A = 0;
            value.B = 0;
            value.C = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTrio(WriteStream stream, Trio value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTrioBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTrioBatch(ref WriteBatch batch, Trio value)
        {
            {
                // flat run: 64 bits in 1 chunk(s) — the field placement is folded
                ulong f0 = ((ulong)value.A) & 0xfffffUL;
                ulong f1 = ((ulong)value.B) & 0xfffffUL;
                ulong f2 = ((ulong)value.C) & 0xffffffUL;
                ulong w0 = f0 | (f1 << 20) | (f2 << 40);
                if (!batch.SerializeBits64(ref w0, 64))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadTrio(ReadStream stream, Trio value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTrioBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTrioBatch(ref ReadBatch batch, Trio value)
        {
            if (!batch.SerializeBits(ref value.A, 20))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.B, 20))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.C, 24))
            {
                return false;
            }
            return true;
        }

        // TrioSoleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TrioSoleMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TrioSoleMaxBits = 64;
        public const long TrioSoleMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTrioSole(TrioSole value)
        {
            ZeroTrio(value.Inner);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTrioSole(WriteStream stream, TrioSole value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTrioSoleBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTrioSoleBatch(ref WriteBatch batch, TrioSole value)
        {
            if (!WriteTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            return true;
        }

        public static bool ReadTrioSole(ReadStream stream, TrioSole value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTrioSoleBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTrioSoleBatch(ref ReadBatch batch, TrioSole value)
        {
            if (!ReadTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            return true;
        }

        // TrioFirstMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TrioFirstMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TrioFirstMaxBits = 80;
        public const long TrioFirstMaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTrioFirst(TrioFirst value)
        {
            ZeroTrio(value.Inner);
            value.Trailer = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTrioFirst(WriteStream stream, TrioFirst value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTrioFirstBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTrioFirstBatch(ref WriteBatch batch, TrioFirst value)
        {
            if (!WriteTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.Trailer, 16))
            {
                return false;
            }
            return true;
        }

        public static bool ReadTrioFirst(ReadStream stream, TrioFirst value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTrioFirstBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTrioFirstBatch(ref ReadBatch batch, TrioFirst value)
        {
            if (!ReadTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.Trailer, 16))
            {
                return false;
            }
            return true;
        }

        // TrioStraddleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // TrioStraddleMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long TrioStraddleMaxBits = 408;
        public const long TrioStraddleMaxBytes = 56;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroTrioStraddle(TrioStraddle value)
        {
            value.Pad0 = 0;
            value.Pad1 = 0;
            value.Pad2 = 0;
            value.Pad3 = 0;
            value.Pad4 = 0;
            value.Pad5 = 0;
            ZeroTrio(value.Inner);
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteTrioStraddle(WriteStream stream, TrioStraddle value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteTrioStraddleBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteTrioStraddleBatch(ref WriteBatch batch, TrioStraddle value)
        {
            if (!batch.SerializeBits64(ref value.Pad0, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad1, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad2, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad3, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad4, 64))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.Pad5, 24))
            {
                return false;
            }
            if (!WriteTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            return true;
        }

        public static bool ReadTrioStraddle(ReadStream stream, TrioStraddle value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadTrioStraddleBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadTrioStraddleBatch(ref ReadBatch batch, TrioStraddle value)
        {
            if (!batch.SerializeBits64(ref value.Pad0, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad1, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad2, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad3, 64))
            {
                return false;
            }
            if (!batch.SerializeBits64(ref value.Pad4, 64))
            {
                return false;
            }
            if (!batch.SerializeBits(ref value.Pad5, 24))
            {
                return false;
            }
            if (!ReadTrioBatch(ref batch, value.Inner))
            {
                return false;
            }
            return true;
        }
    }

}
