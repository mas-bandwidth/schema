// Generated by the schema compiler from Render.schema. DO NOT EDIT.
// package example — protocol id 0x311b2f6c84685619
// The TABLE wire (evolution-tolerant, notes/table-wire.md): plain byte
// code, no serialize dependency — Unity/IL2CPP-safe.

namespace Example
{

    // The table codecs live on the same partial Schema class as the rest of
    // the generated surface (SPEC §6.1 naming) — one slice per generated file.
    public static partial class Schema
    {
        // TableWriteRenderSprite returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteRenderSprite(RenderSprite value)
        {
            TableWriter w = new TableWriter();
            TableWriteRenderSprite(w, value);
            return w.ToArray();
        }

        // AppendTableRenderSprite appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteRenderSprite.
        public static void AppendTableRenderSprite(TableWriter w, RenderSprite value)
        {
            TableWriteRenderSprite(w, value);
        }

        private static void TableWriteRenderSprite(TableWriter w, RenderSprite value)
        {
            if (value.SortKey != 0)
            {
                w.U16(0x4fe9); // sort_key
                w.U8(9);
                w.U64((ulong)value.SortKey);
            }
            if (value.MeshId != 0)
            {
                w.U16(0xd021); // mesh_id
                w.U8(8);
                w.U32((uint)value.MeshId);
            }
            if (value.MaterialId != 0)
            {
                w.U16(0x78c0); // material_id
                w.U8(8);
                w.U32((uint)value.MaterialId);
            }
            if (value.Layer != 0)
            {
                w.U16(0x4750); // layer
                w.U8(6);
                w.U8((byte)value.Layer);
            }
            if (value.Team != Team.None)
            {
                w.U16(0xdff1); // team
                w.U8(6);
                w.U8((byte)value.Team);
            }
            w.U16(0); // terminator
        }

        // TableReadRenderSprite decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadRenderSprite(byte[] data, ref RenderSprite value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadRenderSprite(r, ref value);
        }

        private static bool TableReadRenderSprite(TableReader r, ref RenderSprite value)
        {
            value = new RenderSprite(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0x4fe9: // sort_key
                    {
                        if (kind != 9)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        ulong decoded = r.Get64();
                        value.SortKey = (ulong)decoded;
                        break;
                    }
                    case 0xd021: // mesh_id
                    {
                        if (kind != 8)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        uint decoded = r.Get32();
                        value.MeshId = (uint)decoded;
                        break;
                    }
                    case 0x78c0: // material_id
                    {
                        if (kind != 8)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        uint decoded = r.Get32();
                        value.MaterialId = (uint)decoded;
                        break;
                    }
                    case 0x4750: // layer
                    {
                        if (kind != 6)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(1))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        byte decoded = r.Get8();
                        value.Layer = (byte)decoded;
                        break;
                    }
                    case 0xdff1: // team
                    {
                        if (kind != 6)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(1))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        byte raw = r.Get8();
                        if (raw > 2)
                        {
                            raw = 0; // out-of-set -> None
                            r.Report.Clamped++;
                        }
                        value.Team = (Team)raw;
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // TableWriteRenderBlock returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteRenderBlock(RenderBlock value)
        {
            TableWriter w = new TableWriter();
            TableWriteRenderBlock(w, value);
            return w.ToArray();
        }

        // AppendTableRenderBlock appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteRenderBlock.
        public static void AppendTableRenderBlock(TableWriter w, RenderBlock value)
        {
            TableWriteRenderBlock(w, value);
        }

        private static void TableWriteRenderBlock(TableWriter w, RenderBlock value)
        {
            if (value.WorkerIndex != 0)
            {
                w.U16(0xc58e); // worker_index
                w.U8(8);
                w.U32((uint)value.WorkerIndex);
            }
            if (value.SpriteCountHint != 0)
            {
                w.U16(0xe26a); // sprite_count_hint
                w.U8(8);
                w.U32((uint)value.SpriteCountHint);
            }
            if (value.SpritesCount > 0)
            {
                w.U16(0xd0ec); // sprites
                w.U8(14);
                int lenAt = w.Len;
                w.U32(0);
                w.U8(13);
                w.U16((ushort)value.SpritesCount);
                for (int i = 0; i < value.SpritesCount; i++)
                {
                    int elemLenAt = w.Len;
                    w.U32(0);
                    TableWriteRenderSprite(w, value.Sprites[i]);
                    w.Patch32(elemLenAt, (uint)(w.Len - elemLenAt - 4));
                }
                w.Patch32(lenAt, (uint)(w.Len - lenAt - 4));
            }
            w.U16(0); // terminator
        }

        // TableReadRenderBlock decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadRenderBlock(byte[] data, ref RenderBlock value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadRenderBlock(r, ref value);
        }

        private static bool TableReadRenderBlock(TableReader r, ref RenderBlock value)
        {
            value = new RenderBlock(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xc58e: // worker_index
                    {
                        if (kind != 8)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        uint decoded = r.Get32();
                        value.WorkerIndex = (uint)decoded;
                        break;
                    }
                    case 0xe26a: // sprite_count_hint
                    {
                        if (kind != 8)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        uint decoded = r.Get32();
                        value.SpriteCountHint = (uint)decoded;
                        break;
                    }
                    case 0xd0ec: // sprites
                    {
                        if (kind != 14)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyLen = (int)r.Get32();
                        if (!r.Has(bodyLen))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyEnd = r.Off + bodyLen;
                        if (bodyLen >= 3)
                        {
                            byte elemKind = r.Get8();
                            int count = r.Get16();
                            if (elemKind != 13)
                            {
                                r.Report.KindMismatch++;
                                r.Off = bodyEnd;
                                break;
                            }
                            int keep = count;
                            if (keep > 64)
                            {
                                keep = 64;
                                r.Report.Clamped++;
                            }
                            for (int i = 0; i < keep; i++)
                            {
                                if (!r.Has(4))
                                {
                                    r.Report.Malformed = true;
                                    return false;
                                }
                                int elemLen = (int)r.Get32();
                                if (!r.Has(elemLen))
                                {
                                    r.Report.Malformed = true;
                                    return false;
                                }
                                TableReader sub = new TableReader(r.Buf, r.Off, r.Off + elemLen, r.Report);
                                TableReadRenderSprite(sub, ref value.Sprites[i]);
                                r.Off += elemLen;
                            }
                            value.SpritesCount = keep;
                        }
                        r.Off = bodyEnd; // excess elements and slack skip via the length
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // ---- reflection descriptors (tables only, notes/table-wire.md) ----
        //
        // One flat TableFieldInfo[] per closure type (SPEC §4.11: contiguous
        // structs, sequential walks, zero per-instance weight), built lazily on
        // first use: C# gives partial classes no cross-file static initializer
        // order, so an eager initializer could observe a not-yet-built nested
        // descriptor — the accessors compose in dependency order instead (the
        // closure is acyclic). A double-build under a race is benign: equivalent
        // data, last write wins.

        private static TableTypeInfo _tableTypeRenderSprite;

        // TableTypeRenderSprite returns RenderSprite's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetRenderSprite/TableSetRenderSprite to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeRenderSprite()
        {
            if (_tableTypeRenderSprite != null)
            {
                return _tableTypeRenderSprite;
            }
            TableFieldInfo[] fields = new TableFieldInfo[5];
            fields[0] = new TableFieldInfo { Name = "sort_key", TypeName = "uint64", Id = 0x4fe9, Kind = 9, EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "mesh_id", TypeName = "uint32", Id = 0xd021, Kind = 8, EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "material_id", TypeName = "uint32", Id = 0x78c0, Kind = 8, EnumMax = -1, Guard = "" };
            fields[3] = new TableFieldInfo { Name = "layer", TypeName = "uint8", Id = 0x4750, Kind = 6, EnumMax = -1, Guard = "" };
            fields[4] = new TableFieldInfo { Name = "team", TypeName = "Team", Id = 0xdff1, Kind = 6, EnumMax = 2, EnumName = EnumNameTeam, Guard = "" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "RenderSprite";
            info.Fields = fields;
            _tableTypeRenderSprite = info;
            return info;
        }

        // TableGetRenderSprite reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetRenderSprite(RenderSprite value, string field, out TableValue result)
        {
            switch (field)
            {
                case "sort_key":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.SortKey };
                    return true;
                }
                case "mesh_id":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.MeshId };
                    return true;
                }
                case "material_id":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.MaterialId };
                    return true;
                }
                case "layer":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.Layer };
                    return true;
                }
                case "team":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = (ulong)value.Team };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetRenderSprite writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetRenderSprite(RenderSprite value, string field, in TableValue v)
        {
            switch (field)
            {
                case "sort_key":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.SortKey = n;
                    return true;
                }
                case "mesh_id":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.MeshId = (uint)n;
                    return true;
                }
                case "material_id":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.MaterialId = (uint)n;
                    return true;
                }
                case "layer":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.Layer = (byte)n;
                    return true;
                }
                case "team":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    if (n > 2UL)
                    {
                        n = 0; // out-of-set -> None, as the read side does
                    }
                    value.Team = (Team)n;
                    return true;
                }
            }
            return false;
        }

        private static TableTypeInfo _tableTypeRenderBlock;

        // TableTypeRenderBlock returns RenderBlock's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetRenderBlock/TableSetRenderBlock to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeRenderBlock()
        {
            if (_tableTypeRenderBlock != null)
            {
                return _tableTypeRenderBlock;
            }
            TableFieldInfo[] fields = new TableFieldInfo[3];
            fields[0] = new TableFieldInfo { Name = "worker_index", TypeName = "uint32", Id = 0xc58e, Kind = 8, EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "sprite_count_hint", TypeName = "uint32", Id = 0xe26a, Kind = 8, EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "sprites", TypeName = "RenderSprite", Id = 0xd0ec, Kind = 13, IsArray = true, Counted = true, ArrayBound = 64, Table = TableTypeRenderSprite(), EnumMax = -1, Guard = "" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "RenderBlock";
            info.Fields = fields;
            _tableTypeRenderBlock = info;
            return info;
        }

        // TableGetRenderBlock reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetRenderBlock(RenderBlock value, string field, out TableValue result)
        {
            switch (field)
            {
                case "worker_index":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.WorkerIndex };
                    return true;
                }
                case "sprite_count_hint":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.SpriteCountHint };
                    return true;
                }
                case "sprites":
                {
                    result = new TableValue { Kind = TableValueKind.Array, Obj = value.Sprites, Count = value.SpritesCount };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetRenderBlock writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetRenderBlock(RenderBlock value, string field, in TableValue v)
        {
            switch (field)
            {
                case "worker_index":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.WorkerIndex = (uint)n;
                    return true;
                }
                case "sprite_count_hint":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.SpriteCountHint = (uint)n;
                    return true;
                }
            }
            return false;
        }
    }

}
