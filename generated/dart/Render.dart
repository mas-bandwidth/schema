// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x3d5823781128b414

import 'dart:typed_data';

import 'Enums.dart' show Team;

const int renderBlockMaxSprites = 64;

// type RenderSprite
final class RenderSprite {
  int sortKey = 0;
  int meshId = 0;
  int materialId = 0;
  int layer = 0;
  int team = Team.none;
}

// renderSpriteMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// renderSpriteMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int renderSpriteMaxBits = 138;
const int renderSpriteMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroRenderSprite(RenderSprite value) {
  value.sortKey = 0;
  value.meshId = 0;
  value.materialId = 0;
  value.layer = 0;
  value.team = 0;
}

// writeRenderSprite packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// renderSpriteMaxBytes. Returns the bytes written.
int writeRenderSprite(RenderSprite value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= renderSpriteMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  v = (value.sortKey);
  scratch |= v << scratchBits;
  scratchBits += 64;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (64 - scratchBits);
  }
  v = ((value.meshId) & 0xffffffff) | (((value.materialId) & 0xffffffff) << 32);
  scratch |= v << scratchBits;
  scratchBits += 64;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (64 - scratchBits);
  }
  assert(value.team >= 0);
  assert(value.team <= 2);
  v = ((value.layer) & 0xff) | (((value.team) & 0x3) << 8);
  scratch |= v << scratchBits;
  scratchBits += 10;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (10 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readRenderSprite decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readRenderSprite(RenderSprite value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 138 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  lo = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  lo |= v << 32;
  value.sortKey = lo;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  value.meshId = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  value.materialId = v;
  v = (window >>> 32) & 0xff;
  bitsRead += 8;
  value.layer = v;
  v = (window >>> 40) & 0x3;
  bitsRead += 2;
  if (v > 2) {
    return false; // headroom above the wire range is refused
  }
  value.team = v;
  return true;
}

// measureRenderSprite is the exact wire bits writeRenderSprite would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureRenderSprite(RenderSprite value) => 138;

// type RenderBlock
final class RenderBlock {
  int workerIndex = 0;
  int spriteCountHint = 0;
  final List<RenderSprite> sprites = List.generate(
    renderBlockMaxSprites,
    (_) => RenderSprite(),
  );
  int spritesCount = 0;
}

// renderBlockMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// renderBlockMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int renderBlockMaxBits = 8903;
const int renderBlockMaxBytes = 1120;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroRenderBlock(RenderBlock value) {
  value.workerIndex = 0;
  value.spriteCountHint = 0;
  for (var i0 = 0; i0 < 64; i0++) {
    zeroRenderSprite(value.sprites[i0]);
  }
  value.spritesCount = 0;
}

// writeRenderBlock packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// renderBlockMaxBytes. Returns the bytes written.
int writeRenderBlock(RenderBlock value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= renderBlockMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.spritesCount >= 0);
  assert(value.spritesCount <= 64);
  v =
      ((value.workerIndex) & 0xffffffff) |
      (((value.spriteCountHint) & 0xffffffff) << 32);
  scratch |= v << scratchBits;
  scratchBits += 64;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (64 - scratchBits);
  }
  v = ((value.spritesCount) & 0x7f);
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  for (var i0 = 0; i0 < value.spritesCount; i0++) {
    final e0 = value.sprites[i0];
    v = (e0.sortKey);
    scratch |= v << scratchBits;
    scratchBits += 64;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (64 - scratchBits);
    }
    v = ((e0.meshId) & 0xffffffff) | (((e0.materialId) & 0xffffffff) << 32);
    scratch |= v << scratchBits;
    scratchBits += 64;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (64 - scratchBits);
    }
    assert(e0.team >= 0);
    assert(e0.team <= 2);
    v = ((e0.layer) & 0xff) | (((e0.team) & 0x3) << 8);
    scratch |= v << scratchBits;
    scratchBits += 10;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (10 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readRenderBlock decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readRenderBlock(RenderBlock value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 64 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  value.workerIndex = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xffffffff;
  bitsRead += 32;
  value.spriteCountHint = v;
  if (bitsRead + 7 > numBits) {
    return false;
  }
  v = (window >>> 32) & 0x7f;
  bitsRead += 7;
  if (v > 64) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.spritesCount = v;
  if (bitsRead + value.spritesCount * 138 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.spritesCount; i0++) {
    final e0 = value.sprites[i0];
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
    } else {
      window = tailWord >>> (bitsRead - tailBase * 8);
    }
    v = window & 0xffffffff;
    bitsRead += 32;
    lo = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
    } else {
      window = tailWord >>> (bitsRead - tailBase * 8);
    }
    v = window & 0xffffffff;
    bitsRead += 32;
    lo |= v << 32;
    e0.sortKey = lo;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
    } else {
      window = tailWord >>> (bitsRead - tailBase * 8);
    }
    v = window & 0xffffffff;
    bitsRead += 32;
    e0.meshId = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
    } else {
      window = tailWord >>> (bitsRead - tailBase * 8);
    }
    v = window & 0xffffffff;
    bitsRead += 32;
    e0.materialId = v;
    v = (window >>> 32) & 0xff;
    bitsRead += 8;
    e0.layer = v;
    v = (window >>> 40) & 0x3;
    bitsRead += 2;
    if (v > 2) {
      return false; // headroom above the wire range is refused
    }
    e0.team = v;
  }
  return true;
}

// measureRenderBlock is the exact wire bits writeRenderBlock would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureRenderBlock(RenderBlock value) {
  var bits = 0;
  bits += 71;
  bits += value.spritesCount * 138;
  return bits;
}
