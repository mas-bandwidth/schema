// Code generated by the schema compiler from Enums.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

// Hex of a uint64 bit pattern held in a signed int (toRadixString would
// render the sign, not the pattern).
String _hex64(int value) => value < 0
    ? (value >>> 32).toRadixString(16) +
          (value & 0xffffffff).toRadixString(16).padLeft(8, '0')
    : value.toRadixString(16);

// Team — None = 0 implicit, variants dense from 1, wire range [0, 2] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class Team {
  static const int none = 0;
  static const int red = 1;
  static const int blue = 2;
  static const int count = 2; // the declared variant count (SPEC §4.2)
  static const int max = 2; // the exported extent (SPEC §4.2)
}

// enumNameTeam: debug/log/tooling name for any Team wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNameTeam(int value) {
  switch (value) {
    case Team.none:
      return 'None';
    case Team.red:
      return 'Red';
    case Team.blue:
      return 'Blue';
    default:
      return '???';
  }
}

// ShipType — None = 0 implicit, variants dense from 1, wire range [0, 5] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class ShipType {
  static const int none = 0;
  static const int fighter = 1;
  static const int corvette = 2;
  static const int bomber = 3;
  static const int destroyer = 4;
  static const int carrier = 5;
  static const int count = 5; // the declared variant count (SPEC §4.2)
  static const int max = 5; // the exported extent (SPEC §4.2)
}

// enumNameShipType: debug/log/tooling name for any ShipType wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNameShipType(int value) {
  switch (value) {
    case ShipType.none:
      return 'None';
    case ShipType.fighter:
      return 'Fighter';
    case ShipType.corvette:
      return 'Corvette';
    case ShipType.bomber:
      return 'Bomber';
    case ShipType.destroyer:
      return 'Destroyer';
    case ShipType.carrier:
      return 'Carrier';
    default:
      return '???';
  }
}

// MissileType — None = 0 implicit, variants dense from 1, wire range [0, 3] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class MissileType {
  static const int none = 0;
  static const int heatseeker = 1;
  static const int torpedo = 2;
  static const int nuke = 3;
  static const int count = 3; // the declared variant count (SPEC §4.2)
  static const int max = 3; // the exported extent (SPEC §4.2)
}

// enumNameMissileType: debug/log/tooling name for any MissileType wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNameMissileType(int value) {
  switch (value) {
    case MissileType.none:
      return 'None';
    case MissileType.heatseeker:
      return 'Heatseeker';
    case MissileType.torpedo:
      return 'Torpedo';
    case MissileType.nuke:
      return 'Nuke';
    default:
      return '???';
  }
}

// PropType — None = 0 implicit, variants dense from 1, wire range [0, 6] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class PropType {
  static const int none = 0;
  static const int asteroid = 1;
  static const int chunk = 2;
  static const int fragment = 3;
  static const int sphere = 4;
  static const int blackHole = 5;
  static const int dysonPanel = 6;
  static const int count = 6; // the declared variant count (SPEC §4.2)
  static const int max = 6; // the exported extent (SPEC §4.2)
}

// enumNamePropType: debug/log/tooling name for any PropType wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNamePropType(int value) {
  switch (value) {
    case PropType.none:
      return 'None';
    case PropType.asteroid:
      return 'Asteroid';
    case PropType.chunk:
      return 'Chunk';
    case PropType.fragment:
      return 'Fragment';
    case PropType.sphere:
      return 'Sphere';
    case PropType.blackHole:
      return 'BlackHole';
    case PropType.dysonPanel:
      return 'DysonPanel';
    default:
      return '???';
  }
}

// Pending — None = 0 implicit, variants dense from 1, wire range [0, 0] (SPEC §4.2);
// an int-constant namespace — the Dart translation of the family's integer-
// backed enums: storage must hold every wire-legal value, and | max = ...
// headroom values have no Dart enum member to be
abstract final class Pending {
  static const int none = 0;
  static const int count = 0; // the declared variant count (SPEC §4.2)
  static const int max = 0; // the exported extent (SPEC §4.2)
}

// enumNamePending: debug/log/tooling name for any Pending wire value —
// out-of-set values (wire-legal up to the declared max) name as '???'
String enumNamePending(int value) {
  switch (value) {
    case Pending.none:
      return 'None';
    default:
      return '???';
  }
}

// ShipFlags — one bit per variant, consumed as masks; flags-typed fields store
// uint64 in every target — a bit-transparent int here — wire 4 bits
// (SPEC §4.2). Mask names are the family's flat spelling, lowerCamel.
const int shipFlagsFiringLaser = 1 << 0;
const int shipFlagsBoosting = 1 << 1;
const int shipFlagsBraking = 1 << 2;
const int shipFlagsAiming = 1 << 3;
// the declared variant count (SPEC §4.2)
const int shipFlagsCount = 4;

// flagNameShipFlags: debug/log/tooling name for bit i of ShipFlags —
// out-of-range bits name as '???'
String flagNameShipFlags(int bit) {
  switch (bit) {
    case 0:
      return 'FiringLaser';
    case 1:
      return 'Boosting';
    case 2:
      return 'Braking';
    case 3:
      return 'Aiming';
    default:
      return '???';
  }
}

// flagNamesShipFlags renders the set bits of value as 'A|B' — '0' for the
// empty set, bits past the declared variants as hex
String flagNamesShipFlags(int value) {
  final names = <String>[];
  if (value & (1 << 0) != 0) {
    names.add('FiringLaser');
  }
  if (value & (1 << 1) != 0) {
    names.add('Boosting');
  }
  if (value & (1 << 2) != 0) {
    names.add('Braking');
  }
  if (value & (1 << 3) != 0) {
    names.add('Aiming');
  }
  if (value >>> 4 != 0) {
    names.add('0x${_hex64((value >>> 4) << 4)}');
  }
  return names.isEmpty ? '0' : names.join('|');
}
