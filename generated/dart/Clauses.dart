// Code generated by the schema compiler from Clauses.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xdd71ce8b50d78939

import 'dart:typed_data';

// type W13
final class W13 {
  // wire [0, 8191]
  final Uint16List items = Uint16List(12);
  int itemsCount = 0;
}

// w13MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w13MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w13MaxBits = 160;
const int w13MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW13(W13 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW13 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w13MaxBytes. Returns the bytes written.
int writeW13(W13 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w13MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 12);
  v = ((value.itemsCount) & 0xf);
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 8191);
    v = ((value.items[i0]) & 0x1fff);
    scratch |= v << scratchBits;
    scratchBits += 13;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (13 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW13 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW13(W13 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  if (v > 12) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 13 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x1fff;
    bitsRead += 13;
    value.items[i0] = v;
  }
  return true;
}

// measureW13 is the exact wire bits writeW13 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW13(W13 value) {
  var bits = 0;
  bits += 4;
  bits += value.itemsCount * 13;
  return bits;
}

// type W17
final class W17 {
  // wire [0, 131071]
  final Uint32List items = Uint32List(9);
  int itemsCount = 0;
}

// w17MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w17MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w17MaxBits = 157;
const int w17MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW17(W17 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW17 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w17MaxBytes. Returns the bytes written.
int writeW17(W17 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w17MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 9);
  v = ((value.itemsCount) & 0xf);
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 131071);
    v = ((value.items[i0]) & 0x1ffff);
    scratch |= v << scratchBits;
    scratchBits += 17;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (17 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW17 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW17(W17 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  if (v > 9) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 17 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x1ffff;
    bitsRead += 17;
    value.items[i0] = v;
  }
  return true;
}

// measureW17 is the exact wire bits writeW17 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW17(W17 value) {
  var bits = 0;
  bits += 4;
  bits += value.itemsCount * 17;
  return bits;
}

// type W26
final class W26 {
  // wire [0, 67108863]
  final Uint32List items = Uint32List(6);
  int itemsCount = 0;
}

// w26MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w26MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w26MaxBits = 159;
const int w26MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW26(W26 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW26 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w26MaxBytes. Returns the bytes written.
int writeW26(W26 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w26MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 6);
  v = ((value.itemsCount) & 0x7);
  scratch |= v << scratchBits;
  scratchBits += 3;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (3 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 67108863);
    v = ((value.items[i0]) & 0x3ffffff);
    scratch |= v << scratchBits;
    scratchBits += 26;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (26 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW26 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW26(W26 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  if (v > 6) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 26 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x3ffffff;
    bitsRead += 26;
    value.items[i0] = v;
  }
  return true;
}

// measureW26 is the exact wire bits writeW26 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW26(W26 value) {
  var bits = 0;
  bits += 3;
  bits += value.itemsCount * 26;
  return bits;
}

// type W1
final class W1 {
  // wire [0, 1]
  final Uint8List items = Uint8List(20);
  int itemsCount = 0;
}

// w1MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w1MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w1MaxBits = 25;
const int w1MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW1(W1 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW1 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w1MaxBytes. Returns the bytes written.
int writeW1(W1 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w1MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 20);
  v = ((value.itemsCount) & 0x1f);
  scratch |= v << scratchBits;
  scratchBits += 5;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (5 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 1);
    v = ((value.items[i0]) & 0x1);
    scratch |= v << scratchBits;
    scratchBits += 1;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (1 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW1 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW1(W1 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 5 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1f;
  bitsRead += 5;
  if (v > 20) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 1 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x1;
    bitsRead += 1;
    value.items[i0] = v;
  }
  return true;
}

// measureW1 is the exact wire bits writeW1 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW1(W1 value) {
  var bits = 0;
  bits += 5;
  bits += value.itemsCount * 1;
  return bits;
}

// type W52
final class W52 {
  // wire [0, 4503599627370495]
  final Uint64List items = Uint64List(3);
  int itemsCount = 0;
}

// w52MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w52MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w52MaxBits = 158;
const int w52MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW52(W52 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW52 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w52MaxBytes. Returns the bytes written.
int writeW52(W52 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w52MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 3);
  v = ((value.itemsCount) & 0x3);
  scratch |= v << scratchBits;
  scratchBits += 2;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (2 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 4503599627370495);
    v = ((value.items[i0]) & 0xfffffffffffff);
    scratch |= v << scratchBits;
    scratchBits += 52;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (52 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW52 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW52(W52 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 2 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 52 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xffffffff;
    bitsRead += 32;
    lo = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xfffff;
    bitsRead += 20;
    lo |= v << 32;
    value.items[i0] = lo;
  }
  return true;
}

// measureW52 is the exact wire bits writeW52 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW52(W52 value) {
  var bits = 0;
  bits += 2;
  bits += value.itemsCount * 52;
  return bits;
}

// type W50
final class W50 {
  // wire [0, 1125899906842623]
  final Uint64List items = Uint64List(3);
  int itemsCount = 0;
}

// w50MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// w50MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int w50MaxBits = 152;
const int w50MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroW50(W50 value) {
  value.items.fillRange(0, value.items.length, 0);
  value.itemsCount = 0;
}

// writeW50 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// w50MaxBytes. Returns the bytes written.
int writeW50(W50 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= w50MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 3);
  v = ((value.itemsCount) & 0x3);
  scratch |= v << scratchBits;
  scratchBits += 2;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (2 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 1125899906842623);
    v = ((value.items[i0]) & 0x3ffffffffffff);
    scratch |= v << scratchBits;
    scratchBits += 50;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (50 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readW50 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readW50(W50 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  var lo = 0;
  if (bitsRead + 2 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 50 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xffffffff;
    bitsRead += 32;
    lo = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x3ffff;
    bitsRead += 18;
    lo |= v << 32;
    value.items[i0] = lo;
  }
  return true;
}

// measureW50 is the exact wire bits writeW50 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureW50(W50 value) {
  var bits = 0;
  bits += 2;
  bits += value.itemsCount * 50;
  return bits;
}

// type F13
final class F13 {
  // wire [0, 8191]
  final Uint16List items = Uint16List(7);
}

// f13MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// f13MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int f13MaxBits = 91;
const int f13MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroF13(F13 value) {
  value.items.fillRange(0, value.items.length, 0);
}

// writeF13 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// f13MaxBytes. Returns the bytes written.
int writeF13(F13 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= f13MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  for (var i0 = 0; i0 < 7; i0++) {
    assert(value.items[i0] >= 0);
    assert(value.items[i0] <= 8191);
    v = ((value.items[i0]) & 0x1fff);
    scratch |= v << scratchBits;
    scratchBits += 13;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (13 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readF13 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readF13(F13 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 91 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < 7; i0++) {
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x1fff;
    bitsRead += 13;
    value.items[i0] = v;
  }
  return true;
}

// measureF13 is the exact wire bits writeF13 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureF13(F13 value) => 91;

// type Tri3
final class Tri3 {
  int a = 0;
  int b = 0;
}

// tri3MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// tri3MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int tri3MaxBits = 3;
const int tri3MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroTri3(Tri3 value) {
  value.a = 0;
  value.b = 0;
}

// writeTri3 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// tri3MaxBytes. Returns the bytes written.
int writeTri3(Tri3 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= tri3MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  v = ((value.a) & 0x1) | (((value.b) & 0x3) << 1);
  scratch |= v << scratchBits;
  scratchBits += 3;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (3 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readTri3 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readTri3(Tri3 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1;
  bitsRead += 1;
  value.a = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  value.b = v;
  return true;
}

// measureTri3 is the exact wire bits writeTri3 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureTri3(Tri3 value) => 3;

// type ArrTri3
final class ArrTri3 {
  final List<Tri3> items = List.generate(10, (_) => Tri3());
  int itemsCount = 0;
}

// arrTri3MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// arrTri3MaxBytes is rounded up to the 8-byte write-buffer granularity.
const int arrTri3MaxBits = 34;
const int arrTri3MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroArrTri3(ArrTri3 value) {
  for (var i0 = 0; i0 < 10; i0++) {
    zeroTri3(value.items[i0]);
  }
  value.itemsCount = 0;
}

// writeArrTri3 packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// arrTri3MaxBytes. Returns the bytes written.
int writeArrTri3(ArrTri3 value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= arrTri3MaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 10);
  v = ((value.itemsCount) & 0xf);
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    final e0 = value.items[i0];
    v = ((e0.a) & 0x1) | (((e0.b) & 0x3) << 1);
    scratch |= v << scratchBits;
    scratchBits += 3;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (3 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readArrTri3 decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readArrTri3(ArrTri3 value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  if (v > 10) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 3 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    final e0 = value.items[i0];
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x1;
    bitsRead += 1;
    e0.a = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x3;
    bitsRead += 2;
    e0.b = v;
  }
  return true;
}

// measureArrTri3 is the exact wire bits writeArrTri3 would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureArrTri3(ArrTri3 value) {
  var bits = 0;
  bits += 4;
  bits += value.itemsCount * 3;
  return bits;
}

// type Eleven
final class Eleven {
  int a = 0;
  int b = 0;
}

// elevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// elevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int elevenMaxBits = 11;
const int elevenMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroEleven(Eleven value) {
  value.a = 0;
  value.b = 0;
}

// writeEleven packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// elevenMaxBytes. Returns the bytes written.
int writeEleven(Eleven value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= elevenMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  v = ((value.a) & 0x7) | (((value.b) & 0xff) << 3);
  scratch |= v << scratchBits;
  scratchBits += 11;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (11 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readEleven decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readEleven(Eleven value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 11 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  value.a = v;
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xff;
  bitsRead += 8;
  value.b = v;
  return true;
}

// measureEleven is the exact wire bits writeEleven would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureEleven(Eleven value) => 11;

// type ArrEleven
final class ArrEleven {
  final List<Eleven> items = List.generate(9, (_) => Eleven());
}

// arrElevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// arrElevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int arrElevenMaxBits = 99;
const int arrElevenMaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroArrEleven(ArrEleven value) {
  for (var i0 = 0; i0 < 9; i0++) {
    zeroEleven(value.items[i0]);
  }
}

// writeArrEleven packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// arrElevenMaxBytes. Returns the bytes written.
int writeArrEleven(ArrEleven value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= arrElevenMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  for (var i0 = 0; i0 < 9; i0++) {
    final e0 = value.items[i0];
    v = ((e0.a) & 0x7) | (((e0.b) & 0xff) << 3);
    scratch |= v << scratchBits;
    scratchBits += 11;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (11 - scratchBits);
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readArrEleven decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readArrEleven(ArrEleven value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 99 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < 9; i0++) {
    final e0 = value.items[i0];
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x7;
    bitsRead += 3;
    e0.a = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xff;
    bitsRead += 8;
    e0.b = v;
  }
  return true;
}

// measureArrEleven is the exact wire bits writeArrEleven would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureArrEleven(ArrEleven value) => 99;

// type EmptyA
final class EmptyA {
  // empty body — presence is the payload (SPEC §4.6)
}

// emptyAMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// emptyAMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int emptyAMaxBits = 0;
const int emptyAMaxBytes = 0;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroEmptyA(EmptyA value) {
  // empty body — nothing to reset (SPEC §4.6)
}

// writeEmptyA packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// emptyAMaxBytes. Returns the bytes written.
int writeEmptyA(EmptyA value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= emptyAMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  // empty body — presence is the payload (SPEC §4.6)
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readEmptyA decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readEmptyA(EmptyA value, ByteData view, int numBits) {
  return true;
}

// measureEmptyA is the exact wire bits writeEmptyA would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureEmptyA(EmptyA value) => 0;

// type EmptyB
final class EmptyB {
  // empty body — presence is the payload (SPEC §4.6)
}

// emptyBMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// emptyBMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int emptyBMaxBits = 0;
const int emptyBMaxBytes = 0;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroEmptyB(EmptyB value) {
  // empty body — nothing to reset (SPEC §4.6)
}

// writeEmptyB packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// emptyBMaxBytes. Returns the bytes written.
int writeEmptyB(EmptyB value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= emptyBMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  // empty body — presence is the payload (SPEC §4.6)
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readEmptyB decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readEmptyB(EmptyB value, ByteData view, int numBits) {
  return true;
}

// measureEmptyB is the exact wire bits writeEmptyB would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureEmptyB(EmptyB value) => 0;

// EmptyUnionType: union EmptyUnion's tag — None = 0, then each variant in declared order (SPEC §4.8)
abstract final class EmptyUnionType {
  static const int none = 0;
  static const int a = 1;
  static const int b = 2;
  static const int max = 2; // the exported extent (SPEC §4.2)
}

// EmptyUnion — at most one of the arms; type says which. Construction is the empty
// union (None). A read zero-establishes exactly the selected arm before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
final class EmptyUnion {
  int type = EmptyUnionType.none;
  final EmptyA a = EmptyA();
  final EmptyB b = EmptyB();
}

// emptyUnionMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// emptyUnionMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int emptyUnionMaxBits = 2;
const int emptyUnionMaxBytes = 8;

// zeroEmptyUnion resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm re-zeroes at its next selection.
void zeroEmptyUnion(EmptyUnion value) {
  value.type = EmptyUnionType.none;
}

// writeEmptyUnion packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// emptyUnionMaxBytes. Returns the bytes written.
int writeEmptyUnion(EmptyUnion value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= emptyUnionMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.type >= 0);
  assert(value.type <= 2);
  v = ((value.type) & 0x3);
  scratch |= v << scratchBits;
  scratchBits += 2;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (2 - scratchBits);
  }
  switch (value.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readEmptyUnion decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readEmptyUnion(EmptyUnion value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 2 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  if (v > 2) {
    return false; // not a wire-legal tag (SPEC §4.8)
  }
  value.type = v;
  switch (value.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  return true;
}

// measureEmptyUnion is the exact wire bits writeEmptyUnion would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureEmptyUnion(EmptyUnion value) {
  var bits = 0;
  bits += 2;
  switch (value.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  return bits;
}

// type HoldsEmptyUnion
final class HoldsEmptyUnion {
  int lead = 0;
  final EmptyUnion u = EmptyUnion();
  int tail = 0;
}

// holdsEmptyUnionMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// holdsEmptyUnionMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int holdsEmptyUnionMaxBits = 14;
const int holdsEmptyUnionMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroHoldsEmptyUnion(HoldsEmptyUnion value) {
  value.lead = 0;
  value.u.type = 0;
  value.tail = 0;
}

// writeHoldsEmptyUnion packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// holdsEmptyUnionMaxBytes. Returns the bytes written.
int writeHoldsEmptyUnion(HoldsEmptyUnion value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= holdsEmptyUnionMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.u.type >= 0);
  assert(value.u.type <= 2);
  v = ((value.lead) & 0x1f) | (((value.u.type) & 0x3) << 5);
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  switch (value.u.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  v = ((value.tail) & 0x7f);
  scratch |= v << scratchBits;
  scratchBits += 7;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (7 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readHoldsEmptyUnion decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readHoldsEmptyUnion(HoldsEmptyUnion value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 5 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1f;
  bitsRead += 5;
  value.lead = v;
  if (bitsRead + 2 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x3;
  bitsRead += 2;
  if (v > 2) {
    return false; // not a wire-legal tag (SPEC §4.8)
  }
  value.u.type = v;
  switch (value.u.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  if (bitsRead + 7 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7f;
  bitsRead += 7;
  value.tail = v;
  return true;
}

// measureHoldsEmptyUnion is the exact wire bits writeHoldsEmptyUnion would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureHoldsEmptyUnion(HoldsEmptyUnion value) {
  var bits = 0;
  bits += 5;
  bits += 2;
  switch (value.u.type) {
    case 1:
      break; // empty arm — presence is the payload (SPEC §4.6)
    case 2:
      break; // empty arm — presence is the payload (SPEC §4.6)
  }
  bits += 7;
  return bits;
}

// type Strs
final class Strs {
  int lead = 0;
  // string(8): max length, used length beside it (SPEC §4.7)
  final Uint8List s = Uint8List(8);
  int sLength = 0;
  // bytes(8): fixed buffer, used length beside it (SPEC §4.7)
  final Uint8List b = Uint8List(8);
  int bLength = 0;
  int tail = 0;
}

// strsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// strsMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int strsMaxBits = 158;
const int strsMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroStrs(Strs value) {
  value.lead = 0;
  value.s.fillRange(0, value.s.length, 0);
  value.sLength = 0;
  value.b.fillRange(0, value.b.length, 0);
  value.bLength = 0;
  value.tail = 0;
}

// writeStrs packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// strsMaxBytes. Returns the bytes written.
int writeStrs(Strs value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= strsMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.sLength >= 0);
  assert(value.sLength <= 8);
  v = ((value.lead) & 0x1f) | (((value.sLength) & 0xf) << 5);
  scratch |= v << scratchBits;
  scratchBits += 9;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (9 - scratchBits);
  }
  {
    final pad = scratchBits & 7;
    if (pad != 0) {
      scratchBits += 8 - pad;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = 0;
      }
    }
  }
  {
    var i0 = 0;
    for (; i0 + 4 <= value.sLength; i0 += 4) {
      v =
          value.s[i0] |
          (value.s[i0 + 1] << 8) |
          (value.s[i0 + 2] << 16) |
          (value.s[i0 + 3] << 24);
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
    for (; i0 < value.sLength; i0++) {
      v = value.s[i0];
      scratch |= v << scratchBits;
      scratchBits += 8;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (8 - scratchBits);
      }
    }
  }
  assert(value.bLength >= 0);
  assert(value.bLength <= 8);
  v = ((value.bLength) & 0xf);
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  {
    final pad = scratchBits & 7;
    if (pad != 0) {
      scratchBits += 8 - pad;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = 0;
      }
    }
  }
  {
    var i0 = 0;
    for (; i0 + 4 <= value.bLength; i0 += 4) {
      v =
          value.b[i0] |
          (value.b[i0 + 1] << 8) |
          (value.b[i0 + 2] << 16) |
          (value.b[i0 + 3] << 24);
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
    for (; i0 < value.bLength; i0++) {
      v = value.b[i0];
      scratch |= v << scratchBits;
      scratchBits += 8;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (8 - scratchBits);
      }
    }
  }
  v = ((value.tail) & 0x7);
  scratch |= v << scratchBits;
  scratchBits += 3;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (3 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readStrs decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readStrs(Strs value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 5 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1f;
  bitsRead += 5;
  value.lead = v;
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  if (v > 8) {
    return false; // the length guards the slice — reject, never clamp
  }
  value.sLength = v;
  {
    final pad = bitsRead & 7;
    if (pad != 0) {
      if (bitsRead + (8 - pad) > numBits) {
        return false;
      }
      if (view.getUint8(bitsRead >>> 3) >>> pad != 0) {
        return false; // nonzero padding is refused (SPEC §4.3)
      }
      bitsRead += 8 - pad;
    }
  }
  if (bitsRead + value.sLength * 8 > numBits) {
    return false;
  }
  {
    final base = bitsRead >>> 3;
    for (var i0 = 0; i0 < value.sLength; i0++) {
      value.s[i0] = view.getUint8(base + i0);
    }
  }
  bitsRead += value.sLength * 8;
  for (var i0 = 0; i0 < value.sLength; i0++) {
    if (value.s[i0] == 0) {
      return false; // an interior null is content the read refuses (SPEC §4.7)
    }
  }
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0xf;
  bitsRead += 4;
  if (v > 8) {
    return false; // the length guards the slice — reject, never clamp
  }
  value.bLength = v;
  {
    final pad = bitsRead & 7;
    if (pad != 0) {
      if (bitsRead + (8 - pad) > numBits) {
        return false;
      }
      if (view.getUint8(bitsRead >>> 3) >>> pad != 0) {
        return false; // nonzero padding is refused (SPEC §4.3)
      }
      bitsRead += 8 - pad;
    }
  }
  if (bitsRead + value.bLength * 8 > numBits) {
    return false;
  }
  {
    final base = bitsRead >>> 3;
    for (var i0 = 0; i0 < value.bLength; i0++) {
      value.b[i0] = view.getUint8(base + i0);
    }
  }
  bitsRead += value.bLength * 8;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  value.tail = v;
  return true;
}

// measureStrs is the exact wire bits writeStrs would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureStrs(Strs value) {
  var bits = 0;
  bits += 9;
  bits += (8 - (bits & 7)) & 7;
  bits += value.sLength * 8;
  bits += 4;
  bits += (8 - (bits & 7)) & 7;
  bits += value.bLength * 8;
  bits += 3;
  return bits;
}

// type ArrNested
final class ArrNested {
  int lead = 0;
  final List<Eleven> items = List.generate(4, (_) => Eleven());
  int itemsCount = 0;
  int tail = 0;
}

// arrNestedMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// arrNestedMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int arrNestedMaxBits = 55;
const int arrNestedMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroArrNested(ArrNested value) {
  value.lead = 0;
  for (var i0 = 0; i0 < 4; i0++) {
    zeroEleven(value.items[i0]);
  }
  value.itemsCount = 0;
  value.tail = 0;
}

// writeArrNested packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// arrNestedMaxBytes. Returns the bytes written.
int writeArrNested(ArrNested value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= arrNestedMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.itemsCount >= 0);
  assert(value.itemsCount <= 4);
  v = ((value.lead) & 0x1f) | (((value.itemsCount) & 0x7) << 5);
  scratch |= v << scratchBits;
  scratchBits += 8;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (8 - scratchBits);
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    final e0 = value.items[i0];
    v = ((e0.a) & 0x7) | (((e0.b) & 0xff) << 3);
    scratch |= v << scratchBits;
    scratchBits += 11;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (11 - scratchBits);
    }
  }
  v = ((value.tail) & 0x7);
  scratch |= v << scratchBits;
  scratchBits += 3;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (3 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readArrNested decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readArrNested(ArrNested value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 5 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1f;
  bitsRead += 5;
  value.lead = v;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  if (v > 4) {
    return false; // the count guards the loop — reject, never clamp
  }
  value.itemsCount = v;
  if (bitsRead + value.itemsCount * 11 > numBits) {
    return false;
  }
  for (var i0 = 0; i0 < value.itemsCount; i0++) {
    final e0 = value.items[i0];
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0x7;
    bitsRead += 3;
    e0.a = v;
    if (bitsRead >>> 3 < tailBase) {
      window = view.getUint64(bitsRead >>> 3, Endian.little);
      shift = bitsRead & 7;
    } else {
      window = tailWord;
      shift = bitsRead - tailBase * 8;
    }
    v = (window >>> shift) & 0xff;
    bitsRead += 8;
    e0.b = v;
  }
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x7;
  bitsRead += 3;
  value.tail = v;
  return true;
}

// measureArrNested is the exact wire bits writeArrNested would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureArrNested(ArrNested value) {
  var bits = 0;
  bits += 8;
  bits += value.itemsCount * 11;
  bits += 3;
  return bits;
}

// type Sole
final class Sole {
  int only = 0;
}

// soleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// soleMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int soleMaxBits = 13;
const int soleMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroSole(Sole value) {
  value.only = 0;
}

// writeSole packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// soleMaxBytes. Returns the bytes written.
int writeSole(Sole value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= soleMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  v = ((value.only) & 0x1fff);
  scratch |= v << scratchBits;
  scratchBits += 13;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (13 - scratchBits);
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readSole decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readSole(Sole value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var shift = 0;
  var v = 0;
  if (bitsRead + 13 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little);
    shift = bitsRead & 7;
  } else {
    window = tailWord;
    shift = bitsRead - tailBase * 8;
  }
  v = (window >>> shift) & 0x1fff;
  bitsRead += 13;
  value.only = v;
  return true;
}

// measureSole is the exact wire bits writeSole would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureSole(Sole value) => 13;
