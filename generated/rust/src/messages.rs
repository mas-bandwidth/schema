// Code generated by the schema compiler from Messages.schema. DO NOT EDIT.
// package example — protocol id 0x40230069cc791fab

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

// MessageType: the message set, extracted by the compiler — None = 0, then each message sorted by name (SPEC §4.8)
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct MessageType(pub u8);

impl MessageType {
    pub const NONE: MessageType = MessageType(0);
    pub const BLOCK: MessageType = MessageType(1);
    pub const CHAT: MessageType = MessageType(2);
    pub const HEARTBEAT: MessageType = MessageType(3);
    pub const SYNCHRONIZE: MessageType = MessageType(4);
    pub const TEST: MessageType = MessageType(5);
    pub const TIMESCALE: MessageType = MessageType(6);
    pub const MAX: MessageType = MessageType(6); // the exported extent (SPEC §4.2)
}

// message Heartbeat
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Heartbeat {}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Heartbeat {
    fn default() -> Self {
        Heartbeat {}
    }
}

// HEARTBEAT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// HEARTBEAT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const HEARTBEAT_MAX_BITS: u64 = 0;
pub const HEARTBEAT_MAX_BYTES: usize = 0;

#[inline(always)]
pub fn write_heartbeat(stream: &mut WriteStream<'_>, value: &Heartbeat) -> Result {
    let _ = (stream, value); // empty body — presence is the payload (SPEC §4.6)
    Ok(())
}

#[inline]
pub fn read_heartbeat(stream: &mut ReadStream<'_>, value: &mut Heartbeat) -> Result {
    let _ = (stream, value);
    Ok(())
}

// message Test
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Test {
    pub test_a: u16,
    pub test_b: i16, // wire [0, 1000]
    pub test_c: i16, // wire [0, 1000]
    pub test_d: i16, // wire [0, 1000]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Test {
    fn default() -> Self {
        Test {
            test_a: 0,
            test_b: 0,
            test_c: 0,
            test_d: 0,
        }
    }
}

// TEST_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TEST_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TEST_MAX_BITS: u64 = 46;
pub const TEST_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_test(stream: &mut WriteStream<'_>, value: &Test) -> Result {
    {
        let mut raw_value = value.test_a as u32;
        stream.serialize_bits(&mut raw_value, 16)?;
    }
    if value.test_b < 0 || value.test_b > 1000 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.test_b as u32;
        stream.serialize_bits(&mut offset_value, 10)?;
    }
    if value.test_c < 0 || value.test_c > 1000 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.test_c as u32;
        stream.serialize_bits(&mut offset_value, 10)?;
    }
    if value.test_d < 0 || value.test_d > 1000 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.test_d as u32;
        stream.serialize_bits(&mut offset_value, 10)?;
    }
    Ok(())
}

#[inline]
pub fn read_test(stream: &mut ReadStream<'_>, value: &mut Test) -> Result {
    {
        let mut raw_value: u32 = 0;
        stream.serialize_bits(&mut raw_value, 16)?;
        value.test_a = raw_value as u16;
    }
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, 1000)?;
        value.test_b = range_value as i16;
    }
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, 1000)?;
        value.test_c = range_value as i16;
    }
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, 1000)?;
        value.test_d = range_value as i16;
    }
    Ok(())
}

// message Block
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Block {
    pub data: [u8; MAX_BLOCK_SIZE as usize], // bytes(MaxBlockSize): fixed buffer, used length beside it (SPEC §4.7)
    pub data_length: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Block {
    fn default() -> Self {
        Block {
            data: [0; MAX_BLOCK_SIZE as usize],
            data_length: 0,
        }
    }
}

// BLOCK_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// BLOCK_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const BLOCK_MAX_BITS: u64 = 16018;
pub const BLOCK_MAX_BYTES: usize = 2008;

#[inline(always)]
pub fn write_block(stream: &mut WriteStream<'_>, value: &Block) -> Result {
    if value.data_length < 0 || value.data_length > 2000 { // refused, not wrapped or panicked: guards the slice too
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.data_length as u32;
        stream.serialize_bits(&mut offset_value, 11)?; // the length guards the slice (§6.3)
    }
    stream.write_bytes(&value.data[..value.data_length as usize]); // borrowed in place: the write side never mutates (infallible: returns () in serialize.rs 2.0.0)
    Ok(())
}

#[inline]
pub fn read_block(stream: &mut ReadStream<'_>, value: &mut Block) -> Result {
    stream.serialize_int(&mut value.data_length, 0, MAX_BLOCK_SIZE as i32)?; // the length guards the slice (§6.3)
    stream.serialize_bytes(&mut value.data[..value.data_length as usize])?;
    Ok(())
}

// message Chat
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Chat {
    pub text: [u8; MAX_CHAT_LENGTH as usize], // string(MaxChatLength): max length, used length beside it (SPEC §4.7)
    pub text_length: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Chat {
    fn default() -> Self {
        Chat {
            text: [0; MAX_CHAT_LENGTH as usize],
            text_length: 0,
        }
    }
}

// CHAT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// CHAT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const CHAT_MAX_BITS: u64 = 2064;
pub const CHAT_MAX_BYTES: usize = 264;

#[inline(always)]
pub fn write_chat(stream: &mut WriteStream<'_>, value: &Chat) -> Result {
    if value.text_length < 0 || value.text_length > 256 { // refused, not wrapped or panicked: guards the slice too
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    debug_assert!(
        std::str::from_utf8(&value.text[..value.text_length as usize]).is_ok(),
        "string(N) payloads are well-formed UTF-8 by contract (SPEC 4.7)"
    );
    {
        let mut offset_value = value.text_length as u32;
        stream.serialize_bits(&mut offset_value, 9)?; // the length guards the slice (§6.3)
    }
    stream.write_bytes(&value.text[..value.text_length as usize]); // borrowed in place: the write side never mutates (infallible: returns () in serialize.rs 2.0.0)
    Ok(())
}

#[inline]
pub fn read_chat(stream: &mut ReadStream<'_>, value: &mut Chat) -> Result {
    stream.serialize_int(&mut value.text_length, 0, MAX_CHAT_LENGTH as i32)?; // the length guards the slice (§6.3)
    stream.serialize_bytes(&mut value.text[..value.text_length as usize])?;
    for i in 0..value.text_length as usize {
        if value.text[i] == 0 {
            return Err(Error::Validation);
        }
    }
    Ok(())
}

// message Synchronize
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Synchronize {
    pub sync_frame: u64,
    pub sync_sequence: u16,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Synchronize {
    fn default() -> Self {
        Synchronize {
            sync_frame: 0,
            sync_sequence: 0,
        }
    }
}

// SYNCHRONIZE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SYNCHRONIZE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SYNCHRONIZE_MAX_BITS: u64 = 80;
pub const SYNCHRONIZE_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_synchronize(stream: &mut WriteStream<'_>, value: &Synchronize) -> Result {
    {
        let mut raw_value = value.sync_frame;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    {
        let mut raw_value = value.sync_sequence as u32;
        stream.serialize_bits(&mut raw_value, 16)?;
    }
    Ok(())
}

#[inline]
pub fn read_synchronize(stream: &mut ReadStream<'_>, value: &mut Synchronize) -> Result {
    stream.serialize_bits64(&mut value.sync_frame, 64)?;
    {
        let mut raw_value: u32 = 0;
        stream.serialize_bits(&mut raw_value, 16)?;
        value.sync_sequence = raw_value as u16;
    }
    Ok(())
}

// message Timescale
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Timescale {
    pub scale: f64,
    pub frame_a: u32,
    pub frame_b: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Timescale {
    fn default() -> Self {
        Timescale {
            scale: 0.0,
            frame_a: 0,
            frame_b: 0,
        }
    }
}

// TIMESCALE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TIMESCALE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TIMESCALE_MAX_BITS: u64 = 128;
pub const TIMESCALE_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_timescale(stream: &mut WriteStream<'_>, value: &Timescale) -> Result {
    {
        let mut float_value = value.scale;
        stream.serialize_f64(&mut float_value)?;
    }
    {
        let mut raw_value = value.frame_a;
        stream.serialize_bits(&mut raw_value, 32)?;
    }
    {
        let mut raw_value = value.frame_b;
        stream.serialize_bits(&mut raw_value, 32)?;
    }
    Ok(())
}

#[inline]
pub fn read_timescale(stream: &mut ReadStream<'_>, value: &mut Timescale) -> Result {
    stream.serialize_f64(&mut value.scale)?;
    stream.serialize_bits(&mut value.frame_a, 32)?;
    stream.serialize_bits(&mut value.frame_b, 32)?;
    Ok(())
}

// The message tag wire: MessageType in [0, 6], minimal bits; None = 0 is a
// valid wire value meaning *no message* — the stream terminator (SPEC §4.8).
#[inline(always)]
pub fn write_message_type(stream: &mut WriteStream<'_>, value: MessageType) -> Result {
    debug_assert!(value.0 as u32 <= 6); // the runtime ranged form's write assert, kept (debug parity)
    {
        let mut offset_value = value.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_message_type(stream: &mut ReadStream<'_>, value: &mut MessageType) -> Result {
    let mut tag_value: i32 = 0;
    stream.serialize_int(&mut tag_value, 0, 6)?;
    *value = MessageType(tag_value as u8);
    Ok(())
}

// The message-level bound: the tag plus the largest message (SPEC §6.1);
// MESSAGE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const MESSAGE_MAX_BITS: u64 = 16021;
pub const MESSAGE_MAX_BYTES: usize = 2008;

// Message is the dispatch surface: the tagged union over the message set —
// a Rust enum IS that union (stack storage, size = the largest message plus
// the tag; nothing heap-allocates per message, SPEC §6.1). Message::None is
// the stream terminator (SPEC §4.8).
#[derive(Clone, Copy, PartialEq, Debug)]
#[allow(clippy::large_enum_variant)]
pub enum Message {
    None,
    Block(Block),
    Chat(Chat),
    Heartbeat(Heartbeat),
    Synchronize(Synchronize),
    Test(Test),
    Timescale(Timescale),
}

// write_message is deliberately OUTSIDE the write-spine inlining demand:
// plain #[inline], not #[inline(always)]. Its callees (every per-message
// write_*) flatten into its body, but the dispatch match itself stays a
// call boundary — demanding the C++ twin into a batch build loop was
// measured to slow that loop ~21% while every per-message row kept its
// win with the boundary in place (schema #60). The compiler remains free
// to inline it where that pays.
#[inline]
pub fn write_message(stream: &mut WriteStream<'_>, message: &Message) -> Result {
    match message {
        Message::None => write_message_type(stream, MessageType::NONE), // the stream terminator (SPEC §4.8)
        Message::Block(value) => {
            write_message_type(stream, MessageType::BLOCK)?;
            write_block(stream, value)
        }
        Message::Chat(value) => {
            write_message_type(stream, MessageType::CHAT)?;
            write_chat(stream, value)
        }
        Message::Heartbeat(value) => {
            write_message_type(stream, MessageType::HEARTBEAT)?;
            write_heartbeat(stream, value)
        }
        Message::Synchronize(value) => {
            write_message_type(stream, MessageType::SYNCHRONIZE)?;
            write_synchronize(stream, value)
        }
        Message::Test(value) => {
            write_message_type(stream, MessageType::TEST)?;
            write_test(stream, value)
        }
        Message::Timescale(value) => {
            write_message_type(stream, MessageType::TIMESCALE)?;
            write_timescale(stream, value)
        }
    }
}

// read_message_into decodes the next message into caller-owned storage: the
// variant is reset to its zero form (SPEC §5), then its fields decode in
// place — no per-message copy of the union out of the call. On error the
// storage holds the partially decoded zero form: discard it.
//
// Choosing a read surface: read_message for one-shot by-value reads;
// read_message_into for reuse loops — hoist ONE Message and read every
// message into it (the Go/C# MessageStorage discipline). Reuse removes the
// per-message copy-out of the union and measured 2.6x on the steady-state
// batch read (M2, 2026-08-06). The two surfaces stay separate on purpose —
// see the note on read_message.
#[inline]
pub fn read_message_into(stream: &mut ReadStream<'_>, message: &mut Message) -> Result {
    let mut tag_value = MessageType::NONE;
    read_message_type(stream, &mut tag_value)?;
    match tag_value {
        MessageType::NONE => {
            *message = Message::None; // the stream terminator (SPEC §4.8)
            Ok(())
        }
        MessageType::BLOCK => {
            *message = Message::Block(Block::default()); // reused storage starts from the zero form, as the union does
            let Message::Block(value) = message else {
                unreachable!()
            };
            read_block(stream, value)
        }
        MessageType::CHAT => {
            *message = Message::Chat(Chat::default()); // reused storage starts from the zero form, as the union does
            let Message::Chat(value) = message else {
                unreachable!()
            };
            read_chat(stream, value)
        }
        MessageType::HEARTBEAT => {
            *message = Message::Heartbeat(Heartbeat::default()); // reused storage starts from the zero form, as the union does
            let Message::Heartbeat(value) = message else {
                unreachable!()
            };
            read_heartbeat(stream, value)
        }
        MessageType::SYNCHRONIZE => {
            *message = Message::Synchronize(Synchronize::default()); // reused storage starts from the zero form, as the union does
            let Message::Synchronize(value) = message else {
                unreachable!()
            };
            read_synchronize(stream, value)
        }
        MessageType::TEST => {
            *message = Message::Test(Test::default()); // reused storage starts from the zero form, as the union does
            let Message::Test(value) = message else {
                unreachable!()
            };
            read_test(stream, value)
        }
        MessageType::TIMESCALE => {
            *message = Message::Timescale(Timescale::default()); // reused storage starts from the zero form, as the union does
            let Message::Timescale(value) = message else {
                unreachable!()
            };
            read_timescale(stream, value)
        }
        // read_message_type already rejected tags past the set; the match
        // still needs the arm because tag constants are never exhaustive
        _ => Err(Error::Validation),
    }
}

// read_message decodes the next message by value — the one-shot surface.
// It deliberately does NOT delegate to read_message_into: routing the
// return through a &mut out-param defeated LLVM's in-place construction
// of the returned union and cost the batch read 23% (measured M2,
// 2026-08-06). Both surfaces stay; reuse loops call read_message_into.
#[inline]
pub fn read_message(stream: &mut ReadStream<'_>) -> Result<Message> {
    let mut tag_value = MessageType::NONE;
    read_message_type(stream, &mut tag_value)?;
    match tag_value {
        MessageType::NONE => Ok(Message::None), // the stream terminator (SPEC §4.8)
        MessageType::BLOCK => {
            let mut value = Block::default();
            read_block(stream, &mut value)?;
            Ok(Message::Block(value))
        }
        MessageType::CHAT => {
            let mut value = Chat::default();
            read_chat(stream, &mut value)?;
            Ok(Message::Chat(value))
        }
        MessageType::HEARTBEAT => {
            let mut value = Heartbeat::default();
            read_heartbeat(stream, &mut value)?;
            Ok(Message::Heartbeat(value))
        }
        MessageType::SYNCHRONIZE => {
            let mut value = Synchronize::default();
            read_synchronize(stream, &mut value)?;
            Ok(Message::Synchronize(value))
        }
        MessageType::TEST => {
            let mut value = Test::default();
            read_test(stream, &mut value)?;
            Ok(Message::Test(value))
        }
        MessageType::TIMESCALE => {
            let mut value = Timescale::default();
            read_timescale(stream, &mut value)?;
            Ok(Message::Timescale(value))
        }
        // read_message_type already rejected tags past the set; the match
        // still needs the arm because tag constants are never exhaustive
        _ => Err(Error::Validation),
    }
}

