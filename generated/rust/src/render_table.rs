// Generated by the schema compiler from Render.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x40230069cc791fab
// The TABLE wire (evolution-tolerant, notes/table-wire.md).

#![allow(clippy::needless_range_loop)]

use crate::*;

/// Returns `value`'s table-wire encoding in a fresh buffer.
pub fn table_write_render_sprite(value: &RenderSprite) -> Vec<u8> {
    append_table_render_sprite(Vec::new(), value)
}

/// Appends `value`'s table-wire encoding to `dst` and returns the extended
/// buffer — the zero-allocation write path: hand it a buffer you reuse
/// (`v.clear()` then pass `v`) and steady-state writes never touch the heap.
pub fn append_table_render_sprite(dst: Vec<u8>, value: &RenderSprite) -> Vec<u8> {
    let mut w = TableWriter { buf: dst };
    table_write_render_sprite_into(&mut w, value);
    w.buf
}

fn table_write_render_sprite_into(w: &mut TableWriter, value: &RenderSprite) {
    if value.sort_key != 0 {
        w.u16(0x4fe9); // sort_key
        w.u8(9);
        w.u64(value.sort_key as u64);
    }
    if value.mesh_id != 0 {
        w.u16(0xd021); // mesh_id
        w.u8(8);
        w.u32(value.mesh_id as u32);
    }
    if value.material_id != 0 {
        w.u16(0x78c0); // material_id
        w.u8(8);
        w.u32(value.material_id as u32);
    }
    if value.layer != 0 {
        w.u16(0x4750); // layer
        w.u8(6);
        w.u8(value.layer as u8);
    }
    if value.team != Team::NONE {
        w.u16(0xdff1); // team
        w.u8(6);
        w.u8(value.team.0 as u8);
    }
    w.u16(0); // terminator
}

/// Decodes a table-wire buffer under the permissive contract: declared
/// defaults prefill, known fields overlay, unknown ids and kind mismatches
/// skip and count, out-of-range values clamp and count. `false` means
/// malformed — the partial decode up to that point is kept.
pub fn table_read_render_sprite(data: &[u8], value: &mut RenderSprite, report: &mut TableReport) -> bool {
    let mut r = TableReader { buf: data, off: 0 };
    table_read_render_sprite_from(&mut r, value, report)
}

fn table_read_render_sprite_from(r: &mut TableReader, value: &mut RenderSprite, report: &mut TableReport) -> bool {
    *value = RenderSprite::default(); // prefill (all-zero defaults), then overlay
    loop {
        if !r.has(2) { report.malformed = true; return false; }
        let field_id = r.get16();
        if field_id == 0 { return true; }
        if !r.has(1) { report.malformed = true; return false; }
        let kind = r.get8();
        match field_id {
            0x4fe9 => { // sort_key
                if kind != 9 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = r.get64();
                    value.sort_key = raw as u64;
                }
            }
            0xd021 => { // mesh_id
                if kind != 8 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let raw = r.get32();
                    value.mesh_id = raw as u32;
                }
            }
            0x78c0 => { // material_id
                if kind != 8 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let raw = r.get32();
                    value.material_id = raw as u32;
                }
            }
            0x4750 => { // layer
                if kind != 6 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(1) { report.malformed = true; return false; }
                    let raw = r.get8();
                    value.layer = raw as u8;
                }
            }
            0xdff1 => { // team
                if kind != 6 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(1) { report.malformed = true; return false; }
                    let raw = r.get8();
                    let mut v = raw;
                    if v as u64 > 2 { v = 0; report.clamped += 1; }
                    value.team = Team(v as u8);
                }
            }
            _ => {
                report.unknown += 1;
                if !r.skip(kind) { report.malformed = true; return false; }
            }
        }
    }
}

/// Returns `value`'s table-wire encoding in a fresh buffer.
pub fn table_write_render_block(value: &RenderBlock) -> Vec<u8> {
    append_table_render_block(Vec::new(), value)
}

/// Appends `value`'s table-wire encoding to `dst` and returns the extended
/// buffer — the zero-allocation write path: hand it a buffer you reuse
/// (`v.clear()` then pass `v`) and steady-state writes never touch the heap.
pub fn append_table_render_block(dst: Vec<u8>, value: &RenderBlock) -> Vec<u8> {
    let mut w = TableWriter { buf: dst };
    table_write_render_block_into(&mut w, value);
    w.buf
}

fn table_write_render_block_into(w: &mut TableWriter, value: &RenderBlock) {
    if value.worker_index != 0 {
        w.u16(0xc58e); // worker_index
        w.u8(8);
        w.u32(value.worker_index as u32);
    }
    if value.sprite_count_hint != 0 {
        w.u16(0xe26a); // sprite_count_hint
        w.u8(8);
        w.u32(value.sprite_count_hint as u32);
    }
    if value.sprites_count > 0 {
        w.u16(0xd0ec); // sprites
        w.u8(14);
        let len_at = w.buf.len();
        w.u32(0);
        w.u8(13);
        w.u16(value.sprites_count as u16);
        for i in 0..value.sprites_count as usize {
            {
                let len_at = w.buf.len();
                w.u32(0);
                table_write_render_sprite_into(w, &value.sprites[i]);
                let body = (w.buf.len() - len_at - 4) as u32;
                w.patch32(len_at, body);
            }
        }
        let body = (w.buf.len() - len_at - 4) as u32;
        w.patch32(len_at, body);
    }
    w.u16(0); // terminator
}

/// Decodes a table-wire buffer under the permissive contract: declared
/// defaults prefill, known fields overlay, unknown ids and kind mismatches
/// skip and count, out-of-range values clamp and count. `false` means
/// malformed — the partial decode up to that point is kept.
pub fn table_read_render_block(data: &[u8], value: &mut RenderBlock, report: &mut TableReport) -> bool {
    let mut r = TableReader { buf: data, off: 0 };
    table_read_render_block_from(&mut r, value, report)
}

fn table_read_render_block_from(r: &mut TableReader, value: &mut RenderBlock, report: &mut TableReport) -> bool {
    *value = RenderBlock::default(); // prefill (all-zero defaults), then overlay
    loop {
        if !r.has(2) { report.malformed = true; return false; }
        let field_id = r.get16();
        if field_id == 0 { return true; }
        if !r.has(1) { report.malformed = true; return false; }
        let kind = r.get8();
        match field_id {
            0xc58e => { // worker_index
                if kind != 8 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let raw = r.get32();
                    value.worker_index = raw as u32;
                }
            }
            0xe26a => { // sprite_count_hint
                if kind != 8 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let raw = r.get32();
                    value.sprite_count_hint = raw as u32;
                }
            }
            0xd0ec => { // sprites
                if kind != 14 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let body_len = r.get32() as usize;
                    if !r.has(body_len) { report.malformed = true; return false; }
                    let body_end = r.off + body_len;
                    if body_len >= 3 {
                        let elem_kind = r.get8();
                        let count = r.get16() as usize;
                        if elem_kind != 13 {
                            report.kind_mismatch += 1;
                        } else {
                            let mut keep = count;
                            if keep > 64 { keep = 64; report.clamped += 1; }
                            for i in 0..keep {
                                if !r.has(4) { report.malformed = true; return false; }
                                let elem_len = r.get32() as usize;
                                if !r.has(elem_len) { report.malformed = true; return false; }
                                {
                                    let mut sub = TableReader { buf: &r.buf[r.off..r.off + elem_len], off: 0 };
                                    table_read_render_sprite_from(&mut sub, &mut value.sprites[i], report);
                                }
                                r.off += elem_len;
                            }
                            value.sprites_count = keep as i32;
                        }
                    }
                    r.off = body_end; // excess elements and slack skip via the length
                }
            }
            _ => {
                report.unknown += 1;
                if !r.skip(kind) { report.malformed = true; return false; }
            }
        }
    }
}

// ---- reflection descriptors (tables only, notes/table-wire.md) ----
//
// Descriptors are 'static data, so table_type_* costs nothing per instance
// and needs no lazy initialization: Rust resolves the cross-type links at
// compile time, which is the one place this is simpler than the C#/Go
// backends, where the same links are wired at startup.

static TABLE_TYPE_RENDER_SPRITE_FIELDS: &[TableFieldInfo] = &[
    TableFieldInfo {
        name: "sort_key",
        type_name: "uint64",
        id: 0x4fe9,
        kind: 9,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "mesh_id",
        type_name: "uint32",
        id: 0xd021,
        kind: 8,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "material_id",
        type_name: "uint32",
        id: 0x78c0,
        kind: 8,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "layer",
        type_name: "uint8",
        id: 0x4750,
        kind: 6,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "team",
        type_name: "Team",
        id: 0xdff1,
        kind: 6,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: 2,
        enum_name: Some(enum_name_team_dyn),
        guard: "",
    },
];

static TABLE_TYPE_RENDER_SPRITE: TableTypeInfo = TableTypeInfo {
    name: "RenderSprite",
    fields: TABLE_TYPE_RENDER_SPRITE_FIELDS,
};

/// Returns `RenderSprite`'s reflection descriptor — field names, wire ids/kinds,
/// bounds, ranges, enum names and branch guards. Static data: no
/// per-instance cost and no lazy initialization.
pub fn table_type_render_sprite() -> &'static TableTypeInfo { &TABLE_TYPE_RENDER_SPRITE }

static TABLE_TYPE_RENDER_BLOCK_FIELDS: &[TableFieldInfo] = &[
    TableFieldInfo {
        name: "worker_index",
        type_name: "uint32",
        id: 0xc58e,
        kind: 8,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "sprite_count_hint",
        type_name: "uint32",
        id: 0xe26a,
        kind: 8,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "sprites",
        type_name: "RenderSprite",
        id: 0xd0ec,
        kind: 13,
        is_array: true,
        counted: true,
        array_bound: 64,
        table: Some(&TABLE_TYPE_RENDER_SPRITE),
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
];

static TABLE_TYPE_RENDER_BLOCK: TableTypeInfo = TableTypeInfo {
    name: "RenderBlock",
    fields: TABLE_TYPE_RENDER_BLOCK_FIELDS,
};

/// Returns `RenderBlock`'s reflection descriptor — field names, wire ids/kinds,
/// bounds, ranges, enum names and branch guards. Static data: no
/// per-instance cost and no lazy initialization.
pub fn table_type_render_block() -> &'static TableTypeInfo { &TABLE_TYPE_RENDER_BLOCK }

