// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x91a8e85156dfe2b1

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

pub const RENDER_BLOCK_MAX_SPRITES: i64 = 64;

// type RenderSprite
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct RenderSprite {
    pub sort_key: u64,
    pub mesh_id: u32,
    pub material_id: u32,
    pub layer: u8,
    pub team: Team,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for RenderSprite {
    fn default() -> Self {
        RenderSprite {
            sort_key: 0,
            mesh_id: 0,
            material_id: 0,
            layer: 0,
            team: Team::NONE,
        }
    }
}

// RENDER_SPRITE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// RENDER_SPRITE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const RENDER_SPRITE_MAX_BITS: u64 = 138;
pub const RENDER_SPRITE_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_render_sprite(stream: &mut WriteStream<'_>, value: &RenderSprite) -> Result {
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = value.sort_key;
    let f1: u64 = u64::from(value.mesh_id);
    let f2: u64 = u64::from(value.material_id);
    let f3: u64 = u64::from(value.layer);
    let f4: u64 = u64::from(value.team.0);
    let mut w0 = f0 as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = (f0 >> 32) as u32;
    stream.serialize_bits(&mut w1, 32)?;
    let mut w2 = f1 as u32;
    stream.serialize_bits(&mut w2, 32)?;
    let mut w3 = f2 as u32;
    stream.serialize_bits(&mut w3, 32)?;
    let mut w4 = (f3 | (f4 << 8)) as u32;
    stream.serialize_bits(&mut w4, 10)?;
    Ok(())
}

#[inline]
pub fn read_render_sprite(stream: &mut ReadStream<'_>, value: &mut RenderSprite) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c2 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c3 = u64::from(c);
    stream.serialize_bits(&mut c, 10)?;
    let c4 = u64::from(c);
    let v0: u64 = c0 | (c1 << 32);
    value.sort_key = v0;
    let v1: u64 = c2 & 0xffffffff;
    value.mesh_id = v1 as u32;
    let v2: u64 = c3 & 0xffffffff;
    value.material_id = v2 as u32;
    let v3: u64 = c4 & 0xff;
    value.layer = v3 as u8;
    let v4: u64 = (c4 >> 8) & 0x3;
    if v4 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    value.team = Team(v4 as u8);
    Ok(())
}

// type RenderBlock
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct RenderBlock {
    pub worker_index: u32,
    pub sprite_count_hint: u32,
    pub sprites: [RenderSprite; RENDER_BLOCK_MAX_SPRITES as usize], // used count beside it; wire count in [0, RenderBlockMaxSprites]
    pub sprites_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for RenderBlock {
    fn default() -> Self {
        RenderBlock {
            worker_index: 0,
            sprite_count_hint: 0,
            sprites: [RenderSprite::default(); RENDER_BLOCK_MAX_SPRITES as usize],
            sprites_count: 0,
        }
    }
}

// RENDER_BLOCK_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// RENDER_BLOCK_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const RENDER_BLOCK_MAX_BITS: u64 = 8903;
pub const RENDER_BLOCK_MAX_BYTES: usize = 1120;

#[inline(always)]
pub fn write_render_block(stream: &mut WriteStream<'_>, value: &RenderBlock) -> Result {
    let f0: u64 = u64::from(value.worker_index);
    let f1: u64 = u64::from(value.sprite_count_hint);
    let mut w0 = f0 as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = f1 as u32;
    stream.serialize_bits(&mut w1, 32)?;
    if value.sprites_count < 0 || value.sprites_count > 64 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.sprites_count as u32;
        stream.serialize_bits(&mut offset_value, 7)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.sprites_count as usize {
        write_render_sprite(stream, &value.sprites[i])?;
    }
    Ok(())
}

#[inline]
pub fn read_render_block(stream: &mut ReadStream<'_>, value: &mut RenderBlock) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    let v0: u64 = c0 & 0xffffffff;
    value.worker_index = v0 as u32;
    let v1: u64 = c1 & 0xffffffff;
    value.sprite_count_hint = v1 as u32;
    stream.serialize_int(&mut value.sprites_count, 0, RENDER_BLOCK_MAX_SPRITES as i32)?; // the count guards the loop (§6.3)
    for i in 0..value.sprites_count as usize {
        read_render_sprite(stream, &mut value.sprites[i])?;
    }
    Ok(())
}

