// Code generated by the schema compiler from Objects.schema. DO NOT EDIT.
// package example — protocol id 0x14cc156665b9f146

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

// ObjectType: the object set, extracted by the compiler — None = 0, then each object sorted by name (SPEC §4.8)
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct ObjectType(pub u8);

impl ObjectType {
    pub const NONE: ObjectType = ObjectType(0);
    pub const DYNAMIC_PROP: ObjectType = ObjectType(1);
    pub const MISSILE: ObjectType = ObjectType(2);
    pub const SHIP: ObjectType = ObjectType(3);
    pub const TURRET: ObjectType = ObjectType(4);
}

// ---- object Ship — one definition, a generated family per target (SPEC §4.8) ----

// ClientShipState — the full simulation struct for the client context: every `all`
// field plus the fields scoped [local, context = client]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ClientShipState {
    pub ship_type: ShipType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: ShipFlags,
    pub team: Team,
    pub health: f32, // shallow wire: [0.0, 1000.0] @ 1.0 -> int [0, 1000], round up
    pub thrust: f32, // shallow wire: [0.0, 1.0] @ 0.01 -> int [0, 100]
    pub angular_velocity: Vec3,
    pub laser_cooldown: f32,
    pub missile_cooldown: f32,
    pub speed_current: f32,
    pub speed_velocity: f32,
    pub stick_current: Vec3,
    pub stick_velocity: Vec3,
    pub sensitivity_current: f32,
    pub sensitivity_velocity: f32,
    pub roll_current: f32,
    pub roll_velocity: f32,
    pub aim_current: f32,
    pub aim_velocity: f32,
    pub laser_index: i8, // wire [0, 15]
    pub missile_index: i8, // wire [0, 15]
    pub target: Handle,
    pub lock_start_time: f64,
    pub invulnerable: bool, // [local] — no wire
    pub previous_position: Vec3, // [local] — no wire
    pub num_colliders: i32, // [local] — no wire
    pub collider_armor: [i32; MAX_COLLIDERS_PER_SHIP as usize], // [local] — no wire
    pub predicted_explode: bool, // [local, context = client]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ClientShipState {
    fn default() -> Self {
        ClientShipState {
            ship_type: ShipType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
            health: 0.0,
            thrust: 0.0,
            angular_velocity: Vec3::default(),
            laser_cooldown: 0.0,
            missile_cooldown: 0.0,
            speed_current: 0.0,
            speed_velocity: 0.0,
            stick_current: Vec3::default(),
            stick_velocity: Vec3::default(),
            sensitivity_current: 0.0,
            sensitivity_velocity: 0.0,
            roll_current: 0.0,
            roll_velocity: 0.0,
            aim_current: 0.0,
            aim_velocity: 0.0,
            laser_index: 0,
            missile_index: 0,
            target: Handle::default(),
            lock_start_time: 0.0,
            invulnerable: false,
            previous_position: Vec3::default(),
            num_colliders: 0,
            collider_armor: [0; MAX_COLLIDERS_PER_SHIP as usize],
            predicted_explode: false,
        }
    }
}

// ServerShipState — the full simulation struct for the server context: every `all`
// field plus the fields scoped [local, context = server]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ServerShipState {
    pub ship_type: ShipType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: ShipFlags,
    pub team: Team,
    pub health: f32, // shallow wire: [0.0, 1000.0] @ 1.0 -> int [0, 1000], round up
    pub thrust: f32, // shallow wire: [0.0, 1.0] @ 0.01 -> int [0, 100]
    pub angular_velocity: Vec3,
    pub laser_cooldown: f32,
    pub missile_cooldown: f32,
    pub speed_current: f32,
    pub speed_velocity: f32,
    pub stick_current: Vec3,
    pub stick_velocity: Vec3,
    pub sensitivity_current: f32,
    pub sensitivity_velocity: f32,
    pub roll_current: f32,
    pub roll_velocity: f32,
    pub aim_current: f32,
    pub aim_velocity: f32,
    pub laser_index: i8, // wire [0, 15]
    pub missile_index: i8, // wire [0, 15]
    pub target: Handle,
    pub lock_start_time: f64,
    pub invulnerable: bool, // [local] — no wire
    pub previous_position: Vec3, // [local] — no wire
    pub num_colliders: i32, // [local] — no wire
    pub collider_armor: [i32; MAX_COLLIDERS_PER_SHIP as usize], // [local] — no wire
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ServerShipState {
    fn default() -> Self {
        ServerShipState {
            ship_type: ShipType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
            health: 0.0,
            thrust: 0.0,
            angular_velocity: Vec3::default(),
            laser_cooldown: 0.0,
            missile_cooldown: 0.0,
            speed_current: 0.0,
            speed_velocity: 0.0,
            stick_current: Vec3::default(),
            stick_velocity: Vec3::default(),
            sensitivity_current: 0.0,
            sensitivity_velocity: 0.0,
            roll_current: 0.0,
            roll_velocity: 0.0,
            aim_current: 0.0,
            aim_velocity: 0.0,
            laser_index: 0,
            missile_index: 0,
            target: Handle::default(),
            lock_start_time: 0.0,
            invulnerable: false,
            previous_position: Vec3::default(),
            num_colliders: 0,
            collider_armor: [0; MAX_COLLIDERS_PER_SHIP as usize],
        }
    }
}

// ShipData_Deep — every non-[local] field, deep encodings: full state for
// client-side prediction
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ShipData_Deep {
    pub ship_type: ShipType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: ShipFlags,
    pub team: Team,
    pub health: f32, // shallow wire: [0.0, 1000.0] @ 1.0 -> int [0, 1000], round up
    pub thrust: f32, // shallow wire: [0.0, 1.0] @ 0.01 -> int [0, 100]
    pub angular_velocity: Vec3,
    pub laser_cooldown: f32,
    pub missile_cooldown: f32,
    pub speed_current: f32,
    pub speed_velocity: f32,
    pub stick_current: Vec3,
    pub stick_velocity: Vec3,
    pub sensitivity_current: f32,
    pub sensitivity_velocity: f32,
    pub roll_current: f32,
    pub roll_velocity: f32,
    pub aim_current: f32,
    pub aim_velocity: f32,
    pub laser_index: i8, // wire [0, 15]
    pub missile_index: i8, // wire [0, 15]
    pub target: Handle,
    pub lock_start_time: f64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ShipData_Deep {
    fn default() -> Self {
        ShipData_Deep {
            ship_type: ShipType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
            health: 0.0,
            thrust: 0.0,
            angular_velocity: Vec3::default(),
            laser_cooldown: 0.0,
            missile_cooldown: 0.0,
            speed_current: 0.0,
            speed_velocity: 0.0,
            stick_current: Vec3::default(),
            stick_velocity: Vec3::default(),
            sensitivity_current: 0.0,
            sensitivity_velocity: 0.0,
            roll_current: 0.0,
            roll_velocity: 0.0,
            aim_current: 0.0,
            aim_velocity: 0.0,
            laser_index: 0,
            missile_index: 0,
            target: Handle::default(),
            lock_start_time: 0.0,
        }
    }
}

// ShipData_Shallow — the [interpolate] fields on the quantized wire: the
// implementation detail on the way to interpolation on the client
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ShipData_Shallow {
    pub ship_type: ShipType,
    // position: Vec3 quantized by PositionUnits, max MaxWorldMeters — per-component int in [-8388608, 8388608]
    pub position_x: i32,
    pub position_y: i32,
    pub position_z: i32,
    // rotation: Quat quantized by RotationUnits, max 1 — per-component int in [-1024, 1024]
    pub rotation_x: i16,
    pub rotation_y: i16,
    pub rotation_z: i16,
    pub rotation_w: i16,
    // linear_velocity: Vec3 quantized by VelocityUnits, max MaxSpeedMeters — per-component int in [-2097152, 2097152]
    pub linear_velocity_x: i32,
    pub linear_velocity_y: i32,
    pub linear_velocity_z: i32,
    pub flags: ShipFlags,
    pub team: Team,
    pub health: u16, // float [0.0, 1000.0] @ resolution 1.0 -> wire int [0, 1000], round up
    pub thrust: u8, // float [0.0, 1.0] @ resolution 0.01 -> wire int [0, 100]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ShipData_Shallow {
    fn default() -> Self {
        ShipData_Shallow {
            ship_type: ShipType::NONE,
            position_x: 0,
            position_y: 0,
            position_z: 0,
            rotation_x: 0,
            rotation_y: 0,
            rotation_z: 0,
            rotation_w: 0,
            linear_velocity_x: 0,
            linear_velocity_y: 0,
            linear_velocity_z: 0,
            flags: 0,
            team: Team::NONE,
            health: 0,
            thrust: 0,
        }
    }
}

// ShipData_Interpolate — the same fields in interpolate storage: projected
// fields stay in the wire integer domain and snap-interpolate; quantized
// composites store continuous (SPEC §4.8 rule 5)
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ShipData_Interpolate {
    pub ship_type: ShipType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: ShipFlags,
    pub team: Team,
    pub health: u16, // float [0.0, 1000.0] @ resolution 1.0 -> wire int [0, 1000], round up — wire-int domain, snap-interpolated (SPEC §4.8 rule 5)
    pub thrust: u8, // float [0.0, 1.0] @ resolution 0.01 -> wire int [0, 100] — wire-int domain, snap-interpolated (SPEC §4.8 rule 5)
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ShipData_Interpolate {
    fn default() -> Self {
        ShipData_Interpolate {
            ship_type: ShipType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
            health: 0,
            thrust: 0,
        }
    }
}

pub const SHIP_DATA_DEEP_MAX_BITS: u64 = 1703;
pub const SHIP_DATA_DEEP_MAX_BYTES: usize = 216; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_ship_data_deep(stream: &mut WriteStream<'_>, value: &ShipData_Deep) -> Result {
    if value.ship_type.0 > 5 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.ship_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    write_vec3(stream, &value.position)?;
    write_quat(stream, &value.rotation)?;
    write_vec3(stream, &value.linear_velocity)?;
    if value.flags >= 1 << 4 {
        // a mask bit above the wire width cannot ride
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut flags_value = value.flags as u32;
        stream.serialize_bits(&mut flags_value, 4)?;
    }
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    {
        let mut float_value = value.health;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.thrust;
        stream.serialize_f32(&mut float_value)?;
    }
    write_vec3(stream, &value.angular_velocity)?;
    {
        let mut float_value = value.laser_cooldown;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.missile_cooldown;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.speed_current;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.speed_velocity;
        stream.serialize_f32(&mut float_value)?;
    }
    write_vec3(stream, &value.stick_current)?;
    write_vec3(stream, &value.stick_velocity)?;
    {
        let mut float_value = value.sensitivity_current;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.sensitivity_velocity;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.roll_current;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.roll_velocity;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.aim_current;
        stream.serialize_f32(&mut float_value)?;
    }
    {
        let mut float_value = value.aim_velocity;
        stream.serialize_f32(&mut float_value)?;
    }
    if value.laser_index < 0 || value.laser_index > 15 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.laser_index as u32;
        stream.serialize_bits(&mut offset_value, 4)?;
    }
    if value.missile_index < 0 || value.missile_index > 15 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.missile_index as u32;
        stream.serialize_bits(&mut offset_value, 4)?;
    }
    write_handle(stream, &value.target)?;
    {
        let mut float_value = value.lock_start_time;
        stream.serialize_f64(&mut float_value)?;
    }
    Ok(())
}

#[inline]
pub fn read_ship_data_deep(stream: &mut ReadStream<'_>, value: &mut ShipData_Deep) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 5)?;
        value.ship_type = ShipType(enum_value as u8);
    }
    read_vec3(stream, &mut value.position)?;
    read_quat(stream, &mut value.rotation)?;
    read_vec3(stream, &mut value.linear_velocity)?;
    {
        let mut flags_value: u32 = 0;
        stream.serialize_bits(&mut flags_value, 4)?;
        value.flags = flags_value as ShipFlags;
    }
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    stream.serialize_f32(&mut value.health)?;
    stream.serialize_f32(&mut value.thrust)?;
    read_vec3(stream, &mut value.angular_velocity)?;
    stream.serialize_f32(&mut value.laser_cooldown)?;
    stream.serialize_f32(&mut value.missile_cooldown)?;
    stream.serialize_f32(&mut value.speed_current)?;
    stream.serialize_f32(&mut value.speed_velocity)?;
    read_vec3(stream, &mut value.stick_current)?;
    read_vec3(stream, &mut value.stick_velocity)?;
    stream.serialize_f32(&mut value.sensitivity_current)?;
    stream.serialize_f32(&mut value.sensitivity_velocity)?;
    stream.serialize_f32(&mut value.roll_current)?;
    stream.serialize_f32(&mut value.roll_velocity)?;
    stream.serialize_f32(&mut value.aim_current)?;
    stream.serialize_f32(&mut value.aim_velocity)?;
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, (SHIP_MAX_LASERS - 1) as i32)?;
        value.laser_index = range_value as i8;
    }
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, (SHIP_MAX_MISSILES - 1) as i32)?;
        value.missile_index = range_value as i8;
    }
    read_handle(stream, &mut value.target)?;
    stream.serialize_f64(&mut value.lock_start_time)?;
    Ok(())
}

pub const SHIP_DATA_SHALLOW_MAX_BITS: u64 = 218;
pub const SHIP_DATA_SHALLOW_MAX_BYTES: usize = 32; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_ship_data_shallow(stream: &mut WriteStream<'_>, value: &ShipData_Shallow) -> Result {
    if value.ship_type.0 > 5 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.ship_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    debug_assert!(value.position_x >= -8388608 && value.position_x <= 8388608);
    {
        let mut offset_value = (value.position_x as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_y >= -8388608 && value.position_y <= 8388608);
    {
        let mut offset_value = (value.position_y as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_z >= -8388608 && value.position_z <= 8388608);
    {
        let mut offset_value = (value.position_z as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.rotation_x >= -1024 && value.rotation_x <= 1024);
    {
        let mut offset_value = (value.rotation_x as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_y >= -1024 && value.rotation_y <= 1024);
    {
        let mut offset_value = (value.rotation_y as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_z >= -1024 && value.rotation_z <= 1024);
    {
        let mut offset_value = (value.rotation_z as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_w >= -1024 && value.rotation_w <= 1024);
    {
        let mut offset_value = (value.rotation_w as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.linear_velocity_x >= -2097152 && value.linear_velocity_x <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_x as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_y >= -2097152 && value.linear_velocity_y <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_y as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_z >= -2097152 && value.linear_velocity_z <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_z as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    if value.flags >= 1 << 4 {
        // a mask bit above the wire width cannot ride
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut flags_value = value.flags as u32;
        stream.serialize_bits(&mut flags_value, 4)?;
    }
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    debug_assert!(value.health <= 1000);
    {
        let mut offset_value = value.health as u32;
        stream.serialize_bits(&mut offset_value, 10)?;
    }
    debug_assert!(value.thrust <= 100);
    {
        let mut offset_value = value.thrust as u32;
        stream.serialize_bits(&mut offset_value, 7)?;
    }
    Ok(())
}

#[inline]
pub fn read_ship_data_shallow(stream: &mut ReadStream<'_>, value: &mut ShipData_Shallow) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 5)?;
        value.ship_type = ShipType(enum_value as u8);
    }
    stream.serialize_int(&mut value.position_x, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_y, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_z, -8388608, 8388608)?;
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_x = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_y = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_z = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_w = component_value as i16;
    }
    stream.serialize_int(&mut value.linear_velocity_x, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_y, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_z, -2097152, 2097152)?;
    {
        let mut flags_value: u32 = 0;
        stream.serialize_bits(&mut flags_value, 4)?;
        value.flags = flags_value as ShipFlags;
    }
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    {
        let mut projected_value: i32 = 0;
        stream.serialize_int(&mut projected_value, 0, 1000)?;
        value.health = projected_value as u16;
    }
    {
        let mut projected_value: i32 = 0;
        stream.serialize_int(&mut projected_value, 0, 100)?;
        value.thrust = projected_value as u8;
    }
    Ok(())
}

pub fn quantize_ship(input: &ShipData_Interpolate, output: &mut ShipData_Shallow) {
    output.ship_type = input.ship_type;
    {
        let quantized_value = (input.position.x * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_x = component_value as i32;
    }
    {
        let quantized_value = (input.position.y * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_y = component_value as i32;
    }
    {
        let quantized_value = (input.position.z * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_z = component_value as i32;
    }
    {
        let quantized_value = (input.rotation.x * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_x = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.y * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_y = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.z * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_z = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.w * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_w = component_value as i16;
    }
    {
        let quantized_value = (input.linear_velocity.x * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_x = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.y * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_y = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.z * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_z = component_value as i32;
    }
    output.flags = input.flags;
    output.team = input.team;
    output.health = input.health;
    output.thrust = input.thrust;
}

pub fn unquantize_ship(input: &ShipData_Shallow, output: &mut ShipData_Interpolate) {
    output.ship_type = input.ship_type;
    output.position.x = input.position_x as f64 / POSITION_UNITS as f64;
    output.position.y = input.position_y as f64 / POSITION_UNITS as f64;
    output.position.z = input.position_z as f64 / POSITION_UNITS as f64;
    output.rotation.x = input.rotation_x as f64 / ROTATION_UNITS as f64;
    output.rotation.y = input.rotation_y as f64 / ROTATION_UNITS as f64;
    output.rotation.z = input.rotation_z as f64 / ROTATION_UNITS as f64;
    output.rotation.w = input.rotation_w as f64 / ROTATION_UNITS as f64;
    output.linear_velocity.x = input.linear_velocity_x as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.y = input.linear_velocity_y as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.z = input.linear_velocity_z as f64 / VELOCITY_UNITS as f64;
    output.flags = input.flags;
    output.team = input.team;
    output.health = input.health;
    output.thrust = input.thrust;
}

// ---- object Missile — one definition, a generated family per target (SPEC §4.8) ----

// ClientMissileState — the full simulation struct for the client context: every `all`
// field plus the fields scoped [local, context = client]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ClientMissileState {
    pub missile_type: MissileType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub team: Team,
    pub flags: u64,
    pub angular_velocity: Vec3, // [local] — no wire
    pub target: Handle, // [local] — no wire
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ClientMissileState {
    fn default() -> Self {
        ClientMissileState {
            missile_type: MissileType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            team: Team::NONE,
            flags: 0,
            angular_velocity: Vec3::default(),
            target: Handle::default(),
        }
    }
}

// ServerMissileState — the full simulation struct for the server context: every `all`
// field plus the fields scoped [local, context = server]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ServerMissileState {
    pub missile_type: MissileType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub team: Team,
    pub flags: u64,
    pub angular_velocity: Vec3, // [local] — no wire
    pub target: Handle, // [local] — no wire
    pub timer: f64, // [local, context = server]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ServerMissileState {
    fn default() -> Self {
        ServerMissileState {
            missile_type: MissileType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            team: Team::NONE,
            flags: 0,
            angular_velocity: Vec3::default(),
            target: Handle::default(),
            timer: 0.0,
        }
    }
}

// MissileData_Deep — every non-[local] field, deep encodings: full state for
// client-side prediction
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct MissileData_Deep {
    pub missile_type: MissileType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub team: Team,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for MissileData_Deep {
    fn default() -> Self {
        MissileData_Deep {
            missile_type: MissileType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            team: Team::NONE,
            flags: 0,
        }
    }
}

// MissileData_Shallow — the [interpolate] fields on the quantized wire: the
// implementation detail on the way to interpolation on the client
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct MissileData_Shallow {
    pub missile_type: MissileType,
    // position: Vec3 quantized by PositionUnits, max MaxWorldMeters — per-component int in [-8388608, 8388608]
    pub position_x: i32,
    pub position_y: i32,
    pub position_z: i32,
    // rotation: Quat quantized by RotationUnits, max 1 — per-component int in [-1024, 1024]
    pub rotation_x: i16,
    pub rotation_y: i16,
    pub rotation_z: i16,
    pub rotation_w: i16,
    // linear_velocity: Vec3 quantized by VelocityUnits, max MaxSpeedMeters — per-component int in [-2097152, 2097152]
    pub linear_velocity_x: i32,
    pub linear_velocity_y: i32,
    pub linear_velocity_z: i32,
    pub team: Team,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for MissileData_Shallow {
    fn default() -> Self {
        MissileData_Shallow {
            missile_type: MissileType::NONE,
            position_x: 0,
            position_y: 0,
            position_z: 0,
            rotation_x: 0,
            rotation_y: 0,
            rotation_z: 0,
            rotation_w: 0,
            linear_velocity_x: 0,
            linear_velocity_y: 0,
            linear_velocity_z: 0,
            team: Team::NONE,
            flags: 0,
        }
    }
}

// MissileData_Interpolate — the same fields in interpolate storage: projected
// fields stay in the wire integer domain and snap-interpolate; quantized
// composites store continuous (SPEC §4.8 rule 5)
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct MissileData_Interpolate {
    pub missile_type: MissileType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub team: Team,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for MissileData_Interpolate {
    fn default() -> Self {
        MissileData_Interpolate {
            missile_type: MissileType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            team: Team::NONE,
            flags: 0,
        }
    }
}

pub const MISSILE_DATA_DEEP_MAX_BITS: u64 = 708;
pub const MISSILE_DATA_DEEP_MAX_BYTES: usize = 96; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_missile_data_deep(stream: &mut WriteStream<'_>, value: &MissileData_Deep) -> Result {
    if value.missile_type.0 > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.missile_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    write_vec3(stream, &value.position)?;
    write_quat(stream, &value.rotation)?;
    write_vec3(stream, &value.linear_velocity)?;
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    Ok(())
}

#[inline]
pub fn read_missile_data_deep(stream: &mut ReadStream<'_>, value: &mut MissileData_Deep) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 3)?;
        value.missile_type = MissileType(enum_value as u8);
    }
    read_vec3(stream, &mut value.position)?;
    read_quat(stream, &mut value.rotation)?;
    read_vec3(stream, &mut value.linear_velocity)?;
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    stream.serialize_bits64(&mut value.flags, 64)?;
    Ok(())
}

pub const MISSILE_DATA_SHALLOW_MAX_BITS: u64 = 260;
pub const MISSILE_DATA_SHALLOW_MAX_BYTES: usize = 40; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_missile_data_shallow(stream: &mut WriteStream<'_>, value: &MissileData_Shallow) -> Result {
    if value.missile_type.0 > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.missile_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    debug_assert!(value.position_x >= -8388608 && value.position_x <= 8388608);
    {
        let mut offset_value = (value.position_x as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_y >= -8388608 && value.position_y <= 8388608);
    {
        let mut offset_value = (value.position_y as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_z >= -8388608 && value.position_z <= 8388608);
    {
        let mut offset_value = (value.position_z as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.rotation_x >= -1024 && value.rotation_x <= 1024);
    {
        let mut offset_value = (value.rotation_x as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_y >= -1024 && value.rotation_y <= 1024);
    {
        let mut offset_value = (value.rotation_y as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_z >= -1024 && value.rotation_z <= 1024);
    {
        let mut offset_value = (value.rotation_z as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_w >= -1024 && value.rotation_w <= 1024);
    {
        let mut offset_value = (value.rotation_w as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.linear_velocity_x >= -2097152 && value.linear_velocity_x <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_x as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_y >= -2097152 && value.linear_velocity_y <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_y as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_z >= -2097152 && value.linear_velocity_z <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_z as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    Ok(())
}

#[inline]
pub fn read_missile_data_shallow(stream: &mut ReadStream<'_>, value: &mut MissileData_Shallow) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 3)?;
        value.missile_type = MissileType(enum_value as u8);
    }
    stream.serialize_int(&mut value.position_x, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_y, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_z, -8388608, 8388608)?;
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_x = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_y = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_z = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_w = component_value as i16;
    }
    stream.serialize_int(&mut value.linear_velocity_x, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_y, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_z, -2097152, 2097152)?;
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    stream.serialize_bits64(&mut value.flags, 64)?;
    Ok(())
}

pub fn quantize_missile(input: &MissileData_Interpolate, output: &mut MissileData_Shallow) {
    output.missile_type = input.missile_type;
    {
        let quantized_value = (input.position.x * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_x = component_value as i32;
    }
    {
        let quantized_value = (input.position.y * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_y = component_value as i32;
    }
    {
        let quantized_value = (input.position.z * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_z = component_value as i32;
    }
    {
        let quantized_value = (input.rotation.x * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_x = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.y * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_y = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.z * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_z = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.w * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_w = component_value as i16;
    }
    {
        let quantized_value = (input.linear_velocity.x * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_x = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.y * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_y = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.z * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_z = component_value as i32;
    }
    output.team = input.team;
    output.flags = input.flags;
}

pub fn unquantize_missile(input: &MissileData_Shallow, output: &mut MissileData_Interpolate) {
    output.missile_type = input.missile_type;
    output.position.x = input.position_x as f64 / POSITION_UNITS as f64;
    output.position.y = input.position_y as f64 / POSITION_UNITS as f64;
    output.position.z = input.position_z as f64 / POSITION_UNITS as f64;
    output.rotation.x = input.rotation_x as f64 / ROTATION_UNITS as f64;
    output.rotation.y = input.rotation_y as f64 / ROTATION_UNITS as f64;
    output.rotation.z = input.rotation_z as f64 / ROTATION_UNITS as f64;
    output.rotation.w = input.rotation_w as f64 / ROTATION_UNITS as f64;
    output.linear_velocity.x = input.linear_velocity_x as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.y = input.linear_velocity_y as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.z = input.linear_velocity_z as f64 / VELOCITY_UNITS as f64;
    output.team = input.team;
    output.flags = input.flags;
}

// ---- object DynamicProp — one definition, a generated family per target (SPEC §4.8) ----

// DynamicPropState — the full simulation struct: every field
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct DynamicPropState {
    pub prop_type: PropType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: u64,
    pub team: Team,
    pub angular_velocity: Vec3, // [local] — no wire
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for DynamicPropState {
    fn default() -> Self {
        DynamicPropState {
            prop_type: PropType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
            angular_velocity: Vec3::default(),
        }
    }
}

// DynamicPropData_Deep — every non-[local] field, deep encodings: full state for
// client-side prediction
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct DynamicPropData_Deep {
    pub prop_type: PropType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: u64,
    pub team: Team,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for DynamicPropData_Deep {
    fn default() -> Self {
        DynamicPropData_Deep {
            prop_type: PropType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
        }
    }
}

// DynamicPropData_Shallow — the [interpolate] fields on the quantized wire: the
// implementation detail on the way to interpolation on the client
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct DynamicPropData_Shallow {
    pub prop_type: PropType,
    // position: Vec3 quantized by PositionUnits, max MaxWorldMeters — per-component int in [-8388608, 8388608]
    pub position_x: i32,
    pub position_y: i32,
    pub position_z: i32,
    // rotation: Quat quantized by RotationUnits, max 1 — per-component int in [-1024, 1024]
    pub rotation_x: i16,
    pub rotation_y: i16,
    pub rotation_z: i16,
    pub rotation_w: i16,
    // linear_velocity: Vec3 quantized by VelocityUnits, max MaxSpeedMeters — per-component int in [-2097152, 2097152]
    pub linear_velocity_x: i32,
    pub linear_velocity_y: i32,
    pub linear_velocity_z: i32,
    pub flags: u64,
    pub team: Team,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for DynamicPropData_Shallow {
    fn default() -> Self {
        DynamicPropData_Shallow {
            prop_type: PropType::NONE,
            position_x: 0,
            position_y: 0,
            position_z: 0,
            rotation_x: 0,
            rotation_y: 0,
            rotation_z: 0,
            rotation_w: 0,
            linear_velocity_x: 0,
            linear_velocity_y: 0,
            linear_velocity_z: 0,
            flags: 0,
            team: Team::NONE,
        }
    }
}

// DynamicPropData_Interpolate — the same fields in interpolate storage: projected
// fields stay in the wire integer domain and snap-interpolate; quantized
// composites store continuous (SPEC §4.8 rule 5)
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct DynamicPropData_Interpolate {
    pub prop_type: PropType,
    pub position: Vec3,
    pub rotation: Quat,
    pub linear_velocity: Vec3,
    pub flags: u64,
    pub team: Team,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for DynamicPropData_Interpolate {
    fn default() -> Self {
        DynamicPropData_Interpolate {
            prop_type: PropType::NONE,
            position: Vec3::default(),
            rotation: Quat::default(),
            linear_velocity: Vec3::default(),
            flags: 0,
            team: Team::NONE,
        }
    }
}

pub const DYNAMIC_PROP_DATA_DEEP_MAX_BITS: u64 = 709;
pub const DYNAMIC_PROP_DATA_DEEP_MAX_BYTES: usize = 96; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_dynamic_prop_data_deep(stream: &mut WriteStream<'_>, value: &DynamicPropData_Deep) -> Result {
    if value.prop_type.0 > 6 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.prop_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    write_vec3(stream, &value.position)?;
    write_quat(stream, &value.rotation)?;
    write_vec3(stream, &value.linear_velocity)?;
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    Ok(())
}

#[inline]
pub fn read_dynamic_prop_data_deep(stream: &mut ReadStream<'_>, value: &mut DynamicPropData_Deep) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 6)?;
        value.prop_type = PropType(enum_value as u8);
    }
    read_vec3(stream, &mut value.position)?;
    read_quat(stream, &mut value.rotation)?;
    read_vec3(stream, &mut value.linear_velocity)?;
    stream.serialize_bits64(&mut value.flags, 64)?;
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    Ok(())
}

pub const DYNAMIC_PROP_DATA_SHALLOW_MAX_BITS: u64 = 261;
pub const DYNAMIC_PROP_DATA_SHALLOW_MAX_BYTES: usize = 40; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_dynamic_prop_data_shallow(stream: &mut WriteStream<'_>, value: &DynamicPropData_Shallow) -> Result {
    if value.prop_type.0 > 6 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.prop_type.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    debug_assert!(value.position_x >= -8388608 && value.position_x <= 8388608);
    {
        let mut offset_value = (value.position_x as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_y >= -8388608 && value.position_y <= 8388608);
    {
        let mut offset_value = (value.position_y as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.position_z >= -8388608 && value.position_z <= 8388608);
    {
        let mut offset_value = (value.position_z as u32).wrapping_sub((-8388608_i32) as u32);
        stream.serialize_bits(&mut offset_value, 25)?;
    }
    debug_assert!(value.rotation_x >= -1024 && value.rotation_x <= 1024);
    {
        let mut offset_value = (value.rotation_x as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_y >= -1024 && value.rotation_y <= 1024);
    {
        let mut offset_value = (value.rotation_y as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_z >= -1024 && value.rotation_z <= 1024);
    {
        let mut offset_value = (value.rotation_z as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_w >= -1024 && value.rotation_w <= 1024);
    {
        let mut offset_value = (value.rotation_w as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.linear_velocity_x >= -2097152 && value.linear_velocity_x <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_x as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_y >= -2097152 && value.linear_velocity_y <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_y as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    debug_assert!(value.linear_velocity_z >= -2097152 && value.linear_velocity_z <= 2097152);
    {
        let mut offset_value = (value.linear_velocity_z as u32).wrapping_sub((-2097152_i32) as u32);
        stream.serialize_bits(&mut offset_value, 23)?;
    }
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    if value.team.0 > 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.team.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    Ok(())
}

#[inline]
pub fn read_dynamic_prop_data_shallow(stream: &mut ReadStream<'_>, value: &mut DynamicPropData_Shallow) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 6)?;
        value.prop_type = PropType(enum_value as u8);
    }
    stream.serialize_int(&mut value.position_x, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_y, -8388608, 8388608)?;
    stream.serialize_int(&mut value.position_z, -8388608, 8388608)?;
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_x = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_y = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_z = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_w = component_value as i16;
    }
    stream.serialize_int(&mut value.linear_velocity_x, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_y, -2097152, 2097152)?;
    stream.serialize_int(&mut value.linear_velocity_z, -2097152, 2097152)?;
    stream.serialize_bits64(&mut value.flags, 64)?;
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 2)?;
        value.team = Team(enum_value as u8);
    }
    Ok(())
}

pub fn quantize_dynamic_prop(input: &DynamicPropData_Interpolate, output: &mut DynamicPropData_Shallow) {
    output.prop_type = input.prop_type;
    {
        let quantized_value = (input.position.x * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_x = component_value as i32;
    }
    {
        let quantized_value = (input.position.y * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_y = component_value as i32;
    }
    {
        let quantized_value = (input.position.z * POSITION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 8388608.0_f64 {
            8388608
        } else if quantized_value >= -8388608.0_f64 {
            quantized_value as i64
        } else {
            -8388608
        };
        output.position_z = component_value as i32;
    }
    {
        let quantized_value = (input.rotation.x * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_x = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.y * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_y = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.z * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_z = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.w * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_w = component_value as i16;
    }
    {
        let quantized_value = (input.linear_velocity.x * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_x = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.y * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_y = component_value as i32;
    }
    {
        let quantized_value = (input.linear_velocity.z * VELOCITY_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 2097152.0_f64 {
            2097152
        } else if quantized_value >= -2097152.0_f64 {
            quantized_value as i64
        } else {
            -2097152
        };
        output.linear_velocity_z = component_value as i32;
    }
    output.flags = input.flags;
    output.team = input.team;
}

pub fn unquantize_dynamic_prop(input: &DynamicPropData_Shallow, output: &mut DynamicPropData_Interpolate) {
    output.prop_type = input.prop_type;
    output.position.x = input.position_x as f64 / POSITION_UNITS as f64;
    output.position.y = input.position_y as f64 / POSITION_UNITS as f64;
    output.position.z = input.position_z as f64 / POSITION_UNITS as f64;
    output.rotation.x = input.rotation_x as f64 / ROTATION_UNITS as f64;
    output.rotation.y = input.rotation_y as f64 / ROTATION_UNITS as f64;
    output.rotation.z = input.rotation_z as f64 / ROTATION_UNITS as f64;
    output.rotation.w = input.rotation_w as f64 / ROTATION_UNITS as f64;
    output.linear_velocity.x = input.linear_velocity_x as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.y = input.linear_velocity_y as f64 / VELOCITY_UNITS as f64;
    output.linear_velocity.z = input.linear_velocity_z as f64 / VELOCITY_UNITS as f64;
    output.flags = input.flags;
    output.team = input.team;
}

// ---- object Turret — one definition, a generated family per target (SPEC §4.8) ----

// TurretState — the full simulation struct: every field
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TurretState {
    pub parent: Handle,
    pub turret_index: i32, // wire [0, 255]
    pub rotation: Quat,
    pub flags: u64,
    pub team: Team, // [local] — no wire
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TurretState {
    fn default() -> Self {
        TurretState {
            parent: Handle::default(),
            turret_index: 0,
            rotation: Quat::default(),
            flags: 0,
            team: Team::NONE,
        }
    }
}

// TurretData_Deep — every non-[local] field, deep encodings: full state for
// client-side prediction
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TurretData_Deep {
    pub parent: Handle,
    pub turret_index: i32, // wire [0, 255]
    pub rotation: Quat,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TurretData_Deep {
    fn default() -> Self {
        TurretData_Deep {
            parent: Handle::default(),
            turret_index: 0,
            rotation: Quat::default(),
            flags: 0,
        }
    }
}

// TurretData_Shallow — the [interpolate] fields on the quantized wire: the
// implementation detail on the way to interpolation on the client
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TurretData_Shallow {
    pub parent: Handle,
    pub turret_index: i32, // wire [0, 255]
    // rotation: Quat quantized by RotationUnits, max 1 — per-component int in [-1024, 1024]
    pub rotation_x: i16,
    pub rotation_y: i16,
    pub rotation_z: i16,
    pub rotation_w: i16,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TurretData_Shallow {
    fn default() -> Self {
        TurretData_Shallow {
            parent: Handle::default(),
            turret_index: 0,
            rotation_x: 0,
            rotation_y: 0,
            rotation_z: 0,
            rotation_w: 0,
            flags: 0,
        }
    }
}

// TurretData_Interpolate — the same fields in interpolate storage: projected
// fields stay in the wire integer domain and snap-interpolate; quantized
// composites store continuous (SPEC §4.8 rule 5)
#[allow(non_camel_case_types)] // the name is fixed across targets (SPEC §4.8)
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TurretData_Interpolate {
    pub parent: Handle,
    pub turret_index: i32, // wire [0, 255]
    pub rotation: Quat,
    pub flags: u64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TurretData_Interpolate {
    fn default() -> Self {
        TurretData_Interpolate {
            parent: Handle::default(),
            turret_index: 0,
            rotation: Quat::default(),
            flags: 0,
        }
    }
}

pub const TURRET_DATA_DEEP_MAX_BITS: u64 = 350;
pub const TURRET_DATA_DEEP_MAX_BYTES: usize = 48; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_turret_data_deep(stream: &mut WriteStream<'_>, value: &TurretData_Deep) -> Result {
    write_handle(stream, &value.parent)?;
    if value.turret_index < 0 || value.turret_index > 255 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.turret_index as u32;
        stream.serialize_bits(&mut offset_value, 8)?;
    }
    write_quat(stream, &value.rotation)?;
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    Ok(())
}

#[inline]
pub fn read_turret_data_deep(stream: &mut ReadStream<'_>, value: &mut TurretData_Deep) -> Result {
    read_handle(stream, &mut value.parent)?;
    stream.serialize_int(&mut value.turret_index, 0, (MAX_TURRETS_PER_SHIP - 1) as i32)?;
    read_quat(stream, &mut value.rotation)?;
    stream.serialize_bits64(&mut value.flags, 64)?;
    Ok(())
}

pub const TURRET_DATA_SHALLOW_MAX_BITS: u64 = 142;
pub const TURRET_DATA_SHALLOW_MAX_BYTES: usize = 24; // rounded up to the 8-byte write-buffer granularity

#[inline]
pub fn write_turret_data_shallow(stream: &mut WriteStream<'_>, value: &TurretData_Shallow) -> Result {
    write_handle(stream, &value.parent)?;
    if value.turret_index < 0 || value.turret_index > 255 { // out-of-contract writes are refused, not wrapped
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.turret_index as u32;
        stream.serialize_bits(&mut offset_value, 8)?;
    }
    debug_assert!(value.rotation_x >= -1024 && value.rotation_x <= 1024);
    {
        let mut offset_value = (value.rotation_x as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_y >= -1024 && value.rotation_y <= 1024);
    {
        let mut offset_value = (value.rotation_y as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_z >= -1024 && value.rotation_z <= 1024);
    {
        let mut offset_value = (value.rotation_z as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    debug_assert!(value.rotation_w >= -1024 && value.rotation_w <= 1024);
    {
        let mut offset_value = (value.rotation_w as u32).wrapping_sub((-1024_i32) as u32);
        stream.serialize_bits(&mut offset_value, 12)?;
    }
    {
        let mut raw_value = value.flags;
        stream.serialize_bits64(&mut raw_value, 64)?;
    }
    Ok(())
}

#[inline]
pub fn read_turret_data_shallow(stream: &mut ReadStream<'_>, value: &mut TurretData_Shallow) -> Result {
    read_handle(stream, &mut value.parent)?;
    stream.serialize_int(&mut value.turret_index, 0, (MAX_TURRETS_PER_SHIP - 1) as i32)?;
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_x = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_y = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_z = component_value as i16;
    }
    {
        let mut component_value: i32 = 0;
        stream.serialize_int(&mut component_value, -1024, 1024)?;
        value.rotation_w = component_value as i16;
    }
    stream.serialize_bits64(&mut value.flags, 64)?;
    Ok(())
}

pub fn quantize_turret(input: &TurretData_Interpolate, output: &mut TurretData_Shallow) {
    output.parent = input.parent;
    output.turret_index = input.turret_index;
    {
        let quantized_value = (input.rotation.x * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_x = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.y * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_y = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.z * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_z = component_value as i16;
    }
    {
        let quantized_value = (input.rotation.w * ROTATION_UNITS as f64 + 0.5).floor();
        let component_value: i64 = if quantized_value > 1024.0_f64 {
            1024
        } else if quantized_value >= -1024.0_f64 {
            quantized_value as i64
        } else {
            -1024
        };
        output.rotation_w = component_value as i16;
    }
    output.flags = input.flags;
}

pub fn unquantize_turret(input: &TurretData_Shallow, output: &mut TurretData_Interpolate) {
    output.parent = input.parent;
    output.turret_index = input.turret_index;
    output.rotation.x = input.rotation_x as f64 / ROTATION_UNITS as f64;
    output.rotation.y = input.rotation_y as f64 / ROTATION_UNITS as f64;
    output.rotation.z = input.rotation_z as f64 / ROTATION_UNITS as f64;
    output.rotation.w = input.rotation_w as f64 / ROTATION_UNITS as f64;
    output.flags = input.flags;
}

// The object tag wire: ObjectType in [0, 4], minimal bits; None = 0 is the
// null — the sentinel the surveyed baseline streams terminate with (SPEC §4.8).
#[inline]
pub fn write_object_type(stream: &mut WriteStream<'_>, value: ObjectType) -> Result {
    debug_assert!(value.0 as u32 <= 4); // the runtime ranged form's write assert, kept (debug parity)
    {
        let mut offset_value = value.0 as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_object_type(stream: &mut ReadStream<'_>, value: &mut ObjectType) -> Result {
    let mut tag_value: i32 = 0;
    stream.serialize_int(&mut tag_value, 0, 4)?;
    *value = ObjectType(tag_value as u8);
    Ok(())
}

