// Code generated by the schema compiler from Clauses.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

// type W13
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W13 {
    pub items: [u16; 12], // used count beside it; wire count in [0, 12]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W13 {
    fn default() -> Self {
        W13 {
            items: [0; 12],
            items_count: 0,
        }
    }
}

// W13_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W13_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W13_MAX_BITS: u64 = 160;
pub const W13_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_w13(stream: &mut WriteStream<'_>, value: &W13) -> Result {
    if value.items_count < 0 || value.items_count > 12 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 4)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 8191 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i] as u32;
            stream.serialize_bits(&mut offset_value, 13)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w13(stream: &mut ReadStream<'_>, value: &mut W13) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 12)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i32 = 0;
            stream.serialize_int(&mut range_value, 0, 8191)?;
            value.items[i] = range_value as u16;
        }
    }
    Ok(())
}

// type W17
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W17 {
    pub items: [u32; 9], // used count beside it; wire count in [0, 9]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W17 {
    fn default() -> Self {
        W17 {
            items: [0; 9],
            items_count: 0,
        }
    }
}

// W17_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W17_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W17_MAX_BITS: u64 = 157;
pub const W17_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_w17(stream: &mut WriteStream<'_>, value: &W17) -> Result {
    if value.items_count < 0 || value.items_count > 9 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 4)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 131071 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i];
            stream.serialize_bits(&mut offset_value, 17)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w17(stream: &mut ReadStream<'_>, value: &mut W17) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 9)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i32 = 0;
            stream.serialize_int(&mut range_value, 0, 131071)?;
            value.items[i] = range_value as u32;
        }
    }
    Ok(())
}

// type W26
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W26 {
    pub items: [u32; 6], // used count beside it; wire count in [0, 6]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W26 {
    fn default() -> Self {
        W26 {
            items: [0; 6],
            items_count: 0,
        }
    }
}

// W26_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W26_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W26_MAX_BITS: u64 = 159;
pub const W26_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_w26(stream: &mut WriteStream<'_>, value: &W26) -> Result {
    if value.items_count < 0 || value.items_count > 6 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 3)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 67108863 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i];
            stream.serialize_bits(&mut offset_value, 26)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w26(stream: &mut ReadStream<'_>, value: &mut W26) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 6)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i32 = 0;
            stream.serialize_int(&mut range_value, 0, 67108863)?;
            value.items[i] = range_value as u32;
        }
    }
    Ok(())
}

// type W1
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W1 {
    pub items: [u8; 20], // used count beside it; wire count in [0, 20]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W1 {
    fn default() -> Self {
        W1 {
            items: [0; 20],
            items_count: 0,
        }
    }
}

// W1_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W1_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W1_MAX_BITS: u64 = 25;
pub const W1_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_w1(stream: &mut WriteStream<'_>, value: &W1) -> Result {
    if value.items_count < 0 || value.items_count > 20 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 5)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 1 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i] as u32;
            stream.serialize_bits(&mut offset_value, 1)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w1(stream: &mut ReadStream<'_>, value: &mut W1) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 20)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i32 = 0;
            stream.serialize_int(&mut range_value, 0, 1)?;
            value.items[i] = range_value as u8;
        }
    }
    Ok(())
}

// type W52
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W52 {
    pub items: [u64; 3], // used count beside it; wire count in [0, 3]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W52 {
    fn default() -> Self {
        W52 {
            items: [0; 3],
            items_count: 0,
        }
    }
}

// W52_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W52_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W52_MAX_BITS: u64 = 158;
pub const W52_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_w52(stream: &mut WriteStream<'_>, value: &W52) -> Result {
    if value.items_count < 0 || value.items_count > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 2)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 4503599627370495 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i];
            stream.serialize_bits64(&mut offset_value, 52)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w52(stream: &mut ReadStream<'_>, value: &mut W52) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 3)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i64 = 0;
            stream.serialize_int64(&mut range_value, 0, 4503599627370495)?;
            value.items[i] = range_value as u64;
        }
    }
    Ok(())
}

// type W50
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct W50 {
    pub items: [u64; 3], // used count beside it; wire count in [0, 3]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for W50 {
    fn default() -> Self {
        W50 {
            items: [0; 3],
            items_count: 0,
        }
    }
}

// W50_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// W50_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const W50_MAX_BITS: u64 = 152;
pub const W50_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_w50(stream: &mut WriteStream<'_>, value: &W50) -> Result {
    if value.items_count < 0 || value.items_count > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 2)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        if value.items[i] > 1125899906842623 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i];
            stream.serialize_bits64(&mut offset_value, 50)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_w50(stream: &mut ReadStream<'_>, value: &mut W50) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 3)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        {
            let mut range_value: i64 = 0;
            stream.serialize_int64(&mut range_value, 0, 1125899906842623)?;
            value.items[i] = range_value as u64;
        }
    }
    Ok(())
}

// type F13
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct F13 {
    pub items: [u16; 7], // wire [0, 8191]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for F13 {
    fn default() -> Self {
        F13 {
            items: [0; 7],
        }
    }
}

// F13_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// F13_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const F13_MAX_BITS: u64 = 91;
pub const F13_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_f13(stream: &mut WriteStream<'_>, value: &F13) -> Result {
    for i in 0..7 {
        if value.items[i] > 8191 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut offset_value = value.items[i] as u32;
            stream.serialize_bits(&mut offset_value, 13)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_f13(stream: &mut ReadStream<'_>, value: &mut F13) -> Result {
    for i in 0..7 {
        {
            let mut range_value: i32 = 0;
            stream.serialize_int(&mut range_value, 0, 8191)?;
            value.items[i] = range_value as u16;
        }
    }
    Ok(())
}

// type Tri3
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Tri3 {
    pub a: u32,
    pub b: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Tri3 {
    fn default() -> Self {
        Tri3 {
            a: 0,
            b: 0,
        }
    }
}

// TRI3_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TRI3_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TRI3_MAX_BITS: u64 = 3;
pub const TRI3_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_tri3(stream: &mut WriteStream<'_>, value: &Tri3) -> Result {
    if value.a >= 1 << 1 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.b >= 1 << 2 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.a);
    let f1: u64 = u64::from(value.b);
    let mut w0 = (f0 | (f1 << 1)) as u32;
    stream.serialize_bits(&mut w0, 3)?;
    Ok(())
}

#[inline]
pub fn read_tri3(stream: &mut ReadStream<'_>, value: &mut Tri3) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 3)?;
    let c0 = u64::from(c);
    let v0: u64 = c0 & 0x1;
    value.a = v0 as u32;
    let v1: u64 = (c0 >> 1) & 0x3;
    value.b = v1 as u32;
    Ok(())
}

// type ArrTri3
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ArrTri3 {
    pub items: [Tri3; 10], // used count beside it; wire count in [0, 10]
    pub items_count: i32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ArrTri3 {
    fn default() -> Self {
        ArrTri3 {
            items: [Tri3::default(); 10],
            items_count: 0,
        }
    }
}

// ARR_TRI3_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// ARR_TRI3_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const ARR_TRI3_MAX_BITS: u64 = 34;
pub const ARR_TRI3_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_arr_tri3(stream: &mut WriteStream<'_>, value: &ArrTri3) -> Result {
    if value.items_count < 0 || value.items_count > 10 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 4)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        write_tri3(stream, &value.items[i])?;
    }
    Ok(())
}

#[inline]
pub fn read_arr_tri3(stream: &mut ReadStream<'_>, value: &mut ArrTri3) -> Result {
    stream.serialize_int(&mut value.items_count, 0, 10)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        read_tri3(stream, &mut value.items[i])?;
    }
    Ok(())
}

// type Eleven
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Eleven {
    pub a: u32,
    pub b: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Eleven {
    fn default() -> Self {
        Eleven {
            a: 0,
            b: 0,
        }
    }
}

// ELEVEN_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// ELEVEN_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const ELEVEN_MAX_BITS: u64 = 11;
pub const ELEVEN_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_eleven(stream: &mut WriteStream<'_>, value: &Eleven) -> Result {
    if value.a >= 1 << 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.b >= 1 << 8 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.a);
    let f1: u64 = u64::from(value.b);
    let mut w0 = (f0 | (f1 << 3)) as u32;
    stream.serialize_bits(&mut w0, 11)?;
    Ok(())
}

#[inline]
pub fn read_eleven(stream: &mut ReadStream<'_>, value: &mut Eleven) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 11)?;
    let c0 = u64::from(c);
    let v0: u64 = c0 & 0x7;
    value.a = v0 as u32;
    let v1: u64 = (c0 >> 3) & 0xff;
    value.b = v1 as u32;
    Ok(())
}

// type ArrEleven
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ArrEleven {
    pub items: [Eleven; 9],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ArrEleven {
    fn default() -> Self {
        ArrEleven {
            items: [Eleven::default(); 9],
        }
    }
}

// ARR_ELEVEN_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// ARR_ELEVEN_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const ARR_ELEVEN_MAX_BITS: u64 = 99;
pub const ARR_ELEVEN_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_arr_eleven(stream: &mut WriteStream<'_>, value: &ArrEleven) -> Result {
    for i in 0..9 {
        write_eleven(stream, &value.items[i])?;
    }
    Ok(())
}

#[inline]
pub fn read_arr_eleven(stream: &mut ReadStream<'_>, value: &mut ArrEleven) -> Result {
    for i in 0..9 {
        read_eleven(stream, &mut value.items[i])?;
    }
    Ok(())
}

// type EmptyA
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct EmptyA {}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for EmptyA {
    fn default() -> Self {
        EmptyA {}
    }
}

// EMPTY_A_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// EMPTY_A_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const EMPTY_A_MAX_BITS: u64 = 0;
pub const EMPTY_A_MAX_BYTES: usize = 0;

#[inline(always)]
pub fn write_empty_a(stream: &mut WriteStream<'_>, value: &EmptyA) -> Result {
    let _ = (stream, value); // empty body — presence is the payload (SPEC §4.6)
    Ok(())
}

#[inline]
pub fn read_empty_a(stream: &mut ReadStream<'_>, value: &mut EmptyA) -> Result {
    let _ = (stream, value);
    Ok(())
}

// type EmptyB
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct EmptyB {}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for EmptyB {
    fn default() -> Self {
        EmptyB {}
    }
}

// EMPTY_B_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// EMPTY_B_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const EMPTY_B_MAX_BITS: u64 = 0;
pub const EMPTY_B_MAX_BYTES: usize = 0;

#[inline(always)]
pub fn write_empty_b(stream: &mut WriteStream<'_>, value: &EmptyB) -> Result {
    let _ = (stream, value); // empty body — presence is the payload (SPEC §4.6)
    Ok(())
}

#[inline]
pub fn read_empty_b(stream: &mut ReadStream<'_>, value: &mut EmptyB) -> Result {
    let _ = (stream, value);
    Ok(())
}

// EmptyUnionType: union EmptyUnion's tag — None = 0, then each variant in declared order (SPEC §4.8)
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct EmptyUnionType(pub u8);

impl EmptyUnionType {
    pub const NONE: EmptyUnionType = EmptyUnionType(0);
    pub const A: EmptyUnionType = EmptyUnionType(1);
    pub const B: EmptyUnionType = EmptyUnionType(2);
    pub const COUNT: EmptyUnionType = EmptyUnionType(2); // the declared variant count (SPEC §4.2)
    pub const MAX: EmptyUnionType = EmptyUnionType(2); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `EmptyUnionType` value, out-of-set included.
pub fn enum_name_empty_union_type(value: EmptyUnionType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "A",
        2 => "B",
        _ => "???",
    }
}

// EmptyUnion — at most one of the arms. The default is None (the empty union);
// every read reconstructs the selected payload with its declared initial
// values, even at the same tag, and replaces the whole value on success.
#[derive(Clone, Copy, PartialEq, Debug, Default)]
pub enum EmptyUnion {
    #[default]
    None,
    A(EmptyA),
    B(EmptyB),
}

// EMPTY_UNION_MAX_BITS is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// EMPTY_UNION_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const EMPTY_UNION_MAX_BITS: u64 = 2;
pub const EMPTY_UNION_MAX_BYTES: usize = 8;

pub fn write_empty_union(stream: &mut WriteStream<'_>, value: &EmptyUnion) -> Result {
    match value {
        EmptyUnion::None => {
            let mut offset_value: u32 = 0;
            stream.serialize_bits(&mut offset_value, 2)?;
            Ok(()) // no payload — the tag is the whole wire (SPEC §4.8)
        }
        EmptyUnion::A(arm) => {
            let mut offset_value: u32 = 1;
            stream.serialize_bits(&mut offset_value, 2)?;
            write_empty_a(stream, arm)
        }
        EmptyUnion::B(arm) => {
            let mut offset_value: u32 = 2;
            stream.serialize_bits(&mut offset_value, 2)?;
            write_empty_b(stream, arm)
        }
    }
}

pub fn read_empty_union(stream: &mut ReadStream<'_>, value: &mut EmptyUnion) -> Result {
    let mut tag_value: i32 = 0;
    stream.serialize_int(&mut tag_value, 0, 2)?; // rejects a tag above the count (SPEC §4.8)
    match tag_value {
        1 => {
            let mut arm = EmptyA::default();
            read_empty_a(stream, &mut arm)?;
            *value = EmptyUnion::A(arm);
        }
        2 => {
            let mut arm = EmptyB::default();
            read_empty_b(stream, &mut arm)?;
            *value = EmptyUnion::B(arm);
        }
        _ => *value = EmptyUnion::None,
    }
    Ok(())
}

// type HoldsEmptyUnion
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct HoldsEmptyUnion {
    pub lead: u32,
    pub u: EmptyUnion,
    pub tail: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for HoldsEmptyUnion {
    fn default() -> Self {
        HoldsEmptyUnion {
            lead: 0,
            u: EmptyUnion::None,
            tail: 0,
        }
    }
}

// HOLDS_EMPTY_UNION_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// HOLDS_EMPTY_UNION_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const HOLDS_EMPTY_UNION_MAX_BITS: u64 = 14;
pub const HOLDS_EMPTY_UNION_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_holds_empty_union(stream: &mut WriteStream<'_>, value: &HoldsEmptyUnion) -> Result {
    if value.lead >= 1 << 5 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.lead;
        stream.serialize_bits(&mut raw_value, 5)?;
    }
    write_empty_union(stream, &value.u)?;
    if value.tail >= 1 << 7 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.tail;
        stream.serialize_bits(&mut raw_value, 7)?;
    }
    Ok(())
}

#[inline]
pub fn read_holds_empty_union(stream: &mut ReadStream<'_>, value: &mut HoldsEmptyUnion) -> Result {
    stream.serialize_bits(&mut value.lead, 5)?;
    read_empty_union(stream, &mut value.u)?;
    stream.serialize_bits(&mut value.tail, 7)?;
    Ok(())
}

// type Strs
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Strs {
    pub lead: u32,
    pub s: [u8; 8], // string(8): max length, used length beside it (SPEC §4.7)
    pub s_length: i32,
    pub b: [u8; 8], // bytes(8): fixed buffer, used length beside it (SPEC §4.7)
    pub b_length: i32,
    pub tail: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Strs {
    fn default() -> Self {
        Strs {
            lead: 0,
            s: [0; 8],
            s_length: 0,
            b: [0; 8],
            b_length: 0,
            tail: 0,
        }
    }
}

// STRS_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// STRS_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const STRS_MAX_BITS: u64 = 158;
pub const STRS_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_strs(stream: &mut WriteStream<'_>, value: &Strs) -> Result {
    if value.lead >= 1 << 5 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.lead;
        stream.serialize_bits(&mut raw_value, 5)?;
    }
    if value.s_length < 0 || value.s_length > 8 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    debug_assert!(
        std::str::from_utf8(&value.s[..value.s_length as usize]).is_ok(),
        "string(N) payloads are well-formed UTF-8 by contract (SPEC 4.7)"
    );
    {
        let mut offset_value = value.s_length as u32;
        stream.serialize_bits(&mut offset_value, 4)?; // the length guards the slice (§6.3)
    }
    stream.write_bytes(&value.s[..value.s_length as usize]); // borrowed in place: the write side never mutates (infallible: returns () in serialize.rs 2.0.0)
    if value.b_length < 0 || value.b_length > 8 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.b_length as u32;
        stream.serialize_bits(&mut offset_value, 4)?; // the length guards the slice (§6.3)
    }
    stream.write_bytes(&value.b[..value.b_length as usize]); // borrowed in place: the write side never mutates (infallible: returns () in serialize.rs 2.0.0)
    if value.tail >= 1 << 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.tail;
        stream.serialize_bits(&mut raw_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_strs(stream: &mut ReadStream<'_>, value: &mut Strs) -> Result {
    stream.serialize_bits(&mut value.lead, 5)?;
    stream.serialize_int(&mut value.s_length, 0, 8)?; // the length guards the slice (§6.3)
    stream.serialize_bytes(&mut value.s[..value.s_length as usize])?;
    for i in 0..value.s_length as usize {
        if value.s[i] == 0 {
            return Err(Error::Validation);
        }
    }
    stream.serialize_int(&mut value.b_length, 0, 8)?; // the length guards the slice (§6.3)
    stream.serialize_bytes(&mut value.b[..value.b_length as usize])?;
    stream.serialize_bits(&mut value.tail, 3)?;
    Ok(())
}

// type ArrNested
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct ArrNested {
    pub lead: u32,
    pub items: [Eleven; 4], // used count beside it; wire count in [0, 4]
    pub items_count: i32,
    pub tail: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for ArrNested {
    fn default() -> Self {
        ArrNested {
            lead: 0,
            items: [Eleven::default(); 4],
            items_count: 0,
            tail: 0,
        }
    }
}

// ARR_NESTED_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// ARR_NESTED_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const ARR_NESTED_MAX_BITS: u64 = 55;
pub const ARR_NESTED_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_arr_nested(stream: &mut WriteStream<'_>, value: &ArrNested) -> Result {
    if value.lead >= 1 << 5 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.lead;
        stream.serialize_bits(&mut raw_value, 5)?;
    }
    if value.items_count < 0 || value.items_count > 4 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.items_count as u32;
        stream.serialize_bits(&mut offset_value, 3)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.items_count as usize {
        write_eleven(stream, &value.items[i])?;
    }
    if value.tail >= 1 << 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.tail;
        stream.serialize_bits(&mut raw_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_arr_nested(stream: &mut ReadStream<'_>, value: &mut ArrNested) -> Result {
    stream.serialize_bits(&mut value.lead, 5)?;
    stream.serialize_int(&mut value.items_count, 0, 4)?; // the count guards the loop (§6.3)
    for i in 0..value.items_count as usize {
        read_eleven(stream, &mut value.items[i])?;
    }
    stream.serialize_bits(&mut value.tail, 3)?;
    Ok(())
}

// type Sole
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Sole {
    pub only: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Sole {
    fn default() -> Self {
        Sole {
            only: 0,
        }
    }
}

// SOLE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SOLE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SOLE_MAX_BITS: u64 = 13;
pub const SOLE_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_sole(stream: &mut WriteStream<'_>, value: &Sole) -> Result {
    if value.only >= 1 << 13 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.only;
        stream.serialize_bits(&mut raw_value, 13)?;
    }
    Ok(())
}

#[inline]
pub fn read_sole(stream: &mut ReadStream<'_>, value: &mut Sole) -> Result {
    stream.serialize_bits(&mut value.only, 13)?;
    Ok(())
}

