// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

// type VoidPulse
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct VoidPulse {
    pub strength: u16,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for VoidPulse {
    fn default() -> Self {
        VoidPulse {
            strength: 0,
        }
    }
}

// VOID_PULSE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// VOID_PULSE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const VOID_PULSE_MAX_BITS: u64 = 16;
pub const VOID_PULSE_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_void_pulse(stream: &mut WriteStream<'_>, value: &VoidPulse) -> Result {
    {
        let mut raw_value = value.strength as u32;
        stream.serialize_bits(&mut raw_value, 16)?;
    }
    Ok(())
}

#[inline]
pub fn read_void_pulse(stream: &mut ReadStream<'_>, value: &mut VoidPulse) -> Result {
    {
        let mut raw_value: u32 = 0;
        stream.serialize_bits(&mut raw_value, 16)?;
        value.strength = raw_value as u16;
    }
    Ok(())
}

// type VoidLevel
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct VoidLevel {
    pub level: u8, // wire [0, 7]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for VoidLevel {
    fn default() -> Self {
        VoidLevel {
            level: 0,
        }
    }
}

// VOID_LEVEL_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// VOID_LEVEL_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const VOID_LEVEL_MAX_BITS: u64 = 3;
pub const VOID_LEVEL_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_void_level(stream: &mut WriteStream<'_>, value: &VoidLevel) -> Result {
    debug_assert!(value.level <= 7, "level out of range [0, 7]");
    {
        let mut offset_value = value.level as u32;
        stream.serialize_bits(&mut offset_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_void_level(stream: &mut ReadStream<'_>, value: &mut VoidLevel) -> Result {
    {
        let mut range_value: i32 = 0;
        stream.serialize_int(&mut range_value, 0, 7)?;
        value.level = range_value as u8;
    }
    Ok(())
}

// VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct VoidSignalType(pub u8);

impl VoidSignalType {
    pub const NONE: VoidSignalType = VoidSignalType(0);
    pub const PING: VoidSignalType = VoidSignalType(1);
    pub const PULSE: VoidSignalType = VoidSignalType(2);
    pub const LEVEL: VoidSignalType = VoidSignalType(3);
    pub const ACK: VoidSignalType = VoidSignalType(4);
    pub const COUNT: VoidSignalType = VoidSignalType(4); // the declared variant count (SPEC §4.2)
    pub const MAX: VoidSignalType = VoidSignalType(4); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `VoidSignalType` value, out-of-set included.
pub fn enum_name_void_signal_type(value: VoidSignalType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Ping",
        2 => "Pulse",
        3 => "Level",
        4 => "Ack",
        _ => "???",
    }
}

// VoidSignal — at most one of the arms. The default is None (the empty union);
// every read reconstructs the selected payload with its declared initial
// values, even at the same tag, and replaces the whole value on success.
#[derive(Clone, Copy, PartialEq, Debug, Default)]
pub enum VoidSignal {
    #[default]
    None,
    Ping,
    Pulse(VoidPulse),
    Level(VoidLevel),
    Ack,
}

// VOID_SIGNAL_MAX_BITS is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// VOID_SIGNAL_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const VOID_SIGNAL_MAX_BITS: u64 = 19;
pub const VOID_SIGNAL_MAX_BYTES: usize = 8;

pub fn write_void_signal(stream: &mut WriteStream<'_>, value: &VoidSignal) -> Result {
    match value {
        VoidSignal::None => {
            let mut offset_value: u32 = 0;
            stream.serialize_bits(&mut offset_value, 3)?;
            Ok(()) // no payload — the tag is the whole wire (SPEC §4.8)
        }
        VoidSignal::Ping => {
            let mut offset_value: u32 = 1;
            stream.serialize_bits(&mut offset_value, 3)?;
            Ok(()) // a payload-free arm: the tag is the whole wire (SPEC §4.8)
        }
        VoidSignal::Pulse(arm) => {
            let mut offset_value: u32 = 2;
            stream.serialize_bits(&mut offset_value, 3)?;
            write_void_pulse(stream, arm)
        }
        VoidSignal::Level(arm) => {
            let mut offset_value: u32 = 3;
            stream.serialize_bits(&mut offset_value, 3)?;
            write_void_level(stream, arm)
        }
        VoidSignal::Ack => {
            let mut offset_value: u32 = 4;
            stream.serialize_bits(&mut offset_value, 3)?;
            Ok(()) // a payload-free arm: the tag is the whole wire (SPEC §4.8)
        }
    }
}

pub fn read_void_signal(stream: &mut ReadStream<'_>, value: &mut VoidSignal) -> Result {
    let mut tag_value: i32 = 0;
    stream.serialize_int(&mut tag_value, 0, 4)?; // rejects a tag above the count (SPEC §4.8)
    match tag_value {
        1 => *value = VoidSignal::Ping, // a payload-free arm: the tag is the whole wire (SPEC §4.8)
        2 => {
            let mut arm = VoidPulse::default();
            read_void_pulse(stream, &mut arm)?;
            *value = VoidSignal::Pulse(arm);
        }
        3 => {
            let mut arm = VoidLevel::default();
            read_void_level(stream, &mut arm)?;
            *value = VoidSignal::Level(arm);
        }
        4 => *value = VoidSignal::Ack, // a payload-free arm: the tag is the whole wire (SPEC §4.8)
        _ => *value = VoidSignal::None,
    }
    Ok(())
}

// type VoidSignalPacket
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct VoidSignalPacket {
    pub lead: u32,
    pub signal: VoidSignal,
    pub echo: VoidSignal,
    pub history: [VoidSignal; 2], // used count beside it; wire count in [0, 2]
    pub history_count: i32,
    pub tail: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for VoidSignalPacket {
    fn default() -> Self {
        VoidSignalPacket {
            lead: 0,
            signal: VoidSignal::None,
            echo: VoidSignal::None,
            history: [VoidSignal::None; 2],
            history_count: 0,
            tail: 0,
        }
    }
}

// VOID_SIGNAL_PACKET_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// VOID_SIGNAL_PACKET_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const VOID_SIGNAL_PACKET_MAX_BITS: u64 = 84;
pub const VOID_SIGNAL_PACKET_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_void_signal_packet(stream: &mut WriteStream<'_>, value: &VoidSignalPacket) -> Result {
    debug_assert!(value.lead < 1 << 3, "lead above the bits(3) wire width");
    {
        let mut raw_value = value.lead;
        stream.serialize_bits(&mut raw_value, 3)?;
    }
    write_void_signal(stream, &value.signal)?;
    write_void_signal(stream, &value.echo)?;
    debug_assert!(value.history_count >= 0 && value.history_count <= 2, "history_count out of range [0, 2]");
    {
        let mut offset_value = value.history_count as u32;
        stream.serialize_bits(&mut offset_value, 2)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.history_count as usize {
        write_void_signal(stream, &value.history[i])?;
    }
    debug_assert!(value.tail < 1 << 3, "tail above the bits(3) wire width");
    {
        let mut raw_value = value.tail;
        stream.serialize_bits(&mut raw_value, 3)?;
    }
    Ok(())
}

#[inline]
pub fn read_void_signal_packet(stream: &mut ReadStream<'_>, value: &mut VoidSignalPacket) -> Result {
    stream.serialize_bits(&mut value.lead, 3)?;
    read_void_signal(stream, &mut value.signal)?;
    read_void_signal(stream, &mut value.echo)?;
    stream.serialize_int(&mut value.history_count, 0, 2)?; // the count guards the loop (§6.3)
    for i in 0..value.history_count as usize {
        read_void_signal(stream, &mut value.history[i])?;
    }
    stream.serialize_bits(&mut value.tail, 3)?;
    Ok(())
}

