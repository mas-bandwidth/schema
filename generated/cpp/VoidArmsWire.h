// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

#pragma once

#include <cstdint>
#include <new>

#include "serialize.h"

#include "VoidArms.h"

namespace example {

#ifndef SCHEMA_WRITE_INLINE_DEFINED
#define SCHEMA_WRITE_INLINE_DEFINED
// SCHEMA_WRITE_INLINE — how every generated Write function is spelled: an
// inlining DEMAND (always_inline / __forceinline), the serialize family's
// remedy for LLVM refusing the linkonce_odr Write entries into their call
// sites at cost over threshold — no last-call-to-static bonus exists for a
// header function, so unlike C's static entries these never flatten on
// their own. Measured and shipped; branch-weight hints are NOT the fix
// here — measured in this family to invite the machine outliner into the
// hot bodies. Do not add them.
#if defined( _MSC_VER )
#define SCHEMA_WRITE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_WRITE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_WRITE_INLINE inline
#endif
#endif // SCHEMA_WRITE_INLINE_DEFINED

#ifndef SCHEMA_READ_INLINE_DEFINED
#define SCHEMA_READ_INLINE_DEFINED
// SCHEMA_READ_INLINE — how every generated Read function is spelled: an
// inlining DEMAND (always_inline / __forceinline), the serialize family's
// remedy for LLVM's fallible-chain frequency decay: each Ok/Err split is
// priced at ~even odds, block frequency decays geometrically down a read
// chain, and later call sites are held to the cold-callsite inline
// threshold. Measured and shipped; branch-weight hints are NOT the fix
// here — measured in this family to invite the machine outliner into the
// hot bodies. Do not add them.
#if defined( _MSC_VER )
#define SCHEMA_READ_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_READ_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_READ_INLINE inline
#endif
#endif // SCHEMA_READ_INLINE_DEFINED

SCHEMA_WRITE_INLINE bool WriteVoidPulse( serialize::WriteStream & stream, const VoidPulse & value )
{
    write_bits( stream, value.strength, 16 );
    return true;
}

SCHEMA_READ_INLINE bool ReadVoidPulse( serialize::ReadStream & stream, VoidPulse & value )
{
    {
        uint32_t raw_value = 0;
        read_bits( stream, raw_value, 16 );
        value.strength = uint16_t( raw_value );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteVoidLevel( serialize::WriteStream & stream, const VoidLevel & value )
{
    serialize_assert( int32_t( value.level ) >= int32_t( 0 ) && int32_t( value.level ) <= int32_t( 7 ) );
    write_bits( stream, uint32_t( value.level ), 3 );
    return true;
}

SCHEMA_READ_INLINE bool ReadVoidLevel( serialize::ReadStream & stream, VoidLevel & value )
{
    {
        int32_t range_value = 0;
        read_int( stream, range_value, 0, 7 );
        value.level = uint8_t( range_value );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteVoidSignal( serialize::WriteStream & stream, const VoidSignal & value )
{
    serialize_assert( value.type <= VoidSignalType::Max ); // an out-of-set tag is caller error (SPEC §4.8, §5)
    switch ( value.type )
    {
        case VoidSignalType::None:
            write_bits( stream, 0u, 3 );
            return true; // no payload — the tag is the whole wire (SPEC §4.8)
        case VoidSignalType::Ping:
            write_bits( stream, 1u, 3 );
            return true; // payload-free arm — the tag is the whole wire (SPEC §4.8)
        case VoidSignalType::Pulse:
            write_bits( stream, 2u, 3 );
            return WriteVoidPulse( stream, value.pulse );
        case VoidSignalType::Level:
            write_bits( stream, 3u, 3 );
            return WriteVoidLevel( stream, value.level );
        case VoidSignalType::Ack:
            write_bits( stream, 4u, 3 );
            return true; // payload-free arm — the tag is the whole wire (SPEC §4.8)
        default:
            break;
    }
    return true; // an out-of-set tag selected no arm, so no bits rode: the assert above is the contract (SPEC §5)
}

SCHEMA_READ_INLINE bool ReadVoidSignal( serialize::ReadStream & stream, VoidSignal & value )
{
    int32_t tag_value = 0;
    read_int( stream, tag_value, 0, 4 ); // rejects a tag above the count (SPEC §4.8)
    value.type = VoidSignalType( tag_value );
    switch ( value.type )
    {
        case VoidSignalType::None:
            return true;
        case VoidSignalType::Ping:
            return true; // payload-free arm — the tag is the whole wire (SPEC §4.8)
        case VoidSignalType::Pulse:
            ::new ( (void*) &value.pulse ) VoidPulse{};
            return ReadVoidPulse( stream, value.pulse );
        case VoidSignalType::Level:
            ::new ( (void*) &value.level ) VoidLevel{};
            return ReadVoidLevel( stream, value.level );
        case VoidSignalType::Ack:
            return true; // payload-free arm — the tag is the whole wire (SPEC §4.8)
    }
    return false;
}

SCHEMA_WRITE_INLINE bool WriteVoidSignalPacket( serialize::WriteStream & stream, const VoidSignalPacket & value )
{
    write_bits( stream, value.lead, 3 );
    if ( !WriteVoidSignal( stream, value.signal ) )
    {
        return false;
    }
    if ( !WriteVoidSignal( stream, value.echo ) )
    {
        return false;
    }
    serialize_assert( int32_t( value.history_count ) >= int32_t( 0 ) && int32_t( value.history_count ) <= int32_t( 2 ) );
    write_bits( stream, uint32_t( value.history_count ), 2 );
    for ( int32_t i = 0; i < value.history_count; i++ )
    {
        if ( !WriteVoidSignal( stream, value.history[i] ) )
        {
            return false;
        }
    }
    write_bits( stream, value.tail, 3 );
    return true;
}

SCHEMA_READ_INLINE bool ReadVoidSignalPacket( serialize::ReadStream & stream, VoidSignalPacket & value )
{
    read_bits( stream, value.lead, 3 );
    if ( !ReadVoidSignal( stream, value.signal ) )
    {
        return false;
    }
    if ( !ReadVoidSignal( stream, value.echo ) )
    {
        return false;
    }
    read_int( stream, value.history_count, 0, 2 );
    for ( int32_t i = 0; i < value.history_count; i++ )
    {
        if ( !ReadVoidSignal( stream, value.history[i] ) )
        {
            return false;
        }
    }
    read_bits( stream, value.tail, 3 );
    return true;
}

} // namespace example
