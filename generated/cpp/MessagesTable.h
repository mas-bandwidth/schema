// Generated by the schema compiler from Messages.schema. DO NOT EDIT.
// package example — protocol id 0xaacab4aa04d2189b
// The TABLE wire (evolution-tolerant, notes/table-wire.md): no serialize
// dependency — includable from any TU.

#pragma once

#include <cstdint>
#include <cstring>

#include "Messages.h"

#ifndef EXAMPLE_SCHEMA_TABLE_PRIMITIVES
#define EXAMPLE_SCHEMA_TABLE_PRIMITIVES

namespace example {

// The table-wire read report — the permissive contract's ledger. Silence
// (all zero) means the data matched this reader's schema exactly.
struct TableReport
{
    int32_t unknown = 0;       // unknown field ids skipped (newer data)
    int32_t kind_mismatch = 0; // known id, changed type — skipped, never misdecoded
    int32_t clamped = 0;       // out-of-range values clamped to declared bounds
    bool malformed = false;    // framing damage; decode stopped, partial result kept
};

struct TableWriter
{
    uint8_t * buffer;
    int64_t capacity;
    int64_t offset = 0;
    bool overflow = false;

    TableWriter( uint8_t * buffer, int64_t capacity ) : buffer( buffer ), capacity( capacity ) {}

    void raw( const void * data, int64_t bytes )
    {
        if ( offset + bytes > capacity ) { overflow = true; return; }
        memcpy( buffer + offset, data, (size_t) bytes );
        offset += bytes;
    }
    void put8( uint8_t v )   { raw( &v, 1 ); }
    void put16( uint16_t v ) { uint8_t b[2] = { uint8_t( v ), uint8_t( v >> 8 ) }; raw( b, 2 ); }
    void put32( uint32_t v ) { uint8_t b[4] = { uint8_t( v ), uint8_t( v >> 8 ), uint8_t( v >> 16 ), uint8_t( v >> 24 ) }; raw( b, 4 ); }
    void put64( uint64_t v ) { put32( uint32_t( v ) ); put32( uint32_t( v >> 32 ) ); }
    void patch32( int64_t at, uint32_t v )
    {
        if ( at + 4 > capacity ) { overflow = true; return; }
        buffer[at] = uint8_t( v ); buffer[at+1] = uint8_t( v >> 8 );
        buffer[at+2] = uint8_t( v >> 16 ); buffer[at+3] = uint8_t( v >> 24 );
    }
};

struct TableReader
{
    const uint8_t * buffer;
    int64_t size;
    int64_t offset = 0;
    TableReport * report;

    TableReader( const uint8_t * buffer, int64_t size, TableReport * report )
        : buffer( buffer ), size( size ), report( report ) {}

    bool has( int64_t bytes ) const { return offset + bytes <= size; }
    uint8_t get8()   { return buffer[offset++]; }
    uint16_t get16() { uint16_t v = uint16_t( buffer[offset] ) | uint16_t( buffer[offset+1] ) << 8; offset += 2; return v; }
    uint32_t get32() { uint32_t v = uint32_t( buffer[offset] ) | uint32_t( buffer[offset+1] ) << 8 | uint32_t( buffer[offset+2] ) << 16 | uint32_t( buffer[offset+3] ) << 24; offset += 4; return v; }
    uint64_t get64() { uint64_t lo = get32(); uint64_t hi = get32(); return lo | ( hi << 32 ); }

    // skip one payload by kind; false = framing damage
    bool skip( uint8_t kind )
    {
        switch ( kind )
        {
            case 1: case 2: case 6: return has( 1 ) ? ( offset += 1, true ) : false;
            case 3: case 7:         return has( 2 ) ? ( offset += 2, true ) : false;
            case 4: case 8: case 10: return has( 4 ) ? ( offset += 4, true ) : false;
            case 5: case 9: case 11: return has( 8 ) ? ( offset += 8, true ) : false;
            case 12:
            {
                if ( !has( 2 ) ) return false;
                uint16_t n = get16();
                return has( n ) ? ( offset += n, true ) : false;
            }
            case 13: case 14:
            {
                if ( !has( 4 ) ) return false;
                uint32_t n = get32();
                return has( n ) ? ( offset += n, true ) : false;
            }
        }
        return false;
    }
};

inline float table_bits_to_float( uint32_t bits ) { float f; memcpy( &f, &bits, 4 ); return f; }
inline uint32_t table_float_to_bits( float f ) { uint32_t b; memcpy( &b, &f, 4 ); return b; }
inline double table_bits_to_double( uint64_t bits ) { double d; memcpy( &d, &bits, 8 ); return d; }
inline uint64_t table_double_to_bits( double d ) { uint64_t b; memcpy( &b, &d, 8 ); return b; }

} // namespace example

#endif // EXAMPLE_SCHEMA_TABLE_PRIMITIVES

namespace example {

inline bool TableWriteHeartbeat( TableWriter & w, const Heartbeat & value )
{
    (void) value; // empty type: presence is the payload
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadHeartbeat( TableReader & r, Heartbeat & value )
{
    value = Heartbeat{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadHeartbeat( const uint8_t * buffer, int64_t bytes, Heartbeat & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadHeartbeat( r, value );
}

inline bool TableWriteTest( TableWriter & w, const Test & value )
{
    if ( value.test_a != 0 )
    {
        w.put16( 0xcb58 ); w.put8( 7 ); // test_a
        w.put16( uint16_t( value.test_a ) );
    }
    if ( value.test_b != 0 )
    {
        w.put16( 0xc3e1 ); w.put8( 3 ); // test_b
        w.put16( uint16_t( value.test_b ) );
    }
    if ( value.test_c != 0 )
    {
        w.put16( 0xc472 ); w.put8( 3 ); // test_c
        w.put16( uint16_t( value.test_c ) );
    }
    if ( value.test_d != 0 )
    {
        w.put16( 0xdebb ); w.put8( 3 ); // test_d
        w.put16( uint16_t( value.test_d ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadTest( TableReader & r, Test & value )
{
    value = Test{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xcb58: // test_a
            {
                if ( kind != 7 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                uint16_t decoded = uint16_t( r.get16( ) );
                value.test_a = decoded;
                break;
            }
            case 0xc3e1: // test_b
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 1000 ) { decoded = 1000; r.report->clamped++; }
                value.test_b = decoded;
                break;
            }
            case 0xc472: // test_c
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 1000 ) { decoded = 1000; r.report->clamped++; }
                value.test_c = decoded;
                break;
            }
            case 0xdebb: // test_d
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 1000 ) { decoded = 1000; r.report->clamped++; }
                value.test_d = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadTest( const uint8_t * buffer, int64_t bytes, Test & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadTest( r, value );
}

inline bool TableWriteBlock( TableWriter & w, const Block & value )
{
    if ( value.data_length > 0 )
    {
        w.put16( 0x3ad7 ); w.put8( 14 ); // data
        w.put32( uint32_t( 3 + value.data_length ) );
        w.put8( 6 ); w.put16( uint16_t( value.data_length ) );
        w.raw( value.data, value.data_length );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadBlock( TableReader & r, Block & value )
{
    value = Block{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x3ad7: // data
            {
                if ( kind != 14 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                int64_t body_end = r.offset + body_len;
                if ( body_len >= 3 )
                {
                    uint8_t elem_kind = r.get8();
                    uint16_t count = r.get16();
                    if ( elem_kind != 6 ) { r.report->kind_mismatch++; r.offset = body_end; break; }
                    uint16_t keep = count;
                    if ( keep > 2000 ) { keep = 2000; r.report->clamped++; }
                    for ( uint16_t i = 0; i < keep; i++ )
                    {
                        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                        uint8_t decoded = uint8_t( r.get8( ) );
                        value.data[i] = decoded;
                    }
                    value.data_length = keep;
                }
                r.offset = body_end; // excess elements and slack skip via the length
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadBlock( const uint8_t * buffer, int64_t bytes, Block & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadBlock( r, value );
}

inline bool TableWriteChat( TableWriter & w, const Chat & value )
{
    if ( value.text_length > 0 )
    {
        w.put16( 0xf3d8 ); w.put8( 12 ); // text
        w.put16( uint16_t( value.text_length ) );
        w.raw( value.text, value.text_length );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadChat( TableReader & r, Chat & value )
{
    value = Chat{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xf3d8: // text
            {
                if ( kind != 12 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                uint16_t len = r.get16();
                if ( !r.has( len ) ) { r.report->malformed = true; return false; }
                uint16_t keep = len;
                if ( keep > 256 ) { keep = 256; r.report->clamped++; }
                memcpy( value.text, r.buffer + r.offset, keep );
                value.text[keep] = 0;
                value.text_length = keep;
                r.offset += len;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadChat( const uint8_t * buffer, int64_t bytes, Chat & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadChat( r, value );
}

inline bool TableWriteSynchronize( TableWriter & w, const Synchronize & value )
{
    if ( value.sync_frame != 0 )
    {
        w.put16( 0x6831 ); w.put8( 9 ); // sync_frame
        w.put64( uint64_t( value.sync_frame ) );
    }
    if ( value.sync_sequence != 0 )
    {
        w.put16( 0x03a0 ); w.put8( 7 ); // sync_sequence
        w.put16( uint16_t( value.sync_sequence ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadSynchronize( TableReader & r, Synchronize & value )
{
    value = Synchronize{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x6831: // sync_frame
            {
                if ( kind != 9 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                uint64_t decoded = uint64_t( r.get64( ) );
                value.sync_frame = decoded;
                break;
            }
            case 0x03a0: // sync_sequence
            {
                if ( kind != 7 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                uint16_t decoded = uint16_t( r.get16( ) );
                value.sync_sequence = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadSynchronize( const uint8_t * buffer, int64_t bytes, Synchronize & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadSynchronize( r, value );
}

inline bool TableWriteTimescale( TableWriter & w, const Timescale & value )
{
    if ( value.scale != 0.0 )
    {
        w.put16( 0x9ee6 ); w.put8( 11 ); // scale
        w.put64( table_double_to_bits( value.scale ) );
    }
    if ( value.frame_a != 0 )
    {
        w.put16( 0x49e4 ); w.put8( 8 ); // frame_a
        w.put32( uint32_t( value.frame_a ) );
    }
    if ( value.frame_b != 0 )
    {
        w.put16( 0x5651 ); w.put8( 8 ); // frame_b
        w.put32( uint32_t( value.frame_b ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadTimescale( TableReader & r, Timescale & value )
{
    value = Timescale{}; // prefill declared defaults, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x9ee6: // scale
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.scale = table_bits_to_double( r.get64() );
                break;
            }
            case 0x49e4: // frame_a
            {
                if ( kind != 8 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t decoded = uint32_t( r.get32( ) );
                value.frame_a = decoded;
                break;
            }
            case 0x5651: // frame_b
            {
                if ( kind != 8 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t decoded = uint32_t( r.get32( ) );
                value.frame_b = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadTimescale( const uint8_t * buffer, int64_t bytes, Timescale & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadTimescale( r, value );
}

} // namespace example
