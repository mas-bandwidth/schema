// Code generated by the schema compiler from Joins.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

#pragma once

#include <cstdint>
#include <new>

namespace example {

// type ArmsAgree
struct ArmsAgree {
    uint32_t lead = 0;
    bool flag = false;

    // flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t a = 0;

    // !flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t b = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmsAgreeMaxBits = 24; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmsAgreeMaxBytes = 8; // rounded up to the 8-byte write-buffer granularity; a read buffer's allocation must extend at least 8 bytes past the data — the reader loads 64-bit windows

// type ArmsDisagree
struct ArmsDisagree {
    uint32_t lead = 0;
    bool flag = false;

    // flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t a = 0;

    // !flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t b = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmsDisagreeMaxBits = 24; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmsDisagreeMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArmEmpty
struct ArmEmpty {
    uint32_t lead = 0;
    bool flag = false;

    // flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t a = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmEmptyMaxBits = 32; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmEmptyMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArmsNested
struct ArmsNested {
    uint32_t lead = 0;
    bool outer = false;

    // outer — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    bool inner = false;

    // outer && inner — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t x = 0;

    // outer && !inner — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t y = 0;

    // !outer — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t z = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmsNestedMaxBits = 40; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmsNestedMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArmAlign
struct ArmAlign {
    uint32_t lead = 0;
    bool flag = false;

    // flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    char s[4 + 1] = {}; // string(4): max length, used length beside it (SPEC §4.7)
    int32_t s_length = 0;

    // !flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t b = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmAlignMaxBits = 55; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmAlignMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArmArray
struct ArmArray {
    uint32_t lead = 0;
    bool flag = false;

    // flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint16_t items[3] = {}; // used count beside it; wire count in [0, 3]
    int32_t items_count = 0;

    // !flag — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    uint32_t b = 0;

    uint32_t tail = 0;
};

inline constexpr int64_t ArmArrayMaxBits = 54; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArmArrayMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type Narrow
struct Narrow {
    uint32_t n = 0;
};

inline constexpr int64_t NarrowMaxBits = 3; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t NarrowMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type Wide
struct Wide {
    uint64_t w = 0;
};

inline constexpr int64_t WideMaxBits = 37; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t WideMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// UnevenType: union Uneven's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class UnevenType : uint8_t {
    None = 0,
    Narrow = 1,
    Wide = 2,
    Count = 2, // the declared variant count (SPEC §4.2)
    Max = 2, // the exported extent (SPEC §4.2)
};

// EnumName: debug/log name for any UnevenType value, out-of-set included
inline const char * EnumName( UnevenType value )
{
    switch ( value )
    {
        case UnevenType::None: return "None";
        case UnevenType::Narrow: return "Narrow";
        case UnevenType::Wide: return "Wide";
        default: return "???";
    }
}

// union Uneven — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm is freshly constructed with its defaults
// when selected, by ReadUneven before decoding (SPEC §4.8), or explicitly:
// ::new ( (void*) &value.narrow ) Narrow{}; value.type = UnevenType::Narrow;
// Bytes of unselected arms are indeterminate.
struct Uneven
{
    UnevenType type;

    union
    {
        Narrow narrow;
        Wide wide;
    };

    Uneven() : type( UnevenType::None ) {} // the tag only — arms are freshly constructed at selection
};

inline constexpr int64_t UnevenMaxBits = 39; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t UnevenMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type HoldsUneven
struct HoldsUneven {
    uint32_t lead = 0;
    Uneven u;
    uint32_t tail = 0;
};

inline constexpr int64_t HoldsUnevenMaxBits = 55; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t HoldsUnevenMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArrUneven
struct ArrUneven {
    uint32_t lead = 0;
    Uneven items[3] = {}; // used count beside it; wire count in [0, 3]
    int32_t items_count = 0;
    uint32_t tail = 0;
};

inline constexpr int64_t ArrUnevenMaxBits = 127; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArrUnevenMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// type RegainAfterAlign
struct RegainAfterAlign {
    uint32_t lead = 0;
    uint16_t items[3] = {}; // used count beside it; wire count in [0, 3]
    int32_t items_count = 0;
    char s[4 + 1] = {}; // string(4): max length, used length beside it (SPEC §4.7)
    int32_t s_length = 0;
    uint32_t p = 0;
    uint32_t q = 0;
    uint32_t r = 0;
    uint32_t tail = 0;
};

inline constexpr int64_t RegainAfterAlignMaxBits = 172; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t RegainAfterAlignMaxBytes = 24; // 8-byte write granularity; read slack per the contract above

} // namespace example
