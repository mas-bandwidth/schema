// Code generated by the schema compiler from WideText.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package wide — protocol id 0x53835dbf9f05e4d9

package wide

import (
	"errors"
	"unicode/utf8"

	"github.com/mas-bandwidth/serialize.go"
)

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
const ProtocolId uint64 = 0x53835dbf9f05e4d9

// ErrValidation is returned when a read rejects the wire: a wrong constant,
// nonzero reserved bits, or an interior null in a string (SPEC §4.3, §4.7).
var ErrValidation = errors.New("wide: wire validation failed")

// type WideSeven
type WideSeven struct {
	Text       [7]uint16
	TextLength int32
}

// WideSevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// WideSevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const WideSevenMaxBits = 227
const WideSevenMaxBytes = 32

func WriteWideSeven(stream *serialize.WriteStream, value *WideSeven) error {
	if value.TextLength < 0 || value.TextLength > 7 {
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.TextLength)
		stream.SerializeBits(&offsetValue, 3)
	}
	for _, unit := range value.Text[:value.TextLength] {
		if unit == 0 {
			return ErrValidation
		}
		group := uint32(unit)
		stream.SerializeBits(&group, 32)
	}
	return stream.Err()
}

func ReadWideSeven(stream *serialize.ReadStream, value *WideSeven) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 3)
		if stream.Err() != nil {
			return stream.Err()
		}
		value.TextLength = int32(offsetValue)
	}
	{
		expectLow := false
		for i := int32(0); i < value.TextLength; i++ {
			var group uint32
			stream.SerializeBits(&group, 32)
			if stream.Err() != nil {
				return stream.Err()
			}
			if group == 0 || group > 0xFFFF {
				return ErrValidation
			}
			high := group >= 0xD800 && group <= 0xDBFF
			low := group >= 0xDC00 && group <= 0xDFFF
			if low != expectLow {
				return ErrValidation
			}
			expectLow = high
			value.Text[i] = uint16(group)
		}
		if expectLow {
			return ErrValidation
		}
	}
	return stream.Err()
}

// type WideFour
type WideFour struct {
	Text       [4]uint16
	TextLength int32
}

// WideFourMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// WideFourMaxBytes is rounded up to the 8-byte write-buffer granularity.
const WideFourMaxBits = 131
const WideFourMaxBytes = 24

func WriteWideFour(stream *serialize.WriteStream, value *WideFour) error {
	if value.TextLength < 0 || value.TextLength > 4 {
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.TextLength)
		stream.SerializeBits(&offsetValue, 3)
	}
	for _, unit := range value.Text[:value.TextLength] {
		if unit == 0 {
			return ErrValidation
		}
		group := uint32(unit)
		stream.SerializeBits(&group, 32)
	}
	return stream.Err()
}

func ReadWideFour(stream *serialize.ReadStream, value *WideFour) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 3)
		if stream.Err() != nil {
			return stream.Err()
		}
		if offsetValue > 4 { // a value smuggled into the bit headroom is refused (SPEC §4.3)
			return serialize.ErrValueOutOfRange
		}
		value.TextLength = int32(offsetValue)
	}
	{
		expectLow := false
		for i := int32(0); i < value.TextLength; i++ {
			var group uint32
			stream.SerializeBits(&group, 32)
			if stream.Err() != nil {
				return stream.Err()
			}
			if group == 0 || group > 0xFFFF {
				return ErrValidation
			}
			high := group >= 0xD800 && group <= 0xDBFF
			low := group >= 0xDC00 && group <= 0xDFFF
			if low != expectLow {
				return ErrValidation
			}
			expectLow = high
			value.Text[i] = uint16(group)
		}
		if expectLow {
			return ErrValidation
		}
	}
	return stream.Err()
}

// type NarrowFifteen
type NarrowFifteen struct {
	Text       [15]byte // string(15): max length, used length beside it (SPEC §4.7)
	TextLength int32
}

// NarrowFifteenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// NarrowFifteenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const NarrowFifteenMaxBits = 131
const NarrowFifteenMaxBytes = 24

func WriteNarrowFifteen(stream *serialize.WriteStream, value *NarrowFifteen) error {
	if value.TextLength < 0 || value.TextLength > 15 {
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.TextLength)
		stream.SerializeBits(&offsetValue, 4)
	}
	if stream.Err() != nil { // the length guards the slice (§6.3)
		return stream.Err()
	}
	stream.SerializeBytes(value.Text[:value.TextLength])
	return stream.Err()
}

func ReadNarrowFifteen(stream *serialize.ReadStream, value *NarrowFifteen) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 4)
		if stream.Err() != nil {
			return stream.Err()
		}
		value.TextLength = int32(offsetValue)
	}
	stream.SerializeBytes(value.Text[:value.TextLength])
	if stream.Err() != nil {
		return stream.Err()
	}
	if !utf8.Valid(value.Text[:value.TextLength]) {
		return ErrValidation // malformed UTF-8 (SPEC §4.7)
	}
	for i := int32(0); i < value.TextLength; i++ {
		if value.Text[i] == 0 {
			return ErrValidation
		}
	}
	return stream.Err()
}

// type WideInterop
type WideInterop struct {
	Caption       [7]uint16
	CaptionLength int32
}

// WideInteropMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// WideInteropMaxBytes is rounded up to the 8-byte write-buffer granularity.
const WideInteropMaxBits = 227
const WideInteropMaxBytes = 32

func WriteWideInterop(stream *serialize.WriteStream, value *WideInterop) error {
	if value.CaptionLength < 0 || value.CaptionLength > 7 {
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.CaptionLength)
		stream.SerializeBits(&offsetValue, 3)
	}
	for _, unit := range value.Caption[:value.CaptionLength] {
		if unit == 0 {
			return ErrValidation
		}
		group := uint32(unit)
		stream.SerializeBits(&group, 32)
	}
	return stream.Err()
}

func ReadWideInterop(stream *serialize.ReadStream, value *WideInterop) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 3)
		if stream.Err() != nil {
			return stream.Err()
		}
		value.CaptionLength = int32(offsetValue)
	}
	{
		expectLow := false
		for i := int32(0); i < value.CaptionLength; i++ {
			var group uint32
			stream.SerializeBits(&group, 32)
			if stream.Err() != nil {
				return stream.Err()
			}
			if group == 0 || group > 0xFFFF {
				return ErrValidation
			}
			high := group >= 0xD800 && group <= 0xDBFF
			low := group >= 0xDC00 && group <= 0xDFFF
			if low != expectLow {
				return ErrValidation
			}
			expectLow = high
			value.Caption[i] = uint16(group)
		}
		if expectLow {
			return ErrValidation
		}
	}
	return stream.Err()
}
