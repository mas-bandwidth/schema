// Code generated by the schema compiler from WideText.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package wide — protocol id 0x53835dbf9f05e4d9
//
// Wire functions return bool — the C++-style early-out. A READ-side schema
// validation failure (a wrong wire constant, nonzero reserved bits, an
// interior null) returns false WITHOUT latching; stream failures latch on
// stream.Error — the runtime's own sticky latch. Callers get bool always;
// Error tells the two apart.
//
// WRITE-side contracts — a value outside its declared range, a count or a
// length outside its bound, a mask bit above the wire width, an interior
// null in a wide string — are CALLER ERROR and ride on Debug.Assert, so
// they compile out of a release build along with the call, exactly as
// serialize.cs's own WriteStream does. In a shipping release build it is
// the caller's responsibility to be correct on the write side; every check
// the reader needs is on the read side and runs in every build.

using System;
using System.Diagnostics;
using Serialize;

namespace Wide
{

    // type WideSeven
    public sealed class WideSeven
    {
        public char[] Text = new char[7];
        public int TextLength;
    }

    // type WideFour
    public sealed class WideFour
    {
        public char[] Text = new char[4];
        public int TextLength;
    }

    // type NarrowFifteen
    public sealed class NarrowFifteen
    {
        public byte[] Text = new byte[15]; // string(15): max length, used length beside it (SPEC §4.7)
        public int TextLength;
    }

    // type WideInterop
    public sealed class WideInterop
    {
        public char[] Caption = new char[7];
        public int CaptionLength;
    }

    // Schema carries every generated function and constant of the unit — C# has
    // no namespace-level functions or constants, so the static class is their
    // home (SPEC §6.1 naming); partial, one slice per generated file.
    public static partial class Schema
    {
        // The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
        // sides at the same id speak identical bits; there is no other versioning.
        public const ulong ProtocolId = 0x53835dbf9f05e4d9;

        // WideSevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // WideSevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long WideSevenMaxBits = 227;
        public const long WideSevenMaxBytes = 32;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroWideSeven(WideSeven value)
        {
            Array.Clear(value.Text, 0, 7);
            value.TextLength = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitWideSeven(WideSeven value)
        {
            Array.Clear(value.Text, 0, 7);
            value.TextLength = 0;
        }

        public static bool WriteWideSeven(WriteStream stream, WideSeven value)
        {
            Debug.Assert(value.TextLength >= 0 && value.TextLength <= 7, "value.TextLength out of range [0, 7]");
            {
                uint offsetValue = (uint)(value.TextLength);
                if (!stream.SerializeBits(ref offsetValue, 3))
                {
                    return false;
                }
            }
            for (int wideIndex = 0; wideIndex < value.TextLength; wideIndex++)
            {
                uint wideGroup = value.Text[wideIndex];
                Debug.Assert(wideGroup != 0, "a wide string's used units carry an interior null");
                if (!stream.SerializeBits(ref wideGroup, 32))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadWideSeven(ReadStream stream, WideSeven value)
        {
            if (!stream.SerializeInt(ref value.TextLength, 0, 7))
            {
                return false;
            }
            {
                bool expectLow = false;
                for (int wideIndex = 0; wideIndex < value.TextLength; wideIndex++)
                {
                    uint wideGroup = 0;
                    if (!stream.SerializeBits(ref wideGroup, 32))
                    {
                        return false;
                    }
                    if (wideGroup == 0 || wideGroup > 0xFFFF) return false;
                    bool high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
                    bool low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
                    if (low != expectLow) return false;
                    expectLow = high;
                    value.Text[wideIndex] = (char)wideGroup;
                }
                if (expectLow) return false;
            }
            return true;
        }

        // WideFourMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // WideFourMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long WideFourMaxBits = 131;
        public const long WideFourMaxBytes = 24;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroWideFour(WideFour value)
        {
            Array.Clear(value.Text, 0, 4);
            value.TextLength = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitWideFour(WideFour value)
        {
            Array.Clear(value.Text, 0, 4);
            value.TextLength = 0;
        }

        public static bool WriteWideFour(WriteStream stream, WideFour value)
        {
            Debug.Assert(value.TextLength >= 0 && value.TextLength <= 4, "value.TextLength out of range [0, 4]");
            {
                uint offsetValue = (uint)(value.TextLength);
                if (!stream.SerializeBits(ref offsetValue, 3))
                {
                    return false;
                }
            }
            for (int wideIndex = 0; wideIndex < value.TextLength; wideIndex++)
            {
                uint wideGroup = value.Text[wideIndex];
                Debug.Assert(wideGroup != 0, "a wide string's used units carry an interior null");
                if (!stream.SerializeBits(ref wideGroup, 32))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadWideFour(ReadStream stream, WideFour value)
        {
            if (!stream.SerializeInt(ref value.TextLength, 0, 4))
            {
                return false;
            }
            {
                bool expectLow = false;
                for (int wideIndex = 0; wideIndex < value.TextLength; wideIndex++)
                {
                    uint wideGroup = 0;
                    if (!stream.SerializeBits(ref wideGroup, 32))
                    {
                        return false;
                    }
                    if (wideGroup == 0 || wideGroup > 0xFFFF) return false;
                    bool high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
                    bool low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
                    if (low != expectLow) return false;
                    expectLow = high;
                    value.Text[wideIndex] = (char)wideGroup;
                }
                if (expectLow) return false;
            }
            return true;
        }

        // NarrowFifteenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // NarrowFifteenMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long NarrowFifteenMaxBits = 131;
        public const long NarrowFifteenMaxBytes = 24;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroNarrowFifteen(NarrowFifteen value)
        {
            Array.Clear(value.Text, 0, 15);
            value.TextLength = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitNarrowFifteen(NarrowFifteen value)
        {
            Array.Clear(value.Text, 0, 15);
            value.TextLength = 0;
        }

        public static bool WriteNarrowFifteen(WriteStream stream, NarrowFifteen value)
        {
            Debug.Assert(value.TextLength >= 0 && value.TextLength <= 15, "value.TextLength out of range [0, 15]"); // the length guards the slice (§6.3); an out-of-contract length is caller error
            {
                uint offsetValue = (uint)(value.TextLength);
                if (!stream.SerializeBits(ref offsetValue, 4))
                {
                    return false;
                }
            }
            if (!stream.SerializeBytes(value.Text.AsSpan(0, value.TextLength)))
            {
                return false;
            }
            return true;
        }

        public static bool ReadNarrowFifteen(ReadStream stream, NarrowFifteen value)
        {
            if (!stream.SerializeInt(ref value.TextLength, 0, 15)) // the length guards the slice (§6.3)
            {
                return false;
            }
            if (!stream.SerializeBytes(value.Text.AsSpan(0, value.TextLength)))
            {
                return false;
            }
            for (int utf8Index = 0; utf8Index < value.TextLength;)
            {
                int utf8Lead = value.Text[utf8Index++];
                if (utf8Lead < 0x80) continue;
                int utf8Remaining, utf8Point;
                if ((utf8Lead & 0xE0) == 0xC0) { utf8Remaining = 1; utf8Point = utf8Lead & 0x1F; }
                else if ((utf8Lead & 0xF0) == 0xE0) { utf8Remaining = 2; utf8Point = utf8Lead & 0x0F; }
                else if ((utf8Lead & 0xF8) == 0xF0) { utf8Remaining = 3; utf8Point = utf8Lead & 0x07; }
                else return false; // malformed UTF-8 lead (SPEC §4.7)
                if (utf8Remaining > value.TextLength - utf8Index) return false;
                for (int utf8Part = 0; utf8Part < utf8Remaining; utf8Part++)
                {
                    int utf8Byte = value.Text[utf8Index++];
                    if ((utf8Byte & 0xC0) != 0x80) return false;
                    utf8Point = (utf8Point << 6) | (utf8Byte & 0x3F);
                }
                if (utf8Remaining == 1 && utf8Point < 0x80) return false;
                if (utf8Remaining == 2 && (utf8Point < 0x800 || (utf8Point >= 0xD800 && utf8Point <= 0xDFFF))) return false;
                if (utf8Remaining == 3 && (utf8Point < 0x10000 || utf8Point > 0x10FFFF)) return false;
            }
            for (int i = 0; i < value.TextLength; i++)
            {
                if (value.Text[i] == 0) // an interior null is content the read refuses (SPEC §4.7)
                {
                    return false;
                }
            }
            return true;
        }

        // WideInteropMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // WideInteropMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long WideInteropMaxBits = 227;
        public const long WideInteropMaxBytes = 32;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroWideInterop(WideInterop value)
        {
            Array.Clear(value.Caption, 0, 7);
            value.CaptionLength = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitWideInterop(WideInterop value)
        {
            Array.Clear(value.Caption, 0, 7);
            value.CaptionLength = 0;
        }

        public static bool WriteWideInterop(WriteStream stream, WideInterop value)
        {
            Debug.Assert(value.CaptionLength >= 0 && value.CaptionLength <= 7, "value.CaptionLength out of range [0, 7]");
            {
                uint offsetValue = (uint)(value.CaptionLength);
                if (!stream.SerializeBits(ref offsetValue, 3))
                {
                    return false;
                }
            }
            for (int wideIndex = 0; wideIndex < value.CaptionLength; wideIndex++)
            {
                uint wideGroup = value.Caption[wideIndex];
                Debug.Assert(wideGroup != 0, "a wide string's used units carry an interior null");
                if (!stream.SerializeBits(ref wideGroup, 32))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadWideInterop(ReadStream stream, WideInterop value)
        {
            if (!stream.SerializeInt(ref value.CaptionLength, 0, 7))
            {
                return false;
            }
            {
                bool expectLow = false;
                for (int wideIndex = 0; wideIndex < value.CaptionLength; wideIndex++)
                {
                    uint wideGroup = 0;
                    if (!stream.SerializeBits(ref wideGroup, 32))
                    {
                        return false;
                    }
                    if (wideGroup == 0 || wideGroup > 0xFFFF) return false;
                    bool high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
                    bool low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
                    if (low != expectLow) return false;
                    expectLow = high;
                    value.Caption[wideIndex] = (char)wideGroup;
                }
                if (expectLow) return false;
            }
            return true;
        }
    }

}
