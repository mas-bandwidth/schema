// Code generated by the schema compiler from WideText.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package wide — protocol id 0x53835dbf9f05e4d9
//
// The shipped Dart wire path (issue #155): the serialize.dart bitpacker
// inlined at every field, literal constant widths and masks, monomorphic
// write/read/measure functions per type, zero runtime dependencies.
//
// write<Name>(value, view) -> bytes written. The writer TRUSTS the caller
// (the C++ stance): contracts are asserted — active under --enable-asserts,
// compiled out in release — and width masks stay, as wire arithmetic. The
// buffer behind view must hold <name>MaxBytes (a multiple of 8).
//
// read<Name>(value, view, numBits) -> bool, the family read verdict. The
// wire is a trust boundary: reads validate everything — bounds fused per
// static run, ranges, wire constants, reserved and align padding, union
// tags — and NEVER throw on hostile bytes. No slack past the payload is
// required: the reader prices its 64-bit windows inside the buffer.
//
// measure<Name>(value) -> exact wire bits for that value (trusted, like
// the writer). zero<Name>(value) -> the §5 zero form.

import 'dart:typed_data';

// Malformed UTF-8 fails the read in every build mode (SPEC §4.7).
bool _schemaUtf8Valid(Uint8List bytes, int length) {
  var index = 0;
  while (index < length) {
    final lead = bytes[index++];
    if (lead < 0x80) continue;
    int remaining, point;
    if ((lead & 0xE0) == 0xC0) {
      remaining = 1;
      point = lead & 0x1F;
    } else if ((lead & 0xF0) == 0xE0) {
      remaining = 2;
      point = lead & 0x0F;
    } else if ((lead & 0xF8) == 0xF0) {
      remaining = 3;
      point = lead & 0x07;
    } else {
      return false;
    }
    if (remaining > length - index) return false;
    for (var part = 0; part < remaining; part++) {
      final byte = bytes[index++];
      if ((byte & 0xC0) != 0x80) return false;
      point = (point << 6) | (byte & 0x3F);
    }
    if (remaining == 1 && point < 0x80) return false;
    if (remaining == 2 &&
        (point < 0x800 || (point >= 0xD800 && point <= 0xDFFF))) {
      return false;
    }
    if (remaining == 3 && (point < 0x10000 || point > 0x10FFFF)) {
      return false;
    }
  }
  return true;
}

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
const int protocolId = 0x53835dbf9f05e4d9;

// type WideSeven
final class WideSeven {
  final Uint16List text = Uint16List(7);
  int textLength = 0;
}

// wideSevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// wideSevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int wideSevenMaxBits = 227;
const int wideSevenMaxBytes = 32;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroWideSeven(WideSeven value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// Restore construction defaults in place; buffers and objects are retained.
void initWideSeven(WideSeven value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// writeWideSeven packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// wideSevenMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeWideSeven(WideSeven value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= wideSevenMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  {
    final wideLength = value.textLength;
    if (wideLength < 0 || wideLength > 7) {
      throw ArgumentError('wstring length');
    }
    v = ((wideLength) & 0x7);
    scratch |= v << scratchBits;
    scratchBits += 3;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (3 - scratchBits);
    }
    for (var wideIndex = 0; wideIndex < wideLength; wideIndex++) {
      v = value.text[wideIndex];
      if (v == 0) {
        throw ArgumentError('wstring null unit');
      }
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readWideSeven decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readWideSeven(WideSeven value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x7;
  bitsRead += 3;
  if (v > 7) {
    return false;
  }
  value.textLength = v;
  if (bitsRead + value.textLength * 32 > numBits) {
    return false;
  }
  {
    var expectLow = false;
    for (var wideIndex = 0; wideIndex < value.textLength; wideIndex++) {
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0xffffffff;
      bitsRead += 32;
      if (v == 0 || v > 0xFFFF) {
        return false;
      }
      final high = v >= 0xD800 && v <= 0xDBFF;
      final low = v >= 0xDC00 && v <= 0xDFFF;
      if (low != expectLow) {
        return false;
      }
      expectLow = high;
      value.text[wideIndex] = v;
    }
    if (expectLow) {
      return false;
    }
  }
  return true;
}

// measureWideSeven is the exact wire bits writeWideSeven would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureWideSeven(WideSeven value) {
  var bits = 0;
  bits += 3;
  bits += value.textLength * 32;
  return bits;
}

// type WideFour
final class WideFour {
  final Uint16List text = Uint16List(4);
  int textLength = 0;
}

// wideFourMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// wideFourMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int wideFourMaxBits = 131;
const int wideFourMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroWideFour(WideFour value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// Restore construction defaults in place; buffers and objects are retained.
void initWideFour(WideFour value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// writeWideFour packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// wideFourMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeWideFour(WideFour value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= wideFourMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  {
    final wideLength = value.textLength;
    if (wideLength < 0 || wideLength > 4) {
      throw ArgumentError('wstring length');
    }
    v = ((wideLength) & 0x7);
    scratch |= v << scratchBits;
    scratchBits += 3;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (3 - scratchBits);
    }
    for (var wideIndex = 0; wideIndex < wideLength; wideIndex++) {
      v = value.text[wideIndex];
      if (v == 0) {
        throw ArgumentError('wstring null unit');
      }
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readWideFour decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readWideFour(WideFour value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x7;
  bitsRead += 3;
  if (v > 4) {
    return false;
  }
  value.textLength = v;
  if (bitsRead + value.textLength * 32 > numBits) {
    return false;
  }
  {
    var expectLow = false;
    for (var wideIndex = 0; wideIndex < value.textLength; wideIndex++) {
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0xffffffff;
      bitsRead += 32;
      if (v == 0 || v > 0xFFFF) {
        return false;
      }
      final high = v >= 0xD800 && v <= 0xDBFF;
      final low = v >= 0xDC00 && v <= 0xDFFF;
      if (low != expectLow) {
        return false;
      }
      expectLow = high;
      value.text[wideIndex] = v;
    }
    if (expectLow) {
      return false;
    }
  }
  return true;
}

// measureWideFour is the exact wire bits writeWideFour would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureWideFour(WideFour value) {
  var bits = 0;
  bits += 3;
  bits += value.textLength * 32;
  return bits;
}

// type NarrowFifteen
final class NarrowFifteen {
  // string(15): max length, used length beside it (SPEC §4.7)
  final Uint8List text = Uint8List(15);
  int textLength = 0;
}

// narrowFifteenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// narrowFifteenMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int narrowFifteenMaxBits = 131;
const int narrowFifteenMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroNarrowFifteen(NarrowFifteen value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// Restore construction defaults in place; buffers and objects are retained.
void initNarrowFifteen(NarrowFifteen value) {
  value.text.fillRange(0, value.text.length, 0);
  value.textLength = 0;
}

// writeNarrowFifteen packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// narrowFifteenMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeNarrowFifteen(NarrowFifteen value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= narrowFifteenMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  assert(value.textLength >= 0);
  assert(value.textLength <= 15);
  v = ((value.textLength) & 0xf);
  scratch |= v << scratchBits;
  scratchBits += 4;
  if (scratchBits >= 64) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
    wordIndex++;
    scratchBits -= 64;
    scratch = v >>> (4 - scratchBits);
  }
  {
    final pad = scratchBits & 7;
    if (pad != 0) {
      scratchBits += 8 - pad;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = 0;
      }
    }
  }
  {
    var i0 = 0;
    for (; i0 + 4 <= value.textLength; i0 += 4) {
      v =
          value.text[i0] |
          (value.text[i0 + 1] << 8) |
          (value.text[i0 + 2] << 16) |
          (value.text[i0 + 3] << 24);
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
    for (; i0 < value.textLength; i0++) {
      v = value.text[i0];
      scratch |= v << scratchBits;
      scratchBits += 8;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (8 - scratchBits);
      }
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readNarrowFifteen decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readNarrowFifteen(NarrowFifteen value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 4 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0xf;
  bitsRead += 4;
  value.textLength = v;
  {
    final pad = bitsRead & 7;
    if (pad != 0) {
      if (bitsRead + (8 - pad) > numBits) {
        return false;
      }
      if (view.getUint8(bitsRead >>> 3) >>> pad != 0) {
        return false; // nonzero padding is refused (SPEC §4.3)
      }
      bitsRead += 8 - pad;
    }
  }
  if (bitsRead + value.textLength * 8 > numBits) {
    return false;
  }
  {
    final base = bitsRead >>> 3;
    for (var i0 = 0; i0 < value.textLength; i0++) {
      value.text[i0] = view.getUint8(base + i0);
    }
  }
  bitsRead += value.textLength * 8;
  if (!_schemaUtf8Valid(value.text, value.textLength)) {
    return false;
  }
  for (var i0 = 0; i0 < value.textLength; i0++) {
    if (value.text[i0] == 0) {
      return false; // an interior null is content the read refuses (SPEC §4.7)
    }
  }
  return true;
}

// measureNarrowFifteen is the exact wire bits writeNarrowFifteen would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureNarrowFifteen(NarrowFifteen value) {
  var bits = 0;
  bits += 4;
  bits += (8 - (bits & 7)) & 7;
  bits += value.textLength * 8;
  return bits;
}

// type WideInterop
final class WideInterop {
  final Uint16List caption = Uint16List(7);
  int captionLength = 0;
}

// wideInteropMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// wideInteropMaxBytes is rounded up to the 8-byte write-buffer granularity.
const int wideInteropMaxBits = 227;
const int wideInteropMaxBytes = 32;

// The §5 zero form: all-zero storage; specified defaults live only in
// construction.
void zeroWideInterop(WideInterop value) {
  value.caption.fillRange(0, value.caption.length, 0);
  value.captionLength = 0;
}

// Restore construction defaults in place; buffers and objects are retained.
void initWideInterop(WideInterop value) {
  value.caption.fillRange(0, value.caption.length, 0);
  value.captionLength = 0;
}

// writeWideInterop packs value into view — the trusted writer (contracts asserted,
// compiled out without --enable-asserts). The buffer behind view must hold
// wideInteropMaxBytes. Returns the bytes written, or -1 when a count is outside its
// wire range, which is refused in every build (SPEC §4.6).
int writeWideInterop(WideInterop value, ByteData view) {
  assert(view.lengthInBytes % 8 == 0);
  assert(view.lengthInBytes >= wideInteropMaxBytes);
  var scratch = 0;
  var scratchBits = 0;
  var wordIndex = 0;
  var v = 0;
  {
    final wideLength = value.captionLength;
    if (wideLength < 0 || wideLength > 7) {
      throw ArgumentError('wstring length');
    }
    v = ((wideLength) & 0x7);
    scratch |= v << scratchBits;
    scratchBits += 3;
    if (scratchBits >= 64) {
      view.setUint64(wordIndex * 8, scratch, Endian.little);
      wordIndex++;
      scratchBits -= 64;
      scratch = v >>> (3 - scratchBits);
    }
    for (var wideIndex = 0; wideIndex < wideLength; wideIndex++) {
      v = value.caption[wideIndex];
      if (v == 0) {
        throw ArgumentError('wstring null unit');
      }
      scratch |= v << scratchBits;
      scratchBits += 32;
      if (scratchBits >= 64) {
        view.setUint64(wordIndex * 8, scratch, Endian.little);
        wordIndex++;
        scratchBits -= 64;
        scratch = v >>> (32 - scratchBits);
      }
    }
  }
  if (scratchBits != 0) {
    view.setUint64(wordIndex * 8, scratch, Endian.little);
  }
  return wordIndex * 8 + ((scratchBits + 7) >>> 3);
}

// readWideInterop decodes value from the first numBits of view — the family read
// verdict: false rejects the wire (bounds, ranges, wire constants, padding);
// hostile bytes never throw. No slack past the payload is required.
bool readWideInterop(WideInterop value, ByteData view, int numBits) {
  if (numBits > view.lengthInBytes * 8) {
    return false; // the payload cannot exceed the buffer behind view
  }
  // the final 64-bit window, assembled once so every load stays inside
  // the buffer (serialize.dart's own no-slack reader stance)
  var tailBase = view.lengthInBytes - 8;
  var tailWord = 0;
  if (tailBase >= 0) {
    tailWord = view.getUint64(tailBase, Endian.little);
  } else {
    tailBase = 0;
    for (var i = view.lengthInBytes - 1; i >= 0; i--) {
      tailWord = (tailWord << 8) | view.getUint8(i);
    }
  }
  var bitsRead = 0;
  var window = 0;
  var v = 0;
  if (bitsRead + 3 > numBits) {
    return false;
  }
  if (bitsRead >>> 3 < tailBase) {
    window = view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
  } else {
    window = tailWord >>> (bitsRead - tailBase * 8);
  }
  v = window & 0x7;
  bitsRead += 3;
  if (v > 7) {
    return false;
  }
  value.captionLength = v;
  if (bitsRead + value.captionLength * 32 > numBits) {
    return false;
  }
  {
    var expectLow = false;
    for (var wideIndex = 0; wideIndex < value.captionLength; wideIndex++) {
      if (bitsRead >>> 3 < tailBase) {
        window =
            view.getUint64(bitsRead >>> 3, Endian.little) >>> (bitsRead & 7);
      } else {
        window = tailWord >>> (bitsRead - tailBase * 8);
      }
      v = window & 0xffffffff;
      bitsRead += 32;
      if (v == 0 || v > 0xFFFF) {
        return false;
      }
      final high = v >= 0xD800 && v <= 0xDBFF;
      final low = v >= 0xDC00 && v <= 0xDFFF;
      if (low != expectLow) {
        return false;
      }
      expectLow = high;
      value.caption[wideIndex] = v;
    }
    if (expectLow) {
      return false;
    }
  }
  return true;
}

// measureWideInterop is the exact wire bits writeWideInterop would produce for value —
// trusted like the writer; static runs fold to literals at generation time.
int measureWideInterop(WideInterop value) {
  var bits = 0;
  bits += 3;
  bits += value.captionLength * 32;
  return bits;
}
