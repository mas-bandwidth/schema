// Code generated by the schema compiler from WideText.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package wide — protocol id 0x53835dbf9f05e4d9
//
// THE FLAT TIER — the shipped JavaScript wire path: a single-word 32-bit
// bitpacker inlined at every field (byte-identical wire to serialize.js),
// constant widths and masks, zero function calls. Same generated classes,
// same bytes as the runtime tier (a standing CI gate); the runtime tier
// remains the diagnostic and reference surface — re-read a failing buffer
// through it to learn WHICH operation failed and why.
//
// Write<Name>Flat(value, view) -> bytes written, or -1 when the checked
// writer refuses an out-of-contract value (the production writer trusts
// the caller — serialize.js src/mode.js's own NODE_ENV fork, frozen at
// module load). Read<Name>Flat(value, view, numBits) -> bool, the family
// read verdict; reader obligations ride in every mode.
//
// Buffers are caller-owned DataViews. Write buffers: at least <Name>MaxBytes
// (the constants live in the runtime-tier module). Read buffers: at least
// FLAT_READ_SLACK = 8 bytes past the payload — 64-bit windows load
// unconditionally; copy exactly-sized receive buffers into a persistent
// MaxBytes + 8 scratch first.

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type WideSeven: the flat codec ----

function writeWideSevenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 7) return -1;
  v = (((value.TextLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
    v = value.Text[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeWideSevenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 7) return -1;
  v = (((value.TextLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
    v = value.Text[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteWideSevenFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold WideSevenMaxBytes.
export const WriteWideSevenFlat = PRODUCTION ? writeWideSevenFlatProduction : writeWideSevenFlatChecked;

// ReadWideSevenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadWideSevenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) return false;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 7) return false;
  value.TextLength = v;
  br += 3;
  if (br + value.TextLength * 32 > numBits) return false;
  {
    let expectLow = false;
    for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = out >>> 0;
      const wideGroup = v;
      if (wideGroup === 0 || wideGroup > 0xFFFF) return false;
      const high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
      const low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
      if (low !== expectLow) return false;
      br += 32;
      expectLow = high;
      value.Text[wideIndex] = wideGroup;
    }
    if (expectLow) return false;
  }
  return true;
}

// ---- type WideFour: the flat codec ----

function writeWideFourFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 4) return -1;
  v = (((value.TextLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
    v = value.Text[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeWideFourFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 4) return -1;
  v = (((value.TextLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
    v = value.Text[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteWideFourFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold WideFourMaxBytes.
export const WriteWideFourFlat = PRODUCTION ? writeWideFourFlatProduction : writeWideFourFlatChecked;

// ReadWideFourFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadWideFourFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) return false;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 4) return false;
  value.TextLength = v;
  br += 3;
  if (br + value.TextLength * 32 > numBits) return false;
  {
    let expectLow = false;
    for (let wideIndex = 0; wideIndex < value.TextLength; wideIndex++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = out >>> 0;
      const wideGroup = v;
      if (wideGroup === 0 || wideGroup > 0xFFFF) return false;
      const high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
      const low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
      if (low !== expectLow) return false;
      br += 32;
      expectLow = high;
      value.Text[wideIndex] = wideGroup;
    }
    if (expectLow) return false;
  }
  return true;
}

// ---- type NarrowFifteen: the flat codec ----

function writeNarrowFifteenFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.TextLength) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.TextLength; i0 += 4) {
      v = (value.Text[i0] | (value.Text[i0 + 1] << 8) | (value.Text[i0 + 2] << 16) | (value.Text[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.TextLength; i0++) {
      v = value.Text[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeNarrowFifteenFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.TextLength) || value.TextLength < 0 || value.TextLength > 15) { // the length guards the slice; out-of-contract writes are refused
    return -1;
  }
  v = (((value.TextLength) & 0xf)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 4;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (4 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.TextLength; i0 += 4) {
      v = (value.Text[i0] | (value.Text[i0 + 1] << 8) | (value.Text[i0 + 2] << 16) | (value.Text[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.TextLength; i0++) {
      v = value.Text[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteNarrowFifteenFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold NarrowFifteenMaxBytes.
export const WriteNarrowFifteenFlat = PRODUCTION ? writeNarrowFifteenFlatProduction : writeNarrowFifteenFlatChecked;

// ReadNarrowFifteenFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadNarrowFifteenFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 4 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xf) >>> 0;
  value.TextLength = v;
  br += 4;
  s2 = br & 7;
  if (s2 !== 0) {
    if (br + (8 - s2) > numBits) {
      return false;
    }
    if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
      return false;
    }
    br += 8 - s2;
  }
  if (br + value.TextLength * 8 > numBits) {
    return false;
  }
  bi = br >>> 3;
  for (let i0 = 0; i0 < value.TextLength; i0++) {
    value.Text[i0] = view.getUint8(bi + i0);
  }
  br += value.TextLength * 8;
  for (let utf8Index = 0; utf8Index < value.TextLength;)
  {
    const utf8Lead = value.Text[utf8Index++];
    if (utf8Lead < 0x80) continue;
    let utf8Remaining, utf8Point;
    if ((utf8Lead & 0xE0) === 0xC0) { utf8Remaining = 1; utf8Point = utf8Lead & 0x1F; }
    else if ((utf8Lead & 0xF0) === 0xE0) { utf8Remaining = 2; utf8Point = utf8Lead & 0x0F; }
    else if ((utf8Lead & 0xF8) === 0xF0) { utf8Remaining = 3; utf8Point = utf8Lead & 0x07; }
    else return false; // malformed UTF-8 lead (SPEC §4.7)
    if (utf8Remaining > value.TextLength - utf8Index) return false;
    for (let utf8Part = 0; utf8Part < utf8Remaining; utf8Part++)
    {
      const utf8Byte = value.Text[utf8Index++];
      if ((utf8Byte & 0xC0) !== 0x80) return false;
      utf8Point = (utf8Point << 6) | (utf8Byte & 0x3F);
    }
    if (utf8Remaining === 1 && utf8Point < 0x80) return false;
    if (utf8Remaining === 2 && (utf8Point < 0x800 || (utf8Point >= 0xD800 && utf8Point <= 0xDFFF))) return false;
    if (utf8Remaining === 3 && (utf8Point < 0x10000 || utf8Point > 0x10FFFF)) return false;
  }
  for (let i0 = 0; i0 < value.TextLength; i0++) {
    if (value.Text[i0] === 0) { // an interior null is content the read refuses
      return false;
    }
  }
  return true;
}

// ---- type WideInterop: the flat codec ----

function writeWideInteropFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.CaptionLength) || value.CaptionLength < 0 || value.CaptionLength > 7) return -1;
  v = (((value.CaptionLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.CaptionLength; wideIndex++) {
    v = value.Caption[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeWideInteropFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.CaptionLength) || value.CaptionLength < 0 || value.CaptionLength > 7) return -1;
  v = (((value.CaptionLength) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  for (let wideIndex = 0; wideIndex < value.CaptionLength; wideIndex++) {
    v = value.Caption[wideIndex];
    if (v === 0) return -1;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteWideInteropFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold WideInteropMaxBytes.
export const WriteWideInteropFlat = PRODUCTION ? writeWideInteropFlatProduction : writeWideInteropFlatChecked;

// ReadWideInteropFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadWideInteropFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) return false;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 7) return false;
  value.CaptionLength = v;
  br += 3;
  if (br + value.CaptionLength * 32 > numBits) return false;
  {
    let expectLow = false;
    for (let wideIndex = 0; wideIndex < value.CaptionLength; wideIndex++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = out >>> 0;
      const wideGroup = v;
      if (wideGroup === 0 || wideGroup > 0xFFFF) return false;
      const high = wideGroup >= 0xD800 && wideGroup <= 0xDBFF;
      const low = wideGroup >= 0xDC00 && wideGroup <= 0xDFFF;
      if (low !== expectLow) return false;
      br += 32;
      expectLow = high;
      value.Caption[wideIndex] = wideGroup;
    }
    if (expectLow) return false;
  }
  return true;
}

