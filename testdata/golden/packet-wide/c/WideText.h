/* Code generated by the schema compiler from WideText.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package wide — protocol id 0x53835dbf9f05e4d9 */

#ifndef SCHEMA_WIDE_WIDE_TEXT_H
#define SCHEMA_WIDE_WIDE_TEXT_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
   sides at the same id speak identical bits; there is no other versioning. */
#define WIDE_PROTOCOL_ID 0x53835dbf9f05e4d9ULL


/* type WideSeven */
typedef struct WideSeven {
    uint16_t text[7 + 1]; /* UTF-16 code units, plus the read terminator (SPEC §4.12) */
    int32_t text_length;
} WideSeven;

#define WIDE_SEVEN_MAX_BITS 227   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define WIDE_SEVEN_MAX_BYTES 32  /* rounded up to the 8-byte write-buffer granularity; a READ buffer's allocation must extend at least 8 bytes past the data — serialize.c loads 64-bit windows */


/* type WideFour */
typedef struct WideFour {
    uint16_t text[4 + 1]; /* UTF-16 code units, plus the read terminator (SPEC §4.12) */
    int32_t text_length;
} WideFour;

#define WIDE_FOUR_MAX_BITS 131   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define WIDE_FOUR_MAX_BYTES 24  /* 8-byte write granularity; read slack per the contract above */


/* type NarrowFifteen */
typedef struct NarrowFifteen {
    char text[15 + 1]; /* string(15): N + 1 for the terminator the wire does not carry */
    int32_t text_length;
} NarrowFifteen;

#define NARROW_FIFTEEN_MAX_BITS 131   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define NARROW_FIFTEEN_MAX_BYTES 24  /* 8-byte write granularity; read slack per the contract above */


/* type WideInterop */
typedef struct WideInterop {
    uint16_t caption[7 + 1]; /* UTF-16 code units, plus the read terminator (SPEC §4.12) */
    int32_t caption_length;
} WideInterop;

#define WIDE_INTEROP_MAX_BITS 227   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define WIDE_INTEROP_MAX_BYTES 32  /* 8-byte write granularity; read slack per the contract above */

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_WIDE_WIDE_TEXT_H */
