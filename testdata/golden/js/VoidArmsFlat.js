// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type VoidPulse: the flat codec ----

function writeVoidPulseFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.Strength) & 0xffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 16;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (16 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeVoidPulseFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.Strength) & 0xffff)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 16;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (16 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteVoidPulseFlat(value, view) -> bytes written (>= 0), or -1 on a refused
// writer contract in the checked build — the production writer holds none
// of them (SPEC §5). The buffer behind view must hold VoidPulseMaxBytes.
export const WriteVoidPulseFlat = PRODUCTION ? writeVoidPulseFlatProduction : writeVoidPulseFlatChecked;

// ReadVoidPulseFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadVoidPulseFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 16 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0xffff) >>> 0;
  value.Strength = v;
  return true;
}

// ---- type VoidLevel: the flat codec ----

function writeVoidLevelFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = (((value.Level) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeVoidLevelFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Level) || value.Level < 0 || value.Level > 7) {
    return -1;
  }
  v = (((value.Level) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteVoidLevelFlat(value, view) -> bytes written (>= 0), or -1 on a refused
// writer contract in the checked build — the production writer holds none
// of them (SPEC §5). The buffer behind view must hold VoidLevelMaxBytes.
export const WriteVoidLevelFlat = PRODUCTION ? writeVoidLevelFlatProduction : writeVoidLevelFlatChecked;

// ReadVoidLevelFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadVoidLevelFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Level = v | 0;
  return true;
}

// ---- type VoidSignalPacket: the flat codec ----

function writeVoidSignalPacketFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Lead & 0x7) | ((value.Signal.Type & 0x7) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 6;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (6 - sb);
  }
  switch (value.Signal.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      v = (((value.Signal.Pulse.Strength) & 0xffff)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 16;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (16 - sb);
      }
      break;
    }
    case 3: {
      v = (((value.Signal.Level.Level) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  v = ((value.Echo.Type & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  switch (value.Echo.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      v = (((value.Echo.Pulse.Strength) & 0xffff)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 16;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (16 - sb);
      }
      break;
    }
    case 3: {
      v = (((value.Echo.Level.Level) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  v = (((value.HistoryCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.HistoryCount; i0++) {
    const e0 = value.History[i0];
    v = ((e0.Type & 0x7)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
    switch (e0.Type) {
      case 1: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
      case 2: {
        v = (((e0.Pulse.Strength) & 0xffff)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 16;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (16 - sb);
        }
        break;
      }
      case 3: {
        v = (((e0.Level.Level) & 0x7)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 3;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (3 - sb);
        }
        break;
      }
      case 4: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeVoidSignalPacketFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Signal.Type) || value.Signal.Type < 0 || value.Signal.Type > 4) { // the tag validates BEFORE it rides (SPEC §4.8)
    return -1;
  }
  v = ((value.Lead & 0x7) | ((value.Signal.Type & 0x7) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 6;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (6 - sb);
  }
  switch (value.Signal.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      v = (((value.Signal.Pulse.Strength) & 0xffff)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 16;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (16 - sb);
      }
      break;
    }
    case 3: {
      if (!Number.isInteger(value.Signal.Level.Level) || value.Signal.Level.Level < 0 || value.Signal.Level.Level > 7) {
        return -1;
      }
      v = (((value.Signal.Level.Level) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  if (!Number.isInteger(value.Echo.Type) || value.Echo.Type < 0 || value.Echo.Type > 4) { // the tag validates BEFORE it rides (SPEC §4.8)
    return -1;
  }
  v = ((value.Echo.Type & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  switch (value.Echo.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      v = (((value.Echo.Pulse.Strength) & 0xffff)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 16;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (16 - sb);
      }
      break;
    }
    case 3: {
      if (!Number.isInteger(value.Echo.Level.Level) || value.Echo.Level.Level < 0 || value.Echo.Level.Level > 7) {
        return -1;
      }
      v = (((value.Echo.Level.Level) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  if (!Number.isInteger(value.HistoryCount) || value.HistoryCount < 0 || value.HistoryCount > 2) { // the count guards the loop; its range is a writer contract (SPEC §4.6)
    return -1;
  }
  v = (((value.HistoryCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.HistoryCount; i0++) {
    const e0 = value.History[i0];
    if (!Number.isInteger(e0.Type) || e0.Type < 0 || e0.Type > 4) { // the tag validates BEFORE it rides (SPEC §4.8)
      return -1;
    }
    v = ((e0.Type & 0x7)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 3;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (3 - sb);
    }
    switch (e0.Type) {
      case 1: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
      case 2: {
        v = (((e0.Pulse.Strength) & 0xffff)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 16;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (16 - sb);
        }
        break;
      }
      case 3: {
        if (!Number.isInteger(e0.Level.Level) || e0.Level.Level < 0 || e0.Level.Level > 7) {
          return -1;
        }
        v = (((e0.Level.Level) & 0x7)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 3;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (3 - sb);
        }
        break;
      }
      case 4: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
    }
  }
  v = ((value.Tail & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteVoidSignalPacketFlat(value, view) -> bytes written (>= 0), or -1 on a refused
// writer contract in the checked build — the production writer holds none
// of them (SPEC §5). The buffer behind view must hold VoidSignalPacketMaxBytes.
export const WriteVoidSignalPacketFlat = PRODUCTION ? writeVoidSignalPacketFlatProduction : writeVoidSignalPacketFlatChecked;

// ReadVoidSignalPacketFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadVoidSignalPacketFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Lead = v;
  br += 3;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 4) { // not a wire-legal tag (SPEC §4.8)
    return false;
  }
  value.Signal.Type = v;
  br += 3;
  switch (value.Signal.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      value.Signal.Pulse.Strength = 0;
      if (br + 16 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0xffff) >>> 0;
      value.Signal.Pulse.Strength = v;
      br += 16;
      break;
    }
    case 3: {
      value.Signal.Level.Level = 0;
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x7) >>> 0;
      value.Signal.Level.Level = v | 0;
      br += 3;
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  if (v > 4) { // not a wire-legal tag (SPEC §4.8)
    return false;
  }
  value.Echo.Type = v;
  br += 3;
  switch (value.Echo.Type) {
    case 1: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
    case 2: {
      value.Echo.Pulse.Strength = 0;
      if (br + 16 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0xffff) >>> 0;
      value.Echo.Pulse.Strength = v;
      br += 16;
      break;
    }
    case 3: {
      value.Echo.Level.Level = 0;
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x7) >>> 0;
      value.Echo.Level.Level = v | 0;
      br += 3;
      break;
    }
    case 4: {
      break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    }
  }
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.HistoryCount = v;
  br += 2;
  for (let i0 = 0; i0 < value.HistoryCount; i0++) {
    const e0 = value.History[i0];
    if (br + 3 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0x7) >>> 0;
    if (v > 4) { // not a wire-legal tag (SPEC §4.8)
      return false;
    }
    e0.Type = v;
    br += 3;
    switch (e0.Type) {
      case 1: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
      case 2: {
        e0.Pulse.Strength = 0;
        if (br + 16 > numBits) {
          return false;
        }
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = (out & 0xffff) >>> 0;
        e0.Pulse.Strength = v;
        br += 16;
        break;
      }
      case 3: {
        e0.Level.Level = 0;
        if (br + 3 > numBits) {
          return false;
        }
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = (out & 0x7) >>> 0;
        e0.Level.Level = v | 0;
        br += 3;
        break;
      }
      case 4: {
        break; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
      }
    }
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Tail = v;
  return true;
}

