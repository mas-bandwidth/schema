// Code generated by the schema compiler from Constants.schema. DO NOT EDIT.
// package example — protocol id 0x01125e6515d05ec4
//
// Storage members are PascalCase via the same mapping as the Go target, so
// the checker's collision registry covers JS for free. Wire functions return
// bool — the C++-style early-out. A schema validation failure (a wrong wire
// constant, nonzero reserved bits, an out-of-contract write) returns false
// WITHOUT latching; stream failures latch on stream.error — the runtime's own
// sticky latch. Callers get bool always; error tells the two apart.
//
// Number storage for widths of 32 bits or fewer, BigInt for 64 and 128 —
// the serialize.js value-domain seam. Checked/production comes from the
// stream object; generated code never reads NODE_ENV.

// The unit's protocol id — the hash of its schema files (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
export const ProtocolId = 0x01125e6515d05ec4n;

export const PositionUnits = 1024;

export const VelocityUnits = 1024;

export const RotationUnits = 1024;

export const MaxWorldMeters = 8192;

export const MaxPositionUnits = MaxWorldMeters * PositionUnits;

export const MaxSpeedMeters = 2048;

export const MaxVelocityUnits = MaxSpeedMeters * VelocityUnits;

export const MaxHealth = 1000;

export const MaxObjects = 10000;

export const ShipMaxLasers = 16;

export const ShipMaxMissiles = 16;

export const MaxTurretsPerShip = 256;

export const MaxCollidersPerShip = 64;

export const NumTeams = 2; // = Team.Max

export const MaxInputsPerPacket = 16;

export const MaxBlockSize = 2000;

export const MaxChatLength = 256;

