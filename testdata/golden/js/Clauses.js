// Code generated by the schema compiler from Clauses.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x91a8e85156dfe2b1

// Scratch holders for the runtime's {value} refs — single threaded per
// realm, always consumed in the same call that fills them.
const NUMBER_SCRATCH = { value: 0 };
const BIGINT_SCRATCH = { value: 0n };

// type W13
export class W13 {
  constructor() {
    this.Items = new Array(12).fill(0); // wire [0, 8191]
    this.ItemsCount = 0;
  }
}

// W13MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W13MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W13MaxBits = 160;
export const W13MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW13(value) {
  value.Items.fill(0);
  value.ItemsCount = 0;
}

export function WriteW13(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 12) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 8191) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
      return false;
    }
  }
  return true;
}

export function ReadW13(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 12)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8191)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type W17
export class W17 {
  constructor() {
    this.Items = new Array(9).fill(0); // wire [0, 131071]
    this.ItemsCount = 0;
  }
}

// W17MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W17MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W17MaxBits = 157;
export const W17MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW17(value) {
  value.Items.fill(0);
  value.ItemsCount = 0;
}

export function WriteW17(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 9) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 131071) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 17)) {
      return false;
    }
  }
  return true;
}

export function ReadW17(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 9)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 131071)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type W26
export class W26 {
  constructor() {
    this.Items = new Array(6).fill(0); // wire [0, 67108863]
    this.ItemsCount = 0;
  }
}

// W26MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W26MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W26MaxBits = 159;
export const W26MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW26(value) {
  value.Items.fill(0);
  value.ItemsCount = 0;
}

export function WriteW26(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 6) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 67108863) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 26)) {
      return false;
    }
  }
  return true;
}

export function ReadW26(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 6)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 67108863)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type W1
export class W1 {
  constructor() {
    this.Items = new Uint8Array(20); // wire [0, 1]
    this.ItemsCount = 0;
  }
}

// W1MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W1MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W1MaxBits = 25;
export const W1MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW1(value) {
  value.Items.fill(0);
  value.ItemsCount = 0;
}

export function WriteW1(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 20) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 1) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 1)) {
      return false;
    }
  }
  return true;
}

export function ReadW1(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 20)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 1)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type W52
export class W52 {
  constructor() {
    this.Items = new Array(3).fill(0n); // wire [0, 4503599627370495]
    this.ItemsCount = 0;
  }
}

// W52MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W52MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W52MaxBits = 158;
export const W52MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW52(value) {
  value.Items.fill(0n);
  value.ItemsCount = 0;
}

export function WriteW52(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (value.Items[i] > 4503599627370495n) {
      return false;
    }
    BIGINT_SCRATCH.value = BigInt.asUintN(64, value.Items[i]);
    if (!stream.serializeBits64(BIGINT_SCRATCH, 52)) {
      return false;
    }
  }
  return true;
}

export function ReadW52(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt64(BIGINT_SCRATCH, 0n, 4503599627370495n)) {
      return false;
    }
    value.Items[i] = BIGINT_SCRATCH.value;
  }
  return true;
}

// type W50
export class W50 {
  constructor() {
    this.Items = new Array(3).fill(0n); // wire [0, 1125899906842623]
    this.ItemsCount = 0;
  }
}

// W50MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// W50MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const W50MaxBits = 152;
export const W50MaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroW50(value) {
  value.Items.fill(0n);
  value.ItemsCount = 0;
}

export function WriteW50(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 3) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (value.Items[i] > 1125899906842623n) {
      return false;
    }
    BIGINT_SCRATCH.value = BigInt.asUintN(64, value.Items[i]);
    if (!stream.serializeBits64(BIGINT_SCRATCH, 50)) {
      return false;
    }
  }
  return true;
}

export function ReadW50(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 3)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!stream.serializeInt64(BIGINT_SCRATCH, 0n, 1125899906842623n)) {
      return false;
    }
    value.Items[i] = BIGINT_SCRATCH.value;
  }
  return true;
}

// type F13
export class F13 {
  constructor() {
    this.Items = new Array(7).fill(0); // wire [0, 8191]
  }
}

// F13MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// F13MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const F13MaxBits = 91;
export const F13MaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroF13(value) {
  value.Items.fill(0);
}

export function WriteF13(stream, value) {
  for (let i = 0; i < 7; i++) {
    if (!Number.isInteger(value.Items[i]) || value.Items[i] < 0 || value.Items[i] > 8191) {
      return false;
    }
    NUMBER_SCRATCH.value = value.Items[i];
    if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
      return false;
    }
  }
  return true;
}

export function ReadF13(stream, value) {
  for (let i = 0; i < 7; i++) {
    if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8191)) {
      return false;
    }
    value.Items[i] = NUMBER_SCRATCH.value;
  }
  return true;
}

// type Tri3
export class Tri3 {
  constructor() {
    this.A = 0;
    this.B = 0;
  }
}

// Tri3MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// Tri3MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const Tri3MaxBits = 3;
export const Tri3MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroTri3(value) {
  value.A = 0;
  value.B = 0;
}

export function WriteTri3(stream, value) {
  NUMBER_SCRATCH.value = value.A;
  if (!stream.serializeBits(NUMBER_SCRATCH, 1)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.B;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  return true;
}

export function ReadTri3(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 1)) {
    return false;
  }
  value.A = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  value.B = NUMBER_SCRATCH.value;
  return true;
}

// type ArrTri3
export class ArrTri3 {
  constructor() {
    this.Items = Array.from({ length: 10 }, () => new Tri3());
    this.ItemsCount = 0;
  }
}

// ArrTri3MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArrTri3MaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArrTri3MaxBits = 34;
export const ArrTri3MaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArrTri3(value) {
  for (let i = 0; i < 10; i++) {
    ZeroTri3(value.Items[i]);
  }
  value.ItemsCount = 0;
}

export function WriteArrTri3(stream, value) {
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 10) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!WriteTri3(stream, value.Items[i])) {
      return false;
    }
  }
  return true;
}

export function ReadArrTri3(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 10)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!ReadTri3(stream, value.Items[i])) {
      return false;
    }
  }
  return true;
}

// type Eleven
export class Eleven {
  constructor() {
    this.A = 0;
    this.B = 0;
  }
}

// ElevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ElevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ElevenMaxBits = 11;
export const ElevenMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroEleven(value) {
  value.A = 0;
  value.B = 0;
}

export function WriteEleven(stream, value) {
  NUMBER_SCRATCH.value = value.A;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.B;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  return true;
}

export function ReadEleven(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.A = NUMBER_SCRATCH.value;
  if (!stream.serializeBits(NUMBER_SCRATCH, 8)) {
    return false;
  }
  value.B = NUMBER_SCRATCH.value;
  return true;
}

// type ArrEleven
export class ArrEleven {
  constructor() {
    this.Items = Array.from({ length: 9 }, () => new Eleven());
  }
}

// ArrElevenMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArrElevenMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArrElevenMaxBits = 99;
export const ArrElevenMaxBytes = 16;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArrEleven(value) {
  for (let i = 0; i < 9; i++) {
    ZeroEleven(value.Items[i]);
  }
}

export function WriteArrEleven(stream, value) {
  for (let i = 0; i < 9; i++) {
    if (!WriteEleven(stream, value.Items[i])) {
      return false;
    }
  }
  return true;
}

export function ReadArrEleven(stream, value) {
  for (let i = 0; i < 9; i++) {
    if (!ReadEleven(stream, value.Items[i])) {
      return false;
    }
  }
  return true;
}

// type EmptyA
export class EmptyA {
  constructor() {
    // empty body — presence is the payload (SPEC §4.6)
  }
}

// EmptyAMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// EmptyAMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const EmptyAMaxBits = 0;
export const EmptyAMaxBytes = 0;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroEmptyA(value) {
  // empty body — nothing to reset (SPEC §4.6)
}

export function WriteEmptyA(stream, value) {
  // empty body — presence is the payload (SPEC §4.6)
  return true;
}

export function ReadEmptyA(stream, value) {
  return true;
}

// type EmptyB
export class EmptyB {
  constructor() {
    // empty body — presence is the payload (SPEC §4.6)
  }
}

// EmptyBMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// EmptyBMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const EmptyBMaxBits = 0;
export const EmptyBMaxBytes = 0;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroEmptyB(value) {
  // empty body — nothing to reset (SPEC §4.6)
}

export function WriteEmptyB(stream, value) {
  // empty body — presence is the payload (SPEC §4.6)
  return true;
}

export function ReadEmptyB(stream, value) {
  return true;
}

// EmptyUnionType: union EmptyUnion's tag — None = 0, then each variant in declared order (SPEC §4.8)
export const EmptyUnionType = Object.freeze({
  None: 0,
  A: 1,
  B: 2,
  Max: 2, // the exported extent (SPEC §4.2)
});

// EmptyUnion — at most one of the arms; Type says which. Construction is the empty
// union (None). A read zero-establishes exactly the selected arm before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
export class EmptyUnion {
  constructor() {
    this.Type = EmptyUnionType.None;
    this.A = new EmptyA();
    this.B = new EmptyB();
  }
}

// EmptyUnionMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// EmptyUnionMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const EmptyUnionMaxBits = 2;
export const EmptyUnionMaxBytes = 8;

// ZeroEmptyUnion resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm re-zeroes at its next selection.
export function ZeroEmptyUnion(value) {
  value.Type = EmptyUnionType.None;
}

export function WriteEmptyUnion(stream, value) {
  if (!Number.isInteger(value.Type) || value.Type < 0 || value.Type > 2) {
    return false; // the tag validates BEFORE it rides (SPEC §4.8)
  }
  NUMBER_SCRATCH.value = value.Type;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  switch (value.Type) {
    case EmptyUnionType.A:
      return WriteEmptyA(stream, value.A);
    case EmptyUnionType.B:
      return WriteEmptyB(stream, value.B);
  }
  return true; // None — the tag is the whole wire (SPEC §4.8)
}

export function ReadEmptyUnion(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 2)) { // rejects a tag above the count (SPEC §4.8)
    return false;
  }
  value.Type = NUMBER_SCRATCH.value;
  switch (value.Type) {
    case EmptyUnionType.A:
      ZeroEmptyA(value.A); // the selected arm starts from the zero form (SPEC §5)
      return ReadEmptyA(stream, value.A);
    case EmptyUnionType.B:
      ZeroEmptyB(value.B); // the selected arm starts from the zero form (SPEC §5)
      return ReadEmptyB(stream, value.B);
  }
  return true; // None
}

// type HoldsEmptyUnion
export class HoldsEmptyUnion {
  constructor() {
    this.Lead = 0;
    this.U = new EmptyUnion();
    this.Tail = 0;
  }
}

// HoldsEmptyUnionMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// HoldsEmptyUnionMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const HoldsEmptyUnionMaxBits = 14;
export const HoldsEmptyUnionMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroHoldsEmptyUnion(value) {
  value.Lead = 0;
  ZeroEmptyUnion(value.U);
  value.Tail = 0;
}

export function WriteHoldsEmptyUnion(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!WriteEmptyUnion(stream, value.U)) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  return true;
}

export function ReadHoldsEmptyUnion(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!ReadEmptyUnion(stream, value.U)) {
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 7)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type Strs
export class Strs {
  constructor() {
    this.Lead = 0;
    this.S = new Uint8Array(8); // string(8): max length, used length beside it (SPEC §4.7)
    this.SLength = 0;
    this.B = new Uint8Array(8); // bytes(8): fixed buffer, used length beside it (SPEC §4.7)
    this.BLength = 0;
    this.Tail = 0;
  }
}

// StrsMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// StrsMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const StrsMaxBits = 158;
export const StrsMaxBytes = 24;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroStrs(value) {
  value.Lead = 0;
  value.S.fill(0);
  value.SLength = 0;
  value.B.fill(0);
  value.BLength = 0;
  value.Tail = 0;
}

export function WriteStrs(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!Number.isInteger(value.SLength) || value.SLength < 0 || value.SLength > 8) { // the length guards the slice (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.SLength;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
    return false;
  }
  if (!Number.isInteger(value.BLength) || value.BLength < 0 || value.BLength > 8) { // the length guards the slice (§6.3); out-of-contract writes are refused
    return false;
  }
  NUMBER_SCRATCH.value = value.BLength;
  if (!stream.serializeBits(NUMBER_SCRATCH, 4)) {
    return false;
  }
  if (!stream.serializeBytes(value.B.subarray(0, value.BLength))) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadStrs(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8)) { // the length guards the slice (§6.3)
    return false;
  }
  value.SLength = NUMBER_SCRATCH.value;
  if (!stream.serializeBytes(value.S.subarray(0, value.SLength))) {
    return false;
  }
  for (let i = 0; i < value.SLength; i++) {
    if (value.S[i] === 0) { // an interior null is content the read refuses (SPEC §4.7)
      return false;
    }
  }
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 8)) { // the length guards the slice (§6.3)
    return false;
  }
  value.BLength = NUMBER_SCRATCH.value;
  if (!stream.serializeBytes(value.B.subarray(0, value.BLength))) {
    return false;
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type ArrNested
export class ArrNested {
  constructor() {
    this.Lead = 0;
    this.Items = Array.from({ length: 4 }, () => new Eleven());
    this.ItemsCount = 0;
    this.Tail = 0;
  }
}

// ArrNestedMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// ArrNestedMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const ArrNestedMaxBits = 55;
export const ArrNestedMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroArrNested(value) {
  value.Lead = 0;
  for (let i = 0; i < 4; i++) {
    ZeroEleven(value.Items[i]);
  }
  value.ItemsCount = 0;
  value.Tail = 0;
}

export function WriteArrNested(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  if (!Number.isInteger(value.ItemsCount) || value.ItemsCount < 0 || value.ItemsCount > 4) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.ItemsCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!WriteEleven(stream, value.Items[i])) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadArrNested(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 5)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 4)) { // the count guards the loop (§6.3)
    return false;
  }
  value.ItemsCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.ItemsCount; i++) {
    if (!ReadEleven(stream, value.Items[i])) {
      return false;
    }
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

// type Sole
export class Sole {
  constructor() {
    this.Only = 0;
  }
}

// SoleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// SoleMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const SoleMaxBits = 13;
export const SoleMaxBytes = 8;

// The §5 zero form: all-zero storage; specified defaults live only in construction.
export function ZeroSole(value) {
  value.Only = 0;
}

export function WriteSole(stream, value) {
  NUMBER_SCRATCH.value = value.Only;
  if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
    return false;
  }
  return true;
}

export function ReadSole(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 13)) {
    return false;
  }
  value.Only = NUMBER_SCRATCH.value;
  return true;
}

