// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xbc05d83a8135cdb9

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type RenderSprite: the flat codec ----

function writeRenderSpriteFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, BigInt.asUintN(64, value.SortKey), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.MeshId) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.MaterialId) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.Layer) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.Team & 0x3) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeRenderSpriteFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, BigInt.asUintN(64, value.SortKey), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.MeshId) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.MaterialId) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.Layer) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Team) || value.Team < 0 || value.Team > 2) { // headroom above the wire range cannot ride
    return -1;
  }
  v = (value.Team & 0x3) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteRenderSpriteFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold RenderSpriteMaxBytes.
export const WriteRenderSpriteFlat = PRODUCTION ? writeRenderSpriteFlatProduction : writeRenderSpriteFlatChecked;

// ReadRenderSpriteFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadRenderSpriteFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 138 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  value.SortKey = bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  value.MeshId = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  value.MaterialId = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 8;
  v = (out & 0xff) >>> 0;
  value.Layer = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  if (v > 2) { // headroom above the wire range is refused
    return false;
  }
  value.Team = v;
  return true;
}

// ---- type RenderBlock: the flat codec ----

function writeRenderBlockFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = (value.WorkerIndex) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.SpriteCountHint) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.SpritesCount) & 0x7f) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 7;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < value.SpritesCount; i0++) {
    const e0 = value.Sprites[i0];
    SC.setBigUint64(0, BigInt.asUintN(64, e0.SortKey), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (e0.MeshId) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (e0.MaterialId) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((e0.Layer) & 0xff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 8;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (e0.Team & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeRenderBlockFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = (value.WorkerIndex) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (value.SpriteCountHint) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.SpritesCount) || value.SpritesCount < 0 || value.SpritesCount > 64) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = ((value.SpritesCount) & 0x7f) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 7;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < value.SpritesCount; i0++) {
    const e0 = value.Sprites[i0];
    SC.setBigUint64(0, BigInt.asUintN(64, e0.SortKey), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (e0.MeshId) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (e0.MaterialId) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((e0.Layer) & 0xff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 8;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(e0.Team) || e0.Team < 0 || e0.Team > 2) { // headroom above the wire range cannot ride
      return -1;
    }
    v = (e0.Team & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteRenderBlockFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold RenderBlockMaxBytes.
export const WriteRenderBlockFlat = PRODUCTION ? writeRenderBlockFlatProduction : writeRenderBlockFlatChecked;

// ReadRenderBlockFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadRenderBlockFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 64 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  value.WorkerIndex = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  value.SpriteCountHint = v;
  if (br + 7 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 7;
  v = (out & 0x7f) >>> 0;
  if (v > 64) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.SpritesCount = v;
  if (br + value.SpritesCount * 138 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.SpritesCount; i0++) {
    const e0 = value.Sprites[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    SC.setUint32(0, v, true);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    e0.SortKey = bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    e0.MeshId = v;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    e0.MaterialId = v;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 8;
    v = (out & 0xff) >>> 0;
    e0.Layer = v;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 2;
    v = (out & 0x3) >>> 0;
    if (v > 2) { // headroom above the wire range is refused
      return false;
    }
    e0.Team = v;
  }
  return true;
}

