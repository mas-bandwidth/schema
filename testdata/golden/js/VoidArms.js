// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

// Scratch holders for the runtime's {value} refs — single threaded per
// realm, always consumed in the same call that fills them.
const NUMBER_SCRATCH = { value: 0 };

// type VoidPulse
export class VoidPulse {
  constructor() {
    this.Strength = 0;
  }
}

// VoidPulseMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidPulseMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const VoidPulseMaxBits = 16;
export const VoidPulseMaxBytes = 8;

// InitVoidPulse restores fresh construction values in place, preserving storage.
export function InitVoidPulse(value) {
  value.Strength = 0;
}

// The §5 zero form: all-zero storage, without declared defaults or birth counts.
export function ZeroVoidPulse(value) {
  value.Strength = 0;
}

export function WriteVoidPulse(stream, value) {
  NUMBER_SCRATCH.value = value.Strength;
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  return true;
}

export function ReadVoidPulse(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 16)) {
    return false;
  }
  value.Strength = NUMBER_SCRATCH.value;
  return true;
}

// type VoidLevel
export class VoidLevel {
  constructor() {
    this.Level = 0; // wire [0, 7]
  }
}

// VoidLevelMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidLevelMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const VoidLevelMaxBits = 3;
export const VoidLevelMaxBytes = 8;

// InitVoidLevel restores fresh construction values in place, preserving storage.
export function InitVoidLevel(value) {
  value.Level = 0;
}

// The §5 zero form: all-zero storage, without declared defaults or birth counts.
export function ZeroVoidLevel(value) {
  value.Level = 0;
}

export function WriteVoidLevel(stream, value) {
  if (!Number.isInteger(value.Level) || value.Level < 0 || value.Level > 7) {
    return false;
  }
  NUMBER_SCRATCH.value = value.Level;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadVoidLevel(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 7)) {
    return false;
  }
  value.Level = NUMBER_SCRATCH.value;
  return true;
}

// VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
export const VoidSignalType = Object.freeze({
  None: 0,
  Ping: 1,
  Pulse: 2,
  Level: 3,
  Ack: 4,
  Count: 4, // the declared variant count (SPEC §4.2)
  Max: 4, // the exported extent (SPEC §4.2)
});

// EnumNameVoidSignalType: debug/log/tooling name for any VoidSignalType wire value —
// out-of-set values (wire-legal up to the declared max) name as "???"
export function EnumNameVoidSignalType(value) {
  switch (value) {
    case VoidSignalType.None:
      return "None";
    case VoidSignalType.Ping:
      return "Ping";
    case VoidSignalType.Pulse:
      return "Pulse";
    case VoidSignalType.Level:
      return "Level";
    case VoidSignalType.Ack:
      return "Ack";
    default:
      return "???";
  }
}

// VoidSignal — at most one of the arms; Type says which. Construction is the empty
// union (None). Every read initializes the selected arm as a fresh value
// before decoding it; unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
export class VoidSignal {
  constructor() {
    this.Type = VoidSignalType.None;
    this.Pulse = new VoidPulse();
    this.Level = new VoidLevel();
  }
}

// VoidSignalMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// VoidSignalMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const VoidSignalMaxBits = 19;
export const VoidSignalMaxBytes = 8;

// ZeroVoidSignal resets value to the §5 zero form — the empty union. The tag alone
// resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
// is unselected at None; an arm is initialized at its next selection.
export function ZeroVoidSignal(value) {
  value.Type = VoidSignalType.None;
}

export function WriteVoidSignal(stream, value) {
  if (!Number.isInteger(value.Type) || value.Type < 0 || value.Type > 4) {
    return false; // the tag validates BEFORE it rides (SPEC §4.8)
  }
  NUMBER_SCRATCH.value = value.Type;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  switch (value.Type) {
    case VoidSignalType.Ping:
      return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    case VoidSignalType.Pulse:
      return WriteVoidPulse(stream, value.Pulse);
    case VoidSignalType.Level:
      return WriteVoidLevel(stream, value.Level);
    case VoidSignalType.Ack:
      return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
  }
  return true; // None — the tag is the whole wire (SPEC §4.8)
}

export function ReadVoidSignal(stream, value) {
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 4)) { // rejects a tag above the count (SPEC §4.8)
    return false;
  }
  value.Type = NUMBER_SCRATCH.value;
  switch (value.Type) {
    case VoidSignalType.Ping:
      return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
    case VoidSignalType.Pulse:
      InitVoidPulse(value.Pulse); // fresh declared defaults on every selection
      return ReadVoidPulse(stream, value.Pulse);
    case VoidSignalType.Level:
      InitVoidLevel(value.Level); // fresh declared defaults on every selection
      return ReadVoidLevel(stream, value.Level);
    case VoidSignalType.Ack:
      return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
  }
  return true; // None
}

// type VoidSignalPacket
export class VoidSignalPacket {
  constructor() {
    this.Lead = 0;
    this.Signal = new VoidSignal();
    this.Echo = new VoidSignal();
    this.History = Array.from({ length: 2 }, () => new VoidSignal());
    this.HistoryCount = 0;
    this.Tail = 0;
  }
}

// VoidSignalPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidSignalPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
export const VoidSignalPacketMaxBits = 84;
export const VoidSignalPacketMaxBytes = 16;

// InitVoidSignalPacket restores fresh construction values in place, preserving storage.
export function InitVoidSignalPacket(value) {
  value.Lead = 0;
  value.Signal.Type = 0; // None; dormant arms keep their storage
  value.Echo.Type = 0; // None; dormant arms keep their storage
  for (let initIndex0 = 0; initIndex0 < 2; initIndex0++) {
    const initValue0 = value.History[initIndex0];
    initValue0.Type = 0; // None; dormant arms keep their storage
  }
  value.HistoryCount = 0;
  value.Tail = 0;
}

// The §5 zero form: all-zero storage, without declared defaults or birth counts.
export function ZeroVoidSignalPacket(value) {
  value.Lead = 0;
  ZeroVoidSignal(value.Signal);
  ZeroVoidSignal(value.Echo);
  for (let i = 0; i < 2; i++) {
    ZeroVoidSignal(value.History[i]);
  }
  value.HistoryCount = 0;
  value.Tail = 0;
}

export function WriteVoidSignalPacket(stream, value) {
  NUMBER_SCRATCH.value = value.Lead;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  if (!WriteVoidSignal(stream, value.Signal)) {
    return false;
  }
  if (!WriteVoidSignal(stream, value.Echo)) {
    return false;
  }
  if (!Number.isInteger(value.HistoryCount) || value.HistoryCount < 0 || value.HistoryCount > 2) { // the count guards the loop (§6.3)
    return false;
  }
  NUMBER_SCRATCH.value = value.HistoryCount;
  if (!stream.serializeBits(NUMBER_SCRATCH, 2)) {
    return false;
  }
  for (let i = 0; i < value.HistoryCount; i++) {
    if (!WriteVoidSignal(stream, value.History[i])) {
      return false;
    }
  }
  NUMBER_SCRATCH.value = value.Tail;
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  return true;
}

export function ReadVoidSignalPacket(stream, value) {
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Lead = NUMBER_SCRATCH.value;
  if (!ReadVoidSignal(stream, value.Signal)) {
    return false;
  }
  if (!ReadVoidSignal(stream, value.Echo)) {
    return false;
  }
  if (!stream.serializeInt(NUMBER_SCRATCH, 0, 2)) { // the count guards the loop (§6.3)
    return false;
  }
  value.HistoryCount = NUMBER_SCRATCH.value;
  for (let i = 0; i < value.HistoryCount; i++) {
    if (!ReadVoidSignal(stream, value.History[i])) {
      return false;
    }
  }
  if (!stream.serializeBits(NUMBER_SCRATCH, 3)) {
    return false;
  }
  value.Tail = NUMBER_SCRATCH.value;
  return true;
}

