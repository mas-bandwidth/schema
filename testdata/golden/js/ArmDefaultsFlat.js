// Code generated by the schema compiler from ArmDefaults.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7
//
// THE FLAT TIER — the shipped JavaScript wire path: a single-word 32-bit
// bitpacker inlined at every field (byte-identical wire to serialize.js),
// constant widths and masks, zero function calls. Same generated classes,
// same bytes as the runtime tier (a standing CI gate); the runtime tier
// remains the diagnostic and reference surface — re-read a failing buffer
// through it to learn WHICH operation failed and why.
//
// Write<Name>Flat(value, view) -> bytes written, or -1 when the checked
// writer refuses an out-of-contract value (the production writer trusts
// the caller — serialize.js src/mode.js's own NODE_ENV fork, frozen at
// module load). Read<Name>Flat(value, view, numBits) -> bool, the family
// read verdict; reader obligations ride in every mode.
//
// Buffers are caller-owned DataViews. Write buffers: at least <Name>MaxBytes
// (the constants live in the runtime-tier module). Read buffers: at least
// FLAT_READ_SLACK = 8 bytes past the payload — 64-bit windows load
// unconditionally; copy exactly-sized receive buffers into a persistent
// MaxBytes + 8 scratch first.

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type DefaultArm: the flat codec ----

function writeDefaultArmFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (value.EntriesCount < 0 || value.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
    return -1;
  }
  v = (((value.EntriesCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.EntriesCount; i0++) {
    const e0 = value.Entries[i0];
    v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = ((e0.Preferred & 0xf)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 4;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (4 - sb);
    }
  }
  v = (((value.Marker) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeDefaultArmFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.EntriesCount) || value.EntriesCount < 0 || value.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
    return -1;
  }
  v = (((value.EntriesCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.EntriesCount; i0++) {
    const e0 = value.Entries[i0];
    if (!Number.isInteger(e0.Preferred) || e0.Preferred < 0 || e0.Preferred > 15) { // headroom above the wire range cannot ride
      return -1;
    }
    v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = ((e0.Preferred & 0xf)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 4;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (4 - sb);
    }
  }
  if (!Number.isInteger(value.Marker) || value.Marker < 0 || value.Marker > 7) {
    return -1;
  }
  v = (((value.Marker) & 0x7)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 3;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (3 - sb);
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteDefaultArmFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold DefaultArmMaxBytes.
export const WriteDefaultArmFlat = PRODUCTION ? writeDefaultArmFlatProduction : writeDefaultArmFlatChecked;

// ReadDefaultArmFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadDefaultArmFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.EntriesCount = v;
  br += 2;
  if (br + value.EntriesCount * 36 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.EntriesCount; i0++) {
    const e0 = value.Entries[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    e0.Retries = v | 0;
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0xf) >>> 0;
    e0.Preferred = v;
    br += 4;
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Marker = v | 0;
  return true;
}

// ---- type DefaultChoicePacket: the flat codec ----

function writeDefaultChoicePacketFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  v = ((value.Choice.Type & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  switch (value.Choice.Type) {
    case 1: {
      if (value.Choice.First.EntriesCount < 0 || value.Choice.First.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
        return -1;
      }
      v = (((value.Choice.First.EntriesCount) & 0x3)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 2;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (2 - sb);
      }
      for (let i0 = 0; i0 < value.Choice.First.EntriesCount; i0++) {
        const e0 = value.Choice.First.Entries[i0];
        v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = ((e0.Preferred & 0xf)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 4;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (4 - sb);
        }
      }
      v = (((value.Choice.First.Marker) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 2: {
      if (value.Choice.Second.EntriesCount < 0 || value.Choice.Second.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
        return -1;
      }
      v = (((value.Choice.Second.EntriesCount) & 0x3)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 2;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (2 - sb);
      }
      for (let i0 = 0; i0 < value.Choice.Second.EntriesCount; i0++) {
        const e0 = value.Choice.Second.Entries[i0];
        v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = ((e0.Preferred & 0xf)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 4;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (4 - sb);
        }
      }
      v = (((value.Choice.Second.Marker) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeDefaultChoicePacketFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Choice.Type) || value.Choice.Type < 0 || value.Choice.Type > 2) { // the tag validates BEFORE it rides (SPEC §4.8)
    return -1;
  }
  v = ((value.Choice.Type & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  switch (value.Choice.Type) {
    case 1: {
      if (!Number.isInteger(value.Choice.First.EntriesCount) || value.Choice.First.EntriesCount < 0 || value.Choice.First.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
        return -1;
      }
      v = (((value.Choice.First.EntriesCount) & 0x3)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 2;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (2 - sb);
      }
      for (let i0 = 0; i0 < value.Choice.First.EntriesCount; i0++) {
        const e0 = value.Choice.First.Entries[i0];
        if (!Number.isInteger(e0.Preferred) || e0.Preferred < 0 || e0.Preferred > 15) { // headroom above the wire range cannot ride
          return -1;
        }
        v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = ((e0.Preferred & 0xf)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 4;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (4 - sb);
        }
      }
      if (!Number.isInteger(value.Choice.First.Marker) || value.Choice.First.Marker < 0 || value.Choice.First.Marker > 7) {
        return -1;
      }
      v = (((value.Choice.First.Marker) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
    case 2: {
      if (!Number.isInteger(value.Choice.Second.EntriesCount) || value.Choice.Second.EntriesCount < 0 || value.Choice.Second.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
        return -1;
      }
      v = (((value.Choice.Second.EntriesCount) & 0x3)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 2;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (2 - sb);
      }
      for (let i0 = 0; i0 < value.Choice.Second.EntriesCount; i0++) {
        const e0 = value.Choice.Second.Entries[i0];
        if (!Number.isInteger(e0.Preferred) || e0.Preferred < 0 || e0.Preferred > 15) { // headroom above the wire range cannot ride
          return -1;
        }
        v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 32;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (32 - sb);
        }
        v = ((e0.Preferred & 0xf)) >>> 0;
        lo = (lo | (v << sb)) >>> 0;
        sb += 4;
        if (sb >= 32) {
          view.setUint32(wi, lo, true);
          wi += 4;
          sb -= 32;
          lo = sb === 0 ? 0 : v >>> (4 - sb);
        }
      }
      if (!Number.isInteger(value.Choice.Second.Marker) || value.Choice.Second.Marker < 0 || value.Choice.Second.Marker > 7) {
        return -1;
      }
      v = (((value.Choice.Second.Marker) & 0x7)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 3;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (3 - sb);
      }
      break;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteDefaultChoicePacketFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold DefaultChoicePacketMaxBytes.
export const WriteDefaultChoicePacketFlat = PRODUCTION ? writeDefaultChoicePacketFlatProduction : writeDefaultChoicePacketFlatChecked;

// ReadDefaultChoicePacketFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadDefaultChoicePacketFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // not a wire-legal tag (SPEC §4.8)
    return false;
  }
  value.Choice.Type = v;
  br += 2;
  switch (value.Choice.Type) {
    case 1: {
      for (let initIndex0 = 0; initIndex0 < 2; initIndex0++) {
        const initValue0 = value.Choice.First.Entries[initIndex0];
        initValue0.Retries = -1;
        initValue0.Preferred = 3;
      }
      value.Choice.First.EntriesCount = 0;
      value.Choice.First.Marker = 0;
      if (br + 2 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x3) >>> 0;
      if (v > 2) { // the count guards the loop — reject, never clamp
        return false;
      }
      value.Choice.First.EntriesCount = v;
      br += 2;
      if (br + value.Choice.First.EntriesCount * 36 > numBits) {
        return false;
      }
      for (let i0 = 0; i0 < value.Choice.First.EntriesCount; i0++) {
        const e0 = value.Choice.First.Entries[i0];
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = out >>> 0;
        e0.Retries = v | 0;
        br += 32;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = (out & 0xf) >>> 0;
        e0.Preferred = v;
        br += 4;
      }
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x7) >>> 0;
      value.Choice.First.Marker = v | 0;
      br += 3;
      break;
    }
    case 2: {
      for (let initIndex0 = 0; initIndex0 < 2; initIndex0++) {
        const initValue0 = value.Choice.Second.Entries[initIndex0];
        initValue0.Retries = -1;
        initValue0.Preferred = 3;
      }
      value.Choice.Second.EntriesCount = 0;
      value.Choice.Second.Marker = 0;
      if (br + 2 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x3) >>> 0;
      if (v > 2) { // the count guards the loop — reject, never clamp
        return false;
      }
      value.Choice.Second.EntriesCount = v;
      br += 2;
      if (br + value.Choice.Second.EntriesCount * 36 > numBits) {
        return false;
      }
      for (let i0 = 0; i0 < value.Choice.Second.EntriesCount; i0++) {
        const e0 = value.Choice.Second.Entries[i0];
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = out >>> 0;
        e0.Retries = v | 0;
        br += 32;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        v = (out & 0xf) >>> 0;
        e0.Preferred = v;
        br += 4;
      }
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      v = (out & 0x7) >>> 0;
      value.Choice.Second.Marker = v | 0;
      br += 3;
      break;
    }
  }
  return true;
}

// ---- type DefaultBulkArm: the flat codec ----

function writeDefaultBulkArmFlatProduction(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (value.Payload.EntriesCount < 0 || value.Payload.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
    return -1;
  }
  v = (((value.Payload.EntriesCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.Payload.EntriesCount; i0++) {
    const e0 = value.Payload.Entries[i0];
    v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = ((e0.Preferred & 0xf)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 4;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (4 - sb);
    }
  }
  v = (((value.Payload.Marker) & 0x7) | (((value.DataLength) & 0x3) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.DataLength; i0 += 4) {
      v = (value.Data[i0] | (value.Data[i0 + 1] << 8) | (value.Data[i0 + 2] << 16) | (value.Data[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.DataLength; i0++) {
      v = value.Data[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeDefaultBulkArmFlatChecked(value, view) {
  let v = 0;
  let lo = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Payload.EntriesCount) || value.Payload.EntriesCount < 0 || value.Payload.EntriesCount > 2) { // the count guards the loop; a count outside its wire range is refused in every build (SPEC §4.6)
    return -1;
  }
  v = (((value.Payload.EntriesCount) & 0x3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 2;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (2 - sb);
  }
  for (let i0 = 0; i0 < value.Payload.EntriesCount; i0++) {
    const e0 = value.Payload.Entries[i0];
    if (!Number.isInteger(e0.Preferred) || e0.Preferred < 0 || e0.Preferred > 15) { // headroom above the wire range cannot ride
      return -1;
    }
    v = (((e0.Retries >>> 0) >>> 0)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 32;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (32 - sb);
    }
    v = ((e0.Preferred & 0xf)) >>> 0;
    lo = (lo | (v << sb)) >>> 0;
    sb += 4;
    if (sb >= 32) {
      view.setUint32(wi, lo, true);
      wi += 4;
      sb -= 32;
      lo = sb === 0 ? 0 : v >>> (4 - sb);
    }
  }
  if (!Number.isInteger(value.Payload.Marker) || value.Payload.Marker < 0 || value.Payload.Marker > 7) {
    return -1;
  }
  if (!Number.isInteger(value.DataLength) || value.DataLength < 0 || value.DataLength > 2) { // the length guards the slice; out-of-contract writes are refused
    return -1;
  }
  v = (((value.Payload.Marker) & 0x7) | (((value.DataLength) & 0x3) << 3)) >>> 0;
  lo = (lo | (v << sb)) >>> 0;
  sb += 5;
  if (sb >= 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    sb -= 32;
    lo = sb === 0 ? 0 : v >>> (5 - sb);
  }
  sb = (sb + 7) & -8;
  if (sb === 32) {
    view.setUint32(wi, lo, true);
    wi += 4;
    lo = 0;
    sb = 0;
  }
  {
    let i0 = 0;
    for (; i0 + 4 <= value.DataLength; i0 += 4) {
      v = (value.Data[i0] | (value.Data[i0 + 1] << 8) | (value.Data[i0 + 2] << 16) | (value.Data[i0 + 3] << 24)) >>> 0;
      lo = (lo | (v << sb)) >>> 0;
      sb += 32;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (32 - sb);
      }
    }
    for (; i0 < value.DataLength; i0++) {
      v = value.Data[i0];
      lo = (lo | (v << sb)) >>> 0;
      sb += 8;
      if (sb >= 32) {
        view.setUint32(wi, lo, true);
        wi += 4;
        sb -= 32;
        lo = sb === 0 ? 0 : v >>> (8 - sb);
      }
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteDefaultBulkArmFlat(value, view) -> bytes written (>= 0), or -1 on a refusal: a
// count outside its wire range in every build (SPEC §4.6), and any other
// contract in the checked build. The buffer behind view must hold DefaultBulkArmMaxBytes.
export const WriteDefaultBulkArmFlat = PRODUCTION ? writeDefaultBulkArmFlatProduction : writeDefaultBulkArmFlatChecked;

// ReadDefaultBulkArmFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadDefaultBulkArmFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.Payload.EntriesCount = v;
  br += 2;
  if (br + value.Payload.EntriesCount * 36 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.Payload.EntriesCount; i0++) {
    const e0 = value.Payload.Entries[i0];
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = out >>> 0;
    e0.Retries = v | 0;
    br += 32;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    v = (out & 0xf) >>> 0;
    e0.Preferred = v;
    br += 4;
  }
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x7) >>> 0;
  value.Payload.Marker = v | 0;
  br += 3;
  if (br + 2 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  v = (out & 0x3) >>> 0;
  if (v > 2) { // the length guards the slice — reject, never clamp
    return false;
  }
  value.DataLength = v;
  br += 2;
  s2 = br & 7;
  if (s2 !== 0) {
    if (br + (8 - s2) > numBits) {
      return false;
    }
    if ((view.getUint8(br >>> 3) >>> s2) !== 0) { // nonzero padding is refused
      return false;
    }
    br += 8 - s2;
  }
  if (br + value.DataLength * 8 > numBits) {
    return false;
  }
  bi = br >>> 3;
  for (let i0 = 0; i0 < value.DataLength; i0++) {
    value.Data[i0] = view.getUint8(bi + i0);
  }
  br += value.DataLength * 8;
  return true;
}

