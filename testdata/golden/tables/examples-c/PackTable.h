/* Code generated by the schema compiler from Pack.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package tabledemo — protocol id 0x9924bf6d375ec24d (packets only: tables version by field id, not by protocol id)
   The TABLE wire (evolution-tolerant, docs/SPEC-TABLES.md): no serialize
   dependency — includable from any TU. Compile the .c beside this header
   to use the reflection descriptors or the text form. */

#ifndef SCHEMA_TABLEDEMO_PACKTABLE_H
#define SCHEMA_TABLEDEMO_PACKTABLE_H

#include <stdint.h>
#include <stddef.h> /* offsetof, for the reflection descriptors */
#include <string.h> /* memcpy, memset — the prefill and the wire's raw moves */
#include <assert.h> /* the keyed accessor's None refusal, in a debug build */
#include <stdlib.h> /* and its abort, which NDEBUG does not remove */

#include "Pack.h"

#ifndef SCHEMA_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define SCHEMA_UNUSED __attribute__((unused))
#else
#define SCHEMA_UNUSED
#endif
#endif

#ifdef __cplusplus
extern "C" {
#endif

#ifndef SCHEMA_TABLEDEMO_TABLE_PRIMITIVES
#define SCHEMA_TABLEDEMO_TABLE_PRIMITIVES

/* THE CODEC DOES NOT DEPEND ON THE COMPILER'S INLINING BUDGET. A table of a
   realistic field count emits one large body per type, and the cursor a body
   writes through lives in the caller's TableWriter: across a call boundary that
   cursor round-trips through memory, and a uint8_t * store may alias the writer
   itself, so every put reloads it. When a budget runs out mid-body the codec
   silently degrades to that shape. Forcing the primitives and the fixed-class
   bodies inline is what keeps the cursor in registers and lets adjacent
   constant framing bytes merge into one store.

   The spelling is FEATURE TESTED rather than assumed, exactly as the packet
   emitter's own inlining demand is (SPEC §6.1's C column), and the macro is
   package-scoped for the same reason the guard above is: a consumer compiles
   several packages' Table.h files in one translation unit, and a macro every
   one of them spelled the same way would be a redefinition. */
#ifndef SCHEMA_TABLEDEMO_TABLE_INLINE
#if defined( _MSC_VER )
#define SCHEMA_TABLEDEMO_TABLE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_TABLEDEMO_TABLE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_TABLEDEMO_TABLE_INLINE inline
#endif
#endif

/* A COMPILE-TIME assertion under -std=c99, which has none of its own. C11's
   _Static_assert carries the message into the build log verbatim and is used
   wherever the compiler has it; everywhere else the negative array bound is
   the same refusal with a tag a reader can find. The tag is unique per record
   and field, and each record is asserted by the ONE header that declares it,
   so no two of these ever meet. */
#ifndef SCHEMA_TABLE_STATIC_ASSERT
#if defined( __STDC_VERSION__ ) && __STDC_VERSION__ >= 201112L
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) _Static_assert( cond, message )
#elif defined( __cplusplus ) && __cplusplus >= 201103L
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) static_assert( cond, message )
#else
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) typedef char schema_table_assert_##tag[ ( cond ) ? 1 : -1 ]
#endif
#endif

/* A record's ALIGNMENT, which the cook's layout contract asserts and which the
   cook's Open compares the header's alignment word against (§7.1, §20.3).
   C99 has no alignof either; C11's _Alignof, the two GNU spellings and MSVC's
   cover every compiler this repo builds under, and the last form is the
   portable definition of the same number. */
#ifndef SCHEMA_TABLE_ALIGNOF
#if defined( __STDC_VERSION__ ) && __STDC_VERSION__ >= 201112L
#define SCHEMA_TABLE_ALIGNOF( T ) _Alignof( T )
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_TABLE_ALIGNOF( T ) __alignof__( T )
#elif defined( _MSC_VER )
#define SCHEMA_TABLE_ALIGNOF( T ) __alignof( T )
#else
#define SCHEMA_TABLE_ALIGNOF( T ) ( (size_t) offsetof( struct { char schema_pad_; T schema_member_; }, schema_member_ ) )
#endif
#endif

/* The table-wire read report — the permissive contract's ledger. Silence
   (all zero) means the data matched this reader's schema exactly. */
typedef struct TableReport
{
    int32_t unknown;       /* unknown field ids skipped (newer data) */
    int32_t kind_mismatch; /* known id, changed type — skipped, never misdecoded */
    int32_t clamped;       /* out-of-range values clamped to declared bounds */
    /* a key the TEXT form saw twice: last wins, and the repeat is counted
       (docs/SPEC-TABLES.md §16.2). The wire never raises it — a body carrying an
       id twice is legal input whose last occurrence wins, silently (§3). */
    int32_t duplicate;
    int malformed;         /* framing damage; decode stopped, partial result kept */
} TableReport;

/* ---- reflection (tables only, docs/SPEC-TABLES.md) ----

   Static field descriptors for every type in the table closure: name, wire
   id/kind, storage offset, bounds, ranges, enum names and branch guards —
   enough to walk, print, diff, edit or bind any table value at runtime with
   no schema files. <Name>TableType() returns <Name>'s descriptor. */

struct TableTypeInfo;

/* One arm of a union field: where its payload sits inside the union's storage
   and what its payload looks like. The arm's NAME and its table-wire id ride
   in the field's own vocabulary table at the same tag, so nothing is spelled
   twice (docs/SPEC-TABLES.md §8). */
typedef struct TableUnionArmInfo
{
    uint32_t offset;                    /* offsetof the arm's payload within the union storage */
    const struct TableTypeInfo * table; /* the arm payload's descriptor */
} TableUnionArmInfo;

/* A union field's shape: the tag, and the arms indexed by it. Arms run
   [0, enum_max]; index 0 is the EMPTY arm and carries no payload. */
typedef struct TableUnionInfo
{
    uint32_t tag_offset; /* offsetof the tag within the union storage */
    uint32_t tag_size;   /* sizeof the tag */
    const TableUnionArmInfo * arms;
} TableUnionInfo;

/* ONE ENTRY OF A VOCABULARY, and the whole of what C++ spells as a pair of
   captureless lambdas. An enum's values, a union's arms and a flags field's
   BITS are each a named set indexed by [0, enum_max], so the set rides as an
   ARRAY indexed the same way: no function per enum, and therefore no name per
   enum for a schema to collide with (docs/SPEC-TABLES.md §11).

   name is the variant's spelling, or NULL for a value the declared set does
   not name. id is the TABLE-WIRE id it rides under (§5) — the hash of the
   variant's name, 0 for the reserved None and for the empty union arm. A
   FLAGS field's variants are BIT POSITIONS and have no per-variant wire id
   (§4), so has_ids is 0 there and 1 for an enum and a union: a vocabulary
   with names and no ids is what says "flags". */
typedef struct TableVariantInfo
{
    const char * name;
    uint16_t id;
} TableVariantInfo;

typedef struct TableFieldInfo
{
    const char * name;      /* schema field name, e.g. "health" */
    const char * json;      /* the TEXT form's key: the json = "key" attribute, else name (§16.3) */
    const char * type_name; /* schema type name, e.g. "float32", "Grade" */
    uint16_t id;            /* table-wire field id (name hash; the was alias's hash after a rename) */
    uint8_t kind;           /* table-wire kind; for arrays/strings/bytes, the ELEMENT kind */
    int is_array;           /* fixed or counted array (bytes included) */
    int counted;            /* a _count/_length int32 companion exists (counted arrays, strings, bytes) */
    int optional;           /* a ?T field: a _present companion decides whether it rides */
    int32_t array_bound;    /* array capacity / string max length; 0 for plain scalars */
    uint32_t offset;        /* offsetof the storage member */
    uint32_t elem_size;     /* sizeof the member (element size for arrays) */
    uint32_t count_offset;  /* offsetof the _count/_length companion, or 0xffffffff */
    uint32_t present_offset; /* offsetof the _present companion, or 0xffffffff */
    const struct TableTypeInfo * table; /* nested table's descriptor, or NULL */
    int has_range;          /* a declared [min, max] (int or float) */
    double range_min;       /* NOTE: int64 ranges beyond 2^53 lose precision here */
    double range_max;
    int64_t enum_max;       /* enums: highest valid value (None = 0 always valid);
                               unions: the arm count (tag range [0, enum_max]);
                               flags: the highest declared BIT INDEX; else -1 */
    /* the vocabulary, indexed the same way enum_max bounds: an enum's value ->
       (name, id), a union's tag -> (arm name, arm id), a FLAGS field's bit
       index -> (variant name, 0). NULL for every other kind. */
    const TableVariantInfo * variants;
    int has_variant_ids;    /* 1 for an enum or a union, 0 for flags (§4) */
    /* an ENUM-KEYED array (docs/SPEC-TABLES.md §2.4): the array has one slot per
       variant of key_type_name, the key k at index k-1, and its slots ride
       under variant ids rather than positions. keys is the key's vocabulary,
       indexed by the KEY — walk [0, array_bound) and ask about index + 1 to
       print slots by name. NULL on every other field. */
    const char * key_type_name;
    const TableVariantInfo * keys;
    int64_t key_max;        /* the key enum's highest value, bounding keys */
    /* union fields: the tag and its arms. NULL for every other kind. */
    const TableUnionInfo * arms;
    const char * guard;     /* branch guard, e.g. "at_rest" or "!at_rest"; "" if unguarded */
} TableFieldInfo;

typedef struct TableTypeInfo
{
    const char * name;   /* schema type name */
    uint32_t size;       /* sizeof the storage struct */
    int32_t num_fields;
    const TableFieldInfo * fields;
    /* put one instance back at its declared defaults, in place. A generic
       walker that fills a value has to be able to establish the defaults an
       absent field takes, and it holds no type to spell — this is the one
       thing the descriptors could not express without it. It is <Name>ResetRaw,
       the void * form of <Name>Reset and the same code. */
    void (*reset)( void * storage );
} TableTypeInfo;

typedef struct TableWriter
{
    uint8_t * buffer;
    int64_t capacity;
    int64_t offset;
    int overflow;
} TableWriter;

static SCHEMA_UNUSED TableWriter TableWriterMake( uint8_t * buffer, int64_t capacity )
{
    TableWriter w;
    w.buffer = buffer;
    w.capacity = capacity;
    w.offset = 0;
    w.overflow = 0;
    return w;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterRaw( TableWriter * w, const void * data, int64_t bytes )
{
    if ( w->offset + bytes > w->capacity ) { w->overflow = 1; return; }
    memcpy( w->buffer + w->offset, data, (size_t) bytes );
    w->offset += bytes;
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterPut8( TableWriter * w, uint8_t v ) { TableWriterRaw( w, &v, 1 ); }
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterPut16( TableWriter * w, uint16_t v )
{
    uint8_t b[2];
    b[0] = (uint8_t) v; b[1] = (uint8_t) ( v >> 8 );
    TableWriterRaw( w, b, 2 );
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterPut32( TableWriter * w, uint32_t v )
{
    uint8_t b[4];
    b[0] = (uint8_t) v; b[1] = (uint8_t) ( v >> 8 ); b[2] = (uint8_t) ( v >> 16 ); b[3] = (uint8_t) ( v >> 24 );
    TableWriterRaw( w, b, 4 );
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterPut64( TableWriter * w, uint64_t v )
{
    TableWriterPut32( w, (uint32_t) v );
    TableWriterPut32( w, (uint32_t) ( v >> 32 ) );
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE void TableWriterPatch32( TableWriter * w, int64_t at, uint32_t v )
{
    if ( at + 4 > w->capacity ) { w->overflow = 1; return; }
    w->buffer[at] = (uint8_t) v; w->buffer[at+1] = (uint8_t) ( v >> 8 );
    w->buffer[at+2] = (uint8_t) ( v >> 16 ); w->buffer[at+3] = (uint8_t) ( v >> 24 );
}

typedef struct TableReader
{
    const uint8_t * buffer;
    int64_t size;
    int64_t offset;
    TableReport * report;
} TableReader;

static SCHEMA_UNUSED TableReader TableReaderMake( const uint8_t * buffer, int64_t size, TableReport * report )
{
    TableReader r;
    r.buffer = buffer;
    r.size = size;
    r.offset = 0;
    r.report = report;
    return r;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int TableReaderHas( const TableReader * r, int64_t bytes ) { return r->offset + bytes <= r->size; }
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE uint8_t TableReaderGet8( TableReader * r ) { return r->buffer[r->offset++]; }
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE uint16_t TableReaderGet16( TableReader * r )
{
    uint16_t v = (uint16_t) ( (uint16_t) r->buffer[r->offset] | ( (uint16_t) r->buffer[r->offset+1] << 8 ) );
    r->offset += 2;
    return v;
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE uint32_t TableReaderGet32( TableReader * r )
{
    uint32_t v = (uint32_t) r->buffer[r->offset] | ( (uint32_t) r->buffer[r->offset+1] << 8 )
               | ( (uint32_t) r->buffer[r->offset+2] << 16 ) | ( (uint32_t) r->buffer[r->offset+3] << 24 );
    r->offset += 4;
    return v;
}
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE uint64_t TableReaderGet64( TableReader * r )
{
    uint64_t lo = TableReaderGet32( r );
    uint64_t hi = TableReaderGet32( r );
    return lo | ( hi << 32 );
}

/* skip one payload by kind; 0 = framing damage */
static SCHEMA_UNUSED int TableReaderSkip( TableReader * r, uint8_t kind )
{
    switch ( kind )
    {
        case 1: case 2: case 6:
            if ( !TableReaderHas( r, 1 ) ) { return 0; }
            r->offset += 1; return 1;
        case 3: case 7:
            if ( !TableReaderHas( r, 2 ) ) { return 0; }
            r->offset += 2; return 1;
        case 4: case 8: case 10:
            if ( !TableReaderHas( r, 4 ) ) { return 0; }
            r->offset += 4; return 1;
        case 5: case 9: case 11:
            if ( !TableReaderHas( r, 8 ) ) { return 0; }
            r->offset += 8; return 1;
        case 12: case 13: case 14: case 16:
        {
            uint32_t n;
            if ( !TableReaderHas( r, 4 ) ) { return 0; }
            n = TableReaderGet32( r );
            if ( !TableReaderHas( r, n ) ) { return 0; }
            r->offset += n;
            return 1;
        }
        case 15: /* union: u16 arm id, then the arm length-prefixed (id 0 = empty, no body) */
        {
            uint32_t n;
            if ( !TableReaderHas( r, 2 ) ) { return 0; }
            if ( TableReaderGet16( r ) == 0 ) { return 1; }
            if ( !TableReaderHas( r, 4 ) ) { return 0; }
            n = TableReaderGet32( r );
            if ( !TableReaderHas( r, n ) ) { return 0; }
            r->offset += n;
            return 1;
        }
        default: break;
    }
    return 0;
}

/* The storage index a key names, with the None refusal that stands in EVERY
   build. The storage shifts left and holds no slot for None, so a build that
   skipped this compare would index one element BEFORE the array — undefined
   behaviour in the configuration a game ships. */
static SCHEMA_UNUSED int32_t TableKeyedSlot( int32_t key )
{
    if ( key == 0 )
    {
        assert( 0 && "None is the null key of an enum-keyed array: it keys no slot" );
        abort();
    }
    return key - 1;
}

/* keyed[key] — the slot a variant owns, as an LVALUE. The key is evaluated
   once. ITERATION is the surface a consumer of the whole array wants: walk
   1..E_MAX and index with the key, so a call site writes no shift. */
#define TableKeyedAt( array, key ) ( (array)[ TableKeyedSlot( (int32_t) ( key ) ) ] )


static SCHEMA_UNUSED float table_bits_to_float( uint32_t bits ) { float f; memcpy( &f, &bits, 4 ); return f; }
static SCHEMA_UNUSED uint32_t table_float_to_bits( float f ) { uint32_t b; memcpy( &b, &f, 4 ); return b; }
static SCHEMA_UNUSED double table_bits_to_double( uint64_t bits ) { double d; memcpy( &d, &bits, 8 ); return d; }
static SCHEMA_UNUSED uint64_t table_double_to_bits( double d ) { uint64_t b; memcpy( &b, &d, 8 ); return b; }

#endif /* SCHEMA_TABLEDEMO_TABLE_PRIMITIVES */

#ifndef SCHEMA_TABLEDEMO_BUILD_VERSION
#define SCHEMA_TABLEDEMO_BUILD_VERSION

/* THE BUILD VERSION (docs/SPEC-TABLES.md §20): one digest over every fact the bytes
   this build produces depend on — the type wire's protocol id, every record's
   layout as the compiler's own C ABI model computes it, and the facts that
   decide what a load PUTS in those slots. It is the number a cook's header
   carries and the number Open compares, and the number a block's prologue
   carries and BlockOpen compares: a build version answers "which build?" and
   not "which form?", and what separates the two forms is their MAGIC.

   There are TWO ids in the design and they are not interchangeable: the
   PROTOCOL ID is the type wire's and nothing else, and the BUILD VERSION is
   what everything cooked or blocked is keyed by. A table edit moves this and
   never the protocol id; a type edit moves both. */
#define BuildVersion 0x734b26e1a4d11cadull

#endif /* SCHEMA_TABLEDEMO_BUILD_VERSION */

#ifndef SCHEMA_TABLEDEMO_TABLE_COOK
#define SCHEMA_TABLEDEMO_TABLE_COOK

/* ---- the cooked form (docs/SPEC-TABLES.md §7) ----

   A cooked file is a HEADER, a DATA part and an ATTRIBUTION part, in that
   order. Every word of the header is a u64 written in the byte order the cook
   was produced in, and the header is 64 bytes:

       0  magic               0x4b4f4f434d484353, read BYTEWISE before anything else
       8  build_version       the unit's id (docs/SPEC-TABLES.md §20)
      16  byte_order          1 little, 2 big — the order that WROTE the file
      24  data_length         the region's bytes, rounded up to alignment
      32  attribution_length  the directory's bytes, or 0
      40  alignment           the region's alignment, never below eight
      48  reserved            zero
      56  reserved            zero

   The DATA part is Lock's region written verbatim (§7.2) — the root at its
   base — and it is what a runtime points at. The ATTRIBUTION part is the node
   directory (§6.3), and NOTHING THAT READS THE STRUCTURE TOUCHES IT: it is
   written beside the data for schema cook-check, so a build that ships no
   tooling need not carry it at all. */
#define kTableCookHeaderBytes 64

/* THE MAGIC'S VALUE, and a consumer written from the page needs the constant
   rather than a description of one. It is "SCHMCOOK" read as ASCII in the byte
   order a little-endian store produces — the same shape the block form's
   SCHMABLK takes, so a hex dump of a little-endian cook is legible and the two
   accelerators sit in one vocabulary.

   IT IS STORED IN THE PRODUCER'S ORDER, which is what makes it the byte-order
   check as well as the form check: a consumer reads back this build's
   constant, or that constant byte-reversed — which identifies a cook of the
   OTHER order — or something that is not a cook. All three answers but the
   first refuse, and a cook and a BLOCK are separated here too, because a
   form's identity belongs in its magic rather than in a second digest. */
#define TableCookMagic 0x4b4f4f434d484353ull

/* THIS BUILD's byte order, as the header's own word carries it. The magic is
   what REFUSES a foreign order; this word is what RECORDS which order wrote
   the file, so a refusal names the order rather than inferring it and a tool
   dumping a cook reads the fact. A file whose magic matched and whose order
   word did not is corrupt, and there is no reading that recovers it.

   The BUILD VERSION cannot do either job: §20.1 digests byteorder as a
   GENERATION input, little for every target schema generates for today, so
   two builds of one schema for two orders emit the same id. */
#if defined( __BYTE_ORDER__ ) && defined( __ORDER_BIG_ENDIAN__ ) && __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
#define TableCookByteOrder 2ull /* big */
#else
#define TableCookByteOrder 1ull /* little */
#endif

/* The greatest region alignment a cooked file may name. The DATA part begins
   at align_up( 64, alignment ), which is 64 for every unit this language can
   declare — the largest alignment it has is sixteen — so a word past this cap
   describes a file no build of this schema wrote (docs/SPEC-TABLES.md §7.1). */
#define TableCookMaxAlign 64ull

/* The header read, BYTEWISE. memcpy is the portable spelling of "these eight
   bytes, in this machine's order"; every compiler this repo builds under folds
   it to one load, and it is the only read in the whole of Open that is not a
   comparison. */
static SCHEMA_UNUSED uint64_t table_cook_read64( const uint8_t * p )
{
    uint64_t v;
    memcpy( &v, p, sizeof( v ) );
    return v;
}

/* TableCookOpen: THE WHOLE CHECK, in one place, because §7 states the
   enumeration once and every generated <Name>Open is that one enumeration plus
   its own root's two layout facts.

   THE CHECK, in order: the magic read bytewise, the byte order it establishes,
   the build version against this build's own, both RESERVED words zero, the
   region alignment the header names, the two part lengths against the length
   the caller passed — a truncated file and a file with trailing bytes are the
   same refusal — the root's own storage inside the data part, and the
   alignment of the base.

   AND THAT IS ALL OF IT. On a match the bytes ARE what this build wrote, in
   this build's layout and this build's byte order, so there is nothing to
   validate and nothing to fix up: the caller gets the root. Nothing per node
   happens here, which is what makes open O(1) in the file's size.

   EVERY NUMBER BELOW COMES OUT OF THE FILE, so the arithmetic is unsigned and
   each term is BOUNDED BEFORE IT IS ADDED: a forged length near 2^64 must
   refuse, and an addition that wrapped would be the defect the comparison
   after it was supposed to catch. Nothing past length is read on any path,
   including every refusing one. */
static SCHEMA_UNUSED const uint8_t * TableCookOpen( const void * bytes, uint64_t length, uint64_t root_size, uint64_t root_align )
{
    const uint8_t * raw;
    uint64_t data_length, attribution_length, alignment, data_offset;
    const uint8_t * base;
    if ( bytes == NULL ) { return NULL; }
    if ( length < (uint64_t) kTableCookHeaderBytes ) { return NULL; }
    raw = (const uint8_t *) bytes;
    /* the MAGIC, bytewise and first: it is what establishes the byte order
       every other header word is read in, so nothing else may be read before
       it. A byte-reversed constant is a cook of the other order and refuses
       here, which is why the order never reaches a fix-up pass. */
    if ( table_cook_read64( raw ) != TableCookMagic ) { return NULL; }
    if ( table_cook_read64( raw + 16 ) != TableCookByteOrder ) { return NULL; }
    if ( table_cook_read64( raw + 8 ) != BuildVersion ) { return NULL; }
    /* the RESERVED words: a non-zero one means a writer used a form this build
       does not understand, and Open refuses rather than ignoring it. */
    if ( table_cook_read64( raw + 48 ) != 0 ) { return NULL; }
    if ( table_cook_read64( raw + 56 ) != 0 ) { return NULL; }
    data_length = table_cook_read64( raw + 24 );
    attribution_length = table_cook_read64( raw + 32 );
    alignment = table_cook_read64( raw + 40 );
    /* THE ALIGNMENT WORD IS DATA, and it is the one header field the rest of
       the check does arithmetic WITH rather than only comparison against. A
       region's alignment is a power of two, never below eight (the floor that
       puts the attribution part on an eight-byte boundary without a second
       padding rule) and never past the cap above; a word that is none of those
       rounds nothing and aligns nothing, so it is refused before it is used. */
    if ( alignment < 8 || alignment > TableCookMaxAlign ) { return NULL; }
    if ( ( alignment & ( alignment - 1 ) ) != 0 ) { return NULL; }
    /* and it must be an alignment THE ROOT CAN SIT AT, since the root is at
       the region's base: both are powers of two, so "at least the root's"
       is one division. */
    if ( ( alignment % root_align ) != 0 ) { return NULL; }
    /* The DATA part begins at align_up( 64, alignment ). It is DERIVED and not
       a header field, because a fact a reader computes is a fact two writers
       cannot disagree about. */
    data_offset = ( (uint64_t) kTableCookHeaderBytes + alignment - 1 ) & ~( alignment - 1 );
    if ( length < data_offset ) { return NULL; }
    /* the two part lengths against the length the caller passed. The whole
       file is data_offset + data_length + attribution_length, and a length
       that is not EXACTLY that refuses — truncation and trailing bytes are one
       refusal, and both terms are subtracted rather than added so no sum can
       carry. */
    if ( data_length > length - data_offset ) { return NULL; }
    if ( attribution_length != length - data_offset - data_length ) { return NULL; }
    /* the ROOT sits at the region's base, so the region has to hold it: a
       shorter data part describes a root partly outside the file, which is the
       one way a match-and-point reader could hand back storage it never
       received. */
    if ( data_length < root_size ) { return NULL; }
    base = raw + data_offset;
    /* the alignment of the BASE. The header pads the data part to the region's
       alignment, so a base an allocator or mmap gave you is already aligned —
       mmap gives page alignment for free — and a base that is not is a caller's
       buffer this form cannot be read out of. */
    if ( ( (uintptr_t) base % (uintptr_t) alignment ) != 0 ) { return NULL; }
    return base;
}

#endif /* SCHEMA_TABLEDEMO_TABLE_COOK */

/* table GunnerSettings — TABLE-wire storage: relocatable, bounded. C has no member
   initializers, so the declared defaults live in GunnerSettingsReset and nowhere
   else — one definition of what a default is (docs/SPEC-TABLES.md) */
typedef struct GunnerSettings {
    float reaction;
    uint8_t tracking;
    char callsign[24 + 1]; /* string(24): N + 1 for the terminator the wire does not carry */
    int32_t callsign_length;
} GunnerSettings;

/* table ShipEntry — TABLE-wire storage: relocatable, bounded. C has no member
   initializers, so the declared defaults live in ShipEntryReset and nowhere
   else — one definition of what a default is (docs/SPEC-TABLES.md) */
typedef struct ShipEntry {
    char display_name[32 + 1]; /* string(32): N + 1 for the terminator the wire does not carry */
    int32_t display_name_length;
    float health;
    float mass;
    int32_t hardpoints[4]; /* used count beside it; count in [0, 4] */
    int32_t hardpoints_count;
    GunnerSettings gunner;
    uint8_t gunner_present; /* ?GunnerSettings: absent until set */
} ShipEntry;

/* table GlobalSettings — TABLE-wire storage: relocatable, bounded. C has no member
   initializers, so the declared defaults live in GlobalSettingsReset and nowhere
   else — one definition of what a default is (docs/SPEC-TABLES.md) */
typedef struct GlobalSettings {
    uint32_t tick_rate;
    Difficulty difficulty;
    char build_note[48 + 1]; /* string(48): N + 1 for the terminator the wire does not carry */
    int32_t build_note_length;
    float spawn_delays[3];
} GlobalSettings;

/* table PackConfig — TABLE-wire storage: relocatable, bounded. C has no member
   initializers, so the declared defaults live in PackConfigReset and nowhere
   else — one definition of what a default is (docs/SPEC-TABLES.md) */
typedef struct PackConfig {
    uint32_t version;
    GlobalSettings global;
    ShipEntry ships[SHIP_TYPE_MAX]; /* [ShipType]: one slot per named variant, the key k at index k-1 */
    int32_t thresholds[DIFFICULTY_MAX]; /* [Difficulty]: one slot per named variant, the key k at index k-1 */
    ShipEntry reserves[3]; /* used count beside it; count in [0, 3] */
    int32_t reserves_count;
} PackConfig;

/* ---- prefill: the declared defaults, in place (docs/SPEC-TABLES.md) ---- */

static SCHEMA_UNUSED void GunnerSettingsReset( GunnerSettings * value );
static SCHEMA_UNUSED void schema_tabledemo_gunner_settings_reset_raw_( void * storage );
static SCHEMA_UNUSED void ShipEntryReset( ShipEntry * value );
static SCHEMA_UNUSED void schema_tabledemo_ship_entry_reset_raw_( void * storage );
static SCHEMA_UNUSED void GlobalSettingsReset( GlobalSettings * value );
static SCHEMA_UNUSED void schema_tabledemo_global_settings_reset_raw_( void * storage );
static SCHEMA_UNUSED void PackConfigReset( PackConfig * value );
static SCHEMA_UNUSED void schema_tabledemo_pack_config_reset_raw_( void * storage );

static SCHEMA_UNUSED void GunnerSettingsReset( GunnerSettings * value )
{
    value->reaction = 0.2f;
    value->tracking = 0;
    memset( value->callsign, 0, sizeof( value->callsign ) );
    value->callsign_length = 0;
}

static SCHEMA_UNUSED void schema_tabledemo_gunner_settings_reset_raw_( void * storage ) { GunnerSettingsReset( (GunnerSettings *) storage ); }

static SCHEMA_UNUSED void ShipEntryReset( ShipEntry * value )
{
    memset( value->display_name, 0, sizeof( value->display_name ) );
    value->display_name_length = 0;
    value->health = 100.0f;
    value->mass = 1.0f;
    memset( value->hardpoints, 0, sizeof( value->hardpoints ) );
    value->hardpoints_count = 0;
    GunnerSettingsReset( &value->gunner );
    value->gunner_present = 0;
}

static SCHEMA_UNUSED void schema_tabledemo_ship_entry_reset_raw_( void * storage ) { ShipEntryReset( (ShipEntry *) storage ); }

static SCHEMA_UNUSED void GlobalSettingsReset( GlobalSettings * value )
{
    value->tick_rate = 60;
    value->difficulty = DIFFICULTY_NORMAL;
    memset( value->build_note, 0, sizeof( value->build_note ) );
    value->build_note_length = 0;
    memset( value->spawn_delays, 0, sizeof( value->spawn_delays ) );
}

static SCHEMA_UNUSED void schema_tabledemo_global_settings_reset_raw_( void * storage ) { GlobalSettingsReset( (GlobalSettings *) storage ); }

static SCHEMA_UNUSED void PackConfigReset( PackConfig * value )
{
    value->version = 1;
    GlobalSettingsReset( &value->global );
    ShipEntryReset( &value->ships[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( SHIP_TYPE_MAX ); i++ ) { value->ships[i] = value->ships[0]; } }
    memset( value->thresholds, 0, sizeof( value->thresholds ) );
    ShipEntryReset( &value->reserves[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 3 ); i++ ) { value->reserves[i] = value->reserves[0]; } }
    value->reserves_count = 0;
}

static SCHEMA_UNUSED void schema_tabledemo_pack_config_reset_raw_( void * storage ) { PackConfigReset( (PackConfig *) storage ); }

/* ---- codecs: measure/save/load per closure member ---- */

static SCHEMA_UNUSED int64_t GunnerSettingsMeasure( const GunnerSettings * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GunnerSettingsSaveBody( TableWriter * w, const GunnerSettings * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GunnerSettingsLoadBody( TableReader * r, GunnerSettings * value );
static SCHEMA_UNUSED int64_t ShipEntryMeasure( const ShipEntry * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int ShipEntrySaveBody( TableWriter * w, const ShipEntry * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int ShipEntryLoadBody( TableReader * r, ShipEntry * value );
static SCHEMA_UNUSED int64_t GlobalSettingsMeasure( const GlobalSettings * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GlobalSettingsSaveBody( TableWriter * w, const GlobalSettings * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GlobalSettingsLoadBody( TableReader * r, GlobalSettings * value );
static SCHEMA_UNUSED int64_t PackConfigMeasure( const PackConfig * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int PackConfigSaveBody( TableWriter * w, const PackConfig * value );
static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int PackConfigLoadBody( TableReader * r, PackConfig * value );

static SCHEMA_UNUSED int64_t GunnerSettingsMeasure( const GunnerSettings * value )
{
    int64_t bytes = 2; /* terminator */
    if ( value->reaction != 0.2f ) { bytes += 3 + 4; } /* reaction */
    if ( value->tracking != 0 ) { bytes += 3 + 1; } /* tracking */
    if ( value->callsign_length < 0 || value->callsign_length > 24 ) { return -1; } /* storage invariant */
    if ( value->callsign_length > 0 ) { bytes += 3 + 4 + value->callsign_length; } /* callsign */
    return bytes;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GunnerSettingsSaveBody( TableWriter * w, const GunnerSettings * value )
{
    if ( value->reaction != 0.2f )
    {
        TableWriterPut16( w, 0x900f ); TableWriterPut8( w, 10 ); /* reaction */
        TableWriterPut32( w, table_float_to_bits( value->reaction ) );
    }
    if ( value->tracking != 0 )
    {
        TableWriterPut16( w, 0x53d5 ); TableWriterPut8( w, 1 ); /* tracking */
        TableWriterPut8( w, value->tracking ? 1 : 0 );
    }
    if ( value->callsign_length < 0 || value->callsign_length > 24 ) { return 0; } /* storage invariant */
    if ( value->callsign_length > 0 )
    {
        TableWriterPut16( w, 0x74dc ); TableWriterPut8( w, 12 ); /* callsign */
        TableWriterPut32( w, (uint32_t) value->callsign_length );
        TableWriterRaw( w, value->callsign, value->callsign_length );
    }
    TableWriterPut16( w, 0 ); /* terminator */
    return !w->overflow;
}

static SCHEMA_UNUSED int64_t GunnerSettingsSave( const GunnerSettings * value, uint8_t * buffer, int64_t capacity )
{
    TableWriter w = TableWriterMake( buffer, capacity );
    if ( !GunnerSettingsSaveBody( &w, value ) ) { return -1; }
    return w.offset; /* == GunnerSettingsMeasure( value ) */
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GunnerSettingsLoadBody( TableReader * r, GunnerSettings * value )
{
    GunnerSettingsReset( value ); /* prefill declared defaults in place, then overlay */
    for ( ;; )
    {
        uint16_t field_id;
        uint8_t kind;
        if ( !TableReaderHas( r, 2 ) ) { r->report->malformed = 1; return 0; }
        field_id = TableReaderGet16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !TableReaderHas( r, 1 ) ) { r->report->malformed = 1; return 0; }
        kind = TableReaderGet8( r );
        switch ( field_id )
        {
            case 0x900f: /* reaction */
            {
                if ( kind != 10 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 4 ) ) { r->report->malformed = 1; return 0; }
                value->reaction = table_bits_to_float( TableReaderGet32( &(*r) ) );
                break;
            }
            case 0x53d5: /* tracking */
            {
                if ( kind != 1 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 1 ) ) { r->report->malformed = 1; return 0; }
                value->tracking = TableReaderGet8( &(*r) ) != 0;
                break;
            }
            case 0x74dc: /* callsign */
            {
                if ( kind != 12 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t len;
                uint32_t keep;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                len = TableReaderGet32( r );
                if ( !TableReaderHas( r, len ) ) { r->report->malformed = 1; return 0; }
                keep = len;
                if ( keep > 24 ) { keep = 24; r->report->clamped++; }
                memcpy( value->callsign, r->buffer + r->offset, keep );
                value->callsign[keep] = 0;
                value->callsign_length = (int32_t) keep;
                r->offset += len;
                break;
            }
            default:
            {
                r->report->unknown++;
                if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

static SCHEMA_UNUSED int GunnerSettingsLoad( GunnerSettings * value, const uint8_t * buffer, int64_t bytes, TableReport * report )
{
    TableReport ignored;
    TableReader r;
    memset( &ignored, 0, sizeof( ignored ) );
    r = TableReaderMake( buffer, bytes, report != NULL ? report : &ignored );
    return GunnerSettingsLoadBody( &r, value );
}

static SCHEMA_UNUSED int64_t ShipEntryMeasure( const ShipEntry * value )
{
    int64_t bytes = 2; /* terminator */
    if ( value->display_name_length < 0 || value->display_name_length > 32 ) { return -1; } /* storage invariant */
    if ( value->display_name_length > 0 ) { bytes += 3 + 4 + value->display_name_length; } /* display_name */
    if ( value->health != 100.0f ) { bytes += 3 + 4; } /* health */
    if ( value->mass != 1.0f ) { bytes += 3 + 4; } /* mass */
    if ( value->hardpoints_count < 0 || value->hardpoints_count > 4 ) { return -1; } /* storage invariant */
    if ( value->hardpoints_count > 0 )
    {
        bytes += 3 + 4 + 5 + (int64_t) value->hardpoints_count * 4; /* hardpoints */
    }
    if ( value->gunner_present ) /* ?GunnerSettings: presence decides, not content */
    {
        int64_t body_gunner = GunnerSettingsMeasure( &value->gunner );
        if ( body_gunner < 0 ) { return -1; }
        bytes += 3 + 4 + body_gunner; /* gunner */
    }
    return bytes;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int ShipEntrySaveBody( TableWriter * w, const ShipEntry * value )
{
    if ( value->display_name_length < 0 || value->display_name_length > 32 ) { return 0; } /* storage invariant */
    if ( value->display_name_length > 0 )
    {
        TableWriterPut16( w, 0xcb8b ); TableWriterPut8( w, 12 ); /* display_name */
        TableWriterPut32( w, (uint32_t) value->display_name_length );
        TableWriterRaw( w, value->display_name, value->display_name_length );
    }
    if ( value->health != 100.0f )
    {
        TableWriterPut16( w, 0x8617 ); TableWriterPut8( w, 10 ); /* health */
        TableWriterPut32( w, table_float_to_bits( value->health ) );
    }
    if ( value->mass != 1.0f )
    {
        TableWriterPut16( w, 0xe7a6 ); TableWriterPut8( w, 10 ); /* mass */
        TableWriterPut32( w, table_float_to_bits( value->mass ) );
    }
    if ( value->hardpoints_count < 0 || value->hardpoints_count > 4 ) { return 0; } /* storage invariant */
    if ( value->hardpoints_count > 0 )
    {
        int64_t len_at_hardpoints;
        int32_t i;
        TableWriterPut16( w, 0xc4b2 ); TableWriterPut8( w, 14 ); /* hardpoints */
        len_at_hardpoints = w->offset; TableWriterPut32( w, 0 );
        TableWriterPut8( w, 4 ); TableWriterPut32( w, (uint32_t) value->hardpoints_count );
        for ( i = 0; i < value->hardpoints_count; i++ )
        {
            TableWriterPut32( w, (uint32_t) ( value->hardpoints[i] ) );
        }
        TableWriterPatch32( w, len_at_hardpoints, (uint32_t) ( w->offset - len_at_hardpoints - 4 ) );
    }
    if ( value->gunner_present ) /* ?GunnerSettings */
    {
        int64_t body_gunner = GunnerSettingsMeasure( &value->gunner );
        if ( body_gunner < 0 ) { return 0; } /* storage invariant, refused as measure refuses it */
        TableWriterPut16( w, 0x2bc9 ); TableWriterPut8( w, 13 ); /* gunner */
        TableWriterPut32( w, (uint32_t) body_gunner );
        if ( !GunnerSettingsSaveBody( w, &value->gunner ) ) { return 0; }
    }
    TableWriterPut16( w, 0 ); /* terminator */
    return !w->overflow;
}

static SCHEMA_UNUSED int64_t ShipEntrySave( const ShipEntry * value, uint8_t * buffer, int64_t capacity )
{
    TableWriter w = TableWriterMake( buffer, capacity );
    if ( !ShipEntrySaveBody( &w, value ) ) { return -1; }
    return w.offset; /* == ShipEntryMeasure( value ) */
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int ShipEntryLoadBody( TableReader * r, ShipEntry * value )
{
    ShipEntryReset( value ); /* prefill declared defaults in place, then overlay */
    for ( ;; )
    {
        uint16_t field_id;
        uint8_t kind;
        if ( !TableReaderHas( r, 2 ) ) { r->report->malformed = 1; return 0; }
        field_id = TableReaderGet16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !TableReaderHas( r, 1 ) ) { r->report->malformed = 1; return 0; }
        kind = TableReaderGet8( r );
        switch ( field_id )
        {
            case 0xcb8b: /* display_name */
            {
                if ( kind != 12 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t len;
                uint32_t keep;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                len = TableReaderGet32( r );
                if ( !TableReaderHas( r, len ) ) { r->report->malformed = 1; return 0; }
                keep = len;
                if ( keep > 32 ) { keep = 32; r->report->clamped++; }
                memcpy( value->display_name, r->buffer + r->offset, keep );
                value->display_name[keep] = 0;
                value->display_name_length = (int32_t) keep;
                r->offset += len;
                break;
            }
            case 0x8617: /* health */
            {
                if ( kind != 10 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 4 ) ) { r->report->malformed = 1; return 0; }
                value->health = table_bits_to_float( TableReaderGet32( &(*r) ) );
                break;
            }
            case 0xe7a6: /* mass */
            {
                if ( kind != 10 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 4 ) ) { r->report->malformed = 1; return 0; }
                value->mass = table_bits_to_float( TableReaderGet32( &(*r) ) );
                break;
            }
            case 0xc4b2: /* hardpoints */
            {
                if ( kind != 14 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                int64_t body_end;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                body_end = r->offset + body_len;
                if ( body_len >= 5 )
                {
                    uint8_t elem_kind = TableReaderGet8( r );
                    uint32_t count = TableReaderGet32( r );
                    uint32_t keep;
                    TableReader sub;
                    uint32_t i;
                    uint32_t decoded = 0;
                    if ( elem_kind != 4 ) { r->report->kind_mismatch++; r->offset = body_end; break; }
                    keep = count;
                    if ( keep > 4 ) { keep = 4; r->report->clamped++; }
                    /* elements are BOUNDED by the field body: a count the length
                       cannot cover keeps the decoded prefix, flags malformed, and
                       the parent continues at the next field — following fields'
                       bytes are never fabricated into elements */
                    sub = TableReaderMake( r->buffer + r->offset, body_end - r->offset, r->report );
                    for ( i = 0; i < keep; i++ )
                    {
                        if ( !TableReaderHas( &sub, 4 ) ) { r->report->malformed = 1; break; }
                        {
                            int32_t decoded_v = (int32_t) TableReaderGet32( &sub );
                            if ( decoded_v < 0 ) { decoded_v = 0; r->report->clamped++; }
                            else if ( decoded_v > 8 ) { decoded_v = 8; r->report->clamped++; }
                            value->hardpoints[i] = decoded_v;
                        }
                        decoded = i + 1;
                    }
                    value->hardpoints_count = (int32_t) decoded;
                }
                r->offset = body_end; /* excess elements and slack skip via the length */
                break;
            }
            case 0x2bc9: /* gunner */
            {
                if ( kind != 13 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                {
                    TableReader sub = TableReaderMake( r->buffer + r->offset, body_len, r->report );
                    GunnerSettingsLoadBody( &sub, &value->gunner );
                }
                r->offset += body_len;
                value->gunner_present = 1;
                break;
            }
            default:
            {
                r->report->unknown++;
                if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

static SCHEMA_UNUSED int ShipEntryLoad( ShipEntry * value, const uint8_t * buffer, int64_t bytes, TableReport * report )
{
    TableReport ignored;
    TableReader r;
    memset( &ignored, 0, sizeof( ignored ) );
    r = TableReaderMake( buffer, bytes, report != NULL ? report : &ignored );
    return ShipEntryLoadBody( &r, value );
}

static SCHEMA_UNUSED int64_t GlobalSettingsMeasure( const GlobalSettings * value )
{
    int64_t bytes = 2; /* terminator */
    if ( value->tick_rate != 60 ) { bytes += 3 + 4; } /* tick_rate */
    if ( value->difficulty != DIFFICULTY_NORMAL )
    {
        uint16_t id_difficulty = 0;
        switch ( value->difficulty ) /* Difficulty rides as the u16 hash of its VARIANT NAME (§5) */
        {
            case DIFFICULTY_NONE: id_difficulty = 0; break;
            case DIFFICULTY_EASY: id_difficulty = 0x7d08; break;
            case DIFFICULTY_NORMAL: id_difficulty = 0x7fac; break;
            case DIFFICULTY_HARD: id_difficulty = 0xc313; break;
            default: return -1; /* no variant names this value: no wire identity */
        }
        (void) id_difficulty;
        bytes += 3 + 2; /* difficulty: the variant's name hash */
    }
    if ( value->build_note_length < 0 || value->build_note_length > 48 ) { return -1; } /* storage invariant */
    if ( value->build_note_length > 0 ) { bytes += 3 + 4 + value->build_note_length; } /* build_note */
    {
        int all_default_spawn_delays = 1;
        int32_t i;
        for ( i = 0; i < 3; i++ ) { if ( value->spawn_delays[i] != 0.0f ) { all_default_spawn_delays = 0; break; } }
        if ( !all_default_spawn_delays )
        {
            bytes += 3 + 4 + 5 + 12; /* spawn_delays */
        }
    }
    return bytes;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GlobalSettingsSaveBody( TableWriter * w, const GlobalSettings * value )
{
    if ( value->tick_rate != 60 )
    {
        TableWriterPut16( w, 0x1953 ); TableWriterPut8( w, 8 ); /* tick_rate */
        TableWriterPut32( w, (uint32_t) ( value->tick_rate ) );
    }
    if ( value->difficulty != DIFFICULTY_NORMAL )
    {
        uint16_t id_difficulty = 0;
        switch ( value->difficulty ) /* Difficulty rides as the u16 hash of its VARIANT NAME (§5) */
        {
            case DIFFICULTY_NONE: id_difficulty = 0; break;
            case DIFFICULTY_EASY: id_difficulty = 0x7d08; break;
            case DIFFICULTY_NORMAL: id_difficulty = 0x7fac; break;
            case DIFFICULTY_HARD: id_difficulty = 0xc313; break;
            default: return 0; /* no variant names this value: no wire identity */
        }
        TableWriterPut16( w, 0xd53f ); TableWriterPut8( w, 7 ); /* difficulty */
        TableWriterPut16( w, id_difficulty );
    }
    if ( value->build_note_length < 0 || value->build_note_length > 48 ) { return 0; } /* storage invariant */
    if ( value->build_note_length > 0 )
    {
        TableWriterPut16( w, 0xb2d4 ); TableWriterPut8( w, 12 ); /* build_note */
        TableWriterPut32( w, (uint32_t) value->build_note_length );
        TableWriterRaw( w, value->build_note, value->build_note_length );
    }
    {
        int all_default_spawn_delays = 1;
        int32_t i;
        for ( i = 0; i < 3; i++ ) { if ( value->spawn_delays[i] != 0.0f ) { all_default_spawn_delays = 0; break; } }
        if ( !all_default_spawn_delays )
        {
            int64_t len_at_spawn_delays;
            TableWriterPut16( w, 0x7c01 ); TableWriterPut8( w, 14 ); /* spawn_delays (fixed [3]) */
            len_at_spawn_delays = w->offset; TableWriterPut32( w, 0 );
            TableWriterPut8( w, 10 ); TableWriterPut32( w, 3 );
            for ( i = 0; i < 3; i++ )
            {
                TableWriterPut32( w, table_float_to_bits( value->spawn_delays[i] ) );
            }
            TableWriterPatch32( w, len_at_spawn_delays, (uint32_t) ( w->offset - len_at_spawn_delays - 4 ) );
        }
    }
    TableWriterPut16( w, 0 ); /* terminator */
    return !w->overflow;
}

static SCHEMA_UNUSED int64_t GlobalSettingsSave( const GlobalSettings * value, uint8_t * buffer, int64_t capacity )
{
    TableWriter w = TableWriterMake( buffer, capacity );
    if ( !GlobalSettingsSaveBody( &w, value ) ) { return -1; }
    return w.offset; /* == GlobalSettingsMeasure( value ) */
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int GlobalSettingsLoadBody( TableReader * r, GlobalSettings * value )
{
    GlobalSettingsReset( value ); /* prefill declared defaults in place, then overlay */
    for ( ;; )
    {
        uint16_t field_id;
        uint8_t kind;
        if ( !TableReaderHas( r, 2 ) ) { r->report->malformed = 1; return 0; }
        field_id = TableReaderGet16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !TableReaderHas( r, 1 ) ) { r->report->malformed = 1; return 0; }
        kind = TableReaderGet8( r );
        switch ( field_id )
        {
            case 0x1953: /* tick_rate */
            {
                if ( kind != 8 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 4 ) ) { r->report->malformed = 1; return 0; }
                {
                    uint32_t decoded_v = (uint32_t) TableReaderGet32( &(*r) );
                    if ( decoded_v < 1 ) { decoded_v = 1; r->report->clamped++; }
                    else if ( decoded_v > 240 ) { decoded_v = 240; r->report->clamped++; }
                    value->tick_rate = decoded_v;
                }
                break;
            }
            case 0xd53f: /* difficulty */
            {
                if ( kind != 7 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 2 ) ) { r->report->malformed = 1; return 0; }
                {
                    uint16_t variant = TableReaderGet16( &(*r) );
                    switch ( variant )
                    {
                        case 0: value->difficulty = DIFFICULTY_NONE; break;
                        case 0x7d08: value->difficulty = DIFFICULTY_EASY; break;
                        case 0x7fac: value->difficulty = DIFFICULTY_NORMAL; break;
                        case 0xc313: value->difficulty = DIFFICULTY_HARD; break;
                        default: value->difficulty = DIFFICULTY_NONE; r->report->unknown++; break; /* an id this build cannot name */
                    }
                }
                break;
            }
            case 0xb2d4: /* build_note */
            {
                if ( kind != 12 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t len;
                uint32_t keep;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                len = TableReaderGet32( r );
                if ( !TableReaderHas( r, len ) ) { r->report->malformed = 1; return 0; }
                keep = len;
                if ( keep > 48 ) { keep = 48; r->report->clamped++; }
                memcpy( value->build_note, r->buffer + r->offset, keep );
                value->build_note[keep] = 0;
                value->build_note_length = (int32_t) keep;
                r->offset += len;
                break;
            }
            case 0x7c01: /* spawn_delays */
            {
                if ( kind != 14 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                int64_t body_end;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                body_end = r->offset + body_len;
                if ( body_len >= 5 )
                {
                    uint8_t elem_kind = TableReaderGet8( r );
                    uint32_t count = TableReaderGet32( r );
                    uint32_t keep;
                    TableReader sub;
                    uint32_t i;
                    if ( elem_kind != 10 ) { r->report->kind_mismatch++; r->offset = body_end; break; }
                    keep = count;
                    if ( keep > 3 ) { keep = 3; r->report->clamped++; }
                    /* elements are BOUNDED by the field body: a count the length
                       cannot cover keeps the decoded prefix, flags malformed, and
                       the parent continues at the next field — following fields'
                       bytes are never fabricated into elements */
                    sub = TableReaderMake( r->buffer + r->offset, body_end - r->offset, r->report );
                    for ( i = 0; i < keep; i++ )
                    {
                        if ( !TableReaderHas( &sub, 4 ) ) { r->report->malformed = 1; break; }
                        value->spawn_delays[i] = table_bits_to_float( TableReaderGet32( &sub ) );
                    }
                }
                r->offset = body_end; /* excess elements and slack skip via the length */
                break;
            }
            default:
            {
                r->report->unknown++;
                if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

static SCHEMA_UNUSED int GlobalSettingsLoad( GlobalSettings * value, const uint8_t * buffer, int64_t bytes, TableReport * report )
{
    TableReport ignored;
    TableReader r;
    memset( &ignored, 0, sizeof( ignored ) );
    r = TableReaderMake( buffer, bytes, report != NULL ? report : &ignored );
    return GlobalSettingsLoadBody( &r, value );
}

static SCHEMA_UNUSED int64_t PackConfigMeasure( const PackConfig * value )
{
    int64_t bytes = 2; /* terminator */
    if ( value->version != 1 ) { bytes += 3 + 4; } /* version */
    {
        int64_t body_global = GlobalSettingsMeasure( &value->global );
        if ( body_global < 0 ) { return -1; }
        if ( body_global > 2 ) { bytes += 3 + 4 + body_global; } /* global: all-default nested elides */
    }
    {
        int64_t pairs_ships = 0, body_ships = 0;
        int32_t i;
        for ( i = 0; i < SHIP_TYPE_MAX; i++ ) /* [ShipType]: every stored slot is a named variant's */
        {
            int64_t elem_bytes = ShipEntryMeasure( &value->ships[i] );
            if ( elem_bytes < 0 ) { return -1; }
            if ( elem_bytes <= 2 ) { continue; } /* an all-default slot elides */
            uint16_t key_id = 0;
            switch ( (ShipType) ( i + 1 ) ) /* ShipType rides as the u16 hash of its VARIANT NAME (§5) */
            {
                case SHIP_TYPE_NONE: key_id = 0; break;
                case SHIP_TYPE_FIGHTER: key_id = 0x412b; break;
                case SHIP_TYPE_BOMBER: key_id = 0xb7ce; break;
                case SHIP_TYPE_SCOUT: key_id = 0xd97a; break;
                default: return -1; /* no variant names this value: no wire identity */
            }
            (void) key_id;
            pairs_ships++; body_ships += 2 + 4 + elem_bytes; /* key, length, body */
        }
        if ( pairs_ships > 0 ) { bytes += 3 + 4 + 5 + body_ships; } /* ships */
    }
    {
        int64_t pairs_thresholds = 0, body_thresholds = 0;
        int32_t i;
        for ( i = 0; i < DIFFICULTY_MAX; i++ ) /* [Difficulty]: every stored slot is a named variant's */
        {
            if ( value->thresholds[i] == 0 ) { continue; } /* a default slot elides */
            uint16_t key_id = 0;
            switch ( (Difficulty) ( i + 1 ) ) /* Difficulty rides as the u16 hash of its VARIANT NAME (§5) */
            {
                case DIFFICULTY_NONE: key_id = 0; break;
                case DIFFICULTY_EASY: key_id = 0x7d08; break;
                case DIFFICULTY_NORMAL: key_id = 0x7fac; break;
                case DIFFICULTY_HARD: key_id = 0xc313; break;
                default: return -1; /* no variant names this value: no wire identity */
            }
            (void) key_id;
            pairs_thresholds++; body_thresholds += 2 + 4 + 4; /* key, length, element */
        }
        if ( pairs_thresholds > 0 ) { bytes += 3 + 4 + 5 + body_thresholds; } /* thresholds */
    }
    if ( value->reserves_count < 0 || value->reserves_count > 3 ) { return -1; } /* storage invariant */
    if ( value->reserves_count > 0 )
    {
        int32_t i;
        bytes += 3 + 4 + 5; /* reserves */
        for ( i = 0; i < value->reserves_count; i++ )
        {
            int64_t elem_reserves = ShipEntryMeasure( &value->reserves[i] );
            if ( elem_reserves < 0 ) { return -1; }
            bytes += 4 + elem_reserves;
        }
    }
    return bytes;
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int PackConfigSaveBody( TableWriter * w, const PackConfig * value )
{
    if ( value->version != 1 )
    {
        TableWriterPut16( w, 0xe8e6 ); TableWriterPut8( w, 8 ); /* version */
        TableWriterPut32( w, (uint32_t) ( value->version ) );
    }
    {
        int64_t body_global = GlobalSettingsMeasure( &value->global );
        if ( body_global < 0 ) { return 0; } /* storage invariant, refused as measure refuses it */
        if ( body_global > 2 ) /* all-default nested elides */
        {
            TableWriterPut16( w, 0x1b51 ); TableWriterPut8( w, 13 ); /* global */
            TableWriterPut32( w, (uint32_t) body_global );
            if ( !GlobalSettingsSaveBody( w, &value->global ) ) { return 0; }
        }
    }
    {
        uint32_t pairs_ships = 0;
        int32_t i;
        for ( i = 0; i < SHIP_TYPE_MAX; i++ ) /* [ShipType]: every stored slot is a named variant's */
        {
            int64_t elem_bytes = ShipEntryMeasure( &value->ships[i] );
            if ( elem_bytes < 0 ) { return 0; }
            if ( elem_bytes <= 2 ) { continue; } /* an all-default slot elides */
            uint16_t key_id = 0;
            switch ( (ShipType) ( i + 1 ) ) /* ShipType rides as the u16 hash of its VARIANT NAME (§5) */
            {
                case SHIP_TYPE_NONE: key_id = 0; break;
                case SHIP_TYPE_FIGHTER: key_id = 0x412b; break;
                case SHIP_TYPE_BOMBER: key_id = 0xb7ce; break;
                case SHIP_TYPE_SCOUT: key_id = 0xd97a; break;
                default: return 0; /* no variant names this value: no wire identity */
            }
            (void) key_id;
            pairs_ships++;
        }
        if ( pairs_ships > 0 )
        {
            int64_t len_at_ships;
            /* KIND 16, not 14: a keyed body and a positional one are
               incompatible, so a reader of the other kind must see a kind
               mismatch and skip, never misdecode (docs/SPEC-TABLES.md §3.2) */
            TableWriterPut16( w, 0x2d39 ); TableWriterPut8( w, 16 ); /* ships (keyed by ShipType) */
            len_at_ships = w->offset; TableWriterPut32( w, 0 );
            TableWriterPut8( w, 13 ); TableWriterPut32( w, pairs_ships );
            /* ASCENDING BY VARIANT ORDINAL, which is slot order — this
               writer's choice, and a reader must not rely on it: every
               slot is found by its key (docs/SPEC-TABLES.md §3.2) */
            for ( i = 0; i < SHIP_TYPE_MAX; i++ )
            {
                int64_t elem_len_at_ships;
                int64_t elem_bytes = ShipEntryMeasure( &value->ships[i] );
                if ( elem_bytes < 0 ) { return 0; }
                if ( elem_bytes <= 2 ) { continue; } /* an all-default slot elides */
                uint16_t key_id = 0;
                switch ( (ShipType) ( i + 1 ) ) /* ShipType rides as the u16 hash of its VARIANT NAME (§5) */
                {
                    case SHIP_TYPE_NONE: key_id = 0; break;
                    case SHIP_TYPE_FIGHTER: key_id = 0x412b; break;
                    case SHIP_TYPE_BOMBER: key_id = 0xb7ce; break;
                    case SHIP_TYPE_SCOUT: key_id = 0xd97a; break;
                    default: return 0; /* no variant names this value: no wire identity */
                }
                (void) key_id;
                TableWriterPut16( w, key_id ); /* the slot's VARIANT id, not its position */
                elem_len_at_ships = w->offset; TableWriterPut32( w, 0 );
                if ( !ShipEntrySaveBody( w, &value->ships[i] ) ) { return 0; }
                TableWriterPatch32( w, elem_len_at_ships, (uint32_t) ( w->offset - elem_len_at_ships - 4 ) );
            }
            TableWriterPatch32( w, len_at_ships, (uint32_t) ( w->offset - len_at_ships - 4 ) );
        }
    }
    {
        uint32_t pairs_thresholds = 0;
        int32_t i;
        for ( i = 0; i < DIFFICULTY_MAX; i++ ) /* [Difficulty]: every stored slot is a named variant's */
        {
            if ( value->thresholds[i] == 0 ) { continue; } /* a default slot elides */
            uint16_t key_id = 0;
            switch ( (Difficulty) ( i + 1 ) ) /* Difficulty rides as the u16 hash of its VARIANT NAME (§5) */
            {
                case DIFFICULTY_NONE: key_id = 0; break;
                case DIFFICULTY_EASY: key_id = 0x7d08; break;
                case DIFFICULTY_NORMAL: key_id = 0x7fac; break;
                case DIFFICULTY_HARD: key_id = 0xc313; break;
                default: return 0; /* no variant names this value: no wire identity */
            }
            (void) key_id;
            pairs_thresholds++;
        }
        if ( pairs_thresholds > 0 )
        {
            int64_t len_at_thresholds;
            /* KIND 16, not 14: a keyed body and a positional one are
               incompatible, so a reader of the other kind must see a kind
               mismatch and skip, never misdecode (docs/SPEC-TABLES.md §3.2) */
            TableWriterPut16( w, 0xb2eb ); TableWriterPut8( w, 16 ); /* thresholds (keyed by Difficulty) */
            len_at_thresholds = w->offset; TableWriterPut32( w, 0 );
            TableWriterPut8( w, 4 ); TableWriterPut32( w, pairs_thresholds );
            /* ASCENDING BY VARIANT ORDINAL, which is slot order — this
               writer's choice, and a reader must not rely on it: every
               slot is found by its key (docs/SPEC-TABLES.md §3.2) */
            for ( i = 0; i < DIFFICULTY_MAX; i++ )
            {
                int64_t elem_len_at_thresholds;
                if ( value->thresholds[i] == 0 ) { continue; } /* a default slot elides */
                uint16_t key_id = 0;
                switch ( (Difficulty) ( i + 1 ) ) /* Difficulty rides as the u16 hash of its VARIANT NAME (§5) */
                {
                    case DIFFICULTY_NONE: key_id = 0; break;
                    case DIFFICULTY_EASY: key_id = 0x7d08; break;
                    case DIFFICULTY_NORMAL: key_id = 0x7fac; break;
                    case DIFFICULTY_HARD: key_id = 0xc313; break;
                    default: return 0; /* no variant names this value: no wire identity */
                }
                (void) key_id;
                TableWriterPut16( w, key_id ); /* the slot's VARIANT id, not its position */
                elem_len_at_thresholds = w->offset; TableWriterPut32( w, 0 );
                TableWriterPut32( w, (uint32_t) ( value->thresholds[i] ) );
                TableWriterPatch32( w, elem_len_at_thresholds, (uint32_t) ( w->offset - elem_len_at_thresholds - 4 ) );
            }
            TableWriterPatch32( w, len_at_thresholds, (uint32_t) ( w->offset - len_at_thresholds - 4 ) );
        }
    }
    if ( value->reserves_count < 0 || value->reserves_count > 3 ) { return 0; } /* storage invariant */
    if ( value->reserves_count > 0 )
    {
        int64_t len_at_reserves;
        int32_t i;
        TableWriterPut16( w, 0x049d ); TableWriterPut8( w, 14 ); /* reserves */
        len_at_reserves = w->offset; TableWriterPut32( w, 0 );
        TableWriterPut8( w, 13 ); TableWriterPut32( w, (uint32_t) value->reserves_count );
        for ( i = 0; i < value->reserves_count; i++ )
        {
            {
                int64_t elem_len_at = w->offset;
                TableWriterPut32( w, 0 );
                if ( !ShipEntrySaveBody( w, &value->reserves[i] ) ) { return 0; }
                TableWriterPatch32( w, elem_len_at, (uint32_t) ( w->offset - elem_len_at - 4 ) );
            }
        }
        TableWriterPatch32( w, len_at_reserves, (uint32_t) ( w->offset - len_at_reserves - 4 ) );
    }
    TableWriterPut16( w, 0 ); /* terminator */
    return !w->overflow;
}

static SCHEMA_UNUSED int64_t PackConfigSave( const PackConfig * value, uint8_t * buffer, int64_t capacity )
{
    TableWriter w = TableWriterMake( buffer, capacity );
    if ( !PackConfigSaveBody( &w, value ) ) { return -1; }
    return w.offset; /* == PackConfigMeasure( value ) */
}

static SCHEMA_UNUSED SCHEMA_TABLEDEMO_TABLE_INLINE int PackConfigLoadBody( TableReader * r, PackConfig * value )
{
    PackConfigReset( value ); /* prefill declared defaults in place, then overlay */
    for ( ;; )
    {
        uint16_t field_id;
        uint8_t kind;
        if ( !TableReaderHas( r, 2 ) ) { r->report->malformed = 1; return 0; }
        field_id = TableReaderGet16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !TableReaderHas( r, 1 ) ) { r->report->malformed = 1; return 0; }
        kind = TableReaderGet8( r );
        switch ( field_id )
        {
            case 0xe8e6: /* version */
            {
                if ( kind != 8 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                if ( !TableReaderHas( &(*r), 4 ) ) { r->report->malformed = 1; return 0; }
                {
                    uint32_t decoded_v = (uint32_t) TableReaderGet32( &(*r) );
                    value->version = decoded_v;
                }
                break;
            }
            case 0x1b51: /* global */
            {
                if ( kind != 13 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                {
                    TableReader sub = TableReaderMake( r->buffer + r->offset, body_len, r->report );
                    GlobalSettingsLoadBody( &sub, &value->global );
                }
                r->offset += body_len;
                break;
            }
            case 0x2d39: /* ships */
            {
                if ( kind != 16 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                int64_t body_end;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                body_end = r->offset + body_len;
                if ( body_len >= 5 )
                {
                    uint8_t elem_kind = TableReaderGet8( r );
                    uint32_t count = TableReaderGet32( r );
                    TableReader sub;
                    uint32_t i;
                    if ( elem_kind != 13 ) { r->report->kind_mismatch++; r->offset = body_end; break; }
                    sub = TableReaderMake( r->buffer + r->offset, body_end - r->offset, r->report );
                    for ( i = 0; i < count; i++ )
                    {
                        uint16_t key;
                        uint32_t elem_len;
                        ShipType slot;
                        if ( !TableReaderHas( &sub, 2 ) ) { r->report->malformed = 1; break; }
                        key = TableReaderGet16( &sub );
                        if ( !TableReaderHas( &sub, 4 ) ) { r->report->malformed = 1; break; }
                        elem_len = TableReaderGet32( &sub );
                        if ( !TableReaderHas( &sub, elem_len ) ) { r->report->malformed = 1; break; }
                        if ( key == 0 )
                        {
                            /* None is the NULL KEY: 0 is the reserved id no declared
                               name can fold to, so a body carrying one is DAMAGED, not
                               merely foreign. Framing damage stops this body, keeps what
                               it decoded, and the parent reads on past the length
                               (docs/SPEC-TABLES.md §3.2, §4). */
                            r->report->malformed = 1;
                            break;
                        }
                        slot = SHIP_TYPE_NONE;
                        {
                            int32_t before = r->report->unknown;
                            switch ( key )
                            {
                                case 0: slot = SHIP_TYPE_NONE; break;
                                case 0x412b: slot = SHIP_TYPE_FIGHTER; break;
                                case 0xb7ce: slot = SHIP_TYPE_BOMBER; break;
                                case 0xd97a: slot = SHIP_TYPE_SCOUT; break;
                                default: slot = SHIP_TYPE_NONE; r->report->unknown++; break; /* an id this build cannot name */
                            }
                            if ( r->report->unknown != before )
                            {
                                sub.offset += elem_len; /* a slot this reader cannot name */
                                continue;
                            }
                        }
                        {
                            TableReader elem = TableReaderMake( sub.buffer + sub.offset, elem_len, r->report );
                            ShipEntryLoadBody( &elem, &value->ships[(int32_t) slot - 1] );
                        }
                        sub.offset += elem_len;
                    }
                }
                r->offset = body_end; /* unread pairs and slack skip via the length */
                break;
            }
            case 0xb2eb: /* thresholds */
            {
                if ( kind != 16 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                int64_t body_end;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                body_end = r->offset + body_len;
                if ( body_len >= 5 )
                {
                    uint8_t elem_kind = TableReaderGet8( r );
                    uint32_t count = TableReaderGet32( r );
                    TableReader sub;
                    uint32_t i;
                    if ( elem_kind != 4 ) { r->report->kind_mismatch++; r->offset = body_end; break; }
                    sub = TableReaderMake( r->buffer + r->offset, body_end - r->offset, r->report );
                    for ( i = 0; i < count; i++ )
                    {
                        uint16_t key;
                        uint32_t elem_len;
                        Difficulty slot;
                        if ( !TableReaderHas( &sub, 2 ) ) { r->report->malformed = 1; break; }
                        key = TableReaderGet16( &sub );
                        if ( !TableReaderHas( &sub, 4 ) ) { r->report->malformed = 1; break; }
                        elem_len = TableReaderGet32( &sub );
                        if ( !TableReaderHas( &sub, elem_len ) ) { r->report->malformed = 1; break; }
                        if ( key == 0 )
                        {
                            /* None is the NULL KEY: 0 is the reserved id no declared
                               name can fold to, so a body carrying one is DAMAGED, not
                               merely foreign. Framing damage stops this body, keeps what
                               it decoded, and the parent reads on past the length
                               (docs/SPEC-TABLES.md §3.2, §4). */
                            r->report->malformed = 1;
                            break;
                        }
                        slot = DIFFICULTY_NONE;
                        {
                            int32_t before = r->report->unknown;
                            switch ( key )
                            {
                                case 0: slot = DIFFICULTY_NONE; break;
                                case 0x7d08: slot = DIFFICULTY_EASY; break;
                                case 0x7fac: slot = DIFFICULTY_NORMAL; break;
                                case 0xc313: slot = DIFFICULTY_HARD; break;
                                default: slot = DIFFICULTY_NONE; r->report->unknown++; break; /* an id this build cannot name */
                            }
                            if ( r->report->unknown != before )
                            {
                                sub.offset += elem_len; /* a slot this reader cannot name */
                                continue;
                            }
                        }
                        {
                            TableReader elem = TableReaderMake( sub.buffer + sub.offset, elem_len, r->report );
                            if ( !TableReaderHas( &elem, 4 ) ) { r->report->malformed = 1; sub.offset += elem_len; continue; }
                            {
                                int32_t decoded_v = (int32_t) TableReaderGet32( &elem );
                                if ( decoded_v < 0 ) { decoded_v = 0; r->report->clamped++; }
                                else if ( decoded_v > 1000 ) { decoded_v = 1000; r->report->clamped++; }
                                value->thresholds[(int32_t) slot - 1] = decoded_v;
                            }
                        }
                        sub.offset += elem_len;
                    }
                }
                r->offset = body_end; /* unread pairs and slack skip via the length */
                break;
            }
            case 0x049d: /* reserves */
            {
                if ( kind != 14 )
                {
                    r->report->kind_mismatch++;
                    if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                    break;
                }
                uint32_t body_len;
                int64_t body_end;
                if ( !TableReaderHas( r, 4 ) ) { r->report->malformed = 1; return 0; }
                body_len = TableReaderGet32( r );
                if ( !TableReaderHas( r, body_len ) ) { r->report->malformed = 1; return 0; }
                body_end = r->offset + body_len;
                if ( body_len >= 5 )
                {
                    uint8_t elem_kind = TableReaderGet8( r );
                    uint32_t count = TableReaderGet32( r );
                    uint32_t keep;
                    TableReader sub;
                    uint32_t i;
                    uint32_t decoded = 0;
                    if ( elem_kind != 13 ) { r->report->kind_mismatch++; r->offset = body_end; break; }
                    keep = count;
                    if ( keep > 3 ) { keep = 3; r->report->clamped++; }
                    /* elements are BOUNDED by the field body: a count the length
                       cannot cover keeps the decoded prefix, flags malformed, and
                       the parent continues at the next field — following fields'
                       bytes are never fabricated into elements */
                    sub = TableReaderMake( r->buffer + r->offset, body_end - r->offset, r->report );
                    for ( i = 0; i < keep; i++ )
                    {
                        uint32_t elem_len;
                        if ( !TableReaderHas( &sub, 4 ) ) { r->report->malformed = 1; break; }
                        elem_len = TableReaderGet32( &sub );
                        if ( !TableReaderHas( &sub, elem_len ) ) { r->report->malformed = 1; break; }
                        {
                            TableReader elem = TableReaderMake( sub.buffer + sub.offset, elem_len, r->report );
                            ShipEntryLoadBody( &elem, &value->reserves[i] );
                        }
                        sub.offset += elem_len;
                        decoded = i + 1;
                    }
                    value->reserves_count = (int32_t) decoded;
                }
                r->offset = body_end; /* excess elements and slack skip via the length */
                break;
            }
            default:
            {
                r->report->unknown++;
                if ( !TableReaderSkip( r, kind ) ) { r->report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

static SCHEMA_UNUSED int PackConfigLoad( PackConfig * value, const uint8_t * buffer, int64_t bytes, TableReport * report )
{
    TableReport ignored;
    TableReader r;
    memset( &ignored, 0, sizeof( ignored ) );
    r = TableReaderMake( buffer, bytes, report != NULL ? report : &ignored );
    return PackConfigLoadBody( &r, value );
}

/* ---- the cooked form: point at a cook (docs/SPEC-TABLES.md §7) ---- */

/* GunnerSettingsOpen: match the header and POINT. On a match the bytes ARE what this
   build wrote, in this build's layout and this build's byte order, so there
   is nothing to validate and nothing to fix up and the root comes back as it
   lies. On ANY refusal it returns NULL and the caller falls back to a wire
   load, which is the path that carries every version.

   It is O(1) IN THE FILE'S SIZE — the header and nothing per node — so a one
   megabyte cook and a one gigabyte cook open in the same time, and a mapped
   file's pages are touched only as they are used.

   GunnerSettings IS FIXED-SIZE, so its cook is ONE REGION OF ONE NODE and not a second
   shape (§7): one struct behind the header, at the region's base, which is
   what this returns. There is no graph below it and nothing to resolve.

   There is ONE entry point and no tolerant twin: a build either wrote this
   file or it did not, and the build version is what says which. Validating a
   file whose provenance a person doubts is schema cook-check, offline, over
   the ATTRIBUTION part beside the data — a person's decision, never a
   parameter on a load. */
static SCHEMA_UNUSED const GunnerSettings * GunnerSettingsOpen( const void * bytes, uint64_t length )
{
    return (const GunnerSettings *) TableCookOpen( bytes, length, (uint64_t) sizeof( GunnerSettings ), (uint64_t) SCHEMA_TABLE_ALIGNOF( GunnerSettings ) );
}

/* ShipEntryOpen: match the header and POINT. On a match the bytes ARE what this
   build wrote, in this build's layout and this build's byte order, so there
   is nothing to validate and nothing to fix up and the root comes back as it
   lies. On ANY refusal it returns NULL and the caller falls back to a wire
   load, which is the path that carries every version.

   It is O(1) IN THE FILE'S SIZE — the header and nothing per node — so a one
   megabyte cook and a one gigabyte cook open in the same time, and a mapped
   file's pages are touched only as they are used.

   ShipEntry IS FIXED-SIZE, so its cook is ONE REGION OF ONE NODE and not a second
   shape (§7): one struct behind the header, at the region's base, which is
   what this returns. There is no graph below it and nothing to resolve.

   There is ONE entry point and no tolerant twin: a build either wrote this
   file or it did not, and the build version is what says which. Validating a
   file whose provenance a person doubts is schema cook-check, offline, over
   the ATTRIBUTION part beside the data — a person's decision, never a
   parameter on a load. */
static SCHEMA_UNUSED const ShipEntry * ShipEntryOpen( const void * bytes, uint64_t length )
{
    return (const ShipEntry *) TableCookOpen( bytes, length, (uint64_t) sizeof( ShipEntry ), (uint64_t) SCHEMA_TABLE_ALIGNOF( ShipEntry ) );
}

/* GlobalSettingsOpen: match the header and POINT. On a match the bytes ARE what this
   build wrote, in this build's layout and this build's byte order, so there
   is nothing to validate and nothing to fix up and the root comes back as it
   lies. On ANY refusal it returns NULL and the caller falls back to a wire
   load, which is the path that carries every version.

   It is O(1) IN THE FILE'S SIZE — the header and nothing per node — so a one
   megabyte cook and a one gigabyte cook open in the same time, and a mapped
   file's pages are touched only as they are used.

   GlobalSettings IS FIXED-SIZE, so its cook is ONE REGION OF ONE NODE and not a second
   shape (§7): one struct behind the header, at the region's base, which is
   what this returns. There is no graph below it and nothing to resolve.

   There is ONE entry point and no tolerant twin: a build either wrote this
   file or it did not, and the build version is what says which. Validating a
   file whose provenance a person doubts is schema cook-check, offline, over
   the ATTRIBUTION part beside the data — a person's decision, never a
   parameter on a load. */
static SCHEMA_UNUSED const GlobalSettings * GlobalSettingsOpen( const void * bytes, uint64_t length )
{
    return (const GlobalSettings *) TableCookOpen( bytes, length, (uint64_t) sizeof( GlobalSettings ), (uint64_t) SCHEMA_TABLE_ALIGNOF( GlobalSettings ) );
}

/* PackConfigOpen: match the header and POINT. On a match the bytes ARE what this
   build wrote, in this build's layout and this build's byte order, so there
   is nothing to validate and nothing to fix up and the root comes back as it
   lies. On ANY refusal it returns NULL and the caller falls back to a wire
   load, which is the path that carries every version.

   It is O(1) IN THE FILE'S SIZE — the header and nothing per node — so a one
   megabyte cook and a one gigabyte cook open in the same time, and a mapped
   file's pages are touched only as they are used.

   PackConfig IS FIXED-SIZE, so its cook is ONE REGION OF ONE NODE and not a second
   shape (§7): one struct behind the header, at the region's base, which is
   what this returns. There is no graph below it and nothing to resolve.

   There is ONE entry point and no tolerant twin: a build either wrote this
   file or it did not, and the build version is what says which. Validating a
   file whose provenance a person doubts is schema cook-check, offline, over
   the ATTRIBUTION part beside the data — a person's decision, never a
   parameter on a load. */
static SCHEMA_UNUSED const PackConfig * PackConfigOpen( const void * bytes, uint64_t length )
{
    return (const PackConfig *) TableCookOpen( bytes, length, (uint64_t) sizeof( PackConfig ), (uint64_t) SCHEMA_TABLE_ALIGNOF( PackConfig ) );
}

/* ---- relocatability: the wire is a pure length-prefixed stream AND the
   decoded storage is pointer-free — every closure type is a plain C struct
   of scalars and arrays, so instances can be memcpy'd, mmap'd, shared
   across processes, and walked through descriptor offsets. C has no way to
   put a pointer, a vtable or a non-trivial member in one of these, which is
   why the property is a fact of the language here rather than an assert.
   What IS asserted is the layout the build version was taken over. */

/* ---- the cook's layout contract (docs/SPEC-TABLES.md §20.3) ----

   The compiler derived every number below from the declaration and folded it
   into the BUILD VERSION; these asserts are this compiler saying whether it
   agrees. The model is not self-evidently right — on 32-bit System V
   alignof(uint64_t) is 4, not 8 — which is precisely why it is asserted
   rather than assumed. */
SCHEMA_TABLE_STATIC_ASSERT( GunnerSettings_sizeof, sizeof( GunnerSettings ) == 36, "GunnerSettings's sizeof moved: the build version was taken over 36, so a cook of it would not be this build's file (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GunnerSettings_alignof, SCHEMA_TABLE_ALIGNOF( GunnerSettings ) == 4, "GunnerSettings's alignof moved: the build version was taken over 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GunnerSettings_reaction_offset, offsetof( GunnerSettings, reaction ) == 0, "GunnerSettings's field reaction moved: the build version was taken over offset 0 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GunnerSettings_tracking_offset, offsetof( GunnerSettings, tracking ) == 4, "GunnerSettings's field tracking moved: the build version was taken over offset 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GunnerSettings_callsign_offset, offsetof( GunnerSettings, callsign ) == 5, "GunnerSettings's field callsign moved: the build version was taken over offset 5 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_sizeof, sizeof( ShipEntry ) == 108, "ShipEntry's sizeof moved: the build version was taken over 108, so a cook of it would not be this build's file (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_alignof, SCHEMA_TABLE_ALIGNOF( ShipEntry ) == 4, "ShipEntry's alignof moved: the build version was taken over 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_display_name_offset, offsetof( ShipEntry, display_name ) == 0, "ShipEntry's field display_name moved: the build version was taken over offset 0 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_health_offset, offsetof( ShipEntry, health ) == 40, "ShipEntry's field health moved: the build version was taken over offset 40 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_mass_offset, offsetof( ShipEntry, mass ) == 44, "ShipEntry's field mass moved: the build version was taken over offset 44 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_hardpoints_offset, offsetof( ShipEntry, hardpoints ) == 48, "ShipEntry's field hardpoints moved: the build version was taken over offset 48 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( ShipEntry_gunner_offset, offsetof( ShipEntry, gunner ) == 68, "ShipEntry's field gunner moved: the build version was taken over offset 68 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_sizeof, sizeof( GlobalSettings ) == 72, "GlobalSettings's sizeof moved: the build version was taken over 72, so a cook of it would not be this build's file (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_alignof, SCHEMA_TABLE_ALIGNOF( GlobalSettings ) == 4, "GlobalSettings's alignof moved: the build version was taken over 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_tick_rate_offset, offsetof( GlobalSettings, tick_rate ) == 0, "GlobalSettings's field tick_rate moved: the build version was taken over offset 0 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_difficulty_offset, offsetof( GlobalSettings, difficulty ) == 4, "GlobalSettings's field difficulty moved: the build version was taken over offset 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_build_note_offset, offsetof( GlobalSettings, build_note ) == 5, "GlobalSettings's field build_note moved: the build version was taken over offset 5 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( GlobalSettings_spawn_delays_offset, offsetof( GlobalSettings, spawn_delays ) == 60, "GlobalSettings's field spawn_delays moved: the build version was taken over offset 60 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_sizeof, sizeof( PackConfig ) == 740, "PackConfig's sizeof moved: the build version was taken over 740, so a cook of it would not be this build's file (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_alignof, SCHEMA_TABLE_ALIGNOF( PackConfig ) == 4, "PackConfig's alignof moved: the build version was taken over 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_version_offset, offsetof( PackConfig, version ) == 0, "PackConfig's field version moved: the build version was taken over offset 0 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_global_offset, offsetof( PackConfig, global ) == 4, "PackConfig's field global moved: the build version was taken over offset 4 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_ships_offset, offsetof( PackConfig, ships ) == 76, "PackConfig's field ships moved: the build version was taken over offset 76 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_thresholds_offset, offsetof( PackConfig, thresholds ) == 400, "PackConfig's field thresholds moved: the build version was taken over offset 400 (docs/SPEC-TABLES.md §20.3)" );
SCHEMA_TABLE_STATIC_ASSERT( PackConfig_reserves_offset, offsetof( PackConfig, reserves ) == 412, "PackConfig's field reserves moved: the build version was taken over offset 412 (docs/SPEC-TABLES.md §20.3)" );

/* ---- reflection descriptors (tables only, docs/SPEC-TABLES.md) ----

   The descriptors are CONSTANT data DEFINED in PackTable.c: a translation
   unit that includes this header for the wire codecs pays nothing for
   them, and one definition per program means one address per type. The
   whole reflection surface is immutable — read it from any thread, any
   time. */

extern const TableTypeInfo schema_tabledemo_gunner_settings_info_;
extern const TableTypeInfo schema_tabledemo_ship_entry_info_;
extern const TableTypeInfo schema_tabledemo_global_settings_info_;
extern const TableTypeInfo schema_tabledemo_pack_config_info_;

static SCHEMA_UNUSED const TableTypeInfo * GunnerSettingsTableType( void ) { return &schema_tabledemo_gunner_settings_info_; }
static SCHEMA_UNUSED const TableTypeInfo * ShipEntryTableType( void ) { return &schema_tabledemo_ship_entry_info_; }
static SCHEMA_UNUSED const TableTypeInfo * GlobalSettingsTableType( void ) { return &schema_tabledemo_global_settings_info_; }
static SCHEMA_UNUSED const TableTypeInfo * PackConfigTableType( void ) { return &schema_tabledemo_pack_config_info_; }

/* ---- the text form (docs/SPEC-TABLES.md §16) ---- */

/* GunnerSettings in and out of a JSON text — one instance, one text, the generic
   walk over this type's descriptors (docs/SPEC-TABLES.md §16). Defined in
   PackTable.c; compile it to use them. */
int schema_tabledemo_gunner_settings_from_json_( GunnerSettings * value, const char * text, int64_t bytes, TableReport * report );
int64_t schema_tabledemo_gunner_settings_to_json_( const GunnerSettings * value, char * buffer, int64_t capacity );
static SCHEMA_UNUSED int GunnerSettingsFromJson( GunnerSettings * value, const char * text, int64_t bytes, TableReport * report )
{
    return schema_tabledemo_gunner_settings_from_json_( value, text, bytes, report );
}
static SCHEMA_UNUSED int64_t GunnerSettingsToJsonMeasure( const GunnerSettings * value ) { return schema_tabledemo_gunner_settings_to_json_( value, NULL, 0 ); }
static SCHEMA_UNUSED int64_t GunnerSettingsToJson( const GunnerSettings * value, char * buffer, int64_t capacity )
{
    return schema_tabledemo_gunner_settings_to_json_( value, buffer, capacity );
}

/* ShipEntry in and out of a JSON text — one instance, one text, the generic
   walk over this type's descriptors (docs/SPEC-TABLES.md §16). Defined in
   PackTable.c; compile it to use them. */
int schema_tabledemo_ship_entry_from_json_( ShipEntry * value, const char * text, int64_t bytes, TableReport * report );
int64_t schema_tabledemo_ship_entry_to_json_( const ShipEntry * value, char * buffer, int64_t capacity );
static SCHEMA_UNUSED int ShipEntryFromJson( ShipEntry * value, const char * text, int64_t bytes, TableReport * report )
{
    return schema_tabledemo_ship_entry_from_json_( value, text, bytes, report );
}
static SCHEMA_UNUSED int64_t ShipEntryToJsonMeasure( const ShipEntry * value ) { return schema_tabledemo_ship_entry_to_json_( value, NULL, 0 ); }
static SCHEMA_UNUSED int64_t ShipEntryToJson( const ShipEntry * value, char * buffer, int64_t capacity )
{
    return schema_tabledemo_ship_entry_to_json_( value, buffer, capacity );
}

/* GlobalSettings in and out of a JSON text — one instance, one text, the generic
   walk over this type's descriptors (docs/SPEC-TABLES.md §16). Defined in
   PackTable.c; compile it to use them. */
int schema_tabledemo_global_settings_from_json_( GlobalSettings * value, const char * text, int64_t bytes, TableReport * report );
int64_t schema_tabledemo_global_settings_to_json_( const GlobalSettings * value, char * buffer, int64_t capacity );
static SCHEMA_UNUSED int GlobalSettingsFromJson( GlobalSettings * value, const char * text, int64_t bytes, TableReport * report )
{
    return schema_tabledemo_global_settings_from_json_( value, text, bytes, report );
}
static SCHEMA_UNUSED int64_t GlobalSettingsToJsonMeasure( const GlobalSettings * value ) { return schema_tabledemo_global_settings_to_json_( value, NULL, 0 ); }
static SCHEMA_UNUSED int64_t GlobalSettingsToJson( const GlobalSettings * value, char * buffer, int64_t capacity )
{
    return schema_tabledemo_global_settings_to_json_( value, buffer, capacity );
}

/* PackConfig in and out of a JSON text — one instance, one text, the generic
   walk over this type's descriptors (docs/SPEC-TABLES.md §16). Defined in
   PackTable.c; compile it to use them. */
int schema_tabledemo_pack_config_from_json_( PackConfig * value, const char * text, int64_t bytes, TableReport * report );
int64_t schema_tabledemo_pack_config_to_json_( const PackConfig * value, char * buffer, int64_t capacity );
static SCHEMA_UNUSED int PackConfigFromJson( PackConfig * value, const char * text, int64_t bytes, TableReport * report )
{
    return schema_tabledemo_pack_config_from_json_( value, text, bytes, report );
}
static SCHEMA_UNUSED int64_t PackConfigToJsonMeasure( const PackConfig * value ) { return schema_tabledemo_pack_config_to_json_( value, NULL, 0 ); }
static SCHEMA_UNUSED int64_t PackConfigToJson( const PackConfig * value, char * buffer, int64_t capacity )
{
    return schema_tabledemo_pack_config_to_json_( value, buffer, capacity );
}


#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_TABLEDEMO_PACKTABLE_H */
