// Code generated by the schema compiler from Padded.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package blockdemo — the TABLE wire (docs/SPEC-TABLES.md): evolution-tolerant, neutral
// bytes, no serialize dependency. Tables version by field id, never by the
// unit's protocol id.

using System;

namespace Blockdemo
{

    // table PaddedRow — TABLE-wire storage: public fields, every buffer allocated at
    // construction, declared defaults in the field initializers (docs/SPEC-TABLES.md)
    public sealed class PaddedRow
    {
        public byte Tag;
        public double Value;
        public bool Flag;
        public uint Id;
        public byte[] Label = new byte[15]; // string(15): max length, used length beside it
        public int LabelLength;
        public ushort[] Slots = new ushort[4];
        public TableKeyed<byte, Team> Teams = new TableKeyed<byte, Team>(); // [Team]: one slot per named variant, keyed by the value
        public int Counter;
        public bool CounterPresent; // ?int32: absent until set
    }

    // table PaddedFrame — TABLE-wire storage: public fields, every buffer allocated at
    // construction, declared defaults in the field initializers (docs/SPEC-TABLES.md)
    public sealed class PaddedFrame
    {
        public byte Marker;
        public ulong Stamp;
        public PaddedRow[] Rows = new PaddedRow[64]; // used count beside it; count in [0, 64]
        public int RowsCount;
        public byte[] Blob = new byte[12]; // bytes(12): fixed buffer, used length beside it
        public int BlobLength;

        public PaddedFrame()
        {
            for (int i = 0; i < Rows.Length; i++)
            {
                Rows[i] = new PaddedRow();
            }
        }
    }

    // Schema carries every generated function of the unit — C# has no
    // namespace-level functions, so the static class is their home; partial,
    // one slice per generated file.
    public static partial class Schema
    {
        // TableReset(PaddedRow) restores PaddedRow's declared defaults in place, reusing every
        // buffer the value already owns. The reader calls it before overlaying.
        public static void TableReset(PaddedRow value)
        {
            value.Tag = 0;
            value.Value = 0.0;
            value.Flag = false;
            value.Id = 0;
            Array.Clear(value.Label, 0, value.Label.Length);
            value.LabelLength = 0;
            Array.Clear(value.Slots, 0, value.Slots.Length);
            Array.Clear(value.Teams.Slots, 0, value.Teams.Slots.Length);
            value.Counter = 0;
            value.CounterPresent = false;
        }

        public static long PaddedRowMeasure(PaddedRow value)
        {
            long bytes = 2; // terminator
            if (value.Tag != 0) { bytes += 3 + 1; } // tag
            if (value.Value != 0.0) { bytes += 3 + 8; } // value
            if (value.Flag != false) { bytes += 3 + 1; } // flag
            if (value.Id != 0) { bytes += 3 + 4; } // id
            if (value.LabelLength < 0 || value.LabelLength > 15) { return -1; } // storage invariant
            if (value.LabelLength > 0) { bytes += 3 + 4 + value.LabelLength; } // label
            {
                bool allDefault = true;
                for (int i = 0; i < 4; i++) { if (value.Slots[i] != 0) { allDefault = false; break; } }
                if (!allDefault)
                {
                    bytes += 3 + 4 + 5 + 8; // slots
                }
            }
            {
                long pairs = 0, keyedBytes = 0;
                for (int i = 0; i < 4; i++) // [Team]: every stored slot is a named variant's
                {
                    if (value.Teams.Slots[i] == 0) { continue; } // a default slot elides
                    ushort keyId;
                    if (!TableEnumId((Team)(i + 1), out keyId)) { return -1; } // i is the STORAGE index; the key it holds is i + 1
                    pairs++; keyedBytes += 2 + 4 + 1; // key, length, element
                }
                if (pairs > 0) { bytes += 3 + 4 + 5 + keyedBytes; } // teams
            }
            if (value.CounterPresent) // ?int32: presence decides, not content
            {
                bytes += 3 + 4; // counter
            }
            return bytes;
        }

        public static bool PaddedRowSaveBody(ref TableWriter w, PaddedRow value)
        {
            if (value.Tag != 0)
            {
                w.Put16(0xbc64); w.Put8(6); // tag
                w.Put8(unchecked((byte)(value.Tag)));
            }
            if (value.Value != 0.0)
            {
                w.Put16(0x9194); w.Put8(11); // value
                w.Put64(TableDoubleToBits(value.Value));
            }
            if (value.Flag != false)
            {
                w.Put16(0x153d); w.Put8(1); // flag
                w.Put8(value.Flag ? (byte)1 : (byte)0);
            }
            if (value.Id != 0)
            {
                w.Put16(0x5dd8); w.Put8(8); // id
                w.Put32(unchecked((uint)(value.Id)));
            }
            if (value.LabelLength < 0 || value.LabelLength > 15) { return false; } // storage invariant
            if (value.LabelLength > 0)
            {
                w.Put16(0xe16a); w.Put8(12); // label
                w.Put32((uint)value.LabelLength);
                w.Raw(new ReadOnlySpan<byte>(value.Label, 0, value.LabelLength));
            }
            {
                bool allDefault = true;
                for (int i = 0; i < 4; i++) { if (value.Slots[i] != 0) { allDefault = false; break; } }
                if (!allDefault)
                {
                    w.Put16(0xf4d8); w.Put8(14); // slots (fixed [4])
                    int lenAt = w.Offset; w.Put32(0);
                    w.Put8(7); w.Put32(4);
                    for (int i = 0; i < 4; i++)
                    {
                        w.Put16(unchecked((ushort)(value.Slots[i])));
                    }
                    w.Patch32(lenAt, (uint)(w.Offset - lenAt - 4));
                }
            }
            {
                uint pairs = 0;
                for (int i = 0; i < 4; i++) // [Team]: every stored slot is a named variant's
                {
                    if (value.Teams.Slots[i] == 0) { continue; } // a default slot elides
                    ushort keyId;
                    if (!TableEnumId((Team)(i + 1), out keyId)) { return false; } // i is the STORAGE index; the key it holds is i + 1
                    pairs++;
                }
                if (pairs > 0)
                {
                    // KIND 16, not 14: a keyed body and a positional one are
                    // incompatible, so a reader of the other kind must see a kind
                    // mismatch and skip, never misdecode (docs/SPEC-TABLES.md §3.2)
                    w.Put16(0x9ae1); w.Put8(16); // teams (keyed by Team)
                    int lenAt = w.Offset; w.Put32(0);
                    w.Put8(6); w.Put32(pairs);
                    // ASCENDING BY VARIANT ORDINAL, which is slot order — this
                    // writer's choice, and a reader must not rely on it: every
                    // slot is found by its key (docs/SPEC-TABLES.md §3.2)
                    for (int i = 0; i < 4; i++)
                    {
                        if (value.Teams.Slots[i] == 0) { continue; } // a default slot elides
                        ushort keyId;
                        if (!TableEnumId((Team)(i + 1), out keyId)) { return false; } // i is the STORAGE index; the key it holds is i + 1
                        w.Put16(keyId); // the slot's VARIANT id, not its position
                        int elemLenAt = w.Offset; w.Put32(0);
                        w.Put8(unchecked((byte)(value.Teams.Slots[i])));
                        w.Patch32(elemLenAt, (uint)(w.Offset - elemLenAt - 4));
                    }
                    w.Patch32(lenAt, (uint)(w.Offset - lenAt - 4));
                }
            }
            if (value.CounterPresent) // ?int32
            {
                w.Put16(0x428f); w.Put8(4); // counter
                w.Put32(unchecked((uint)(value.Counter)));
            }
            w.Put16(0); // terminator
            return !w.Overflow;
        }

        public static long PaddedRowSave(PaddedRow value, Span<byte> buffer)
        {
            TableWriter w = new TableWriter(buffer);
            if (!PaddedRowSaveBody(ref w, value)) { return -1; }
            return w.Offset; // == PaddedRowMeasure(value)
        }

        public static bool PaddedRowLoadBody(ref TableReader r, PaddedRow value)
        {
            TableReset(value); // restore declared defaults in place, then overlay
            for (;;)
            {
                if (!r.Has(2)) { r.Report.Malformed = true; return false; }
                ushort fieldId = r.Get16();
                if (fieldId == 0) { return true; }
                if (!r.Has(1)) { r.Report.Malformed = true; return false; }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xbc64: // tag
                    {
                        if (kind != 6)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(1)) { r.Report.Malformed = true; return false; }
                        {
                            byte decodedV = unchecked((byte)r.Get8());
                            value.Tag = decodedV;
                        }
                        break;
                    }
                    case 0x9194: // value
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(8)) { r.Report.Malformed = true; return false; }
                        value.Value = TableBitsToDouble(r.Get64());
                        break;
                    }
                    case 0x153d: // flag
                    {
                        if (kind != 1)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(1)) { r.Report.Malformed = true; return false; }
                        value.Flag = r.Get8() != 0;
                        break;
                    }
                    case 0x5dd8: // id
                    {
                        if (kind != 8)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        {
                            uint decodedV = unchecked((uint)r.Get32());
                            value.Id = decodedV;
                        }
                        break;
                    }
                    case 0xe16a: // label
                    {
                        if (kind != 12)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        uint len = r.Get32();
                        if (!r.Has(len)) { r.Report.Malformed = true; return false; }
                        uint keep = len;
                        if (keep > 15) { keep = 15; r.Report.Clamped++; }
                        r.Buffer.Slice(r.Offset, (int)keep).CopyTo(new Span<byte>(value.Label, 0, (int)keep));
                        value.LabelLength = (int)keep;
                        r.Offset += (int)len;
                        break;
                    }
                    case 0xf4d8: // slots
                    {
                        if (kind != 14)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        uint bodyLen = r.Get32();
                        if (!r.Has(bodyLen)) { r.Report.Malformed = true; return false; }
                        int bodyEnd = r.Offset + (int)bodyLen;
                        if (bodyLen >= 5)
                        {
                            byte elemKind = r.Get8();
                            uint count = r.Get32();
                            if (elemKind != 7) { r.Report.KindMismatch++; r.Offset = bodyEnd; break; }
                            uint keep = count;
                            if (keep > 4) { keep = 4; r.Report.Clamped++; }
                            // elements are BOUNDED by the field body: a count the length
                            // cannot cover keeps the decoded prefix, flags malformed, and
                            // the parent continues at the next field — following fields'
                            // bytes are never fabricated into elements
                            TableReader sub = new TableReader(r.Buffer.Slice(r.Offset, bodyEnd - r.Offset), r.Report);
                            for (uint i = 0; i < keep; i++)
                            {
                                if (!sub.Has(2)) { r.Report.Malformed = true; break; }
                                {
                                    ushort decodedV = unchecked((ushort)sub.Get16());
                                    value.Slots[i] = decodedV;
                                }
                            }
                        }
                        r.Offset = bodyEnd; // excess elements and slack skip via the length
                        break;
                    }
                    case 0x9ae1: // teams
                    {
                        if (kind != 16)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        uint bodyLen = r.Get32();
                        if (!r.Has(bodyLen)) { r.Report.Malformed = true; return false; }
                        int bodyEnd = r.Offset + (int)bodyLen;
                        if (bodyLen >= 5)
                        {
                            byte elemKind = r.Get8();
                            uint count = r.Get32();
                            if (elemKind != 6) { r.Report.KindMismatch++; r.Offset = bodyEnd; break; }
                            TableReader sub = new TableReader(r.Buffer.Slice(r.Offset, bodyEnd - r.Offset), r.Report);
                            for (uint i = 0; i < count; i++)
                            {
                                if (!sub.Has(2)) { r.Report.Malformed = true; break; }
                                ushort key = sub.Get16();
                                if (!sub.Has(4)) { r.Report.Malformed = true; break; }
                                uint elemLen = sub.Get32();
                                if (!sub.Has(elemLen)) { r.Report.Malformed = true; break; }
                                if (key == 0)
                                {
                                    // None is the NULL KEY: 0 is the reserved id no declared
                                    // name can fold to, so a body carrying one is DAMAGED, not
                                    // merely foreign. Framing damage stops this body, keeps what
                                    // it decoded, and the parent reads on past the length
                                    // (docs/SPEC-TABLES.md §3.2, §4).
                                    r.Report.Malformed = true;
                                    break;
                                }
                                Team slot;
                                if (!TableEnumValue(key, out slot))
                                {
                                    r.Report.Unknown++; // a slot this reader cannot name
                                    sub.Offset += (int)elemLen;
                                    continue;
                                }
                                {
                                    TableReader elem = new TableReader(sub.Buffer.Slice(sub.Offset, (int)elemLen), r.Report);
                                    if (!elem.Has(1)) { r.Report.Malformed = true; sub.Offset += (int)elemLen; continue; }
                                    {
                                        byte decodedV = unchecked((byte)elem.Get8());
                                        value.Teams.Slots[(int)slot - 1] = decodedV;
                                    }
                                }
                                sub.Offset += (int)elemLen;
                            }
                        }
                        r.Offset = bodyEnd; // unread pairs and slack skip via the length
                        break;
                    }
                    case 0x428f: // counter
                    {
                        if (kind != 4)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        {
                            int decodedV = unchecked((int)r.Get32());
                            value.Counter = decodedV;
                        }
                        value.CounterPresent = true;
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                        break;
                    }
                }
            }
        }

        public static bool PaddedRowLoad(PaddedRow value, ReadOnlySpan<byte> bytes, TableReport report)
        {
            TableReader r = new TableReader(bytes, report != null ? report : new TableReport());
            return PaddedRowLoadBody(ref r, value);
        }

        // TableReset(PaddedFrame) restores PaddedFrame's declared defaults in place, reusing every
        // buffer the value already owns. The reader calls it before overlaying.
        public static void TableReset(PaddedFrame value)
        {
            value.Marker = 0;
            value.Stamp = 0;
            for (int i = 0; i < value.Rows.Length; i++)
            {
                TableReset(value.Rows[i]);
            }
            value.RowsCount = 0;
            Array.Clear(value.Blob, 0, value.Blob.Length);
            value.BlobLength = 0;
        }

        public static long PaddedFrameMeasure(PaddedFrame value)
        {
            long bytes = 2; // terminator
            if (value.Marker != 0) { bytes += 3 + 1; } // marker
            if (value.Stamp != 0) { bytes += 3 + 8; } // stamp
            if (value.RowsCount < 0 || value.RowsCount > 64) { return -1; } // storage invariant
            if (value.RowsCount > 0)
            {
                bytes += 3 + 4 + 5; // rows
                for (int i = 0; i < value.RowsCount; i++)
                {
                    long elem = PaddedRowMeasure(value.Rows[i]);
                    if (elem < 0) { return -1; }
                    bytes += 4 + elem;
                }
            }
            if (value.BlobLength < 0 || value.BlobLength > 12) { return -1; } // storage invariant
            if (value.BlobLength > 0) { bytes += 3 + 4 + 5 + value.BlobLength; } // blob
            return bytes;
        }

        public static bool PaddedFrameSaveBody(ref TableWriter w, PaddedFrame value)
        {
            if (value.Marker != 0)
            {
                w.Put16(0x866f); w.Put8(6); // marker
                w.Put8(unchecked((byte)(value.Marker)));
            }
            if (value.Stamp != 0)
            {
                w.Put16(0x0dc6); w.Put8(9); // stamp
                w.Put64(unchecked((ulong)(value.Stamp)));
            }
            if (value.RowsCount < 0 || value.RowsCount > 64) { return false; } // storage invariant
            if (value.RowsCount > 0)
            {
                w.Put16(0x99af); w.Put8(14); // rows
                int lenAt = w.Offset; w.Put32(0);
                w.Put8(13); w.Put32((uint)value.RowsCount);
                for (int i = 0; i < value.RowsCount; i++)
                {
                    {
                        int elemLenAt = w.Offset; w.Put32(0);
                        if (!PaddedRowSaveBody(ref w, value.Rows[i])) { return false; }
                        w.Patch32(elemLenAt, (uint)(w.Offset - elemLenAt - 4));
                    }
                }
                w.Patch32(lenAt, (uint)(w.Offset - lenAt - 4));
            }
            if (value.BlobLength < 0 || value.BlobLength > 12) { return false; } // storage invariant
            if (value.BlobLength > 0)
            {
                w.Put16(0xd316); w.Put8(14); // blob
                w.Put32((uint)(5 + value.BlobLength));
                w.Put8(6); w.Put32((uint)value.BlobLength);
                w.Raw(new ReadOnlySpan<byte>(value.Blob, 0, value.BlobLength));
            }
            w.Put16(0); // terminator
            return !w.Overflow;
        }

        public static long PaddedFrameSave(PaddedFrame value, Span<byte> buffer)
        {
            TableWriter w = new TableWriter(buffer);
            if (!PaddedFrameSaveBody(ref w, value)) { return -1; }
            return w.Offset; // == PaddedFrameMeasure(value)
        }

        public static bool PaddedFrameLoadBody(ref TableReader r, PaddedFrame value)
        {
            TableReset(value); // restore declared defaults in place, then overlay
            for (;;)
            {
                if (!r.Has(2)) { r.Report.Malformed = true; return false; }
                ushort fieldId = r.Get16();
                if (fieldId == 0) { return true; }
                if (!r.Has(1)) { r.Report.Malformed = true; return false; }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0x866f: // marker
                    {
                        if (kind != 6)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(1)) { r.Report.Malformed = true; return false; }
                        {
                            byte decodedV = unchecked((byte)r.Get8());
                            value.Marker = decodedV;
                        }
                        break;
                    }
                    case 0x0dc6: // stamp
                    {
                        if (kind != 9)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(8)) { r.Report.Malformed = true; return false; }
                        {
                            ulong decodedV = unchecked((ulong)r.Get64());
                            value.Stamp = decodedV;
                        }
                        break;
                    }
                    case 0x99af: // rows
                    {
                        if (kind != 14)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        uint bodyLen = r.Get32();
                        if (!r.Has(bodyLen)) { r.Report.Malformed = true; return false; }
                        int bodyEnd = r.Offset + (int)bodyLen;
                        if (bodyLen >= 5)
                        {
                            byte elemKind = r.Get8();
                            uint count = r.Get32();
                            if (elemKind != 13) { r.Report.KindMismatch++; r.Offset = bodyEnd; break; }
                            uint keep = count;
                            if (keep > 64) { keep = 64; r.Report.Clamped++; }
                            // elements are BOUNDED by the field body: a count the length
                            // cannot cover keeps the decoded prefix, flags malformed, and
                            // the parent continues at the next field — following fields'
                            // bytes are never fabricated into elements
                            TableReader sub = new TableReader(r.Buffer.Slice(r.Offset, bodyEnd - r.Offset), r.Report);
                            uint decoded = 0;
                            for (uint i = 0; i < keep; i++)
                            {
                                if (!sub.Has(4)) { r.Report.Malformed = true; break; }
                                uint elemLen = sub.Get32();
                                if (!sub.Has(elemLen)) { r.Report.Malformed = true; break; }
                                {
                                    TableReader elem = new TableReader(sub.Buffer.Slice(sub.Offset, (int)elemLen), r.Report);
                                    PaddedRowLoadBody(ref elem, value.Rows[i]);
                                }
                                sub.Offset += (int)elemLen;
                                decoded = i + 1;
                            }
                            value.RowsCount = (int)decoded;
                        }
                        r.Offset = bodyEnd; // excess elements and slack skip via the length
                        break;
                    }
                    case 0xd316: // blob
                    {
                        if (kind != 14)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                            break;
                        }
                        if (!r.Has(4)) { r.Report.Malformed = true; return false; }
                        uint bodyLen = r.Get32();
                        if (!r.Has(bodyLen)) { r.Report.Malformed = true; return false; }
                        int bodyEnd = r.Offset + (int)bodyLen;
                        if (bodyLen >= 5)
                        {
                            byte elemKind = r.Get8();
                            uint count = r.Get32();
                            if (elemKind != 6) { r.Report.KindMismatch++; r.Offset = bodyEnd; break; }
                            uint keep = count;
                            if (keep > 12) { keep = 12; r.Report.Clamped++; }
                            // elements are BOUNDED by the field body: a count the length
                            // cannot cover keeps the decoded prefix, flags malformed, and
                            // the parent continues at the next field — following fields'
                            // bytes are never fabricated into elements
                            TableReader sub = new TableReader(r.Buffer.Slice(r.Offset, bodyEnd - r.Offset), r.Report);
                            uint decoded = 0;
                            for (uint i = 0; i < keep; i++)
                            {
                                if (!sub.Has(1)) { r.Report.Malformed = true; break; }
                                {
                                    byte decodedV = unchecked((byte)sub.Get8());
                                    value.Blob[i] = decodedV;
                                }
                                decoded = i + 1;
                            }
                            value.BlobLength = (int)decoded;
                        }
                        r.Offset = bodyEnd; // excess elements and slack skip via the length
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind)) { r.Report.Malformed = true; return false; }
                        break;
                    }
                }
            }
        }

        public static bool PaddedFrameLoad(PaddedFrame value, ReadOnlySpan<byte> bytes, TableReport report)
        {
            TableReader r = new TableReader(bytes, report != null ? report : new TableReport());
            return PaddedFrameLoadBody(ref r, value);
        }

        // ---- reflection descriptors (tables only, docs/SPEC-TABLES.md §8) ----

        private static TableTypeInfo PaddedRowTableInfo;
        public static TableTypeInfo PaddedRowTableType()
        {
            TableTypeInfo info = PaddedRowTableInfo;
            if (info != null) { return info; }
            info = new TableTypeInfo();
            info.Name = "PaddedRow";
            info.NumFields = 8;
            info.Fields = new TableFieldInfo[]
            {
                new TableFieldInfo { Name = "tag", Json = "tag", TypeName = "uint8", Id = 0xbc64, Kind = 6, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 1, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedRow)o).Tag; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Tag = unchecked((byte)r); } },
                new TableFieldInfo { Name = "value", Json = "value", TypeName = "float64", Id = 0x9194, Kind = 11, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 8, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return TableDoubleToBits(((PaddedRow)o).Value); }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Value = TableBitsToDouble(r); } },
                new TableFieldInfo { Name = "flag", Json = "flag", TypeName = "bool", Id = 0x153d, Kind = 1, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 1, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return ((PaddedRow)o).Flag ? 1ul : 0ul; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Flag = r != 0; } },
                new TableFieldInfo { Name = "id", Json = "id", TypeName = "uint32", Id = 0x5dd8, Kind = 8, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 4, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedRow)o).Id; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Id = unchecked((uint)r); } },
                new TableFieldInfo { Name = "label", Json = "label", TypeName = "string", Id = 0xe16a, Kind = 12, IsArray = false, Counted = true, Optional = false, ArrayBound = 15, ElemWidth = 0, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetBuffer = delegate(object o) { return ((PaddedRow)o).Label; }, GetCount = delegate(object o) { return ((PaddedRow)o).LabelLength; }, SetCount = delegate(object o, int n) { ((PaddedRow)o).LabelLength = n; } },
                new TableFieldInfo { Name = "slots", Json = "slots", TypeName = "uint16", Id = 0xf4d8, Kind = 7, IsArray = true, Counted = false, Optional = false, ArrayBound = 4, ElemWidth = 2, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedRow)o).Slots[i]; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Slots[i] = unchecked((ushort)r); } },
                new TableFieldInfo { Name = "teams", Json = "teams", TypeName = "uint8", Id = 0x9ae1, Kind = 6, IsArray = true, Counted = false, Optional = false, ArrayBound = (int)Team.Max, ElemWidth = 1, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = "Team", KeyName = delegate(ulong v) { return EnumNameTeam(v); }, KeyId = delegate(ulong v) { ushort id; TableEnumId((Team)v, out id); return id; }, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedRow)o).Teams.Slots[i]; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Teams.Slots[i] = unchecked((byte)r); } },
                new TableFieldInfo { Name = "counter", Json = "counter", TypeName = "int32", Id = 0x428f, Kind = 4, IsArray = false, Counted = false, Optional = true, ArrayBound = 0, ElemWidth = 4, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)(long)((PaddedRow)o).Counter; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedRow)o).Counter = unchecked((int)(long)r); }, GetPresent = delegate(object o) { return ((PaddedRow)o).CounterPresent; }, SetPresent = delegate(object o, bool p) { ((PaddedRow)o).CounterPresent = p; } },
            };
            info.Reset = delegate(object o) { TableReset((PaddedRow)o); };
            PaddedRowTableInfo = info;
            return info;
        }

        private static TableTypeInfo PaddedFrameTableInfo;
        public static TableTypeInfo PaddedFrameTableType()
        {
            TableTypeInfo info = PaddedFrameTableInfo;
            if (info != null) { return info; }
            info = new TableTypeInfo();
            info.Name = "PaddedFrame";
            info.NumFields = 4;
            info.Fields = new TableFieldInfo[]
            {
                new TableFieldInfo { Name = "marker", Json = "marker", TypeName = "uint8", Id = 0x866f, Kind = 6, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 1, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedFrame)o).Marker; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedFrame)o).Marker = unchecked((byte)r); } },
                new TableFieldInfo { Name = "stamp", Json = "stamp", TypeName = "uint64", Id = 0x0dc6, Kind = 9, IsArray = false, Counted = false, Optional = false, ArrayBound = 0, ElemWidth = 8, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetRaw = delegate(object o, int i) { return (ulong)((PaddedFrame)o).Stamp; }, SetRaw = delegate(object o, int i, ulong r) { ((PaddedFrame)o).Stamp = unchecked((ulong)r); } },
                new TableFieldInfo { Name = "rows", Json = "rows", TypeName = "PaddedRow", Id = 0x99af, Kind = 13, IsArray = true, Counted = true, Optional = false, ArrayBound = 64, ElemWidth = 0, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = delegate { return PaddedRowTableType(); }, Arms = null, GetChild = delegate(object o, int i) { return ((PaddedFrame)o).Rows[i]; }, GetCount = delegate(object o) { return ((PaddedFrame)o).RowsCount; }, SetCount = delegate(object o, int n) { ((PaddedFrame)o).RowsCount = n; } },
                new TableFieldInfo { Name = "blob", Json = "blob", TypeName = "bytes", Id = 0xd316, Kind = 6, IsArray = true, Counted = true, Optional = false, ArrayBound = 12, ElemWidth = 0, HasRange = false, RangeMin = 0.0, RangeMax = 0.0, EnumMax = -1, EnumName = null, VariantId = null, KeyTypeName = null, KeyName = null, KeyId = null, Guard = "", TableRef = null, Arms = null, GetBuffer = delegate(object o) { return ((PaddedFrame)o).Blob; }, GetCount = delegate(object o) { return ((PaddedFrame)o).BlobLength; }, SetCount = delegate(object o, int n) { ((PaddedFrame)o).BlobLength = n; } },
            };
            info.Reset = delegate(object o) { TableReset((PaddedFrame)o); };
            PaddedFrameTableInfo = info;
            return info;
        }

        // ---- the text form: JSON in and out of one table (docs/SPEC-TABLES.md §16) ----

        // PaddedRow in and out of a JSON text — one instance, one text, the generic
        // walk over this type's descriptors (docs/SPEC-TABLES.md §16).
        public static bool PaddedRowFromJson(PaddedRow value, ReadOnlySpan<byte> text, TableReport report)
        {
            return TableJson.Read(value, PaddedRowTableType(), text, report);
        }

        public static long PaddedRowToJsonMeasure(PaddedRow value)
        {
            return TableJson.Write(value, PaddedRowTableType(), Span<byte>.Empty, true);
        }

        public static long PaddedRowToJson(PaddedRow value, Span<byte> buffer)
        {
            return TableJson.Write(value, PaddedRowTableType(), buffer, false);
        }

        // PaddedFrame in and out of a JSON text — one instance, one text, the generic
        // walk over this type's descriptors (docs/SPEC-TABLES.md §16).
        public static bool PaddedFrameFromJson(PaddedFrame value, ReadOnlySpan<byte> text, TableReport report)
        {
            return TableJson.Read(value, PaddedFrameTableType(), text, report);
        }

        public static long PaddedFrameToJsonMeasure(PaddedFrame value)
        {
            return TableJson.Write(value, PaddedFrameTableType(), Span<byte>.Empty, true);
        }

        public static long PaddedFrameToJson(PaddedFrame value, Span<byte> buffer)
        {
            return TableJson.Write(value, PaddedFrameTableType(), buffer, false);
        }
    }

}
