// Generated by the schema compiler from Types.schema. DO NOT EDIT.
// package example — protocol id 0xaacab4aa04d2189b
// The TABLE wire (evolution-tolerant, notes/table-wire.md): no serialize
// dependency — includable from any TU.

#pragma once

#include <cstdint>
#include <cstring>
#include <new> // in-place prefill (placement new): no giant stack temporaries

#include "Types.h"
#include "EnumsTable.h"

#ifndef EXAMPLE_SCHEMA_TABLE_PRIMITIVES
#define EXAMPLE_SCHEMA_TABLE_PRIMITIVES

namespace example {

// The table-wire read report — the permissive contract's ledger. Silence
// (all zero) means the data matched this reader's schema exactly.
struct TableReport
{
    int32_t unknown = 0;       // unknown field ids skipped (newer data)
    int32_t kind_mismatch = 0; // known id, changed type — skipped, never misdecoded
    int32_t clamped = 0;       // out-of-range values clamped to declared bounds
    bool malformed = false;    // framing damage; decode stopped, partial result kept
};

struct TableWriter
{
    uint8_t * buffer;
    int64_t capacity;
    int64_t offset = 0;
    bool overflow = false;

    TableWriter( uint8_t * buffer, int64_t capacity ) : buffer( buffer ), capacity( capacity ) {}

    void raw( const void * data, int64_t bytes )
    {
        if ( offset + bytes > capacity ) { overflow = true; return; }
        memcpy( buffer + offset, data, (size_t) bytes );
        offset += bytes;
    }
    void put8( uint8_t v )   { raw( &v, 1 ); }
    void put16( uint16_t v ) { uint8_t b[2] = { uint8_t( v ), uint8_t( v >> 8 ) }; raw( b, 2 ); }
    void put32( uint32_t v ) { uint8_t b[4] = { uint8_t( v ), uint8_t( v >> 8 ), uint8_t( v >> 16 ), uint8_t( v >> 24 ) }; raw( b, 4 ); }
    void put64( uint64_t v ) { put32( uint32_t( v ) ); put32( uint32_t( v >> 32 ) ); }
    void patch32( int64_t at, uint32_t v )
    {
        if ( at + 4 > capacity ) { overflow = true; return; }
        buffer[at] = uint8_t( v ); buffer[at+1] = uint8_t( v >> 8 );
        buffer[at+2] = uint8_t( v >> 16 ); buffer[at+3] = uint8_t( v >> 24 );
    }
};

struct TableReader
{
    const uint8_t * buffer;
    int64_t size;
    int64_t offset = 0;
    TableReport * report;

    TableReader( const uint8_t * buffer, int64_t size, TableReport * report )
        : buffer( buffer ), size( size ), report( report ) {}

    bool has( int64_t bytes ) const { return offset + bytes <= size; }
    uint8_t get8()   { return buffer[offset++]; }
    uint16_t get16() { uint16_t v = uint16_t( buffer[offset] ) | uint16_t( buffer[offset+1] ) << 8; offset += 2; return v; }
    uint32_t get32() { uint32_t v = uint32_t( buffer[offset] ) | uint32_t( buffer[offset+1] ) << 8 | uint32_t( buffer[offset+2] ) << 16 | uint32_t( buffer[offset+3] ) << 24; offset += 4; return v; }
    uint64_t get64() { uint64_t lo = get32(); uint64_t hi = get32(); return lo | ( hi << 32 ); }

    // skip one payload by kind; false = framing damage
    bool skip( uint8_t kind )
    {
        switch ( kind )
        {
            case 1: case 2: case 6: return has( 1 ) ? ( offset += 1, true ) : false;
            case 3: case 7:         return has( 2 ) ? ( offset += 2, true ) : false;
            case 4: case 8: case 10: return has( 4 ) ? ( offset += 4, true ) : false;
            case 5: case 9: case 11: return has( 8 ) ? ( offset += 8, true ) : false;
            case 12:
            {
                if ( !has( 2 ) ) return false;
                uint16_t n = get16();
                return has( n ) ? ( offset += n, true ) : false;
            }
            case 13: case 14:
            {
                if ( !has( 4 ) ) return false;
                uint32_t n = get32();
                return has( n ) ? ( offset += n, true ) : false;
            }
        }
        return false;
    }
};

inline float table_bits_to_float( uint32_t bits ) { float f; memcpy( &f, &bits, 4 ); return f; }
inline uint32_t table_float_to_bits( float f ) { uint32_t b; memcpy( &b, &f, 4 ); return b; }
inline double table_bits_to_double( uint64_t bits ) { double d; memcpy( &d, &bits, 8 ); return d; }
inline uint64_t table_double_to_bits( double d ) { uint64_t b; memcpy( &b, &d, 8 ); return b; }

} // namespace example

#endif // EXAMPLE_SCHEMA_TABLE_PRIMITIVES

namespace example {

inline bool TableWriteVec3( TableWriter & w, const Vec3 & value )
{
    if ( value.x != 0.0 )
    {
        w.put16( 0xad8b ); w.put8( 11 ); // x
        w.put64( table_double_to_bits( value.x ) );
    }
    if ( value.y != 0.0 )
    {
        w.put16( 0xb2f8 ); w.put8( 11 ); // y
        w.put64( table_double_to_bits( value.y ) );
    }
    if ( value.z != 0.0 )
    {
        w.put16( 0xaca1 ); w.put8( 11 ); // z
        w.put64( table_double_to_bits( value.z ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadVec3( TableReader & r, Vec3 & value )
{
    new ( &value ) Vec3{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xad8b: // x
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.x = table_bits_to_double( r.get64() );
                break;
            }
            case 0xb2f8: // y
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.y = table_bits_to_double( r.get64() );
                break;
            }
            case 0xaca1: // z
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.z = table_bits_to_double( r.get64() );
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadVec3( const uint8_t * buffer, int64_t bytes, Vec3 & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadVec3( r, value );
}

inline bool TableWriteQuat( TableWriter & w, const Quat & value )
{
    if ( value.x != 0.0 )
    {
        w.put16( 0xad8b ); w.put8( 11 ); // x
        w.put64( table_double_to_bits( value.x ) );
    }
    if ( value.y != 0.0 )
    {
        w.put16( 0xb2f8 ); w.put8( 11 ); // y
        w.put64( table_double_to_bits( value.y ) );
    }
    if ( value.z != 0.0 )
    {
        w.put16( 0xaca1 ); w.put8( 11 ); // z
        w.put64( table_double_to_bits( value.z ) );
    }
    if ( value.w != 0.0 )
    {
        w.put16( 0xcd3a ); w.put8( 11 ); // w
        w.put64( table_double_to_bits( value.w ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadQuat( TableReader & r, Quat & value )
{
    new ( &value ) Quat{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xad8b: // x
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.x = table_bits_to_double( r.get64() );
                break;
            }
            case 0xb2f8: // y
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.y = table_bits_to_double( r.get64() );
                break;
            }
            case 0xaca1: // z
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.z = table_bits_to_double( r.get64() );
                break;
            }
            case 0xcd3a: // w
            {
                if ( kind != 11 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                value.w = table_bits_to_double( r.get64() );
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadQuat( const uint8_t * buffer, int64_t bytes, Quat & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadQuat( r, value );
}

inline bool TableWriteHandle( TableWriter & w, const Handle & value )
{
    if ( value.object_id != 0 )
    {
        w.put16( 0xdc71 ); w.put8( 4 ); // object_id
        w.put32( uint32_t( value.object_id ) );
    }
    if ( value.object_sequence != 0 )
    {
        w.put16( 0x57ee ); w.put8( 6 ); // object_sequence
        w.put8( uint8_t( value.object_sequence ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadHandle( TableReader & r, Handle & value )
{
    new ( &value ) Handle{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xdc71: // object_id
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 9999 ) { decoded = 9999; r.report->clamped++; }
                value.object_id = decoded;
                break;
            }
            case 0x57ee: // object_sequence
            {
                if ( kind != 6 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                uint8_t decoded = uint8_t( r.get8( ) );
                value.object_sequence = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadHandle( const uint8_t * buffer, int64_t bytes, Handle & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadHandle( r, value );
}

inline bool TableWriteQuantizedPosition( TableWriter & w, const QuantizedPosition & value )
{
    if ( value.x != 0 )
    {
        w.put16( 0xad8b ); w.put8( 4 ); // x
        w.put32( uint32_t( value.x ) );
    }
    if ( value.y != 0 )
    {
        w.put16( 0xb2f8 ); w.put8( 4 ); // y
        w.put32( uint32_t( value.y ) );
    }
    if ( value.z != 0 )
    {
        w.put16( 0xaca1 ); w.put8( 4 ); // z
        w.put32( uint32_t( value.z ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadQuantizedPosition( TableReader & r, QuantizedPosition & value )
{
    new ( &value ) QuantizedPosition{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xad8b: // x
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -8388608 ) { decoded = -8388608; r.report->clamped++; }
                else if ( decoded > 8388608 ) { decoded = 8388608; r.report->clamped++; }
                value.x = decoded;
                break;
            }
            case 0xb2f8: // y
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -8388608 ) { decoded = -8388608; r.report->clamped++; }
                else if ( decoded > 8388608 ) { decoded = 8388608; r.report->clamped++; }
                value.y = decoded;
                break;
            }
            case 0xaca1: // z
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -8388608 ) { decoded = -8388608; r.report->clamped++; }
                else if ( decoded > 8388608 ) { decoded = 8388608; r.report->clamped++; }
                value.z = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadQuantizedPosition( const uint8_t * buffer, int64_t bytes, QuantizedPosition & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadQuantizedPosition( r, value );
}

inline bool TableWriteQuantizedVelocity( TableWriter & w, const QuantizedVelocity & value )
{
    if ( value.x != 0 )
    {
        w.put16( 0xad8b ); w.put8( 4 ); // x
        w.put32( uint32_t( value.x ) );
    }
    if ( value.y != 0 )
    {
        w.put16( 0xb2f8 ); w.put8( 4 ); // y
        w.put32( uint32_t( value.y ) );
    }
    if ( value.z != 0 )
    {
        w.put16( 0xaca1 ); w.put8( 4 ); // z
        w.put32( uint32_t( value.z ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadQuantizedVelocity( TableReader & r, QuantizedVelocity & value )
{
    new ( &value ) QuantizedVelocity{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xad8b: // x
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -2097152 ) { decoded = -2097152; r.report->clamped++; }
                else if ( decoded > 2097152 ) { decoded = 2097152; r.report->clamped++; }
                value.x = decoded;
                break;
            }
            case 0xb2f8: // y
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -2097152 ) { decoded = -2097152; r.report->clamped++; }
                else if ( decoded > 2097152 ) { decoded = 2097152; r.report->clamped++; }
                value.y = decoded;
                break;
            }
            case 0xaca1: // z
            {
                if ( kind != 4 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                int32_t decoded = int32_t( r.get32( ) );
                if ( decoded < -2097152 ) { decoded = -2097152; r.report->clamped++; }
                else if ( decoded > 2097152 ) { decoded = 2097152; r.report->clamped++; }
                value.z = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadQuantizedVelocity( const uint8_t * buffer, int64_t bytes, QuantizedVelocity & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadQuantizedVelocity( r, value );
}

inline bool TableWriteQuantizedRotation( TableWriter & w, const QuantizedRotation & value )
{
    if ( value.x != 0 )
    {
        w.put16( 0xad8b ); w.put8( 3 ); // x
        w.put16( uint16_t( value.x ) );
    }
    if ( value.y != 0 )
    {
        w.put16( 0xb2f8 ); w.put8( 3 ); // y
        w.put16( uint16_t( value.y ) );
    }
    if ( value.z != 0 )
    {
        w.put16( 0xaca1 ); w.put8( 3 ); // z
        w.put16( uint16_t( value.z ) );
    }
    if ( value.w != 0 )
    {
        w.put16( 0xcd3a ); w.put8( 3 ); // w
        w.put16( uint16_t( value.w ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadQuantizedRotation( TableReader & r, QuantizedRotation & value )
{
    new ( &value ) QuantizedRotation{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xad8b: // x
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < -1024 ) { decoded = -1024; r.report->clamped++; }
                else if ( decoded > 1024 ) { decoded = 1024; r.report->clamped++; }
                value.x = decoded;
                break;
            }
            case 0xb2f8: // y
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < -1024 ) { decoded = -1024; r.report->clamped++; }
                else if ( decoded > 1024 ) { decoded = 1024; r.report->clamped++; }
                value.y = decoded;
                break;
            }
            case 0xaca1: // z
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < -1024 ) { decoded = -1024; r.report->clamped++; }
                else if ( decoded > 1024 ) { decoded = 1024; r.report->clamped++; }
                value.z = decoded;
                break;
            }
            case 0xcd3a: // w
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < -1024 ) { decoded = -1024; r.report->clamped++; }
                else if ( decoded > 1024 ) { decoded = 1024; r.report->clamped++; }
                value.w = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadQuantizedRotation( const uint8_t * buffer, int64_t bytes, QuantizedRotation & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadQuantizedRotation( r, value );
}

inline bool TableWriteRigidBody( TableWriter & w, const RigidBody & value )
{
    {
        int64_t field_at_position = w.offset;
        w.put16( 0xdd45 ); w.put8( 13 ); // position
        int64_t len_at_position = w.offset; w.put32( 0 );
        if ( !TableWriteVec3( w, value.position ) ) return false;
        int64_t body_position = w.offset - len_at_position - 4;
        if ( body_position <= 2 ) { w.offset = field_at_position; } // all-default nested elides
        else { w.patch32( len_at_position, uint32_t( body_position ) ); }
    }
    {
        int64_t field_at_orientation = w.offset;
        w.put16( 0x7964 ); w.put8( 13 ); // orientation
        int64_t len_at_orientation = w.offset; w.put32( 0 );
        if ( !TableWriteQuat( w, value.orientation ) ) return false;
        int64_t body_orientation = w.offset - len_at_orientation - 4;
        if ( body_orientation <= 2 ) { w.offset = field_at_orientation; } // all-default nested elides
        else { w.patch32( len_at_orientation, uint32_t( body_orientation ) ); }
    }
    if ( value.at_rest != false )
    {
        w.put16( 0xf9eb ); w.put8( 1 ); // at_rest
        w.put8( value.at_rest ? 1 : 0 );
    }
    if ( !value.at_rest )
    {
        {
            int64_t field_at_linear_velocity = w.offset;
            w.put16( 0x0637 ); w.put8( 13 ); // linear_velocity
            int64_t len_at_linear_velocity = w.offset; w.put32( 0 );
            if ( !TableWriteVec3( w, value.linear_velocity ) ) return false;
            int64_t body_linear_velocity = w.offset - len_at_linear_velocity - 4;
            if ( body_linear_velocity <= 2 ) { w.offset = field_at_linear_velocity; } // all-default nested elides
            else { w.patch32( len_at_linear_velocity, uint32_t( body_linear_velocity ) ); }
        }
    }
    if ( !value.at_rest )
    {
        {
            int64_t field_at_angular_velocity = w.offset;
            w.put16( 0x01a7 ); w.put8( 13 ); // angular_velocity
            int64_t len_at_angular_velocity = w.offset; w.put32( 0 );
            if ( !TableWriteVec3( w, value.angular_velocity ) ) return false;
            int64_t body_angular_velocity = w.offset - len_at_angular_velocity - 4;
            if ( body_angular_velocity <= 2 ) { w.offset = field_at_angular_velocity; } // all-default nested elides
            else { w.patch32( len_at_angular_velocity, uint32_t( body_angular_velocity ) ); }
        }
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadRigidBody( TableReader & r, RigidBody & value )
{
    new ( &value ) RigidBody{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0xdd45: // position
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadVec3( sub, value.position );
                }
                r.offset += body_len;
                break;
            }
            case 0x7964: // orientation
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadQuat( sub, value.orientation );
                }
                r.offset += body_len;
                break;
            }
            case 0xf9eb: // at_rest
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.at_rest = r.get8() != 0;
                break;
            }
            case 0x0637: // linear_velocity
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadVec3( sub, value.linear_velocity );
                }
                r.offset += body_len;
                break;
            }
            case 0x01a7: // angular_velocity
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadVec3( sub, value.angular_velocity );
                }
                r.offset += body_len;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadRigidBody( const uint8_t * buffer, int64_t bytes, RigidBody & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadRigidBody( r, value );
}

inline bool TableWriteInput( TableWriter & w, const Input & value )
{
    if ( value.stick_x != 0.0f )
    {
        w.put16( 0x5c38 ); w.put8( 10 ); // stick_x
        w.put32( table_float_to_bits( value.stick_x ) );
    }
    if ( value.stick_y != 0.0f )
    {
        w.put16( 0x5c8b ); w.put8( 10 ); // stick_y
        w.put32( table_float_to_bits( value.stick_y ) );
    }
    if ( value.throttle != 0.0f )
    {
        w.put16( 0x0552 ); w.put8( 10 ); // throttle
        w.put32( table_float_to_bits( value.throttle ) );
    }
    if ( value.yaw != 0.0f )
    {
        w.put16( 0x80c1 ); w.put8( 10 ); // yaw
        w.put32( table_float_to_bits( value.yaw ) );
    }
    if ( value.pitch != 0.0f )
    {
        w.put16( 0xf783 ); w.put8( 10 ); // pitch
        w.put32( table_float_to_bits( value.pitch ) );
    }
    if ( value.fire != false )
    {
        w.put16( 0x9872 ); w.put8( 1 ); // fire
        w.put8( value.fire ? 1 : 0 );
    }
    if ( value.alt_fire != false )
    {
        w.put16( 0x048a ); w.put8( 1 ); // alt_fire
        w.put8( value.alt_fire ? 1 : 0 );
    }
    if ( value.boost != false )
    {
        w.put16( 0xdcc5 ); w.put8( 1 ); // boost
        w.put8( value.boost ? 1 : 0 );
    }
    if ( value.brake != false )
    {
        w.put16( 0xa81d ); w.put8( 1 ); // brake
        w.put8( value.brake ? 1 : 0 );
    }
    if ( value.aim != false )
    {
        w.put16( 0x69c4 ); w.put8( 1 ); // aim
        w.put8( value.aim ? 1 : 0 );
    }
    if ( value.lock_on != false )
    {
        w.put16( 0x21cc ); w.put8( 1 ); // lock_on
        w.put8( value.lock_on ? 1 : 0 );
    }
    if ( value.zoom != false )
    {
        w.put16( 0x3da0 ); w.put8( 1 ); // zoom
        w.put8( value.zoom ? 1 : 0 );
    }
    if ( value.ping != false )
    {
        w.put16( 0xe6d4 ); w.put8( 1 ); // ping
        w.put8( value.ping ? 1 : 0 );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadInput( TableReader & r, Input & value )
{
    new ( &value ) Input{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x5c38: // stick_x
            {
                if ( kind != 10 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                value.stick_x = table_bits_to_float( r.get32() );
                break;
            }
            case 0x5c8b: // stick_y
            {
                if ( kind != 10 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                value.stick_y = table_bits_to_float( r.get32() );
                break;
            }
            case 0x0552: // throttle
            {
                if ( kind != 10 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                value.throttle = table_bits_to_float( r.get32() );
                break;
            }
            case 0x80c1: // yaw
            {
                if ( kind != 10 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                value.yaw = table_bits_to_float( r.get32() );
                break;
            }
            case 0xf783: // pitch
            {
                if ( kind != 10 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                value.pitch = table_bits_to_float( r.get32() );
                break;
            }
            case 0x9872: // fire
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.fire = r.get8() != 0;
                break;
            }
            case 0x048a: // alt_fire
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.alt_fire = r.get8() != 0;
                break;
            }
            case 0xdcc5: // boost
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.boost = r.get8() != 0;
                break;
            }
            case 0xa81d: // brake
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.brake = r.get8() != 0;
                break;
            }
            case 0x69c4: // aim
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.aim = r.get8() != 0;
                break;
            }
            case 0x21cc: // lock_on
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.lock_on = r.get8() != 0;
                break;
            }
            case 0x3da0: // zoom
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.zoom = r.get8() != 0;
                break;
            }
            case 0xe6d4: // ping
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.ping = r.get8() != 0;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadInput( const uint8_t * buffer, int64_t bytes, Input & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadInput( r, value );
}

inline bool TableWriteInputPacket( TableWriter & w, const InputPacket & value )
{
    if ( value.synchronize_sequence != 0 )
    {
        w.put16( 0x44ed ); w.put8( 7 ); // synchronize_sequence
        w.put16( uint16_t( value.synchronize_sequence ) );
    }
    if ( value.current_frame != 0 )
    {
        w.put16( 0xc9ce ); w.put8( 9 ); // current_frame
        w.put64( uint64_t( value.current_frame ) );
    }
    if ( value.start_frame != 0 )
    {
        w.put16( 0x3d37 ); w.put8( 9 ); // start_frame
        w.put64( uint64_t( value.start_frame ) );
    }
    if ( value.inputs_count > 0 )
    {
        w.put16( 0x9e2e ); w.put8( 14 ); // inputs
        int64_t len_at_inputs = w.offset; w.put32( 0 );
        w.put8( 13 ); w.put16( uint16_t( value.inputs_count ) );
        for ( int32_t i = 0; i < value.inputs_count; i++ )
        {
            {
                int64_t elem_len_at = w.offset; w.put32( 0 );
                if ( !TableWriteInput( w, value.inputs[i] ) ) return false;
                w.patch32( elem_len_at, uint32_t( w.offset - elem_len_at - 4 ) );
            }
        }
        w.patch32( len_at_inputs, uint32_t( w.offset - len_at_inputs - 4 ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadInputPacket( TableReader & r, InputPacket & value )
{
    new ( &value ) InputPacket{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x44ed: // synchronize_sequence
            {
                if ( kind != 7 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                uint16_t decoded = uint16_t( r.get16( ) );
                value.synchronize_sequence = decoded;
                break;
            }
            case 0xc9ce: // current_frame
            {
                if ( kind != 9 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                uint64_t decoded = uint64_t( r.get64( ) );
                value.current_frame = decoded;
                break;
            }
            case 0x3d37: // start_frame
            {
                if ( kind != 9 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                uint64_t decoded = uint64_t( r.get64( ) );
                value.start_frame = decoded;
                break;
            }
            case 0x9e2e: // inputs
            {
                if ( kind != 14 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                int64_t body_end = r.offset + body_len;
                if ( body_len >= 3 )
                {
                    uint8_t elem_kind = r.get8();
                    uint16_t count = r.get16();
                    if ( elem_kind != 13 ) { r.report->kind_mismatch++; r.offset = body_end; break; }
                    uint16_t keep = count;
                    if ( keep > 16 ) { keep = 16; r.report->clamped++; }
                    for ( uint16_t i = 0; i < keep; i++ )
                    {
                        if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                        uint32_t elem_len = r.get32();
                        if ( !r.has( elem_len ) ) { r.report->malformed = true; return false; }
                        {
                            TableReader sub( r.buffer + r.offset, elem_len, r.report );
                            TableReadInput( sub, value.inputs[i] );
                        }
                        r.offset += elem_len;
                    }
                    value.inputs_count = keep;
                }
                r.offset = body_end; // excess elements and slack skip via the length
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadInputPacket( const uint8_t * buffer, int64_t bytes, InputPacket & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadInputPacket( r, value );
}

inline bool TableWriteShipCreate( TableWriter & w, const ShipCreate & value )
{
    if ( value.ship_type != ShipType::None )
    {
        w.put16( 0x38e9 ); w.put8( 6 ); // ship_type
        w.put8( uint8_t( value.ship_type ) );
    }
    {
        int64_t field_at_position = w.offset;
        w.put16( 0xdd45 ); w.put8( 13 ); // position
        int64_t len_at_position = w.offset; w.put32( 0 );
        if ( !TableWriteQuantizedPosition( w, value.position ) ) return false;
        int64_t body_position = w.offset - len_at_position - 4;
        if ( body_position <= 2 ) { w.offset = field_at_position; } // all-default nested elides
        else { w.patch32( len_at_position, uint32_t( body_position ) ); }
    }
    {
        int64_t field_at_rotation = w.offset;
        w.put16( 0x60f3 ); w.put8( 13 ); // rotation
        int64_t len_at_rotation = w.offset; w.put32( 0 );
        if ( !TableWriteQuantizedRotation( w, value.rotation ) ) return false;
        int64_t body_rotation = w.offset - len_at_rotation - 4;
        if ( body_rotation <= 2 ) { w.offset = field_at_rotation; } // all-default nested elides
        else { w.patch32( len_at_rotation, uint32_t( body_rotation ) ); }
    }
    {
        int64_t field_at_linear_velocity = w.offset;
        w.put16( 0x0637 ); w.put8( 13 ); // linear_velocity
        int64_t len_at_linear_velocity = w.offset; w.put32( 0 );
        if ( !TableWriteQuantizedVelocity( w, value.linear_velocity ) ) return false;
        int64_t body_linear_velocity = w.offset - len_at_linear_velocity - 4;
        if ( body_linear_velocity <= 2 ) { w.offset = field_at_linear_velocity; } // all-default nested elides
        else { w.patch32( len_at_linear_velocity, uint32_t( body_linear_velocity ) ); }
    }
    if ( value.has_flags != false )
    {
        w.put16( 0xaa8d ); w.put8( 1 ); // has_flags
        w.put8( value.has_flags ? 1 : 0 );
    }
    if ( value.has_flags )
    {
        if ( value.flags != 0 )
        {
            w.put16( 0xe64b ); w.put8( 9 ); // flags
            w.put64( uint64_t( value.flags ) );
        }
    }
    if ( value.team != Team::None )
    {
        w.put16( 0xdff1 ); w.put8( 6 ); // team
        w.put8( uint8_t( value.team ) );
    }
    if ( value.health != 0 )
    {
        w.put16( 0x8617 ); w.put8( 3 ); // health
        w.put16( uint16_t( value.health ) );
    }
    if ( value.thrust != 0 )
    {
        w.put16( 0x9e51 ); w.put8( 2 ); // thrust
        w.put8( uint8_t( value.thrust ) );
    }
    w.put16( 0 ); // terminator
    return !w.overflow;
}

inline bool TableReadShipCreate( TableReader & r, ShipCreate & value )
{
    new ( &value ) ShipCreate{}; // prefill declared defaults in place, then overlay
    for ( ;; )
    {
        if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
        uint16_t field_id = r.get16();
        if ( field_id == 0 ) return true;
        if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
        uint8_t kind = r.get8();
        switch ( field_id )
        {
            case 0x38e9: // ship_type
            {
                if ( kind != 6 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                uint8_t raw = r.get8( );
                if ( raw > 5 ) { raw = 0; r.report->clamped++; } // out-of-set -> None
                value.ship_type = ShipType( raw );
                break;
            }
            case 0xdd45: // position
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadQuantizedPosition( sub, value.position );
                }
                r.offset += body_len;
                break;
            }
            case 0x60f3: // rotation
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadQuantizedRotation( sub, value.rotation );
                }
                r.offset += body_len;
                break;
            }
            case 0x0637: // linear_velocity
            {
                if ( kind != 13 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 4 ) ) { r.report->malformed = true; return false; }
                uint32_t body_len = r.get32();
                if ( !r.has( body_len ) ) { r.report->malformed = true; return false; }
                {
                    TableReader sub( r.buffer + r.offset, body_len, r.report );
                    TableReadQuantizedVelocity( sub, value.linear_velocity );
                }
                r.offset += body_len;
                break;
            }
            case 0xaa8d: // has_flags
            {
                if ( kind != 1 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                value.has_flags = r.get8() != 0;
                break;
            }
            case 0xe64b: // flags
            {
                if ( kind != 9 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 8 ) ) { r.report->malformed = true; return false; }
                uint64_t decoded = uint64_t( r.get64( ) );
                value.flags = decoded;
                break;
            }
            case 0xdff1: // team
            {
                if ( kind != 6 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                uint8_t raw = r.get8( );
                if ( raw > 2 ) { raw = 0; r.report->clamped++; } // out-of-set -> None
                value.team = Team( raw );
                break;
            }
            case 0x8617: // health
            {
                if ( kind != 3 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 2 ) ) { r.report->malformed = true; return false; }
                int16_t decoded = int16_t( r.get16( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 1000 ) { decoded = 1000; r.report->clamped++; }
                value.health = decoded;
                break;
            }
            case 0x9e51: // thrust
            {
                if ( kind != 2 )
                {
                    r.report->kind_mismatch++;
                    if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                    break;
                }
                if ( !r.has( 1 ) ) { r.report->malformed = true; return false; }
                int8_t decoded = int8_t( r.get8( ) );
                if ( decoded < 0 ) { decoded = 0; r.report->clamped++; }
                else if ( decoded > 100 ) { decoded = 100; r.report->clamped++; }
                value.thrust = decoded;
                break;
            }
            default:
            {
                r.report->unknown++;
                if ( !r.skip( kind ) ) { r.report->malformed = true; return false; }
                break;
            }
        }
    }
}

inline bool TableReadShipCreate( const uint8_t * buffer, int64_t bytes, ShipCreate & value, TableReport & report )
{
    TableReader r( buffer, bytes, &report );
    return TableReadShipCreate( r, value );
}

} // namespace example
