// Code generated by the schema compiler from Ludicrous.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package ludicrous — protocol id 0x9660fa8c14d38d67

use serialize::{ReadStream, Stream, WriteStream};

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
pub const PROTOCOL_ID: u64 = 0x9660fa8c14d38d67;

/// The generated crate's error: the runtime's own errors pass through;
/// Validation is a read rejecting the wire (SPEC §4.3, §4.7).
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub enum Error {
    Stream(serialize::Error),
    Validation,
}

impl From<serialize::Error> for Error {
    fn from(e: serialize::Error) -> Error {
        Error::Stream(e)
    }
}

// serialize.rs 2.0.0 writes are infallible: their Result carries an
// uninhabited error, and `?` on one needs this vacuous conversion.
impl From<core::convert::Infallible> for Error {
    fn from(e: core::convert::Infallible) -> Error {
        match e {}
    }
}

pub type Result<T = ()> = core::result::Result<T, Error>;

pub const MAX_WORLD_UNITS: i64 = 30000;

// DriveMode — None = 0 implicit, variants dense from 1, wire range [0, 3] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct DriveMode(pub u8);

impl DriveMode {
    pub const NONE: DriveMode = DriveMode(0);
    pub const CRUISE: DriveMode = DriveMode(1);
    pub const WARP: DriveMode = DriveMode(2);
    pub const LUDICROUS: DriveMode = DriveMode(3);
    pub const COUNT: DriveMode = DriveMode(3); // the declared variant count (SPEC §4.2)
    pub const MAX: DriveMode = DriveMode(3); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `DriveMode` value, out-of-set included.
pub fn enum_name_drive_mode(value: DriveMode) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Cruise",
        2 => "Warp",
        3 => "Ludicrous",
        _ => "???",
    }
}

// type FixedProbe
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct FixedProbe {
    pub angle: i32, // wire [-180, 180]
    pub position: i64, // wire [-30000, 30000]
    pub reach: i128, // wire [-1000000, 1000000]
    pub ticks: i32, // wire [0, 1000000]
    pub samples: [i32; 2], // wire [-8, 8]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for FixedProbe {
    fn default() -> Self {
        FixedProbe {
            angle: 0,
            position: 0,
            reach: 0,
            ticks: 0,
            samples: [0; 2],
        }
    }
}

// FIXED_PROBE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// FIXED_PROBE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const FIXED_PROBE_MAX_BITS: u64 = 156;
pub const FIXED_PROBE_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_fixed_probe(stream: &mut WriteStream<'_>, value: &FixedProbe) -> Result {
    if value.angle < -11796480 || value.angle > 11796480 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.angle;
        stream.serialize_fixed(&mut fixed_value, 16, 16, -180, 180)?;
    }
    if value.position < -1966080000 || value.position > 1966080000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.position;
        stream.serialize_fixed(&mut fixed_value, 48, 16, -MAX_WORLD_UNITS, MAX_WORLD_UNITS)?;
    }
    if value.reach < -65536000000 || value.reach > 65536000000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.reach;
        stream.serialize_fixed(&mut fixed_value, 112, 16, -1000000, 1000000)?;
    }
    if value.ticks < 0 || value.ticks > 1000000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.ticks;
        stream.serialize_fixed(&mut fixed_value, 32, 0, 0, 1000000)?;
    }
    for i in 0..2 {
        if value.samples[i] < -524288 || value.samples[i] > 524288 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut fixed_value = value.samples[i];
            stream.serialize_fixed(&mut fixed_value, 16, 16, -8, 8)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_fixed_probe(stream: &mut ReadStream<'_>, value: &mut FixedProbe) -> Result {
    stream.serialize_fixed(&mut value.angle, 16, 16, -180, 180)?;
    stream.serialize_fixed(&mut value.position, 48, 16, -MAX_WORLD_UNITS, MAX_WORLD_UNITS)?;
    stream.serialize_fixed(&mut value.reach, 112, 16, -1000000, 1000000)?;
    stream.serialize_fixed(&mut value.ticks, 32, 0, 0, 1000000)?;
    for i in 0..2 {
        stream.serialize_fixed(&mut value.samples[i], 16, 16, -8, 8)?;
    }
    Ok(())
}

// type UnsignedProbe
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct UnsignedProbe {
    pub angle: u32, // wire [0, 360]
    pub span: u64, // wire [0, 281474976710655]
    pub reach: u128, // wire [0, 2000000]
    pub ticks: u32, // wire [0, 1000000]
    pub samples: [u32; 2], // wire [0, 16]
    pub locked: u32, // = 196608 in new() (zero value otherwise); wire [3, 3]
    pub tail: u8,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for UnsignedProbe {
    fn default() -> Self {
        UnsignedProbe {
            angle: 0,
            span: 0,
            reach: 0,
            ticks: 0,
            samples: [0; 2],
            locked: 0,
            tail: 0,
        }
    }
}

impl UnsignedProbe {
    // new returns a UnsignedProbe with its specified defaults applied; the plain
    // Default::default() is the all-zero form (SPEC §4.2: zero initialization
    // unless a specified default overrides it).
    pub fn new() -> Self {
        let mut value = UnsignedProbe::default();
        value.locked = 196608;
        value
    }
}

// UNSIGNED_PROBE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// UNSIGNED_PROBE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const UNSIGNED_PROBE_MAX_BITS: u64 = 196;
pub const UNSIGNED_PROBE_MAX_BYTES: usize = 32;

#[inline(always)]
pub fn write_unsigned_probe(stream: &mut WriteStream<'_>, value: &UnsignedProbe) -> Result {
    if value.angle > 23592960 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.angle;
        stream.serialize_fixed(&mut fixed_value, 16, 16, 0, 360)?;
    }
    if value.span > 18446744073709486080 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.span;
        stream.serialize_fixed(&mut fixed_value, 48, 16, 0, 281474976710655)?;
    }
    if value.reach > 131072000000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.reach;
        stream.serialize_fixed(&mut fixed_value, 112, 16, 0, 2000000)?;
    }
    if value.ticks > 1000000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.ticks;
        stream.serialize_fixed(&mut fixed_value, 32, 0, 0, 1000000)?;
    }
    for i in 0..2 {
        if value.samples[i] > 1048576 {
            return Err(Error::Stream(serialize::Error::ValueOutOfRange));
        }
        {
            let mut fixed_value = value.samples[i];
            stream.serialize_fixed(&mut fixed_value, 16, 16, 0, 16)?;
        }
    }
    if value.locked < 196608 || value.locked > 196608 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.tail as u32;
        stream.serialize_bits(&mut raw_value, 8)?;
    }
    Ok(())
}

#[inline]
pub fn read_unsigned_probe(stream: &mut ReadStream<'_>, value: &mut UnsignedProbe) -> Result {
    stream.serialize_fixed(&mut value.angle, 16, 16, 0, 360)?;
    stream.serialize_fixed(&mut value.span, 48, 16, 0, 281474976710655)?;
    stream.serialize_fixed(&mut value.reach, 112, 16, 0, 2000000)?;
    stream.serialize_fixed(&mut value.ticks, 32, 0, 0, 1000000)?;
    for i in 0..2 {
        stream.serialize_fixed(&mut value.samples[i], 16, 16, 0, 16)?;
    }
    value.locked = 196608_u32;
    {
        let mut raw_value: u32 = 0;
        stream.serialize_bits(&mut raw_value, 8)?;
        value.tail = raw_value as u8;
    }
    Ok(())
}

// type WideProbe
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct WideProbe {
    pub entity_id: u128,
    pub energy: i128, // wire [-5000000000, 5000000000]
    pub flux: i128, // wire [-1267650600228229401496703205376, 1267650600228229401496703205376]
    pub bias: i128, // = -250 in new() (zero value otherwise); wire [-1000, 1000]
    pub seed: u128, // = 36893488147419103232 in new() (zero value otherwise)
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for WideProbe {
    fn default() -> Self {
        WideProbe {
            entity_id: 0,
            energy: 0,
            flux: 0,
            bias: 0,
            seed: 0,
        }
    }
}

impl WideProbe {
    // new returns a WideProbe with its specified defaults applied; the plain
    // Default::default() is the all-zero form (SPEC §4.2: zero initialization
    // unless a specified default overrides it).
    pub fn new() -> Self {
        let mut value = WideProbe::default();
        value.bias = -250;
        value.seed = 36893488147419103232;
        value
    }
}

// WIDE_PROBE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// WIDE_PROBE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const WIDE_PROBE_MAX_BITS: u64 = 403;
pub const WIDE_PROBE_MAX_BYTES: usize = 56;

#[inline(always)]
pub fn write_wide_probe(stream: &mut WriteStream<'_>, value: &WideProbe) -> Result {
    {
        let mut raw_value = value.entity_id;
        stream.serialize_u128(&mut raw_value)?;
    }
    if value.energy < -5000000000 || value.energy > 5000000000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut range_value = value.energy;
        stream.serialize_int128(&mut range_value, -5000000000, 5000000000)?;
    }
    if value.flux < -1267650600228229401496703205376 || value.flux > 1267650600228229401496703205376 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut range_value = value.flux;
        stream.serialize_int128(&mut range_value, -1267650600228229401496703205376, 1267650600228229401496703205376)?;
    }
    if value.bias < -1000 || value.bias > 1000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut range_value = value.bias;
        stream.serialize_int128(&mut range_value, -1000, 1000)?;
    }
    {
        let mut raw_value = value.seed;
        stream.serialize_u128(&mut raw_value)?;
    }
    Ok(())
}

#[inline]
pub fn read_wide_probe(stream: &mut ReadStream<'_>, value: &mut WideProbe) -> Result {
    stream.serialize_u128(&mut value.entity_id)?;
    stream.serialize_int128(&mut value.energy, -5000000000, 5000000000)?;
    stream.serialize_int128(&mut value.flux, -1267650600228229401496703205376, 1267650600228229401496703205376)?;
    stream.serialize_int128(&mut value.bias, -1000, 1000)?;
    stream.serialize_u128(&mut value.seed)?;
    Ok(())
}

// type LudicrousState
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct LudicrousState {
    pub mode: DriveMode,
    pub probe: FixedProbe,
    pub wide: WideProbe,
    pub keys: [u128; 4], // used count beside it; wire count in [0, 4]
    pub keys_count: i32,
    pub has_target: bool,

    // has_target — wire branch; storage holds both sides, a read zeroes the
    // untaken side (SPEC §5)
    pub target_id: u128,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for LudicrousState {
    fn default() -> Self {
        LudicrousState {
            mode: DriveMode::NONE,
            probe: FixedProbe::default(),
            wide: WideProbe::default(),
            keys: [0; 4],
            keys_count: 0,
            has_target: false,
            target_id: 0,
        }
    }
}

impl LudicrousState {
    // new returns a LudicrousState with its specified defaults applied; the plain
    // Default::default() is the all-zero form (SPEC §4.2: zero initialization
    // unless a specified default overrides it).
    pub fn new() -> Self {
        let mut value = LudicrousState::default();
        value.wide = WideProbe::new();
        value
    }
}

// LUDICROUS_STATE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// LUDICROUS_STATE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const LUDICROUS_STATE_MAX_BITS: u64 = 1205;
pub const LUDICROUS_STATE_MAX_BYTES: usize = 152;

#[inline(always)]
pub fn write_ludicrous_state(stream: &mut WriteStream<'_>, value: &LudicrousState) -> Result {
    if value.mode.0 > 3 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.mode.0 as u32;
        stream.serialize_bits(&mut offset_value, 2)?;
    }
    write_fixed_probe(stream, &value.probe)?;
    write_wide_probe(stream, &value.wide)?;
    if value.keys_count < 0 || value.keys_count > 4 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut offset_value = value.keys_count as u32;
        stream.serialize_bits(&mut offset_value, 3)?; // the count guards the loop (§6.3)
    }
    for i in 0..value.keys_count as usize {
        {
            let mut raw_value = value.keys[i];
            stream.serialize_u128(&mut raw_value)?;
        }
    }
    {
        let mut bool_value = value.has_target;
        stream.serialize_bool(&mut bool_value)?;
    }
    if value.has_target {
        {
            let mut raw_value = value.target_id;
            stream.serialize_u128(&mut raw_value)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_ludicrous_state(stream: &mut ReadStream<'_>, value: &mut LudicrousState) -> Result {
    {
        let mut enum_value: i32 = 0;
        stream.serialize_int(&mut enum_value, 0, 3)?;
        value.mode = DriveMode(enum_value as u8);
    }
    read_fixed_probe(stream, &mut value.probe)?;
    read_wide_probe(stream, &mut value.wide)?;
    stream.serialize_int(&mut value.keys_count, 0, 4)?; // the count guards the loop (§6.3)
    for i in 0..value.keys_count as usize {
        stream.serialize_u128(&mut value.keys[i])?;
    }
    stream.serialize_bool(&mut value.has_target)?;
    if value.has_target {
        stream.serialize_u128(&mut value.target_id)?;
    } else {
        value.target_id = 0;
    }
    Ok(())
}

// type DegenerateProbe
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct DegenerateProbe {
    pub locked_fixed: i32, // = -196608 in new() (zero value otherwise); wire [-3, -3]
    pub locked_int: i32, // = 7 in new() (zero value otherwise); wire [7, 7]
    pub locked_wide: i128, // = -12345678901234 in new() (zero value otherwise); wire [-12345678901234, -12345678901234]
    pub tail: u8,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for DegenerateProbe {
    fn default() -> Self {
        DegenerateProbe {
            locked_fixed: 0,
            locked_int: 0,
            locked_wide: 0,
            tail: 0,
        }
    }
}

impl DegenerateProbe {
    // new returns a DegenerateProbe with its specified defaults applied; the plain
    // Default::default() is the all-zero form (SPEC §4.2: zero initialization
    // unless a specified default overrides it).
    pub fn new() -> Self {
        let mut value = DegenerateProbe::default();
        value.locked_fixed = -196608;
        value.locked_int = 7;
        value.locked_wide = -12345678901234;
        value
    }
}

// DEGENERATE_PROBE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// DEGENERATE_PROBE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const DEGENERATE_PROBE_MAX_BITS: u64 = 8;
pub const DEGENERATE_PROBE_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_degenerate_probe(stream: &mut WriteStream<'_>, value: &DegenerateProbe) -> Result {
    if value.locked_fixed < -196608 || value.locked_fixed > -196608 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.locked_int < 7 || value.locked_int > 7 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.locked_wide < -12345678901234 || value.locked_wide > -12345678901234 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.tail as u32;
        stream.serialize_bits(&mut raw_value, 8)?;
    }
    Ok(())
}

#[inline]
pub fn read_degenerate_probe(stream: &mut ReadStream<'_>, value: &mut DegenerateProbe) -> Result {
    value.locked_fixed = -196608_i32;
    value.locked_int = 7_i32;
    value.locked_wide = -12345678901234_i128;
    {
        let mut raw_value: u32 = 0;
        stream.serialize_bits(&mut raw_value, 8)?;
        value.tail = raw_value as u8;
    }
    Ok(())
}

// type FixedVec [vec3] — tags are user-chosen and inert in v1 (SPEC §4.2, Type tags)
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct FixedVec {
    pub x: i64, // wire [-100000, 100000]
    pub y: i64, // wire [-100000, 100000]
    pub z: i64, // wire [-100000, 100000]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for FixedVec {
    fn default() -> Self {
        FixedVec {
            x: 0,
            y: 0,
            z: 0,
        }
    }
}

// FIXED_VEC_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// FIXED_VEC_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const FIXED_VEC_MAX_BITS: u64 = 102;
pub const FIXED_VEC_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_fixed_vec(stream: &mut WriteStream<'_>, value: &FixedVec) -> Result {
    if value.x < -6553600000 || value.x > 6553600000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.x;
        stream.serialize_fixed(&mut fixed_value, 48, 16, -100000, 100000)?;
    }
    if value.y < -6553600000 || value.y > 6553600000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.y;
        stream.serialize_fixed(&mut fixed_value, 48, 16, -100000, 100000)?;
    }
    if value.z < -6553600000 || value.z > 6553600000 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.z;
        stream.serialize_fixed(&mut fixed_value, 48, 16, -100000, 100000)?;
    }
    Ok(())
}

#[inline]
pub fn read_fixed_vec(stream: &mut ReadStream<'_>, value: &mut FixedVec) -> Result {
    stream.serialize_fixed(&mut value.x, 48, 16, -100000, 100000)?;
    stream.serialize_fixed(&mut value.y, 48, 16, -100000, 100000)?;
    stream.serialize_fixed(&mut value.z, 48, 16, -100000, 100000)?;
    Ok(())
}

// type FixedQuat [quat4] — tags are user-chosen and inert in v1 (SPEC §4.2, Type tags)
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct FixedQuat {
    pub x: i32, // wire [-1, 1]
    pub y: i32, // wire [-1, 1]
    pub z: i32, // wire [-1, 1]
    pub w: i32, // = 1073741824 in new() (zero value otherwise); wire [-1, 1]
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for FixedQuat {
    fn default() -> Self {
        FixedQuat {
            x: 0,
            y: 0,
            z: 0,
            w: 0,
        }
    }
}

impl FixedQuat {
    // new returns a FixedQuat with its specified defaults applied; the plain
    // Default::default() is the all-zero form (SPEC §4.2: zero initialization
    // unless a specified default overrides it).
    pub fn new() -> Self {
        let mut value = FixedQuat::default();
        value.w = 1073741824;
        value
    }
}

// FIXED_QUAT_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// FIXED_QUAT_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const FIXED_QUAT_MAX_BITS: u64 = 128;
pub const FIXED_QUAT_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_fixed_quat(stream: &mut WriteStream<'_>, value: &FixedQuat) -> Result {
    if value.x < -1073741824 || value.x > 1073741824 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.x;
        stream.serialize_fixed(&mut fixed_value, 2, 30, -1, 1)?;
    }
    if value.y < -1073741824 || value.y > 1073741824 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.y;
        stream.serialize_fixed(&mut fixed_value, 2, 30, -1, 1)?;
    }
    if value.z < -1073741824 || value.z > 1073741824 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.z;
        stream.serialize_fixed(&mut fixed_value, 2, 30, -1, 1)?;
    }
    if value.w < -1073741824 || value.w > 1073741824 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut fixed_value = value.w;
        stream.serialize_fixed(&mut fixed_value, 2, 30, -1, 1)?;
    }
    Ok(())
}

#[inline]
pub fn read_fixed_quat(stream: &mut ReadStream<'_>, value: &mut FixedQuat) -> Result {
    stream.serialize_fixed(&mut value.x, 2, 30, -1, 1)?;
    stream.serialize_fixed(&mut value.y, 2, 30, -1, 1)?;
    stream.serialize_fixed(&mut value.z, 2, 30, -1, 1)?;
    stream.serialize_fixed(&mut value.w, 2, 30, -1, 1)?;
    Ok(())
}

