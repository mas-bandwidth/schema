// Code generated by the schema compiler from Ludicrous.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package ludicrous — protocol id 0xa925c86b46058512
//
// THE FLAT TIER — the shipped JavaScript wire path: the serialize.js
// two-lane bitpacker inlined at every field, constant widths and masks,
// zero function calls. Same generated classes, same bytes as the runtime
// tier (a standing CI gate); the runtime tier remains the diagnostic and
// reference surface — re-read a failing buffer through it to learn WHICH
// operation failed and why.
//
// Write<Name>Flat(value, view) -> bytes written, or -1 when the checked
// writer refuses an out-of-contract value (the production writer trusts
// the caller — serialize.js src/mode.js's own NODE_ENV fork, frozen at
// module load). Read<Name>Flat(value, view, numBits) -> bool, the family
// read verdict; reader obligations ride in every mode.
//
// Buffers are caller-owned DataViews. Write buffers: at least <Name>MaxBytes
// (the constants live in the runtime-tier module). Read buffers: at least
// FLAT_READ_SLACK = 8 bytes past the payload — 64-bit windows load
// unconditionally; copy exactly-sized receive buffers into a persistent
// MaxBytes + 8 scratch first.

import { LudicrousState } from "./Ludicrous.js";

// The 8-byte conversion scratch — serialize.js's FLOAT_SCRATCH twin. Module
// scope is safe: single threaded per realm, consumed in the same op that
// fills it.
const SC = new DataView(new ArrayBuffer(8));

// The JavaScript #ifdef, frozen at module load (serialize.js src/mode.js):
// bundlers statically replace NODE_ENV, so production bundles tree-shake
// the checked writers out. Existence-guarded for browsers without process.
const PRODUCTION = typeof process !== "undefined" && !!process.env && process.env.NODE_ENV === "production";

// Read buffers must extend at least this many bytes past the payload.
export const FLAT_READ_SLACK = 8;

// ---- type FixedProbe: the flat codec ----

function writeFixedProbeFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = ((((value.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(BigInt.asUintN(64, value.Position - -1966080000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Reach - -65536000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    v = ((((value.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeFixedProbeFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Angle) || value.Angle < -11796480 || value.Angle > 11796480) {
    return -1;
  }
  v = ((((value.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Position < -1966080000n || value.Position > 1966080000n) {
    return -1;
  }
  v = Number(BigInt.asUintN(64, value.Position - -1966080000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Reach < -65536000000n || value.Reach > 65536000000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Reach - -65536000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Ticks) || value.Ticks < 0 || value.Ticks > 1000000) {
    return -1;
  }
  v = ((value.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    if (!Number.isInteger(value.Samples[i0]) || value.Samples[i0] < -524288 || value.Samples[i0] > 524288) {
      return -1;
    }
    v = ((((value.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteFixedProbeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold FixedProbeMaxBytes.
export const WriteFixedProbeFlat = PRODUCTION ? writeFixedProbeFlatProduction : writeFixedProbeFlatChecked;

// ReadFixedProbeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadFixedProbeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 156 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 25;
  v = (out & 0x1ffffff) >>> 0;
  if (v > 23592960) { // a smuggled raw offset is refused
    return false;
  }
  value.Angle = -11796480 + v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  if (bg > 3932160000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Position = -1966080000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 131072000000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Reach = -65536000000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 20;
  v = (out & 0xfffff) >>> 0;
  if (v > 1000000) { // a smuggled raw offset is refused
    return false;
  }
  value.Ticks = v;
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 21;
    v = (out & 0x1fffff) >>> 0;
    if (v > 1048576) { // a smuggled raw offset is refused
      return false;
    }
    value.Samples[i0] = -524288 + v;
  }
  return true;
}

// ---- type UnsignedProbe: the flat codec ----

function writeUnsignedProbeFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = ((value.Angle) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Span), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Reach), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    v = ((value.Samples[i0]) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  v = ((value.Tail) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeUnsignedProbeFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Angle) || value.Angle < 0 || value.Angle > 23592960) {
    return -1;
  }
  v = ((value.Angle) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Span < 0n || value.Span > 18446744073709486080n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Span), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Reach < 0n || value.Reach > 131072000000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Reach), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Ticks) || value.Ticks < 0 || value.Ticks > 1000000) {
    return -1;
  }
  v = ((value.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    if (!Number.isInteger(value.Samples[i0]) || value.Samples[i0] < 0 || value.Samples[i0] > 1048576) {
      return -1;
    }
    v = ((value.Samples[i0]) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (value.Locked !== 196608) {
    return -1;
  }
  v = ((value.Tail) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteUnsignedProbeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold UnsignedProbeMaxBytes.
export const WriteUnsignedProbeFlat = PRODUCTION ? writeUnsignedProbeFlatProduction : writeUnsignedProbeFlatChecked;

// ReadUnsignedProbeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadUnsignedProbeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 196 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 25;
  v = (out & 0x1ffffff) >>> 0;
  if (v > 23592960) { // a smuggled raw offset is refused
    return false;
  }
  value.Angle = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 18446744073709486080n) { // a smuggled raw offset is refused
    return false;
  }
  value.Span = bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 131072000000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Reach = bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 20;
  v = (out & 0xfffff) >>> 0;
  if (v > 1000000) { // a smuggled raw offset is refused
    return false;
  }
  value.Ticks = v;
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 21;
    v = (out & 0x1fffff) >>> 0;
    if (v > 1048576) { // a smuggled raw offset is refused
      return false;
    }
    value.Samples[i0] = v;
  }
  value.Locked = 196608;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 8;
  v = (out & 0xff) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type WideProbe: the flat codec ----

function writeWideProbeFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  bg = BigInt.asUintN(128, value.EntityId);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Energy - -5000000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Flux - -1267650600228229401496703205376n);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 6;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(BigInt.asUintN(64, value.Bias - -1000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 11;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Seed);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeWideProbeFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  bg = BigInt.asUintN(128, value.EntityId);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Energy < -5000000000n || value.Energy > 5000000000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Energy - -5000000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Flux < -1267650600228229401496703205376n || value.Flux > 1267650600228229401496703205376n) {
    return -1;
  }
  bg = BigInt.asUintN(128, value.Flux - -1267650600228229401496703205376n);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 6;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Bias < -1000n || value.Bias > 1000n) {
    return -1;
  }
  v = Number(BigInt.asUintN(64, value.Bias - -1000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 11;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Seed);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteWideProbeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold WideProbeMaxBytes.
export const WriteWideProbeFlat = PRODUCTION ? writeWideProbeFlatProduction : writeWideProbeFlatChecked;

// ReadWideProbeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadWideProbeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 403 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 96n;
  value.EntityId = bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 10000000000n) { // a smuggled offset is refused
    return false;
  }
  value.Energy = -5000000000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 6;
  v = (out & 0x3f) >>> 0;
  bg |= BigInt(v) << 96n;
  if (bg > 2535301200456458802993406410752n) { // a smuggled offset is refused
    return false;
  }
  value.Flux = -1267650600228229401496703205376n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 11;
  v = (out & 0x7ff) >>> 0;
  bg = BigInt(v);
  if (bg > 2000n) { // a smuggled offset is refused
    return false;
  }
  value.Bias = -1000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 96n;
  value.Seed = bg;
  return true;
}

// ---- message LudicrousState: the flat codec ----

function writeLudicrousStateFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = (value.Mode & 0x3) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((((value.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(BigInt.asUintN(64, value.Probe.Position - -1966080000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Probe.Reach - -65536000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.Probe.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    v = ((((value.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  bg = BigInt.asUintN(128, value.Wide.EntityId);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Wide.Energy - -5000000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Wide.Flux - -1267650600228229401496703205376n);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 6;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(BigInt.asUintN(64, value.Wide.Bias - -1000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 11;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Wide.Seed);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = ((value.KeysCount) & 0x7) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 3;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < value.KeysCount; i0++) {
    bg = BigInt.asUintN(128, value.Keys[i0]);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  v = value.HasTarget ? 1 : 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 1;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.HasTarget) {
    bg = BigInt.asUintN(128, value.TargetId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeLudicrousStateFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.Mode) || value.Mode < 0 || value.Mode > 3) { // headroom above the wire range cannot ride
    return -1;
  }
  v = (value.Mode & 0x3) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Probe.Angle) || value.Probe.Angle < -11796480 || value.Probe.Angle > 11796480) {
    return -1;
  }
  v = ((((value.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 25;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Probe.Position < -1966080000n || value.Probe.Position > 1966080000n) {
    return -1;
  }
  v = Number(BigInt.asUintN(64, value.Probe.Position - -1966080000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Probe.Reach < -65536000000n || value.Probe.Reach > 65536000000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Probe.Reach - -65536000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x1f;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 5;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Probe.Ticks) || value.Probe.Ticks < 0 || value.Probe.Ticks > 1000000) {
    return -1;
  }
  v = ((value.Probe.Ticks) & 0xfffff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 20;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < 2; i0++) {
    if (!Number.isInteger(value.Probe.Samples[i0]) || value.Probe.Samples[i0] < -524288 || value.Probe.Samples[i0] > 524288) {
      return -1;
    }
    v = ((((value.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 21;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  bg = BigInt.asUintN(128, value.Wide.EntityId);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Wide.Energy < -5000000000n || value.Wide.Energy > 5000000000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Wide.Energy - -5000000000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Wide.Flux < -1267650600228229401496703205376n || value.Wide.Flux > 1267650600228229401496703205376n) {
    return -1;
  }
  bg = BigInt.asUintN(128, value.Wide.Flux - -1267650600228229401496703205376n);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 6;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Wide.Bias < -1000n || value.Wide.Bias > 1000n) {
    return -1;
  }
  v = Number(BigInt.asUintN(64, value.Wide.Bias - -1000n));
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 11;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  bg = BigInt.asUintN(128, value.Wide.Seed);
  v = Number(bg & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 32n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number((bg >> 64n) & 0xffffffffn);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = Number(bg >> 96n);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.KeysCount) || value.KeysCount < 0 || value.KeysCount > 4) { // the count guards the loop; out-of-contract writes are refused
    return -1;
  }
  v = ((value.KeysCount) & 0x7) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 3;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  for (let i0 = 0; i0 < value.KeysCount; i0++) {
    bg = BigInt.asUintN(128, value.Keys[i0]);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  v = value.HasTarget ? 1 : 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 1;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.HasTarget) {
    bg = BigInt.asUintN(128, value.TargetId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteLudicrousStateFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold LudicrousStateMaxBytes.
export const WriteLudicrousStateFlat = PRODUCTION ? writeLudicrousStateFlatProduction : writeLudicrousStateFlatChecked;

// ReadLudicrousStateFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadLudicrousStateFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 561 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  value.Mode = v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 25;
  v = (out & 0x1ffffff) >>> 0;
  if (v > 23592960) { // a smuggled raw offset is refused
    return false;
  }
  value.Probe.Angle = -11796480 + v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  if (bg > 3932160000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Probe.Position = -1966080000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 5;
  v = (out & 0x1f) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 131072000000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Probe.Reach = -65536000000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 20;
  v = (out & 0xfffff) >>> 0;
  if (v > 1000000) { // a smuggled raw offset is refused
    return false;
  }
  value.Probe.Ticks = v;
  for (let i0 = 0; i0 < 2; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 21;
    v = (out & 0x1fffff) >>> 0;
    if (v > 1048576) { // a smuggled raw offset is refused
      return false;
    }
    value.Probe.Samples[i0] = -524288 + v;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 96n;
  value.Wide.EntityId = bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 10000000000n) { // a smuggled offset is refused
    return false;
  }
  value.Wide.Energy = -5000000000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 6;
  v = (out & 0x3f) >>> 0;
  bg |= BigInt(v) << 96n;
  if (bg > 2535301200456458802993406410752n) { // a smuggled offset is refused
    return false;
  }
  value.Wide.Flux = -1267650600228229401496703205376n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 11;
  v = (out & 0x7ff) >>> 0;
  bg = BigInt(v);
  if (bg > 2000n) { // a smuggled offset is refused
    return false;
  }
  value.Wide.Bias = -1000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg = BigInt(v);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 32n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 64n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  bg |= BigInt(v) << 96n;
  value.Wide.Seed = bg;
  if (br + 3 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 3;
  v = (out & 0x7) >>> 0;
  if (v > 4) { // the count guards the loop — reject, never clamp
    return false;
  }
  value.KeysCount = v;
  if (br + value.KeysCount * 128 > numBits) {
    return false;
  }
  for (let i0 = 0; i0 < value.KeysCount; i0++) {
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 32n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 64n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 96n;
    value.Keys[i0] = bg;
  }
  if (br + 1 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  value.HasTarget = v !== 0;
  if (value.HasTarget) {
    if (br + 128 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 32n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 64n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 96n;
    value.TargetId = bg;
  } else {
    value.TargetId = 0n;
  }
  return true;
}

function writeLudicrousStateFlatArrayProduction(values, count, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  let base = 0;
  for (let k = 0; k < count; k++) {
    const value = values[k];
    lo = 0;
    hi = 0;
    sb = 0;
    wi = base;
    v = (value.Mode & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((((value.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 25;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(BigInt.asUintN(64, value.Probe.Position - -1966080000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, value.Probe.Reach - -65536000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x1f;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 5;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((value.Probe.Ticks) & 0xfffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 20;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < 2; i0++) {
      v = ((((value.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 21;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    bg = BigInt.asUintN(128, value.Wide.EntityId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, value.Wide.Energy - -5000000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x3;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, value.Wide.Flux - -1267650600228229401496703205376n);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 6;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(BigInt.asUintN(64, value.Wide.Bias - -1000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 11;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, value.Wide.Seed);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((value.KeysCount) & 0x7) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 3;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < value.KeysCount; i0++) {
      bg = BigInt.asUintN(128, value.Keys[i0]);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    v = value.HasTarget ? 1 : 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.HasTarget) {
      bg = BigInt.asUintN(128, value.TargetId);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    base = wi + ((sb + 7) >> 3);
  }
  return base;
}

function writeLudicrousStateFlatArrayChecked(values, count, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  let base = 0;
  for (let k = 0; k < count; k++) {
    const value = values[k];
    lo = 0;
    hi = 0;
    sb = 0;
    wi = base;
    if (!Number.isInteger(value.Mode) || value.Mode < 0 || value.Mode > 3) { // headroom above the wire range cannot ride
      return -1;
    }
    v = (value.Mode & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(value.Probe.Angle) || value.Probe.Angle < -11796480 || value.Probe.Angle > 11796480) {
      return -1;
    }
    v = ((((value.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 25;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.Probe.Position < -1966080000n || value.Probe.Position > 1966080000n) {
      return -1;
    }
    v = Number(BigInt.asUintN(64, value.Probe.Position - -1966080000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.Probe.Reach < -65536000000n || value.Probe.Reach > 65536000000n) {
      return -1;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, value.Probe.Reach - -65536000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x1f;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 5;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(value.Probe.Ticks) || value.Probe.Ticks < 0 || value.Probe.Ticks > 1000000) {
      return -1;
    }
    v = ((value.Probe.Ticks) & 0xfffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 20;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < 2; i0++) {
      if (!Number.isInteger(value.Probe.Samples[i0]) || value.Probe.Samples[i0] < -524288 || value.Probe.Samples[i0] > 524288) {
        return -1;
      }
      v = ((((value.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 21;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    bg = BigInt.asUintN(128, value.Wide.EntityId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.Wide.Energy < -5000000000n || value.Wide.Energy > 5000000000n) {
      return -1;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, value.Wide.Energy - -5000000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x3;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.Wide.Flux < -1267650600228229401496703205376n || value.Wide.Flux > 1267650600228229401496703205376n) {
      return -1;
    }
    bg = BigInt.asUintN(128, value.Wide.Flux - -1267650600228229401496703205376n);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 6;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.Wide.Bias < -1000n || value.Wide.Bias > 1000n) {
      return -1;
    }
    v = Number(BigInt.asUintN(64, value.Wide.Bias - -1000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 11;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, value.Wide.Seed);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(value.KeysCount) || value.KeysCount < 0 || value.KeysCount > 4) { // the count guards the loop; out-of-contract writes are refused
      return -1;
    }
    v = ((value.KeysCount) & 0x7) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 3;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < value.KeysCount; i0++) {
      bg = BigInt.asUintN(128, value.Keys[i0]);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    v = value.HasTarget ? 1 : 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (value.HasTarget) {
      bg = BigInt.asUintN(128, value.TargetId);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    base = wi + ((sb + 7) >> 3);
  }
  return base;
}

// WriteLudicrousStateFlatArray(values, count, view) -> total bytes, or -1 on a checked
// refusal. Packets pack back to back, each starting on its own byte; the
// buffer must hold the running total plus LudicrousStateMaxBytes at every step
// (count * LudicrousStateMaxBytes always suffices).
export const WriteLudicrousStateFlatArray = PRODUCTION ? writeLudicrousStateFlatArrayProduction : writeLudicrousStateFlatArrayChecked;

// ReadLudicrousStateFlatArray(values, count, view, numBits) -> bool: count back-to-back
// packets, each starting on its own byte boundary — the loop-fused batch
// shape. The buffer must extend FLAT_READ_SLACK bytes past the payload.
export function ReadLudicrousStateFlatArray(values, count, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  for (let k = 0; k < count; k++) {
    const value = values[k];
    if (br + 561 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 2;
    v = (out & 0x3) >>> 0;
    value.Mode = v;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 25;
    v = (out & 0x1ffffff) >>> 0;
    if (v > 23592960) { // a smuggled raw offset is refused
      return false;
    }
    value.Probe.Angle = -11796480 + v;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    if (bg > 3932160000n) { // a smuggled raw offset is refused
      return false;
    }
    value.Probe.Position = -1966080000n + bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    SC.setUint32(0, v, true);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 5;
    v = (out & 0x1f) >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    if (bg > 131072000000n) { // a smuggled raw offset is refused
      return false;
    }
    value.Probe.Reach = -65536000000n + bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 20;
    v = (out & 0xfffff) >>> 0;
    if (v > 1000000) { // a smuggled raw offset is refused
      return false;
    }
    value.Probe.Ticks = v;
    for (let i0 = 0; i0 < 2; i0++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 21;
      v = (out & 0x1fffff) >>> 0;
      if (v > 1048576) { // a smuggled raw offset is refused
        return false;
      }
      value.Probe.Samples[i0] = -524288 + v;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 32n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 64n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 96n;
    value.Wide.EntityId = bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    SC.setUint32(0, v, true);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 2;
    v = (out & 0x3) >>> 0;
    SC.setUint32(4, v, true);
    bg = SC.getBigUint64(0, true);
    if (bg > 10000000000n) { // a smuggled offset is refused
      return false;
    }
    value.Wide.Energy = -5000000000n + bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 32n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 64n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 6;
    v = (out & 0x3f) >>> 0;
    bg |= BigInt(v) << 96n;
    if (bg > 2535301200456458802993406410752n) { // a smuggled offset is refused
      return false;
    }
    value.Wide.Flux = -1267650600228229401496703205376n + bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 11;
    v = (out & 0x7ff) >>> 0;
    bg = BigInt(v);
    if (bg > 2000n) { // a smuggled offset is refused
      return false;
    }
    value.Wide.Bias = -1000n + bg;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg = BigInt(v);
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 32n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 64n;
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 32;
    v = out >>> 0;
    bg |= BigInt(v) << 96n;
    value.Wide.Seed = bg;
    if (br + 3 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 3;
    v = (out & 0x7) >>> 0;
    if (v > 4) { // the count guards the loop — reject, never clamp
      return false;
    }
    value.KeysCount = v;
    if (br + value.KeysCount * 128 > numBits) {
      return false;
    }
    for (let i0 = 0; i0 < value.KeysCount; i0++) {
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 32n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 64n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 96n;
      value.Keys[i0] = bg;
    }
    if (br + 1 > numBits) {
      return false;
    }
    bi = br >>> 3;
    wlo = view.getUint32(bi, true);
    whi = view.getUint32(bi + 4, true);
    s2 = br & 7;
    out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
    br += 1;
    v = (out & 0x1) >>> 0;
    value.HasTarget = v !== 0;
    if (value.HasTarget) {
      if (br + 128 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 32n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 64n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 96n;
      value.TargetId = bg;
    } else {
      value.TargetId = 0n;
    }
    br = (br + 7) & ~7;
  }
  return true;
}

// ---- type DegenerateProbe: the flat codec ----

function writeDegenerateProbeFlatProduction(value, view) {
  let v = 0, s = 0;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = ((value.Tail) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeDegenerateProbeFlatChecked(value, view) {
  let v = 0, s = 0;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (value.LockedFixed !== -196608) {
    return -1;
  }
  if (value.LockedInt !== 7) {
    return -1;
  }
  if (value.LockedWide !== -12345678901234n) {
    return -1;
  }
  v = ((value.Tail) & 0xff) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 8;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteDegenerateProbeFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold DegenerateProbeMaxBytes.
export const WriteDegenerateProbeFlat = PRODUCTION ? writeDegenerateProbeFlatProduction : writeDegenerateProbeFlatChecked;

// ReadDegenerateProbeFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadDegenerateProbeFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 8 > numBits) {
    return false;
  }
  value.LockedFixed = -196608;
  value.LockedInt = 7;
  value.LockedWide = -12345678901234n;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 8;
  v = (out & 0xff) >>> 0;
  value.Tail = v;
  return true;
}

// ---- type FixedVec: the flat codec ----

function writeFixedVecFlatProduction(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  SC.setBigUint64(0, BigInt.asUintN(64, value.X - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Y - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Z - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeFixedVecFlatChecked(value, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (value.X < -6553600000n || value.X > 6553600000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.X - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Y < -6553600000n || value.Y > 6553600000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Y - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (value.Z < -6553600000n || value.Z > 6553600000n) {
    return -1;
  }
  SC.setBigUint64(0, BigInt.asUintN(64, value.Z - -6553600000n), true);
  v = SC.getUint32(0, true);
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = SC.getUint32(4, true) & 0x3;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 2;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteFixedVecFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold FixedVecMaxBytes.
export const WriteFixedVecFlat = PRODUCTION ? writeFixedVecFlatProduction : writeFixedVecFlatChecked;

// ReadFixedVecFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadFixedVecFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  if (br + 102 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 13107200000n) { // a smuggled raw offset is refused
    return false;
  }
  value.X = -6553600000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 13107200000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Y = -6553600000n + bg;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  SC.setUint32(0, v, true);
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 2;
  v = (out & 0x3) >>> 0;
  SC.setUint32(4, v, true);
  bg = SC.getBigUint64(0, true);
  if (bg > 13107200000n) { // a smuggled raw offset is refused
    return false;
  }
  value.Z = -6553600000n + bg;
  return true;
}

// ---- type FixedQuat: the flat codec ----

function writeFixedQuatFlatProduction(value, view) {
  let v = 0, s = 0;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  v = (((value.X >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (((value.Y >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (((value.Z >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  v = (((value.W >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

function writeFixedQuatFlatChecked(value, view) {
  let v = 0, s = 0;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (!Number.isInteger(value.X) || value.X < -1073741824 || value.X > 1073741824) {
    return -1;
  }
  v = (((value.X >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Y) || value.Y < -1073741824 || value.Y > 1073741824) {
    return -1;
  }
  v = (((value.Y >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.Z) || value.Z < -1073741824 || value.Z > 1073741824) {
    return -1;
  }
  v = (((value.Z >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (!Number.isInteger(value.W) || value.W < -1073741824 || value.W > 1073741824) {
    return -1;
  }
  v = (((value.W >>> 0) - 3221225472) >>> 0) >>> 0;
  s = sb;
  if (s < 32) {
    lo = (lo | (v << s)) >>> 0;
    if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
  } else {
    hi = (hi | (v << (s - 32))) >>> 0;
  }
  sb = s + 32;
  if (sb >= 64) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
    wi += 8;
    lo = s === 32 ? 0 : v >>> (64 - s);
    hi = 0;
    sb -= 64;
  }
  if (sb !== 0) {
    view.setUint32(wi, lo, true);
    view.setUint32(wi + 4, hi, true);
  }
  return ((wi * 8 + sb) + 7) >> 3;
}

// WriteFixedQuatFlat(value, view) -> bytes written (>= 0), or -1 on a checked
// contract refusal. The buffer behind view must hold FixedQuatMaxBytes.
export const WriteFixedQuatFlat = PRODUCTION ? writeFixedQuatFlatProduction : writeFixedQuatFlatChecked;

// ReadFixedQuatFlat(value, view, numBits) -> bool. The buffer behind view must
// extend FLAT_READ_SLACK bytes past the payload.
export function ReadFixedQuatFlat(value, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let br = 0;
  if (br + 128 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  if (v > 2147483648) { // a smuggled raw offset is refused
    return false;
  }
  value.X = -1073741824 + v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  if (v > 2147483648) { // a smuggled raw offset is refused
    return false;
  }
  value.Y = -1073741824 + v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  if (v > 2147483648) { // a smuggled raw offset is refused
    return false;
  }
  value.Z = -1073741824 + v;
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 32;
  v = out >>> 0;
  if (v > 2147483648) { // a smuggled raw offset is refused
    return false;
  }
  value.W = -1073741824 + v;
  return true;
}

function writeMessageFlatProduction(message, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (message === null) {
    v = 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    return ((wi * 8 + sb) + 7) >> 3; // the None terminator
  }
  if (message instanceof LudicrousState) {
    v = 1;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = (message.Mode & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((((message.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 25;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(BigInt.asUintN(64, message.Probe.Position - -1966080000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, message.Probe.Reach - -65536000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x1f;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 5;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((message.Probe.Ticks) & 0xfffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 20;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < 2; i0++) {
      v = ((((message.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 21;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    bg = BigInt.asUintN(128, message.Wide.EntityId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, message.Wide.Energy - -5000000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x3;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, message.Wide.Flux - -1267650600228229401496703205376n);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 6;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(BigInt.asUintN(64, message.Wide.Bias - -1000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 11;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, message.Wide.Seed);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = ((message.KeysCount) & 0x7) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 3;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < message.KeysCount; i0++) {
      bg = BigInt.asUintN(128, message.Keys[i0]);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    v = message.HasTarget ? 1 : 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.HasTarget) {
      bg = BigInt.asUintN(128, message.TargetId);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    return ((wi * 8 + sb) + 7) >> 3;
  }
  return -1; // not a generated message class; nothing was written
}

function writeMessageFlatChecked(message, view) {
  let v = 0, s = 0;
  let bg = 0n;
  let lo = 0, hi = 0, sb = 0, wi = 0;
  if (message === null) {
    v = 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    return ((wi * 8 + sb) + 7) >> 3; // the None terminator
  }
  if (message instanceof LudicrousState) {
    v = 1;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(message.Mode) || message.Mode < 0 || message.Mode > 3) { // headroom above the wire range cannot ride
      return -1;
    }
    v = (message.Mode & 0x3) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(message.Probe.Angle) || message.Probe.Angle < -11796480 || message.Probe.Angle > 11796480) {
      return -1;
    }
    v = ((((message.Probe.Angle >>> 0) - 4283170816) >>> 0) & 0x1ffffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 25;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.Probe.Position < -1966080000n || message.Probe.Position > 1966080000n) {
      return -1;
    }
    v = Number(BigInt.asUintN(64, message.Probe.Position - -1966080000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.Probe.Reach < -65536000000n || message.Probe.Reach > 65536000000n) {
      return -1;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, message.Probe.Reach - -65536000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x1f;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 5;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(message.Probe.Ticks) || message.Probe.Ticks < 0 || message.Probe.Ticks > 1000000) {
      return -1;
    }
    v = ((message.Probe.Ticks) & 0xfffff) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 20;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < 2; i0++) {
      if (!Number.isInteger(message.Probe.Samples[i0]) || message.Probe.Samples[i0] < -524288 || message.Probe.Samples[i0] > 524288) {
        return -1;
      }
      v = ((((message.Probe.Samples[i0] >>> 0) - 4294443008) >>> 0) & 0x1fffff) >>> 0;
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 21;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    bg = BigInt.asUintN(128, message.Wide.EntityId);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.Wide.Energy < -5000000000n || message.Wide.Energy > 5000000000n) {
      return -1;
    }
    SC.setBigUint64(0, BigInt.asUintN(64, message.Wide.Energy - -5000000000n), true);
    v = SC.getUint32(0, true);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = SC.getUint32(4, true) & 0x3;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 2;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.Wide.Flux < -1267650600228229401496703205376n || message.Wide.Flux > 1267650600228229401496703205376n) {
      return -1;
    }
    bg = BigInt.asUintN(128, message.Wide.Flux - -1267650600228229401496703205376n);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 6;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.Wide.Bias < -1000n || message.Wide.Bias > 1000n) {
      return -1;
    }
    v = Number(BigInt.asUintN(64, message.Wide.Bias - -1000n));
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 11;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    bg = BigInt.asUintN(128, message.Wide.Seed);
    v = Number(bg & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 32n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number((bg >> 64n) & 0xffffffffn);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    v = Number(bg >> 96n);
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 32;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (!Number.isInteger(message.KeysCount) || message.KeysCount < 0 || message.KeysCount > 4) { // the count guards the loop; out-of-contract writes are refused
      return -1;
    }
    v = ((message.KeysCount) & 0x7) >>> 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 3;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    for (let i0 = 0; i0 < message.KeysCount; i0++) {
      bg = BigInt.asUintN(128, message.Keys[i0]);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    v = message.HasTarget ? 1 : 0;
    s = sb;
    if (s < 32) {
      lo = (lo | (v << s)) >>> 0;
      if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
    } else {
      hi = (hi | (v << (s - 32))) >>> 0;
    }
    sb = s + 1;
    if (sb >= 64) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
      wi += 8;
      lo = s === 32 ? 0 : v >>> (64 - s);
      hi = 0;
      sb -= 64;
    }
    if (message.HasTarget) {
      bg = BigInt.asUintN(128, message.TargetId);
      v = Number(bg & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 32n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number((bg >> 64n) & 0xffffffffn);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
      v = Number(bg >> 96n);
      s = sb;
      if (s < 32) {
        lo = (lo | (v << s)) >>> 0;
        if (s > 0) { hi = (hi | (v >>> (32 - s))) >>> 0; }
      } else {
        hi = (hi | (v << (s - 32))) >>> 0;
      }
      sb = s + 32;
      if (sb >= 64) {
        view.setUint32(wi, lo, true);
        view.setUint32(wi + 4, hi, true);
        wi += 8;
        lo = s === 32 ? 0 : v >>> (64 - s);
        hi = 0;
        sb -= 64;
      }
    }
    if (sb !== 0) {
      view.setUint32(wi, lo, true);
      view.setUint32(wi + 4, hi, true);
    }
    return ((wi * 8 + sb) + 7) >> 3;
  }
  return -1; // not a generated message class; nothing was written
}

// WriteMessageFlat(message, view) -> bytes written, or -1: null is the None
// terminator; a value outside the generated class set writes NOTHING (a tag
// with no payload would desynchronize the stream). Tag framing is the
// runtime tier's exactly; the message body inlines beneath the tag bits.
export const WriteMessageFlat = PRODUCTION ? writeMessageFlatProduction : writeMessageFlatChecked;

// ReadMessageFlat(storage, message, view, numBits) -> bool: the tag, then
// the message into the caller's MessageStorage (zeroed to the §5 form
// first — the union's own discipline); message.value is null under a None
// tag (the terminator, a success), the storage's instance otherwise.
export function ReadMessageFlat(storage, message, view, numBits) {
  let v = 0, bi = 0, wlo = 0, whi = 0, s2 = 0, out = 0;
  let bg = 0n;
  let br = 0;
  message.value = null;
  if (br + 1 > numBits) {
    return false;
  }
  bi = br >>> 3;
  wlo = view.getUint32(bi, true);
  whi = view.getUint32(bi + 4, true);
  s2 = br & 7;
  out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
  br += 1;
  v = (out & 0x1) >>> 0;
  switch (v) {
    case 0:
      return true; // the None terminator — message stays null
    case 1: { // LudicrousState
      const value = storage.LudicrousState;
      value.Mode = 0;
      value.Probe.Angle = 0;
      value.Probe.Position = 0n;
      value.Probe.Reach = 0n;
      value.Probe.Ticks = 0;
      value.Probe.Samples.fill(0);
      value.Wide.EntityId = 0n;
      value.Wide.Energy = 0n;
      value.Wide.Flux = 0n;
      value.Wide.Bias = 0n;
      value.Wide.Seed = 0n;
      value.Keys.fill(0n);
      value.KeysCount = 0;
      value.HasTarget = false;
      value.TargetId = 0n;
      if (br + 561 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 2;
      v = (out & 0x3) >>> 0;
      value.Mode = v;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 25;
      v = (out & 0x1ffffff) >>> 0;
      if (v > 23592960) { // a smuggled raw offset is refused
        return false;
      }
      value.Probe.Angle = -11796480 + v;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      if (bg > 3932160000n) { // a smuggled raw offset is refused
        return false;
      }
      value.Probe.Position = -1966080000n + bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      SC.setUint32(0, v, true);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 5;
      v = (out & 0x1f) >>> 0;
      SC.setUint32(4, v, true);
      bg = SC.getBigUint64(0, true);
      if (bg > 131072000000n) { // a smuggled raw offset is refused
        return false;
      }
      value.Probe.Reach = -65536000000n + bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 20;
      v = (out & 0xfffff) >>> 0;
      if (v > 1000000) { // a smuggled raw offset is refused
        return false;
      }
      value.Probe.Ticks = v;
      for (let i0 = 0; i0 < 2; i0++) {
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 21;
        v = (out & 0x1fffff) >>> 0;
        if (v > 1048576) { // a smuggled raw offset is refused
          return false;
        }
        value.Probe.Samples[i0] = -524288 + v;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 32n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 64n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 96n;
      value.Wide.EntityId = bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      SC.setUint32(0, v, true);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 2;
      v = (out & 0x3) >>> 0;
      SC.setUint32(4, v, true);
      bg = SC.getBigUint64(0, true);
      if (bg > 10000000000n) { // a smuggled offset is refused
        return false;
      }
      value.Wide.Energy = -5000000000n + bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 32n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 64n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 6;
      v = (out & 0x3f) >>> 0;
      bg |= BigInt(v) << 96n;
      if (bg > 2535301200456458802993406410752n) { // a smuggled offset is refused
        return false;
      }
      value.Wide.Flux = -1267650600228229401496703205376n + bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 11;
      v = (out & 0x7ff) >>> 0;
      bg = BigInt(v);
      if (bg > 2000n) { // a smuggled offset is refused
        return false;
      }
      value.Wide.Bias = -1000n + bg;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg = BigInt(v);
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 32n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 64n;
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 32;
      v = out >>> 0;
      bg |= BigInt(v) << 96n;
      value.Wide.Seed = bg;
      if (br + 3 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 3;
      v = (out & 0x7) >>> 0;
      if (v > 4) { // the count guards the loop — reject, never clamp
        return false;
      }
      value.KeysCount = v;
      if (br + value.KeysCount * 128 > numBits) {
        return false;
      }
      for (let i0 = 0; i0 < value.KeysCount; i0++) {
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg = BigInt(v);
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 32n;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 64n;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 96n;
        value.Keys[i0] = bg;
      }
      if (br + 1 > numBits) {
        return false;
      }
      bi = br >>> 3;
      wlo = view.getUint32(bi, true);
      whi = view.getUint32(bi + 4, true);
      s2 = br & 7;
      out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
      br += 1;
      v = (out & 0x1) >>> 0;
      value.HasTarget = v !== 0;
      if (value.HasTarget) {
        if (br + 128 > numBits) {
          return false;
        }
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg = BigInt(v);
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 32n;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 64n;
        bi = br >>> 3;
        wlo = view.getUint32(bi, true);
        whi = view.getUint32(bi + 4, true);
        s2 = br & 7;
        out = s2 === 0 ? wlo : ((wlo >>> s2) | (whi << (32 - s2)));
        br += 32;
        v = out >>> 0;
        bg |= BigInt(v) << 96n;
        value.TargetId = bg;
      } else {
        value.TargetId = 0n;
      }
      message.value = value;
      return true;
    }
    default:
      // unreachable: the tag was bounded above; the default keeps the
      // verdict total
      return false;
  }
}

