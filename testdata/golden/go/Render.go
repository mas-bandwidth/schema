// Code generated by the schema compiler from Render.schema. DO NOT EDIT.
// package example — protocol id 0xbc05d83a8135cdb9

package example

import (
	"github.com/mas-bandwidth/serialize.go"
)

const RenderBlockMaxSprites = 64

// type RenderSprite
type RenderSprite struct {
	SortKey    uint64
	MeshId     uint32
	MaterialId uint32
	Layer      uint8
	Team       Team
}

// RenderSpriteMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// RenderSpriteMaxBytes is rounded up to the 8-byte write-buffer granularity.
const RenderSpriteMaxBits = 138
const RenderSpriteMaxBytes = 24

func WriteRenderSprite(stream *serialize.WriteStream, value *RenderSprite) error {
	stream.SerializeBits64(&value.SortKey, 64)
	stream.SerializeBits(&value.MeshId, 32)
	stream.SerializeBits(&value.MaterialId, 32)
	{
		rawValue := uint32(value.Layer)
		stream.SerializeBits(&rawValue, 8)
	}
	{
		enumValue := int32(value.Team)
		if enumValue < 0 || enumValue > 2 { // the runtime range refusal, folded (SPEC §5)
			return serialize.ErrValueOutOfRange
		}
		{
			offsetValue := uint32(enumValue)
			stream.SerializeBits(&offsetValue, 2)
		}
	}
	return stream.Err()
}

func ReadRenderSprite(stream *serialize.ReadStream, value *RenderSprite) error {
	stream.SerializeBits64(&value.SortKey, 64)
	stream.SerializeBits(&value.MeshId, 32)
	stream.SerializeBits(&value.MaterialId, 32)
	{
		rawValue := uint32(0)
		stream.SerializeBits(&rawValue, 8)
		value.Layer = uint8(rawValue)
	}
	{
		enumValue := int32(0)
		stream.SerializeInt(&enumValue, 0, 2)
		value.Team = Team(enumValue)
	}
	return stream.Err()
}

// type RenderBlock
type RenderBlock struct {
	WorkerIndex     uint32
	SpriteCountHint uint32
	Sprites         [RenderBlockMaxSprites]RenderSprite // used count beside it; wire count in [0, RenderBlockMaxSprites]
	SpritesCount    int32
}

// RenderBlockMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// RenderBlockMaxBytes is rounded up to the 8-byte write-buffer granularity.
const RenderBlockMaxBits = 8903
const RenderBlockMaxBytes = 1120

func WriteRenderBlock(stream *serialize.WriteStream, value *RenderBlock) error {
	stream.SerializeBits(&value.WorkerIndex, 32)
	stream.SerializeBits(&value.SpriteCountHint, 32)
	if value.SpritesCount < 0 || value.SpritesCount > RenderBlockMaxSprites { // the runtime range refusal, folded (SPEC §5)
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.SpritesCount)
		stream.SerializeBits(&offsetValue, 7)
	}
	if stream.Err() != nil { // the count guards the loop (§6.3)
		return stream.Err()
	}
	for i := int32(0); i < value.SpritesCount; i++ {
		if err := WriteRenderSprite(stream, &value.Sprites[i]); err != nil {
			return err
		}
	}
	return stream.Err()
}

func ReadRenderBlock(stream *serialize.ReadStream, value *RenderBlock) error {
	stream.SerializeBits(&value.WorkerIndex, 32)
	stream.SerializeBits(&value.SpriteCountHint, 32)
	stream.SerializeInt(&value.SpritesCount, 0, RenderBlockMaxSprites)
	if stream.Err() != nil { // the count guards the loop (§6.3)
		return stream.Err()
	}
	for i := int32(0); i < value.SpritesCount; i++ {
		if err := ReadRenderSprite(stream, &value.Sprites[i]); err != nil {
			return err
		}
	}
	return stream.Err()
}
