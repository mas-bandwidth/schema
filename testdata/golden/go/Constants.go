// Code generated by the schema compiler from Constants.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

package example

import (
	"errors"
)

// The unit's protocol id — the hash of its wire shape (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
const ProtocolId uint64 = 0x8656ae68c06b97a7

// ErrValidation is returned when a read rejects the wire: a wrong constant,
// nonzero reserved bits, or an interior null in a string (SPEC §4.3, §4.7).
var ErrValidation = errors.New("example: wire validation failed")

const PositionUnits = 1024

const VelocityUnits = 1024

const RotationUnits = 1024

const MaxWorldMeters = 8192

const MaxPositionUnits = MaxWorldMeters * PositionUnits

const MaxSpeedMeters = 2048

const MaxVelocityUnits = MaxSpeedMeters * VelocityUnits

const MaxHealth = 1000

const MaxObjects = 10000

const ShipMaxLasers = 16

const ShipMaxMissiles = 16

const NumTeams = 2 // = Team.Max

const MaxInputsPerPacket = 16

const MaxBlockSize = 2000

const MaxChatLength = 256
