// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

package example

import (
	"github.com/mas-bandwidth/serialize.go"
)

// type VoidPulse
type VoidPulse struct {
	Strength uint16
}

// VoidPulseMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidPulseMaxBytes is rounded up to the 8-byte write-buffer granularity.
const VoidPulseMaxBits = 16
const VoidPulseMaxBytes = 8

func WriteVoidPulse(stream *serialize.WriteStream, value *VoidPulse) error {
	{
		rawValue := uint32(value.Strength)
		stream.SerializeBits(&rawValue, 16)
	}
	return stream.Err()
}

func ReadVoidPulse(stream *serialize.ReadStream, value *VoidPulse) error {
	{
		rawValue := uint32(0)
		stream.SerializeBits(&rawValue, 16)
		value.Strength = uint16(rawValue)
	}
	return stream.Err()
}

// type VoidLevel
type VoidLevel struct {
	Level uint8 // wire [0, 7]
}

// VoidLevelMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidLevelMaxBytes is rounded up to the 8-byte write-buffer granularity.
const VoidLevelMaxBits = 3
const VoidLevelMaxBytes = 8

func WriteVoidLevel(stream *serialize.WriteStream, value *VoidLevel) error {
	{
		rangeValue := int32(value.Level)
		if rangeValue < 0 || rangeValue > 7 {
			return serialize.ErrValueOutOfRange
		}
		{
			offsetValue := uint32(rangeValue)
			stream.SerializeBits(&offsetValue, 3)
		}
	}
	return stream.Err()
}

func ReadVoidLevel(stream *serialize.ReadStream, value *VoidLevel) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 3)
		if stream.Err() != nil {
			return stream.Err()
		}
		value.Level = uint8(int32(offsetValue))
	}
	return stream.Err()
}

// VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
type VoidSignalType uint8

const (
	VoidSignalTypeNone  VoidSignalType = 0
	VoidSignalTypePing  VoidSignalType = 1
	VoidSignalTypePulse VoidSignalType = 2
	VoidSignalTypeLevel VoidSignalType = 3
	VoidSignalTypeAck   VoidSignalType = 4
	VoidSignalTypeCount VoidSignalType = 4 // the declared variant count (SPEC §4.2)
	VoidSignalTypeMax   VoidSignalType = 4 // the exported extent (SPEC §4.2)
)

// EnumNameVoidSignalType: debug/log/tooling name for any VoidSignalType wire value —
// out-of-set values (wire-legal up to the declared max) name as "???"
func EnumNameVoidSignalType(value VoidSignalType) string {
	switch value {
	case VoidSignalTypeNone:
		return "None"
	case VoidSignalTypePing:
		return "Ping"
	case VoidSignalTypePulse:
		return "Pulse"
	case VoidSignalTypeLevel:
		return "Level"
	case VoidSignalTypeAck:
		return "Ack"
	}
	return "???"
}

// VoidSignal — at most one of the arms; Type says which. The zero value is the
// empty union (None). A read constructs the selected arm with its defaults before
// decoding it (SPEC §5); unselected arms keep what they last held — the
// reused-storage discipline. Consumers read the selected arm only.
type VoidSignal struct {
	Type  VoidSignalType
	Pulse VoidPulse
	Level VoidLevel
}

// VoidSignalMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
// VoidSignalMaxBytes is rounded up to the 8-byte write-buffer granularity.
const VoidSignalMaxBits = 19
const VoidSignalMaxBytes = 8

func WriteVoidSignal(stream *serialize.WriteStream, value *VoidSignal) error {
	tagValue := int32(value.Type)
	if tagValue < 0 || tagValue > 4 { // the tag validates BEFORE it rides (SPEC §4.8)
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(tagValue)
		stream.SerializeBits(&offsetValue, 3)
	}
	switch value.Type {
	case VoidSignalTypePing:
		return stream.Err() // a payload-free arm: the tag is the whole wire (SPEC §4.8)
	case VoidSignalTypePulse:
		return WriteVoidPulse(stream, &value.Pulse)
	case VoidSignalTypeLevel:
		return WriteVoidLevel(stream, &value.Level)
	case VoidSignalTypeAck:
		return stream.Err() // a payload-free arm: the tag is the whole wire (SPEC §4.8)
	}
	return stream.Err() // None — the tag is the whole wire (SPEC §4.8)
}

func ReadVoidSignal(stream *serialize.ReadStream, value *VoidSignal) error {
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 3)
		if stream.Err() != nil {
			return stream.Err()
		}
		if offsetValue > 4 { // a value smuggled into the bit headroom is refused (SPEC §4.3)
			return serialize.ErrValueOutOfRange
		}
		value.Type = VoidSignalType(int32(offsetValue))
	}
	switch value.Type {
	case VoidSignalTypePing:
		return stream.Err() // a payload-free arm: the tag is the whole wire (SPEC §4.8)
	case VoidSignalTypePulse:
		value.Pulse = VoidPulse{} // fresh payload on every selection (SPEC §4.8)
		return ReadVoidPulse(stream, &value.Pulse)
	case VoidSignalTypeLevel:
		value.Level = VoidLevel{} // fresh payload on every selection (SPEC §4.8)
		return ReadVoidLevel(stream, &value.Level)
	case VoidSignalTypeAck:
		return stream.Err() // a payload-free arm: the tag is the whole wire (SPEC §4.8)
	}
	return stream.Err() // None
}

// type VoidSignalPacket
type VoidSignalPacket struct {
	Lead         uint32
	Signal       VoidSignal
	Echo         VoidSignal
	History      [2]VoidSignal // used count beside it; wire count in [0, 2]
	HistoryCount int32
	Tail         uint32
}

// VoidSignalPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
// VoidSignalPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
const VoidSignalPacketMaxBits = 84
const VoidSignalPacketMaxBytes = 16

func WriteVoidSignalPacket(stream *serialize.WriteStream, value *VoidSignalPacket) error {
	stream.SerializeBits(&value.Lead, 3)
	if err := WriteVoidSignal(stream, &value.Signal); err != nil {
		return err
	}
	if err := WriteVoidSignal(stream, &value.Echo); err != nil {
		return err
	}
	if value.HistoryCount < 0 || value.HistoryCount > 2 {
		return serialize.ErrValueOutOfRange
	}
	{
		offsetValue := uint32(value.HistoryCount)
		stream.SerializeBits(&offsetValue, 2)
	}
	if stream.Err() != nil { // the count guards the loop (§6.3)
		return stream.Err()
	}
	for i := int32(0); i < value.HistoryCount; i++ {
		if err := WriteVoidSignal(stream, &value.History[i]); err != nil {
			return err
		}
	}
	stream.SerializeBits(&value.Tail, 3)
	return stream.Err()
}

func ReadVoidSignalPacket(stream *serialize.ReadStream, value *VoidSignalPacket) error {
	stream.SerializeBits(&value.Lead, 3)
	if err := ReadVoidSignal(stream, &value.Signal); err != nil {
		return err
	}
	if err := ReadVoidSignal(stream, &value.Echo); err != nil {
		return err
	}
	{
		offsetValue := uint32(0)
		stream.SerializeBits(&offsetValue, 2)
		if stream.Err() != nil {
			return stream.Err()
		}
		if offsetValue > 2 { // a value smuggled into the bit headroom is refused (SPEC §4.3)
			return serialize.ErrValueOutOfRange
		}
		value.HistoryCount = int32(offsetValue)
	}
	for i := int32(0); i < value.HistoryCount; i++ {
		if err := ReadVoidSignal(stream, &value.History[i]); err != nil {
			return err
		}
	}
	stream.SerializeBits(&value.Tail, 3)
	return stream.Err()
}
