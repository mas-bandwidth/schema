// Generated by the schema compiler from Messages.schema. DO NOT EDIT.
// package example — protocol id 0xaacab4aa04d2189b
// The TABLE wire (evolution-tolerant, notes/table-wire.md).

package example

import "math"

// TableWriteHeartbeat appends value's table-wire encoding and returns the buffer.
func TableWriteHeartbeat(value *Heartbeat) []byte {
	w := &tableWriter{}
	tableWriteHeartbeat(w, value)
	return w.buf
}

func tableWriteHeartbeat(w *tableWriter, value *Heartbeat) {
	w.u16(0) // terminator
}

// TableReadHeartbeat decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadHeartbeat(data []byte, value *Heartbeat, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadHeartbeat(r, value)
}

func tableReadHeartbeat(r *tableReader, value *Heartbeat) bool {
	*value = Heartbeat{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}

// TableWriteTest appends value's table-wire encoding and returns the buffer.
func TableWriteTest(value *Test) []byte {
	w := &tableWriter{}
	tableWriteTest(w, value)
	return w.buf
}

func tableWriteTest(w *tableWriter, value *Test) {
	if value.TestA != 0 {
		w.u16(0xcb58) // test_a
		w.u8(7)
		w.u16(uint16(value.TestA))
	}
	if value.TestB != 0 {
		w.u16(0xc3e1) // test_b
		w.u8(3)
		w.u16(uint16(value.TestB))
	}
	if value.TestC != 0 {
		w.u16(0xc472) // test_c
		w.u8(3)
		w.u16(uint16(value.TestC))
	}
	if value.TestD != 0 {
		w.u16(0xdebb) // test_d
		w.u8(3)
		w.u16(uint16(value.TestD))
	}
	w.u16(0) // terminator
}

// TableReadTest decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadTest(data []byte, value *Test, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadTest(r, value)
}

func tableReadTest(r *tableReader, value *Test) bool {
	*value = Test{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		case 0xcb58: // test_a
			if kind != 7 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			decoded := r.get16()
			value.TestA = uint16(decoded)
		case 0xc3e1: // test_b
			if kind != 3 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			decoded := int16(r.get16())
			if decoded < 0 {
				decoded = 0
				r.rep.Clamped++
			} else if decoded > 1000 {
				decoded = 1000
				r.rep.Clamped++
			}
			value.TestB = int16(decoded)
		case 0xc472: // test_c
			if kind != 3 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			decoded := int16(r.get16())
			if decoded < 0 {
				decoded = 0
				r.rep.Clamped++
			} else if decoded > 1000 {
				decoded = 1000
				r.rep.Clamped++
			}
			value.TestC = int16(decoded)
		case 0xdebb: // test_d
			if kind != 3 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			decoded := int16(r.get16())
			if decoded < 0 {
				decoded = 0
				r.rep.Clamped++
			} else if decoded > 1000 {
				decoded = 1000
				r.rep.Clamped++
			}
			value.TestD = int16(decoded)
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}

// TableWriteBlock appends value's table-wire encoding and returns the buffer.
func TableWriteBlock(value *Block) []byte {
	w := &tableWriter{}
	tableWriteBlock(w, value)
	return w.buf
}

func tableWriteBlock(w *tableWriter, value *Block) {
	if value.DataLength > 0 {
		w.u16(0x3ad7) // data
		w.u8(14)
		w.u32(uint32(3 + value.DataLength))
		w.u8(6)
		w.u16(uint16(value.DataLength))
		w.raw(value.Data[:value.DataLength])
	}
	w.u16(0) // terminator
}

// TableReadBlock decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadBlock(data []byte, value *Block, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadBlock(r, value)
}

func tableReadBlock(r *tableReader, value *Block) bool {
	*value = Block{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		case 0x3ad7: // data
			if kind != 14 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(4) {
				r.rep.Malformed = true
				return false
			}
			bodyLen := int(r.get32())
			if !r.has(bodyLen) {
				r.rep.Malformed = true
				return false
			}
			bodyEnd := r.off + bodyLen
			if bodyLen >= 3 {
				elemKind := r.get8()
				count := int(r.get16())
				if elemKind != 6 {
					r.rep.KindMismatch++
					r.off = bodyEnd
					break
				}
				keep := count
				if keep > 2000 {
					keep = 2000
					r.rep.Clamped++
				}
				for i := 0; i < keep; i++ {
					if !r.has(1) {
						r.rep.Malformed = true
						return false
					}
					decoded := r.get8()
					value.Data[i] = uint8(decoded)
				}
				value.DataLength = int32(keep)
			}
			r.off = bodyEnd // excess elements and slack skip via the length
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}

// TableWriteChat appends value's table-wire encoding and returns the buffer.
func TableWriteChat(value *Chat) []byte {
	w := &tableWriter{}
	tableWriteChat(w, value)
	return w.buf
}

func tableWriteChat(w *tableWriter, value *Chat) {
	if value.TextLength > 0 {
		w.u16(0xf3d8) // text
		w.u8(12)
		w.u16(uint16(value.TextLength))
		w.raw(value.Text[:value.TextLength])
	}
	w.u16(0) // terminator
}

// TableReadChat decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadChat(data []byte, value *Chat, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadChat(r, value)
}

func tableReadChat(r *tableReader, value *Chat) bool {
	*value = Chat{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		case 0xf3d8: // text
			if kind != 12 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			length := int(r.get16())
			if !r.has(length) {
				r.rep.Malformed = true
				return false
			}
			keep := length
			if keep > 256 {
				keep = 256
				r.rep.Clamped++
			}
			copy(value.Text[:keep], r.buf[r.off:r.off+keep])
			value.TextLength = int32(keep)
			r.off += length
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}

// TableWriteSynchronize appends value's table-wire encoding and returns the buffer.
func TableWriteSynchronize(value *Synchronize) []byte {
	w := &tableWriter{}
	tableWriteSynchronize(w, value)
	return w.buf
}

func tableWriteSynchronize(w *tableWriter, value *Synchronize) {
	if value.SyncFrame != 0 {
		w.u16(0x6831) // sync_frame
		w.u8(9)
		w.u64(uint64(value.SyncFrame))
	}
	if value.SyncSequence != 0 {
		w.u16(0x03a0) // sync_sequence
		w.u8(7)
		w.u16(uint16(value.SyncSequence))
	}
	w.u16(0) // terminator
}

// TableReadSynchronize decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadSynchronize(data []byte, value *Synchronize, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadSynchronize(r, value)
}

func tableReadSynchronize(r *tableReader, value *Synchronize) bool {
	*value = Synchronize{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		case 0x6831: // sync_frame
			if kind != 9 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(8) {
				r.rep.Malformed = true
				return false
			}
			decoded := r.get64()
			value.SyncFrame = uint64(decoded)
		case 0x03a0: // sync_sequence
			if kind != 7 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(2) {
				r.rep.Malformed = true
				return false
			}
			decoded := r.get16()
			value.SyncSequence = uint16(decoded)
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}

// TableWriteTimescale appends value's table-wire encoding and returns the buffer.
func TableWriteTimescale(value *Timescale) []byte {
	w := &tableWriter{}
	tableWriteTimescale(w, value)
	return w.buf
}

func tableWriteTimescale(w *tableWriter, value *Timescale) {
	if value.Scale != 0 {
		w.u16(0x9ee6) // scale
		w.u8(11)
		w.u64(math.Float64bits(value.Scale))
	}
	if value.FrameA != 0 {
		w.u16(0x49e4) // frame_a
		w.u8(8)
		w.u32(uint32(value.FrameA))
	}
	if value.FrameB != 0 {
		w.u16(0x5651) // frame_b
		w.u8(8)
		w.u32(uint32(value.FrameB))
	}
	w.u16(0) // terminator
}

// TableReadTimescale decodes a table-wire buffer under the permissive contract:
// declared defaults prefill, known fields overlay, unknown ids and kind
// mismatches skip and count, out-of-range values clamp and count. false
// means malformed — the partial decode up to that point is kept.
func TableReadTimescale(data []byte, value *Timescale, report *TableReport) bool {
	r := &tableReader{buf: data, rep: report}
	return tableReadTimescale(r, value)
}

func tableReadTimescale(r *tableReader, value *Timescale) bool {
	*value = Timescale{} // prefill (all-zero defaults), then overlay
	for {
		if !r.has(2) {
			r.rep.Malformed = true
			return false
		}
		fieldId := r.get16()
		if fieldId == 0 {
			return true
		}
		if !r.has(1) {
			r.rep.Malformed = true
			return false
		}
		kind := r.get8()
		switch fieldId {
		case 0x9ee6: // scale
			if kind != 11 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(8) {
				r.rep.Malformed = true
				return false
			}
			value.Scale = math.Float64frombits(r.get64())
		case 0x49e4: // frame_a
			if kind != 8 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(4) {
				r.rep.Malformed = true
				return false
			}
			decoded := r.get32()
			value.FrameA = uint32(decoded)
		case 0x5651: // frame_b
			if kind != 8 {
				r.rep.KindMismatch++
				if !r.skip(kind) {
					r.rep.Malformed = true
					return false
				}
				break
			}
			if !r.has(4) {
				r.rep.Malformed = true
				return false
			}
			decoded := r.get32()
			value.FrameB = uint32(decoded)
		default:
			r.rep.Unknown++
			if !r.skip(kind) {
				r.rep.Malformed = true
				return false
			}
		}
	}
}
