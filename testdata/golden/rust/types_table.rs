// Generated by the schema compiler from Types.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x39e4aa9c08faf8f8
// The TABLE wire (evolution-tolerant, notes/table-wire.md).

#![allow(clippy::needless_range_loop)]

use crate::*;

/// Returns `value`'s table-wire encoding in a fresh buffer.
pub fn table_write_vec3(value: &Vec3) -> Vec<u8> {
    append_table_vec3(Vec::new(), value)
}

/// Appends `value`'s table-wire encoding to `dst` and returns the extended
/// buffer — the zero-allocation write path: hand it a buffer you reuse
/// (`v.clear()` then pass `v`) and steady-state writes never touch the heap.
pub fn append_table_vec3(dst: Vec<u8>, value: &Vec3) -> Vec<u8> {
    let mut w = TableWriter { buf: dst };
    table_write_vec3_into(&mut w, value);
    w.buf
}

fn table_write_vec3_into(w: &mut TableWriter, value: &Vec3) {
    if value.x != 0.0 {
        w.u16(0xad8b); // x
        w.u8(11);
        w.u64(f64::to_bits(value.x));
    }
    if value.y != 0.0 {
        w.u16(0xb2f8); // y
        w.u8(11);
        w.u64(f64::to_bits(value.y));
    }
    if value.z != 0.0 {
        w.u16(0xaca1); // z
        w.u8(11);
        w.u64(f64::to_bits(value.z));
    }
    w.u16(0); // terminator
}

/// Decodes a table-wire buffer under the permissive contract: declared
/// defaults prefill, known fields overlay, unknown ids and kind mismatches
/// skip and count, out-of-range values clamp and count. `false` means
/// malformed — the partial decode up to that point is kept.
pub fn table_read_vec3(data: &[u8], value: &mut Vec3, report: &mut TableReport) -> bool {
    let mut r = TableReader { buf: data, off: 0 };
    table_read_vec3_from(&mut r, value, report)
}

fn table_read_vec3_from(r: &mut TableReader, value: &mut Vec3, report: &mut TableReport) -> bool {
    *value = Vec3::default(); // prefill (all-zero defaults), then overlay
    loop {
        if !r.has(2) { report.malformed = true; return false; }
        let field_id = r.get16();
        if field_id == 0 { return true; }
        if !r.has(1) { report.malformed = true; return false; }
        let kind = r.get8();
        match field_id {
            0xad8b => { // x
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.x = raw;
                }
            }
            0xb2f8 => { // y
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.y = raw;
                }
            }
            0xaca1 => { // z
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.z = raw;
                }
            }
            _ => {
                report.unknown += 1;
                if !r.skip(kind) { report.malformed = true; return false; }
            }
        }
    }
}

/// Returns `value`'s table-wire encoding in a fresh buffer.
pub fn table_write_quat(value: &Quat) -> Vec<u8> {
    append_table_quat(Vec::new(), value)
}

/// Appends `value`'s table-wire encoding to `dst` and returns the extended
/// buffer — the zero-allocation write path: hand it a buffer you reuse
/// (`v.clear()` then pass `v`) and steady-state writes never touch the heap.
pub fn append_table_quat(dst: Vec<u8>, value: &Quat) -> Vec<u8> {
    let mut w = TableWriter { buf: dst };
    table_write_quat_into(&mut w, value);
    w.buf
}

fn table_write_quat_into(w: &mut TableWriter, value: &Quat) {
    if value.x != 0.0 {
        w.u16(0xad8b); // x
        w.u8(11);
        w.u64(f64::to_bits(value.x));
    }
    if value.y != 0.0 {
        w.u16(0xb2f8); // y
        w.u8(11);
        w.u64(f64::to_bits(value.y));
    }
    if value.z != 0.0 {
        w.u16(0xaca1); // z
        w.u8(11);
        w.u64(f64::to_bits(value.z));
    }
    if value.w != 0.0 {
        w.u16(0xcd3a); // w
        w.u8(11);
        w.u64(f64::to_bits(value.w));
    }
    w.u16(0); // terminator
}

/// Decodes a table-wire buffer under the permissive contract: declared
/// defaults prefill, known fields overlay, unknown ids and kind mismatches
/// skip and count, out-of-range values clamp and count. `false` means
/// malformed — the partial decode up to that point is kept.
pub fn table_read_quat(data: &[u8], value: &mut Quat, report: &mut TableReport) -> bool {
    let mut r = TableReader { buf: data, off: 0 };
    table_read_quat_from(&mut r, value, report)
}

fn table_read_quat_from(r: &mut TableReader, value: &mut Quat, report: &mut TableReport) -> bool {
    *value = Quat::default(); // prefill (all-zero defaults), then overlay
    loop {
        if !r.has(2) { report.malformed = true; return false; }
        let field_id = r.get16();
        if field_id == 0 { return true; }
        if !r.has(1) { report.malformed = true; return false; }
        let kind = r.get8();
        match field_id {
            0xad8b => { // x
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.x = raw;
                }
            }
            0xb2f8 => { // y
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.y = raw;
                }
            }
            0xaca1 => { // z
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.z = raw;
                }
            }
            0xcd3a => { // w
                if kind != 11 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(8) { report.malformed = true; return false; }
                    let raw = f64::from_bits(r.get64());
                    value.w = raw;
                }
            }
            _ => {
                report.unknown += 1;
                if !r.skip(kind) { report.malformed = true; return false; }
            }
        }
    }
}

/// Returns `value`'s table-wire encoding in a fresh buffer.
pub fn table_write_rigid_body(value: &RigidBody) -> Vec<u8> {
    append_table_rigid_body(Vec::new(), value)
}

/// Appends `value`'s table-wire encoding to `dst` and returns the extended
/// buffer — the zero-allocation write path: hand it a buffer you reuse
/// (`v.clear()` then pass `v`) and steady-state writes never touch the heap.
pub fn append_table_rigid_body(dst: Vec<u8>, value: &RigidBody) -> Vec<u8> {
    let mut w = TableWriter { buf: dst };
    table_write_rigid_body_into(&mut w, value);
    w.buf
}

fn table_write_rigid_body_into(w: &mut TableWriter, value: &RigidBody) {
    {
        let field_at = w.buf.len();
        w.u16(0xdd45); // position
        w.u8(13);
        let len_at = w.buf.len();
        w.u32(0);
        table_write_vec3_into(w, &value.position);
        if w.buf.len() - len_at - 4 <= 2 {
            w.buf.truncate(field_at); // all-default nested elides
        } else {
            let body = (w.buf.len() - len_at - 4) as u32;
            w.patch32(len_at, body);
        }
    }
    {
        let field_at = w.buf.len();
        w.u16(0x7964); // orientation
        w.u8(13);
        let len_at = w.buf.len();
        w.u32(0);
        table_write_quat_into(w, &value.orientation);
        if w.buf.len() - len_at - 4 <= 2 {
            w.buf.truncate(field_at); // all-default nested elides
        } else {
            let body = (w.buf.len() - len_at - 4) as u32;
            w.patch32(len_at, body);
        }
    }
    if value.at_rest != false {
        w.u16(0xf9eb); // at_rest
        w.u8(1);
        w.u8(if value.at_rest { 1 } else { 0 });
    }
    if !value.at_rest {
        {
            let field_at = w.buf.len();
            w.u16(0x0637); // linear_velocity
            w.u8(13);
            let len_at = w.buf.len();
            w.u32(0);
            table_write_vec3_into(w, &value.linear_velocity);
            if w.buf.len() - len_at - 4 <= 2 {
                w.buf.truncate(field_at); // all-default nested elides
            } else {
                let body = (w.buf.len() - len_at - 4) as u32;
                w.patch32(len_at, body);
            }
        }
    }
    if !value.at_rest {
        {
            let field_at = w.buf.len();
            w.u16(0x01a7); // angular_velocity
            w.u8(13);
            let len_at = w.buf.len();
            w.u32(0);
            table_write_vec3_into(w, &value.angular_velocity);
            if w.buf.len() - len_at - 4 <= 2 {
                w.buf.truncate(field_at); // all-default nested elides
            } else {
                let body = (w.buf.len() - len_at - 4) as u32;
                w.patch32(len_at, body);
            }
        }
    }
    w.u16(0); // terminator
}

/// Decodes a table-wire buffer under the permissive contract: declared
/// defaults prefill, known fields overlay, unknown ids and kind mismatches
/// skip and count, out-of-range values clamp and count. `false` means
/// malformed — the partial decode up to that point is kept.
pub fn table_read_rigid_body(data: &[u8], value: &mut RigidBody, report: &mut TableReport) -> bool {
    let mut r = TableReader { buf: data, off: 0 };
    table_read_rigid_body_from(&mut r, value, report)
}

fn table_read_rigid_body_from(r: &mut TableReader, value: &mut RigidBody, report: &mut TableReport) -> bool {
    *value = RigidBody::default(); // prefill (all-zero defaults), then overlay
    loop {
        if !r.has(2) { report.malformed = true; return false; }
        let field_id = r.get16();
        if field_id == 0 { return true; }
        if !r.has(1) { report.malformed = true; return false; }
        let kind = r.get8();
        match field_id {
            0xdd45 => { // position
                if kind != 13 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let body_len = r.get32() as usize;
                    if !r.has(body_len) { report.malformed = true; return false; }
                    {
                        let mut sub = TableReader { buf: &r.buf[r.off..r.off + body_len], off: 0 };
                        table_read_vec3_from(&mut sub, &mut value.position, report);
                    }
                    r.off += body_len;
                }
            }
            0x7964 => { // orientation
                if kind != 13 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let body_len = r.get32() as usize;
                    if !r.has(body_len) { report.malformed = true; return false; }
                    {
                        let mut sub = TableReader { buf: &r.buf[r.off..r.off + body_len], off: 0 };
                        table_read_quat_from(&mut sub, &mut value.orientation, report);
                    }
                    r.off += body_len;
                }
            }
            0xf9eb => { // at_rest
                if kind != 1 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(1) { report.malformed = true; return false; }
                    value.at_rest = r.get8() != 0;
                }
            }
            0x0637 => { // linear_velocity
                if kind != 13 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let body_len = r.get32() as usize;
                    if !r.has(body_len) { report.malformed = true; return false; }
                    {
                        let mut sub = TableReader { buf: &r.buf[r.off..r.off + body_len], off: 0 };
                        table_read_vec3_from(&mut sub, &mut value.linear_velocity, report);
                    }
                    r.off += body_len;
                }
            }
            0x01a7 => { // angular_velocity
                if kind != 13 {
                    report.kind_mismatch += 1;
                    if !r.skip(kind) { report.malformed = true; return false; }
                } else {
                    if !r.has(4) { report.malformed = true; return false; }
                    let body_len = r.get32() as usize;
                    if !r.has(body_len) { report.malformed = true; return false; }
                    {
                        let mut sub = TableReader { buf: &r.buf[r.off..r.off + body_len], off: 0 };
                        table_read_vec3_from(&mut sub, &mut value.angular_velocity, report);
                    }
                    r.off += body_len;
                }
            }
            _ => {
                report.unknown += 1;
                if !r.skip(kind) { report.malformed = true; return false; }
            }
        }
    }
}

// ---- reflection descriptors (tables only, notes/table-wire.md) ----
//
// Descriptors are 'static data, so table_type_* costs nothing per instance
// and needs no lazy initialization: Rust resolves the cross-type links at
// compile time, which is the one place this is simpler than the C#/Go
// backends, where the same links are wired at startup.

static TABLE_TYPE_VEC3_FIELDS: &[TableFieldInfo] = &[
    TableFieldInfo {
        name: "x",
        type_name: "float64",
        id: 0xad8b,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "y",
        type_name: "float64",
        id: 0xb2f8,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "z",
        type_name: "float64",
        id: 0xaca1,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
];

static TABLE_TYPE_VEC3: TableTypeInfo = TableTypeInfo {
    name: "Vec3",
    fields: TABLE_TYPE_VEC3_FIELDS,
};

/// Returns `Vec3`'s reflection descriptor — field names, wire ids/kinds,
/// bounds, ranges, enum names and branch guards. Static data: no
/// per-instance cost and no lazy initialization.
pub fn table_type_vec3() -> &'static TableTypeInfo { &TABLE_TYPE_VEC3 }

static TABLE_TYPE_QUAT_FIELDS: &[TableFieldInfo] = &[
    TableFieldInfo {
        name: "x",
        type_name: "float64",
        id: 0xad8b,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "y",
        type_name: "float64",
        id: 0xb2f8,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "z",
        type_name: "float64",
        id: 0xaca1,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "w",
        type_name: "float64",
        id: 0xcd3a,
        kind: 11,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
];

static TABLE_TYPE_QUAT: TableTypeInfo = TableTypeInfo {
    name: "Quat",
    fields: TABLE_TYPE_QUAT_FIELDS,
};

/// Returns `Quat`'s reflection descriptor — field names, wire ids/kinds,
/// bounds, ranges, enum names and branch guards. Static data: no
/// per-instance cost and no lazy initialization.
pub fn table_type_quat() -> &'static TableTypeInfo { &TABLE_TYPE_QUAT }

static TABLE_TYPE_RIGID_BODY_FIELDS: &[TableFieldInfo] = &[
    TableFieldInfo {
        name: "position",
        type_name: "Vec3",
        id: 0xdd45,
        kind: 13,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: Some(&TABLE_TYPE_VEC3),
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "orientation",
        type_name: "Quat",
        id: 0x7964,
        kind: 13,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: Some(&TABLE_TYPE_QUAT),
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "at_rest",
        type_name: "bool",
        id: 0xf9eb,
        kind: 1,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: None,
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "",
    },
    TableFieldInfo {
        name: "linear_velocity",
        type_name: "Vec3",
        id: 0x0637,
        kind: 13,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: Some(&TABLE_TYPE_VEC3),
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "!at_rest",
    },
    TableFieldInfo {
        name: "angular_velocity",
        type_name: "Vec3",
        id: 0x01a7,
        kind: 13,
        is_array: false,
        counted: false,
        array_bound: 0,
        table: Some(&TABLE_TYPE_VEC3),
        has_range: false,
        range_min: 0.0,
        range_max: 0.0,
        enum_max: -1,
        enum_name: None,
        guard: "!at_rest",
    },
];

static TABLE_TYPE_RIGID_BODY: TableTypeInfo = TableTypeInfo {
    name: "RigidBody",
    fields: TABLE_TYPE_RIGID_BODY_FIELDS,
};

/// Returns `RigidBody`'s reflection descriptor — field names, wire ids/kinds,
/// bounds, ranges, enum names and branch guards. Static data: no
/// per-instance cost and no lazy initialization.
pub fn table_type_rigid_body() -> &'static TableTypeInfo { &TABLE_TYPE_RIGID_BODY }

