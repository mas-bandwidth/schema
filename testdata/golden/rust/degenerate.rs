// Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

use crate::*;
use serialize::{ReadStream, Stream, WriteStream};

// type Vec2
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Vec2 {
    pub x: f64,
    pub y: f64,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Vec2 {
    fn default() -> Self {
        Vec2 {
            x: 0.0,
            y: 0.0,
        }
    }
}

// VEC2_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// VEC2_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const VEC2_MAX_BITS: u64 = 128;
pub const VEC2_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_vec2(stream: &mut WriteStream<'_>, value: &Vec2) -> Result {
    let f0: u64 = value.x.to_bits();
    let f1: u64 = value.y.to_bits();
    let mut w0 = f0 as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = (f0 >> 32) as u32;
    stream.serialize_bits(&mut w1, 32)?;
    let mut w2 = f1 as u32;
    stream.serialize_bits(&mut w2, 32)?;
    let mut w3 = (f1 >> 32) as u32;
    stream.serialize_bits(&mut w3, 32)?;
    Ok(())
}

#[inline]
pub fn read_vec2(stream: &mut ReadStream<'_>, value: &mut Vec2) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c2 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c3 = u64::from(c);
    let v0: u64 = c0 | (c1 << 32);
    value.x = f64::from_bits(v0);
    let v1: u64 = c2 | (c3 << 32);
    value.y = f64::from_bits(v1);
    Ok(())
}

// type SpanF64
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanF64 {
    pub values: [f64; 2],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanF64 {
    fn default() -> Self {
        SpanF64 {
            values: [0.0; 2],
        }
    }
}

// SPAN_F64_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_F64_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_F64_MAX_BITS: u64 = 128;
pub const SPAN_F64_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_span_f64(stream: &mut WriteStream<'_>, value: &SpanF64) -> Result {
    for i in 0..2 {
        {
            let mut float_value = value.values[i];
            stream.serialize_f64(&mut float_value)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_f64(stream: &mut ReadStream<'_>, value: &mut SpanF64) -> Result {
    for i in 0..2 {
        stream.serialize_f64(&mut value.values[i])?;
    }
    Ok(())
}

// type SpanU64
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanU64 {
    pub values: [u64; 2],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanU64 {
    fn default() -> Self {
        SpanU64 {
            values: [0; 2],
        }
    }
}

// SPAN_U64_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_U64_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_U64_MAX_BITS: u64 = 128;
pub const SPAN_U64_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_span_u64(stream: &mut WriteStream<'_>, value: &SpanU64) -> Result {
    for i in 0..2 {
        {
            let mut raw_value = value.values[i];
            stream.serialize_bits64(&mut raw_value, 64)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_u64(stream: &mut ReadStream<'_>, value: &mut SpanU64) -> Result {
    for i in 0..2 {
        stream.serialize_bits64(&mut value.values[i], 64)?;
    }
    Ok(())
}

// type SpanI64
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanI64 {
    pub values: [i64; 2],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanI64 {
    fn default() -> Self {
        SpanI64 {
            values: [0; 2],
        }
    }
}

// SPAN_I64_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_I64_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_I64_MAX_BITS: u64 = 128;
pub const SPAN_I64_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_span_i64(stream: &mut WriteStream<'_>, value: &SpanI64) -> Result {
    for i in 0..2 {
        {
            let mut raw_value = value.values[i] as u64;
            stream.serialize_bits64(&mut raw_value, 64)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_i64(stream: &mut ReadStream<'_>, value: &mut SpanI64) -> Result {
    for i in 0..2 {
        {
            let mut raw_value: u64 = 0;
            stream.serialize_bits64(&mut raw_value, 64)?;
            value.values[i] = raw_value as i64;
        }
    }
    Ok(())
}

// type SpanOne
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanOne {
    pub values: [u64; 1],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanOne {
    fn default() -> Self {
        SpanOne {
            values: [0; 1],
        }
    }
}

// SPAN_ONE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_ONE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_ONE_MAX_BITS: u64 = 64;
pub const SPAN_ONE_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_span_one(stream: &mut WriteStream<'_>, value: &SpanOne) -> Result {
    for i in 0..1 {
        {
            let mut raw_value = value.values[i];
            stream.serialize_bits64(&mut raw_value, 64)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_one(stream: &mut ReadStream<'_>, value: &mut SpanOne) -> Result {
    for i in 0..1 {
        stream.serialize_bits64(&mut value.values[i], 64)?;
    }
    Ok(())
}

// type SpanChunk
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanChunk {
    pub values: [u16; 4],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanChunk {
    fn default() -> Self {
        SpanChunk {
            values: [0; 4],
        }
    }
}

// SPAN_CHUNK_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_CHUNK_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_CHUNK_MAX_BITS: u64 = 64;
pub const SPAN_CHUNK_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_span_chunk(stream: &mut WriteStream<'_>, value: &SpanChunk) -> Result {
    for i in 0..4 {
        {
            let mut raw_value = value.values[i] as u32;
            stream.serialize_bits(&mut raw_value, 16)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_chunk(stream: &mut ReadStream<'_>, value: &mut SpanChunk) -> Result {
    for i in 0..4 {
        {
            let mut raw_value: u32 = 0;
            stream.serialize_bits(&mut raw_value, 16)?;
            value.values[i] = raw_value as u16;
        }
    }
    Ok(())
}

// type SpanTail
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanTail {
    pub values: [f64; 2],
    pub tail: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanTail {
    fn default() -> Self {
        SpanTail {
            values: [0.0; 2],
            tail: 0,
        }
    }
}

// SPAN_TAIL_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_TAIL_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_TAIL_MAX_BITS: u64 = 160;
pub const SPAN_TAIL_MAX_BYTES: usize = 24;

#[inline(always)]
pub fn write_span_tail(stream: &mut WriteStream<'_>, value: &SpanTail) -> Result {
    for i in 0..2 {
        {
            let mut float_value = value.values[i];
            stream.serialize_f64(&mut float_value)?;
        }
    }
    {
        let mut raw_value = value.tail;
        stream.serialize_bits(&mut raw_value, 32)?;
    }
    Ok(())
}

#[inline]
pub fn read_span_tail(stream: &mut ReadStream<'_>, value: &mut SpanTail) -> Result {
    for i in 0..2 {
        stream.serialize_f64(&mut value.values[i])?;
    }
    stream.serialize_bits(&mut value.tail, 32)?;
    Ok(())
}

// type SpanTwice
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct SpanTwice {
    pub a: [f64; 2],
    pub b: [f64; 2],
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for SpanTwice {
    fn default() -> Self {
        SpanTwice {
            a: [0.0; 2],
            b: [0.0; 2],
        }
    }
}

// SPAN_TWICE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// SPAN_TWICE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const SPAN_TWICE_MAX_BITS: u64 = 256;
pub const SPAN_TWICE_MAX_BYTES: usize = 32;

#[inline(always)]
pub fn write_span_twice(stream: &mut WriteStream<'_>, value: &SpanTwice) -> Result {
    for i in 0..2 {
        {
            let mut float_value = value.a[i];
            stream.serialize_f64(&mut float_value)?;
        }
    }
    for i in 0..2 {
        {
            let mut float_value = value.b[i];
            stream.serialize_f64(&mut float_value)?;
        }
    }
    Ok(())
}

#[inline]
pub fn read_span_twice(stream: &mut ReadStream<'_>, value: &mut SpanTwice) -> Result {
    for i in 0..2 {
        stream.serialize_f64(&mut value.a[i])?;
    }
    for i in 0..2 {
        stream.serialize_f64(&mut value.b[i])?;
    }
    Ok(())
}

// type Trio
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct Trio {
    pub a: u32,
    pub b: u32,
    pub c: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for Trio {
    fn default() -> Self {
        Trio {
            a: 0,
            b: 0,
            c: 0,
        }
    }
}

// TRIO_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TRIO_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TRIO_MAX_BITS: u64 = 64;
pub const TRIO_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_trio(stream: &mut WriteStream<'_>, value: &Trio) -> Result {
    if value.a >= 1 << 20 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.b >= 1 << 20 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    if value.c >= 1 << 24 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = u64::from(value.a);
    let f1: u64 = u64::from(value.b);
    let f2: u64 = u64::from(value.c);
    let mut w0 = (f0 | (f1 << 20)) as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = ((f1 >> 12) | (f2 << 8)) as u32;
    stream.serialize_bits(&mut w1, 32)?;
    Ok(())
}

#[inline]
pub fn read_trio(stream: &mut ReadStream<'_>, value: &mut Trio) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    let v0: u64 = c0 & 0xfffff;
    value.a = v0 as u32;
    let v1: u64 = ((c0 >> 20) | (c1 << 12)) & 0xfffff;
    value.b = v1 as u32;
    let v2: u64 = (c1 >> 8) & 0xffffff;
    value.c = v2 as u32;
    Ok(())
}

// type TrioSole
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TrioSole {
    pub inner: Trio,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TrioSole {
    fn default() -> Self {
        TrioSole {
            inner: Trio::default(),
        }
    }
}

// TRIO_SOLE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TRIO_SOLE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TRIO_SOLE_MAX_BITS: u64 = 64;
pub const TRIO_SOLE_MAX_BYTES: usize = 8;

#[inline(always)]
pub fn write_trio_sole(stream: &mut WriteStream<'_>, value: &TrioSole) -> Result {
    write_trio(stream, &value.inner)?;
    Ok(())
}

#[inline]
pub fn read_trio_sole(stream: &mut ReadStream<'_>, value: &mut TrioSole) -> Result {
    read_trio(stream, &mut value.inner)?;
    Ok(())
}

// type TrioFirst
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TrioFirst {
    pub inner: Trio,
    pub trailer: u32,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TrioFirst {
    fn default() -> Self {
        TrioFirst {
            inner: Trio::default(),
            trailer: 0,
        }
    }
}

// TRIO_FIRST_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TRIO_FIRST_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TRIO_FIRST_MAX_BITS: u64 = 80;
pub const TRIO_FIRST_MAX_BYTES: usize = 16;

#[inline(always)]
pub fn write_trio_first(stream: &mut WriteStream<'_>, value: &TrioFirst) -> Result {
    write_trio(stream, &value.inner)?;
    if value.trailer >= 1 << 16 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    {
        let mut raw_value = value.trailer;
        stream.serialize_bits(&mut raw_value, 16)?;
    }
    Ok(())
}

#[inline]
pub fn read_trio_first(stream: &mut ReadStream<'_>, value: &mut TrioFirst) -> Result {
    read_trio(stream, &mut value.inner)?;
    stream.serialize_bits(&mut value.trailer, 16)?;
    Ok(())
}

// type TrioStraddle
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Debug)]
pub struct TrioStraddle {
    pub pad0: u64,
    pub pad1: u64,
    pub pad2: u64,
    pub pad3: u64,
    pub pad4: u64,
    pub pad5: u32,
    pub inner: Trio,
}

// The zero form (SPEC §5): specified defaults live only in new(), never here.
impl Default for TrioStraddle {
    fn default() -> Self {
        TrioStraddle {
            pad0: 0,
            pad1: 0,
            pad2: 0,
            pad3: 0,
            pad4: 0,
            pad5: 0,
            inner: Trio::default(),
        }
    }
}

// TRIO_STRADDLE_MAX_BITS is the longest wire path; align pads at worst case (SPEC §6.1).
// TRIO_STRADDLE_MAX_BYTES is rounded up to the 8-byte write-buffer granularity.
pub const TRIO_STRADDLE_MAX_BITS: u64 = 408;
pub const TRIO_STRADDLE_MAX_BYTES: usize = 56;

#[inline(always)]
pub fn write_trio_straddle(stream: &mut WriteStream<'_>, value: &TrioStraddle) -> Result {
    let f0: u64 = value.pad0;
    let f1: u64 = value.pad1;
    let f2: u64 = value.pad2;
    let f3: u64 = value.pad3;
    let mut w0 = f0 as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = (f0 >> 32) as u32;
    stream.serialize_bits(&mut w1, 32)?;
    let mut w2 = f1 as u32;
    stream.serialize_bits(&mut w2, 32)?;
    let mut w3 = (f1 >> 32) as u32;
    stream.serialize_bits(&mut w3, 32)?;
    let mut w4 = f2 as u32;
    stream.serialize_bits(&mut w4, 32)?;
    let mut w5 = (f2 >> 32) as u32;
    stream.serialize_bits(&mut w5, 32)?;
    let mut w6 = f3 as u32;
    stream.serialize_bits(&mut w6, 32)?;
    let mut w7 = (f3 >> 32) as u32;
    stream.serialize_bits(&mut w7, 32)?;
    if value.pad5 >= 1 << 24 {
        return Err(Error::Stream(serialize::Error::ValueOutOfRange));
    }
    let f0: u64 = value.pad4;
    let f1: u64 = u64::from(value.pad5);
    let mut w0 = f0 as u32;
    stream.serialize_bits(&mut w0, 32)?;
    let mut w1 = (f0 >> 32) as u32;
    stream.serialize_bits(&mut w1, 32)?;
    let mut w2 = f1 as u32;
    stream.serialize_bits(&mut w2, 24)?;
    write_trio(stream, &value.inner)?;
    Ok(())
}

#[inline]
pub fn read_trio_straddle(stream: &mut ReadStream<'_>, value: &mut TrioStraddle) -> Result {
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c2 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c3 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c4 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c5 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c6 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c7 = u64::from(c);
    let v0: u64 = c0 | (c1 << 32);
    value.pad0 = v0;
    let v1: u64 = c2 | (c3 << 32);
    value.pad1 = v1;
    let v2: u64 = c4 | (c5 << 32);
    value.pad2 = v2;
    let v3: u64 = c6 | (c7 << 32);
    value.pad3 = v3;
    let mut c: u32 = 0;
    stream.serialize_bits(&mut c, 32)?;
    let c0 = u64::from(c);
    stream.serialize_bits(&mut c, 32)?;
    let c1 = u64::from(c);
    stream.serialize_bits(&mut c, 24)?;
    let c2 = u64::from(c);
    let v0: u64 = c0 | (c1 << 32);
    value.pad4 = v0;
    let v1: u64 = c2 & 0xffffff;
    value.pad5 = v1 as u32;
    read_trio(stream, &mut value.inner)?;
    Ok(())
}

