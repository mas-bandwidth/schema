// Code generated by the schema compiler from Constants.schema. DO NOT EDIT.
// package example — protocol id 0xbc05d83a8135cdb9

// The unit's protocol id — the hash of its schema files (SPEC §3.1). Two
// sides at the same id speak identical bits; there is no other versioning.
pub const PROTOCOL_ID: u64 = 0xbc05d83a8135cdb9;

/// The generated crate's error: the runtime's own errors pass through;
/// Validation is a read rejecting the wire (SPEC §4.3, §4.7).
#[derive(Clone, Copy, PartialEq, Eq, Debug)]
pub enum Error {
    Stream(serialize::Error),
    Validation,
}

impl From<serialize::Error> for Error {
    fn from(e: serialize::Error) -> Error {
        Error::Stream(e)
    }
}

// serialize.rs 2.0.0 writes are infallible: their Result carries an
// uninhabited error, and `?` on one needs this vacuous conversion.
impl From<core::convert::Infallible> for Error {
    fn from(e: core::convert::Infallible) -> Error {
        match e {}
    }
}

pub type Result<T = ()> = core::result::Result<T, Error>;

pub const POSITION_UNITS: i64 = 1024;

pub const VELOCITY_UNITS: i64 = 1024;

pub const ROTATION_UNITS: i64 = 1024;

pub const MAX_WORLD_METERS: i64 = 8192;

pub const MAX_POSITION_UNITS: i64 = MAX_WORLD_METERS * POSITION_UNITS;

pub const MAX_SPEED_METERS: i64 = 2048;

pub const MAX_VELOCITY_UNITS: i64 = MAX_SPEED_METERS * VELOCITY_UNITS;

pub const MAX_HEALTH: i64 = 1000;

pub const MAX_OBJECTS: i64 = 10000;

pub const SHIP_MAX_LASERS: i64 = 16;

pub const SHIP_MAX_MISSILES: i64 = 16;

pub const MAX_TURRETS_PER_SHIP: i64 = 256;

pub const MAX_COLLIDERS_PER_SHIP: i64 = 64;

pub const NUM_TEAMS: i64 = 2; // = Team.Max

pub const MAX_INPUTS_PER_PACKET: i64 = 16;

pub const MAX_BLOCK_SIZE: i64 = 2000;

pub const MAX_CHAT_LENGTH: i64 = 256;

