// Code generated by the schema compiler from Enums.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x91a8e85156dfe2b1

// Team — None = 0 implicit, variants dense from 1, wire range [0, 2] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct Team(pub u8);

impl Team {
    pub const NONE: Team = Team(0);
    pub const RED: Team = Team(1);
    pub const BLUE: Team = Team(2);
    pub const MAX: Team = Team(2); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `Team` value, out-of-set included.
pub fn enum_name_team(value: Team) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Red",
        2 => "Blue",
        _ => "???",
    }
}

// ShipType — None = 0 implicit, variants dense from 1, wire range [0, 5] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct ShipType(pub u8);

impl ShipType {
    pub const NONE: ShipType = ShipType(0);
    pub const FIGHTER: ShipType = ShipType(1);
    pub const CORVETTE: ShipType = ShipType(2);
    pub const BOMBER: ShipType = ShipType(3);
    pub const DESTROYER: ShipType = ShipType(4);
    pub const CARRIER: ShipType = ShipType(5);
    pub const MAX: ShipType = ShipType(5); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `ShipType` value, out-of-set included.
pub fn enum_name_ship_type(value: ShipType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Fighter",
        2 => "Corvette",
        3 => "Bomber",
        4 => "Destroyer",
        5 => "Carrier",
        _ => "???",
    }
}

// MissileType — None = 0 implicit, variants dense from 1, wire range [0, 3] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct MissileType(pub u8);

impl MissileType {
    pub const NONE: MissileType = MissileType(0);
    pub const HEATSEEKER: MissileType = MissileType(1);
    pub const TORPEDO: MissileType = MissileType(2);
    pub const NUKE: MissileType = MissileType(3);
    pub const MAX: MissileType = MissileType(3); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `MissileType` value, out-of-set included.
pub fn enum_name_missile_type(value: MissileType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Heatseeker",
        2 => "Torpedo",
        3 => "Nuke",
        _ => "???",
    }
}

// PropType — None = 0 implicit, variants dense from 1, wire range [0, 6] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct PropType(pub u8);

impl PropType {
    pub const NONE: PropType = PropType(0);
    pub const ASTEROID: PropType = PropType(1);
    pub const CHUNK: PropType = PropType(2);
    pub const FRAGMENT: PropType = PropType(3);
    pub const SPHERE: PropType = PropType(4);
    pub const BLACK_HOLE: PropType = PropType(5);
    pub const DYSON_PANEL: PropType = PropType(6);
    pub const MAX: PropType = PropType(6); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `PropType` value, out-of-set included.
pub fn enum_name_prop_type(value: PropType) -> &'static str {
    match value.0 {
        0 => "None",
        1 => "Asteroid",
        2 => "Chunk",
        3 => "Fragment",
        4 => "Sphere",
        5 => "BlackHole",
        6 => "DysonPanel",
        _ => "???",
    }
}

// Pending — None = 0 implicit, variants dense from 1, wire range [0, 0] (SPEC §4.2);
// a newtype because | max = ... headroom makes non-variant values wire-legal
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, Default)]
pub struct Pending(pub u8);

impl Pending {
    pub const NONE: Pending = Pending(0);
    pub const MAX: Pending = Pending(0); // the exported extent (SPEC §4.2)
}

/// Debug/log name for any `Pending` value, out-of-set included.
pub fn enum_name_pending(value: Pending) -> &'static str {
    match value.0 {
        0 => "None",
        _ => "???",
    }
}

// ShipFlags — one bit per variant, consumed as masks; storage u64 in every
// target, wire 4 bits (SPEC §4.2)
pub type ShipFlags = u64;

pub const SHIP_FLAGS_FIRING_LASER: ShipFlags = 1 << 0;
pub const SHIP_FLAGS_BOOSTING: ShipFlags = 1 << 1;
pub const SHIP_FLAGS_BRAKING: ShipFlags = 1 << 2;
pub const SHIP_FLAGS_AIMING: ShipFlags = 1 << 3;
pub const SHIP_FLAGS_COUNT: i64 = 4; // the declared variant count (SPEC §4.2)

/// Debug/log name for bit `i` of `ShipFlags` — out-of-range bits name as `"???"`.
pub fn flag_name_ship_flags(bit: u32) -> &'static str {
    match bit {
        0 => "FiringLaser",
        1 => "Boosting",
        2 => "Braking",
        3 => "Aiming",
        _ => "???",
    }
}

/// Renders the set bits of `value` as `"A|B"` — `"0"` for the empty set,
/// bits past the declared variants as hex.
pub fn flag_names_ship_flags(value: ShipFlags) -> String {
    let mut names = String::new();
    if value & (1 << 0) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_ship_flags(0));
    }
    if value & (1 << 1) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_ship_flags(1));
    }
    if value & (1 << 2) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_ship_flags(2));
    }
    if value & (1 << 3) != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(flag_name_ship_flags(3));
    }
    if value >> 4 != 0 {
        if !names.is_empty() {
            names.push('|');
        }
        names.push_str(&format!("{:#x}", (value >> 4) << 4));
    }
    if names.is_empty() {
        names.push('0');
    }
    names
}

