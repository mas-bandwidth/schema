// Code generated by the schema compiler from ArmDefaults.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

package example;

// ArmDefaults carries every generated declaration of ArmDefaults.schema — Java has no
// top-level functions or constants, so the file's one public class is their
// home; types nest inside it (SPEC §6.1 naming).
public final class ArmDefaults {
    private ArmDefaults() {}

    // 64-bit little-endian word access into byte[] — the proven-fast JVM
    // form (VarHandle byteArrayViewVarHandle, serialize.java's own).
    private static final java.lang.invoke.VarHandle LONG_LE =
            java.lang.invoke.MethodHandles.byteArrayViewVarHandle(
                    long[].class, java.nio.ByteOrder.LITTLE_ENDIAN);

    // type DefaultArm
    public static final class DefaultArm {
        public final Wire.ProbeConfig[] entries = new Wire.ProbeConfig[2];
        public int entriesCount;
        // wire [0, 7]
        public byte marker;

        // pre-allocated element instances — the storage principle (SPEC §6.1):
        // every buffer exists at construction
        public DefaultArm() {
            for (int i = 0; i < entries.length; i++) {
                entries[i] = new Wire.ProbeConfig();
            }
        }
    }

    // defaultArmMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // defaultArmMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int defaultArmMaxBits = 77;
    public static final int defaultArmMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroDefaultArm(DefaultArm value) {
        for (int i0 = 0; i0 < 2; i0++) {
            Wire.zeroProbeConfig(value.entries[i0]);
        }
        value.entriesCount = 0;
        value.marker = 0;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initDefaultArm(DefaultArm value) {
        for (int i0 = 0; i0 < 2; i0++) {
            Wire.initProbeConfig(value.entries[i0]);
        }
        value.entriesCount = 0;
        value.marker = 0;
    }

    // checkWriteDefaultArm is writeDefaultArm's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteDefaultArm(DefaultArm value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= defaultArmMaxBytes;
        for (int i0 = 0; i0 < value.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.entries[i0];
            assert (e0.preferred & 0xffL) >= 0;
            assert (e0.preferred & 0xffL) <= 15;
        }
        assert (value.marker & 0xffL) >= 0;
        assert (value.marker & 0xffL) <= 7;
        return true;
    }

    // writeDefaultArm packs value into data — the trusted writer (contracts in the
    // checkWriteDefaultArm predicate, one dormant assert call without -ea). The buffer
    // must hold defaultArmMaxBytes. Returns the bytes written.
    public static int writeDefaultArm(DefaultArm value, byte[] data) {
        assert checkWriteDefaultArm(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        if (value.entriesCount < 0 || value.entriesCount > 2) {
            return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
        }
        v = (value.entriesCount) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        for (int i0 = 0; i0 < value.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.entries[i0];
            v = e0.retries & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = e0.preferred & 0xfL;
            scratch |= v << scratchBits;
            scratchBits += 4;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (4 - scratchBits);
            }
        }
        v = ((value.marker & 0xffL)) & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readDefaultArm decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readDefaultArm(DefaultArm value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // the count guards the loop — reject, never clamp
        }
        value.entriesCount = (int) v;
        if (bitsRead + value.entriesCount * 36L > numBits) {
            return false;
        }
        for (int i0 = 0; i0 < value.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.entries[i0];
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.retries = (int) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xfL;
            bitsRead += 4;
            e0.preferred = (byte) v;
        }
        if (bitsRead + 3 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        value.marker = (byte) v;
        return true;
    }

    // measureDefaultArm is the exact wire bits writeDefaultArm would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureDefaultArm(DefaultArm value) {
        int bits = 0;
        bits += 2;
        bits += value.entriesCount * 36;
        bits += 3;
        return bits;
    }

    // DefaultChoiceType: union DefaultChoice's tag — None = 0, then each variant in declared order (SPEC §4.8)
    public static final class DefaultChoiceType {
        private DefaultChoiceType() {}

        public static final byte none = 0;
        public static final byte first = 1;
        public static final byte second = 2;
        // the declared variant count (SPEC §4.2)
        public static final byte count = 2;
        // the exported extent (SPEC §4.2)
        public static final byte max = 2;
    }

    // enumNameDefaultChoiceType: debug/log/tooling name for any DefaultChoiceType storage value —
    // out-of-set values (wire-legal up to the declared max) name as "???"
    public static String enumNameDefaultChoiceType(long value) {
        if (value == DefaultChoiceType.none) {
            return "None";
        }
        if (value == DefaultChoiceType.first) {
            return "First";
        }
        if (value == DefaultChoiceType.second) {
            return "Second";
        }
        return "???";
    }

    // DefaultChoice — at most one of the arms; type says which. Construction is the empty
    // union (None). Every read selection initializes the chosen payload from
    // construction defaults; unselected arms keep what they last held — the
    // reused-storage discipline. Consumers read the selected arm only.
    public static final class DefaultChoice {
        public byte type = DefaultChoiceType.none;
        public final DefaultArm first = new DefaultArm();
        public final DefaultArm second = new DefaultArm();
    }

    // defaultChoiceMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
    // defaultChoiceMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int defaultChoiceMaxBits = 79;
    public static final int defaultChoiceMaxBytes = 16;

    // zeroDefaultChoice resets value to the §5 zero form — the empty union. The tag alone
    // resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
    // is unselected at None; an arm initializes at its next selection.
    public static void zeroDefaultChoice(DefaultChoice value) {
        value.type = DefaultChoiceType.none;
    }

    // checkWriteDefaultChoice is writeDefaultChoice's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteDefaultChoice(DefaultChoice value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= defaultChoiceMaxBytes;
        assert (value.type & 0xffL) >= 0;
        assert (value.type & 0xffL) <= 2;
        switch (value.type) {
            case 1:
                for (int i0 = 0; i0 < value.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.first.marker & 0xffL) >= 0;
                assert (value.first.marker & 0xffL) <= 7;
                break;
            case 2:
                for (int i0 = 0; i0 < value.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.second.marker & 0xffL) >= 0;
                assert (value.second.marker & 0xffL) <= 7;
                break;
        }
        return true;
    }

    // writeDefaultChoice packs value into data — the trusted writer (contracts in the
    // checkWriteDefaultChoice predicate, one dormant assert call without -ea). The buffer
    // must hold defaultChoiceMaxBytes. Returns the bytes written.
    public static int writeDefaultChoice(DefaultChoice value, byte[] data) {
        assert checkWriteDefaultChoice(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.type & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        switch (value.type) {
            case 1:
                if (value.first.entriesCount < 0 || value.first.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.first.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.first.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                break;
            case 2:
                if (value.second.entriesCount < 0 || value.second.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.second.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.second.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                break;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readDefaultChoice decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readDefaultChoice(DefaultChoice value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // not a wire-legal tag (SPEC §4.8)
        }
        value.type = (byte) v;
        switch (value.type) {
            case 1:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.first.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.first.entriesCount = 0;
                value.first.marker = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.first.entriesCount = (int) v;
                if (bitsRead + value.first.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.first.marker = (byte) v;
                break;
            case 2:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.second.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.second.entriesCount = 0;
                value.second.marker = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.second.entriesCount = (int) v;
                if (bitsRead + value.second.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.second.marker = (byte) v;
                break;
        }
        return true;
    }

    // measureDefaultChoice is the exact wire bits writeDefaultChoice would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureDefaultChoice(DefaultChoice value) {
        int bits = 0;
        bits += 2;
        switch (value.type) {
            case 1:
                bits += 2;
                bits += value.first.entriesCount * 36;
                bits += 3;
                break;
            case 2:
                bits += 2;
                bits += value.second.entriesCount * 36;
                bits += 3;
                break;
        }
        return bits;
    }

    // type DefaultChoicePacket
    public static final class DefaultChoicePacket {
        public final DefaultChoice choice = new DefaultChoice();
    }

    // defaultChoicePacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // defaultChoicePacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int defaultChoicePacketMaxBits = 79;
    public static final int defaultChoicePacketMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroDefaultChoicePacket(DefaultChoicePacket value) {
        value.choice.type = 0;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initDefaultChoicePacket(DefaultChoicePacket value) {
        value.choice.type = 0;
    }

    // checkWriteDefaultChoicePacket is writeDefaultChoicePacket's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteDefaultChoicePacket(DefaultChoicePacket value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= defaultChoicePacketMaxBytes;
        assert (value.choice.type & 0xffL) >= 0;
        assert (value.choice.type & 0xffL) <= 2;
        switch (value.choice.type) {
            case 1:
                for (int i0 = 0; i0 < value.choice.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.first.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.choice.first.marker & 0xffL) >= 0;
                assert (value.choice.first.marker & 0xffL) <= 7;
                break;
            case 2:
                for (int i0 = 0; i0 < value.choice.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.second.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.choice.second.marker & 0xffL) >= 0;
                assert (value.choice.second.marker & 0xffL) <= 7;
                break;
        }
        return true;
    }

    // writeDefaultChoicePacket packs value into data — the trusted writer (contracts in the
    // checkWriteDefaultChoicePacket predicate, one dormant assert call without -ea). The buffer
    // must hold defaultChoicePacketMaxBytes. Returns the bytes written.
    public static int writeDefaultChoicePacket(DefaultChoicePacket value, byte[] data) {
        assert checkWriteDefaultChoicePacket(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.choice.type & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        switch (value.choice.type) {
            case 1:
                if (value.choice.first.entriesCount < 0 || value.choice.first.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.choice.first.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.choice.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.first.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.choice.first.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                break;
            case 2:
                if (value.choice.second.entriesCount < 0 || value.choice.second.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.choice.second.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.choice.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.second.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.choice.second.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                break;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readDefaultChoicePacket decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readDefaultChoicePacket(DefaultChoicePacket value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // not a wire-legal tag (SPEC §4.8)
        }
        value.choice.type = (byte) v;
        switch (value.choice.type) {
            case 1:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.first.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.choice.first.entriesCount = 0;
                value.choice.first.marker = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.choice.first.entriesCount = (int) v;
                if (bitsRead + value.choice.first.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.choice.first.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.first.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.choice.first.marker = (byte) v;
                break;
            case 2:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.second.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.choice.second.entriesCount = 0;
                value.choice.second.marker = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.choice.second.entriesCount = (int) v;
                if (bitsRead + value.choice.second.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.choice.second.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.choice.second.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.choice.second.marker = (byte) v;
                break;
        }
        return true;
    }

    // measureDefaultChoicePacket is the exact wire bits writeDefaultChoicePacket would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureDefaultChoicePacket(DefaultChoicePacket value) {
        int bits = 0;
        bits += 2;
        switch (value.choice.type) {
            case 1:
                bits += 2;
                bits += value.choice.first.entriesCount * 36;
                bits += 3;
                break;
            case 2:
                bits += 2;
                bits += value.choice.second.entriesCount * 36;
                bits += 3;
                break;
        }
        return bits;
    }

    // type DefaultBulkArm
    public static final class DefaultBulkArm {
        public final DefaultArm payload = new DefaultArm();
        // bytes(2): fixed buffer, used length beside it (SPEC §4.7)
        public final byte[] data = new byte[2];
        public int dataLength;
    }

    // defaultBulkArmMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // defaultBulkArmMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int defaultBulkArmMaxBits = 102;
    public static final int defaultBulkArmMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroDefaultBulkArm(DefaultBulkArm value) {
        zeroDefaultArm(value.payload);
        java.util.Arrays.fill(value.data, (byte) 0);
        value.dataLength = 0;
    }

    // Restore construction defaults in place; buffers and objects are retained.
    public static void initDefaultBulkArm(DefaultBulkArm value) {
        initDefaultArm(value.payload);
        java.util.Arrays.fill(value.data, (byte) 0);
        value.dataLength = 0;
    }

    // checkWriteDefaultBulkArm is writeDefaultBulkArm's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteDefaultBulkArm(DefaultBulkArm value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= defaultBulkArmMaxBytes;
        for (int i0 = 0; i0 < value.payload.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.payload.entries[i0];
            assert (e0.preferred & 0xffL) >= 0;
            assert (e0.preferred & 0xffL) <= 15;
        }
        assert (value.payload.marker & 0xffL) >= 0;
        assert (value.payload.marker & 0xffL) <= 7;
        assert value.dataLength >= 0;
        assert value.dataLength <= 2;
        return true;
    }

    // writeDefaultBulkArm packs value into data — the trusted writer (contracts in the
    // checkWriteDefaultBulkArm predicate, one dormant assert call without -ea). The buffer
    // must hold defaultBulkArmMaxBytes. Returns the bytes written.
    public static int writeDefaultBulkArm(DefaultBulkArm value, byte[] data) {
        assert checkWriteDefaultBulkArm(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        if (value.payload.entriesCount < 0 || value.payload.entriesCount > 2) {
            return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
        }
        v = (value.payload.entriesCount) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        for (int i0 = 0; i0 < value.payload.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.payload.entries[i0];
            v = e0.retries & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = e0.preferred & 0xfL;
            scratch |= v << scratchBits;
            scratchBits += 4;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (4 - scratchBits);
            }
        }
        v = ((value.payload.marker & 0xffL)) & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = (value.dataLength) & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        {
            final int pad = scratchBits & 7;
            if (pad != 0) {
                scratchBits += 8 - pad;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = 0;
                }
            }
        }
        // byte-aligned payload — the fused bulk copy (flush, copy, reload),
        // wire-identical to the per-byte merge
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        System.arraycopy(value.data, 0, data, wordIndex * 8 + (scratchBits >>> 3), value.dataLength);
        scratchBits += value.dataLength * 8;
        wordIndex += scratchBits >>> 6;
        scratchBits &= 63;
        if (scratchBits != 0) {
            scratch = (long) LONG_LE.get(data, wordIndex * 8) & ((1L << scratchBits) - 1);
        } else {
            scratch = 0;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readDefaultBulkArm decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readDefaultBulkArm(DefaultBulkArm value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // the count guards the loop — reject, never clamp
        }
        value.payload.entriesCount = (int) v;
        if (bitsRead + value.payload.entriesCount * 36L > numBits) {
            return false;
        }
        for (int i0 = 0; i0 < value.payload.entriesCount; i0++) {
            final Wire.ProbeConfig e0 = value.payload.entries[i0];
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.retries = (int) v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xfL;
            bitsRead += 4;
            e0.preferred = (byte) v;
        }
        if (bitsRead + 3 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        value.payload.marker = (byte) v;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // the length guards the copy — reject, never clamp
        }
        value.dataLength = (int) v;
        {
            final int pad = bitsRead & 7;
            if (pad != 0) {
                if (bitsRead + (8 - pad) > numBits) {
                    return false;
                }
                if ((data[bitsRead >>> 3] & 0xff) >>> pad != 0) {
                    return false; // nonzero padding is refused (SPEC §4.3)
                }
                bitsRead += 8 - pad;
            }
        }
        if (bitsRead + value.dataLength * 8L > numBits) {
            return false;
        }
        System.arraycopy(data, bitsRead >>> 3, value.data, 0, value.dataLength);
        bitsRead += value.dataLength * 8;
        return true;
    }

    // measureDefaultBulkArm is the exact wire bits writeDefaultBulkArm would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureDefaultBulkArm(DefaultBulkArm value) {
        int bits = 0;
        bits += 2;
        bits += value.payload.entriesCount * 36;
        bits += 5;
        bits += (8 - (bits & 7)) & 7;
        bits += value.dataLength * 8;
        return bits;
    }

    // DefaultBulkChoiceType: union DefaultBulkChoice's tag — None = 0, then each variant in declared order (SPEC §4.8)
    public static final class DefaultBulkChoiceType {
        private DefaultBulkChoiceType() {}

        public static final byte none = 0;
        public static final byte first = 1;
        public static final byte second = 2;
        // the declared variant count (SPEC §4.2)
        public static final byte count = 2;
        // the exported extent (SPEC §4.2)
        public static final byte max = 2;
    }

    // enumNameDefaultBulkChoiceType: debug/log/tooling name for any DefaultBulkChoiceType storage value —
    // out-of-set values (wire-legal up to the declared max) name as "???"
    public static String enumNameDefaultBulkChoiceType(long value) {
        if (value == DefaultBulkChoiceType.none) {
            return "None";
        }
        if (value == DefaultBulkChoiceType.first) {
            return "First";
        }
        if (value == DefaultBulkChoiceType.second) {
            return "Second";
        }
        return "???";
    }

    // DefaultBulkChoice — at most one of the arms; type says which. Construction is the empty
    // union (None). Every read selection initializes the chosen payload from
    // construction defaults; unselected arms keep what they last held — the
    // reused-storage discipline. Consumers read the selected arm only.
    public static final class DefaultBulkChoice {
        public byte type = DefaultBulkChoiceType.none;
        public final DefaultBulkArm first = new DefaultBulkArm();
        public final DefaultBulkArm second = new DefaultBulkArm();
    }

    // defaultBulkChoiceMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
    // defaultBulkChoiceMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int defaultBulkChoiceMaxBits = 104;
    public static final int defaultBulkChoiceMaxBytes = 16;

    // zeroDefaultBulkChoice resets value to the §5 zero form — the empty union. The tag alone
    // resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
    // is unselected at None; an arm initializes at its next selection.
    public static void zeroDefaultBulkChoice(DefaultBulkChoice value) {
        value.type = DefaultBulkChoiceType.none;
    }

    // checkWriteDefaultBulkChoice is writeDefaultBulkChoice's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteDefaultBulkChoice(DefaultBulkChoice value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= defaultBulkChoiceMaxBytes;
        assert (value.type & 0xffL) >= 0;
        assert (value.type & 0xffL) <= 2;
        switch (value.type) {
            case 1:
                for (int i0 = 0; i0 < value.first.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.payload.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.first.payload.marker & 0xffL) >= 0;
                assert (value.first.payload.marker & 0xffL) <= 7;
                assert value.first.dataLength >= 0;
                assert value.first.dataLength <= 2;
                break;
            case 2:
                for (int i0 = 0; i0 < value.second.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.payload.entries[i0];
                    assert (e0.preferred & 0xffL) >= 0;
                    assert (e0.preferred & 0xffL) <= 15;
                }
                assert (value.second.payload.marker & 0xffL) >= 0;
                assert (value.second.payload.marker & 0xffL) <= 7;
                assert value.second.dataLength >= 0;
                assert value.second.dataLength <= 2;
                break;
        }
        return true;
    }

    // writeDefaultBulkChoice packs value into data — the trusted writer (contracts in the
    // checkWriteDefaultBulkChoice predicate, one dormant assert call without -ea). The buffer
    // must hold defaultBulkChoiceMaxBytes. Returns the bytes written.
    public static int writeDefaultBulkChoice(DefaultBulkChoice value, byte[] data) {
        assert checkWriteDefaultBulkChoice(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.type & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        switch (value.type) {
            case 1:
                if (value.first.payload.entriesCount < 0 || value.first.payload.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.first.payload.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.first.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.payload.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.first.payload.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                v = (value.first.dataLength) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                {
                    final int pad = scratchBits & 7;
                    if (pad != 0) {
                        scratchBits += 8 - pad;
                        if (scratchBits >= 64) {
                            LONG_LE.set(data, wordIndex * 8, scratch);
                            wordIndex++;
                            scratchBits -= 64;
                            scratch = 0;
                        }
                    }
                }
                // byte-aligned payload — the fused bulk copy (flush, copy, reload),
                // wire-identical to the per-byte merge
                if (scratchBits != 0) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                }
                System.arraycopy(value.first.data, 0, data, wordIndex * 8 + (scratchBits >>> 3), value.first.dataLength);
                scratchBits += value.first.dataLength * 8;
                wordIndex += scratchBits >>> 6;
                scratchBits &= 63;
                if (scratchBits != 0) {
                    scratch = (long) LONG_LE.get(data, wordIndex * 8) & ((1L << scratchBits) - 1);
                } else {
                    scratch = 0;
                }
                break;
            case 2:
                if (value.second.payload.entriesCount < 0 || value.second.payload.entriesCount > 2) {
                    return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
                }
                v = (value.second.payload.entriesCount) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                for (int i0 = 0; i0 < value.second.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.payload.entries[i0];
                    v = e0.retries & 0xffffffffL;
                    scratch |= v << scratchBits;
                    scratchBits += 32;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (32 - scratchBits);
                    }
                    v = e0.preferred & 0xfL;
                    scratch |= v << scratchBits;
                    scratchBits += 4;
                    if (scratchBits >= 64) {
                        LONG_LE.set(data, wordIndex * 8, scratch);
                        wordIndex++;
                        scratchBits -= 64;
                        scratch = v >>> (4 - scratchBits);
                    }
                }
                v = ((value.second.payload.marker & 0xffL)) & 0x7L;
                scratch |= v << scratchBits;
                scratchBits += 3;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (3 - scratchBits);
                }
                v = (value.second.dataLength) & 0x3L;
                scratch |= v << scratchBits;
                scratchBits += 2;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (2 - scratchBits);
                }
                {
                    final int pad = scratchBits & 7;
                    if (pad != 0) {
                        scratchBits += 8 - pad;
                        if (scratchBits >= 64) {
                            LONG_LE.set(data, wordIndex * 8, scratch);
                            wordIndex++;
                            scratchBits -= 64;
                            scratch = 0;
                        }
                    }
                }
                // byte-aligned payload — the fused bulk copy (flush, copy, reload),
                // wire-identical to the per-byte merge
                if (scratchBits != 0) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                }
                System.arraycopy(value.second.data, 0, data, wordIndex * 8 + (scratchBits >>> 3), value.second.dataLength);
                scratchBits += value.second.dataLength * 8;
                wordIndex += scratchBits >>> 6;
                scratchBits &= 63;
                if (scratchBits != 0) {
                    scratch = (long) LONG_LE.get(data, wordIndex * 8) & ((1L << scratchBits) - 1);
                } else {
                    scratch = 0;
                }
                break;
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readDefaultBulkChoice decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readDefaultBulkChoice(DefaultBulkChoice value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 2 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // not a wire-legal tag (SPEC §4.8)
        }
        value.type = (byte) v;
        switch (value.type) {
            case 1:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.first.payload.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.first.payload.entriesCount = 0;
                value.first.payload.marker = 0;
                java.util.Arrays.fill(value.first.data, (byte) 0);
                value.first.dataLength = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.first.payload.entriesCount = (int) v;
                if (bitsRead + value.first.payload.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.first.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.first.payload.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.first.payload.marker = (byte) v;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the length guards the copy — reject, never clamp
                }
                value.first.dataLength = (int) v;
                {
                    final int pad = bitsRead & 7;
                    if (pad != 0) {
                        if (bitsRead + (8 - pad) > numBits) {
                            return false;
                        }
                        if ((data[bitsRead >>> 3] & 0xff) >>> pad != 0) {
                            return false; // nonzero padding is refused (SPEC §4.3)
                        }
                        bitsRead += 8 - pad;
                    }
                }
                if (bitsRead + value.first.dataLength * 8L > numBits) {
                    return false;
                }
                System.arraycopy(data, bitsRead >>> 3, value.first.data, 0, value.first.dataLength);
                bitsRead += value.first.dataLength * 8;
                break;
            case 2:
                for (int i0 = 0; i0 < 2; i0++) {
                    final Wire.ProbeConfig e0 = value.second.payload.entries[i0];
                    e0.retries = -1;
                    e0.preferred = Wire.Weapon.railgun;
                }
                value.second.payload.entriesCount = 0;
                value.second.payload.marker = 0;
                java.util.Arrays.fill(value.second.data, (byte) 0);
                value.second.dataLength = 0;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the count guards the loop — reject, never clamp
                }
                value.second.payload.entriesCount = (int) v;
                if (bitsRead + value.second.payload.entriesCount * 36L > numBits) {
                    return false;
                }
                for (int i0 = 0; i0 < value.second.payload.entriesCount; i0++) {
                    final Wire.ProbeConfig e0 = value.second.payload.entries[i0];
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xffffffffL;
                    bitsRead += 32;
                    e0.retries = (int) v;
                    if (bitsRead >>> 3 < tailBase) {
                        window = (long) LONG_LE.get(data, bitsRead >>> 3);
                        shift = bitsRead & 7;
                    } else {
                        window = tailWord;
                        shift = bitsRead - tailBase * 8;
                    }
                    v = (window >>> shift) & 0xfL;
                    bitsRead += 4;
                    e0.preferred = (byte) v;
                }
                if (bitsRead + 3 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x7L;
                bitsRead += 3;
                value.second.payload.marker = (byte) v;
                if (bitsRead + 2 > numBits) {
                    return false;
                }
                if (bitsRead >>> 3 < tailBase) {
                    window = (long) LONG_LE.get(data, bitsRead >>> 3);
                    shift = bitsRead & 7;
                } else {
                    window = tailWord;
                    shift = bitsRead - tailBase * 8;
                }
                v = (window >>> shift) & 0x3L;
                bitsRead += 2;
                if (v > 2) {
                    return false; // the length guards the copy — reject, never clamp
                }
                value.second.dataLength = (int) v;
                {
                    final int pad = bitsRead & 7;
                    if (pad != 0) {
                        if (bitsRead + (8 - pad) > numBits) {
                            return false;
                        }
                        if ((data[bitsRead >>> 3] & 0xff) >>> pad != 0) {
                            return false; // nonzero padding is refused (SPEC §4.3)
                        }
                        bitsRead += 8 - pad;
                    }
                }
                if (bitsRead + value.second.dataLength * 8L > numBits) {
                    return false;
                }
                System.arraycopy(data, bitsRead >>> 3, value.second.data, 0, value.second.dataLength);
                bitsRead += value.second.dataLength * 8;
                break;
        }
        return true;
    }

    // measureDefaultBulkChoice is the exact wire bits writeDefaultBulkChoice would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureDefaultBulkChoice(DefaultBulkChoice value) {
        int bits = 0;
        bits += 2;
        switch (value.type) {
            case 1:
                bits += 2;
                bits += value.first.payload.entriesCount * 36;
                bits += 5;
                bits += (8 - (bits & 7)) & 7;
                bits += value.first.dataLength * 8;
                break;
            case 2:
                bits += 2;
                bits += value.second.payload.entriesCount * 36;
                bits += 5;
                bits += (8 - (bits & 7)) & 7;
                bits += value.second.dataLength * 8;
                break;
        }
        return bits;
    }
}
