// Code generated by the schema compiler from Types.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x682e2a15a56b78bf

package example;

// Types carries every generated declaration of Types.schema — Java has no
// top-level functions or constants, so the file's one public class is their
// home; types nest inside it (SPEC §6.1 naming).
public final class Types {
    private Types() {}

    // 64-bit little-endian word access into byte[] — the proven-fast JVM
    // form (VarHandle byteArrayViewVarHandle, serialize.java's own).
    private static final java.lang.invoke.VarHandle LONG_LE =
            java.lang.invoke.MethodHandles.byteArrayViewVarHandle(
                    long[].class, java.nio.ByteOrder.LITTLE_ENDIAN);

    // type Vec3 [vec3] — tags are user-chosen and inert in v1 (SPEC §4.2, Type tags)
    public static final class Vec3 {
        public double x;
        public double y;
        public double z;
    }

    // vec3MaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // vec3MaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int vec3MaxBits = 192;
    public static final int vec3MaxBytes = 24;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroVec3(Vec3 value) {
        value.x = 0.0;
        value.y = 0.0;
        value.z = 0.0;
    }

    // checkWriteVec3 is writeVec3's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteVec3(Vec3 value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= vec3MaxBytes;
        return true;
    }

    // writeVec3 packs value into data — the trusted writer (contracts in the
    // checkWriteVec3 predicate, one dormant assert call without -ea). The buffer
    // must hold vec3MaxBytes. Returns the bytes written.
    public static int writeVec3(Vec3 value, byte[] data) {
        assert checkWriteVec3(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        {
            final long b = Double.doubleToRawLongBits(value.x);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.y);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.z);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readVec3 decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readVec3(Vec3 value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 192 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.x = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.y = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.z = Double.longBitsToDouble(lo);
        return true;
    }

    // measureVec3 is the exact wire bits writeVec3 would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureVec3(Vec3 value) {
        return 192;
    }

    // type Quat [quat4] — tags are user-chosen and inert in v1 (SPEC §4.2, Type tags)
    public static final class Quat {
        public double x;
        public double y;
        public double z;
        public double w;
    }

    // quatMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // quatMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int quatMaxBits = 256;
    public static final int quatMaxBytes = 32;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroQuat(Quat value) {
        value.x = 0.0;
        value.y = 0.0;
        value.z = 0.0;
        value.w = 0.0;
    }

    // checkWriteQuat is writeQuat's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteQuat(Quat value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= quatMaxBytes;
        return true;
    }

    // writeQuat packs value into data — the trusted writer (contracts in the
    // checkWriteQuat predicate, one dormant assert call without -ea). The buffer
    // must hold quatMaxBytes. Returns the bytes written.
    public static int writeQuat(Quat value, byte[] data) {
        assert checkWriteQuat(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        {
            final long b = Double.doubleToRawLongBits(value.x);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.y);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.z);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.w);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readQuat decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readQuat(Quat value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 256 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.x = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.y = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.z = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.w = Double.longBitsToDouble(lo);
        return true;
    }

    // measureQuat is the exact wire bits writeQuat would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureQuat(Quat value) {
        return 256;
    }

    // type Handle
    public static final class Handle {
        // wire [0, 9999]
        public int objectId;
        public byte objectSequence;
    }

    // handleMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // handleMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int handleMaxBits = 22;
    public static final int handleMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroHandle(Handle value) {
        value.objectId = 0;
        value.objectSequence = 0;
    }

    // checkWriteHandle is writeHandle's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteHandle(Handle value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= handleMaxBytes;
        assert value.objectId >= 0;
        assert value.objectId <= 9999;
        return true;
    }

    // writeHandle packs value into data — the trusted writer (contracts in the
    // checkWriteHandle predicate, one dormant assert call without -ea). The buffer
    // must hold handleMaxBytes. Returns the bytes written.
    public static int writeHandle(Handle value, byte[] data) {
        assert checkWriteHandle(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.objectId) & 0x3fffL;
        scratch |= v << scratchBits;
        scratchBits += 14;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (14 - scratchBits);
        }
        v = value.objectSequence & 0xffL;
        scratch |= v << scratchBits;
        scratchBits += 8;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (8 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readHandle decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readHandle(Handle value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 22 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3fffL;
        bitsRead += 14;
        if (v > 9999) {
            return false; // a smuggled offset is refused
        }
        value.objectId = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffL;
        bitsRead += 8;
        value.objectSequence = (byte) v;
        return true;
    }

    // measureHandle is the exact wire bits writeHandle would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureHandle(Handle value) {
        return 22;
    }

    // type QuantizedPosition
    public static final class QuantizedPosition {
        // wire [-8388608, 8388608]
        public int x;
        // wire [-8388608, 8388608]
        public int y;
        // wire [-8388608, 8388608]
        public int z;
    }

    // quantizedPositionMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // quantizedPositionMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int quantizedPositionMaxBits = 75;
    public static final int quantizedPositionMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroQuantizedPosition(QuantizedPosition value) {
        value.x = 0;
        value.y = 0;
        value.z = 0;
    }

    // checkWriteQuantizedPosition is writeQuantizedPosition's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteQuantizedPosition(QuantizedPosition value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= quantizedPositionMaxBytes;
        assert value.x >= -8388608;
        assert value.x <= 8388608;
        assert value.y >= -8388608;
        assert value.y <= 8388608;
        assert value.z >= -8388608;
        assert value.z <= 8388608;
        return true;
    }

    // writeQuantizedPosition packs value into data — the trusted writer (contracts in the
    // checkWriteQuantizedPosition predicate, one dormant assert call without -ea). The buffer
    // must hold quantizedPositionMaxBytes. Returns the bytes written.
    public static int writeQuantizedPosition(QuantizedPosition value, byte[] data) {
        assert checkWriteQuantizedPosition(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.x + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = (value.y + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = (value.z + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readQuantizedPosition decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readQuantizedPosition(QuantizedPosition value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 75 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.x = (int) (v + -8388608);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.y = (int) (v + -8388608);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.z = (int) (v + -8388608);
        return true;
    }

    // measureQuantizedPosition is the exact wire bits writeQuantizedPosition would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureQuantizedPosition(QuantizedPosition value) {
        return 75;
    }

    // type QuantizedVelocity
    public static final class QuantizedVelocity {
        // wire [-2097152, 2097152]
        public int x;
        // wire [-2097152, 2097152]
        public int y;
        // wire [-2097152, 2097152]
        public int z;
    }

    // quantizedVelocityMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // quantizedVelocityMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int quantizedVelocityMaxBits = 69;
    public static final int quantizedVelocityMaxBytes = 16;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroQuantizedVelocity(QuantizedVelocity value) {
        value.x = 0;
        value.y = 0;
        value.z = 0;
    }

    // checkWriteQuantizedVelocity is writeQuantizedVelocity's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteQuantizedVelocity(QuantizedVelocity value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= quantizedVelocityMaxBytes;
        assert value.x >= -2097152;
        assert value.x <= 2097152;
        assert value.y >= -2097152;
        assert value.y <= 2097152;
        assert value.z >= -2097152;
        assert value.z <= 2097152;
        return true;
    }

    // writeQuantizedVelocity packs value into data — the trusted writer (contracts in the
    // checkWriteQuantizedVelocity predicate, one dormant assert call without -ea). The buffer
    // must hold quantizedVelocityMaxBytes. Returns the bytes written.
    public static int writeQuantizedVelocity(QuantizedVelocity value, byte[] data) {
        assert checkWriteQuantizedVelocity(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.x + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = (value.y + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = (value.z + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readQuantizedVelocity decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readQuantizedVelocity(QuantizedVelocity value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 69 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.x = (int) (v + -2097152);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.y = (int) (v + -2097152);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.z = (int) (v + -2097152);
        return true;
    }

    // measureQuantizedVelocity is the exact wire bits writeQuantizedVelocity would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureQuantizedVelocity(QuantizedVelocity value) {
        return 69;
    }

    // type QuantizedRotation
    public static final class QuantizedRotation {
        // wire [-1024, 1024]
        public short x;
        // wire [-1024, 1024]
        public short y;
        // wire [-1024, 1024]
        public short z;
        // wire [-1024, 1024]
        public short w;
    }

    // quantizedRotationMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // quantizedRotationMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int quantizedRotationMaxBits = 48;
    public static final int quantizedRotationMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroQuantizedRotation(QuantizedRotation value) {
        value.x = 0;
        value.y = 0;
        value.z = 0;
        value.w = 0;
    }

    // checkWriteQuantizedRotation is writeQuantizedRotation's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteQuantizedRotation(QuantizedRotation value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= quantizedRotationMaxBytes;
        assert value.x >= -1024;
        assert value.x <= 1024;
        assert value.y >= -1024;
        assert value.y <= 1024;
        assert value.z >= -1024;
        assert value.z <= 1024;
        assert value.w >= -1024;
        assert value.w <= 1024;
        return true;
    }

    // writeQuantizedRotation packs value into data — the trusted writer (contracts in the
    // checkWriteQuantizedRotation predicate, one dormant assert call without -ea). The buffer
    // must hold quantizedRotationMaxBytes. Returns the bytes written.
    public static int writeQuantizedRotation(QuantizedRotation value, byte[] data) {
        assert checkWriteQuantizedRotation(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.x + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.y + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.z + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.w + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readQuantizedRotation decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readQuantizedRotation(QuantizedRotation value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 48 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.x = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.y = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.z = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.w = (short) (v + -1024);
        return true;
    }

    // measureQuantizedRotation is the exact wire bits writeQuantizedRotation would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureQuantizedRotation(QuantizedRotation value) {
        return 48;
    }

    // type RigidBody
    public static final class RigidBody {
        public final Vec3 position = new Vec3();
        public final Quat orientation = new Quat();
        public boolean atRest;

        // !at_rest — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        public final Vec3 linearVelocity = new Vec3();
        public final Vec3 angularVelocity = new Vec3();
    }

    // rigidBodyMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // rigidBodyMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int rigidBodyMaxBits = 833;
    public static final int rigidBodyMaxBytes = 112;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroRigidBody(RigidBody value) {
        zeroVec3(value.position);
        zeroQuat(value.orientation);
        value.atRest = false;
        zeroVec3(value.linearVelocity);
        zeroVec3(value.angularVelocity);
    }

    // checkWriteRigidBody is writeRigidBody's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteRigidBody(RigidBody value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= rigidBodyMaxBytes;
        return true;
    }

    // writeRigidBody packs value into data — the trusted writer (contracts in the
    // checkWriteRigidBody predicate, one dormant assert call without -ea). The buffer
    // must hold rigidBodyMaxBytes. Returns the bytes written.
    public static int writeRigidBody(RigidBody value, byte[] data) {
        assert checkWriteRigidBody(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        {
            final long b = Double.doubleToRawLongBits(value.position.x);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.position.y);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.position.z);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.orientation.x);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.orientation.y);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.orientation.z);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long b = Double.doubleToRawLongBits(value.orientation.w);
            v = b & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (b >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = value.atRest ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (!value.atRest) {
            {
                final long b = Double.doubleToRawLongBits(value.linearVelocity.x);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
            {
                final long b = Double.doubleToRawLongBits(value.linearVelocity.y);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
            {
                final long b = Double.doubleToRawLongBits(value.linearVelocity.z);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
            {
                final long b = Double.doubleToRawLongBits(value.angularVelocity.x);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
            {
                final long b = Double.doubleToRawLongBits(value.angularVelocity.y);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
            {
                final long b = Double.doubleToRawLongBits(value.angularVelocity.z);
                v = b & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
                v = (b >>> 32) & 0xffffffffL;
                scratch |= v << scratchBits;
                scratchBits += 32;
                if (scratchBits >= 64) {
                    LONG_LE.set(data, wordIndex * 8, scratch);
                    wordIndex++;
                    scratchBits -= 64;
                    scratch = v >>> (32 - scratchBits);
                }
            }
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readRigidBody decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readRigidBody(RigidBody value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 449 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.position.x = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.position.y = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.position.z = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.orientation.x = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.orientation.y = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.orientation.z = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.orientation.w = Double.longBitsToDouble(lo);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.atRest = v != 0;
        if (!value.atRest) {
            if (bitsRead + 384 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.linearVelocity.x = Double.longBitsToDouble(lo);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.linearVelocity.y = Double.longBitsToDouble(lo);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.linearVelocity.z = Double.longBitsToDouble(lo);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.angularVelocity.x = Double.longBitsToDouble(lo);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.angularVelocity.y = Double.longBitsToDouble(lo);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo = v;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            lo |= v << 32;
            value.angularVelocity.z = Double.longBitsToDouble(lo);
        } else {
            value.linearVelocity.x = 0.0;
            value.linearVelocity.y = 0.0;
            value.linearVelocity.z = 0.0;
            value.angularVelocity.x = 0.0;
            value.angularVelocity.y = 0.0;
            value.angularVelocity.z = 0.0;
        }
        return true;
    }

    // measureRigidBody is the exact wire bits writeRigidBody would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureRigidBody(RigidBody value) {
        int bits = 0;
        bits += 449;
        if (!value.atRest) {
            bits += 384;
        }
        return bits;
    }

    // type Input
    public static final class Input {
        public float stickX;
        public float stickY;
        public float throttle;
        public float yaw;
        public float pitch;
        public boolean fire;
        public boolean altFire;
        public boolean boost;
        public boolean brake;
        public boolean aim;
        public boolean lockOn;
        public boolean zoom;
        public boolean ping;
    }

    // inputMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // inputMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int inputMaxBits = 168;
    public static final int inputMaxBytes = 24;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroInput(Input value) {
        value.stickX = 0.0f;
        value.stickY = 0.0f;
        value.throttle = 0.0f;
        value.yaw = 0.0f;
        value.pitch = 0.0f;
        value.fire = false;
        value.altFire = false;
        value.boost = false;
        value.brake = false;
        value.aim = false;
        value.lockOn = false;
        value.zoom = false;
        value.ping = false;
    }

    // checkWriteInput is writeInput's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteInput(Input value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= inputMaxBytes;
        return true;
    }

    // writeInput packs value into data — the trusted writer (contracts in the
    // checkWriteInput predicate, one dormant assert call without -ea). The buffer
    // must hold inputMaxBytes. Returns the bytes written.
    public static int writeInput(Input value, byte[] data) {
        assert checkWriteInput(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = Float.floatToRawIntBits(value.stickX) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.stickY) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.throttle) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.yaw) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = Float.floatToRawIntBits(value.pitch) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.fire ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.altFire ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.boost ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.brake ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.aim ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.lockOn ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.zoom ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        v = value.ping ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readInput decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readInput(Input value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 168 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.stickX = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.stickY = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.throttle = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.yaw = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        value.pitch = Float.intBitsToFloat((int) v);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.fire = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.altFire = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.boost = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.brake = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.aim = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.lockOn = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.zoom = v != 0;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.ping = v != 0;
        return true;
    }

    // measureInput is the exact wire bits writeInput would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureInput(Input value) {
        return 168;
    }

    // type InputPacket
    public static final class InputPacket {
        public short synchronizeSequence;
        public long currentFrame;
        public long startFrame;
        public final Input[] inputs = new Input[Constants.maxInputsPerPacket];
        public int inputsCount;

        // pre-allocated element instances — the storage principle (SPEC §6.1):
        // every buffer exists at construction
        public InputPacket() {
            for (int i = 0; i < inputs.length; i++) {
                inputs[i] = new Input();
            }
        }
    }

    // inputPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // inputPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int inputPacketMaxBits = 2837;
    public static final int inputPacketMaxBytes = 360;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroInputPacket(InputPacket value) {
        value.synchronizeSequence = 0;
        value.currentFrame = 0;
        value.startFrame = 0;
        for (int i0 = 0; i0 < 16; i0++) {
            zeroInput(value.inputs[i0]);
        }
        value.inputsCount = 0;
    }

    // checkWriteInputPacket is writeInputPacket's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteInputPacket(InputPacket value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= inputPacketMaxBytes;
        return true;
    }

    // writeInputPacket packs value into data — the trusted writer (contracts in the
    // checkWriteInputPacket predicate, one dormant assert call without -ea). The buffer
    // must hold inputPacketMaxBytes. Returns the bytes written.
    public static int writeInputPacket(InputPacket value, byte[] data) {
        assert checkWriteInputPacket(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.synchronizeSequence & 0xffffL;
        scratch |= v << scratchBits;
        scratchBits += 16;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (16 - scratchBits);
        }
        v = value.currentFrame & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.currentFrame >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.startFrame & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.startFrame >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        if (value.inputsCount < 0 || value.inputsCount > 16) {
            return -1; // a count outside its wire range is refused in every build (SPEC §4.6)
        }
        v = (value.inputsCount) & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        for (int i0 = 0; i0 < value.inputsCount; i0++) {
            final Input e0 = value.inputs[i0];
            v = Float.floatToRawIntBits(e0.stickX) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = Float.floatToRawIntBits(e0.stickY) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = Float.floatToRawIntBits(e0.throttle) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = Float.floatToRawIntBits(e0.yaw) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = Float.floatToRawIntBits(e0.pitch) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = e0.fire ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.altFire ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.boost ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.brake ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.aim ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.lockOn ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.zoom ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
            v = e0.ping ? 1 : 0;
            scratch |= v << scratchBits;
            scratchBits += 1;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (1 - scratchBits);
            }
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readInputPacket decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readInputPacket(InputPacket value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 144 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffL;
        bitsRead += 16;
        value.synchronizeSequence = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.currentFrame = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.startFrame = lo;
        if (bitsRead + 5 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        if (v > 16) {
            return false; // the count guards the loop — reject, never clamp
        }
        value.inputsCount = (int) v;
        if (bitsRead + value.inputsCount * 168L > numBits) {
            return false;
        }
        for (int i0 = 0; i0 < value.inputsCount; i0++) {
            final Input e0 = value.inputs[i0];
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.stickX = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.stickY = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.throttle = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.yaw = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xffffffffL;
            bitsRead += 32;
            e0.pitch = Float.intBitsToFloat((int) v);
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.fire = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.altFire = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.boost = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.brake = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.aim = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.lockOn = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.zoom = v != 0;
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0x1L;
            bitsRead += 1;
            e0.ping = v != 0;
        }
        return true;
    }

    // measureInputPacket is the exact wire bits writeInputPacket would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureInputPacket(InputPacket value) {
        int bits = 0;
        bits += 149;
        bits += value.inputsCount * 168;
        return bits;
    }

    // type ShipCreate
    public static final class ShipCreate {
        public byte shipType;
        public final QuantizedPosition position = new QuantizedPosition();
        public final QuantizedRotation rotation = new QuantizedRotation();
        public final QuantizedVelocity linearVelocity = new QuantizedVelocity();
        public boolean hasFlags;

        // has_flags — wire branch; storage holds both sides, a read zeroes the
        // untaken side (SPEC §5)
        // ShipFlags — consumed as masks, uint64 storage (SPEC §4.2)
        public long flags;

        public byte team;
        // wire [0, 1000]
        public short health;
        // wire [0, 100]
        public byte thrust;
        public byte pending;
    }

    // shipCreateMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // shipCreateMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int shipCreateMaxBits = 219;
    public static final int shipCreateMaxBytes = 32;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroShipCreate(ShipCreate value) {
        value.shipType = 0;
        zeroQuantizedPosition(value.position);
        zeroQuantizedRotation(value.rotation);
        zeroQuantizedVelocity(value.linearVelocity);
        value.hasFlags = false;
        value.flags = 0;
        value.team = 0;
        value.health = 0;
        value.thrust = 0;
        value.pending = 0;
    }

    // checkWriteShipCreate is writeShipCreate's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteShipCreate(ShipCreate value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= shipCreateMaxBytes;
        assert (value.shipType & 0xffL) >= 0;
        assert (value.shipType & 0xffL) <= 5;
        assert value.position.x >= -8388608;
        assert value.position.x <= 8388608;
        assert value.position.y >= -8388608;
        assert value.position.y <= 8388608;
        assert value.position.z >= -8388608;
        assert value.position.z <= 8388608;
        assert value.rotation.x >= -1024;
        assert value.rotation.x <= 1024;
        assert value.rotation.y >= -1024;
        assert value.rotation.y <= 1024;
        assert value.rotation.z >= -1024;
        assert value.rotation.z <= 1024;
        assert value.rotation.w >= -1024;
        assert value.rotation.w <= 1024;
        assert value.linearVelocity.x >= -2097152;
        assert value.linearVelocity.x <= 2097152;
        assert value.linearVelocity.y >= -2097152;
        assert value.linearVelocity.y <= 2097152;
        assert value.linearVelocity.z >= -2097152;
        assert value.linearVelocity.z <= 2097152;
        if (value.hasFlags) {
            assert value.flags >>> 4 == 0;
        }
        assert (value.team & 0xffL) >= 0;
        assert (value.team & 0xffL) <= 2;
        assert value.health >= 0;
        assert value.health <= 1000;
        assert value.thrust >= 0;
        assert value.thrust <= 100;
        assert (value.pending & 0xffL) >= 0;
        assert (value.pending & 0xffL) <= 0;
        return true;
    }

    // writeShipCreate packs value into data — the trusted writer (contracts in the
    // checkWriteShipCreate predicate, one dormant assert call without -ea). The buffer
    // must hold shipCreateMaxBytes. Returns the bytes written.
    public static int writeShipCreate(ShipCreate value, byte[] data) {
        assert checkWriteShipCreate(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = value.shipType & 0x7L;
        scratch |= v << scratchBits;
        scratchBits += 3;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (3 - scratchBits);
        }
        v = (value.position.x + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = (value.position.y + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = (value.position.z + 8388608) & 0x1ffffffL;
        scratch |= v << scratchBits;
        scratchBits += 25;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (25 - scratchBits);
        }
        v = (value.rotation.x + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.rotation.y + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.rotation.z + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.rotation.w + 1024) & 0xfffL;
        scratch |= v << scratchBits;
        scratchBits += 12;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (12 - scratchBits);
        }
        v = (value.linearVelocity.x + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = (value.linearVelocity.y + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = (value.linearVelocity.z + 2097152) & 0x7fffffL;
        scratch |= v << scratchBits;
        scratchBits += 23;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (23 - scratchBits);
        }
        v = value.hasFlags ? 1 : 0;
        scratch |= v << scratchBits;
        scratchBits += 1;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (1 - scratchBits);
        }
        if (value.hasFlags) {
            v = value.flags & 0xfL;
            scratch |= v << scratchBits;
            scratchBits += 4;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (4 - scratchBits);
            }
        }
        v = value.team & 0x3L;
        scratch |= v << scratchBits;
        scratchBits += 2;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (2 - scratchBits);
        }
        v = (value.health) & 0x3ffL;
        scratch |= v << scratchBits;
        scratchBits += 10;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (10 - scratchBits);
        }
        v = (value.thrust) & 0x7fL;
        scratch |= v << scratchBits;
        scratchBits += 7;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (7 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readShipCreate decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readShipCreate(ShipCreate value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 196 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7L;
        bitsRead += 3;
        if (v > 5) {
            return false; // headroom above the wire range is refused
        }
        value.shipType = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.position.x = (int) (v + -8388608);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.position.y = (int) (v + -8388608);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1ffffffL;
        bitsRead += 25;
        if (v > 16777216) {
            return false; // a smuggled offset is refused
        }
        value.position.z = (int) (v + -8388608);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.rotation.x = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.rotation.y = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.rotation.z = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xfffL;
        bitsRead += 12;
        if (v > 2048) {
            return false; // a smuggled offset is refused
        }
        value.rotation.w = (short) (v + -1024);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.linearVelocity.x = (int) (v + -2097152);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.linearVelocity.y = (int) (v + -2097152);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fffffL;
        bitsRead += 23;
        if (v > 4194304) {
            return false; // a smuggled offset is refused
        }
        value.linearVelocity.z = (int) (v + -2097152);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1L;
        bitsRead += 1;
        value.hasFlags = v != 0;
        if (value.hasFlags) {
            if (bitsRead + 4 > numBits) {
                return false;
            }
            if (bitsRead >>> 3 < tailBase) {
                window = (long) LONG_LE.get(data, bitsRead >>> 3);
                shift = bitsRead & 7;
            } else {
                window = tailWord;
                shift = bitsRead - tailBase * 8;
            }
            v = (window >>> shift) & 0xfL;
            bitsRead += 4;
            value.flags = v;
        } else {
            value.flags = 0;
        }
        if (bitsRead + 19 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3L;
        bitsRead += 2;
        if (v > 2) {
            return false; // headroom above the wire range is refused
        }
        value.team = (byte) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x3ffL;
        bitsRead += 10;
        if (v > 1000) {
            return false; // a smuggled offset is refused
        }
        value.health = (short) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7fL;
        bitsRead += 7;
        if (v > 100) {
            return false; // a smuggled offset is refused
        }
        value.thrust = (byte) v;
        value.pending = 0;
        return true;
    }

    // measureShipCreate is the exact wire bits writeShipCreate would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureShipCreate(ShipCreate value) {
        int bits = 0;
        bits += 196;
        if (value.hasFlags) {
            bits += 4;
        }
        bits += 19;
        return bits;
    }

    // type ExpressionProbe
    public static final class ExpressionProbe {
        // wire [0, 31]
        public int hardpointIndex;
        // specified default at construction; zero* gives the §5 zero form; wire [1024, 2048]
        public int spinRate = Constants.rotationUnits;
    }

    // expressionProbeMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // expressionProbeMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int expressionProbeMaxBits = 16;
    public static final int expressionProbeMaxBytes = 8;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroExpressionProbe(ExpressionProbe value) {
        value.hardpointIndex = 0;
        value.spinRate = 0;
    }

    // checkWriteExpressionProbe is writeExpressionProbe's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteExpressionProbe(ExpressionProbe value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= expressionProbeMaxBytes;
        assert value.hardpointIndex >= 0;
        assert value.hardpointIndex <= 31;
        assert value.spinRate >= 1024;
        assert value.spinRate <= 2048;
        return true;
    }

    // writeExpressionProbe packs value into data — the trusted writer (contracts in the
    // checkWriteExpressionProbe predicate, one dormant assert call without -ea). The buffer
    // must hold expressionProbeMaxBytes. Returns the bytes written.
    public static int writeExpressionProbe(ExpressionProbe value, byte[] data) {
        assert checkWriteExpressionProbe(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        v = (value.hardpointIndex) & 0x1fL;
        scratch |= v << scratchBits;
        scratchBits += 5;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (5 - scratchBits);
        }
        v = (value.spinRate - 1024) & 0x7ffL;
        scratch |= v << scratchBits;
        scratchBits += 11;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (11 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readExpressionProbe decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readExpressionProbe(ExpressionProbe value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        if (bitsRead + 16 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x1fL;
        bitsRead += 5;
        value.hardpointIndex = (int) v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0x7ffL;
        bitsRead += 11;
        if (v > 1024) {
            return false; // a smuggled offset is refused
        }
        value.spinRate = (int) (v + 1024);
        return true;
    }

    // measureExpressionProbe is the exact wire bits writeExpressionProbe would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureExpressionProbe(ExpressionProbe value) {
        return 16;
    }

    public static final long floorLimit = 0x8000000000000000L;

    public static final long ceilingCount = 0xffffffffffffffffL;

    // type ExtremeProbe
    public static final class ExtremeProbe {
        // wire [-9223372036854775808, 100]
        public long floorBound;
        // wire [-9223372036854775808, 100]
        public long doubledFloor;
        // specified default at construction; zero* gives the §5 zero form; wire [1, 18446744073709551615]
        public long ceilingRange = 1;
        // specified default at construction; zero* gives the §5 zero form
        public long floorDefault = 0x8000000000000000L;
        // specified default at construction; zero* gives the §5 zero form
        public long ceilingDefault = 0xffffffffffffffffL;
    }

    // extremeProbeMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // extremeProbeMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int extremeProbeMaxBits = 320;
    public static final int extremeProbeMaxBytes = 40;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroExtremeProbe(ExtremeProbe value) {
        value.floorBound = 0;
        value.doubledFloor = 0;
        value.ceilingRange = 0;
        value.floorDefault = 0;
        value.ceilingDefault = 0;
    }

    // checkWriteExtremeProbe is writeExtremeProbe's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteExtremeProbe(ExtremeProbe value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= extremeProbeMaxBytes;
        assert value.floorBound <= 100;
        assert value.doubledFloor <= 100;
        assert Long.compareUnsigned(value.ceilingRange, 1) >= 0;
        return true;
    }

    // writeExtremeProbe packs value into data — the trusted writer (contracts in the
    // checkWriteExtremeProbe predicate, one dormant assert call without -ea). The buffer
    // must hold extremeProbeMaxBytes. Returns the bytes written.
    public static int writeExtremeProbe(ExtremeProbe value, byte[] data) {
        assert checkWriteExtremeProbe(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        {
            final long off = value.floorBound - 0x8000000000000000L;
            v = off & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (off >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long off = value.doubledFloor - 0x8000000000000000L;
            v = off & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (off >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long off = value.ceilingRange - 1;
            v = off & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (off >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = value.floorDefault & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.floorDefault >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.ceilingDefault & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.ceilingDefault >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readExtremeProbe decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readExtremeProbe(ExtremeProbe value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 320 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        if (Long.compareUnsigned(lo, 0x8000000000000064L) > 0) {
            return false; // a smuggled offset is refused
        }
        value.floorBound = (lo + 0x8000000000000000L);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        if (Long.compareUnsigned(lo, 0x8000000000000064L) > 0) {
            return false; // a smuggled offset is refused
        }
        value.doubledFloor = (lo + 0x8000000000000000L);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        if (Long.compareUnsigned(lo, 0xfffffffffffffffeL) > 0) {
            return false; // a smuggled offset is refused
        }
        value.ceilingRange = (lo + 1);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.floorDefault = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.ceilingDefault = lo;
        return true;
    }

    // measureExtremeProbe is the exact wire bits writeExtremeProbe would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureExtremeProbe(ExtremeProbe value) {
        return 320;
    }

    // type ExtremeRow
    public static final class ExtremeRow {
        // wire [-9223372036854775808, 100]
        public long clampedFloor;
        // specified default at construction; zero* gives the §5 zero form; wire [1, 18446744073709551614]
        public long clampedCeiling = 1;
        // specified default at construction; zero* gives the §5 zero form
        public long floorDef = 0x8000000000000000L;
        // specified default at construction; zero* gives the §5 zero form
        public long ceilingDef = 0xffffffffffffffffL;
    }

    // extremeRowMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
    // extremeRowMaxBytes is rounded up to the 8-byte write-buffer granularity.
    public static final int extremeRowMaxBits = 256;
    public static final int extremeRowMaxBytes = 32;

    // The §5 zero form: all-zero storage; specified defaults live only in
    // construction.
    public static void zeroExtremeRow(ExtremeRow value) {
        value.clampedFloor = 0;
        value.clampedCeiling = 0;
        value.floorDef = 0;
        value.ceilingDef = 0;
    }

    // checkWriteExtremeRow is writeExtremeRow's contract walk, called once through assert —
    // the predicate-extraction form: dormant assert bodies count against the
    // JIT's inline thresholds, so the hot body carries one small call and the
    // contracts live here (issue #156).
    private static boolean checkWriteExtremeRow(ExtremeRow value, byte[] data) {
        assert data.length % 8 == 0;
        assert data.length >= extremeRowMaxBytes;
        assert value.clampedFloor <= 100;
        assert Long.compareUnsigned(value.clampedCeiling, 1) >= 0;
        assert Long.compareUnsigned(value.clampedCeiling, 0xfffffffffffffffeL) <= 0;
        return true;
    }

    // writeExtremeRow packs value into data — the trusted writer (contracts in the
    // checkWriteExtremeRow predicate, one dormant assert call without -ea). The buffer
    // must hold extremeRowMaxBytes. Returns the bytes written.
    public static int writeExtremeRow(ExtremeRow value, byte[] data) {
        assert checkWriteExtremeRow(value, data);
        long scratch = 0;
        int scratchBits = 0;
        int wordIndex = 0;
        long v = 0;
        {
            final long off = value.clampedFloor - 0x8000000000000000L;
            v = off & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (off >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        {
            final long off = value.clampedCeiling - 1;
            v = off & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
            v = (off >>> 32) & 0xffffffffL;
            scratch |= v << scratchBits;
            scratchBits += 32;
            if (scratchBits >= 64) {
                LONG_LE.set(data, wordIndex * 8, scratch);
                wordIndex++;
                scratchBits -= 64;
                scratch = v >>> (32 - scratchBits);
            }
        }
        v = value.floorDef & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.floorDef >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = value.ceilingDef & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        v = (value.ceilingDef >>> 32) & 0xffffffffL;
        scratch |= v << scratchBits;
        scratchBits += 32;
        if (scratchBits >= 64) {
            LONG_LE.set(data, wordIndex * 8, scratch);
            wordIndex++;
            scratchBits -= 64;
            scratch = v >>> (32 - scratchBits);
        }
        if (scratchBits != 0) {
            LONG_LE.set(data, wordIndex * 8, scratch);
        }
        return wordIndex * 8 + ((scratchBits + 7) >>> 3);
    }

    // readExtremeRow decodes value from the first numBits of data — the family read
    // verdict: false rejects the wire (bounds, ranges, wire constants, padding);
    // hostile bytes never throw. No slack past the payload is required.
    public static boolean readExtremeRow(ExtremeRow value, byte[] data, int numBits) {
        if (numBits > (long) data.length * 8) {
            return false; // the payload cannot exceed the buffer behind data
        }
        // the final 64-bit window, assembled once so every load stays inside
        // the buffer (the family's no-slack reader stance)
        int tailBase = data.length - 8;
        long tailWord = 0;
        if (tailBase >= 0) {
            tailWord = (long) LONG_LE.get(data, tailBase);
        } else {
            tailBase = 0;
            for (int i = data.length - 1; i >= 0; i--) {
                tailWord = (tailWord << 8) | (data[i] & 0xffL);
            }
        }
        int bitsRead = 0;
        long window = 0;
        int shift = 0;
        long v = 0;
        long lo = 0;
        if (bitsRead + 256 > numBits) {
            return false;
        }
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        if (Long.compareUnsigned(lo, 0x8000000000000064L) > 0) {
            return false; // a smuggled offset is refused
        }
        value.clampedFloor = (lo + 0x8000000000000000L);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        if (Long.compareUnsigned(lo, 0xfffffffffffffffdL) > 0) {
            return false; // a smuggled offset is refused
        }
        value.clampedCeiling = (lo + 1);
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.floorDef = lo;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo = v;
        if (bitsRead >>> 3 < tailBase) {
            window = (long) LONG_LE.get(data, bitsRead >>> 3);
            shift = bitsRead & 7;
        } else {
            window = tailWord;
            shift = bitsRead - tailBase * 8;
        }
        v = (window >>> shift) & 0xffffffffL;
        bitsRead += 32;
        lo |= v << 32;
        value.ceilingDef = lo;
        return true;
    }

    // measureExtremeRow is the exact wire bits writeExtremeRow would produce for value —
    // trusted like the writer; static runs fold to literals at generation time.
    public static int measureExtremeRow(ExtremeRow value) {
        return 256;
    }
}
