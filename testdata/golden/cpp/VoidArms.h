// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

#pragma once

#include <cstdint>

namespace example {

// type VoidPulse
struct VoidPulse {
    uint16_t strength = 0;
};

inline constexpr int64_t VoidPulseMaxBits = 16; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t VoidPulseMaxBytes = 8; // rounded up to the 8-byte write-buffer granularity; a read buffer's allocation must extend at least 8 bytes past the data — the reader loads 64-bit windows

// type VoidLevel
struct VoidLevel {
    uint8_t level = 0; // wire [0, 7]
};

inline constexpr int64_t VoidLevelMaxBits = 3; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t VoidLevelMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class VoidSignalType : uint8_t {
    None = 0,
    Ping = 1,
    Pulse = 2,
    Level = 3,
    Ack = 4,
    Count = 4, // the declared variant count (SPEC §4.2)
    Max = 4, // the exported extent (SPEC §4.2)
};

// EnumName: debug/log name for any VoidSignalType value, out-of-set included
inline const char * EnumName( VoidSignalType value )
{
    switch ( value )
    {
        case VoidSignalType::None: return "None";
        case VoidSignalType::Ping: return "Ping";
        case VoidSignalType::Pulse: return "Pulse";
        case VoidSignalType::Level: return "Level";
        case VoidSignalType::Ack: return "Ack";
        default: return "???";
    }
}

// union VoidSignal — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm is freshly constructed with its defaults
// when selected, by ReadVoidSignal before decoding (SPEC §4.8), or by application code.
// Payload-free arms select by assigning the tag alone; they have no storage.
// Application code must include <new> before constructing an arm in place:
// ::new ( (void*) &value.pulse ) VoidPulse{}; value.type = VoidSignalType::Pulse;
// Bytes of unselected arms are indeterminate.
struct VoidSignal
{
    VoidSignalType type;

    union
    {
        VoidPulse pulse;
        VoidLevel level;
    };

    VoidSignal() : type( VoidSignalType::None ) {} // the tag only — arms are freshly constructed at selection
};

inline constexpr int64_t VoidSignalMaxBits = 19; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t VoidSignalMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type VoidSignalPacket
struct VoidSignalPacket {
    uint32_t lead = 0;
    VoidSignal signal;
    VoidSignal echo;
    VoidSignal history[2] = {}; // used count beside it; wire count in [0, 2]
    int32_t history_count = 0;
    uint32_t tail = 0;
};

inline constexpr int64_t VoidSignalPacketMaxBits = 84; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t VoidSignalPacketMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

} // namespace example
