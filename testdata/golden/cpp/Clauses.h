// Code generated by the schema compiler from Clauses.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x91a8e85156dfe2b1

#pragma once

#include <cstdint>

namespace example {

// type W13
struct W13 {
    uint16_t items[12] = {}; // used count beside it; wire count in [0, 12]
    int32_t items_count = 0;
};

inline constexpr int64_t W13MaxBits = 160; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W13MaxBytes = 24; // rounded up to the 8-byte write-buffer granularity; a read buffer's allocation must extend at least 8 bytes past the data — the reader loads 64-bit windows

// type W17
struct W17 {
    uint32_t items[9] = {}; // used count beside it; wire count in [0, 9]
    int32_t items_count = 0;
};

inline constexpr int64_t W17MaxBits = 157; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W17MaxBytes = 24; // 8-byte write granularity; read slack per the contract above

// type W26
struct W26 {
    uint32_t items[6] = {}; // used count beside it; wire count in [0, 6]
    int32_t items_count = 0;
};

inline constexpr int64_t W26MaxBits = 159; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W26MaxBytes = 24; // 8-byte write granularity; read slack per the contract above

// type W1
struct W1 {
    uint8_t items[20] = {}; // used count beside it; wire count in [0, 20]
    int32_t items_count = 0;
};

inline constexpr int64_t W1MaxBits = 25; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W1MaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type W52
struct W52 {
    uint64_t items[3] = {}; // used count beside it; wire count in [0, 3]
    int32_t items_count = 0;
};

inline constexpr int64_t W52MaxBits = 158; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W52MaxBytes = 24; // 8-byte write granularity; read slack per the contract above

// type W50
struct W50 {
    uint64_t items[3] = {}; // used count beside it; wire count in [0, 3]
    int32_t items_count = 0;
};

inline constexpr int64_t W50MaxBits = 152; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t W50MaxBytes = 24; // 8-byte write granularity; read slack per the contract above

// type F13
struct F13 {
    uint16_t items[7] = {}; // wire [0, 8191]
};

inline constexpr int64_t F13MaxBits = 91; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t F13MaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// type Tri3
struct Tri3 {
    uint32_t a = 0;
    uint32_t b = 0;
};

inline constexpr int64_t Tri3MaxBits = 3; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t Tri3MaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArrTri3
struct ArrTri3 {
    Tri3 items[10] = {}; // used count beside it; wire count in [0, 10]
    int32_t items_count = 0;
};

inline constexpr int64_t ArrTri3MaxBits = 34; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArrTri3MaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type Eleven
struct Eleven {
    uint32_t a = 0;
    uint32_t b = 0;
};

inline constexpr int64_t ElevenMaxBits = 11; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ElevenMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type ArrEleven
struct ArrEleven {
    Eleven items[9] = {};
};

inline constexpr int64_t ArrElevenMaxBits = 99; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArrElevenMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// type EmptyA
struct EmptyA {
};

inline constexpr int64_t EmptyAMaxBits = 0; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t EmptyAMaxBytes = 0; // 8-byte write granularity; read slack per the contract above

// type EmptyB
struct EmptyB {
};

inline constexpr int64_t EmptyBMaxBits = 0; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t EmptyBMaxBytes = 0; // 8-byte write granularity; read slack per the contract above

// EmptyUnionType: union EmptyUnion's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class EmptyUnionType : uint8_t {
    None = 0,
    A = 1,
    B = 2,
    Max = 2, // the exported extent (SPEC §4.2)
};

// union EmptyUnion — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm's storage is established ZEROED
// when the arm is selected — by ReadEmptyUnion before it decodes (SPEC §5), or by
// assigning it: value.a = EmptyA{}. Bytes of unselected arms are indeterminate.
struct EmptyUnion
{
    EmptyUnionType type;

    union
    {
        EmptyA a;
        EmptyB b;
    };

    EmptyUnion() : type( EmptyUnionType::None ) {} // the tag only — arms are zero-established at selection
};

inline constexpr int64_t EmptyUnionMaxBits = 2; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t EmptyUnionMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type HoldsEmptyUnion
struct HoldsEmptyUnion {
    uint32_t lead = 0;
    EmptyUnion u;
    uint32_t tail = 0;
};

inline constexpr int64_t HoldsEmptyUnionMaxBits = 14; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t HoldsEmptyUnionMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type Strs
struct Strs {
    uint32_t lead = 0;
    char s[8 + 1] = {}; // string(8): max length, used length beside it (SPEC §4.7)
    int32_t s_length = 0;
    uint8_t b[8] = {}; // bytes(8): fixed buffer, used length beside it (SPEC §4.7)
    int32_t b_length = 0;
    uint32_t tail = 0;
};

inline constexpr int64_t StrsMaxBits = 158; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t StrsMaxBytes = 24; // 8-byte write granularity; read slack per the contract above

// type ArrNested
struct ArrNested {
    uint32_t lead = 0;
    Eleven items[4] = {}; // used count beside it; wire count in [0, 4]
    int32_t items_count = 0;
    uint32_t tail = 0;
};

inline constexpr int64_t ArrNestedMaxBits = 55; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t ArrNestedMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

// type Sole
struct Sole {
    uint32_t only = 0;
};

inline constexpr int64_t SoleMaxBits = 13; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t SoleMaxBytes = 8; // 8-byte write granularity; read slack per the contract above

} // namespace example
