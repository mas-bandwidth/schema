// Code generated by the schema compiler from ArmDefaults.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x8656ae68c06b97a7

#pragma once

#include <cstdint>

#include "Wire.h"

namespace example {

// type DefaultArm
struct DefaultArm {
    ProbeConfig entries[2] = {}; // used count beside it; wire count in [0, 2]
    int32_t entries_count = 0;
    uint8_t marker = 0; // wire [0, 7]
};

inline constexpr int64_t DefaultArmMaxBits = 77; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t DefaultArmMaxBytes = 16; // rounded up to the 8-byte write-buffer granularity; a read buffer's allocation must extend at least 8 bytes past the data — the reader loads 64-bit windows

// DefaultChoiceType: union DefaultChoice's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class DefaultChoiceType : uint8_t {
    None = 0,
    First = 1,
    Second = 2,
    Count = 2, // the declared variant count (SPEC §4.2)
    Max = 2, // the exported extent (SPEC §4.2)
};

// EnumName: debug/log name for any DefaultChoiceType value, out-of-set included
inline const char * EnumName( DefaultChoiceType value )
{
    switch ( value )
    {
        case DefaultChoiceType::None: return "None";
        case DefaultChoiceType::First: return "First";
        case DefaultChoiceType::Second: return "Second";
        default: return "???";
    }
}

// union DefaultChoice — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm is freshly constructed with its defaults
// when selected, by ReadDefaultChoice before decoding (SPEC §4.8), or by application code.
// Application code must include <new> before constructing an arm in place:
// ::new ( (void*) &value.first ) DefaultArm{}; value.type = DefaultChoiceType::First;
// Bytes of unselected arms are indeterminate.
struct DefaultChoice
{
    DefaultChoiceType type;

    union
    {
        DefaultArm first;
        DefaultArm second;
    };

    DefaultChoice() : type( DefaultChoiceType::None ) {} // the tag only — arms are freshly constructed at selection
};

inline constexpr int64_t DefaultChoiceMaxBits = 79; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t DefaultChoiceMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// type DefaultChoicePacket
struct DefaultChoicePacket {
    DefaultChoice choice;
};

inline constexpr int64_t DefaultChoicePacketMaxBits = 79; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t DefaultChoicePacketMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// type DefaultBulkArm
struct DefaultBulkArm {
    DefaultArm payload;
    uint8_t data[2] = {}; // bytes(2): fixed buffer, used length beside it (SPEC §4.7)
    int32_t data_length = 0;
};

inline constexpr int64_t DefaultBulkArmMaxBits = 102; // longest wire path; align pads at worst case (SPEC §6.1)
inline constexpr int64_t DefaultBulkArmMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

// DefaultBulkChoiceType: union DefaultBulkChoice's tag — None = 0, then each variant in declared order (SPEC §4.8)
enum class DefaultBulkChoiceType : uint8_t {
    None = 0,
    First = 1,
    Second = 2,
    Count = 2, // the declared variant count (SPEC §4.2)
    Max = 2, // the exported extent (SPEC §4.2)
};

// EnumName: debug/log name for any DefaultBulkChoiceType value, out-of-set included
inline const char * EnumName( DefaultBulkChoiceType value )
{
    switch ( value )
    {
        case DefaultBulkChoiceType::None: return "None";
        case DefaultBulkChoiceType::First: return "First";
        case DefaultBulkChoiceType::Second: return "Second";
        default: return "???";
    }
}

// union DefaultBulkChoice — at most one of the arms; the tag says which. Construction is
// None: the tag alone is initialized; an arm is freshly constructed with its defaults
// when selected, by ReadDefaultBulkChoice before decoding (SPEC §4.8), or by application code.
// Application code must include <new> before constructing an arm in place:
// ::new ( (void*) &value.first ) DefaultBulkArm{}; value.type = DefaultBulkChoiceType::First;
// Bytes of unselected arms are indeterminate.
struct DefaultBulkChoice
{
    DefaultBulkChoiceType type;

    union
    {
        DefaultBulkArm first;
        DefaultBulkArm second;
    };

    DefaultBulkChoice() : type( DefaultBulkChoiceType::None ) {} // the tag only — arms are freshly constructed at selection
};

inline constexpr int64_t DefaultBulkChoiceMaxBits = 104; // tag + the largest arm; None costs the tag only (SPEC §4.8)
inline constexpr int64_t DefaultBulkChoiceMaxBytes = 16; // 8-byte write granularity; read slack per the contract above

} // namespace example
