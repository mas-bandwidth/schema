// Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xa89dd9e036603208

#pragma once

#include <cstdint>

#include "serialize.h"

#include "Degenerate.h"

namespace example {

#ifndef SCHEMA_WRITE_INLINE_DEFINED
#define SCHEMA_WRITE_INLINE_DEFINED
// SCHEMA_WRITE_INLINE — how every generated Write function is spelled: an
// inlining DEMAND (always_inline / __forceinline), the serialize family's
// remedy for LLVM refusing the linkonce_odr Write entries into their call
// sites at cost over threshold — no last-call-to-static bonus exists for a
// header function, so unlike C's static entries these never flatten on
// their own. Measured and shipped; branch-weight hints are NOT the fix
// here — measured in this family to invite the machine outliner into the
// hot bodies. Do not add them.
#if defined( _MSC_VER )
#define SCHEMA_WRITE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_WRITE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_WRITE_INLINE inline
#endif
#endif // SCHEMA_WRITE_INLINE_DEFINED

#ifndef SCHEMA_READ_INLINE_DEFINED
#define SCHEMA_READ_INLINE_DEFINED
// SCHEMA_READ_INLINE — how every generated Read function is spelled: an
// inlining DEMAND (always_inline / __forceinline), the serialize family's
// remedy for LLVM's fallible-chain frequency decay: each Ok/Err split is
// priced at ~even odds, block frequency decays geometrically down a read
// chain, and later call sites are held to the cold-callsite inline
// threshold. Measured and shipped; branch-weight hints are NOT the fix
// here — measured in this family to invite the machine outliner into the
// hot bodies. Do not add them.
#if defined( _MSC_VER )
#define SCHEMA_READ_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_READ_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_READ_INLINE inline
#endif
#endif // SCHEMA_READ_INLINE_DEFINED

SCHEMA_WRITE_INLINE bool WriteVec2( serialize::WriteStream & stream, const Vec2 & value )
{
    write_double( stream, value.x );
    write_double( stream, value.y );
    return true;
}

SCHEMA_READ_INLINE bool ReadVec2( serialize::ReadStream & stream, Vec2 & value )
{
    read_double( stream, value.x );
    read_double( stream, value.y );
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanF64( serialize::WriteStream & stream, const SpanF64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_double( stream, value.values[i] );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanF64( serialize::ReadStream & stream, SpanF64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        read_double( stream, value.values[i] );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanU64( serialize::WriteStream & stream, const SpanU64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_bits( stream, value.values[i], 64 );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanU64( serialize::ReadStream & stream, SpanU64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        read_bits( stream, value.values[i], 64 );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanI64( serialize::WriteStream & stream, const SpanI64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_bits( stream, value.values[i], 64 );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanI64( serialize::ReadStream & stream, SpanI64 & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        {
            uint64_t raw_value = 0;
            read_bits( stream, raw_value, 64 );
            value.values[i] = int64_t( raw_value );
        }
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanOne( serialize::WriteStream & stream, const SpanOne & value )
{
    for ( int32_t i = 0; i < 1; i++ )
    {
        write_bits( stream, value.values[i], 64 );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanOne( serialize::ReadStream & stream, SpanOne & value )
{
    for ( int32_t i = 0; i < 1; i++ )
    {
        read_bits( stream, value.values[i], 64 );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanChunk( serialize::WriteStream & stream, const SpanChunk & value )
{
    for ( int32_t i = 0; i < 4; i++ )
    {
        write_bits( stream, value.values[i], 16 );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanChunk( serialize::ReadStream & stream, SpanChunk & value )
{
    for ( int32_t i = 0; i < 4; i++ )
    {
        {
            uint32_t raw_value = 0;
            read_bits( stream, raw_value, 16 );
            value.values[i] = uint16_t( raw_value );
        }
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanTail( serialize::WriteStream & stream, const SpanTail & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_double( stream, value.values[i] );
    }
    write_bits( stream, value.tail, 32 );
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanTail( serialize::ReadStream & stream, SpanTail & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        read_double( stream, value.values[i] );
    }
    read_bits( stream, value.tail, 32 );
    return true;
}

SCHEMA_WRITE_INLINE bool WriteSpanTwice( serialize::WriteStream & stream, const SpanTwice & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_double( stream, value.a[i] );
    }
    for ( int32_t i = 0; i < 2; i++ )
    {
        write_double( stream, value.b[i] );
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadSpanTwice( serialize::ReadStream & stream, SpanTwice & value )
{
    for ( int32_t i = 0; i < 2; i++ )
    {
        read_double( stream, value.a[i] );
    }
    for ( int32_t i = 0; i < 2; i++ )
    {
        read_double( stream, value.b[i] );
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteTrio( serialize::WriteStream & stream, const Trio & value )
{
    write_bits( stream, value.a, 20 );
    write_bits( stream, value.b, 20 );
    write_bits( stream, value.c, 24 );
    return true;
}

SCHEMA_READ_INLINE bool ReadTrio( serialize::ReadStream & stream, Trio & value )
{
    read_bits( stream, value.a, 20 );
    read_bits( stream, value.b, 20 );
    read_bits( stream, value.c, 24 );
    return true;
}

SCHEMA_WRITE_INLINE bool WriteTrioSole( serialize::WriteStream & stream, const TrioSole & value )
{
    if ( !WriteTrio( stream, value.inner ) )
    {
        return false;
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadTrioSole( serialize::ReadStream & stream, TrioSole & value )
{
    if ( !ReadTrio( stream, value.inner ) )
    {
        return false;
    }
    return true;
}

SCHEMA_WRITE_INLINE bool WriteTrioFirst( serialize::WriteStream & stream, const TrioFirst & value )
{
    if ( !WriteTrio( stream, value.inner ) )
    {
        return false;
    }
    write_bits( stream, value.trailer, 16 );
    return true;
}

SCHEMA_READ_INLINE bool ReadTrioFirst( serialize::ReadStream & stream, TrioFirst & value )
{
    if ( !ReadTrio( stream, value.inner ) )
    {
        return false;
    }
    read_bits( stream, value.trailer, 16 );
    return true;
}

SCHEMA_WRITE_INLINE bool WriteTrioStraddle( serialize::WriteStream & stream, const TrioStraddle & value )
{
    write_bits( stream, value.pad0, 64 );
    write_bits( stream, value.pad1, 64 );
    write_bits( stream, value.pad2, 64 );
    write_bits( stream, value.pad3, 64 );
    write_bits( stream, value.pad4, 64 );
    write_bits( stream, value.pad5, 24 );
    if ( !WriteTrio( stream, value.inner ) )
    {
        return false;
    }
    return true;
}

SCHEMA_READ_INLINE bool ReadTrioStraddle( serialize::ReadStream & stream, TrioStraddle & value )
{
    read_bits( stream, value.pad0, 64 );
    read_bits( stream, value.pad1, 64 );
    read_bits( stream, value.pad2, 64 );
    read_bits( stream, value.pad3, 64 );
    read_bits( stream, value.pad4, 64 );
    read_bits( stream, value.pad5, 24 );
    if ( !ReadTrio( stream, value.inner ) )
    {
        return false;
    }
    return true;
}

} // namespace example
