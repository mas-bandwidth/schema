# Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
# SPDX-License-Identifier: NONE — this generated output is yours, under terms of
# your choice. See the LICENSE exception in the schema compiler; the compiler is
# AGPL-3.0, its output is not.
# package example — protocol id 0xcaa884d7062cccc8

# type VoidPulse
defmodule Example.VoidPulse do
  defstruct strength: 0
end

# type VoidLevel
defmodule Example.VoidLevel do
  # level: wire [0, 7]
  defstruct level: 0
end

# VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
defmodule Example.VoidSignalType do
  def none, do: 0
  def ping, do: 1
  def pulse, do: 2
  def level, do: 3
  def ack, do: 4
  # the declared variant count (SPEC §4.2)
  def count, do: 4
  # the exported extent (SPEC §4.2)
  def max, do: 4
end

# VoidSignal — at most one of the arms; type says which. Construction is the empty
# union (None). A read builds the selected arm from the wire; unselected
# arms hold their construction form (SPEC §4.8 leaves them unspecified).
# Consumers read the selected arm only.
defmodule Example.VoidSignal do
  defstruct type: 0, pulse: %Example.VoidPulse{}, level: %Example.VoidLevel{}
end

# type VoidSignalPacket
defmodule Example.VoidSignalPacket do
  # history: counted array — a list of up to 2 elements
  defstruct lead: 0,
            signal: %Example.VoidSignal{},
            echo: %Example.VoidSignal{},
            history: [],
            tail: 0
end

# The file-scope surface: constants, flags masks, name helpers and the
# wire functions of every declaration in VoidArms.schema.
defmodule Example.VoidArms do
  import Bitwise

  @compile {:inline, rd: 3}

  @compile {:inline, rdw: 3}

  # void_pulse_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # void_pulse_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def void_pulse_max_bits, do: 16
  def void_pulse_max_bytes, do: 8

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Example.VoidPulse{}).
  def zero_void_pulse, do: %Example.VoidPulse{strength: 0}

  # write_void_pulse packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_void_pulse(value) do
    data = <<>>
    v = value.strength &&& 0xFFFF
    scratch = v
    data = <<data::binary, scratch::little-size(2)-unit(8)>>
    data
  end

  # read_void_pulse decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_void_pulse(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 16 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 16)
      v = rv
      bits_read = bits_read + 16
      v_strength = v
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      value = %Example.VoidPulse{strength: v_strength}
      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_void_pulse is the exact wire bits write_void_pulse would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_void_pulse(_value), do: 16

  # void_level_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # void_level_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def void_level_max_bits, do: 3
  def void_level_max_bytes, do: 8

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Example.VoidLevel{}).
  def zero_void_level, do: %Example.VoidLevel{level: 0}

  # write_void_level packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_void_level(value) do
    data = <<>>

    if value.level < 0 do
      raise ArgumentError, "value.level is below the wire minimum"
    end

    if value.level > 7 do
      raise ArgumentError, "value.level is above the wire maximum"
    end

    v = value.level
    scratch = v
    # the residual byte, statically known to be there
    <<data::binary, scratch>>
  end

  # read_void_level decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_void_level(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 3)
      v = rv
      bits_read = bits_read + 3
      v_level = v
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      value = %Example.VoidLevel{level: v_level}
      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_void_level is the exact wire bits write_void_level would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_void_level(_value), do: 3

  # void_signal_max_bits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
  # void_signal_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def void_signal_max_bits, do: 19
  def void_signal_max_bytes, do: 8

  # enum_name_void_signal_type: debug/log/tooling name for any VoidSignalType wire value —
  # out-of-set values (wire-legal up to the declared max) name as "???"
  def enum_name_void_signal_type(value) do
    case value do
      0 -> "None"
      1 -> "Ping"
      2 -> "Pulse"
      3 -> "Level"
      4 -> "Ack"
      _ -> "???"
    end
  end

  # The §5 zero form — the empty union (None). Arms hold their construction
  # form: every arm is unselected at None, and unselected arms are
  # unspecified by rule (SPEC §4.8).
  def zero_void_signal, do: %Example.VoidSignal{}

  # write_void_signal packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_void_signal(value) do
    data = <<>>

    if value.type < 0 do
      raise ArgumentError, "value.type is below the wire minimum"
    end

    if value.type > 4 do
      raise ArgumentError, "value.type is above the wire maximum"
    end

    v = value.type
    scratch = v

    {data, scratch, scratch_bits} =
      case value.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          scratch_bits = 3
          {data, scratch, scratch_bits}

        2 ->
          v = value.pulse.strength &&& 0xFFFF
          scratch = scratch ||| v <<< 3
          data = <<data::binary, scratch::little-size(2)-unit(8)>>
          scratch = scratch >>> 16
          scratch_bits = 3
          {data, scratch, scratch_bits}

        3 ->
          if value.level.level < 0 do
            raise ArgumentError, "value.level.level is below the wire minimum"
          end

          if value.level.level > 7 do
            raise ArgumentError, "value.level.level is above the wire maximum"
          end

          v = value.level.level
          scratch = scratch ||| v <<< 3
          scratch_bits = 6
          {data, scratch, scratch_bits}

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          scratch_bits = 3
          {data, scratch, scratch_bits}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          scratch_bits = 3
          {data, scratch, scratch_bits}
      end

    if scratch_bits != 0, do: <<data::binary, scratch>>, else: data
  end

  # read_void_signal decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_void_signal(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rdw(data, bits_read, 49)
      v = rv &&& 0x7
      bits_read = bits_read + 3
      # not a wire-legal tag (SPEC §4.8)
      if v > 4, do: throw(:invalid)

      {bits_read, v} =
        case v do
          1 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 1}
            {bits_read, u}

          2 ->
            if bits_read + 16 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 16)
            v = rv
            bits_read = bits_read + 16
            v_pulse_strength = v
            v_pulse = %Example.VoidPulse{strength: v_pulse_strength}
            u = %Example.VoidSignal{type: 2, pulse: v_pulse}
            {bits_read, u}

          3 ->
            if bits_read + 3 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 3)
            v = rv
            bits_read = bits_read + 3
            v_level_level = v
            v_level = %Example.VoidLevel{level: v_level_level}
            u = %Example.VoidSignal{type: 3, level: v_level}
            {bits_read, u}

          4 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 4}
            {bits_read, u}

          _ ->
            # None — the tag is the whole wire (SPEC §4.8)
            {bits_read, %Example.VoidSignal{}}
        end

      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read
      {:ok, v}
    catch
      :invalid -> :error
    end
  end

  # measure_void_signal is the exact wire bits write_void_signal would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_void_signal(value) do
    bits = 0
    bits = bits + 3

    bits =
      case value.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        2 ->
          bits + 16

        3 ->
          bits + 3

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          bits
      end

    bits
  end

  # void_signal_packet_max_bits is the longest wire path; align pads at worst case (SPEC §6.1).
  # void_signal_packet_max_bytes is rounded up to the family 8-byte write-buffer granularity.
  def void_signal_packet_max_bits, do: 84
  def void_signal_packet_max_bytes, do: 16

  # The §5 zero form: all-zero storage; specified defaults live only in
  # construction (%Example.VoidSignalPacket{}).
  def zero_void_signal_packet do
    %Example.VoidSignalPacket{
      lead: 0,
      signal: %Example.VoidSignal{},
      echo: %Example.VoidSignal{},
      history: [],
      tail: 0
    }
  end

  # write_void_signal_packet packs value into a fresh binary — the trusted writer; the O(1)
  # contract checks raise ArgumentError, always on (the BEAM has no
  # compile-out assert). Returns the wire bytes.
  def write_void_signal_packet(value) do
    data = <<>>

    %{
      lead: value_lead,
      signal: value_signal,
      echo: value_echo,
      history: value_history,
      tail: value_tail
    } = value

    v = value_lead &&& 0x7
    scratch = v

    if value_signal.type < 0 do
      raise ArgumentError, "value.signal.type is below the wire minimum"
    end

    if value_signal.type > 4 do
      raise ArgumentError, "value.signal.type is above the wire maximum"
    end

    v = value_signal.type
    scratch = scratch ||| v <<< 3

    {data, scratch, scratch_bits} =
      case value_signal.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          scratch_bits = 6
          {data, scratch, scratch_bits}

        2 ->
          v = value_signal.pulse.strength &&& 0xFFFF
          scratch = scratch ||| v <<< 6
          data = <<data::binary, scratch::little-size(2)-unit(8)>>
          scratch = scratch >>> 16
          scratch_bits = 6
          {data, scratch, scratch_bits}

        3 ->
          if value_signal.level.level < 0 do
            raise ArgumentError, "value.signal.level.level is below the wire minimum"
          end

          if value_signal.level.level > 7 do
            raise ArgumentError, "value.signal.level.level is above the wire maximum"
          end

          v = value_signal.level.level
          scratch = scratch ||| v <<< 6
          data = <<data::binary, scratch::little-size(1)-unit(8)>>
          scratch = scratch >>> 8
          scratch_bits = 1
          {data, scratch, scratch_bits}

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          scratch_bits = 6
          {data, scratch, scratch_bits}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          scratch_bits = 6
          {data, scratch, scratch_bits}
      end

    if value_echo.type < 0 do
      raise ArgumentError, "value.echo.type is below the wire minimum"
    end

    if value_echo.type > 4 do
      raise ArgumentError, "value.echo.type is above the wire maximum"
    end

    v = value_echo.type
    scratch = scratch ||| v <<< scratch_bits
    scratch_bits = scratch_bits + 3
    flush = scratch_bits >>> 3
    data = <<data::binary, scratch::little-size(flush)-unit(8)>>
    scratch = scratch >>> (flush <<< 3)
    scratch_bits = scratch_bits &&& 7

    {data, scratch, scratch_bits} =
      case value_echo.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}

        2 ->
          v = value_echo.pulse.strength &&& 0xFFFF
          scratch = scratch ||| v <<< scratch_bits
          scratch_bits = scratch_bits + 16
          flush = scratch_bits >>> 3
          data = <<data::binary, scratch::little-size(flush)-unit(8)>>
          scratch = scratch >>> (flush <<< 3)
          scratch_bits = scratch_bits &&& 7
          {data, scratch, scratch_bits}

        3 ->
          if value_echo.level.level < 0 do
            raise ArgumentError, "value.echo.level.level is below the wire minimum"
          end

          if value_echo.level.level > 7 do
            raise ArgumentError, "value.echo.level.level is above the wire maximum"
          end

          v = value_echo.level.level
          scratch = scratch ||| v <<< scratch_bits
          scratch_bits = scratch_bits + 3
          flush = scratch_bits >>> 3
          data = <<data::binary, scratch::little-size(flush)-unit(8)>>
          scratch = scratch >>> (flush <<< 3)
          scratch_bits = scratch_bits &&& 7
          {data, scratch, scratch_bits}

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}
      end

    n = length(value_history)

    if n > 2 do
      raise ArgumentError, "value.history count is above the wire maximum"
    end

    v = n
    scratch = scratch ||| v <<< scratch_bits
    scratch_bits = scratch_bits + 2
    flush = scratch_bits >>> 3
    data = <<data::binary, scratch::little-size(flush)-unit(8)>>
    scratch = scratch >>> (flush <<< 3)
    scratch_bits = scratch_bits &&& 7

    {data, scratch, scratch_bits} =
      w_void_signal_packet_history(value_history, data, scratch, scratch_bits)

    v = value_tail &&& 0x7
    scratch = scratch ||| v <<< scratch_bits
    scratch_bits = scratch_bits + 3
    flush = scratch_bits >>> 3
    data = <<data::binary, scratch::little-size(flush)-unit(8)>>
    scratch = scratch >>> (flush <<< 3)
    scratch_bits = scratch_bits &&& 7
    if scratch_bits != 0, do: <<data::binary, scratch>>, else: data
  end

  # read_void_signal_packet decodes the first num_bits of data — the family read verdict:
  # :error rejects the wire (bounds, ranges, wire constants, padding);
  # hostile bytes never raise. No slack past the payload is required.
  def read_void_signal_packet(data, num_bits) when is_binary(data) and is_integer(num_bits) do
    try do
      if num_bits > byte_size(data) * 8 do
        # the payload cannot exceed the buffer behind it
        throw(:invalid)
      end

      bits_read = 0
      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 3)
      v = rv
      bits_read = bits_read + 3
      v_lead = v
      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rdw(data, bits_read, 49)
      v = rv &&& 0x7
      bits_read = bits_read + 3
      # not a wire-legal tag (SPEC §4.8)
      if v > 4, do: throw(:invalid)

      {bits_read, v_signal} =
        case v do
          1 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 1}
            {bits_read, u}

          2 ->
            if bits_read + 16 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 16)
            v = rv
            bits_read = bits_read + 16
            v_signal_pulse_strength = v
            v_signal_pulse = %Example.VoidPulse{strength: v_signal_pulse_strength}
            u = %Example.VoidSignal{type: 2, pulse: v_signal_pulse}
            {bits_read, u}

          3 ->
            if bits_read + 3 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 3)
            v = rv
            bits_read = bits_read + 3
            v_signal_level_level = v
            v_signal_level = %Example.VoidLevel{level: v_signal_level_level}
            u = %Example.VoidSignal{type: 3, level: v_signal_level}
            {bits_read, u}

          4 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 4}
            {bits_read, u}

          _ ->
            # None — the tag is the whole wire (SPEC §4.8)
            {bits_read, %Example.VoidSignal{}}
        end

      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rdw(data, bits_read, 49)
      v = rv &&& 0x7
      bits_read = bits_read + 3
      # not a wire-legal tag (SPEC §4.8)
      if v > 4, do: throw(:invalid)

      {bits_read, v_echo} =
        case v do
          1 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 1}
            {bits_read, u}

          2 ->
            if bits_read + 16 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 16)
            v = rv
            bits_read = bits_read + 16
            v_echo_pulse_strength = v
            v_echo_pulse = %Example.VoidPulse{strength: v_echo_pulse_strength}
            u = %Example.VoidSignal{type: 2, pulse: v_echo_pulse}
            {bits_read, u}

          3 ->
            if bits_read + 3 > num_bits, do: throw(:invalid)
            rv = rd(data, bits_read, 3)
            v = rv
            bits_read = bits_read + 3
            v_echo_level_level = v
            v_echo_level = %Example.VoidLevel{level: v_echo_level_level}
            u = %Example.VoidSignal{type: 3, level: v_echo_level}
            {bits_read, u}

          4 ->
            # a payload-free arm: the tag is the whole wire (SPEC §4.8)
            u = %Example.VoidSignal{type: 4}
            {bits_read, u}

          _ ->
            # None — the tag is the whole wire (SPEC §4.8)
            {bits_read, %Example.VoidSignal{}}
        end

      if bits_read + 2 > num_bits, do: throw(:invalid)
      rv = rdw(data, bits_read, 49)
      v = rv &&& 0x3
      bits_read = bits_read + 2
      # the count guards the loop — reject, never clamp
      if v > 2, do: throw(:invalid)
      n = v
      {bits_read, v_history} = r_void_signal_packet_history(n, [], data, num_bits, bits_read)
      if bits_read + 3 > num_bits, do: throw(:invalid)
      rv = rd(data, bits_read, 3)
      v = rv
      bits_read = bits_read + 3
      v_tail = v
      # the final position is unobserved — the verdict and value are the surface
      _ = bits_read

      value = %Example.VoidSignalPacket{
        lead: v_lead,
        signal: v_signal,
        echo: v_echo,
        history: v_history,
        tail: v_tail
      }

      {:ok, value}
    catch
      :invalid -> :error
    end
  end

  # measure_void_signal_packet is the exact wire bits write_void_signal_packet would produce for value —
  # trusted like the writer; static runs fold to literals at generation time.
  def measure_void_signal_packet(value) do
    bits = 0
    bits = bits + 3
    bits = bits + 3

    bits =
      case value.signal.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        2 ->
          bits + 16

        3 ->
          bits + 3

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          bits
      end

    bits = bits + 3

    bits =
      case value.echo.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        2 ->
          bits + 16

        3 ->
          bits + 3

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          bits
      end

    bits = bits + 2
    bits = m_void_signal_packet_history(value.history, bits)
    bits + 3
  end

  defp w_void_signal_packet_history([], data, scratch, scratch_bits),
    do: {data, scratch, scratch_bits}

  defp w_void_signal_packet_history([e | rest], data, scratch, scratch_bits) do
    if e.type < 0 do
      raise ArgumentError, "e.type is below the wire minimum"
    end

    if e.type > 4 do
      raise ArgumentError, "e.type is above the wire maximum"
    end

    v = e.type
    scratch = scratch ||| v <<< scratch_bits
    scratch_bits = scratch_bits + 3
    flush = scratch_bits >>> 3
    data = <<data::binary, scratch::little-size(flush)-unit(8)>>
    scratch = scratch >>> (flush <<< 3)
    scratch_bits = scratch_bits &&& 7

    {data, scratch, scratch_bits} =
      case e.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}

        2 ->
          v = e.pulse.strength &&& 0xFFFF
          scratch = scratch ||| v <<< scratch_bits
          scratch_bits = scratch_bits + 16
          flush = scratch_bits >>> 3
          data = <<data::binary, scratch::little-size(flush)-unit(8)>>
          scratch = scratch >>> (flush <<< 3)
          scratch_bits = scratch_bits &&& 7
          {data, scratch, scratch_bits}

        3 ->
          if e.level.level < 0 do
            raise ArgumentError, "e.level.level is below the wire minimum"
          end

          if e.level.level > 7 do
            raise ArgumentError, "e.level.level is above the wire maximum"
          end

          v = e.level.level
          scratch = scratch ||| v <<< scratch_bits
          scratch_bits = scratch_bits + 3
          flush = scratch_bits >>> 3
          data = <<data::binary, scratch::little-size(flush)-unit(8)>>
          scratch = scratch >>> (flush <<< 3)
          scratch_bits = scratch_bits &&& 7
          {data, scratch, scratch_bits}

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          {data, scratch, scratch_bits}
      end

    w_void_signal_packet_history(rest, data, scratch, scratch_bits)
  end

  defp r_void_signal_packet_history(0, acc, _data, _num_bits, bits_read),
    do: {bits_read, Enum.reverse(acc)}

  defp r_void_signal_packet_history(remaining, acc, data, num_bits, bits_read) do
    if bits_read + 3 > num_bits, do: throw(:invalid)
    rv = rdw(data, bits_read, 49)
    v = rv &&& 0x7
    bits_read = bits_read + 3
    # not a wire-legal tag (SPEC §4.8)
    if v > 4, do: throw(:invalid)

    {bits_read, e} =
      case v do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          u = %Example.VoidSignal{type: 1}
          {bits_read, u}

        2 ->
          if bits_read + 16 > num_bits, do: throw(:invalid)
          rv = rd(data, bits_read, 16)
          v = rv
          bits_read = bits_read + 16
          e_pulse_strength = v
          e_pulse = %Example.VoidPulse{strength: e_pulse_strength}
          u = %Example.VoidSignal{type: 2, pulse: e_pulse}
          {bits_read, u}

        3 ->
          if bits_read + 3 > num_bits, do: throw(:invalid)
          rv = rd(data, bits_read, 3)
          v = rv
          bits_read = bits_read + 3
          e_level_level = v
          e_level = %Example.VoidLevel{level: e_level_level}
          u = %Example.VoidSignal{type: 3, level: e_level}
          {bits_read, u}

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          u = %Example.VoidSignal{type: 4}
          {bits_read, u}

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          {bits_read, %Example.VoidSignal{}}
      end

    r_void_signal_packet_history(remaining - 1, [e | acc], data, num_bits, bits_read)
  end

  defp m_void_signal_packet_history([], bits), do: bits

  defp m_void_signal_packet_history([e | rest], bits) do
    bits = bits + 3

    bits =
      case e.type do
        1 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        2 ->
          bits + 16

        3 ->
          bits + 3

        4 ->
          # a payload-free arm: the tag is the whole wire (SPEC §4.8)
          bits

        _ ->
          # None — the tag is the whole wire (SPEC §4.8)
          bits
      end

    m_void_signal_packet_history(rest, bits)
  end

  # The port's 40-bit window decode (issue #167): enough for a 7-bit offset
  # plus a 32-bit group, small enough that no intermediate ever boxes. The
  # callers have already priced the read inside num_bits, so the tail
  # fallback decodes exactly the bytes that exist.
  defp rd(data, bits_read, bits) do
    i = bits_read >>> 3

    window =
      case data do
        <<_::binary-size(^i), w::little-40, _::binary>> -> w
        <<_::binary-size(^i), rest::binary>> -> :binary.decode_unsigned(rest, :little)
      end

    window >>> (bits_read &&& 7) &&& (1 <<< bits) - 1
  end

  # The wide window decode: 56 bits, enough for a 7-bit offset plus a
  # 49-bit group, and still under the 2^59 fixnum boundary — one match
  # context serves a whole group of fields instead of one per field. A
  # 64-bit window would box and measures slower than the reads it saves.
  defp rdw(data, bits_read, bits) do
    i = bits_read >>> 3

    window =
      case data do
        <<_::binary-size(^i), w::little-56, _::binary>> -> w
        <<_::binary-size(^i), rest::binary>> -> :binary.decode_unsigned(rest, :little)
      end

    window >>> (bits_read &&& 7) &&& (1 <<< bits) - 1
  end
end
