// Generated by the schema compiler from Types.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0x40230069cc791fab
// The TABLE wire (evolution-tolerant, notes/table-wire.md): plain byte
// code, no serialize dependency — Unity/IL2CPP-safe.

namespace Example
{

    // The table codecs live on the same partial Schema class as the rest of
    // the generated surface (SPEC §6.1 naming) — one slice per generated file.
    public static partial class Schema
    {
        // TableWriteVec3 returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteVec3(Vec3 value)
        {
            TableWriter w = new TableWriter();
            TableWriteVec3(w, value);
            return w.ToArray();
        }

        // AppendTableVec3 appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteVec3.
        public static void AppendTableVec3(TableWriter w, Vec3 value)
        {
            TableWriteVec3(w, value);
        }

        private static void TableWriteVec3(TableWriter w, Vec3 value)
        {
            if (value.X != 0.0)
            {
                w.U16(0xad8b); // x
                w.U8(11);
                w.F64(value.X);
            }
            if (value.Y != 0.0)
            {
                w.U16(0xb2f8); // y
                w.U8(11);
                w.F64(value.Y);
            }
            if (value.Z != 0.0)
            {
                w.U16(0xaca1); // z
                w.U8(11);
                w.F64(value.Z);
            }
            w.U16(0); // terminator
        }

        // TableReadVec3 decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadVec3(byte[] data, ref Vec3 value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadVec3(r, ref value);
        }

        private static bool TableReadVec3(TableReader r, ref Vec3 value)
        {
            value = new Vec3(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xad8b: // x
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.X = r.GetF64();
                        break;
                    }
                    case 0xb2f8: // y
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.Y = r.GetF64();
                        break;
                    }
                    case 0xaca1: // z
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.Z = r.GetF64();
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // TableWriteQuat returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteQuat(Quat value)
        {
            TableWriter w = new TableWriter();
            TableWriteQuat(w, value);
            return w.ToArray();
        }

        // AppendTableQuat appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteQuat.
        public static void AppendTableQuat(TableWriter w, Quat value)
        {
            TableWriteQuat(w, value);
        }

        private static void TableWriteQuat(TableWriter w, Quat value)
        {
            if (value.X != 0.0)
            {
                w.U16(0xad8b); // x
                w.U8(11);
                w.F64(value.X);
            }
            if (value.Y != 0.0)
            {
                w.U16(0xb2f8); // y
                w.U8(11);
                w.F64(value.Y);
            }
            if (value.Z != 0.0)
            {
                w.U16(0xaca1); // z
                w.U8(11);
                w.F64(value.Z);
            }
            if (value.W != 0.0)
            {
                w.U16(0xcd3a); // w
                w.U8(11);
                w.F64(value.W);
            }
            w.U16(0); // terminator
        }

        // TableReadQuat decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadQuat(byte[] data, ref Quat value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadQuat(r, ref value);
        }

        private static bool TableReadQuat(TableReader r, ref Quat value)
        {
            value = new Quat(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xad8b: // x
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.X = r.GetF64();
                        break;
                    }
                    case 0xb2f8: // y
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.Y = r.GetF64();
                        break;
                    }
                    case 0xaca1: // z
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.Z = r.GetF64();
                        break;
                    }
                    case 0xcd3a: // w
                    {
                        if (kind != 11)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.W = r.GetF64();
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // TableWriteRigidBody returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteRigidBody(RigidBody value)
        {
            TableWriter w = new TableWriter();
            TableWriteRigidBody(w, value);
            return w.ToArray();
        }

        // AppendTableRigidBody appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteRigidBody.
        public static void AppendTableRigidBody(TableWriter w, RigidBody value)
        {
            TableWriteRigidBody(w, value);
        }

        private static void TableWriteRigidBody(TableWriter w, RigidBody value)
        {
            {
                int fieldAt = w.Len;
                w.U16(0xdd45); // position
                w.U8(13);
                int lenAt = w.Len;
                w.U32(0);
                TableWriteVec3(w, value.Position);
                if (w.Len - lenAt - 4 <= 2)
                {
                    w.Len = fieldAt; // all-default nested elides
                }
                else
                {
                    w.Patch32(lenAt, (uint)(w.Len - lenAt - 4));
                }
            }
            {
                int fieldAt = w.Len;
                w.U16(0x7964); // orientation
                w.U8(13);
                int lenAt = w.Len;
                w.U32(0);
                TableWriteQuat(w, value.Orientation);
                if (w.Len - lenAt - 4 <= 2)
                {
                    w.Len = fieldAt; // all-default nested elides
                }
                else
                {
                    w.Patch32(lenAt, (uint)(w.Len - lenAt - 4));
                }
            }
            if (value.AtRest != false)
            {
                w.U16(0xf9eb); // at_rest
                w.U8(1);
                if (value.AtRest)
                {
                    w.U8(1);
                }
                else
                {
                    w.U8(0);
                }
            }
            if (!value.AtRest)
            {
                {
                    int fieldAt = w.Len;
                    w.U16(0x0637); // linear_velocity
                    w.U8(13);
                    int lenAt = w.Len;
                    w.U32(0);
                    TableWriteVec3(w, value.LinearVelocity);
                    if (w.Len - lenAt - 4 <= 2)
                    {
                        w.Len = fieldAt; // all-default nested elides
                    }
                    else
                    {
                        w.Patch32(lenAt, (uint)(w.Len - lenAt - 4));
                    }
                }
            }
            if (!value.AtRest)
            {
                {
                    int fieldAt = w.Len;
                    w.U16(0x01a7); // angular_velocity
                    w.U8(13);
                    int lenAt = w.Len;
                    w.U32(0);
                    TableWriteVec3(w, value.AngularVelocity);
                    if (w.Len - lenAt - 4 <= 2)
                    {
                        w.Len = fieldAt; // all-default nested elides
                    }
                    else
                    {
                        w.Patch32(lenAt, (uint)(w.Len - lenAt - 4));
                    }
                }
            }
            w.U16(0); // terminator
        }

        // TableReadRigidBody decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadRigidBody(byte[] data, ref RigidBody value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadRigidBody(r, ref value);
        }

        private static bool TableReadRigidBody(TableReader r, ref RigidBody value)
        {
            value = new RigidBody(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xdd45: // position
                    {
                        if (kind != 13)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyLen = (int)r.Get32();
                        if (!r.Has(bodyLen))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        TableReader sub = new TableReader(r.Buf, r.Off, r.Off + bodyLen, r.Report);
                        TableReadVec3(sub, ref value.Position);
                        r.Off += bodyLen;
                        break;
                    }
                    case 0x7964: // orientation
                    {
                        if (kind != 13)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyLen = (int)r.Get32();
                        if (!r.Has(bodyLen))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        TableReader sub = new TableReader(r.Buf, r.Off, r.Off + bodyLen, r.Report);
                        TableReadQuat(sub, ref value.Orientation);
                        r.Off += bodyLen;
                        break;
                    }
                    case 0xf9eb: // at_rest
                    {
                        if (kind != 1)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(1))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        value.AtRest = r.Get8() != 0;
                        break;
                    }
                    case 0x0637: // linear_velocity
                    {
                        if (kind != 13)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyLen = (int)r.Get32();
                        if (!r.Has(bodyLen))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        TableReader sub = new TableReader(r.Buf, r.Off, r.Off + bodyLen, r.Report);
                        TableReadVec3(sub, ref value.LinearVelocity);
                        r.Off += bodyLen;
                        break;
                    }
                    case 0x01a7: // angular_velocity
                    {
                        if (kind != 13)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(4))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        int bodyLen = (int)r.Get32();
                        if (!r.Has(bodyLen))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        TableReader sub = new TableReader(r.Buf, r.Off, r.Off + bodyLen, r.Report);
                        TableReadVec3(sub, ref value.AngularVelocity);
                        r.Off += bodyLen;
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // TableWriteExtremeRow returns value's table-wire encoding as a fresh buffer.
        public static byte[] TableWriteExtremeRow(ExtremeRow value)
        {
            TableWriter w = new TableWriter();
            TableWriteExtremeRow(w, value);
            return w.ToArray();
        }

        // AppendTableExtremeRow appends value's table-wire encoding to w — the
        // zero-allocation write path: hold one TableWriter, Clear() it between
        // uses, and steady-state writes never allocate once the buffer has grown.
        // The wire is w.Buf[0 .. w.Len), byte-identical to TableWriteExtremeRow.
        public static void AppendTableExtremeRow(TableWriter w, ExtremeRow value)
        {
            TableWriteExtremeRow(w, value);
        }

        private static void TableWriteExtremeRow(TableWriter w, ExtremeRow value)
        {
            if (value.ClampedFloor != 0)
            {
                w.U16(0xba76); // clamped_floor
                w.U8(5);
                w.U64((ulong)value.ClampedFloor);
            }
            if (value.ClampedCeiling != 0)
            {
                w.U16(0x7a83); // clamped_ceiling
                w.U8(9);
                w.U64((ulong)value.ClampedCeiling);
            }
            if (value.FloorDef != -9223372036854775808)
            {
                w.U16(0x1590); // floor_def
                w.U8(5);
                w.U64((ulong)value.FloorDef);
            }
            if (value.CeilingDef != 18446744073709551615UL)
            {
                w.U16(0xb936); // ceiling_def
                w.U8(9);
                w.U64((ulong)value.CeilingDef);
            }
            w.U16(0); // terminator
        }

        // TableReadExtremeRow decodes a table-wire buffer under the permissive contract:
        // declared defaults prefill, known fields overlay, unknown ids and kind
        // mismatches skip and count, out-of-range values clamp and count. false
        // means malformed — the partial decode up to that point is kept.
        public static bool TableReadExtremeRow(byte[] data, ref ExtremeRow value, TableReport report)
        {
            TableReader r = new TableReader(data, 0, data.Length, report);
            return TableReadExtremeRow(r, ref value);
        }

        private static bool TableReadExtremeRow(TableReader r, ref ExtremeRow value)
        {
            value = new ExtremeRow(); // prefill declared defaults, then overlay
            while (true)
            {
                if (!r.Has(2))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                ushort fieldId = r.Get16();
                if (fieldId == 0)
                {
                    return true;
                }
                if (!r.Has(1))
                {
                    r.Report.Malformed = true;
                    return false;
                }
                byte kind = r.Get8();
                switch (fieldId)
                {
                    case 0xba76: // clamped_floor
                    {
                        if (kind != 5)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        long decoded = (long)r.Get64();
                        if (decoded < -9223372036854775808)
                        {
                            decoded = -9223372036854775808;
                            r.Report.Clamped++;
                        }
                        else if (decoded > 100L)
                        {
                            decoded = 100L;
                            r.Report.Clamped++;
                        }
                        value.ClampedFloor = (long)decoded;
                        break;
                    }
                    case 0x7a83: // clamped_ceiling
                    {
                        if (kind != 9)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        ulong decoded = r.Get64();
                        if (decoded < 1UL)
                        {
                            decoded = 1UL;
                            r.Report.Clamped++;
                        }
                        else if (decoded > 18446744073709551614UL)
                        {
                            decoded = 18446744073709551614UL;
                            r.Report.Clamped++;
                        }
                        value.ClampedCeiling = (ulong)decoded;
                        break;
                    }
                    case 0x1590: // floor_def
                    {
                        if (kind != 5)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        long decoded = (long)r.Get64();
                        value.FloorDef = (long)decoded;
                        break;
                    }
                    case 0xb936: // ceiling_def
                    {
                        if (kind != 9)
                        {
                            r.Report.KindMismatch++;
                            if (!r.Skip(kind))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            break;
                        }
                        if (!r.Has(8))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        ulong decoded = r.Get64();
                        value.CeilingDef = (ulong)decoded;
                        break;
                    }
                    default:
                    {
                        r.Report.Unknown++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                }
            }
        }

        // ---- reflection descriptors (tables only, notes/table-wire.md) ----
        //
        // One flat TableFieldInfo[] per closure type (SPEC §4.11: contiguous
        // structs, sequential walks, zero per-instance weight), built lazily on
        // first use: C# gives partial classes no cross-file static initializer
        // order, so an eager initializer could observe a not-yet-built nested
        // descriptor — the accessors compose in dependency order instead (the
        // closure is acyclic). A double-build under a race is benign: equivalent
        // data, last write wins.

        private static TableTypeInfo _tableTypeVec3;

        // TableTypeVec3 returns Vec3's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetVec3/TableSetVec3 to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeVec3()
        {
            if (_tableTypeVec3 != null)
            {
                return _tableTypeVec3;
            }
            TableFieldInfo[] fields = new TableFieldInfo[3];
            fields[0] = new TableFieldInfo { Name = "x", TypeName = "float64", Id = 0xad8b, Kind = 11, EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "y", TypeName = "float64", Id = 0xb2f8, Kind = 11, EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "z", TypeName = "float64", Id = 0xaca1, Kind = 11, EnumMax = -1, Guard = "" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "Vec3";
            info.Fields = fields;
            _tableTypeVec3 = info;
            return info;
        }

        // TableGetVec3 reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetVec3(Vec3 value, string field, out TableValue result)
        {
            switch (field)
            {
                case "x":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.X };
                    return true;
                }
                case "y":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.Y };
                    return true;
                }
                case "z":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.Z };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetVec3 writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetVec3(Vec3 value, string field, in TableValue v)
        {
            switch (field)
            {
                case "x":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.X = n;
                    return true;
                }
                case "y":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.Y = n;
                    return true;
                }
                case "z":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.Z = n;
                    return true;
                }
            }
            return false;
        }

        private static TableTypeInfo _tableTypeQuat;

        // TableTypeQuat returns Quat's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetQuat/TableSetQuat to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeQuat()
        {
            if (_tableTypeQuat != null)
            {
                return _tableTypeQuat;
            }
            TableFieldInfo[] fields = new TableFieldInfo[4];
            fields[0] = new TableFieldInfo { Name = "x", TypeName = "float64", Id = 0xad8b, Kind = 11, EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "y", TypeName = "float64", Id = 0xb2f8, Kind = 11, EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "z", TypeName = "float64", Id = 0xaca1, Kind = 11, EnumMax = -1, Guard = "" };
            fields[3] = new TableFieldInfo { Name = "w", TypeName = "float64", Id = 0xcd3a, Kind = 11, EnumMax = -1, Guard = "" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "Quat";
            info.Fields = fields;
            _tableTypeQuat = info;
            return info;
        }

        // TableGetQuat reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetQuat(Quat value, string field, out TableValue result)
        {
            switch (field)
            {
                case "x":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.X };
                    return true;
                }
                case "y":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.Y };
                    return true;
                }
                case "z":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.Z };
                    return true;
                }
                case "w":
                {
                    result = new TableValue { Kind = TableValueKind.Float, F = value.W };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetQuat writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetQuat(Quat value, string field, in TableValue v)
        {
            switch (field)
            {
                case "x":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.X = n;
                    return true;
                }
                case "y":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.Y = n;
                    return true;
                }
                case "z":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.Z = n;
                    return true;
                }
                case "w":
                {
                    double n;
                    if (!v.AsDouble(out n))
                    {
                        return false;
                    }
                    value.W = n;
                    return true;
                }
            }
            return false;
        }

        private static TableTypeInfo _tableTypeRigidBody;

        // TableTypeRigidBody returns RigidBody's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetRigidBody/TableSetRigidBody to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeRigidBody()
        {
            if (_tableTypeRigidBody != null)
            {
                return _tableTypeRigidBody;
            }
            TableFieldInfo[] fields = new TableFieldInfo[5];
            fields[0] = new TableFieldInfo { Name = "position", TypeName = "Vec3", Id = 0xdd45, Kind = 13, Table = TableTypeVec3(), EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "orientation", TypeName = "Quat", Id = 0x7964, Kind = 13, Table = TableTypeQuat(), EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "at_rest", TypeName = "bool", Id = 0xf9eb, Kind = 1, EnumMax = -1, Guard = "" };
            fields[3] = new TableFieldInfo { Name = "linear_velocity", TypeName = "Vec3", Id = 0x0637, Kind = 13, Table = TableTypeVec3(), EnumMax = -1, Guard = "!at_rest" };
            fields[4] = new TableFieldInfo { Name = "angular_velocity", TypeName = "Vec3", Id = 0x01a7, Kind = 13, Table = TableTypeVec3(), EnumMax = -1, Guard = "!at_rest" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "RigidBody";
            info.Fields = fields;
            _tableTypeRigidBody = info;
            return info;
        }

        // TableGetRigidBody reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetRigidBody(RigidBody value, string field, out TableValue result)
        {
            switch (field)
            {
                case "position":
                {
                    result = new TableValue { Kind = TableValueKind.Table, Obj = value.Position };
                    return true;
                }
                case "orientation":
                {
                    result = new TableValue { Kind = TableValueKind.Table, Obj = value.Orientation };
                    return true;
                }
                case "at_rest":
                {
                    result = new TableValue { Kind = TableValueKind.Bool, B = value.AtRest };
                    return true;
                }
                case "linear_velocity":
                {
                    result = new TableValue { Kind = TableValueKind.Table, Obj = value.LinearVelocity };
                    return true;
                }
                case "angular_velocity":
                {
                    result = new TableValue { Kind = TableValueKind.Table, Obj = value.AngularVelocity };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetRigidBody writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetRigidBody(RigidBody value, string field, in TableValue v)
        {
            switch (field)
            {
                case "at_rest":
                {
                    if (v.Kind != TableValueKind.Bool)
                    {
                        return false;
                    }
                    value.AtRest = v.B;
                    return true;
                }
            }
            return false;
        }

        private static TableTypeInfo _tableTypeExtremeRow;

        // TableTypeExtremeRow returns ExtremeRow's reflection descriptor — field names, wire
        // ids/kinds, bounds, ranges, enum names and branch guards. Pair it with
        // TableGetExtremeRow/TableSetExtremeRow to walk, print, diff or edit values generically
        // (walk the flat Fields array with `ref readonly var f = ref fields[i];`).
        public static TableTypeInfo TableTypeExtremeRow()
        {
            if (_tableTypeExtremeRow != null)
            {
                return _tableTypeExtremeRow;
            }
            TableFieldInfo[] fields = new TableFieldInfo[4];
            fields[0] = new TableFieldInfo { Name = "clamped_floor", TypeName = "int64", Id = 0xba76, Kind = 5, HasRange = true, RangeMin = -9.223372036854776e+18, RangeMax = 100.0, EnumMax = -1, Guard = "" };
            fields[1] = new TableFieldInfo { Name = "clamped_ceiling", TypeName = "uint64", Id = 0x7a83, Kind = 9, HasRange = true, RangeMin = 1.0, RangeMax = 1.8446744073709552e+19, EnumMax = -1, Guard = "" };
            fields[2] = new TableFieldInfo { Name = "floor_def", TypeName = "int64", Id = 0x1590, Kind = 5, EnumMax = -1, Guard = "" };
            fields[3] = new TableFieldInfo { Name = "ceiling_def", TypeName = "uint64", Id = 0xb936, Kind = 9, EnumMax = -1, Guard = "" };
            TableTypeInfo info = new TableTypeInfo();
            info.Name = "ExtremeRow";
            info.Fields = fields;
            _tableTypeExtremeRow = info;
            return info;
        }

        // TableGetExtremeRow reads the named field from value into a TableValue — no
        // boxing (SPEC §4.11): scalars normalize (signed -> I, unsigned/bits -> U,
        // floats -> F, bools -> B), enums and flags -> U, strings decode into S
        // (the one allocating path), nested tables put the member reference in
        // Obj, arrays put the member array in Obj and the used element count in
        // Count (fixed arrays: the full bound). Unknown field names return false.
        public static bool TableGetExtremeRow(ExtremeRow value, string field, out TableValue result)
        {
            switch (field)
            {
                case "clamped_floor":
                {
                    result = new TableValue { Kind = TableValueKind.Int, I = value.ClampedFloor };
                    return true;
                }
                case "clamped_ceiling":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.ClampedCeiling };
                    return true;
                }
                case "floor_def":
                {
                    result = new TableValue { Kind = TableValueKind.Int, I = value.FloorDef };
                    return true;
                }
                case "ceiling_def":
                {
                    result = new TableValue { Kind = TableValueKind.Uint, U = value.CeilingDef };
                    return true;
                }
            }
            result = new TableValue();
            return false;
        }

        // TableSetExtremeRow writes the named field — the editor write path: scalars,
        // enums, flags, bools and strings only. Numerics accept the Int, Uint and
        // Float kinds; out-of-range values CLAMP exactly as the table-wire read
        // side does (declared ranges, bits width, enum out-of-set -> None), and
        // strings truncate to the declared max. Unknown fields, nested tables and
        // arrays return false.
        public static bool TableSetExtremeRow(ExtremeRow value, string field, in TableValue v)
        {
            switch (field)
            {
                case "clamped_floor":
                {
                    long n;
                    if (!v.AsLong(out n))
                    {
                        return false;
                    }
                    if (n < -9223372036854775808)
                    {
                        n = -9223372036854775808;
                    }
                    else if (n > 100L)
                    {
                        n = 100L;
                    }
                    value.ClampedFloor = n;
                    return true;
                }
                case "clamped_ceiling":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    if (n < 1UL)
                    {
                        n = 1UL;
                    }
                    else if (n > 18446744073709551614UL)
                    {
                        n = 18446744073709551614UL;
                    }
                    value.ClampedCeiling = n;
                    return true;
                }
                case "floor_def":
                {
                    long n;
                    if (!v.AsLong(out n))
                    {
                        return false;
                    }
                    value.FloorDef = n;
                    return true;
                }
                case "ceiling_def":
                {
                    ulong n;
                    if (!v.AsUlong(out n))
                    {
                        return false;
                    }
                    value.CeilingDef = n;
                    return true;
                }
            }
            return false;
        }
    }

}
