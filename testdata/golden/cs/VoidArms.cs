// Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
// SPDX-License-Identifier: NONE — this generated output is yours, under terms of
// your choice. See the LICENSE exception in the schema compiler; the compiler is
// AGPL-3.0, its output is not.
// package example — protocol id 0xcaa884d7062cccc8

using System.Diagnostics;
using System.Runtime.CompilerServices;
using Serialize;

namespace Example
{

    // type VoidPulse
    public sealed class VoidPulse
    {
        public ushort Strength;
    }

    // type VoidLevel
    public sealed class VoidLevel
    {
        public byte Level; // wire [0, 7]
    }

    // VoidSignalType: union VoidSignal's tag — None = 0, then each variant in declared order (SPEC §4.8)
    public enum VoidSignalType : byte
    {
        None = 0,
        Ping = 1,
        Pulse = 2,
        Level = 3,
        Ack = 4,
        Count = 4, // the declared variant count (SPEC §4.2)
        Max = 4, // the exported extent (SPEC §4.2)
    }

    // VoidSignal — at most one of the arms; Type says which. Construction is the empty
    // union (None). Every read selection initializes the chosen payload from
    // construction defaults; unselected arms keep what they last held — the
    // MessageStorage reuse discipline. Consumers read the selected arm only.
    public sealed class VoidSignal
    {
        public VoidSignalType Type;
        public VoidPulse Pulse = new VoidPulse();
        public VoidLevel Level = new VoidLevel();
    }

    // type VoidSignalPacket
    public sealed class VoidSignalPacket
    {
        public uint Lead;
        public VoidSignal Signal = new VoidSignal();
        public VoidSignal Echo = new VoidSignal();
        public VoidSignal[] History = new VoidSignal[2]; // used count beside it; wire count in [0, 2]
        public int HistoryCount;
        public uint Tail;

        public VoidSignalPacket()
        {
            for (int i = 0; i < History.Length; i++)
            {
                History[i] = new VoidSignal();
            }
        }
    }

    // Schema carries every generated function and constant of the unit — C# has
    // no namespace-level functions or constants, so the static class is their
    // home (SPEC §6.1 naming); partial, one slice per generated file.
    public static partial class Schema
    {
        // VoidPulseMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // VoidPulseMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long VoidPulseMaxBits = 16;
        public const long VoidPulseMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroVoidPulse(VoidPulse value)
        {
            value.Strength = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitVoidPulse(VoidPulse value)
        {
            value.Strength = 0;
        }

        public static bool WriteVoidPulse(WriteStream stream, VoidPulse value)
        {
            {
                uint rawValue = value.Strength;
                if (!stream.SerializeBits(ref rawValue, 16))
                {
                    return false;
                }
            }
            return true;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteVoidPulseBatch(ref WriteBatch batch, VoidPulse value)
        {
            {
                uint rawValue = value.Strength;
                if (!batch.SerializeBits(ref rawValue, 16))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadVoidPulse(ReadStream stream, VoidPulse value)
        {
            {
                uint rawValue = 0;
                if (!stream.SerializeBits(ref rawValue, 16))
                {
                    return false;
                }
                value.Strength = (ushort)rawValue;
            }
            return true;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadVoidPulseBatch(ref ReadBatch batch, VoidPulse value)
        {
            {
                uint rawValue = 0;
                if (!batch.SerializeBits(ref rawValue, 16))
                {
                    return false;
                }
                value.Strength = (ushort)rawValue;
            }
            return true;
        }

        // VoidLevelMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // VoidLevelMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long VoidLevelMaxBits = 3;
        public const long VoidLevelMaxBytes = 8;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroVoidLevel(VoidLevel value)
        {
            value.Level = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitVoidLevel(VoidLevel value)
        {
            value.Level = 0;
        }

        public static bool WriteVoidLevel(WriteStream stream, VoidLevel value)
        {
            Debug.Assert(value.Level <= 7, "value.Level out of range [0, 7]");
            {
                uint offsetValue = (uint)(value.Level);
                if (!stream.SerializeBits(ref offsetValue, 3))
                {
                    return false;
                }
            }
            return true;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteVoidLevelBatch(ref WriteBatch batch, VoidLevel value)
        {
            Debug.Assert(value.Level <= 7, "value.Level out of range [0, 7]");
            {
                uint offsetValue = (uint)(value.Level);
                if (!batch.SerializeBits(ref offsetValue, 3))
                {
                    return false;
                }
            }
            return true;
        }

        public static bool ReadVoidLevel(ReadStream stream, VoidLevel value)
        {
            {
                int rangeValue = 0;
                if (!stream.SerializeInt(ref rangeValue, 0, 7))
                {
                    return false;
                }
                value.Level = (byte)rangeValue;
            }
            return true;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadVoidLevelBatch(ref ReadBatch batch, VoidLevel value)
        {
            {
                int rangeValue = 0;
                if (!batch.SerializeInt(ref rangeValue, 0, 7))
                {
                    return false;
                }
                value.Level = (byte)rangeValue;
            }
            return true;
        }

        // EnumNameVoidSignalType: debug/log/tooling name for any VoidSignalType wire value —
        // out-of-set values (wire-legal up to the declared max) name as "???"
        public static string EnumNameVoidSignalType(ulong value)
        {
            switch (value)
            {
                case (ulong)VoidSignalType.None:
                    return "None";
                case (ulong)VoidSignalType.Ping:
                    return "Ping";
                case (ulong)VoidSignalType.Pulse:
                    return "Pulse";
                case (ulong)VoidSignalType.Level:
                    return "Level";
                case (ulong)VoidSignalType.Ack:
                    return "Ack";
                default:
                    return "???";
            }
        }

        // VoidSignalMaxBits is the tag plus the largest arm; None costs the tag only (SPEC §4.8).
        // VoidSignalMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long VoidSignalMaxBits = 19;
        public const long VoidSignalMaxBytes = 8;

        // ZeroVoidSignal resets value to the §5 zero form — the empty union. The tag alone
        // resets: unselected arms are unspecified by rule (SPEC §4.8), and every arm
        // is unselected at None; an arm initializes at its next selection.
        public static void ZeroVoidSignal(VoidSignal value)
        {
            value.Type = VoidSignalType.None;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteVoidSignal(WriteStream stream, VoidSignal value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteVoidSignalBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteVoidSignalBatch(ref WriteBatch batch, VoidSignal value)
        {
            uint tagValue = (uint)value.Type;
            Debug.Assert(tagValue <= 4, "the union tag is outside the variant set [0, 4]"); // the tag contract holds BEFORE it rides (SPEC §4.8)
            if (!batch.SerializeBits(ref tagValue, 3))
            {
                return false;
            }
            switch (value.Type)
            {
                case VoidSignalType.Ping:
                    return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
                case VoidSignalType.Pulse:
                    return WriteVoidPulseBatch(ref batch, value.Pulse);
                case VoidSignalType.Level:
                    return WriteVoidLevelBatch(ref batch, value.Level);
                case VoidSignalType.Ack:
                    return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
            }
            return true; // None — the tag is the whole wire (SPEC §4.8)
        }

        public static bool ReadVoidSignal(ReadStream stream, VoidSignal value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadVoidSignalBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadVoidSignalBatch(ref ReadBatch batch, VoidSignal value)
        {
            int tagValue = 0;
            if (!batch.SerializeInt(ref tagValue, 0, 4)) // rejects a tag above the count (SPEC §4.8)
            {
                return false;
            }
            value.Type = (VoidSignalType)tagValue;
            switch (value.Type)
            {
                case VoidSignalType.Ping:
                    return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
                case VoidSignalType.Pulse:
                    InitVoidPulse(value.Pulse); // every selection starts from construction defaults
                    return ReadVoidPulseBatch(ref batch, value.Pulse);
                case VoidSignalType.Level:
                    InitVoidLevel(value.Level); // every selection starts from construction defaults
                    return ReadVoidLevelBatch(ref batch, value.Level);
                case VoidSignalType.Ack:
                    return true; // a payload-free arm: the tag is the whole wire (SPEC §4.8)
            }
            return true; // None
        }

        // VoidSignalPacketMaxBits is the longest wire path; align pads at worst case (SPEC §6.1).
        // VoidSignalPacketMaxBytes is rounded up to the 8-byte write-buffer granularity.
        public const long VoidSignalPacketMaxBits = 84;
        public const long VoidSignalPacketMaxBytes = 16;

        // The §5 zero form: all-zero storage; specified defaults live only in construction.
        public static void ZeroVoidSignalPacket(VoidSignalPacket value)
        {
            value.Lead = 0;
            ZeroVoidSignal(value.Signal);
            ZeroVoidSignal(value.Echo);
            for (int i = 0; i < 2; i++)
            {
                ZeroVoidSignal(value.History[i]);
            }
            value.HistoryCount = 0;
            value.Tail = 0;
        }

        // Restore construction defaults in place; buffers and objects are retained.
        public static void InitVoidSignalPacket(VoidSignalPacket value)
        {
            value.Lead = 0;
            ZeroVoidSignal(value.Signal);
            ZeroVoidSignal(value.Echo);
            for (int i = 0; i < 2; i++)
            {
                ZeroVoidSignal(value.History[i]);
            }
            value.HistoryCount = 0;
            value.Tail = 0;
        }

        // batch form: stream state stays in registers across the body and End
        // publishes it — same wire bytes, same validation, same error model.
        public static bool WriteVoidSignalPacket(WriteStream stream, VoidSignalPacket value)
        {
            WriteBatch batch = stream.BeginBatch();
            bool result = WriteVoidSignalPacketBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool WriteVoidSignalPacketBatch(ref WriteBatch batch, VoidSignalPacket value)
        {
            if (!batch.SerializeBits(ref value.Lead, 3))
            {
                return false;
            }
            if (!WriteVoidSignalBatch(ref batch, value.Signal))
            {
                return false;
            }
            if (!WriteVoidSignalBatch(ref batch, value.Echo))
            {
                return false;
            }
            Debug.Assert(value.HistoryCount >= 0 && value.HistoryCount <= 2, "value.HistoryCount out of range [0, 2]"); // the count guards the loop (§6.3); an out-of-contract count is caller error
            {
                uint offsetValue = (uint)(value.HistoryCount);
                if (!batch.SerializeBits(ref offsetValue, 2))
                {
                    return false;
                }
            }
            for (int i = 0; i < value.HistoryCount; i++)
            {
                if (!WriteVoidSignalBatch(ref batch, value.History[i]))
                {
                    return false;
                }
            }
            if (!batch.SerializeBits(ref value.Tail, 3))
            {
                return false;
            }
            return true;
        }

        public static bool ReadVoidSignalPacket(ReadStream stream, VoidSignalPacket value)
        {
            ReadBatch batch = stream.BeginBatch();
            bool result = ReadVoidSignalPacketBatch(ref batch, value);
            batch.End();
            return result;
        }

        // inline-only batch core — a real call would address-expose the batch
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        private static bool ReadVoidSignalPacketBatch(ref ReadBatch batch, VoidSignalPacket value)
        {
            if (!batch.SerializeBits(ref value.Lead, 3))
            {
                return false;
            }
            if (!ReadVoidSignalBatch(ref batch, value.Signal))
            {
                return false;
            }
            if (!ReadVoidSignalBatch(ref batch, value.Echo))
            {
                return false;
            }
            if (!batch.SerializeInt(ref value.HistoryCount, 0, 2)) // the count guards the loop (§6.3)
            {
                return false;
            }
            for (int i = 0; i < value.HistoryCount; i++)
            {
                if (!ReadVoidSignalBatch(ref batch, value.History[i]))
                {
                    return false;
                }
            }
            if (!batch.SerializeBits(ref value.Tail, 3))
            {
                return false;
            }
            return true;
        }
    }

}
