// Generated by the schema compiler from Messages.schema. DO NOT EDIT.
// package example — protocol id 0xaacab4aa04d2189b
// The TABLE wire (evolution-tolerant, notes/table-wire.md): plain byte
// code, no serialize dependency — Unity/IL2CPP-safe.

namespace Example;

// The table codecs live on the same partial Schema class as the rest of
// the generated surface (SPEC §6.1 naming) — one slice per generated file.
public static partial class Schema
{
    // TableWriteHeartbeat returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteHeartbeat(Heartbeat value)
    {
        TableWriter w = new TableWriter();
        TableWriteHeartbeat(w, value);
        return w.ToArray();
    }

    private static void TableWriteHeartbeat(TableWriter w, Heartbeat value)
    {
        w.U16(0); // terminator
    }

    // TableReadHeartbeat decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadHeartbeat(byte[] data, ref Heartbeat value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadHeartbeat(r, ref value);
    }

    private static bool TableReadHeartbeat(TableReader r, ref Heartbeat value)
    {
        value = new Heartbeat(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }

    // TableWriteTest returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteTest(Test value)
    {
        TableWriter w = new TableWriter();
        TableWriteTest(w, value);
        return w.ToArray();
    }

    private static void TableWriteTest(TableWriter w, Test value)
    {
        if (value.TestA != 0)
        {
            w.U16(0xcb58); // test_a
            w.U8(7);
            w.U16((ushort)value.TestA);
        }
        if (value.TestB != 0)
        {
            w.U16(0xc3e1); // test_b
            w.U8(3);
            w.U16((ushort)value.TestB);
        }
        if (value.TestC != 0)
        {
            w.U16(0xc472); // test_c
            w.U8(3);
            w.U16((ushort)value.TestC);
        }
        if (value.TestD != 0)
        {
            w.U16(0xdebb); // test_d
            w.U8(3);
            w.U16((ushort)value.TestD);
        }
        w.U16(0); // terminator
    }

    // TableReadTest decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadTest(byte[] data, ref Test value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadTest(r, ref value);
    }

    private static bool TableReadTest(TableReader r, ref Test value)
    {
        value = new Test(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                case 0xcb58: // test_a
                {
                    if (kind != 7)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    ushort decoded = r.Get16();
                    value.TestA = (ushort)decoded;
                    break;
                }
                case 0xc3e1: // test_b
                {
                    if (kind != 3)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    short decoded = (short)r.Get16();
                    if (decoded < 0)
                    {
                        decoded = 0;
                        r.Report.Clamped++;
                    }
                    else if (decoded > 1000)
                    {
                        decoded = 1000;
                        r.Report.Clamped++;
                    }
                    value.TestB = (short)decoded;
                    break;
                }
                case 0xc472: // test_c
                {
                    if (kind != 3)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    short decoded = (short)r.Get16();
                    if (decoded < 0)
                    {
                        decoded = 0;
                        r.Report.Clamped++;
                    }
                    else if (decoded > 1000)
                    {
                        decoded = 1000;
                        r.Report.Clamped++;
                    }
                    value.TestC = (short)decoded;
                    break;
                }
                case 0xdebb: // test_d
                {
                    if (kind != 3)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    short decoded = (short)r.Get16();
                    if (decoded < 0)
                    {
                        decoded = 0;
                        r.Report.Clamped++;
                    }
                    else if (decoded > 1000)
                    {
                        decoded = 1000;
                        r.Report.Clamped++;
                    }
                    value.TestD = (short)decoded;
                    break;
                }
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }

    // TableWriteBlock returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteBlock(Block value)
    {
        TableWriter w = new TableWriter();
        TableWriteBlock(w, value);
        return w.ToArray();
    }

    private static void TableWriteBlock(TableWriter w, Block value)
    {
        if (value.DataLength > 0)
        {
            w.U16(0x3ad7); // data
            w.U8(14);
            w.U32((uint)(3 + value.DataLength));
            w.U8(6);
            w.U16((ushort)value.DataLength);
            w.Raw(value.Data, value.DataLength);
        }
        w.U16(0); // terminator
    }

    // TableReadBlock decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadBlock(byte[] data, ref Block value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadBlock(r, ref value);
    }

    private static bool TableReadBlock(TableReader r, ref Block value)
    {
        value = new Block(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                case 0x3ad7: // data
                {
                    if (kind != 14)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(4))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    int bodyLen = (int)r.Get32();
                    if (!r.Has(bodyLen))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    int bodyEnd = r.Off + bodyLen;
                    if (bodyLen >= 3)
                    {
                        byte elemKind = r.Get8();
                        int count = r.Get16();
                        if (elemKind != 6)
                        {
                            r.Report.KindMismatch++;
                            r.Off = bodyEnd;
                            break;
                        }
                        int keep = count;
                        if (keep > 2000)
                        {
                            keep = 2000;
                            r.Report.Clamped++;
                        }
                        for (int i = 0; i < keep; i++)
                        {
                            if (!r.Has(1))
                            {
                                r.Report.Malformed = true;
                                return false;
                            }
                            byte decoded = r.Get8();
                            value.Data[i] = (byte)decoded;
                        }
                        value.DataLength = keep;
                    }
                    r.Off = bodyEnd; // excess elements and slack skip via the length
                    break;
                }
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }

    // TableWriteChat returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteChat(Chat value)
    {
        TableWriter w = new TableWriter();
        TableWriteChat(w, value);
        return w.ToArray();
    }

    private static void TableWriteChat(TableWriter w, Chat value)
    {
        if (value.TextLength > 0)
        {
            w.U16(0xf3d8); // text
            w.U8(12);
            w.U16((ushort)value.TextLength);
            w.Raw(value.Text, value.TextLength);
        }
        w.U16(0); // terminator
    }

    // TableReadChat decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadChat(byte[] data, ref Chat value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadChat(r, ref value);
    }

    private static bool TableReadChat(TableReader r, ref Chat value)
    {
        value = new Chat(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                case 0xf3d8: // text
                {
                    if (kind != 12)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    int length = r.Get16();
                    if (!r.Has(length))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    int keep = length;
                    if (keep > 256)
                    {
                        keep = 256;
                        r.Report.Clamped++;
                    }
                    r.CopyOut(value.Text, keep);
                    value.TextLength = keep;
                    r.Off += length;
                    break;
                }
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }

    // TableWriteSynchronize returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteSynchronize(Synchronize value)
    {
        TableWriter w = new TableWriter();
        TableWriteSynchronize(w, value);
        return w.ToArray();
    }

    private static void TableWriteSynchronize(TableWriter w, Synchronize value)
    {
        if (value.SyncFrame != 0)
        {
            w.U16(0x6831); // sync_frame
            w.U8(9);
            w.U64((ulong)value.SyncFrame);
        }
        if (value.SyncSequence != 0)
        {
            w.U16(0x03a0); // sync_sequence
            w.U8(7);
            w.U16((ushort)value.SyncSequence);
        }
        w.U16(0); // terminator
    }

    // TableReadSynchronize decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadSynchronize(byte[] data, ref Synchronize value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadSynchronize(r, ref value);
    }

    private static bool TableReadSynchronize(TableReader r, ref Synchronize value)
    {
        value = new Synchronize(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                case 0x6831: // sync_frame
                {
                    if (kind != 9)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(8))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    ulong decoded = r.Get64();
                    value.SyncFrame = (ulong)decoded;
                    break;
                }
                case 0x03a0: // sync_sequence
                {
                    if (kind != 7)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(2))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    ushort decoded = r.Get16();
                    value.SyncSequence = (ushort)decoded;
                    break;
                }
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }

    // TableWriteTimescale returns value's table-wire encoding as a fresh buffer.
    public static byte[] TableWriteTimescale(Timescale value)
    {
        TableWriter w = new TableWriter();
        TableWriteTimescale(w, value);
        return w.ToArray();
    }

    private static void TableWriteTimescale(TableWriter w, Timescale value)
    {
        if (value.Scale != 0.0)
        {
            w.U16(0x9ee6); // scale
            w.U8(11);
            w.F64(value.Scale);
        }
        if (value.FrameA != 0)
        {
            w.U16(0x49e4); // frame_a
            w.U8(8);
            w.U32((uint)value.FrameA);
        }
        if (value.FrameB != 0)
        {
            w.U16(0x5651); // frame_b
            w.U8(8);
            w.U32((uint)value.FrameB);
        }
        w.U16(0); // terminator
    }

    // TableReadTimescale decodes a table-wire buffer under the permissive contract:
    // declared defaults prefill, known fields overlay, unknown ids and kind
    // mismatches skip and count, out-of-range values clamp and count. false
    // means malformed — the partial decode up to that point is kept.
    public static bool TableReadTimescale(byte[] data, ref Timescale value, TableReport report)
    {
        TableReader r = new TableReader(data, 0, data.Length, report);
        return TableReadTimescale(r, ref value);
    }

    private static bool TableReadTimescale(TableReader r, ref Timescale value)
    {
        value = new Timescale(); // prefill declared defaults, then overlay
        while (true)
        {
            if (!r.Has(2))
            {
                r.Report.Malformed = true;
                return false;
            }
            ushort fieldId = r.Get16();
            if (fieldId == 0)
            {
                return true;
            }
            if (!r.Has(1))
            {
                r.Report.Malformed = true;
                return false;
            }
            byte kind = r.Get8();
            switch (fieldId)
            {
                case 0x9ee6: // scale
                {
                    if (kind != 11)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(8))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    value.Scale = r.GetF64();
                    break;
                }
                case 0x49e4: // frame_a
                {
                    if (kind != 8)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(4))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    uint decoded = r.Get32();
                    value.FrameA = (uint)decoded;
                    break;
                }
                case 0x5651: // frame_b
                {
                    if (kind != 8)
                    {
                        r.Report.KindMismatch++;
                        if (!r.Skip(kind))
                        {
                            r.Report.Malformed = true;
                            return false;
                        }
                        break;
                    }
                    if (!r.Has(4))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    uint decoded = r.Get32();
                    value.FrameB = (uint)decoded;
                    break;
                }
                default:
                {
                    r.Report.Unknown++;
                    if (!r.Skip(kind))
                    {
                        r.Report.Malformed = true;
                        return false;
                    }
                    break;
                }
            }
        }
    }
}
