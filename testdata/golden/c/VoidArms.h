/* Code generated by the schema compiler from VoidArms.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0xcaa884d7062cccc8 */

#ifndef SCHEMA_EXAMPLE_VOID_ARMS_H
#define SCHEMA_EXAMPLE_VOID_ARMS_H

#include <stdint.h>

#ifndef SCHEMA_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define SCHEMA_UNUSED __attribute__((unused))
#else
#define SCHEMA_UNUSED
#endif
#endif

#ifdef __cplusplus
extern "C" {
#endif


/* type VoidPulse */
typedef struct VoidPulse {
    uint16_t strength;
} VoidPulse;

#define VOID_PULSE_MAX_BITS 16   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define VOID_PULSE_MAX_BYTES 8  /* rounded up to the 8-byte write-buffer granularity; a READ buffer's allocation must extend at least 8 bytes past the data — serialize.c loads 64-bit windows */


/* type VoidLevel */
typedef struct VoidLevel {
    uint8_t level;
} VoidLevel;

#define VOID_LEVEL_MAX_BITS 3   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define VOID_LEVEL_MAX_BYTES 8  /* 8-byte write granularity; read slack per the contract above */


/* union VoidSignal — first-class one-of (SPEC §4.8): the tag says which arm is
   live; None = 0 is the empty union, and the tag range is [0, 4]. */
typedef uint8_t VoidSignalType;
#define VOID_SIGNAL_TYPE_NONE 0
#define VOID_SIGNAL_TYPE_PING 1
#define VOID_SIGNAL_TYPE_PULSE 2
#define VOID_SIGNAL_TYPE_LEVEL 3
#define VOID_SIGNAL_TYPE_ACK 4
#define VOID_SIGNAL_TYPE_COUNT 4
#define VOID_SIGNAL_TYPE_MAX 4

/* Debug/log name for any VoidSignalType value, out-of-set included. */
static SCHEMA_UNUSED const char * enum_name_void_signal_type( VoidSignalType value )
{
    switch ( value )
    {
        case VOID_SIGNAL_TYPE_NONE: return "None";
        case 1: return "Ping";
        case 2: return "Pulse";
        case 3: return "Level";
        case 4: return "Ack";
        default: return "???";
    }
}

typedef struct VoidSignal {
    VoidSignalType type;
    union {
        VoidPulse pulse;
        VoidLevel level;
    } as;
} VoidSignal;

#define VOID_SIGNAL_MAX_BITS 19   /* tag + the largest arm; None costs the tag only (SPEC §4.8) */
#define VOID_SIGNAL_MAX_BYTES 8  /* 8-byte write granularity; read slack per the contract above */


/* type VoidSignalPacket */
typedef struct VoidSignalPacket {
    uint32_t lead;
    VoidSignal signal;
    VoidSignal echo;
    VoidSignal history[2];
    int32_t history_count;
    uint32_t tail;
} VoidSignalPacket;

#define VOID_SIGNAL_PACKET_MAX_BITS 84   /* longest wire path; align pads at worst case (SPEC §6.1) */
#define VOID_SIGNAL_PACKET_MAX_BYTES 16  /* 8-byte write granularity; read slack per the contract above */

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_VOID_ARMS_H */
