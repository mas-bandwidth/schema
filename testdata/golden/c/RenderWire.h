/* Generated by the schema compiler from Render.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0xf14b209d992acb57 */

#ifndef SCHEMA_EXAMPLE_RENDERWIRE_H
#define SCHEMA_EXAMPLE_RENDERWIRE_H

#include "Render.h"
#include <string.h>   /* memset, strlen */
#include "serialize.h"

#ifndef SCHEMA_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define SCHEMA_UNUSED __attribute__((unused))
#else
#define SCHEMA_UNUSED
#endif
#endif
#include "EnumsWire.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Writes RenderSprite. Returns 1 on success, 0 on failure — the stream latches the
   error, so a caller may check once at the end of a message. */
static SCHEMA_UNUSED int write_render_sprite( serialize_write_stream_t * stream, const RenderSprite * value )
{
    if ( !serialize_write_uint64( stream, (serialize_uint64_t) value->sort_key ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->mesh_id, 32 ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->material_id, 32 ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->layer, 8 ) )
    {
        return 0;
    }
    if ( value->team > 2 )
    {
        return 0; /* headroom above the wire range cannot ride */
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->team, 2 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads RenderSprite. Returns 1 on success, 0 on failure. Out-of-range values are
   REFUSED, never clamped. */
static SCHEMA_UNUSED int read_render_sprite( serialize_read_stream_t * stream, RenderSprite * value )
{
    {
        serialize_uint64_t raw = 0;
        if ( !serialize_read_uint64( stream, &raw ) )
        {
            return 0;
        }
        value->sort_key = (uint64_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 32 ) )
        {
            return 0;
        }
        value->mesh_id = (uint32_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 32 ) )
        {
            return 0;
        }
        value->material_id = (uint32_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 8 ) )
        {
            return 0;
        }
        value->layer = (uint8_t) raw;
    }
    {
        serialize_uint32_t enum_value = 0;
        if ( !serialize_read_bits( stream, &enum_value, 2 ) )
        {
            return 0;
        }
        if ( enum_value > 2 )
        {
            return 0; /* not a wire-legal value */
        }
        value->team = (Team) enum_value;
    }
    return 1;
}

/* Writes RenderBlock. Returns 1 on success, 0 on failure — the stream latches the
   error, so a caller may check once at the end of a message. */
static SCHEMA_UNUSED int write_render_block( serialize_write_stream_t * stream, const RenderBlock * value )
{
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->worker_index, 32 ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->sprite_count_hint, 32 ) )
    {
        return 0;
    }
    if ( !serialize_write_int( stream, value->sprites_count, 0, 64 ) )
    {
        return 0;
    }
    {
        int32_t i;
        for ( i = 0; i < value->sprites_count; i++ )
        {
            if ( !write_render_sprite( stream, &value->sprites[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads RenderBlock. Returns 1 on success, 0 on failure. Out-of-range values are
   REFUSED, never clamped. */
static SCHEMA_UNUSED int read_render_block( serialize_read_stream_t * stream, RenderBlock * value )
{
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 32 ) )
        {
            return 0;
        }
        value->worker_index = (uint32_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 32 ) )
        {
            return 0;
        }
        value->sprite_count_hint = (uint32_t) raw;
    }
    if ( !serialize_read_int( stream, &value->sprites_count, 0, 64 ) )
    {
        return 0;
    }
    {
        int32_t i;
        for ( i = 0; i < value->sprites_count; i++ )
        {
            if ( !read_render_sprite( stream, &value->sprites[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_RENDERWIRE_H */
