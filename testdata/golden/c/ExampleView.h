/* Code generated by the schema compiler. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours. */
#ifndef SCHEMA_EXAMPLE_VIEW_H
#define SCHEMA_EXAMPLE_VIEW_H
#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include "ArmDefaults.h"
#include "Clauses.h"
#include "Constants.h"
#include "Degenerate.h"
#include "Enums.h"
#include "Joins.h"
#include "Render.h"
#include "Types.h"
#include "Wire.h"
#ifndef SCHEMA_EXAMPLE_TABLE_PRIMITIVES
#define SCHEMA_EXAMPLE_TABLE_PRIMITIVES

/* THE CODEC DOES NOT DEPEND ON THE COMPILER'S INLINING BUDGET. A table of a
   realistic field count emits one large body per type, and the cursor a body
   writes through lives in the caller's TableWriter: across a call boundary that
   cursor round-trips through memory, and a uint8_t * store may alias the writer
   itself, so every put reloads it. When a budget runs out mid-body the codec
   silently degrades to that shape. Forcing the primitives and the fixed-class
   bodies inline is what keeps the cursor in registers and lets adjacent
   constant framing bytes merge into one store.

   The spelling is FEATURE TESTED rather than assumed, exactly as the packet
   emitter's own inlining demand is (SPEC §6.1's C column).

   IT CARRIES THE PACKAGE for the reason every other name here does, and that
   reason is the LINKER rather than the preprocessor: two units of the same
   schema family link into one program — two generations of one schema is the
   case the conformance driver itself is — so nothing this backend emits may
   be spelled the same way by two packages. They do NOT share a translation
   unit and cannot: C has no namespace, so two units' headers in one TU
   redefine TableReport, TableWriter and every other runtime name, which is the
   packet emitter's standing limit (SPEC §6.1) and not something tables
   changed. A per-package macro is what keeps the LINK legal. */
#ifndef SCHEMA_EXAMPLE_TABLE_INLINE
#if defined( _MSC_VER )
#define SCHEMA_EXAMPLE_TABLE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_EXAMPLE_TABLE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_EXAMPLE_TABLE_INLINE inline
#endif
#endif

/* A COMPILE-TIME assertion under -std=c99, which has none of its own. C11's
   _Static_assert carries the message into the build log verbatim and is used
   wherever the compiler has it; everywhere else the negative array bound is
   the same refusal with a tag a reader can find. The tag is unique per record
   and field, and each record is asserted by the ONE header that declares it,
   so no two of these ever meet. */
#ifndef SCHEMA_TABLE_STATIC_ASSERT
#if defined( __STDC_VERSION__ ) && __STDC_VERSION__ >= 201112L
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) _Static_assert( cond, message )
#elif defined( __cplusplus ) && __cplusplus >= 201103L
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) static_assert( cond, message )
#else
#define SCHEMA_TABLE_STATIC_ASSERT( tag, cond, message ) typedef char schema_table_assert_##tag[ ( cond ) ? 1 : -1 ]
#endif
#endif

/* A record's ALIGNMENT, which the cook's layout contract asserts and which the
   cook's Open compares the header's alignment word against (§7.1, §20.3).
   C99 has no alignof either; C11's _Alignof, the two GNU spellings and MSVC's
   cover every compiler this repo builds under, and the last form is the
   portable definition of the same number. */
#ifndef SCHEMA_TABLE_ALIGNOF
#if defined( __STDC_VERSION__ ) && __STDC_VERSION__ >= 201112L
#define SCHEMA_TABLE_ALIGNOF( T ) _Alignof( T )
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_TABLE_ALIGNOF( T ) __alignof__( T )
#elif defined( _MSC_VER )
#define SCHEMA_TABLE_ALIGNOF( T ) __alignof( T )
#else
#define SCHEMA_TABLE_ALIGNOF( T ) ( (size_t) offsetof( struct { char schema_pad_; T schema_member_; }, schema_member_ ) )
#endif
#endif

/* The table-wire read report — the permissive contract's ledger. Silence
   (all zero) means the data matched this reader's schema exactly. */
typedef struct TableReport
{
    int32_t unknown;       /* unknown field ids skipped (newer data) */
    int32_t kind_mismatch; /* known id, changed type — skipped, never misdecoded */
    int32_t clamped;       /* out-of-range values clamped to declared bounds */
    /* a key the TEXT form saw twice: last wins, and the repeat is counted
       (docs/SPEC-TABLES.md §16.2). The wire never raises it — a body carrying an
       id twice is legal input whose last occurrence wins, silently (§3). */
    int32_t duplicate;
    int malformed;         /* framing damage; decode stopped, partial result kept */
    int32_t widened;        /* exact widening of a known kind */
    int32_t retained, retain_lost; /* opt-in unknown-field round trips */
    int refused;           /* unsupported form byte, not framing damage */
    int reason;            /* SCHEMA_TABLE_REFUSAL_REASON */
} TableReport;

enum SCHEMA_TABLE_REFUSAL_REASON { SCHEMA_TABLE_NO_REFUSAL, SCHEMA_TABLE_NEWER_FORM, SCHEMA_TABLE_MESSAGE_FORM_AS_FILE };

/* ---- reflection (tables only, docs/SPEC-TABLES.md) ----

   Static field descriptors for every type in the table closure: name, wire
   id/kind, storage offset, bounds, ranges, enum names and branch guards —
   enough to walk, print, diff, edit or bind any table value at runtime with
   no schema files. <name>_table_type() returns <Name>'s descriptor. */

struct TableTypeInfo;
struct TableFieldInfo;

/* One arm of a union field: where its payload sits inside the union's storage
   and what its payload looks like. The arm's NAME and its table-wire id ride
   in the field's own vocabulary table at the same tag, so nothing is spelled
   twice (docs/SPEC-TABLES.md §8). */
typedef struct TableUnionArmInfo
{
    uint32_t offset;                    /* offsetof the arm's payload within the union storage */
    const struct TableTypeInfo * table; /* the arm payload's descriptor */
    const struct TableFieldInfo * field; /* non-body arm, relative to union storage */
    uint32_t size; /* bytes established when this arm is selected */
} TableUnionArmInfo;

/* A union field's shape: the tag, and the arms indexed by it. Arms run
   [0, enum_max]; index 0 is the EMPTY arm and carries no payload. */
typedef struct TableUnionInfo
{
    uint32_t tag_offset; /* offsetof the tag within the union storage */
    uint32_t tag_size;   /* sizeof the tag */
    const TableUnionArmInfo * arms;
} TableUnionInfo;

/* ONE ENTRY OF A VOCABULARY, and the whole of what C++ spells as a pair of
   captureless lambdas. An enum's values, a union's arms and a flags field's
   BITS are each a named set indexed by [0, enum_max], so the set rides as an
   ARRAY indexed the same way: no function per enum, and therefore no name per
   enum for a schema to collide with (docs/SPEC-TABLES.md §11).

   name is the variant's spelling, or NULL for a value the declared set does
   not name. id is the TABLE-WIRE id it rides under (§5) — the hash of the
   variant's name, 0 for the reserved None and for the empty union arm. A
   FLAGS field's variants are BIT POSITIONS and have no per-variant wire id
   (§4), so has_ids is 0 there and 1 for an enum and a union: a vocabulary
   with names and no ids is what says "flags". */
typedef struct TableVariantInfo
{
    const char * name;
    uint64_t id;
} TableVariantInfo;

/* THE SHARED EMPTY DOC (docs/SPEC-TABLES.md §8.1): a declaration with no ///
   block carries a doc column pointing at this one object, so absence costs a
   unit no string data and a printer concatenates doc columns with no null
   test. One definition per translation unit, and the descriptors of a unit
   are one, so every absent doc compares equal by address. */
static SCHEMA_UNUSED const char TableDocNone[1] = "";

typedef struct TableWideRange { uint64_t lo[2], hi[2]; } TableWideRange;

typedef struct TableFieldInfo
{
    const char * name;      /* schema field name, e.g. "health" */
    const char * json;      /* the TEXT form's key: the json = "key" attribute, else name (§16.3) */
    const char * type_name; /* schema type name, e.g. "float32", "Grade" */
    uint64_t id;            /* table-wire field id (name hash; the was alias's hash after a rename) */
    uint8_t kind;           /* table-wire kind; for arrays/strings/bytes, the ELEMENT kind */
    int is_array;           /* fixed or counted array (bytes included) */
    int counted;            /* a _count/_length int32 companion exists (counted arrays, strings, bytes) */
    int optional;           /* a ?T field: a _present companion decides whether it rides */
    int32_t array_bound;    /* array capacity / string max length; 0 for plain scalars */
    uint32_t offset;        /* offsetof the storage member */
    uint32_t elem_size;     /* sizeof the member (element size for arrays) */
    uint32_t count_offset;  /* offsetof the _count/_length companion, or 0xffffffff */
    uint32_t present_offset; /* offsetof the _present companion, or 0xffffffff */
    const struct TableTypeInfo * table; /* nested table's descriptor, or NULL */
    int has_range;          /* a declared [min, max] (int or float) */
    double range_min;       /* NOTE: int64 ranges beyond 2^53 lose precision here */
    double range_max;
    const TableWideRange * wide; /* exact raw range for wide/fixed storage */
    int32_t frac_bits;
    int64_t enum_max;       /* enums: highest valid value (None = 0 always valid);
                               unions: the arm count (tag range [0, enum_max]);
                               flags: the highest declared BIT INDEX; else -1 */
    /* the vocabulary, indexed the same way enum_max bounds: an enum's value ->
       (name, id), a union's tag -> (arm name, arm id), a FLAGS field's bit
       index -> (variant name, 0). NULL for every other kind. */
    const TableVariantInfo * variants;
    int has_variant_ids;    /* 1 for an enum or a union, 0 for flags (§4) */
    /* an ENUM-KEYED array (docs/SPEC-TABLES.md §2.4): the array has one slot per
       variant of key_type_name, the key k at index k-1, and its slots ride
       under variant ids rather than positions. keys is the key's vocabulary,
       indexed by the KEY — walk [0, array_bound) and ask about index + 1 to
       print slots by name. NULL on every other field. */
    const char * key_type_name;
    const TableVariantInfo * keys;
    int64_t key_max;        /* the key enum's highest value, bounding keys */
    /* union fields: the tag and its arms. NULL for every other kind. */
    const TableUnionInfo * arms;
    const char * guard;     /* branch guard, e.g. "at_rest" or "!at_rest"; "" if unguarded */
    /* what a PERSON wrote about the field (docs/SPEC-TABLES.md §8.1): the ///
       block above it, verbatim (SPEC §4.1). It is TableDocNone when there is
       none, never NULL. Its tags (SPEC §4.2) follow in declared order, and an
       untagged field is 0 beside NULL. Static, constant-initialized,
       allocating nothing. */
    const char * doc;
    int32_t num_tags;
    const char * const * tags;
    int sequence; /* 0 inline field, 1 unbounded list, 2 map */
    void * (*place)(void * worker,void * slot,const char * key,int32_t length,int64_t number);
} TableFieldInfo;

typedef struct TableTypeInfo
{
    const char * name;   /* schema type name */
    uint32_t size;       /* sizeof the storage struct */
    int32_t num_fields;
    const TableFieldInfo * fields;
    /* put one instance back at its declared defaults, in place. A generic
       walker that fills a value has to be able to establish the defaults an
       absent field takes, and it holds no type to spell — this is the one
       thing the descriptors could not express without it. It is the void *
       form of <name>_reset and the same code. */
    void (*reset)( void * storage );
    /* the declaration's own doc and tags, on the same terms as a field's
       (docs/SPEC-TABLES.md §8.1) */
    const char * doc;
    int32_t num_tags;
    const char * const * tags;
} TableTypeInfo;



static SCHEMA_UNUSED float table_bits_to_float( uint32_t bits ) { float f; memcpy( &f, &bits, 4 ); return f; }
static SCHEMA_UNUSED uint32_t table_float_to_bits( float f ) { uint32_t b; memcpy( &b, &f, 4 ); return b; }
static SCHEMA_UNUSED double table_bits_to_double( uint64_t bits ) { double d; memcpy( &d, &bits, 8 ); return d; }
static SCHEMA_UNUSED uint64_t table_double_to_bits( double d ) { uint64_t b; memcpy( &b, &d, 8 ); return b; }

#endif /* SCHEMA_EXAMPLE_TABLE_PRIMITIVES */

#ifdef __cplusplus
extern "C" {
#endif
typedef struct ViewConstant
{
    const char * name;
    const char * file;       // the declaring schema file's basename
    const char * type_name;  // declared storage: "int64", "float64", "int32", ...
    int is_float;
    int64_t int_value;       // the folded value; float_value when is_float
    double float_value;
    const char * doc;        // the `///` block above it, verbatim (SPEC §4.1);
                             // "" when there is none, never NULL (§8.1)
    int32_t num_tags;             // the declaration's tags (SPEC §4.2), in
    const char * const * tags;    // DECLARED order; 0 and NULL when there are none
} ViewConstant;

typedef struct ViewVariant          // one enum variant, one flags bit, one union arm
{
    uint64_t value;         // an enum's value, a flags BIT INDEX, a union's tag
    const char * name;
    uint64_t id;            // the table-wire id (§5). It is 0 for a flags bit,
                            // and throughout a vocabulary no table closure
                            // reaches (§8.2)
    const char * payload_name;      // a union arm's TYPE as declared — a record's
                                    // name, or a general arm's spelling
                                    // ("int32", "string(64)", "*Chunk"); NULL on
                                    // a payload-free arm and on every other row
    const TableTypeInfo * payload;  // that payload's descriptor — never NULL for
                                    // an arm naming a `type` or a `table` (§8.2);
                                    // NULL on a general arm, on a payload-free
                                    // arm, on tag 0, and on an enum or flags row
    const TableFieldInfo * field;   // a general arm's FIELD descriptor: its kind,
                                    // width, bounds, capacity, companions and the
                                    // offsets §8.1 takes within the union's
                                    // storage. NULL on an arm naming a `type` or
                                    // a `table`, on a payload-free arm, on tag 0,
                                    // and on an enum or flags row
    const char * doc;               // the `///` block above this variant, bit or
                                    // arm, verbatim; "" when there is none (§8.1)
    int32_t num_tags;               // this row's own tags, in DECLARED order;
    const char * const * tags;      // 0 and NULL when there are none. On a general
                                    // arm the `field` descriptor above carries the
                                    // same two values (§8.1)
} ViewVariant;

typedef struct ViewVocabulary       // an enum, a flags or a union declaration
{
    const char * name;
    const char * file;
    int64_t max;            // highest value / highest bit index / highest tag
    int32_t storage_bits;
    int32_t num_variants;
    const ViewVariant * variants;
    const char * doc;             // the declaration's own `///` block; "" when none
    int32_t num_tags;             // the declaration's own tags, in DECLARED order;
    const char * const * tags;    // 0 and NULL when there are none
} ViewVocabulary;

typedef struct ViewType             // one declaration: a type, or a table
{
    const char * name;
    const char * file;
    int table;                     // declared `table`
    const TableTypeInfo * type;     // §8.1's descriptor: the properties, and this
                                    // declaration's own doc and tags beside them
    const char * doc;               // the `///` block above it; "" when none. The
                                    // same text `type->doc` carries (§8.1)
    int32_t num_tags;               // the declaration's tags (SPEC §4.2), in
    const char * const * tags;      // DECLARED order; the same list `type` carries
} ViewType;

typedef struct UnitViewInfo
{
    const char * package;
    uint64_t protocol_id;
    int32_t num_types;      const ViewType * types;
    int32_t num_tables;     const ViewType * tables;
    int32_t num_enums;      const ViewVocabulary * enums;
    int32_t num_flags;      const ViewVocabulary * flags;
    int32_t num_unions;     const ViewVocabulary * unions;
    int32_t num_constants;  const ViewConstant * constants;
} UnitViewInfo;

/* ---- prefill: the declared defaults, in place (docs/SPEC-TABLES.md) ---- */

static SCHEMA_UNUSED void arm_align_reset( ArmAlign * value );
static SCHEMA_UNUSED void schema_example_arm_align_reset_raw_( void * storage );
static SCHEMA_UNUSED void arm_array_reset( ArmArray * value );
static SCHEMA_UNUSED void schema_example_arm_array_reset_raw_( void * storage );
static SCHEMA_UNUSED void arm_empty_reset( ArmEmpty * value );
static SCHEMA_UNUSED void schema_example_arm_empty_reset_raw_( void * storage );
static SCHEMA_UNUSED void arms_agree_reset( ArmsAgree * value );
static SCHEMA_UNUSED void schema_example_arms_agree_reset_raw_( void * storage );
static SCHEMA_UNUSED void arms_disagree_reset( ArmsDisagree * value );
static SCHEMA_UNUSED void schema_example_arms_disagree_reset_raw_( void * storage );
static SCHEMA_UNUSED void arms_nested_reset( ArmsNested * value );
static SCHEMA_UNUSED void schema_example_arms_nested_reset_raw_( void * storage );
static SCHEMA_UNUSED void arr_eleven_reset( ArrEleven * value );
static SCHEMA_UNUSED void schema_example_arr_eleven_reset_raw_( void * storage );
static SCHEMA_UNUSED void arr_nested_reset( ArrNested * value );
static SCHEMA_UNUSED void schema_example_arr_nested_reset_raw_( void * storage );
static SCHEMA_UNUSED void arr_tri3_reset( ArrTri3 * value );
static SCHEMA_UNUSED void schema_example_arr_tri3_reset_raw_( void * storage );
static SCHEMA_UNUSED void arr_uneven_reset( ArrUneven * value );
static SCHEMA_UNUSED void schema_example_arr_uneven_reset_raw_( void * storage );
static SCHEMA_UNUSED void block_reset( Block * value );
static SCHEMA_UNUSED void schema_example_block_reset_raw_( void * storage );
static SCHEMA_UNUSED void chat_reset( Chat * value );
static SCHEMA_UNUSED void schema_example_chat_reset_raw_( void * storage );
static SCHEMA_UNUSED void compressed_probe_reset( CompressedProbe * value );
static SCHEMA_UNUSED void schema_example_compressed_probe_reset_raw_( void * storage );
static SCHEMA_UNUSED void default_arm_reset( DefaultArm * value );
static SCHEMA_UNUSED void schema_example_default_arm_reset_raw_( void * storage );
static SCHEMA_UNUSED void default_bulk_arm_reset( DefaultBulkArm * value );
static SCHEMA_UNUSED void schema_example_default_bulk_arm_reset_raw_( void * storage );
static SCHEMA_UNUSED void default_choice_packet_reset( DefaultChoicePacket * value );
static SCHEMA_UNUSED void schema_example_default_choice_packet_reset_raw_( void * storage );
static SCHEMA_UNUSED void eleven_reset( Eleven * value );
static SCHEMA_UNUSED void schema_example_eleven_reset_raw_( void * storage );
static SCHEMA_UNUSED void empty_a_reset( EmptyA * value );
static SCHEMA_UNUSED void schema_example_empty_a_reset_raw_( void * storage );
static SCHEMA_UNUSED void empty_b_reset( EmptyB * value );
static SCHEMA_UNUSED void schema_example_empty_b_reset_raw_( void * storage );
static SCHEMA_UNUSED void expression_probe_reset( ExpressionProbe * value );
static SCHEMA_UNUSED void schema_example_expression_probe_reset_raw_( void * storage );
static SCHEMA_UNUSED void extreme_probe_reset( ExtremeProbe * value );
static SCHEMA_UNUSED void schema_example_extreme_probe_reset_raw_( void * storage );
static SCHEMA_UNUSED void extreme_row_reset( ExtremeRow * value );
static SCHEMA_UNUSED void schema_example_extreme_row_reset_raw_( void * storage );
static SCHEMA_UNUSED void f13_reset( F13 * value );
static SCHEMA_UNUSED void schema_example_f13_reset_raw_( void * storage );
static SCHEMA_UNUSED void handle_reset( Handle * value );
static SCHEMA_UNUSED void schema_example_handle_reset_raw_( void * storage );
static SCHEMA_UNUSED void heartbeat_reset( Heartbeat * value );
static SCHEMA_UNUSED void schema_example_heartbeat_reset_raw_( void * storage );
static SCHEMA_UNUSED void holds_empty_union_reset( HoldsEmptyUnion * value );
static SCHEMA_UNUSED void schema_example_holds_empty_union_reset_raw_( void * storage );
static SCHEMA_UNUSED void holds_uneven_reset( HoldsUneven * value );
static SCHEMA_UNUSED void schema_example_holds_uneven_reset_raw_( void * storage );
static SCHEMA_UNUSED void input_reset( Input * value );
static SCHEMA_UNUSED void schema_example_input_reset_raw_( void * storage );
static SCHEMA_UNUSED void input_packet_reset( InputPacket * value );
static SCHEMA_UNUSED void schema_example_input_packet_reset_raw_( void * storage );
static SCHEMA_UNUSED void narrow_reset( Narrow * value );
static SCHEMA_UNUSED void schema_example_narrow_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_array_reset( ProbeArray * value );
static SCHEMA_UNUSED void schema_example_probe_array_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_bits_reset( ProbeBits * value );
static SCHEMA_UNUSED void schema_example_probe_bits_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_collider_reset( ProbeCollider * value );
static SCHEMA_UNUSED void schema_example_probe_collider_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_config_reset( ProbeConfig * value );
static SCHEMA_UNUSED void schema_example_probe_config_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_header_reset( ProbeHeader * value );
static SCHEMA_UNUSED void schema_example_probe_header_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_report_reset( ProbeReport * value );
static SCHEMA_UNUSED void schema_example_probe_report_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_ring_reset( ProbeRing * value );
static SCHEMA_UNUSED void schema_example_probe_ring_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_sample_reset( ProbeSample * value );
static SCHEMA_UNUSED void schema_example_probe_sample_reset_raw_( void * storage );
static SCHEMA_UNUSED void probe_slab_reset( ProbeSlab * value );
static SCHEMA_UNUSED void schema_example_probe_slab_reset_raw_( void * storage );
static SCHEMA_UNUSED void quantized_position_reset( QuantizedPosition * value );
static SCHEMA_UNUSED void schema_example_quantized_position_reset_raw_( void * storage );
static SCHEMA_UNUSED void quantized_rotation_reset( QuantizedRotation * value );
static SCHEMA_UNUSED void schema_example_quantized_rotation_reset_raw_( void * storage );
static SCHEMA_UNUSED void quantized_velocity_reset( QuantizedVelocity * value );
static SCHEMA_UNUSED void schema_example_quantized_velocity_reset_raw_( void * storage );
static SCHEMA_UNUSED void quat_reset( Quat * value );
static SCHEMA_UNUSED void schema_example_quat_reset_raw_( void * storage );
static SCHEMA_UNUSED void regain_after_align_reset( RegainAfterAlign * value );
static SCHEMA_UNUSED void schema_example_regain_after_align_reset_raw_( void * storage );
static SCHEMA_UNUSED void render_block_reset( RenderBlock * value );
static SCHEMA_UNUSED void schema_example_render_block_reset_raw_( void * storage );
static SCHEMA_UNUSED void render_sprite_reset( RenderSprite * value );
static SCHEMA_UNUSED void schema_example_render_sprite_reset_raw_( void * storage );
static SCHEMA_UNUSED void rigid_body_reset( RigidBody * value );
static SCHEMA_UNUSED void schema_example_rigid_body_reset_raw_( void * storage );
static SCHEMA_UNUSED void ship_create_reset( ShipCreate * value );
static SCHEMA_UNUSED void schema_example_ship_create_reset_raw_( void * storage );
static SCHEMA_UNUSED void sole_reset( Sole * value );
static SCHEMA_UNUSED void schema_example_sole_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_chunk_reset( SpanChunk * value );
static SCHEMA_UNUSED void schema_example_span_chunk_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_f64_reset( SpanF64 * value );
static SCHEMA_UNUSED void schema_example_span_f64_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_i64_reset( SpanI64 * value );
static SCHEMA_UNUSED void schema_example_span_i64_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_one_reset( SpanOne * value );
static SCHEMA_UNUSED void schema_example_span_one_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_tail_reset( SpanTail * value );
static SCHEMA_UNUSED void schema_example_span_tail_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_twice_reset( SpanTwice * value );
static SCHEMA_UNUSED void schema_example_span_twice_reset_raw_( void * storage );
static SCHEMA_UNUSED void span_u64_reset( SpanU64 * value );
static SCHEMA_UNUSED void schema_example_span_u64_reset_raw_( void * storage );
static SCHEMA_UNUSED void strs_reset( Strs * value );
static SCHEMA_UNUSED void schema_example_strs_reset_raw_( void * storage );
static SCHEMA_UNUSED void test_reset( Test * value );
static SCHEMA_UNUSED void schema_example_test_reset_raw_( void * storage );
static SCHEMA_UNUSED void test_data_reset( TestData * value );
static SCHEMA_UNUSED void schema_example_test_data_reset_raw_( void * storage );
static SCHEMA_UNUSED void tri3_reset( Tri3 * value );
static SCHEMA_UNUSED void schema_example_tri3_reset_raw_( void * storage );
static SCHEMA_UNUSED void trio_reset( Trio * value );
static SCHEMA_UNUSED void schema_example_trio_reset_raw_( void * storage );
static SCHEMA_UNUSED void trio_first_reset( TrioFirst * value );
static SCHEMA_UNUSED void schema_example_trio_first_reset_raw_( void * storage );
static SCHEMA_UNUSED void trio_sole_reset( TrioSole * value );
static SCHEMA_UNUSED void schema_example_trio_sole_reset_raw_( void * storage );
static SCHEMA_UNUSED void trio_straddle_reset( TrioStraddle * value );
static SCHEMA_UNUSED void schema_example_trio_straddle_reset_raw_( void * storage );
static SCHEMA_UNUSED void vec2_reset( Vec2 * value );
static SCHEMA_UNUSED void schema_example_vec2_reset_raw_( void * storage );
static SCHEMA_UNUSED void vec3_reset( Vec3 * value );
static SCHEMA_UNUSED void schema_example_vec3_reset_raw_( void * storage );
static SCHEMA_UNUSED void w1_reset( W1 * value );
static SCHEMA_UNUSED void schema_example_w1_reset_raw_( void * storage );
static SCHEMA_UNUSED void w13_reset( W13 * value );
static SCHEMA_UNUSED void schema_example_w13_reset_raw_( void * storage );
static SCHEMA_UNUSED void w17_reset( W17 * value );
static SCHEMA_UNUSED void schema_example_w17_reset_raw_( void * storage );
static SCHEMA_UNUSED void w26_reset( W26 * value );
static SCHEMA_UNUSED void schema_example_w26_reset_raw_( void * storage );
static SCHEMA_UNUSED void w50_reset( W50 * value );
static SCHEMA_UNUSED void schema_example_w50_reset_raw_( void * storage );
static SCHEMA_UNUSED void w52_reset( W52 * value );
static SCHEMA_UNUSED void schema_example_w52_reset_raw_( void * storage );
static SCHEMA_UNUSED void wide_reset( Wide * value );
static SCHEMA_UNUSED void schema_example_wide_reset_raw_( void * storage );

static SCHEMA_UNUSED void arm_align_reset( ArmAlign * value )
{
    value->lead = 0;
    value->flag = 0;
    memset( value->s, 0, sizeof( value->s ) );
    value->s_length = 0;
    value->b = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arm_align_reset_raw_( void * storage ) { arm_align_reset( (ArmAlign *) storage ); }

extern const TableTypeInfo schema_example_arm_align_info_;
static SCHEMA_UNUSED const TableTypeInfo * arm_align_table_type(void) { return &schema_example_arm_align_info_; }
static SCHEMA_UNUSED void arm_array_reset( ArmArray * value )
{
    value->lead = 0;
    value->flag = 0;
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
    value->b = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arm_array_reset_raw_( void * storage ) { arm_array_reset( (ArmArray *) storage ); }

extern const TableTypeInfo schema_example_arm_array_info_;
static SCHEMA_UNUSED const TableTypeInfo * arm_array_table_type(void) { return &schema_example_arm_array_info_; }
static SCHEMA_UNUSED void arm_empty_reset( ArmEmpty * value )
{
    value->lead = 0;
    value->flag = 0;
    value->a = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arm_empty_reset_raw_( void * storage ) { arm_empty_reset( (ArmEmpty *) storage ); }

extern const TableTypeInfo schema_example_arm_empty_info_;
static SCHEMA_UNUSED const TableTypeInfo * arm_empty_table_type(void) { return &schema_example_arm_empty_info_; }
static SCHEMA_UNUSED void arms_agree_reset( ArmsAgree * value )
{
    value->lead = 0;
    value->flag = 0;
    value->a = 0;
    value->b = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arms_agree_reset_raw_( void * storage ) { arms_agree_reset( (ArmsAgree *) storage ); }

extern const TableTypeInfo schema_example_arms_agree_info_;
static SCHEMA_UNUSED const TableTypeInfo * arms_agree_table_type(void) { return &schema_example_arms_agree_info_; }
static SCHEMA_UNUSED void arms_disagree_reset( ArmsDisagree * value )
{
    value->lead = 0;
    value->flag = 0;
    value->a = 0;
    value->b = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arms_disagree_reset_raw_( void * storage ) { arms_disagree_reset( (ArmsDisagree *) storage ); }

extern const TableTypeInfo schema_example_arms_disagree_info_;
static SCHEMA_UNUSED const TableTypeInfo * arms_disagree_table_type(void) { return &schema_example_arms_disagree_info_; }
static SCHEMA_UNUSED void arms_nested_reset( ArmsNested * value )
{
    value->lead = 0;
    value->outer = 0;
    value->inner = 0;
    value->x = 0;
    value->y = 0;
    value->z = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arms_nested_reset_raw_( void * storage ) { arms_nested_reset( (ArmsNested *) storage ); }

extern const TableTypeInfo schema_example_arms_nested_info_;
static SCHEMA_UNUSED const TableTypeInfo * arms_nested_table_type(void) { return &schema_example_arms_nested_info_; }
static SCHEMA_UNUSED void arr_eleven_reset( ArrEleven * value )
{
    eleven_reset( &value->items[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 9 ); i++ ) { value->items[i] = value->items[0]; } }
}

static SCHEMA_UNUSED void schema_example_arr_eleven_reset_raw_( void * storage ) { arr_eleven_reset( (ArrEleven *) storage ); }

extern const TableTypeInfo schema_example_arr_eleven_info_;
static SCHEMA_UNUSED const TableTypeInfo * arr_eleven_table_type(void) { return &schema_example_arr_eleven_info_; }
static SCHEMA_UNUSED void arr_nested_reset( ArrNested * value )
{
    value->lead = 0;
    eleven_reset( &value->items[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 4 ); i++ ) { value->items[i] = value->items[0]; } }
    value->items_count = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arr_nested_reset_raw_( void * storage ) { arr_nested_reset( (ArrNested *) storage ); }

extern const TableTypeInfo schema_example_arr_nested_info_;
static SCHEMA_UNUSED const TableTypeInfo * arr_nested_table_type(void) { return &schema_example_arr_nested_info_; }
static SCHEMA_UNUSED void arr_tri3_reset( ArrTri3 * value )
{
    tri3_reset( &value->items[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 10 ); i++ ) { value->items[i] = value->items[0]; } }
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_arr_tri3_reset_raw_( void * storage ) { arr_tri3_reset( (ArrTri3 *) storage ); }

extern const TableTypeInfo schema_example_arr_tri3_info_;
static SCHEMA_UNUSED const TableTypeInfo * arr_tri3_table_type(void) { return &schema_example_arr_tri3_info_; }
static SCHEMA_UNUSED void arr_uneven_reset( ArrUneven * value )
{
    value->lead = 0;
    memset( &value->items[0], 0, sizeof( value->items[0] ) );
    { int32_t i; for ( i = 1; i < (int32_t) ( 3 ); i++ ) { value->items[i] = value->items[0]; } }
    value->items_count = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_arr_uneven_reset_raw_( void * storage ) { arr_uneven_reset( (ArrUneven *) storage ); }

extern const TableTypeInfo schema_example_arr_uneven_info_;
static SCHEMA_UNUSED const TableTypeInfo * arr_uneven_table_type(void) { return &schema_example_arr_uneven_info_; }
static SCHEMA_UNUSED void block_reset( Block * value )
{
    memset( value->data, 0, sizeof( value->data ) );
    value->data_length = 0;
}

static SCHEMA_UNUSED void schema_example_block_reset_raw_( void * storage ) { block_reset( (Block *) storage ); }

extern const TableTypeInfo schema_example_block_info_;
static SCHEMA_UNUSED const TableTypeInfo * block_table_type(void) { return &schema_example_block_info_; }
static SCHEMA_UNUSED void chat_reset( Chat * value )
{
    memset( value->text, 0, sizeof( value->text ) );
    value->text_length = 0;
}

static SCHEMA_UNUSED void schema_example_chat_reset_raw_( void * storage ) { chat_reset( (Chat *) storage ); }

extern const TableTypeInfo schema_example_chat_info_;
static SCHEMA_UNUSED const TableTypeInfo * chat_table_type(void) { return &schema_example_chat_info_; }
static SCHEMA_UNUSED void compressed_probe_reset( CompressedProbe * value )
{
    value->boundary = 0.0f;
    value->offset = 0.0f;
}

static SCHEMA_UNUSED void schema_example_compressed_probe_reset_raw_( void * storage ) { compressed_probe_reset( (CompressedProbe *) storage ); }

extern const TableTypeInfo schema_example_compressed_probe_info_;
static SCHEMA_UNUSED const TableTypeInfo * compressed_probe_table_type(void) { return &schema_example_compressed_probe_info_; }
static SCHEMA_UNUSED void default_arm_reset( DefaultArm * value )
{
    probe_config_reset( &value->entries[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 2 ); i++ ) { value->entries[i] = value->entries[0]; } }
    value->entries_count = 0;
    value->marker = 0;
}

static SCHEMA_UNUSED void schema_example_default_arm_reset_raw_( void * storage ) { default_arm_reset( (DefaultArm *) storage ); }

extern const TableTypeInfo schema_example_default_arm_info_;
static SCHEMA_UNUSED const TableTypeInfo * default_arm_table_type(void) { return &schema_example_default_arm_info_; }
static SCHEMA_UNUSED void default_bulk_arm_reset( DefaultBulkArm * value )
{
    default_arm_reset( &value->payload );
    memset( value->data, 0, sizeof( value->data ) );
    value->data_length = 0;
}

static SCHEMA_UNUSED void schema_example_default_bulk_arm_reset_raw_( void * storage ) { default_bulk_arm_reset( (DefaultBulkArm *) storage ); }

extern const TableTypeInfo schema_example_default_bulk_arm_info_;
static SCHEMA_UNUSED const TableTypeInfo * default_bulk_arm_table_type(void) { return &schema_example_default_bulk_arm_info_; }
static SCHEMA_UNUSED void default_choice_packet_reset( DefaultChoicePacket * value )
{
    memset( &value->choice, 0, sizeof( value->choice ) );
}

static SCHEMA_UNUSED void schema_example_default_choice_packet_reset_raw_( void * storage ) { default_choice_packet_reset( (DefaultChoicePacket *) storage ); }

extern const TableTypeInfo schema_example_default_choice_packet_info_;
static SCHEMA_UNUSED const TableTypeInfo * default_choice_packet_table_type(void) { return &schema_example_default_choice_packet_info_; }
static SCHEMA_UNUSED void eleven_reset( Eleven * value )
{
    value->a = 0;
    value->b = 0;
}

static SCHEMA_UNUSED void schema_example_eleven_reset_raw_( void * storage ) { eleven_reset( (Eleven *) storage ); }

extern const TableTypeInfo schema_example_eleven_info_;
static SCHEMA_UNUSED const TableTypeInfo * eleven_table_type(void) { return &schema_example_eleven_info_; }
static SCHEMA_UNUSED void empty_a_reset( EmptyA * value )
{
    memset( value, 0, sizeof( *value ) );
}

static SCHEMA_UNUSED void schema_example_empty_a_reset_raw_( void * storage ) { empty_a_reset( (EmptyA *) storage ); }

extern const TableTypeInfo schema_example_empty_a_info_;
static SCHEMA_UNUSED const TableTypeInfo * empty_a_table_type(void) { return &schema_example_empty_a_info_; }
static SCHEMA_UNUSED void empty_b_reset( EmptyB * value )
{
    memset( value, 0, sizeof( *value ) );
}

static SCHEMA_UNUSED void schema_example_empty_b_reset_raw_( void * storage ) { empty_b_reset( (EmptyB *) storage ); }

extern const TableTypeInfo schema_example_empty_b_info_;
static SCHEMA_UNUSED const TableTypeInfo * empty_b_table_type(void) { return &schema_example_empty_b_info_; }
static SCHEMA_UNUSED void expression_probe_reset( ExpressionProbe * value )
{
    value->hardpoint_index = 0;
    value->spin_rate = 1024;
}

static SCHEMA_UNUSED void schema_example_expression_probe_reset_raw_( void * storage ) { expression_probe_reset( (ExpressionProbe *) storage ); }

extern const TableTypeInfo schema_example_expression_probe_info_;
static SCHEMA_UNUSED const TableTypeInfo * expression_probe_table_type(void) { return &schema_example_expression_probe_info_; }
static SCHEMA_UNUSED void extreme_probe_reset( ExtremeProbe * value )
{
    value->floor_bound = 0;
    value->doubled_floor = 0;
    value->ceiling_range = 1ull;
    value->floor_default = ( -9223372036854775807ll - 1 );
    value->ceiling_default = 18446744073709551615ull;
}

static SCHEMA_UNUSED void schema_example_extreme_probe_reset_raw_( void * storage ) { extreme_probe_reset( (ExtremeProbe *) storage ); }

extern const TableTypeInfo schema_example_extreme_probe_info_;
static SCHEMA_UNUSED const TableTypeInfo * extreme_probe_table_type(void) { return &schema_example_extreme_probe_info_; }
static SCHEMA_UNUSED void extreme_row_reset( ExtremeRow * value )
{
    value->clamped_floor = 0;
    value->clamped_ceiling = 1ull;
    value->floor_def = ( -9223372036854775807ll - 1 );
    value->ceiling_def = 18446744073709551615ull;
}

static SCHEMA_UNUSED void schema_example_extreme_row_reset_raw_( void * storage ) { extreme_row_reset( (ExtremeRow *) storage ); }

extern const TableTypeInfo schema_example_extreme_row_info_;
static SCHEMA_UNUSED const TableTypeInfo * extreme_row_table_type(void) { return &schema_example_extreme_row_info_; }
static SCHEMA_UNUSED void f13_reset( F13 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
}

static SCHEMA_UNUSED void schema_example_f13_reset_raw_( void * storage ) { f13_reset( (F13 *) storage ); }

extern const TableTypeInfo schema_example_f13_info_;
static SCHEMA_UNUSED const TableTypeInfo * f13_table_type(void) { return &schema_example_f13_info_; }
static SCHEMA_UNUSED void handle_reset( Handle * value )
{
    value->object_id = 0;
    value->object_sequence = 0;
}

static SCHEMA_UNUSED void schema_example_handle_reset_raw_( void * storage ) { handle_reset( (Handle *) storage ); }

extern const TableTypeInfo schema_example_handle_info_;
static SCHEMA_UNUSED const TableTypeInfo * handle_table_type(void) { return &schema_example_handle_info_; }
static SCHEMA_UNUSED void heartbeat_reset( Heartbeat * value )
{
    memset( value, 0, sizeof( *value ) );
}

static SCHEMA_UNUSED void schema_example_heartbeat_reset_raw_( void * storage ) { heartbeat_reset( (Heartbeat *) storage ); }

extern const TableTypeInfo schema_example_heartbeat_info_;
static SCHEMA_UNUSED const TableTypeInfo * heartbeat_table_type(void) { return &schema_example_heartbeat_info_; }
static SCHEMA_UNUSED void holds_empty_union_reset( HoldsEmptyUnion * value )
{
    value->lead = 0;
    memset( &value->u, 0, sizeof( value->u ) );
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_holds_empty_union_reset_raw_( void * storage ) { holds_empty_union_reset( (HoldsEmptyUnion *) storage ); }

extern const TableTypeInfo schema_example_holds_empty_union_info_;
static SCHEMA_UNUSED const TableTypeInfo * holds_empty_union_table_type(void) { return &schema_example_holds_empty_union_info_; }
static SCHEMA_UNUSED void holds_uneven_reset( HoldsUneven * value )
{
    value->lead = 0;
    memset( &value->u, 0, sizeof( value->u ) );
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_holds_uneven_reset_raw_( void * storage ) { holds_uneven_reset( (HoldsUneven *) storage ); }

extern const TableTypeInfo schema_example_holds_uneven_info_;
static SCHEMA_UNUSED const TableTypeInfo * holds_uneven_table_type(void) { return &schema_example_holds_uneven_info_; }
static SCHEMA_UNUSED void input_reset( Input * value )
{
    value->stick_x = 0.0f;
    value->stick_y = 0.0f;
    value->throttle = 0.0f;
    value->yaw = 0.0f;
    value->pitch = 0.0f;
    value->fire = 0;
    value->alt_fire = 0;
    value->boost = 0;
    value->brake = 0;
    value->aim = 0;
    value->lock_on = 0;
    value->zoom = 0;
    value->ping = 0;
}

static SCHEMA_UNUSED void schema_example_input_reset_raw_( void * storage ) { input_reset( (Input *) storage ); }

extern const TableTypeInfo schema_example_input_info_;
static SCHEMA_UNUSED const TableTypeInfo * input_table_type(void) { return &schema_example_input_info_; }
static SCHEMA_UNUSED void input_packet_reset( InputPacket * value )
{
    value->synchronize_sequence = 0;
    value->current_frame = 0;
    value->start_frame = 0;
    input_reset( &value->inputs[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 16 ); i++ ) { value->inputs[i] = value->inputs[0]; } }
    value->inputs_count = 0;
}

static SCHEMA_UNUSED void schema_example_input_packet_reset_raw_( void * storage ) { input_packet_reset( (InputPacket *) storage ); }

extern const TableTypeInfo schema_example_input_packet_info_;
static SCHEMA_UNUSED const TableTypeInfo * input_packet_table_type(void) { return &schema_example_input_packet_info_; }
static SCHEMA_UNUSED void narrow_reset( Narrow * value )
{
    value->n = 0;
}

static SCHEMA_UNUSED void schema_example_narrow_reset_raw_( void * storage ) { narrow_reset( (Narrow *) storage ); }

extern const TableTypeInfo schema_example_narrow_info_;
static SCHEMA_UNUSED const TableTypeInfo * narrow_table_type(void) { return &schema_example_narrow_info_; }
static SCHEMA_UNUSED void probe_array_reset( ProbeArray * value )
{
    probe_sample_reset( &value->samples[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 2 ); i++ ) { value->samples[i] = value->samples[0]; } }
    probe_config_reset( &value->config );
}

static SCHEMA_UNUSED void schema_example_probe_array_reset_raw_( void * storage ) { probe_array_reset( (ProbeArray *) storage ); }

extern const TableTypeInfo schema_example_probe_array_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_array_table_type(void) { return &schema_example_probe_array_info_; }
static SCHEMA_UNUSED void probe_bits_reset( ProbeBits * value )
{
    value->small = 0;
    value->boundary = 0;
    value->wide = 0;
    value->sensor = 0;
    value->nonce = 0;
}

static SCHEMA_UNUSED void schema_example_probe_bits_reset_raw_( void * storage ) { probe_bits_reset( (ProbeBits *) storage ); }

extern const TableTypeInfo schema_example_probe_bits_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_bits_table_type(void) { return &schema_example_probe_bits_info_; }
static SCHEMA_UNUSED void probe_collider_reset( ProbeCollider * value )
{
    value->armor = 0;
    memset( &value->shape, 0, sizeof( value->shape ) );
    memset( &value->backup, 0, sizeof( value->backup ) );
    memset( &value->extras[0], 0, sizeof( value->extras[0] ) );
    { int32_t i; for ( i = 1; i < (int32_t) ( 2 ); i++ ) { value->extras[i] = value->extras[0]; } }
    value->extras_count = 0;
}

static SCHEMA_UNUSED void schema_example_probe_collider_reset_raw_( void * storage ) { probe_collider_reset( (ProbeCollider *) storage ); }

extern const TableTypeInfo schema_example_probe_collider_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_collider_table_type(void) { return &schema_example_probe_collider_info_; }
static SCHEMA_UNUSED void probe_config_reset( ProbeConfig * value )
{
    value->retries = -1;
    value->preferred = WEAPON_RAILGUN;
}

static SCHEMA_UNUSED void schema_example_probe_config_reset_raw_( void * storage ) { probe_config_reset( (ProbeConfig *) storage ); }

extern const TableTypeInfo schema_example_probe_config_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_config_table_type(void) { return &schema_example_probe_config_info_; }
static SCHEMA_UNUSED void probe_header_reset( ProbeHeader * value )
{
    value->version = 0;
    value->probe_id = 0;
}

static SCHEMA_UNUSED void schema_example_probe_header_reset_raw_( void * storage ) { probe_header_reset( (ProbeHeader *) storage ); }

extern const TableTypeInfo schema_example_probe_header_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_header_table_type(void) { return &schema_example_probe_header_info_; }
static SCHEMA_UNUSED void probe_report_reset( ProbeReport * value )
{
    probe_header_reset( &value->header );
    value->flags = 0;
    test_reset( &value->echo );
}

static SCHEMA_UNUSED void schema_example_probe_report_reset_raw_( void * storage ) { probe_report_reset( (ProbeReport *) storage ); }

extern const TableTypeInfo schema_example_probe_report_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_report_table_type(void) { return &schema_example_probe_report_info_; }
static SCHEMA_UNUSED void probe_ring_reset( ProbeRing * value )
{
    value->radius = 0;
}

static SCHEMA_UNUSED void schema_example_probe_ring_reset_raw_( void * storage ) { probe_ring_reset( (ProbeRing *) storage ); }

extern const TableTypeInfo schema_example_probe_ring_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_ring_table_type(void) { return &schema_example_probe_ring_info_; }
static SCHEMA_UNUSED void probe_sample_reset( ProbeSample * value )
{
    value->active = 1;
    value->orientation = 0.0f;
    value->raw_delta = 0;
    value->big_delta = 0;
    value->weapon = WEAPON_NONE;
    value->has_target = 0;
    value->target_id = 0;
    value->idle_ticks = 0;
    memset( value->samples, 0, sizeof( value->samples ) );
    value->samples_count = 0;
}

static SCHEMA_UNUSED void schema_example_probe_sample_reset_raw_( void * storage ) { probe_sample_reset( (ProbeSample *) storage ); }

extern const TableTypeInfo schema_example_probe_sample_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_sample_table_type(void) { return &schema_example_probe_sample_info_; }
static SCHEMA_UNUSED void probe_slab_reset( ProbeSlab * value )
{
    value->width = 0;
    value->height = 0;
}

static SCHEMA_UNUSED void schema_example_probe_slab_reset_raw_( void * storage ) { probe_slab_reset( (ProbeSlab *) storage ); }

extern const TableTypeInfo schema_example_probe_slab_info_;
static SCHEMA_UNUSED const TableTypeInfo * probe_slab_table_type(void) { return &schema_example_probe_slab_info_; }
static SCHEMA_UNUSED void quantized_position_reset( QuantizedPosition * value )
{
    value->x = 0;
    value->y = 0;
    value->z = 0;
}

static SCHEMA_UNUSED void schema_example_quantized_position_reset_raw_( void * storage ) { quantized_position_reset( (QuantizedPosition *) storage ); }

extern const TableTypeInfo schema_example_quantized_position_info_;
static SCHEMA_UNUSED const TableTypeInfo * quantized_position_table_type(void) { return &schema_example_quantized_position_info_; }
static SCHEMA_UNUSED void quantized_rotation_reset( QuantizedRotation * value )
{
    value->x = 0;
    value->y = 0;
    value->z = 0;
    value->w = 0;
}

static SCHEMA_UNUSED void schema_example_quantized_rotation_reset_raw_( void * storage ) { quantized_rotation_reset( (QuantizedRotation *) storage ); }

extern const TableTypeInfo schema_example_quantized_rotation_info_;
static SCHEMA_UNUSED const TableTypeInfo * quantized_rotation_table_type(void) { return &schema_example_quantized_rotation_info_; }
static SCHEMA_UNUSED void quantized_velocity_reset( QuantizedVelocity * value )
{
    value->x = 0;
    value->y = 0;
    value->z = 0;
}

static SCHEMA_UNUSED void schema_example_quantized_velocity_reset_raw_( void * storage ) { quantized_velocity_reset( (QuantizedVelocity *) storage ); }

extern const TableTypeInfo schema_example_quantized_velocity_info_;
static SCHEMA_UNUSED const TableTypeInfo * quantized_velocity_table_type(void) { return &schema_example_quantized_velocity_info_; }
static SCHEMA_UNUSED void quat_reset( Quat * value )
{
    value->x = 0.0;
    value->y = 0.0;
    value->z = 0.0;
    value->w = 0.0;
}

static SCHEMA_UNUSED void schema_example_quat_reset_raw_( void * storage ) { quat_reset( (Quat *) storage ); }

extern const TableTypeInfo schema_example_quat_info_;
static SCHEMA_UNUSED const TableTypeInfo * quat_table_type(void) { return &schema_example_quat_info_; }
static SCHEMA_UNUSED void regain_after_align_reset( RegainAfterAlign * value )
{
    value->lead = 0;
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
    memset( value->s, 0, sizeof( value->s ) );
    value->s_length = 0;
    value->p = 0;
    value->q = 0;
    value->r = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_regain_after_align_reset_raw_( void * storage ) { regain_after_align_reset( (RegainAfterAlign *) storage ); }

extern const TableTypeInfo schema_example_regain_after_align_info_;
static SCHEMA_UNUSED const TableTypeInfo * regain_after_align_table_type(void) { return &schema_example_regain_after_align_info_; }
static SCHEMA_UNUSED void render_block_reset( RenderBlock * value )
{
    value->worker_index = 0;
    value->sprite_count_hint = 0;
    render_sprite_reset( &value->sprites[0] );
    { int32_t i; for ( i = 1; i < (int32_t) ( 64 ); i++ ) { value->sprites[i] = value->sprites[0]; } }
    value->sprites_count = 0;
}

static SCHEMA_UNUSED void schema_example_render_block_reset_raw_( void * storage ) { render_block_reset( (RenderBlock *) storage ); }

extern const TableTypeInfo schema_example_render_block_info_;
static SCHEMA_UNUSED const TableTypeInfo * render_block_table_type(void) { return &schema_example_render_block_info_; }
static SCHEMA_UNUSED void render_sprite_reset( RenderSprite * value )
{
    value->sort_key = 0;
    value->mesh_id = 0;
    value->material_id = 0;
    value->layer = 0;
    value->team = TEAM_NONE;
}

static SCHEMA_UNUSED void schema_example_render_sprite_reset_raw_( void * storage ) { render_sprite_reset( (RenderSprite *) storage ); }

extern const TableTypeInfo schema_example_render_sprite_info_;
static SCHEMA_UNUSED const TableTypeInfo * render_sprite_table_type(void) { return &schema_example_render_sprite_info_; }
static SCHEMA_UNUSED void rigid_body_reset( RigidBody * value )
{
    vec3_reset( &value->position );
    quat_reset( &value->orientation );
    value->at_rest = 0;
    vec3_reset( &value->linear_velocity );
    vec3_reset( &value->angular_velocity );
}

static SCHEMA_UNUSED void schema_example_rigid_body_reset_raw_( void * storage ) { rigid_body_reset( (RigidBody *) storage ); }

extern const TableTypeInfo schema_example_rigid_body_info_;
static SCHEMA_UNUSED const TableTypeInfo * rigid_body_table_type(void) { return &schema_example_rigid_body_info_; }
static SCHEMA_UNUSED void ship_create_reset( ShipCreate * value )
{
    value->ship_type = SHIP_TYPE_NONE;
    quantized_position_reset( &value->position );
    quantized_rotation_reset( &value->rotation );
    quantized_velocity_reset( &value->linear_velocity );
    value->has_flags = 0;
    value->flags = 0;
    value->team = TEAM_NONE;
    value->health = 0;
    value->thrust = 0;
    value->pending = PENDING_NONE;
}

static SCHEMA_UNUSED void schema_example_ship_create_reset_raw_( void * storage ) { ship_create_reset( (ShipCreate *) storage ); }

extern const TableTypeInfo schema_example_ship_create_info_;
static SCHEMA_UNUSED const TableTypeInfo * ship_create_table_type(void) { return &schema_example_ship_create_info_; }
static SCHEMA_UNUSED void sole_reset( Sole * value )
{
    value->only = 0;
}

static SCHEMA_UNUSED void schema_example_sole_reset_raw_( void * storage ) { sole_reset( (Sole *) storage ); }

extern const TableTypeInfo schema_example_sole_info_;
static SCHEMA_UNUSED const TableTypeInfo * sole_table_type(void) { return &schema_example_sole_info_; }
static SCHEMA_UNUSED void span_chunk_reset( SpanChunk * value )
{
    memset( value->values, 0, sizeof( value->values ) );
}

static SCHEMA_UNUSED void schema_example_span_chunk_reset_raw_( void * storage ) { span_chunk_reset( (SpanChunk *) storage ); }

extern const TableTypeInfo schema_example_span_chunk_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_chunk_table_type(void) { return &schema_example_span_chunk_info_; }
static SCHEMA_UNUSED void span_f64_reset( SpanF64 * value )
{
    memset( value->values, 0, sizeof( value->values ) );
}

static SCHEMA_UNUSED void schema_example_span_f64_reset_raw_( void * storage ) { span_f64_reset( (SpanF64 *) storage ); }

extern const TableTypeInfo schema_example_span_f64_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_f64_table_type(void) { return &schema_example_span_f64_info_; }
static SCHEMA_UNUSED void span_i64_reset( SpanI64 * value )
{
    memset( value->values, 0, sizeof( value->values ) );
}

static SCHEMA_UNUSED void schema_example_span_i64_reset_raw_( void * storage ) { span_i64_reset( (SpanI64 *) storage ); }

extern const TableTypeInfo schema_example_span_i64_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_i64_table_type(void) { return &schema_example_span_i64_info_; }
static SCHEMA_UNUSED void span_one_reset( SpanOne * value )
{
    memset( value->values, 0, sizeof( value->values ) );
}

static SCHEMA_UNUSED void schema_example_span_one_reset_raw_( void * storage ) { span_one_reset( (SpanOne *) storage ); }

extern const TableTypeInfo schema_example_span_one_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_one_table_type(void) { return &schema_example_span_one_info_; }
static SCHEMA_UNUSED void span_tail_reset( SpanTail * value )
{
    memset( value->values, 0, sizeof( value->values ) );
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_span_tail_reset_raw_( void * storage ) { span_tail_reset( (SpanTail *) storage ); }

extern const TableTypeInfo schema_example_span_tail_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_tail_table_type(void) { return &schema_example_span_tail_info_; }
static SCHEMA_UNUSED void span_twice_reset( SpanTwice * value )
{
    memset( value->a, 0, sizeof( value->a ) );
    memset( value->b, 0, sizeof( value->b ) );
}

static SCHEMA_UNUSED void schema_example_span_twice_reset_raw_( void * storage ) { span_twice_reset( (SpanTwice *) storage ); }

extern const TableTypeInfo schema_example_span_twice_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_twice_table_type(void) { return &schema_example_span_twice_info_; }
static SCHEMA_UNUSED void span_u64_reset( SpanU64 * value )
{
    memset( value->values, 0, sizeof( value->values ) );
}

static SCHEMA_UNUSED void schema_example_span_u64_reset_raw_( void * storage ) { span_u64_reset( (SpanU64 *) storage ); }

extern const TableTypeInfo schema_example_span_u64_info_;
static SCHEMA_UNUSED const TableTypeInfo * span_u64_table_type(void) { return &schema_example_span_u64_info_; }
static SCHEMA_UNUSED void strs_reset( Strs * value )
{
    value->lead = 0;
    memset( value->s, 0, sizeof( value->s ) );
    value->s_length = 0;
    memset( value->b, 0, sizeof( value->b ) );
    value->b_length = 0;
    value->tail = 0;
}

static SCHEMA_UNUSED void schema_example_strs_reset_raw_( void * storage ) { strs_reset( (Strs *) storage ); }

extern const TableTypeInfo schema_example_strs_info_;
static SCHEMA_UNUSED const TableTypeInfo * strs_table_type(void) { return &schema_example_strs_info_; }
static SCHEMA_UNUSED void test_reset( Test * value )
{
    value->test_a = 0;
    value->test_b = 0;
    value->test_c = 0;
    value->test_d = 0;
}

static SCHEMA_UNUSED void schema_example_test_reset_raw_( void * storage ) { test_reset( (Test *) storage ); }

extern const TableTypeInfo schema_example_test_info_;
static SCHEMA_UNUSED const TableTypeInfo * test_table_type(void) { return &schema_example_test_info_; }
static SCHEMA_UNUSED void test_data_reset( TestData * value )
{
    value->a = 0;
    value->b = 0;
    value->c = 0;
    value->d = 0;
    value->e = 0;
    value->f = 0;
    value->g = 0;
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
    value->float_value = 0.0f;
    value->compressed_float_value = 0.0f;
    value->double_value = 0.0;
    value->int8_value = 0;
    value->int16_value = 0;
    value->uint8_value = 0;
    value->uint16_value = 0;
    value->uint32_value = 0;
    value->uint64_value = 0;
    value->int64_full = 0;
    value->int64_range = 0;
    memset( value->fixed_bytes, 0, sizeof( value->fixed_bytes ) );
    memset( value->text, 0, sizeof( value->text ) );
    value->text_length = 0;
}

static SCHEMA_UNUSED void schema_example_test_data_reset_raw_( void * storage ) { test_data_reset( (TestData *) storage ); }

extern const TableTypeInfo schema_example_test_data_info_;
static SCHEMA_UNUSED const TableTypeInfo * test_data_table_type(void) { return &schema_example_test_data_info_; }
static SCHEMA_UNUSED void tri3_reset( Tri3 * value )
{
    value->a = 0;
    value->b = 0;
}

static SCHEMA_UNUSED void schema_example_tri3_reset_raw_( void * storage ) { tri3_reset( (Tri3 *) storage ); }

extern const TableTypeInfo schema_example_tri3_info_;
static SCHEMA_UNUSED const TableTypeInfo * tri3_table_type(void) { return &schema_example_tri3_info_; }
static SCHEMA_UNUSED void trio_reset( Trio * value )
{
    value->a = 0;
    value->b = 0;
    value->c = 0;
}

static SCHEMA_UNUSED void schema_example_trio_reset_raw_( void * storage ) { trio_reset( (Trio *) storage ); }

extern const TableTypeInfo schema_example_trio_info_;
static SCHEMA_UNUSED const TableTypeInfo * trio_table_type(void) { return &schema_example_trio_info_; }
static SCHEMA_UNUSED void trio_first_reset( TrioFirst * value )
{
    trio_reset( &value->inner );
    value->trailer = 0;
}

static SCHEMA_UNUSED void schema_example_trio_first_reset_raw_( void * storage ) { trio_first_reset( (TrioFirst *) storage ); }

extern const TableTypeInfo schema_example_trio_first_info_;
static SCHEMA_UNUSED const TableTypeInfo * trio_first_table_type(void) { return &schema_example_trio_first_info_; }
static SCHEMA_UNUSED void trio_sole_reset( TrioSole * value )
{
    trio_reset( &value->inner );
}

static SCHEMA_UNUSED void schema_example_trio_sole_reset_raw_( void * storage ) { trio_sole_reset( (TrioSole *) storage ); }

extern const TableTypeInfo schema_example_trio_sole_info_;
static SCHEMA_UNUSED const TableTypeInfo * trio_sole_table_type(void) { return &schema_example_trio_sole_info_; }
static SCHEMA_UNUSED void trio_straddle_reset( TrioStraddle * value )
{
    value->pad0 = 0;
    value->pad1 = 0;
    value->pad2 = 0;
    value->pad3 = 0;
    value->pad4 = 0;
    value->pad5 = 0;
    trio_reset( &value->inner );
}

static SCHEMA_UNUSED void schema_example_trio_straddle_reset_raw_( void * storage ) { trio_straddle_reset( (TrioStraddle *) storage ); }

extern const TableTypeInfo schema_example_trio_straddle_info_;
static SCHEMA_UNUSED const TableTypeInfo * trio_straddle_table_type(void) { return &schema_example_trio_straddle_info_; }
static SCHEMA_UNUSED void vec2_reset( Vec2 * value )
{
    value->x = 0.0;
    value->y = 0.0;
}

static SCHEMA_UNUSED void schema_example_vec2_reset_raw_( void * storage ) { vec2_reset( (Vec2 *) storage ); }

extern const TableTypeInfo schema_example_vec2_info_;
static SCHEMA_UNUSED const TableTypeInfo * vec2_table_type(void) { return &schema_example_vec2_info_; }
static SCHEMA_UNUSED void vec3_reset( Vec3 * value )
{
    value->x = 0.0;
    value->y = 0.0;
    value->z = 0.0;
}

static SCHEMA_UNUSED void schema_example_vec3_reset_raw_( void * storage ) { vec3_reset( (Vec3 *) storage ); }

extern const TableTypeInfo schema_example_vec3_info_;
static SCHEMA_UNUSED const TableTypeInfo * vec3_table_type(void) { return &schema_example_vec3_info_; }
static SCHEMA_UNUSED void w1_reset( W1 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w1_reset_raw_( void * storage ) { w1_reset( (W1 *) storage ); }

extern const TableTypeInfo schema_example_w1_info_;
static SCHEMA_UNUSED const TableTypeInfo * w1_table_type(void) { return &schema_example_w1_info_; }
static SCHEMA_UNUSED void w13_reset( W13 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w13_reset_raw_( void * storage ) { w13_reset( (W13 *) storage ); }

extern const TableTypeInfo schema_example_w13_info_;
static SCHEMA_UNUSED const TableTypeInfo * w13_table_type(void) { return &schema_example_w13_info_; }
static SCHEMA_UNUSED void w17_reset( W17 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w17_reset_raw_( void * storage ) { w17_reset( (W17 *) storage ); }

extern const TableTypeInfo schema_example_w17_info_;
static SCHEMA_UNUSED const TableTypeInfo * w17_table_type(void) { return &schema_example_w17_info_; }
static SCHEMA_UNUSED void w26_reset( W26 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w26_reset_raw_( void * storage ) { w26_reset( (W26 *) storage ); }

extern const TableTypeInfo schema_example_w26_info_;
static SCHEMA_UNUSED const TableTypeInfo * w26_table_type(void) { return &schema_example_w26_info_; }
static SCHEMA_UNUSED void w50_reset( W50 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w50_reset_raw_( void * storage ) { w50_reset( (W50 *) storage ); }

extern const TableTypeInfo schema_example_w50_info_;
static SCHEMA_UNUSED const TableTypeInfo * w50_table_type(void) { return &schema_example_w50_info_; }
static SCHEMA_UNUSED void w52_reset( W52 * value )
{
    memset( value->items, 0, sizeof( value->items ) );
    value->items_count = 0;
}

static SCHEMA_UNUSED void schema_example_w52_reset_raw_( void * storage ) { w52_reset( (W52 *) storage ); }

extern const TableTypeInfo schema_example_w52_info_;
static SCHEMA_UNUSED const TableTypeInfo * w52_table_type(void) { return &schema_example_w52_info_; }
static SCHEMA_UNUSED void wide_reset( Wide * value )
{
    value->w = 0;
}

static SCHEMA_UNUSED void schema_example_wide_reset_raw_( void * storage ) { wide_reset( (Wide *) storage ); }

extern const TableTypeInfo schema_example_wide_info_;
static SCHEMA_UNUSED const TableTypeInfo * wide_table_type(void) { return &schema_example_wide_info_; }
const UnitViewInfo * schema_example_unit_view_(void);
static SCHEMA_UNUSED const UnitViewInfo * unit_view(void) { return schema_example_unit_view_(); }
#ifdef __cplusplus
}
#endif
#endif
