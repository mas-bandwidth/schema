/* Code generated by the schema compiler from Degenerate.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0x91a8e85156dfe2b1 */

#ifndef SCHEMA_EXAMPLE_DEGENERATEWIRE_H
#define SCHEMA_EXAMPLE_DEGENERATEWIRE_H

#include "Degenerate.h"
#include "serialize.h"

#ifndef SCHEMA_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define SCHEMA_UNUSED __attribute__((unused))
#else
#define SCHEMA_UNUSED
#endif
#endif

#ifdef __cplusplus
extern "C" {
#endif

/* Every write_x/read_x returns 1 on success, 0 on failure — the stream
   latches the error, so a caller may check once at the end of a message.
   Reads REFUSE out-of-range values, never clamp. A tag is validated BEFORE
   it rides, and a read zero-establishes the selected arm before decoding
   it (SPEC §4.8, §5). */

#ifndef SCHEMA_C_SPINE_INLINE_DEFINED
#define SCHEMA_C_SPINE_INLINE_DEFINED
/* SCHEMA_C_READ_INLINE / SCHEMA_C_WRITE_INLINE — how every generated wire
   function is spelled, beside `static SCHEMA_UNUSED`: an inlining DEMAND
   (always_inline / __forceinline), the serialize family's remedy for LLVM
   refusing the header-static per-message entries into small-message call
   sites at marginal cost over the hot-callsite inline threshold. Measured
   and shipped; compilers with neither spelling get plain `static`.
   Branch-weight hints are NOT the fix here — measured in this family to
   invite the machine outliner into the hot bodies. Do not add them. */
#if defined( _MSC_VER )
#define SCHEMA_C_READ_INLINE __forceinline
#define SCHEMA_C_WRITE_INLINE __forceinline
#elif defined( __GNUC__ ) || defined( __clang__ )
#define SCHEMA_C_READ_INLINE inline __attribute__(( always_inline ))
#define SCHEMA_C_WRITE_INLINE inline __attribute__(( always_inline ))
#else
#define SCHEMA_C_READ_INLINE
#define SCHEMA_C_WRITE_INLINE
#endif
#endif /* SCHEMA_C_SPINE_INLINE_DEFINED */

/* Writes Vec2. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_vec2( serialize_write_stream_t * stream, const Vec2 * value )
{
    if ( !serialize_write_double( stream, value->x ) )
    {
        return 0;
    }
    if ( !serialize_write_double( stream, value->y ) )
    {
        return 0;
    }
    return 1;
}

/* Reads Vec2. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_vec2( serialize_read_stream_t * stream, Vec2 * value )
{
    if ( !serialize_read_double( stream, &value->x ) )
    {
        return 0;
    }
    if ( !serialize_read_double( stream, &value->y ) )
    {
        return 0;
    }
    return 1;
}

/* Writes SpanF64. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_f64( serialize_write_stream_t * stream, const SpanF64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_double( stream, value->values[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanF64. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_f64( serialize_read_stream_t * stream, SpanF64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_read_double( stream, &value->values[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Writes SpanU64. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_u64( serialize_write_stream_t * stream, const SpanU64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_uint64( stream, (serialize_uint64_t) value->values[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanU64. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_u64( serialize_read_stream_t * stream, SpanU64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            {
                serialize_uint64_t raw = 0;
                if ( !serialize_read_uint64( stream, &raw ) )
                {
                    return 0;
                }
                value->values[i] = (uint64_t) raw;
            }
        }
    }
    return 1;
}

/* Writes SpanI64. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_i64( serialize_write_stream_t * stream, const SpanI64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_uint64( stream, (serialize_uint64_t) value->values[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanI64. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_i64( serialize_read_stream_t * stream, SpanI64 * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            {
                serialize_uint64_t raw = 0;
                if ( !serialize_read_uint64( stream, &raw ) )
                {
                    return 0;
                }
                value->values[i] = (int64_t) raw;
            }
        }
    }
    return 1;
}

/* Writes SpanOne. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_one( serialize_write_stream_t * stream, const SpanOne * value )
{
    {
        int32_t i;
        for ( i = 0; i < 1; i++ )
        {
            if ( !serialize_write_uint64( stream, (serialize_uint64_t) value->values[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanOne. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_one( serialize_read_stream_t * stream, SpanOne * value )
{
    {
        int32_t i;
        for ( i = 0; i < 1; i++ )
        {
            {
                serialize_uint64_t raw = 0;
                if ( !serialize_read_uint64( stream, &raw ) )
                {
                    return 0;
                }
                value->values[i] = (uint64_t) raw;
            }
        }
    }
    return 1;
}

/* Writes SpanChunk. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_chunk( serialize_write_stream_t * stream, const SpanChunk * value )
{
    {
        int32_t i;
        for ( i = 0; i < 4; i++ )
        {
            if ( !serialize_write_bits( stream, (serialize_uint32_t) value->values[i], 16 ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanChunk. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_chunk( serialize_read_stream_t * stream, SpanChunk * value )
{
    {
        int32_t i;
        for ( i = 0; i < 4; i++ )
        {
            {
                serialize_uint32_t raw = 0;
                if ( !serialize_read_bits( stream, &raw, 16 ) )
                {
                    return 0;
                }
                value->values[i] = (uint16_t) raw;
            }
        }
    }
    return 1;
}

/* Writes SpanTail. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_tail( serialize_write_stream_t * stream, const SpanTail * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_double( stream, value->values[i] ) )
            {
                return 0;
            }
        }
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->tail, 32 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads SpanTail. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_tail( serialize_read_stream_t * stream, SpanTail * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_read_double( stream, &value->values[i] ) )
            {
                return 0;
            }
        }
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 32 ) )
        {
            return 0;
        }
        value->tail = (uint32_t) raw;
    }
    return 1;
}

/* Writes SpanTwice. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_span_twice( serialize_write_stream_t * stream, const SpanTwice * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_double( stream, value->a[i] ) )
            {
                return 0;
            }
        }
    }
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_write_double( stream, value->b[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Reads SpanTwice. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_span_twice( serialize_read_stream_t * stream, SpanTwice * value )
{
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_read_double( stream, &value->a[i] ) )
            {
                return 0;
            }
        }
    }
    {
        int32_t i;
        for ( i = 0; i < 2; i++ )
        {
            if ( !serialize_read_double( stream, &value->b[i] ) )
            {
                return 0;
            }
        }
    }
    return 1;
}

/* Writes Trio. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_trio( serialize_write_stream_t * stream, const Trio * value )
{
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->a, 20 ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->b, 20 ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->c, 24 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads Trio. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_trio( serialize_read_stream_t * stream, Trio * value )
{
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 20 ) )
        {
            return 0;
        }
        value->a = (uint32_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 20 ) )
        {
            return 0;
        }
        value->b = (uint32_t) raw;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 24 ) )
        {
            return 0;
        }
        value->c = (uint32_t) raw;
    }
    return 1;
}

/* Writes TrioSole. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_trio_sole( serialize_write_stream_t * stream, const TrioSole * value )
{
    if ( !write_trio( stream, &value->inner ) )
    {
        return 0;
    }
    return 1;
}

/* Reads TrioSole. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_trio_sole( serialize_read_stream_t * stream, TrioSole * value )
{
    if ( !read_trio( stream, &value->inner ) )
    {
        return 0;
    }
    return 1;
}

/* Writes TrioFirst. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_trio_first( serialize_write_stream_t * stream, const TrioFirst * value )
{
    if ( !write_trio( stream, &value->inner ) )
    {
        return 0;
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->trailer, 16 ) )
    {
        return 0;
    }
    return 1;
}

/* Reads TrioFirst. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_trio_first( serialize_read_stream_t * stream, TrioFirst * value )
{
    if ( !read_trio( stream, &value->inner ) )
    {
        return 0;
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 16 ) )
        {
            return 0;
        }
        value->trailer = (uint32_t) raw;
    }
    return 1;
}

/* Writes TrioStraddle. */
static SCHEMA_UNUSED SCHEMA_C_WRITE_INLINE int write_trio_straddle( serialize_write_stream_t * stream, const TrioStraddle * value )
{
    {
        serialize_uint64_t bits_value = (serialize_uint64_t) value->pad0;
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value & 0xFFFFFFFFu ), 32 ) )
        {
            return 0;
        }
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value >> 32 ), 32 ) )
        {
            return 0;
        }
    }
    {
        serialize_uint64_t bits_value = (serialize_uint64_t) value->pad1;
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value & 0xFFFFFFFFu ), 32 ) )
        {
            return 0;
        }
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value >> 32 ), 32 ) )
        {
            return 0;
        }
    }
    {
        serialize_uint64_t bits_value = (serialize_uint64_t) value->pad2;
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value & 0xFFFFFFFFu ), 32 ) )
        {
            return 0;
        }
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value >> 32 ), 32 ) )
        {
            return 0;
        }
    }
    {
        serialize_uint64_t bits_value = (serialize_uint64_t) value->pad3;
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value & 0xFFFFFFFFu ), 32 ) )
        {
            return 0;
        }
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value >> 32 ), 32 ) )
        {
            return 0;
        }
    }
    {
        serialize_uint64_t bits_value = (serialize_uint64_t) value->pad4;
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value & 0xFFFFFFFFu ), 32 ) )
        {
            return 0;
        }
        if ( !serialize_write_bits( stream, (serialize_uint32_t) ( bits_value >> 32 ), 32 ) )
        {
            return 0;
        }
    }
    if ( !serialize_write_bits( stream, (serialize_uint32_t) value->pad5, 24 ) )
    {
        return 0;
    }
    if ( !write_trio( stream, &value->inner ) )
    {
        return 0;
    }
    return 1;
}

/* Reads TrioStraddle. */
static SCHEMA_UNUSED SCHEMA_C_READ_INLINE int read_trio_straddle( serialize_read_stream_t * stream, TrioStraddle * value )
{
    {
        serialize_uint32_t lo = 0;
        serialize_uint32_t hi = 0;
        if ( !serialize_read_bits( stream, &lo, 32 ) )
        {
            return 0;
        }
        if ( !serialize_read_bits( stream, &hi, 32 ) )
        {
            return 0;
        }
        value->pad0 = (uint64_t) ( (serialize_uint64_t) lo | ( ( (serialize_uint64_t) hi ) << 32 ) );
    }
    {
        serialize_uint32_t lo = 0;
        serialize_uint32_t hi = 0;
        if ( !serialize_read_bits( stream, &lo, 32 ) )
        {
            return 0;
        }
        if ( !serialize_read_bits( stream, &hi, 32 ) )
        {
            return 0;
        }
        value->pad1 = (uint64_t) ( (serialize_uint64_t) lo | ( ( (serialize_uint64_t) hi ) << 32 ) );
    }
    {
        serialize_uint32_t lo = 0;
        serialize_uint32_t hi = 0;
        if ( !serialize_read_bits( stream, &lo, 32 ) )
        {
            return 0;
        }
        if ( !serialize_read_bits( stream, &hi, 32 ) )
        {
            return 0;
        }
        value->pad2 = (uint64_t) ( (serialize_uint64_t) lo | ( ( (serialize_uint64_t) hi ) << 32 ) );
    }
    {
        serialize_uint32_t lo = 0;
        serialize_uint32_t hi = 0;
        if ( !serialize_read_bits( stream, &lo, 32 ) )
        {
            return 0;
        }
        if ( !serialize_read_bits( stream, &hi, 32 ) )
        {
            return 0;
        }
        value->pad3 = (uint64_t) ( (serialize_uint64_t) lo | ( ( (serialize_uint64_t) hi ) << 32 ) );
    }
    {
        serialize_uint32_t lo = 0;
        serialize_uint32_t hi = 0;
        if ( !serialize_read_bits( stream, &lo, 32 ) )
        {
            return 0;
        }
        if ( !serialize_read_bits( stream, &hi, 32 ) )
        {
            return 0;
        }
        value->pad4 = (uint64_t) ( (serialize_uint64_t) lo | ( ( (serialize_uint64_t) hi ) << 32 ) );
    }
    {
        serialize_uint32_t raw = 0;
        if ( !serialize_read_bits( stream, &raw, 24 ) )
        {
            return 0;
        }
        value->pad5 = (uint32_t) raw;
    }
    if ( !read_trio( stream, &value->inner ) )
    {
        return 0;
    }
    return 1;
}

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_DEGENERATEWIRE_H */
