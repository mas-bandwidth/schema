/* Generated by the schema compiler from Types.schema. DO NOT EDIT.
   SPDX-License-Identifier: NONE — this generated output is yours, under terms of
   your choice. See the LICENSE exception in the schema compiler; the compiler is
   AGPL-3.0, its output is not.
   package example — protocol id 0x14cc156665b9f146
   The TABLE wire (evolution-tolerant). */

#ifndef SCHEMA_EXAMPLE_TYPESTABLE_H
#define SCHEMA_EXAMPLE_TYPESTABLE_H

#include "Types.h"
#include "TableRuntime.h"

#ifdef __cplusplus
extern "C" {
#endif

/* Appends Vec3's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_vec3_into( table_writer_t * w, const Vec3 * value )
{
    if ( value->x != 0 )
    {
        table_w_u16( w, 0xad8b ); /* x */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->x, 8 );
            table_w_u64( w, bits );
        }
    }
    if ( value->y != 0 )
    {
        table_w_u16( w, 0xb2f8 ); /* y */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->y, 8 );
            table_w_u64( w, bits );
        }
    }
    if ( value->z != 0 )
    {
        table_w_u16( w, 0xaca1 ); /* z */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->z, 8 );
            table_w_u64( w, bits );
        }
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes Vec3's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask VEC3_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_vec3( void * buffer, int bytes, const Vec3 * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_vec3_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_vec3_from( table_reader_t * r, Vec3 * value, table_report_t * report )
{
    memset( value, 0, sizeof( *value ) ); /* prefill (all-zero), then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0xad8b: /* x */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->x = raw;
                    }
                }
                break;
            }
            case 0xb2f8: /* y */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->y = raw;
                    }
                }
                break;
            }
            case 0xaca1: /* z */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->z = raw;
                    }
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_vec3( const void * buffer, int bytes, Vec3 * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_vec3_from( &r, value, report );
}

/* Appends Quat's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_quat_into( table_writer_t * w, const Quat * value )
{
    if ( value->x != 0 )
    {
        table_w_u16( w, 0xad8b ); /* x */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->x, 8 );
            table_w_u64( w, bits );
        }
    }
    if ( value->y != 0 )
    {
        table_w_u16( w, 0xb2f8 ); /* y */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->y, 8 );
            table_w_u64( w, bits );
        }
    }
    if ( value->z != 0 )
    {
        table_w_u16( w, 0xaca1 ); /* z */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->z, 8 );
            table_w_u64( w, bits );
        }
    }
    if ( value->w != 0 )
    {
        table_w_u16( w, 0xcd3a ); /* w */
        table_w_u8( w, 11 );
        {
            serialize_uint64_t bits;
            memcpy( &bits, &value->w, 8 );
            table_w_u64( w, bits );
        }
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes Quat's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask QUAT_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_quat( void * buffer, int bytes, const Quat * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_quat_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_quat_from( table_reader_t * r, Quat * value, table_report_t * report )
{
    memset( value, 0, sizeof( *value ) ); /* prefill (all-zero), then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0xad8b: /* x */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->x = raw;
                    }
                }
                break;
            }
            case 0xb2f8: /* y */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->y = raw;
                    }
                }
                break;
            }
            case 0xaca1: /* z */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->z = raw;
                    }
                }
                break;
            }
            case 0xcd3a: /* w */
            {
                if ( kind != 11 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t bits = table_r_u64( r );
                        double raw;
                        memcpy( &raw, &bits, 8 );
                        value->w = raw;
                    }
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_quat( const void * buffer, int bytes, Quat * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_quat_from( &r, value, report );
}

/* Appends RigidBody's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_rigid_body_into( table_writer_t * w, const RigidBody * value )
{
    {
        int field_at = w->length;
        int len_at;
        table_w_u16( w, 0xdd45 ); /* position */
        table_w_u8( w, 13 );
        len_at = w->length;
        table_w_u32( w, 0 );
        table_write_vec3_into( w, &value->position );
        if ( w->length - len_at - 4 <= 2 )
        {
            w->length = field_at; /* all-default nested elides */
        }
        else
        {
            table_w_patch32( w, len_at, (serialize_uint32_t) ( w->length - len_at - 4 ) );
        }
    }
    {
        int field_at = w->length;
        int len_at;
        table_w_u16( w, 0x7964 ); /* orientation */
        table_w_u8( w, 13 );
        len_at = w->length;
        table_w_u32( w, 0 );
        table_write_quat_into( w, &value->orientation );
        if ( w->length - len_at - 4 <= 2 )
        {
            w->length = field_at; /* all-default nested elides */
        }
        else
        {
            table_w_patch32( w, len_at, (serialize_uint32_t) ( w->length - len_at - 4 ) );
        }
    }
    if ( value->at_rest != 0 )
    {
        table_w_u16( w, 0xf9eb ); /* at_rest */
        table_w_u8( w, 1 );
        table_w_u8( w, value->at_rest ? 1 : 0 );
    }
    if ( !value->at_rest )
    {
        {
            int field_at = w->length;
            int len_at;
            table_w_u16( w, 0x0637 ); /* linear_velocity */
            table_w_u8( w, 13 );
            len_at = w->length;
            table_w_u32( w, 0 );
            table_write_vec3_into( w, &value->linear_velocity );
            if ( w->length - len_at - 4 <= 2 )
            {
                w->length = field_at; /* all-default nested elides */
            }
            else
            {
                table_w_patch32( w, len_at, (serialize_uint32_t) ( w->length - len_at - 4 ) );
            }
        }
    }
    if ( !value->at_rest )
    {
        {
            int field_at = w->length;
            int len_at;
            table_w_u16( w, 0x01a7 ); /* angular_velocity */
            table_w_u8( w, 13 );
            len_at = w->length;
            table_w_u32( w, 0 );
            table_write_vec3_into( w, &value->angular_velocity );
            if ( w->length - len_at - 4 <= 2 )
            {
                w->length = field_at; /* all-default nested elides */
            }
            else
            {
                table_w_patch32( w, len_at, (serialize_uint32_t) ( w->length - len_at - 4 ) );
            }
        }
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes RigidBody's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask RIGID_BODY_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_rigid_body( void * buffer, int bytes, const RigidBody * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_rigid_body_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_rigid_body_from( table_reader_t * r, RigidBody * value, table_report_t * report )
{
    memset( value, 0, sizeof( *value ) ); /* prefill (all-zero), then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0xdd45: /* position */
            {
                if ( kind != 13 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    int body_len;
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    body_len = (int) table_r_u32( r );
                    if ( !table_r_has( r, body_len ) ) { report->malformed = 1; return 0; }
                    {
                        table_reader_t sub;
                        sub.data = r->data + r->off;
                        sub.length = body_len;
                        sub.off = 0;
                        table_read_vec3_from( &sub, &value->position, report );
                    }
                    r->off += body_len;
                }
                break;
            }
            case 0x7964: /* orientation */
            {
                if ( kind != 13 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    int body_len;
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    body_len = (int) table_r_u32( r );
                    if ( !table_r_has( r, body_len ) ) { report->malformed = 1; return 0; }
                    {
                        table_reader_t sub;
                        sub.data = r->data + r->off;
                        sub.length = body_len;
                        sub.off = 0;
                        table_read_quat_from( &sub, &value->orientation, report );
                    }
                    r->off += body_len;
                }
                break;
            }
            case 0xf9eb: /* at_rest */
            {
                if ( kind != 1 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
                    value->at_rest = table_r_u8( r ) != 0;
                }
                break;
            }
            case 0x0637: /* linear_velocity */
            {
                if ( kind != 13 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    int body_len;
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    body_len = (int) table_r_u32( r );
                    if ( !table_r_has( r, body_len ) ) { report->malformed = 1; return 0; }
                    {
                        table_reader_t sub;
                        sub.data = r->data + r->off;
                        sub.length = body_len;
                        sub.off = 0;
                        table_read_vec3_from( &sub, &value->linear_velocity, report );
                    }
                    r->off += body_len;
                }
                break;
            }
            case 0x01a7: /* angular_velocity */
            {
                if ( kind != 13 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    int body_len;
                    if ( !table_r_has( r, 4 ) ) { report->malformed = 1; return 0; }
                    body_len = (int) table_r_u32( r );
                    if ( !table_r_has( r, body_len ) ) { report->malformed = 1; return 0; }
                    {
                        table_reader_t sub;
                        sub.data = r->data + r->off;
                        sub.length = body_len;
                        sub.off = 0;
                        table_read_vec3_from( &sub, &value->angular_velocity, report );
                    }
                    r->off += body_len;
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_rigid_body( const void * buffer, int bytes, RigidBody * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_rigid_body_from( &r, value, report );
}

/* Appends ExtremeRow's table-wire encoding to the writer. */
static SCHEMA_UNUSED void table_write_extreme_row_into( table_writer_t * w, const ExtremeRow * value )
{
    if ( value->clamped_floor != 0 )
    {
        table_w_u16( w, 0xba76 ); /* clamped_floor */
        table_w_u8( w, 5 );
        table_w_u64( w, (serialize_uint64_t) value->clamped_floor );
    }
    if ( value->clamped_ceiling != 0 )
    {
        table_w_u16( w, 0x7a83 ); /* clamped_ceiling */
        table_w_u8( w, 9 );
        table_w_u64( w, (serialize_uint64_t) value->clamped_ceiling );
    }
    if ( value->floor_def != ( -9223372036854775807LL - 1 ) )
    {
        table_w_u16( w, 0x1590 ); /* floor_def */
        table_w_u8( w, 5 );
        table_w_u64( w, (serialize_uint64_t) value->floor_def );
    }
    table_w_u16( w, 0 ); /* terminator */
}

/* Writes ExtremeRow's table-wire encoding into buffer, returning the byte count,
   or -1 if the buffer was too small. Ask EXTREME_ROW_TABLE_MAX_BYTES for a size
   that always fits. */
static SCHEMA_UNUSED int table_write_extreme_row( void * buffer, int bytes, const ExtremeRow * value )
{
    table_writer_t w;
    w.data = (serialize_uint8_t *) buffer;
    w.capacity = bytes;
    w.length = 0;
    w.overflow = 0;
    table_write_extreme_row_into( &w, value );
    return w.overflow ? -1 : w.length;
}

/* Decodes a table-wire buffer under the permissive contract: declared
   defaults prefill, known fields overlay, unknown ids and kind mismatches
   skip and count, out-of-range values clamp and count. 0 means malformed —
   the partial decode up to that point is kept. */
static SCHEMA_UNUSED int table_read_extreme_row_from( table_reader_t * r, ExtremeRow * value, table_report_t * report )
{
    *value = new_extreme_row(); /* prefill declared defaults, then overlay */
    for ( ;; )
    {
        serialize_uint16_t field_id;
        serialize_uint8_t kind;
        if ( !table_r_has( r, 2 ) ) { report->malformed = 1; return 0; }
        field_id = table_r_u16( r );
        if ( field_id == 0 ) { return 1; }
        if ( !table_r_has( r, 1 ) ) { report->malformed = 1; return 0; }
        kind = table_r_u8( r );
        switch ( field_id )
        {
            case 0xba76: /* clamped_floor */
            {
                if ( kind != 5 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_int64_t raw = (serialize_int64_t) table_r_u64( r );
                        {
                            serialize_int64_t v = (serialize_int64_t) raw;
                            if ( v > 100LL ) { v = 100LL; report->clamped++; }
                            value->clamped_floor = (int64_t) v;
                        }
                    }
                }
                break;
            }
            case 0x7a83: /* clamped_ceiling */
            {
                if ( kind != 9 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_uint64_t raw = table_r_u64( r );
                        {
                            serialize_uint64_t v = (serialize_uint64_t) raw;
                            if ( v < 1ULL ) { v = 1ULL; report->clamped++; }
                            if ( v > 18446744073709551614ULL ) { v = 18446744073709551614ULL; report->clamped++; }
                            value->clamped_ceiling = (uint64_t) v;
                        }
                    }
                }
                break;
            }
            case 0x1590: /* floor_def */
            {
                if ( kind != 5 )
                {
                    report->kind_mismatch++;
                    if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                }
                else
                {
                    if ( !table_r_has( r, 8 ) ) { report->malformed = 1; return 0; }
                    {
                        serialize_int64_t raw = (serialize_int64_t) table_r_u64( r );
                        value->floor_def = (int64_t) raw;
                    }
                }
                break;
            }
            default:
            {
                report->unknown++;
                if ( !table_r_skip( r, kind ) ) { report->malformed = 1; return 0; }
                break;
            }
        }
    }
}

/* Decodes a table-wire buffer into value. */
static SCHEMA_UNUSED int table_read_extreme_row( const void * buffer, int bytes, ExtremeRow * value, table_report_t * report )
{
    table_reader_t r;
    r.data = (const serialize_uint8_t *) buffer;
    r.length = bytes;
    r.off = 0;
    return table_read_extreme_row_from( &r, value, report );
}

/* ---- reflection descriptors (tables only) ---- */

static const table_field_info_t TABLE_TYPE_VEC3_FIELDS[] = {
    { "x", "float64", 0xad8b, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "y", "float64", 0xb2f8, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "z", "float64", 0xaca1, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
};

static const table_type_info_t TABLE_TYPE_VEC3 = {
    "Vec3",
    TABLE_TYPE_VEC3_FIELDS,
    3
};

/* Returns Vec3's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_vec3( void ) { return &TABLE_TYPE_VEC3; }

static const table_field_info_t TABLE_TYPE_QUAT_FIELDS[] = {
    { "x", "float64", 0xad8b, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "y", "float64", 0xb2f8, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "z", "float64", 0xaca1, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "w", "float64", 0xcd3a, 11, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
};

static const table_type_info_t TABLE_TYPE_QUAT = {
    "Quat",
    TABLE_TYPE_QUAT_FIELDS,
    4
};

/* Returns Quat's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_quat( void ) { return &TABLE_TYPE_QUAT; }

static const table_field_info_t TABLE_TYPE_RIGID_BODY_FIELDS[] = {
    { "position", "Vec3", 0xdd45, 13, 0, 0, 0, &TABLE_TYPE_VEC3, 0, 0.0, 0.0, -1, NULL, "" },
    { "orientation", "Quat", 0x7964, 13, 0, 0, 0, &TABLE_TYPE_QUAT, 0, 0.0, 0.0, -1, NULL, "" },
    { "at_rest", "bool", 0xf9eb, 1, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
    { "linear_velocity", "Vec3", 0x0637, 13, 0, 0, 0, &TABLE_TYPE_VEC3, 0, 0.0, 0.0, -1, NULL, "!at_rest" },
    { "angular_velocity", "Vec3", 0x01a7, 13, 0, 0, 0, &TABLE_TYPE_VEC3, 0, 0.0, 0.0, -1, NULL, "!at_rest" },
};

static const table_type_info_t TABLE_TYPE_RIGID_BODY = {
    "RigidBody",
    TABLE_TYPE_RIGID_BODY_FIELDS,
    5
};

/* Returns RigidBody's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_rigid_body( void ) { return &TABLE_TYPE_RIGID_BODY; }

static const table_field_info_t TABLE_TYPE_EXTREME_ROW_FIELDS[] = {
    { "clamped_floor", "int64", 0xba76, 5, 0, 0, 0, NULL, 1, -9.223372036854776e+18, 100.0, -1, NULL, "" },
    { "clamped_ceiling", "uint64", 0x7a83, 9, 0, 0, 0, NULL, 1, 1.0, 1.8446744073709552e+19, -1, NULL, "" },
    { "floor_def", "int64", 0x1590, 5, 0, 0, 0, NULL, 0, 0.0, 0.0, -1, NULL, "" },
};

static const table_type_info_t TABLE_TYPE_EXTREME_ROW = {
    "ExtremeRow",
    TABLE_TYPE_EXTREME_ROW_FIELDS,
    3
};

/* Returns ExtremeRow's reflection descriptor — field names, wire ids/kinds,
   bounds, ranges, enum names and branch guards. Static data: no
   per-instance cost and no lazy initialization. */
static SCHEMA_UNUSED const table_type_info_t * table_type_extreme_row( void ) { return &TABLE_TYPE_EXTREME_ROW; }

#ifdef __cplusplus
}
#endif

#endif /* SCHEMA_EXAMPLE_TYPESTABLE_H */
