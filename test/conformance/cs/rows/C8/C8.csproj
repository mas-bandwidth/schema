<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>annotations</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <AssemblyName>cell-cs-c8</AssemblyName>
    <TablesGeneratedDir Condition="'$(TablesGeneratedDir)' == ''">../../../../../build/tables-generated-cs</TablesGeneratedDir>
    <RepoRoot Condition="'$(RepoRoot)' == ''">../../../../..</RepoRoot>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Program.cs" />
  </ItemGroup>

  <!-- The generated units are not in the tables-generated-cs stamp, so the row
       generates its own and globs them INSIDE the target: a static glob is
       evaluated before any target runs and would miss a fresh tree. -->
  <Target Name="GenerateC8Tables" BeforeTargets="CoreCompile">
    <Exec Command="$(RepoRoot)/bin/schema generate --lang cs --out $(TablesGeneratedDir)/vnew_bits_grow $(RepoRoot)/test/tables/VNEW_bits_grow.schema" />
    <Exec Command="$(RepoRoot)/bin/schema generate --lang cs --out $(TablesGeneratedDir)/vnew_fixed_i_grow $(RepoRoot)/test/tables/VNEW_fixed_I_grow.schema" />
    <ItemGroup>
      <Compile Include="$(TablesGeneratedDir)/vnew_bits_grow/*.cs" />
      <Compile Include="$(TablesGeneratedDir)/vnew_fixed_i_grow/*.cs" />
    </ItemGroup>
  </Target>

</Project>
