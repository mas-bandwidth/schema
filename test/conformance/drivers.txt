# THE DRIVER REGISTRY (test/conformance/README.md).
#
# One line per registered language:  <language> <command...>
#
# The harness appends the derived manifest, the surface and an output directory
# and runs it. A language is registered when its driver runs; a surface it does
# not `list` prints as ABSENT in the matrix, which is a missing FEATURE and not
# a failing test.
#
# Registering a port is this file plus one driver. Nothing else moves.

cpp  test/conformance/cpp/driver
cs   test/conformance/cs/driver
rust test/conformance/rust/driver
