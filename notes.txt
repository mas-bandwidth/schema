cell: elixir/W9 "prefill unwritten ranges only"
card: RESULT cell-elixir-w9 sha=205a85684bc0

LAW
docs/FIXED-FORM-ALGORITHM.md:308 (governing sentence):
  "**Prefill the bytes the plan does not write.** The plan compiler computes the
  UNWRITTEN RANGES — the destination bytes no entry covers — and only those take
  the declared defaults before the loop; **for the identity plan that list is
  EMPTY**, so the identity read pays no prefill (fix 15)."
docs/FIXED-FORM-ALGORITHM.md:318 (the run):
  "prefill the unwritten ranges of out"
docs/FIXED-FORM-ALGORITHM.md:309: "bytes no entry covers — and only those take the declared defaults before the loop"
docs/FIXED-FORM-ALGORITHM.md:310: "list is EMPTY", so the identity read pays no prefill (fix 15)
roadmap.sexp:3402: (:id "elixir/W9" :title "prefill unwritten ranges only" ... :reported-state "implemented-asserted")
matrix #876 row W9: elixir reads P.

PRODUCTION PATH
entrypoint: Tblp1.P1Fixed.chain_fixed_load/1 (generated, build/tables-generated-elixir/tblp1/P1Fixed.ex)
  -> chain_fixed_records -> select/lineage_lane (:identity lane for own hash)
  -> chain_fixed_run -> R.run(plan, R.detach(body, copy), @chain_prefill, report)  [P1Fixed.ex:315]
  -> FixedRuntime.run/4 -> step -> assemble(writes, prefill)
  -> assemble([{0, image}], prefill) when byte_size(image) == byte_size(prefill): returns the
     body sub-binary, prefill never touched  [FixedRuntime.ex:1801-1805]
  -> general assemble/writes -> splice fills only the gaps (unwritten ranges) from prefill
The load's per-record engine is the function the test drives (R.run/4) AND the whole
chain_fixed_load/1 is driven end to end in the third assertion.

TEST: test/conformance/elixir/rows/W9.exs, run: elixir test/conformance/elixir/rows/W9.exs

STEP 2 run, GREEN (exit 0):
ok   W9 identity plan is one whole-body run  --  the unwritten list is EMPTY
ok   W9 the identity read pays no prefill  --  a poisoned prefill never enters the image
ok   W9 production chain_fixed_load lands the raw body -- the prefill owned no byte
ok   W9 a non-identity plan prefills ONLY the unwritten range
ok   W9 the unwritten range takes the declared defaults, and the write wins its own
elixir/W9 prefill unwritten ranges only: green

STEP 3 negative control: one constant broken in the generated code — the identity
assemble clause's body in build/tables-generated-elixir/tblp1/FixedRuntime.ex
changed from `image` to `prefill` (the identity read pays a prefill). Recompiled the
ebin with the same elixirc line the Makefile uses. Run, RED (exit 1):
ok   W9 identity plan is one whole-body run  --  the unwritten list is EMPTY
FAIL W9 the identity read pays no prefill  --  a poisoned prefill never enters the image
FAIL W9 production chain_fixed_load lands the raw body -- the prefill owned no byte
ok   W9 a non-identity plan prefills ONLY the unwritten range
ok   W9 the unwritten range takes the declared defaults, and the write wins its own
elixir/W9 prefill unwritten ranges only: RED
Restored the clause to `image`, rebuilt the ebin, run again GREEN (exit 0):
elixir/W9 prefill unwritten ranges only: green

GATES
preflight (make build/conformance-harness build-conformance-elixir): exit 0, last lines:
  PATH=".../dist/otp-29.0.5/bin:.../dist/elixir-1.20.4/bin:$PATH" elixirc -o build/elixir-tables-ebin \
      build/tables-generated-elixir/*/*.ex \
      test/conformance/elixir/driver_impl.ex
nova-tools gate: gofmt -l . (empty), go vet ./... (ok), go test ./internal/ci/ (ok 2.729s) — no Go package touched.
schema gate: make check (exit 0).
leg format: mix format --check-formatted test/conformance/elixir/rows/W9.exs (clean).

SCOPE
Touched file: test/conformance/elixir/rows/W9.exs only (STEP 4 PATHS). No driver, no
Makefile, no make/*.mk, no roadmap.sexp, no internal/codegen edit. Wiring rows/ into the
leg's runner is a separate card per leg, so the new case runs standalone by the card's own
RUN command and is not (yet) a make target/CI job — named here under (j).