RESULT: AUDIT java at b7ab66a8 rows=86 implemented-asserted=52 weak=14 owed=16 inapplicable=4

## corpus result (verbatim)
fixed form: the C++ reference's byte oracle is in build/fixedform-corpus
fixed form: what each file holds is in build/fixedform-corpus/manifest.txt (66 lines)

## gate result (verbatim) — make tables-java-fixedform JAVA=…/java JAVAC=…/javac, exit 0, no --- SKIP lines
java fixed form: the bytes are the C++ reference's, and the versioned path is the path

## versioning run (the leg's §5 gate, in ci-full.yml's versioning job; not wired into ci-fast's per-leg java case)
ok  	github.com/mas-bandwidth/schema/v2/internal/codegen/javatable	7.148s
tables Java versioning: §5 read both columns of every row against the C++ reference bytes

## exceptions
1. #876's exact definitions for F1–F12, E3/E5/E6/E8/E9, C8/C12/C13/C14, W1–W4/W6–W15 and P1–P4 are not recoverable from the packet or the repo (they live in the #876 issue matrix). Those 54 non-R rows were audited against the corresponding docs/FIXED-FORM-ALGORITHM.md sentences (§1.1, §3.1, §4.1–4.6, §5, §7 fixtures) with the java-leg evidence; Rowan should reconcile the item-id↔sentence mapping against #876's matrix.
2. E2 (unknown nested type) inapplicable is reader-judgement: §2.3 says "the record names E1 only"; the retiring sentence is §5.6's forward-reads line (FIXED-FORM-ALGORITHM.md:933), quoted in the row.
3. E4 and E7 inapplicable is reader-judgement: the retiring cause is §5.1 BASELINE's commit-time refusals (FAIL "kind changed" line 394; FAIL "variant removed | inserted | reordered | renamed" line 402), quoted in the rows.
4. The java leg's §5 versioning target (make tables-java-versioning) is not in ci-fast.yml's per-leg java gate (§6.2 lists only make tables-java-fixedform); it runs in ci-full.yml's versioning job. Both were run here and pass. R-item gate_run cites the versioning target.
5. The seven §2.7 owed rows are owed on java: writer_bound_count (R11), refuse_writes_nothing (R12), unknown_census and forged_ordinal_both_plans (R16's hostile half), cfloat_res_refine and cfloat_range_widen (R30), and fixed_I_grow_element. build/fixedform-corpus contains no hostile_*.bin / cfloat / band files.
6. R24 (text_ill_formed) appears nowhere in test/ or internal/ at this head; it rides #971 (unmerged). Marked owed, lane_state open-PR #971. C3/C4/C5's content-refusal half carries the same dependency.
7. R5 (the digest carrying every range/resolution/limit) is §5.8 row 10, a debt the reference owes; java cannot assert it and it is owed.
8. L1-L7: the seven §1.1 rule assertions live in Main.layoutValidation(), which §5.6 RETIRES (Main.java:969-970); only the aggregate layout_malformed (known hash, bytes differ) runs. Marked weak, not owed, because the assertions exist.
9. C6 is audited as R31's cell per §2.3 ("on the other seven legs it is R31's cell"), and marked weak to match R31.
10. R29 (band case) and R30 (compressed float) have no corpus and no probe on this leg — owed.
11. The packet's pre-existing reds (G1 19 dead negative controls, G2 Java arm neuter, G3 stale generated/ tree) are cross-cutting CI reds, not per-leg; they were not reproduced and not repaired. The two RED [Reference fixes pending, fix 2] lines (bool_byte_two, present_byte_two) belong to `make tables-fixedform` (cpp), not the java gate; the java gate printed no RED.
12. G5: neither java gate run printed a --- SKIP line (SCHEMA_SLOW=1 and SCHEMA_REQUIRE_CORPUS=1 were set), so no cell was marked weak because of a skip.
13. Lane state: every java item audited here is on the lane at b7ab66a8; only R24 (and C4's refusal half) depend on an open PR (#971).

## TSV (audit-java.tsv, 86 rows)

leg	item_id	status	evidence_path	assertion_or_counterexample	gate_run	lane_state	note
java	L1-L7	weak	test/java-fixedform/src/Main.java:828	"RULE: the entry count fits the layout length exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	layoutValidation() asserted all seven §1.1 rules but is RETIRED by §5.6 (Main.java:969); only the aggregate layout_malformed name runs (versioning harness)
java	F1	weak	internal/codegen/javatable/fixedversioning_test.go:750	"NEW-READS-OLD %s: unknown=%d, want 0: a lawful lineage names every field of every writer it selects"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	unknown counter implemented; only the lawful zero is asserted (§5.9 #30 withdrew the nonzero fixture)
java	F2	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:695	"NEW-READS-OLD: the appended %s = %s, want its declared default %s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	nested_append row: a whole nested type the writer does not carry lands its defaults
java	F3	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:759	"a width row widens once per entry per record (§5.4)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	int_widen/uint_widen/bits_grow rows (widen:true) land exactly via assertValuesLand
java	F4	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	float_widen row; f32 into f64 lands by value
java	F5	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:624	"mine[wireName(k, was)] = v"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	rename_without_was row pairs two spellings through the `was=` map (§5.9 #41)
java	F6	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:695	"NEW-READS-OLD: the appended %s = %s, want its declared default %s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	field_append row: an added field takes its declared default
java	F7	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:695	"NEW-READS-OLD: the appended %s = %s, want its declared default %s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	field_deprecate row: a dropped field is counted unknown and lands the reader's tail
java	F8	weak	internal/codegen/javatable/fixedversioning_test.go:753	"%s %s: kindMismatch=%d, want 0 (§5.9 #30)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	kind_mismatch is structurally zero on a lawful lineage; no fixture moves it (§5.9 #30)
java	F9	inapplicable	docs/FIXED-FORM-ALGORITHM.md:933	"the count clamp across bounds, the range clamp across versions, the remap of an unknown variant to `None`, the drop-and-count of an unknown field — all forward reads, now `layout_newer`"	-	merged-on-lane	narrower int leaves the declared default is a FORWARD read, retired to layout_newer (§5.6)
java	F10	implemented-asserted	test/java-fixedform/src/Main.java:375	"arm text: a COMPILED plan lands a text field under the SECOND arm"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	arg and meta are two lanes (fix 12); armTextUnderASecondArm runs in the gate
java	F11	implemented-asserted	test/java-fixedform/src/Main.java:91	"fx1.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	bytes(6) rides the round-trip; the array row is pinned by TestBytesNDstRowIsAnArray (fixedform_test.go:21)
java	F12	implemented-asserted	test/java-fixedform/src/Main.java:172	"p3: an absent optional this port writes carries the template's zeros, not the caller's storage"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	slack/prefill: the template zeros, holes-only prefill (TestFixedLoadPrefillsHolesOnly)
java	E2	inapplicable	docs/FIXED-FORM-ALGORITHM.md:933	"the count clamp across bounds, the range clamp across versions, the remap of an unknown variant to `None`, the drop-and-count of an unknown field — all forward reads, now `layout_newer`"	-	merged-on-lane	recast-away by §5.6 (unknown nested type is a forward read); reader-judgement, flagged in exceptions
java	E3	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	enum_append row: ordinals remapped by name through the plan's table
java	E4	inapplicable	docs/FIXED-FORM-ALGORITHM.md:394	"LADDER(a, b) or kind(a) == kind(b)     or FAIL \"kind changed\""	-	merged-on-lane	recast: §5.1 BASELINE refuses a kind change at commit, so the read-side pair is unreachable
java	E5	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	union_append/union_arm_payload_widen rows: arms appended and payloads landed
java	E6	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	flags_append row (green on all nine in #876)
java	E7	inapplicable	docs/FIXED-FORM-ALGORITHM.md:402	"enum:          variants(a) prefix of variants(b) by name   or FAIL \"variant removed | inserted | reordered | renamed\""	-	merged-on-lane	recast: §5.1 BASELINE refuses a mid-list insert at commit (S1's refusal; the read-side case is unreachable)
java	E8	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	keyed_array_enum_append row: keyed slots slide by key, appended key takes its default (green on all nine)
java	E9	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:679	"if strings.HasSuffix(k, \"Present\") && have == \"true\""	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	optional_add row: T into ?T lands present==1 and the payload exact (bill §12.8)
java	C1	owed	-	-	-	merged-on-lane	count clamp v<0 hostile half (forged count) has no running assertion; the writer_bound_count probe is owed (§2.7)
java	C2	owed	-	-	-	merged-on-lane	count clamp v>Max hostile half has no running assertion; writer_bound_count probe owed
java	C3	owed	-	-	-	merged-on-lane	text length clamp (used units) has no forged-length assertion; content refusal text_ill_formed rides #971 (unmerged)
java	C4	owed	-	-	-	open-PR #971	text content refuses by name (text_ill_formed) appears nowhere at the head; rides #971
java	C5	owed	-	-	-	merged-on-lane	wide text units clamp has no forged-length assertion; the wide-text flavour oracle is owed (§5.8 row 7)
java	C6	weak	internal/codegen/javatable/fixedruntime.go:429	"for (int i = 0; i < w; i++) { v |= ((long) (src[at + i] & 0xFF)) << (8 * i); }"	-	merged-on-lane	the 64-bit ordinal temporary is R31's cell here; the forged-ordinal probe is owed (§2.7)
java	C7	owed	-	-	-	merged-on-lane	ranged-scalar clamp hostile half has no running assertion; cfloat/range probes owed
java	C8	owed	-	-	-	merged-on-lane	no running assertion at this head for this bounds-pass clamp
java	C9	implemented-asserted	test/java-fixedform/src/Main.java:400	"tag past the last arm: the identity path counts ONE clamped"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	forged tag past the arm count lands None and counts one clamped, both plans
java	C10	implemented-asserted	test/java-fixedform/src/Main.java:424	"ordinal past the last variant: the identity path counts ONE clamped"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	forged enum ordinal past the top lands None and counts one clamped, both plans (Main.java:430)
java	C12	owed	-	-	-	merged-on-lane	no running assertion at this head for this bounds-pass clamp
java	C13	owed	-	-	-	merged-on-lane	no running assertion at this head for this bounds-pass clamp
java	C14	owed	-	-	-	merged-on-lane	no running assertion at this head for this bounds-pass clamp
java	W1	implemented-asserted	test/java-fixedform/src/Main.java:91	"fx1.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	write header/hash/layout/records round-trips byte-identical to the reference
java	W2	implemented-asserted	test/java-fixedform/src/Main.java:109	"fx2.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	write round-trips byte-identical
java	W3	implemented-asserted	test/java-fixedform/src/Main.java:126	"p1.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	write round-trips byte-identical
java	W4	implemented-asserted	test/java-fixedform/src/Main.java:206	"keyed.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	keyed arrays nesting keyed arrays round-trip byte-identical
java	W5	implemented-asserted	test/java-fixedform/src/Main.java:91	"fx1.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	release-config proof: the gate runs -ea AND default (no -ea) and both land the same bytes (java.mk:289-290)
java	W6	implemented-asserted	test/java-fixedform/src/Main.java:231	"pack.bin: the bytes are the C++ reference's, exactly"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	counted arrays and enums write the reference's bytes
java	W7	implemented-asserted	test/java-fixedform/src/Main.java:122	"p1.bin: the text lands at its used length"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	text writes length units, never Max (fix 1)
java	W8	implemented-asserted	test/java-fixedform/src/Main.java:180	"p3: and the SAME payload PRESENT does put those bytes on the wire"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	union writes the tag and the taken arm only
java	W9	implemented-asserted	test/java-fixedform/src/Main.java:147	"p3.bin: an ABSENT optional's payload rides WHOLE, and behind a false flag it is the template's zeros (§3.4)"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	absent optional writes flag 0 and skips the payload store (fix 13)
java	W10	weak	internal/codegen/javatable/fixedruntime.go:1138	"if (report != null) { report.refuse(Reason.layoutRecordTooLarge); }"	-	merged-on-lane	hardened into R14 (layout_record_too_large); implemented in compile, no running forged-entry probe
java	W11	implemented-asserted	test/java-fixedform/src/Main.java:90	"fx1.bin: save fills what measure says"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	measure/save agreement on the write path
java	W12	implemented-asserted	test/java-fixedform/src/Main.java:84	"fx1.bin: its own layout is the identity plan, and a clean read moves no counter"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	zero-fill template; clean read moves no counter
java	W13	implemented-asserted	test/java-fixedform/src/Main.java:104	"fx2.bin: the scalars are the reference's"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	scalars land the reference's values on the identity path
java	W14	implemented-asserted	test/java-fixedform/src/Main.java:220	"pack: an enum rides its ORDINAL, from 1"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	enum ordinal and union tag ride their positions from 1
java	W15	implemented-asserted	test/java-fixedform/src/Main.java:195	"keyed: a `type` nested by value"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	nested tables/types inline without length or terminator
java	W16	weak	internal/codegen/javatable/fixedruntime.go:296	"public byte argw;"	-	merged-on-lane	extends to R28 (guard chain); outer-tag guard2 implemented, no running nested-union probe
java	P1	implemented-asserted	test/java-fixedform/src/Main.java:284	"self plan: a COMPILED plan lands exactly what the identity plan lands"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	compile() from a layout, run() against the record, byte-compare to identity
java	P2	implemented-asserted	test/java-fixedform/src/Main.java:262	"self plan: text, count and the keyed walk break the runs"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	coalesced runs and the split; a real plan, not one run
java	P3	weak	internal/codegen/javatable/fixedform_test.go:89	"load skips the hole loop; identity empty is the list, not a skip"	-	merged-on-lane	prefill-holes-only asserted in a Go unit test, not the §6.2 gate (runs under bare go test)
java	P4	implemented-asserted	test/java-fixedform/src/Main.java:367	"arm text: the SECOND arm is the selected one"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	guarded second half and the guard ordinal; arm under arm answers the outer tag (guard2)
java	R1	implemented-asserted	internal/codegen/javatable/fixedlineage.go:75	"entries = append(entries, own)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	lineage laid down oldest first, current last; exercised by every versioning probe
java	R2	implemented-asserted	internal/codegen/javatable/fixedlineage.go:59	"Record: 8 + fixedTypeBytes(st)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	record_bytes is 8+body, added once at the lineage entry
java	R3	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:812	"floor_below: n=%d reason=%s, want -1 and layoutUnsupported"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	TestFixedVersioningFloor: at floor reads, one below refuses layout_unsupported with the file's hash
java	R4	implemented-asserted	internal/codegen/javatable/fixedlineage.go:57	"Wire:   ir.TableFixedLayoutHash(layout, st)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	the hash is the compiler's function (never a private re-derivation); hash_identity/hash_unknown rows run it
java	R5	owed	-	-	-	merged-on-lane	the digest (every range/resolution/limit) is §5.8 row 10's debt the reference owes; java cannot assert it yet
java	R6	implemented-asserted	internal/codegen/javatable/fixedform.go:448	"a record past 65536 bytes is one no conforming reader decodes, so the table keeps form 1"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	ir.TableFixedFormRoots answers the ceiling; no lineage entry parsed for a past-ceiling table
java	R7	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:855	"hash_identity: the reader's own file does not read: n=%d refused=%v reason=%s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	the identity lane is an index comparison, never a recomputed hash
java	R8	implemented-asserted	test/java-fixedform/src/Main.java:680	"REFUSED BY NAME: a layout this build never locked is layout_newer"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	layout_newer carries the file's hash and nothing else (Main.java:681)
java	R9	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:888	"hash_known_bytes_differ: refused=%v reason=%s, want layoutMalformed"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	a known hash whose layout bytes differ is layout_malformed, one name for all seven
java	R10	implemented-asserted	test/java-fixedform/src/Main.java:718	"REFUSED BY NAME: a header hash no lineage entry holds"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	the stranger's-layout walk and header-hash recompute are retired; the header hash is taken as given
java	R11	owed	-	-	-	merged-on-lane	writer_bound_count probe (hostile_array_bounded_grow) is owed on this leg (§2.7)
java	R12	owed	-	-	-	merged-on-lane	refuse_writes_nothing probe (nolayout_nested_append) is owed on this leg (§2.7); the no_layout check before prefill is in the runtime (fixedform.go:1660)
java	R13	implemented-asserted	test/java-fixedform/src/Main.java:683	"a refusal by name never sets malformed too"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	REFUSE is total; also fixedversioning_test.go:609-611 (no counter moved)
java	R14	weak	internal/codegen/javatable/fixedruntime.go:1348	"final Reason why = (census.refused && census.reason == Reason.layoutRecordTooLarge)"	-	merged-on-lane	layout_record_too_large implemented; the forged-entry probe is owed (plan_too_large control retired, Main.java:732)
java	R15	implemented-asserted	test/java-fixedform/src/Main.java:959	"retired(\"textUnderAnArm\", ...)"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	the four forward-read clamps are retired to layout_newer; six families retired by §5.6 by name
java	R16	weak	internal/codegen/javatable/fixedversioning_test.go:759	"a width row widens once per entry per record (§5.4)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	counters asserted on lawful rows; the forged_ordinal_both_plans and unknown_census hostile probes are owed
java	R17	implemented-asserted	test/java-fixedform/src/Main.java:84	"fx1.bin: its own layout is the identity plan, and a clean read moves no counter"	make tables-java-fixedform -> "java fixed form: the bytes are the C++ reference's, and the versioned path is the path"	merged-on-lane	bool/present normalise to the language's own true and move no counter (bill §12.12)
java	R18	weak	internal/codegen/javatable/fixedruntime.go:488	"if (raw != 0 && Long.compareUnsigned(raw, n) <= 0) { v = remap[p.aux + (int) raw] & 0xFFFF; }"	-	merged-on-lane	a clamp that cannot fire is not emitted; the elision is shape, no running assertion of the elision
java	R19	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:757	"%s %s: clamped=%d, want 0: no clean row asserts a clamp (§5.4)"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	a clean NEW-READS-OLD of an append moves no counter (assertCounters)
java	R20	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:662	"NEW-READS-OLD: %s = %s, the writer wrote %s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	§21.2 landing rules: defaults, drop-and-count, exact widen, template zeros (assertValuesLand)
java	R21	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:583	"NEW-READS-OLD ... want %d records and no refusal"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	widenf is bit-exact; float_widen lands the exact value (NaN payloads included)
java	R22	weak	internal/codegen/javatable/fixedform.go:465	"fixedCollectTypes orders a root's closure so a nested type is emitted before the type that holds it"	-	merged-on-lane	closure is the compiler's (ir); a pointer/map/unbounded array in the closure is a compile refusal, not a java-leg runtime assertion
java	R23	implemented-asserted	internal/codegen/javatable/fixedruntime.go:387	"public long layoutHash;"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	layout_hash is the report's last member and zero on every other path (§5.9 #15, #19)
java	R24	owed	-	-	-	open-PR #971	text_ill_formed appears nowhere in test/ or internal/ at the head; rides #971 (unmerged)
java	R25	weak	internal/codegen/javatable/fixedruntime.go:1350	": Reason.planTooLarge;"	-	merged-on-lane	plan_too_large implemented; the one-entry control is retired (Main.java:732) and no running probe exercises it
java	R26	implemented-asserted	internal/codegen/javatable/fixedruntime.go:1309	"out[i] = new Plan(null, 0, null, 0, 0, Reason.layoutMalformed);"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	a lineage entry that would not build carries its own lane's name, never a throw (§5.9 #36)
java	R27	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:555	"oldFile := filepath.Join(corpus, \"old_\"+row.name+\".bin\")"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	the manifest/corpus is the value oracle every row reads to (§5.9 #32/#37)
java	R28	weak	internal/codegen/javatable/fixedruntime.go:442	"final int gw = guardWidth(p.argw);"	-	merged-on-lane	the outer-tag guard (guard2) is carried; the guard-chain pool (§5.8 row 6, extends W16) is not probed here
java	R29	owed	-	-	-	merged-on-lane	the band case (widening across the 65536 ceiling) has no corpus and no probe on this leg
java	R30	owed	-	-	-	merged-on-lane	compressed float has no oracle bytes (cfloat_res_refine, cfloat_range_widen owed, §2.7)
java	R31	weak	internal/codegen/javatable/fixedruntime.go:429	"for (int i = 0; i < w; i++) { v |= ((long) (src[at + i] & 0xFF)) << (8 * i); }"	-	merged-on-lane	full-width arg read through a long temp and the remap length word are present; the forged-ordinal/remap-past-255 probes are owed (§5.8 row 14)
java	R32	implemented-asserted	internal/codegen/javatable/fixedversioning_test.go:831	"floor_raise_live: yesterday's file must refuse: n=%d reason=%s"	make tables-java-versioning -> "ok github.com/mas-bandwidth/schema/v2/internal/codegen/javatable 7.225s"	merged-on-lane	retire is idempotent; the floor raised by one refuses yesterday's file by name (TestFixedVersioningFloor)
