# SHAPE-GATE.allow — this leg's exemptions from the shape gate (bench/tools/shapegate).
# The format, the rules and the shared tooling's entries: bench/SHAPE-GATE.allow.
# Paths are repository-relative.

names bench/tables/dart/table_main.dart 5 shape-blind leg: `FixedTable` at the FIXED form's two storage declarations and its generated import site (docs/SPEC-TABLES.md §3.4); no field, no pinned value, no wire size (callsite)
