[package]
name = "benchrust"
version = "0.0.0"
edition = "2024"

[dependencies]
benchcorpus = { path = "../../generated/bench/rust" }
serialize = { package = "serialize-official", path = "../../../serialize.rs" }
